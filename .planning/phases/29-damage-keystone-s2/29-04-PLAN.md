---
phase: 29-damage-keystone-s2
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - server/death_mob.go
  - server/combat_mob.go
  - level/loot/context.go
  - level/loot/condition.go
  - level/loot/entity_loot_test.go
  - server/death_mob_test.go
autonomous: true
requirements: [MOB-SUB-01]

must_haves:
  truths:
    - "A mob whose health reaches 0 is REMOVED from its owning region store and a death-status broadcast is sent (the hard deliverable)."
    - "level/loot.Roll over minecraft:entities/pig parses and rolls headless (the entity-table type is accepted; the de-risk runs FIRST)."
    - "The mob's loot table is rolled into Item entities spawned in the OWNER region (regionForEntity, not cur()), via the reused NewItemEntity + loot.Roll pattern."
    - "An XP orb is awarded on death (ExperienceOrb.award) at the jar-verified per-type xpReward, gated by the lastHurtByPlayer rule (or a cited stub if the gate state is not yet tracked)."
    - "If the entity-loot context conditions (entity_properties / killed_by_player) are a large net-new subsystem, they are EITHER implemented (bounded) OR documented-cut with a cited deferral — never silently producing wrong drops."
  artifacts:
    - path: "server/death_mob.go"
      provides: "dieEntity (replaces the Plan-02 seam), dropMobLoot, dropMobExperience, the death broadcast"
      contains: "func (t *TickLoop) dieEntity"
    - path: "level/loot/entity_loot_test.go"
      provides: "Headless entity-table roll de-risk (A4) — runs FIRST"
      contains: "func TestEntityLootRoll"
    - path: "server/death_mob_test.go"
      provides: "death removal + loot spawn + XP orb tests"
      contains: "func TestMobDeath"
  key_links:
    - from: "server/combat_mob.go applyDamageEntity"
      to: "server/death_mob.go dieEntity"
      via: "if e.health <= 0 { t.dieEntity(e, src) }"
      pattern: "dieEntity"
    - from: "server/death_mob.go dropMobLoot"
      to: "level/loot.Roll"
      via: "LoadTable(minecraft:entities/<name>) -> Roll -> NewItemEntity -> regionForEntity(e).entities.add"
      pattern: "loot\\.(Roll|LoadTable)"
    - from: "server/death_mob.go dieEntity"
      to: "owner region store removal + RemoveEntities broadcast"
      via: "regionForEntity(e).entities remove + tracker despawn"
      pattern: "regionForEntity"
---

<objective>
Complete the keystone with mob death: fill the dieEntity seam Plan 02 left. Port LivingEntity.die 1:1 — death removal (the HARD deliverable), the death-status broadcast, the loot roll into Item entities, and the XP orb. The locked CONTEXT decision (Grey Area 2) chose the BROADER scope (death removal + loot + XP). Per the scope note: de-risk the entity-loot path FIRST (a headless loot.Roll over minecraft:entities/pig) — if the entity-loot conditions are a large net-new subsystem, EITHER extend the loot context (bounded) OR document-cut the unhandled conditions with a cited deferral; death REMOVAL is non-negotiable, loot/XP ride with it (MOB-SUB-01).

Purpose: A mob that can take damage but never dies is not the keystone. This closes the observable loop (hit -> health -> death -> drops -> XP).
Output: server/death_mob.go (dieEntity/dropMobLoot/dropMobExperience), the dieEntity call wired in combat_mob.go, any bounded loot-context extension in level/loot/, and the death + entity-loot tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-damage-keystone-s2/29-CONTEXT.md
@.planning/phases/29-damage-keystone-s2/29-RESEARCH.md
@.planning/phases/29-damage-keystone-s2/29-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
The loot evaluator API (level/loot/):
  func LoadTable(id string) (*LootTable, error)                              // embed.go:88 — all 94 entity tables embedded
  func Roll(table *LootTable, seed int64, ctx *LootContext) []component.SlotData  // roll.go:33
  func NewLootContext(seed int64, luck float32) *LootContext                 // context.go — carries rng/luck/Biome/HasTool/Explosion; NO this-entity/attacker/killed_by_player (the A4 gap)
  // condition.go has NO entity_properties / killed_by_player handler today (the gap to extend or cut)

The loot-roll -> Item-entity spawn pattern to MIRROR (server/block_drop.go + chest_loot.go):
  func NewItemEntity(id int32, x, y, z float64, stack component.SlotData) *Entity   // block_drop.go:168 (toss velocity + ITEM metadata + pickupDelay solved)
  func (t *TickLoop) spawnBlockDrop(p *tickPlayer, pos, brokenState)                // block_drop.go:127 (the loot.Roll + per-stack spawn flow; note it uses cur() because it already runs in the owning region)
  // seed: rand.Int64() (math/rand/v2, block_drop.go:137) — event-time draw, OUTSIDE the 500-tick oracle window

Owner-region routing (server/region_transfer.go): regionForEntity(e) (l.56), withRegion (l.100) — the death path runs at the barrier/owner, so use regionForEntity(e).entities.add, NOT cur() (Pitfall 2).

From Plan 02 (server/combat_mob.go): the dieEntity SEAM (the minimal stub) to REPLACE here; applyDamageEntity already calls t.dieEntity(e, src) when health<=0.

The XP orb (data/entity id 49 experience_orb): ExperienceOrb.award(ServerLevel, Vec3, int) is the jar spawn entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): de-risk the entity-loot path FIRST + death test scaffold</name>
  <read_first>
    - level/loot/context.go (LootContext fields — confirm the gap: no this-entity/attacker/killed_by_player), level/loot/condition.go (the implemented condition handlers — confirm entity_properties/killed_by_player are absent), level/loot/parse.go (does it branch on / reject "type":"minecraft:entity"?)
    - level/loot/data/loot_table/entities/pig.json (CONFIRMED present — read its pools/conditions to know exactly what the roll needs)
    - level/loot/embed.go:88 (LoadTable), roll.go:33 (Roll)
    - server/block_drop.go:127-188 (spawnBlockDrop + NewItemEntity — the spawn pattern), chest_loot.go:89-107 (the loot.Roll usage)
    - JAR to javap BEFORE writing the death port: LivingEntity.die -> dropAllDeathLoot -> dropFromLootTable + dropExperience + broadcastEntityEvent(this,3); Mob.getLootTable + getBaseExperienceReward (the xpReward field); Pig.<init> for the pig xpReward value
  </read_first>
  <behavior>
    - TestEntityLootRoll (level/loot — runs FIRST, the A4 de-risk): loot.Roll(LoadTable("minecraft:entities/pig"), seed, NewLootContext(seed,0)) parses and returns >=0 stacks WITHOUT panicking on the "minecraft:entity" table type. If parse rejects the entity type, the parser/context extension is in scope; this test pins the decision.
    - TestMobDeath_Removal (server — the HARD deliverable): a mob taken to health<=0 via applyDamageEntity is removed from its owning region store; a death-status broadcast is queued.
    - TestMobDeath_Loot: dieEntity spawns Item entities for the rolled stacks in the OWNER region (count matches a deterministic seeded roll).
    - TestMobDeath_XP: an XP orb entity is spawned at the jar-verified pig xpReward.
  </behavior>
  <action>
    Create level/loot/entity_loot_test.go FIRST with TestEntityLootRoll — this de-risks Assumption A4 before any death code. Run it; record the outcome (parses cleanly => A4 holds, only the CONDITIONS may need work; rejects => parse.go needs an entity-type branch). Create server/death_mob_test.go with TestMobDeath_Removal / _Loot / _XP using a deterministic seeded roll and the jar-verified xpReward. The server tests reference dieEntity/dropMobLoot/dropMobExperience — red until Tasks 1-3 land. Encode the loot-context-gap DECISION explicitly in the test expectations: if conditions are cut, assert the unconditional pools roll (and document the cut); if extended, assert a condition-gated drop.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go test ./level/loot/ -run TestEntityLootRoll -count=1 2>&1 | tail -15 && CGO_ENABLED=0 go vet ./server/ 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - level/loot/entity_loot_test.go contains TestEntityLootRoll and it RUNS (green or a clear, recorded parse-rejection that scopes Task 2).
    - server/death_mob_test.go contains TestMobDeath_Removal, _Loot, _XP referencing dieEntity/dropMobLoot/dropMobExperience.
    - The A4 outcome (parses vs needs-extension) is recorded in the test file as a comment/decision.
  </acceptance_criteria>
  <done>The entity-loot de-risk (A4) ran FIRST and scoped the conditions decision; the death scaffold exists; server tests red until death lands.</done>
</task>

<task type="auto">
  <name>Task 1: dieEntity — death removal (HARD deliverable) + death broadcast</name>
  <read_first>
    - server/combat_mob.go (Plan 02 — the dieEntity seam stub to REPLACE; applyDamageEntity's health<=0 call site)
    - server/region_transfer.go:56 (regionForEntity), the owner-region entities store API (grep the entities.add/remove/get methods on the store)
    - the tracker despawn / RemoveEntities path (grep tracker.go / entity_events.go for the store-remove broadcast — Assumption A2: confirm removal auto-broadcasts RemoveEntities; if NOT, add the explicit broadcastEntityEvent(this,3) death-status + RemoveEntities send)
    - JAR to javap BEFORE writing: LivingEntity.die — confirm the guard (isRemoved||dead -> return; dead=true), setPose(DYING), broadcastEntityEvent(this,3), and the remove ordering
  </read_first>
  <action>
    Create server/death_mob.go (package server) and REPLACE the Plan-02 dieEntity seam with func (t *TickLoop) dieEntity(e *Entity, src damageSource): guard (if already removed/dead return; set dead=true — add a dead/removed bool to *Entity if one does not exist, value field, snapshot-friendly); call dropMobLoot (Task 2) + dropMobExperience (Task 3) BEFORE removal (vanilla rolls loot in die() before the entity is gone); broadcast death-status 3 (broadcastEntityEvent equivalent — use the existing tracker/entity-event path; if store-remove already broadcasts RemoveEntities, that covers despawn, else send RemoveEntities explicitly per A2); remove the entity from its OWNING region store (regionForEntity(e).entities remove — NOT cur()). Cite LivingEntity.die. Death REMOVAL is the hard requirement — it MUST work even if loot/XP are stubbed.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./server/ && CGO_ENABLED=0 go test ./server/ -run 'TestMobDeath_Removal' -count=1 2>&1 | tail -15 && CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server/death_mob.go contains func (t *TickLoop) dieEntity(e *Entity, src damageSource); the Plan-02 stub is replaced (combat_mob.go's health<=0 branch calls it).
    - dieEntity removes the entity from regionForEntity(e) (grep — not cur()) and broadcasts death-status/RemoveEntities.
    - A dead/removed guard prevents double-death.
    - TestMobDeath_Removal green; TestPluginPigEqualsGoNativePig green.
  </acceptance_criteria>
  <done>dieEntity removes the dead mob from its owner region + broadcasts death; the hard deliverable lands; oracle green.</done>
</task>

<task type="auto">
  <name>Task 2: dropMobLoot — entity loot roll into Item entities (apply the A4 decision)</name>
  <read_first>
    - Task 0 outcome (the recorded A4 decision: parses-clean vs needs-extension; conditions implement vs cut)
    - level/loot/context.go + condition.go + parse.go (extend ONLY if Task 0 proved it necessary)
    - level/loot/data/loot_table/entities/pig.json (the exact pools/conditions)
    - server/block_drop.go:65-188 (the loot.Roll + per-stack NewItemEntity spawn flow), chest_loot.go:98-105
    - .planning/phases/29-damage-keystone-s2/29-PATTERNS.md:114-149 (the death_mob loot pattern + the owner-region routing note + the loot-context-gap handling)
    - JAR to javap BEFORE writing: Mob.getLootTable (Optional + LivingEntity fallback) + dropFromLootTable
  </read_first>
  <action>
    In server/death_mob.go add func (t *TickLoop) dropMobLoot(e *Entity, src damageSource): resolve name := entityLootTableName(e.typ) (the registry name -> "minecraft:entities/pig"; grep for the entity registry-name lookup); tbl, err := loot.LoadTable(name); if err != nil { return } (no table -> no drop, e.g. item/orb entities); seed := rand.Int64() (math/rand/v2, event-time, OUTSIDE the oracle window); for _, stack := range loot.Roll(tbl, seed, loot.NewLootContext(seed, 0)) { if stack.Count <= 0 { continue }; ie := NewItemEntity(t.idAlloc.AllocID(), e.x, e.y+float64(e.height)/2, e.z, stack); t.regionForEntity(e).entities.add(ie) } — OWNER-region routing, NOT cur() (Pitfall 2). APPLY THE TASK-0 A4 DECISION: if the entity-loot conditions (entity_properties / killed_by_player) need handling and Task 0 scoped a BOUNDED extension, add the LootContext fields (this/attacker/killedByPlayer) + the condition handlers in level/loot/ (cite the jar predicate); if Task 0 showed it is a large net-new subsystem, DOCUMENT-CUT the unhandled conditions (roll the unconditional pools, cite the deferral in a comment + the SUMMARY) — never silently produce wrong drops. Cite dropFromLootTable.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./level/loot/ ./server/ -run 'EntityLootRoll|MobDeath_Loot' -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dropMobLoot calls loot.LoadTable + loot.Roll and spawns via NewItemEntity into t.regionForEntity(e).entities (grep — not cur()).
    - The A4 decision is APPLIED and visible: either the level/loot extension exists (new context fields + condition handlers, cited) OR a cited document-cut comment names the deferred conditions.
    - seed is a server-generated rand.Int64() (not client-supplied).
    - TestMobDeath_Loot + TestEntityLootRoll green.
  </acceptance_criteria>
  <done>dropMobLoot rolls the entity table into owner-region Item entities; the loot-context gap is resolved explicitly (extend or cited cut).</done>
</task>

<task type="auto">
  <name>Task 3: dropMobExperience — XP orb on death + final phase gate</name>
  <read_first>
    - server/death_mob.go (dieEntity — the call site, after loot)
    - data/entity (confirm experience_orb id 49 + the AddEntity spawn path; grep how other entities are spawned + AddEntity-broadcast)
    - server/block_drop.go (the entity spawn + tracker broadcast pattern to reuse for the orb)
    - .planning/phases/29-damage-keystone-s2/29-VALIDATION.md (the standing gates + the Docker -race command)
    - JAR to javap BEFORE writing: LivingEntity.dropExperience (the lastHurtByPlayer gate -> ExperienceOrb.award) + getExperienceReward (Mob.getBaseExperienceReward = xpReward) + Pig.<init> (the pig xpReward value)
  </read_first>
  <action>
    In server/death_mob.go add func (t *TickLoop) dropMobExperience(e *Entity, src damageSource): port LivingEntity.dropExperience — the gate is "recently hurt by a player" (lastHurtByPlayerMemoryTime > 0). If that gate-state is not yet tracked on *Entity, use a cited stub: gate on src being a player-attack source (src.is("is_player_attack") or src.typeTag==damageTypePlayerAttack) as the v1 proxy, documenting that the full lastHurtByPlayerMemoryTime window is deferred — never bake the value away, structure it to become a real read later. Compute the reward from the mob's xpReward (javap-verified per-type; for the pig use the Pig.<init> value) via getExperienceReward; spawn an experience_orb entity (id 49) at the mob position and broadcast its AddEntity (reuse the block_drop spawn+broadcast pattern), routed through regionForEntity(e) — NOT cur(). If ExperienceOrb has no spawn helper yet, add a minimal one at the vanilla value (cited stub), structured to become the real ExperienceOrb.award later. Cite dropExperience + getExperienceReward + Pig.<init>. Then run the FULL phase gate (CGO=0 suite + Docker -race + the math/rand grep gate + the oracle).
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test ./server/ ./level/loot/ ./data/tag/ -count=1 2>&1 | tail -25 && CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig -count=1 2>&1 | tail -5 && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ ./level/loot/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dropMobExperience spawns an experience_orb (id 49) at the jar-verified xpReward, gated by a player-attack source (real or cited-stub gate), routed via regionForEntity(e).
    - Any stubbed value (xpReward fallback / award helper / lastHurtByPlayer gate) equals the vanilla default and is structured to become a real read later (CLAUDE.md mandate — never bake away).
    - TestMobDeath_XP green; full CGO=0 suite (./server/ ./level/loot/ ./data/tag/) green.
    - TestPluginPigEqualsGoNativePig GREEN.
    - Docker -race over ./server/ ./level/loot/ CLEAN; go list -deps ./... | grep -i gopy EMPTY.
  </acceptance_criteria>
  <done>An XP orb is awarded on a player-kill at the jar value; the full phase gate is green (CGO=0 suite + Docker -race + oracle); CGO=0 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| death event -> loot seed | The loot/XP seed is server-generated; never client-supplied. |
| death apply -> region store | Removal + Item/orb spawn route through the OWNER region, not the actor's cur(). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Elevation/Tampering | death-loot seed | mitigate | The loot/XP seed is a server-generated rand.Int64(), never client-supplied (same discipline as chest LootTableSeed, T-20-05). |
| T-29-02 | Tampering | death removal + drop spawn region routing | mitigate | dieEntity/dropMobLoot/dropMobExperience route through regionForEntity(e) (owner), not cur(); death runs at the barrier/owner. Docker -race + strictRegion (the Plan-03 gate + the Task-3 -race) prove no cross-goroutine store mutation. |
| T-29-08 | DoS | double-death / removal of an already-gone entity | accept (guarded) | The dead/removed guard in dieEntity prevents double-removal; an already-gone entity is a no-op (defensive), low risk. |

No client-facing input crosses in this plan beyond the (already reach/region-gated, Plan 03) hit that triggered death.
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... && go vet ./... clean.
- CGO_ENABLED=0 go test ./server/ ./level/loot/ ./data/tag/ green (death removal + loot + XP + entity-loot roll + tags).
- TestPluginPigEqualsGoNativePig GREEN (death is event-driven; the oracle pig is never killed in the 500-tick window).
- Docker -race over ./server/ ./level/loot/ CLEAN.
- go list -deps ./... | grep -i gopy EMPTY; every ported function cites its jar method; any stub equals the vanilla default and is structured to become a real read.
</verification>

<success_criteria>
- MOB-SUB-01 closed: a mob at health<=0 is removed + death-broadcast (hard deliverable), drops its loot table into owner-region Item entities, and awards an XP orb on a player-kill at the jar value.
- The entity-loot context gap (A4) is resolved explicitly (de-risked first; extended OR cited-cut).
- 1:1 javap-verified; CGO=0; Docker -race clean; pig oracle green.
</success_criteria>

<output>
After completion, create .planning/phases/29-damage-keystone-s2/29-04-SUMMARY.md.
</output>
