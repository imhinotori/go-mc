---
phase: 16-village-jigsaw-structures-gate
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - world/structure/phase16_acceptance_test.go
  - world/village_pipeline_test.go
autonomous: false
requirements: [STRUCT-06]

must_haves:
  truths:
    - "Every Phase-16 success criterion is an automated assertion: the .nbt template round-trips, the jigsaw Placer terminates + bounds (depth/distance/collision), a village places (a deterministic fingerprint for a known seed), cross-chunk idempotence (a village spanning chunks), the 5x5 determinism + emit-once STILL green with villages placing"
    - "The full structures acceptance asserts ALL v2 structures place in one pass: desert pyramid + jungle temple + igloo + swamp hut + mineshaft + stronghold + the 5 village biome variants, each at its algorithm-derived chunk, deterministic, cross-chunk idempotent"
    - "A real vanilla 26.2 client (protocol 776) exploring the world FINDS, deterministic per seed: the 5 village biome variants (plains/desert/savanna/snowy/taiga houses, paths, farms, wells, lamps — NO villagers/loot, the v3 deferral), the temples, mineshafts underground, strongholds deep underground with the end portal room"
    - "The visual gate is the TERMINAL task of the final plan: autonomous:false, blocking, with a clear what-to-see + how-to-find-a-village checklist (derive a village chunk from the placement math for the fixed seed, or describe walking to one)"
  artifacts:
    - path: world/structure/phase16_acceptance_test.go
      provides: "TestPhase16Acceptance — every STRUCT-05/06 criterion + all-structures-place as algorithm-derived assertions"
      min_lines: 150
    - path: world/village_pipeline_test.go
      provides: "the real-pipeline village placement + cross-chunk idempotence (router + GetBiome + STARTS + REFERENCES + PLACE)"
      min_lines: 80
  key_links:
    - from: world/structure/phase16_acceptance_test.go
      to: world/structure/village.go
      via: "asserts villageStartGen places at the salt-10387312 chunk + fingerprint"
      pattern: "village|Village"
    - from: world/village_pipeline_test.go
      to: world/noisegen.go
      via: "drives the real NoiseGenerator.Decorate pipeline and asserts a village appears"
      pattern: "Decorate|NoiseGenerator"
---

<objective>
Close milestone v2: the full automated structures acceptance (ALL v2 structures place) + THE `autonomous:false` real-client VISUAL GATE (STRUCT-06). This is the FINAL task of the FINAL phase — the gate that ships v2.

Purpose: Before the human gate, prove everything automatically — the .nbt template round-trips, the Placer terminates + bounds, villages place deterministically per biome variant, cross-chunk idempotence holds, and the 5x5 determinism / emit-once gates stay green WITH villages live. Then the visual gate confirms what no automated test can: a real vanilla 26.2 client sees vanilla-positioned, structurally-correct structures of every type, and the same seed reproduces the same world.

Output: TestPhase16Acceptance (every STRUCT-05/06 criterion + all-structures-place), the real-pipeline village test, and the human-verified visual gate that closes v2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# 16-01 + 16-02 deliver the template system + the Placer + the village StartGenerator.
@.planning/phases/16-village-jigsaw-structures-gate/16-01-PLAN.md
@.planning/phases/16-village-jigsaw-structures-gate/16-02-PLAN.md

# The acceptance patterns to mirror (algorithm-derived assertions, all-structures-place,
# cross-chunk idempotence, real-pipeline placement, the fixed surface/biome oracle).
@.planning/phases/15-mineshaft-stronghold/15-03-SUMMARY.md
@.planning/phases/14-structure-pipeline-temples/14-03-SUMMARY.md

# The Phase-13 visual gate is the precedent for the autonomous:false human-verify task
# shape (what-to-see + how-to-find checklist, no capture-diff — worldgen adds no wire surface).
# See ROADMAP Phase 13 plan 13-04 (visual gate APPROVED).

<interfaces>
<!-- The existing acceptance + pipeline test entry points to extend / mirror: -->
```go
// world/structure/phase15_acceptance_test.go: TestPhase15Acceptance — every
//   STRUCT-03/04 criterion as an algorithm-derived assertion + a real-pipeline
//   placement per structure. Mirror its shape for Phase 16.
// world/structure/acceptance_test.go: TestStructuresPipelineAcceptance /
//   TestStructuresStartsPure / TestStructuresCrossChunkIdempotent (14-03, the 4 temples).
// world/ tests: TestDecorationReorderIdentical (5x5 reorder), TestEmitOnce —
//   must STAY green with villages placing.
```
From world/structure (16-02): villageStartGen + the village salt 10387312 + the 5 biome variants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the full structures acceptance (all structures place, every criterion)</name>
  <files>world/structure/phase16_acceptance_test.go, world/village_pipeline_test.go</files>
  <behavior>
    - TestPhase16Acceptance (mirroring TestPhase15Acceptance) maps EVERY Phase-16 success criterion to an algorithm-derived assertion:
      1. .nbt template round-trip: parse a known village .nbt -> expected size + palette + the placed-block fingerprint (re-asserting 16-01's contract at the acceptance level).
      2. The Placer terminates + bounds: assemble a village and assert a bounded piece count + no two pieces' boxes overlap (the VoxelShape contract) + the max-distance-from-center holds (every piece box within 80 of center) + maxDepth not exceeded.
      3. A village places: the salt-10387312 algorithm-derived expected chunk for a fixed seed + the deterministic village fingerprint (stable across two assemblies).
      4. Cross-chunk idempotence: a village spanning >=2 chunks; the union of per-chunk placeInChunk slices == the whole placement, no double-write.
      5. The biome gate is load-bearing: an out-of-biome origin -> NO village start.
    - TestAllStructuresPlace: in ONE pass over a small region, assert each v2 structure type appears at its algorithm-derived chunk with its expected fingerprint: desert pyramid + jungle temple + igloo + swamp hut + mineshaft + stronghold + the 5 village variants. (Reuse the per-structure expected-chunk derivations from the 14/15 acceptances + the new village one.)
    - world/village_pipeline_test.go: TestVillagePlacesInPipeline + TestVillageCrossChunkIdempotent drive the REAL NoiseGenerator.Decorate pipeline (router + GetBiome + STARTS + REFERENCES + PLACE) for a seed whose village lands in a real biome, asserting village blocks appear in the chunk + the overlapping neighbor gets its slice (mirroring TestDesertPyramidPlacesInPipeline).
    - The 5x5 reorder-determinism (TestDecorationReorderIdentical) + emit-once (TestEmitOnce) STAY byte-identical WITH villages placing (run them as part of the verify, do not modify them).
  </behavior>
  <action>
Create world/structure/phase16_acceptance_test.go with TestPhase16Acceptance (the 5 criteria above) + TestAllStructuresPlace (every v2 structure in one pass). Use the fixed surface/biome oracle the 14-02/15-03 tests established (router-independent, stable) for the fingerprints; derive every expected chunk FROM the placement algorithm (getPotentialStructureChunk per salt), never eyeballed.

Create world/village_pipeline_test.go with TestVillagePlacesInPipeline + TestVillageCrossChunkIdempotent driving the real Decorate pipeline (mirror TestDesertPyramidPlacesInPipeline / TestDesertPyramidCrossChunkIdempotent).

Run the determinism gates (TestDecorationReorderIdentical + TestEmitOnce) to confirm they stay green with villages live. Document the Docker -race run uses `-timeout 1800s` (15-03 finding: the ./world suite ~834s with structures live — a timeout dump, NOT a race).

This task is TEST-ONLY (no production code change beyond what 16-01/16-02 landed). If a test surfaces a real bug, fix it in the owning file under Rule 1 and document it.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestPhase16Acceptance|TestAllStructuresPlace' -count=1 && go test ./world/ -run 'TestVillage|TestDecorationReorderIdentical|TestEmitOnce' -count=1</automated>
  </verify>
  <done>TestPhase16Acceptance asserts all 5 criteria; TestAllStructuresPlace asserts every v2 structure places at its algorithm-derived chunk; the real-pipeline village + cross-chunk idempotence pass; the 5x5 reorder + emit-once stay byte-identical with villages live; Docker -race CLEAN on golang:1.26 with -timeout 1800s on ./world; go build + CGO_ENABLED=0 build + vet clean; go.mod/go.sum UNCHANGED; no encoder/packet/chunk-wire/golden file touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: THE structures VISUAL GATE (the v2-closing gate)</name>
  <what-built>
The full v2 structure suite generates in vanilla positions, deterministic per seed: the data-driven village jigsaw (5 biome variants via the .nbt/template_pool/Placer system, 16-01+16-02), plus the Phase-14/15 structures (desert pyramid / jungle temple / igloo / swamp hut, mineshafts, strongholds). Worldgen adds NO new wire surface (the chunk format was sealed in Phase 4), so this gate is VISUAL determinism against a real client, not a capture-diff. Loot/villagers are the documented v3 deferral — structures are visually + structurally complete without them.
  </what-built>
  <how-to-verify>
1. Start the Sulfur server on a FIXED seed (use the seed the acceptance test pins for a known village — the executor will print "VISUAL GATE seed: <N>, village at chunk (<cx>,<cz>) ≈ world (<wx>,<wz>)" derived from the salt-10387312 placement math, OR a seed with a village near spawn). Note the seed.
2. Connect with an UNMODIFIED vanilla 26.2 (protocol 776) client.
3. Travel to the printed village coordinates (use creative/spectator flight or `/tp` to the world (wx,wz) the gate prints). Confirm a VILLAGE is there: houses, paths, a well, farms, lamp posts — structurally coherent, matching the biome variant (plains village = oak/cobble houses; desert = sandstone; savanna = acacia; snowy = spruce/snow; taiga = spruce). NO villagers and EMPTY/absent chest loot are EXPECTED (v3 deferral) — verify the BUILDINGS, not the inhabitants.
4. Confirm the OTHER structures (explore or /tp to their printed coords): a desert pyramid (sandstone, the terracotta mosaic floor), a jungle temple / igloo / swamp hut in their biomes, a mineshaft underground (wooden supports, rails, cobwebs), a stronghold deep underground (stone-brick corridors + the end portal room with the 12-frame end_portal_frame ring + the silverfish spawner). The gate PASSES on the village + at least one confirmation per prior-tier structure.
5. DETERMINISM: regenerate the world from the SAME seed (delete the region files / use a fresh world dir, same seed) and confirm the SAME structures appear in the SAME positions/shapes — the village, the pyramid, the stronghold all reproduce.
  </how-to-verify>
  <resume-signal>Type "approved" if the village (5 variants reachable) + the prior-tier structures generate in vanilla positions and reproduce on the same seed; otherwise describe what is missing/wrong (which structure, which biome variant, what looked off) and the gap-closure pass will address it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated chunk → vanilla client | The client renders the worldgen output; no new wire surface (chunk format sealed Phase 4). The gate verifies VISUAL correctness, not protocol. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | per-seed determinism | mitigate | The visual gate's step 5 re-generates the same seed and confirms identical structures; the automated TestPhase16Acceptance + the 5x5 reorder/emit-once gates pin determinism mechanically before the human gate. |
| T-16-07 | Information | this phase adds no wire surface | accept | Worldgen output flows through the Phase-4-sealed chunk format; no new packet/encoder is touched (grep-confirmed in the acceptance verify). No new attack surface. |
</threat_model>

<verification>
- TestPhase16Acceptance (every STRUCT-05/06 criterion) + TestAllStructuresPlace (every v2 structure) + the real-pipeline village tests pass.
- The 5x5 reorder + emit-once stay byte-identical with villages live.
- Docker -race CLEAN on golang:1.26 with `-timeout 1800s` on ./world (15-03 finding).
- go build ./... + CGO_ENABLED=0 go build ./... + go vet ./world/... clean; go.mod/go.sum UNCHANGED; no encoder/packet/chunk-wire/golden file touched.
- THE VISUAL GATE: human-approved — a real vanilla 26.2 client finds vanilla-positioned villages (5 variants) + temples + mineshafts + strongholds, reproducible per seed.
</verification>

<success_criteria>
STRUCT-06 is delivered and milestone v2 is CLOSED: a real client exploring the world finds vanilla-positioned, structurally-correct mineshafts, desert pyramids/jungle temples/igloos/swamp huts, strongholds, and villages — deterministic per seed (VISUAL GATE, human-verified, no capture-diff). All Phase-16 success criteria map to a task; STRUCT-05 (16-01+16-02) + STRUCT-06 (this plan) complete.
</success_criteria>

<output>
After completion, create `.planning/phases/16-village-jigsaw-structures-gate/16-03-SUMMARY.md`.
</output>
