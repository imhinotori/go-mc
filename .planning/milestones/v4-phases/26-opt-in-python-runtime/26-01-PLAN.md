---
phase: 26-opt-in-python-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - plugin/python/runtime_stub.go
  - plugin/python/runtime_python.go
  - plugin/python/doc.go
  - plugin/python/runtime_stub_test.go
  - plugin/python/runtime_python_test.go
  - plugin/host/runtime.go
  - plugin/host/manager.go
  - plugin/host/runtime_routing_test.go
  - .github/workflows/python.md
autonomous: true
requirements: [PLUGIN-06]

must_haves:
  truths:
    - "CGO_ENABLED=0 go build ./... succeeds and the default import graph carries ZERO gopython / import \"C\""
    - "plugin/python compiles in BOTH builds: the !python stub (cgo-free) and the python impl (gopy) export an identical surface"
    - "gopy is pinned to a python3.14 BRANCH COMMIT as gopython.xyz/py/v14 (NOT /py/v3) and is reachable ONLY behind //go:build python"
    - "a manifest runtime=\"python\" plugin is dispatched to the python lane WITH the tag and skipped gracefully (logged, scan continues) WITHOUT it"
    - "plugin/host stays cgo-free without -tags python (the runtime-routing interface has a no-op python impl on the default build)"
  artifacts:
    - path: "plugin/python/runtime_stub.go"
      provides: "the //go:build !python cgo-free stub (errNotBuilt, Available()==false, same exported surface)"
      contains: "//go:build !python"
    - path: "plugin/python/runtime_python.go"
      provides: "the //go:build python real gopy impl (imports gopython.xyz/py/v14)"
      contains: "//go:build python"
    - path: "plugin/host/runtime.go"
      provides: "the runtime-routing interface + a registration seam so the python impl plugs in behind the tag; default build has a no-op python runtime"
      contains: "interface"
    - path: "plugin/host/manager.go"
      provides: "LoadDir python branch: runtime==\"python\" loads via the routing interface (tag) or logs+skips (default)"
      contains: "python"
  key_links:
    - from: "plugin/host/manager.go (LoadDir)"
      to: "the runtime-routing interface"
      via: "man.Runtime == \"python\" branch (replaces the bare `continue`)"
      pattern: "Runtime ==.*python"
    - from: "plugin/python/runtime_python.go"
      to: "gopython.xyz/py/v14"
      via: "import behind //go:build python"
      pattern: "gopython.xyz/py/v14"
---

<objective>
Land the build-tag isolation primitive and the runtime-routing seam: a new `plugin/python` package with a `//go:build python` real gopy impl + a `//go:build !python` cgo-free stub exporting an identical surface, the gopy dependency pinned to a python3.14 branch commit as `gopython.xyz/py/v14` (behind the tag only), and a runtime-routing interface in `plugin/host` so `LoadDir` dispatches a `runtime="python"` plugin to the python lane WITH the tag and skips it gracefully WITHOUT it.

Purpose: This is THE safety gate of the whole phase. If gopy leaks into the default import graph, `CGO_ENABLED=0 go build ./...` is broken and the static-binary value prop is gone. Everything in Wave 2/3 hangs off this isolation. It must be proven by the default-build no-gopy-in-graph grep before any off-tick code is written.
Output: `plugin/python/{runtime_stub.go, runtime_python.go}`, the routing interface in `plugin/host/runtime.go`, the `LoadDir` python branch, the pinned `gopython.xyz/py/v14` in go.mod (behind the tag), and the two-build matrix gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-opt-in-python-runtime/26-RESEARCH.md
@.planning/phases/26-opt-in-python-runtime/26-CONTEXT.md
@.planning/phases/26-opt-in-python-runtime/26-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- The runtime-neutral host the python branch fills. Use these directly — no exploration needed. -->

From plugin/host/manager.go — the EXISTING skip the python branch replaces (LoadDirWith):
```go
if man.Runtime != "starlark" {
    continue // Phase 26 handles runtime=="python"; skip for the default build
}
```
- `Manager.New() *Manager`, `Manager.LoadDir(root) error`, `Manager.LoadDirWith(root, extra starlark.StringDict) error`.
- `Hook struct { fn starlark.Callable; plugin string }` — host imports plugin/starlark + go.starlark.net. The python lane must NOT add a gopy field to this struct on the default build.
- `loadedPlugin struct { manifest Manifest; loaded *starlarkpkg.LoadedPlugin }`.

From plugin/host/manifest.go:
```go
type Manifest struct {
    Name string; Version string; Entrypoint string; Runtime string; Capabilities []string
}
```
- `man.Runtime` is the selector ("starlark" | "python"). `man.Entrypoint` is filepath.Clean'd + traversal-guarded.

From 26-RESEARCH.md (VERIFIED gopy facts on the python3.14 branch):
```go
// //go:build python
import "gopython.xyz/py/v14"   // cgo preamble: #cgo pkg-config: python-3.14-embed libffi
// py.InitAndLock() *py.Lock  (Py_Initialize + GIL + LockOSThread, returns LOCKED)
// py.NewLock() *py.Lock ; lock.Unlock()    (GIL acquire/release + LockOSThread pin)
// py.RunFile(path, py.FileInput, nil, nil) (*py.Object, error)
// fn.Base().CallGoArgs(args ...any) (*py.Object, error) ; res.Decref()
```
Module-path correction (LOCKED, CONTEXT decision 1): the path is `gopython.xyz/py/v14` (the python3.14 branch's go.mod), NOT `/py/v3` (the old 3.11 line). PIN a branch COMMIT, not the moving `v14.0.0-alpha.0` tag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The build-tag stub/impl pair (the isolation primitive)</name>
  <files>plugin/python/runtime_stub.go, plugin/python/runtime_python.go, plugin/python/doc.go, plugin/python/runtime_stub_test.go</files>
  <action>
read_first: 26-RESEARCH.md (Pattern 1 + Code Examples "the build-tag stub + impl pair"), 26-CONTEXT.md (decision 2 "build-tag isolation"), plugin/host/manager.go (the runtime-neutral host).

Create the `plugin/python` package with a build-tag stub/impl pair that exports a BYTE-IDENTICAL surface in both builds (per RESEARCH Pitfall 7 — stub/impl drift breaks one build).

`plugin/python/doc.go` (NO build tag): the package doc — "the opt-in CPython runtime, gated behind //go:build python; the default build compiles only the stub and stays CGO=0."

`plugin/python/runtime_stub.go` (`//go:build !python`, cgo-free, NO gopy import):
- `var ErrNotBuilt = errors.New("python: runtime not built in this binary (rebuild with -tags python)")`
- `func Available() bool { return false }`
- `type Runtime struct{}` with `func (r *Runtime) Close() {}`
- `func Load(entrypoint string) (*Runtime, error) { return nil, ErrNotBuilt }`
- `func (r *Runtime) CallHook(event string, args ...any) error { return ErrNotBuilt }`
Keep the exported set EXACTLY matching the impl below (Available, Runtime, Load, Runtime.Close, Runtime.CallHook). This file MUST contain no `import "C"` and no `gopython` import.

`plugin/python/runtime_python.go` (`//go:build python`, imports `gopython.xyz/py/v14`):
- mirror the EXACT exported surface (Available()==true; Runtime{ hooks map[string]*py.Object }; Load; Close; CallHook).
- `Load(entrypoint)` calls `py.InitAndLock()` ONCE (process-global, guarded by a package var so a second Load re-Locks instead of re-init), `defer lock.Unlock()`, runs `py.RunFile(entrypoint, py.FileInput, nil, nil)` capturing registered hooks (a `register(event, fn)` builtin exposed to the .py captures into rt.hooks). A stub body that returns "not yet implemented" for the hook-capture detail is acceptable HERE (Wave 2 fleshes the register-capture + CallHook GIL path); the load/init/run + the build MUST be real so `go build -tags python ./...` links libpython.
- `CallHook` acquires `py.NewLock()`, `defer lock.Unlock()`, looks up rt.hooks[event], `fn.Base().CallGoArgs(args...)`, Decref the result. (Wave 2 exercises this off-tick; this task only needs it to COMPILE behind the tag.)
Cite each gopy call against 26-RESEARCH.md Code Examples.

`plugin/python/runtime_stub_test.go` (`//go:build !python`): assert `Available() == false`, `Load("x")` returns ErrNotBuilt, `(&Runtime{}).CallHook("e")` returns ErrNotBuilt. This is the DEFAULT-build test (no libpython needed).

THE DEFAULT-BUILD GATE (run after this task): `CGO_ENABLED=0 go build ./...` exit 0 AND `go list -deps ./... | grep -i gopython` prints NOTHING (the impl file is excluded without the tag).
  </action>
  <verify>
<automated>cd /d/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./plugin/python/ && CGO_ENABLED=0 go test ./plugin/python/ -run TestStub && test -z "$(go list -deps ./... | grep -i gopython)" && echo GATE-OK</automated>
  </verify>
  <done>CGO_ENABLED=0 go build ./... exits 0; `go list -deps ./...` shows ZERO gopython; the stub test passes; runtime_stub.go contains `//go:build !python` and runtime_python.go contains `//go:build python`; the exported surface (Available/Runtime/Load/Close/CallHook) is identical in both files.</done>
</task>

<task type="auto">
  <name>Task 2: Pin gopy @ python3.14 branch commit (behind the tag) + the -tags python build</name>
  <files>go.mod, go.sum, plugin/python/runtime_python_test.go, .github/workflows/python.md</files>
  <action>
read_first: 26-RESEARCH.md (Standard Stack "Pin mechanism" + "Version verification" + Pitfall 4), 26-CONTEXT.md (decision 1 "Dependency — PINNED TO A BRANCH COMMIT"), go.mod (the existing module graph).

Add the gopy dependency PINNED to a python3.14 BRANCH COMMIT (CONTEXT decision 1 — NOT the moving `v14.0.0-alpha.0` tag, NOT a fork):
- First VERIFY the branch + path: `git ls-remote --heads https://github.com/qur/gopy | grep python3.14` (the branch exists) and confirm the module path is `gopython.xyz/py/v14` (RESEARCH: the python3.14 branch's go.mod declares this).
- `go get gopython.xyz/py/v14@<python3.14-branch-commit-sha>` to pin a pseudo-version at the branch HEAD commit (RESEARCH names HEAD `b0bdc04a384b443df2279101ac335b5808727793` as of 2026-06-28 — re-resolve at execution and record the exact commit in the SUMMARY). `go mod tidy`.
- Because the only importer is `plugin/python/runtime_python.go` behind `//go:build python`, the dep is in go.mod but NOT in the default import graph. CONFIRM: `CGO_ENABLED=0 go build ./...` still clean + `go list -deps ./...` still has ZERO gopython (adding to go.mod must not change the default graph — only a tagged build pulls it).

`plugin/python/runtime_python_test.go` (`//go:build python`): a load smoke test — write a tiny .py to a temp dir, `Load` it, assert no error + `Available()==true`. This test runs ONLY on the python3.14 Docker image (libpython present). Mark it clearly as the tagged test.

`.github/workflows/python.md` (a NOTES doc, not a runnable workflow unless CI exists): document the CI matrix per RESEARCH Pitfall 6 — (a) default static `CGO_ENABLED=0 go build ./...` + the no-gopy grep (local, always); (b) default race `CGO_ENABLED=1 go test -race ./...`; (c) NEW `-tags python` build/-race on an image with `python3.14-dev libffi-dev` (`pkg-config --exists python-3.14-embed`). Note the executor likely CANNOT run (c) locally on Windows (no libpython3.14) — it writes the tagged code + tests and documents that (c) runs in the python3.14 Docker image, exactly like the existing -race split.

NOTE for the executor: if `go get` of the alpha branch fails to resolve in this environment, record the exact pin command + the resolved pseudo-version in the SUMMARY and ensure the default build stays green (the dep entry behind the tag must never break CGO=0); the `-tags python` link is gated to the Docker image regardless.
  </action>
  <verify>
<automated>cd /d/ender && CGO_ENABLED=0 go build ./... && test -z "$(go list -deps ./... | grep -i gopython)" && grep -q 'gopython.xyz/py/v14' go.mod && echo PIN-DEFAULT-CLEAN</automated>
  </verify>
  <done>go.mod pins `gopython.xyz/py/v14` at a python3.14 branch commit (pseudo-version, recorded in SUMMARY); the DEFAULT build CGO=0 stays clean with ZERO gopython in the graph; the `-tags python` build links python-3.14-embed (verified on the python3.14 Docker image, documented as manual/CI-image per Pitfall 6); .github/workflows/python.md documents the three-gate matrix.</done>
</task>

<task type="auto">
  <name>Task 3: The runtime-routing interface (host + server stay cgo-free on the default build)</name>
  <files>plugin/host/runtime.go, plugin/host/manager.go, plugin/host/runtime_routing_test.go</files>
  <action>
read_first: 26-RESEARCH.md (Architectural Responsibility Map + "Where the routing branch lives" + Pattern 2), 26-CONTEXT.md (decision 6 "Routing keeps server/plugin-host cgo-free"), plugin/host/manager.go (LoadDirWith, the bare `continue` skip), plugin/host/manifest.go (Manifest.Runtime).

Create a runtime-routing interface in `plugin/host` (NO build tag — cgo-free, lives in the default graph) so the host dispatches a `runtime="python"` plugin WITHOUT importing gopy directly:

`plugin/host/runtime.go` (no tag):
- `type PythonRuntime interface { Load(entrypoint string) (PythonPlugin, error); Available() bool }` and `type PythonPlugin interface { CallHook(event string, args ...any) error; Close() }` — the minimal surface the host needs. These are plain Go interfaces (zero cgo).
- A registration seam: `func (m *Manager) SetPythonRuntime(rt PythonRuntime)` storing it on Manager; a `nil` runtime (default build, nobody registered one) means "python not built in".
- The wiring of the CONCRETE python runtime into this interface lives behind `//go:build python` (a tagged file in `plugin/python` or a tiny tagged adapter the server registers at boot) — so `plugin/host` itself NEVER imports `plugin/python`'s tagged side. The default build registers nothing → the interface is nil → graceful skip.

`plugin/host/manager.go` — replace the bare `if man.Runtime != "starlark" { continue }` with a real branch:
- `man.Runtime == "starlark"` → the EXISTING path (unchanged).
- `man.Runtime == "python"` → if `m.pythonRuntime != nil && m.pythonRuntime.Available()`, Load the plugin through it (store the returned PythonPlugin on loadedPlugin so the off-tick lane in Wave 2 can dispatch to it); ELSE `log.Printf("plugin %q: python runtime not built in this binary; skipping", man.Name)` and `continue` (graceful, scan continues — RESEARCH "skipped/graceful-error without the tag").
- any other runtime → keep erroring/skip as before (an unknown runtime should not silently load).
Extend `loadedPlugin` with an optional `python PythonPlugin` field (nil for starlark plugins) — a plain interface, no cgo.

`plugin/host/runtime_routing_test.go` (no tag, DEFAULT build): `TestRuntimeRoutingDefaultBuild` — a Manager with NO python runtime registered, loading a testdata dir containing a `runtime="python"` manifest, SKIPS it gracefully (no error, PluginCount unchanged, scan completes). Add a fake PythonRuntime (a pure-Go test double implementing the interface) and assert that WHEN registered, the python manifest routes to it (Load called). This proves the routing WITHOUT any gopy.

THE GATE for this task: `CGO_ENABLED=0 go test ./plugin/host/ -run TestRuntimeRoutingDefaultBuild` passes AND `go list -deps ./plugin/host/` has ZERO gopython (the host stays cgo-free).
  </action>
  <verify>
<automated>cd /d/ender && CGO_ENABLED=0 go test ./plugin/host/ -run TestRuntimeRouting && test -z "$(go list -deps ./plugin/host/ | grep -i gopython)" && CGO_ENABLED=0 go build ./... && echo ROUTING-OK</automated>
  </verify>
  <done>`man.Runtime == "python"` routes to the PythonRuntime interface (loaded with the tag, gracefully skipped+logged without it); plugin/host imports ZERO gopy (the concrete impl plugs in behind the tag via SetPythonRuntime); TestRuntimeRoutingDefaultBuild passes on the CGO=0 default build; the default build stays clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-tag → default import graph | The instant gopy reaches an untagged file, CGO=0 is broken — the #1 boundary |
| operator → libpython 3.14 alpha pin | An opt-in operator links an alpha CPython; supply-chain surface for the tagged build only |
| manifest runtime field → loader | A `runtime="python"` manifest selects which lane loads the plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering/Elevation | gopy leaking into the default build graph | mitigate | ALL gopy behind `//go:build python`; the routing interface in plugin/host is plain-Go (no gopy); CI grep gate `go list -deps ./... \| grep -i gopython` EMPTY on every task; the concrete impl plugs in via SetPythonRuntime behind the tag |
| T-26-04 | Tampering (supply chain) | libpython3.14 alpha pin (gopython.xyz/py/v14) | mitigate | PIN a specific branch COMMIT (pseudo-version), not the moving alpha tag; gated behind the tag so only opt-in operators are exposed; recorded in go.sum + the SUMMARY |
| T-26-05 | Spoofing | an unknown manifest runtime value | mitigate | LoadDir routes starlark/python explicitly; an unknown runtime is not silently loaded (keeps the Phase-22 "load loudly or skip" discipline) |
| T-26-06 | DoS | a python manifest on a default (no-tag) build | accept | graceful skip+log (RESEARCH); the operator who wanted python rebuilds with -tags python — a missing optional runtime is not a server-down condition |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exit 0 (the static default build).
- `go list -deps ./...` has ZERO gopython (the #1 isolation gate).
- `CGO_ENABLED=0 go test ./plugin/python/ -run TestStub` + `CGO_ENABLED=0 go test ./plugin/host/ -run TestRuntimeRouting` pass.
- go.mod pins `gopython.xyz/py/v14` at a python3.14 branch commit.
- (CI/Docker image) `CGO_ENABLED=1 go build -tags python ./...` links python-3.14-embed (documented as the python3.14-image gate per Pitfall 6).
</verification>

<success_criteria>
The build-tag isolation primitive exists and is proven: the default CGO=0 build is static with ZERO gopy in the import graph, the `plugin/python` stub/impl pair compiles in both builds with an identical surface, gopy is pinned to a python3.14 branch commit as `/py/v14` behind the tag, and `LoadDir` routes a `runtime="python"` manifest to the runtime interface (loaded with the tag, gracefully skipped without it) while plugin/host stays cgo-free.
</success_criteria>

<output>
After completion, create `.planning/phases/26-opt-in-python-runtime/26-01-SUMMARY.md` (record the exact pinned gopy pseudo-version/commit + confirm the default-build no-gopy-in-graph gate green).
</output>
