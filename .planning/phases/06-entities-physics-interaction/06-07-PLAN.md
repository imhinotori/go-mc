---
phase: 06-entities-physics-interaction
plan: 07
type: execute
wave: 7
depends_on: [02, 04, 05, 06]
files_modified:
  - server/entity_capture_test.go
  - .planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md
  - .planning/phases/06-entities-physics-interaction/fixtures/vanilla-add-entity.bin
  - .planning/phases/06-entities-physics-interaction/fixtures/vanilla-set-entity-data.bin
  - .planning/phases/06-entities-physics-interaction/fixtures/vanilla-container-set-content.bin
  - .planning/phases/06-entities-physics-interaction/fixtures/vanilla-container-click.bin
autonomous: false
requirements: [ENT-01, ENT-02, ENT-03, ENT-04, ENT-05]
user_setup: []

must_haves:
  truths:
    - "A capture-diff test (server/entity_capture_test.go) byte-compares Sulfur's ClientboundAddEntity (a MOVING entity, to seal the Vec3.LP short scaling), ClientboundSetEntityData (the indexed-entry + 0xFF framing), and ClientboundContainerSetContent (a known inventory) against real vanilla 26.2 server packets, AND validates the ServerboundContainerClick HashedStack decode against a real vanilla click capture"
    - "The vanilla golden packets are captured and committed as fixtures so CI byte-diffs without booting Java every run; the test skips gracefully with a clear message if a fixture is absent (native CI stays green; the capture is the gate)"
    - "06-CAPTURE-DIFF.md records the capture method + the per-field diff (AddEntity LP short scaling, SetEntityData 0xFF terminator framing, ContainerSetContent ItemStack component encoding, ContainerClick HashedStack body) and RESOLVES the Open Questions (LP short scaling factor, whether a v1 entity needs non-default metadata to render, HashedStack decode depth, physics faithfulness scope)"
    - "If a capture reveals a divergence, the fix is applied to the OWNING plan's encoder (server/entity_encode.go, server/slot_encode.go, level/component/types.go) and the affected tests re-run before sign-off"
    - "An unmodified vanilla 26.2 client connects to cmd/sulfur and the FULL interactive loop works: an entity is VISIBLE and moves; placing a block shows it persist (and breaking one removes it) with no ghost; taking damage drops the on-screen health bar; death shows the death screen and respawn returns to a streamed world; no kick/hang — the ENT-01..05 interactive milestone, confirmed by a human"
  artifacts:
    - path: "server/entity_capture_test.go"
      provides: "TestEntityBytesVsVanillaCapture + TestSlotBytesVsVanillaCapture: the authoritative ENT-01/04 byte-diff against vanilla golden fixtures"
      contains: "func TestEntityBytesVsVanillaCapture"
    - path: ".planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md"
      provides: "the vanilla 26.2 capture-diff record + the resolution of the Open Questions + the ENT-01..05 interactive human sign-off"
      contains: "Vec3.LP"
    - path: ".planning/phases/06-entities-physics-interaction/fixtures/vanilla-add-entity.bin"
      provides: "the committed golden vanilla ClientboundAddEntity (moving entity) packet"
      contains: ""
  key_links:
    - from: "server/entity_capture_test.go"
      to: ".planning/phases/06-entities-physics-interaction/fixtures/vanilla-add-entity.bin"
      via: "load the golden vanilla packet and byte-diff Sulfur's encoder output"
      pattern: "vanilla-add-entity"
    - from: "06-CAPTURE-DIFF.md"
      to: "the real vanilla 26.2 interactive session"
      via: "the human confirms entity-visible + place/break + health-bar + death/respawn (ENT-01..05)"
      pattern: "interactive"
---

<objective>
Seal the THREE MEDIUM-confidence Phase-6 wire surfaces against the ONLY authoritative source for proto 776 — a byte-for-byte capture-diff against a real vanilla 26.2 server — and then confirm the ENT-01..05 INTERACTIVE milestone: an unmodified vanilla 26.2 client sees an entity, places/breaks a block (and SEES it), takes damage (and SEES the health bar), and completes a death/respawn loop. This mirrors Phases 4/5: a self-round-trip cannot prove vanilla-correctness for a protocol no spec covers (the wiki documents ≤773; the entity-metadata serializer ids, the Vec3.LP short scaling, the component-slot value codecs, and the 1.21.5 HashedStack are jar-confirmed in SHAPE but their exact bytes drift). The capture-diff is the producer-side proof; the real-client interactive run is the consumer-side proof.

The executor FIRST does the automatable capture/diff work (stand up the cached vanilla jar, capture a MOVING entity's AddEntity+SetEntityData, a ContainerSetContent of a known inventory, and a ContainerClick round-trip; commit golden fixtures; byte-diff Sulfur's encoders/decoders; finalize 06-CAPTURE-DIFF.md resolving the Open Questions; apply any fix to the owning plan and re-run), THEN PAUSES for a BLOCKING human-verify checkpoint: the user runs an unmodified vanilla 26.2 client against cmd/sulfur and confirms entity-visible / place-break / health-bar / death-respawn.

Purpose: ENT-01/04 wire-correctness (the uncertain encoders) and ENT-01..05 (the interactive milestone) are the phase's highest-risk requirements — "the world becomes interactive" is only true when a real client SEES entities, blocks, damage, and respawn. This plan is autonomous:false because the decisive check — a real client interacting with a live world — cannot be self-approved.
Output: server/entity_capture_test.go (the golden byte-diff), the committed vanilla golden .bin fixtures, and a signed-off 06-CAPTURE-DIFF.md resolving the Open Questions. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-entities-physics-interaction/06-RESEARCH.md
@.planning/phases/06-entities-physics-interaction/06-VALIDATION.md
@.planning/phases/06-entities-physics-interaction/06-02-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-04-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-05-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-06-PLAN.md
@.planning/phases/05-player-session-in-world-first-playable/05-03-PLAN.md
@.planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md

<interfaces>
<!-- VERIFIED this session. The capture-diff consumes the encoders from Plans 06-02/06-05. -->

From server/entity_encode.go (Plan 06-02 — the encoders under test):
```go
func encodeAddEntity(e *Entity) pk.Packet       // jar-derived: Vec3.LP short movement + byte angles
func encodeSetEntityData(e *Entity) pk.Packet   // indexed entries + 0xFF terminator
```

From server/slot_encode.go + level/component/types.go (Plan 06-05 — under test):
```go
func containerSetContent(containerId, stateId int32, items []component.SlotData, carried component.SlotData) pk.Packet
func (s *component.SlotData) WriteTo(w io.Writer) (int64, error) // extended component-slot ItemStack
// the HashedStack decoder (server/slot_encode.go) — validated against a real vanilla click capture
```

From server/combat.go (Plan 06-06 — the Respawn confirming diff, optional):
```go
func respawnPacket(dataToKeep byte) pk.Packet // reuses commonPlayerSpawnInfoEncoder (already Phase-5-sealed)
```

From the bot harness + the Phase-4/5 capture method (reuse — VERIFIED present):
```go
// Point the fork bot/ client at the VANILLA server (offline login → config → play), capture the raw bytes
// of ClientboundAddEntity / ClientboundSetEntityData / ClientboundContainerSetContent and a ServerboundContainerClick.
// Alternatively a logging TCP proxy between a vanilla client and the vanilla server.
// The exact method is documented in 05-CAPTURE-DIFF.md / WORLD-CAPTURE-DIFF.md.
```

Environment (VERIFIED available per 06-RESEARCH Environment Availability):
```
temp/cache/26.2-server.jar  (the real vanilla 26.2 server)
temp/cache/26.2-inner.jar   (javap source for any remaining jar-derive)
Java (Zulu/Temurin) 25      (run the vanilla server; javap)
golang:1.26 Docker          (-race, host CGO_ENABLED=0)
PrismLauncher 26.2 client   (the interactive gate, operator-run)
```
</interfaces>

<key_facts>
<!-- VERIFIED against 06-RESEARCH this session. -->
- THE AUTHORITATIVE GATE (the Phase-2/4/5 lesson): a Go self-round-trip CANNOT prove the uncertain encoders are vanilla-correct. The SetEntityData 0xFF framing + serializer codecs, the AddEntity Vec3.LP short scaling, the component-slot ItemStack value codecs, and the ContainerClick HashedStack body are jar-confirmed in SHAPE but MEDIUM-confidence in exact bytes (06-RESEARCH A2/A3/A4/A6, Open Questions 1-3). Only a byte-diff vs the real vanilla 26.2 server proves them. (AddEntity/SetHealth/Respawn framing is HIGH-confidence jar-derived — Respawn reuses the Phase-5-sealed encoder — so they need only a confirming diff, not a risk seal.)
- CAPTURE METHOD (mirror 05-CAPTURE-DIFF.md / WORLD-CAPTURE-DIFF.md): boot temp/cache/26.2-server.jar offline + flat with a fixed seed/spawn; capture (1) a MOVING entity's ClientboundAddEntity + ClientboundSetEntityData — the MOTION is what seals the Vec3.LP short scaling (Open Question 1); spawn a simple visible entity near the player; (2) a ClientboundContainerSetContent of a KNOWN inventory (e.g. set a creative slot) — to seal the component-slot ItemStack encoding; (3) a ServerboundContainerClick round-trip — to seal the HashedStack decode (capture what the vanilla CLIENT sends). Commit each as a golden .bin under fixtures/.
- Sulfur side: produce the same clientbound packets with the same inputs and byte-diff the FRAMING (AddEntity: id/uuid/typeId/Double xyz/LP-short movement/byte angles/VarInt data — esp. the LP short scaling; SetEntityData: the indexed-entry framing + the ALWAYS-present 0xFF terminator; ContainerSetContent: containerId/stateId/ItemStack-list/carried — esp. the count+id+added+removed slot framing). For the ContainerClick, assert Sulfur's DECODER consumes the captured vanilla client bytes without mis-framing (the HashedStack form).
- Test TestEntityBytesVsVanillaCapture + TestSlotBytesVsVanillaCapture: load each fixture, parse it, and assert Sulfur's encoder produces a structurally-matching packet (the LP short scaling; the 0xFF terminator; the slot count+id+added+removed framing). For the ContainerClick, assert Sulfur's HashedStack decoder consumes the captured vanilla bytes cleanly. t.Skip with a message pointing at 06-CAPTURE-DIFF.md if a fixture is absent (native CI stays green; the capture is the gate). Mirror 05-03's TestPlayBytesVsVanillaCapture.
- RESOLVE THE OPEN QUESTIONS IN 06-CAPTURE-DIFF.md from the capture/real-client run:
    1. Vec3.LP short scaling (AddEntity/SetEntityMotion) — confirmed at the byte level by the moving-entity capture.
    2. Whether a v1 entity needs non-default metadata to render — the real-client run is the arbiter: if the entity is INVISIBLE with the 0xFF-only metadata, capture-diff vanilla's SetEntityData for that type and add the required entry; otherwise 0xFF-only stands (A3).
    3. HashedStack decode depth — confirmed by the captured real click decoding cleanly.
    4. Physics faithfulness scope — the real-client run confirms the VISIBLE behaviors (entity lands, player can't clip stone); exact-constant parity is explicitly NOT required for v1 (A1) — record this resolution.
- If ANY divergence is found, fix it in the OWNING plan's encoder (server/entity_encode.go for AddEntity/SetEntityData; server/slot_encode.go + level/component/types.go for the slot/HashedStack), re-run that plan's wire tests + this capture-diff, and record the fix in 06-CAPTURE-DIFF.md before sign-off.
- THE ENT-01..05 REAL-CLIENT INTERACTIVE CHECK (the human-verify): connect an unmodified vanilla 26.2 client (PrismLauncher) to cmd/sulfur and confirm: (a) a spawned entity is VISIBLE and moves; (b) placing a block shows it persist and breaking one removes it, NO ghost / no snap-back on a valid edit (Plan 06-04's reconciliation working end-to-end); (c) taking damage drops the on-screen health bar (Plan 06-06's SetHealth); (d) on death the death screen appears and respawn returns the player to a streamed world (Plan 06-06's Respawn reusing the sealed encoder); (e) NO kick / no "Loading terrain…" hang. This is the decisive ENT-01..05 check; it cannot be self-approved.
- A DAMAGE/ENTITY-SPAWN TRIGGER for the interactive run: ensure cmd/sulfur can SPAWN a visible test entity (Plan 06-02's debug spawn) and APPLY damage (a debug damage path, e.g. a periodic tick of fall/void damage or a debug command) so the operator can SEE the health bar drop + the death/respawn — without these triggers the interactive check has nothing to observe. Provide them behind a clear debug flag/helper if not already present from the implementation plans.
- ZERO new dependencies. Pure stdlib testing + the existing bot/ harness + the cached jar.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/entity_capture_test.go (package server) + the fixtures/ dir + .bin files. Finalize 06-CAPTURE-DIFF.md (it was begun in Plans 06-02/06-05 Task 0). If a fix is needed, make a precise symbol-level edit in the owning encoder and re-run. Read 05-03-PLAN.md's Task 1/2 for the exact capture-diff + human-verify checkpoint shape to mirror.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sulfur entity/slot encoders → vanilla 26.2 client | The client silently mis-renders or rejects a packet whose byte framing diverges from vanilla (entity vanishes, ghost item, click desync). The only authoritative validation is a byte-diff vs the real server + a real-client interactive run. |
| Vanilla golden fixture → Sulfur decoder (the captured ContainerClick) | Sulfur's HashedStack decoder must consume the real vanilla client bytes without mis-framing; the captured click is a trusted committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering (data integrity) | a wire-framing divergence (wrong LP short scaling, missing 0xFF terminator, wrong slot added/removed framing, mis-framed HashedStack) that self-round-trips green but the client rejects/mis-renders | mitigate | Byte-diff against the real vanilla 26.2 server output (the golden fixtures) + the real-client interactive run — the framing assertions (LP short scaling, 0xFF terminator, slot count+id+added+removed, HashedStack body) are exactly the divergences that vanish the entity / ghost the item / desync the click. Any divergence is fixed in the owning plan and re-diffed before sign-off. |
| T-6-17 | Denial of Service | decode of a captured golden packet over-allocating from a length field | accept | The golden fixtures are trusted committed artifacts captured from the real vanilla server (not attacker input); parsing them uses bounded pk.Scan with no unbounded make-before-check. Low risk for trusted test fixtures. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture vanilla 26.2 entity/slot packets + the byte-diff test (automatable prep)</name>
  <files>server/entity_capture_test.go, .planning/phases/06-entities-physics-interaction/fixtures/vanilla-add-entity.bin, .planning/phases/06-entities-physics-interaction/fixtures/vanilla-set-entity-data.bin, .planning/phases/06-entities-physics-interaction/fixtures/vanilla-container-set-content.bin, .planning/phases/06-entities-physics-interaction/fixtures/vanilla-container-click.bin, .planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md</files>
  <read_first>
    - .planning/phases/06-entities-physics-interaction/06-RESEARCH.md — the Capture-diff surfaces, Pitfalls 1-3, the Open Questions, Environment Availability.
    - .planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md + 05-03-PLAN.md — the proven capture method + the diff-test shape to mirror.
    - .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md — the exact server.properties / bot-or-proxy dump / golden .bin commit.
    - server/entity_encode.go + server/slot_encode.go + level/component/types.go — the encoders/decoders under test (Plans 06-02/06-05).
  </read_first>
  <behavior>
    ENT-01/04 (authoritative byte-diff — automatable portion):
    - Capture: boot temp/cache/26.2-server.jar offline + flat + fixed seed; capture a MOVING entity's ClientboundAddEntity + ClientboundSetEntityData, a ClientboundContainerSetContent of a known inventory, and a ServerboundContainerClick; commit each raw packet as a golden .bin under fixtures/.
    - Test TestEntityBytesVsVanillaCapture: load the AddEntity + SetEntityData fixtures; assert the vanilla framing (AddEntity LP short movement scaling, byte angles; SetEntityData indexed entries + the 0xFF terminator) and that Sulfur's encoders produce the SAME framing. Test TestSlotBytesVsVanillaCapture: load the ContainerSetContent fixture; assert the slot count+id+added+removed framing matches; load the ContainerClick fixture; assert Sulfur's HashedStack decoder consumes it without mis-framing. Skip with a clear message if a fixture is absent.
  </behavior>
  <action>
    1. Capture (document every step in 06-CAPTURE-DIFF.md so it is reproducible — mirror 05-CAPTURE-DIFF.md):
       - Start the vanilla server: `java -jar temp/cache/26.2-server.jar` in a temp dir with server.properties = flat level-type, fixed level-seed, online-mode=false, eula accepted. Record the exact server.properties.
       - Capture: point the fork bot/ client at the vanilla server (offline login → config → play) OR a logging proxy; spawn/summon a simple visible entity and MOVE it to capture AddEntity + SetEntityData with non-zero motion (seals the LP scaling); set a creative slot to capture ContainerSetContent; perform a container click to capture the ServerboundContainerClick (the HashedStack form). Save each to the four fixtures/*.bin (document whether the id byte is included).
    2. Write server/entity_capture_test.go (package server): TestEntityBytesVsVanillaCapture + TestSlotBytesVsVanillaCapture as in <behavior>. Load each fixture with os.ReadFile (t.Skip if missing). Parse + assert the vanilla framing, then assert Sulfur's encoder matches (or, for the click, that Sulfur's decoder consumes it cleanly). Keep the assertions on the load-bearing framing (LP scaling, 0xFF terminator, slot framing, HashedStack body); document any content (vs framing) difference.
    3. Finalize 06-CAPTURE-DIFF.md (begun in Plans 06-02/06-05 Task 0): the capture method, the exact server.properties/seed/inputs, the per-field diff table, and the resolution of the Open Questions (LP short scaling confirmed; metadata necessity — deferred to the Task-2 real-client run; HashedStack decode depth; physics faithfulness scope = visible behaviors only). If a divergence is found, FIX it in the owning plan's encoder and re-run that plan's wire tests + this test; record the fix.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestEntityBytesVsVanillaCapture|TestSlotBytesVsVanillaCapture|TestAddEntityWire|TestSetEntityDataWire|TestContainerSetContent|TestContainerClickDecode' -count=1 && go vet ./... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The four golden fixtures are captured from the real vanilla 26.2 server and committed.
    - TestEntityBytesVsVanillaCapture + TestSlotBytesVsVanillaCapture load the fixtures and assert Sulfur's encoders match the vanilla framing (LP short scaling, 0xFF terminator, slot count+id+added+removed) and that the HashedStack decode consumes a real click; they skip cleanly if a fixture is absent.
    - 06-CAPTURE-DIFF.md records the reproducible capture method, the per-field diff, and the resolution of the Open Questions (metadata necessity deferred to the real-client run); any divergence found is fixed in the owning plan and re-diffed.
    - `go build ./...`, `go vet ./...` clean; zero new deps.
  </acceptance_criteria>
  <done>Sulfur's SetEntityData / AddEntity / ContainerSetContent / ContainerClick wire is byte-diffed against a real vanilla 26.2 server: the golden fixtures are committed, the diff tests assert the vanilla framing, the Open Questions are resolved (metadata necessity deferred to the real-client run), and any divergence is fixed — the automatable half of the authoritative ENT-01/04 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real vanilla 26.2 client INTERACTS + human sign-off (ENT-01..05 milestone gate)</name>
  <files>.planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md</files>
  <action>
    This is a human-verification checkpoint, not an autonomous code task. The capture-diff (Task 1) proves the bytes match vanilla; this proves a real client SEES an entity, places/breaks a block, takes damage, and respawns — the decisive ENT-01..05 milestone that cannot be self-approved (mirrors Phase 5's 05-03 sign-off). The executor must FIRST ensure Task 1 is complete (fixtures committed, diff green, Open Questions resolved, any divergence fixed and re-diffed), finalize 06-CAPTURE-DIFF.md, then PAUSE and present it. Do not self-approve.

    Automatable preparation (do before pausing):
    1. Confirm Task 1's TestEntityBytesVsVanillaCapture + TestSlotBytesVsVanillaCapture are green and 06-CAPTURE-DIFF.md resolves the Open Questions with any divergence fixed + re-diffed.
    2. Run the full suite + the Docker -race gate over ./server/... ./world/... ./level/... ./save/... to confirm the entity/physics/block/inventory/combat/persistence tick seams are race-clean under load (Plans 06-01..06).
    3. Run cmd/sulfur (which now: spawns a visible test entity, simulates gravity/collision, accepts place/break with reconciliation, holds a component-slot inventory, drives health/damage/respawn, and persists state). Confirm it builds, listens on proto 776, and has the debug entity-spawn + damage triggers wired so the operator has something to SEE.
  </action>
  <what-built>
    The full Phase 6 interactive world: the synchronous entityTracker makes a spawned entity VISIBLE and tracks its movement (Plan 06-02); gravity + per-axis AABB collision land entities and block clip-through (Plan 06-03); place/break with the BlockChangedAck + BlockUpdate reconciliation handshake (Plan 06-04, the new ChunkManager.SetBlock); the component-slot inventory with the HashedStack ContainerClick decode (Plan 06-05); health/damage/death and the respawn flow reusing the Phase-5-sealed spawn-info encoder (Plan 06-06); and Anvil persistence of player + entity state (Plan 06-06). 06-CAPTURE-DIFF.md + the golden fixtures are the audit artifacts proving Sulfur's uncertain entity/slot encoders match a real vanilla 26.2 server's.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md.
    2. Confirm the per-field diff shows Sulfur's encoders match vanilla framing: AddEntity's Vec3.LP short movement scaling; SetEntityData's indexed entries + the 0xFF terminator; ContainerSetContent's slot count+id+added+removed framing; the ContainerClick HashedStack decode consuming a real vanilla click.
    3. Confirm the Open Questions are resolved (LP short scaling factor; whether the v1 entity needs non-default metadata to render; HashedStack decode depth; physics faithfulness = visible behaviors only).
    4. The decisive check — connect an UNMODIFIED vanilla 26.2 client (PrismLauncher) to cmd/sulfur and confirm:
       a. ENTITY VISIBLE: a spawned entity appears and MOVES (the tracker's Add/Move/Remove working end-to-end).
       b. PLACE/BREAK: place a block and SEE it persist; break a block and SEE it vanish — NO ghost block / no snap-back on a valid edit (Plan 06-04's reconciliation).
       c. HEALTH BAR: take damage and SEE the on-screen health bar drop (Plan 06-06's SetHealth).
       d. DEATH/RESPAWN: on death the death screen appears; respawn returns the player to a streamed, walkable world (Plan 06-06's Respawn).
       e. NO KICK / NO HANG: the client is not kicked (malformed packet) and does not hang on "Loading terrain…".
  </how-to-verify>
  <verify>Human sign-off — the user types the resume signal after confirming the capture-diff and that a real client sees an entity, places/breaks a block, takes damage, and respawns.</verify>
  <done>The user has reviewed 06-CAPTURE-DIFF.md, confirmed Sulfur's uncertain entity/slot encoders match vanilla 26.2 (AddEntity LP scaling, SetEntityData 0xFF framing, ContainerSetContent slot framing, ContainerClick HashedStack), and confirmed an unmodified vanilla 26.2 client connects and INTERACTS — an entity is visible and moves, blocks place/break and persist (no ghost), the health bar drops on damage, and death→respawn completes — then typed "approved". The ENT-01..05 interactive milestone is met.</done>
  <resume-signal>Type "approved" if the capture-diff matches vanilla and a real client sees an entity, places/breaks a block (no ghost), takes damage (health bar drops), and respawns, or describe the divergence (entity invisible / vanishes, ghost block / no snap-back, health bar not updating, death screen / respawn broken, kick on a malformed packet) to fix.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestEntityBytesVsVanillaCapture|TestSlotBytesVsVanillaCapture' -count=1` passes (the byte-diff asserts the vanilla framing; the HashedStack decode consumes a real click).
- The four golden fixtures are committed; 06-CAPTURE-DIFF.md records the diff + resolves the Open Questions + is signed off.
- An unmodified vanilla 26.2 client connected to cmd/sulfur SEES an entity, places/breaks a block (no ghost), takes damage (health bar drops), and respawns (human-confirmed).
- Docker `-race` over `./server/... ./world/... ./level/... ./save/...` clean; `go build ./...` clean; any divergence found in the diff was fixed in the owning plan and re-diffed; zero new dependencies.
</verification>

<success_criteria>
- ENT-01/04 (authoritative): Sulfur's AddEntity (LP short scaling) / SetEntityData (0xFF framing) / ContainerSetContent (slot count+id+added+removed) / ContainerClick (HashedStack) bytes match a real vanilla 26.2 server — the framing that vanishes the entity / ghosts the item / desyncs the click is correct.
- ENT-01..05 (real-client milestone): an unmodified vanilla 26.2 client connects and INTERACTS — an entity is visible and moves, blocks place/break and persist (no ghost), the health bar drops on damage, and death→respawn completes — no kick/hang.
- The Open Questions are resolved from the capture + real-client run and recorded (LP short scaling, entity-metadata necessity, HashedStack decode depth, physics faithfulness = visible behaviors); any divergence is fixed in the owning plan and re-diffed before sign-off.
- The capture-diff + real-client interactive run are the authoritative gate (a self-round-trip is insufficient by design — the Phase-2/4/5 symmetric-bug lesson).
</success_criteria>

<output>
After completion, create `.planning/phases/06-entities-physics-interaction/06-07-SUMMARY.md`.
</output>
</content>
