---
phase: 20-structure-polish-loot-inhabitants-beard-persistence
plan: 04
type: execute
wave: 3
depends_on: ["20-03", "20-02", "20-05"]
files_modified:
  - world/worker.go
  - world/structure/spawn.go
  - world/structure/place.go
  - world/structure/cache.go
  - world/noisegen.go
  - world/structure/piece.go
  - world/structure/swamp_hut.go
  - world/structure/stronghold_spawner.go
  - world/structure/village_entities.go
  - server/structure_spawn.go
  - server/structure_spawn_test.go
autonomous: true
requirements: [STRUCT-POLISH-02]

must_haves:
  truths:
    - "A swamp hut emits a witch + cat spawn request (NOT a live off-tick entity); the request crosses to the tick via ChunkResult.Spawns"
    - "A stronghold places a SPAWNER block-entity set to silverfish (a BLOCK, not a live entity) — no entity-store seam"
    - "A village emits villager + cat spawn requests from the template entity list"
    - "Spawn requests reach the tick-owned entity store via the existing chunkReady.applyTo drain; the tracker broadcasts AddEntity for free"
    - "A reloaded structure chunk does NOT double-spawn (region-hit bypasses PostProcess; the one-shot guard is persisted)"
    - "The off-tick spawn-record seam threads cleanly: PostProcess records via the WorldGenView (piece.go) -> placeInChunk/PlaceStructures (place.go) -> the cache's StartsForChunk drain (cache.go) -> placeStructures (noisegen.go) -> ChunkResult.Spawns (worker.go)"
  artifacts:
    - path: "world/structure/spawn.go"
      provides: "SpawnRequest{entityType, x,y,z, finalize} + the off-tick recorder"
      contains: "SpawnRequest"
    - path: "world/worker.go"
      provides: "ChunkResult.Spawns []SpawnRequest"
      contains: "Spawns"
    - path: "world/structure/place.go"
      provides: "placeInChunk/PlaceStructures collect SpawnRequests during the PLACE pass"
      contains: "SpawnRequest|Spawns"
    - path: "world/structure/piece.go"
      provides: "WorldGenView extended so PostProcess can RECORD a spawn (not add a live entity)"
      contains: "RecordSpawn|Spawn"
    - path: "world/noisegen.go"
      provides: "placeStructures forwards the collected spawns onto the emitted ChunkResult"
      contains: "Spawns|SpawnRequest"
    - path: "server/structure_spawn.go"
      provides: "tick-side drain of ChunkResult.Spawns -> entityStore.add"
      contains: "Spawns"
  key_links:
    - from: "world/structure/swamp_hut.go PostProcess"
      to: "world/structure/place.go spawn collector"
      via: "RecordSpawn via the WorldGenView, never a live entity (Pitfall 5)"
      pattern: "SpawnRequest|RecordSpawn"
    - from: "world/structure/place.go (PlaceStructures)"
      to: "world/noisegen.go placeStructures -> ChunkResult.Spawns"
      via: "collected spawns ride the immutable ChunkResult handoff"
      pattern: "Spawns"
    - from: "server/structure_spawn.go"
      to: "entityStore.add"
      via: "chunkReady.applyTo drains Spawns on the tick owner"
      pattern: "entities\\.add|\\.add\\("
---

<objective>
Spawn structure inhabitants (STRUCT-POLISH-02) through the off-tick->tick seam: worldgen runs OFF the tick (immutable ChunkResult) but the entity store is TICK-owned (TICK-05), so a structure piece deciding "spawn a witch here" off-tick CANNOT touch the store. Add `Spawns []SpawnRequest` to `ChunkResult`; a piece RECORDS spawn requests during the PLACE pass (PostProcess -> the WorldGenView spawn recorder -> placeInChunk/PlaceStructures collect them -> placeStructures forwards them onto the emitted ChunkResult); the tick DRAINS them where it already drains `ChunkResult.Chunk` (chunkReady.applyTo, server/tick.go:77), allocates an id, builds the Entity, and `entities.add(e)` — then GAMEPLAY-01's tracker broadcasts AddEntity for free (the same path spawnBlockDrop rides).

This plan owns the FULL spawn data path end-to-end: world/structure/piece.go (extend WorldGenView so PostProcess can RECORD a spawn — this is why this plan runs in a LATER wave than 20-02, which also edits the WorldGenView interface in piece.go for chest BlockEntities; sequencing after 20-02 avoids the piece.go write race), world/structure/place.go (placeInChunk/PlaceStructures collect the recorded spawns), world/structure/cache.go (the StartsForChunk drive that PlaceStructures iterates), world/noisegen.go (placeStructures forwards the collected spawns onto the emitted ChunkResult — note 20-05 owns noisegen.go in the PRIOR wave; this plan's noisegen.go edit is sequential after it), world/worker.go (ChunkResult.Spawns), and the server-side drain.

Per the decompiled mechanisms: swamp hut = hardcoded witch+cat in PostProcess tail (one-shot spawnedWitch/spawnedCat guards); stronghold silverfish = a SPAWNER BLOCK-entity (NOT a live entity, no store seam); village villagers/cat = the jigsaw template entityInfoList. This plan depends on 20-03 (the one-shot guards persist in the piece NBT — Pitfall 6, no double-spawn on reload), 20-02 (shared ownership of the piece.go WorldGenView interface; run later to avoid the write race), AND 20-05 (shared ownership of world/noisegen.go; 20-05 edits it in Wave 2 for the beard term, this plan edits it in Wave 3 to forward the spawns — explicit depends_on makes the noisegen.go sequencing airtight, not merely wave-implicit).

Purpose: structures have their inhabitants. Output: witch/cat/villager spawn requests reach the tick store; silverfish spawner block placed; reload-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-RESEARCH.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-03-SUMMARY.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-02-SUMMARY.md
@CLAUDE.md

<read_first>
- .planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-03-SUMMARY.md — the persisted spawn-guard slot fields (fill them here; do NOT re-derive the piece NBT format)
- .planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-02-SUMMARY.md — the EXACT WorldGenView extension 20-02 made (it added SetBlockEntity for chests); extend the SAME interface with the spawn recorder, do NOT clobber 20-02's method
- temp/cache/26.2-inner.jar (decompile before writing):
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.structures.SwampHutPiece` (postProcess tail: spawnedWitch/spawnedCat, WITCH.create + setPersistenceRequired + snapTo + finalizeSpawn; spawnCat)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.structures.StrongholdPieces` (the Blocks.SPAWNER + SpawnerBlockEntity.setEntityId(SILVERFISH) placement; hasPlacedSpawner)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.templatesystem.StructureTemplate` (placeEntities / entityInfoList / createEntityIgnoreException)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.entity.Mob` (finalizeSpawn — port the call; stub attribute/variant reads behind a CITED vanilla-default constant per CLAUDE.md)
- world/worker.go:22 — ChunkResult (today carries Pos, Chunk, Err) — ADD Spawns
- world/noisegen.go:383 placeStructures + :346 the Decorate seam — placeStructures must collect the spawns recorded during PlaceStructures and attach them to the emitted ChunkResult (worker.go emit path)
- world/structure/place.go:25 placeInChunk + :53 PlaceStructures — the PLACE pass that drives PostProcess; thread a spawn collector through here (the WorldGenView spawn recorder or a returned []SpawnRequest)
- world/structure/cache.go — StartsForChunk (the start iteration PlaceStructures drives); add nothing structural, just confirm the collector threads through
- world/structure/piece.go:16 WorldGenView + :802 Piece.PostProcess — EXTEND WorldGenView with a RecordSpawn(req SpawnRequest) method (20-02 already added SetBlockEntity here; add alongside, do NOT replace). *world.Neighborhood satisfies it structurally — confirm the world-side recorder buffers into the ChunkResult.
- server/tick.go:77 — chunkReady.applyTo (where ChunkResult.Chunk is drained on the owner) — DRAIN Spawns here
- server/entity_store.go — entityStore.add + EntityIDAllocator.AllocID; server/block_drop.go NewItemEntity/NewEntity (the store-add pattern the spawn mirrors)
- world/structure/swamp_hut.go:190 — the "WITCH + BLACK-CAT entities DEFERRED v3" tail to FILL
- world/structure/ stronghold pieces — find the chamber that places the spawner; level/block/blockentities.go MobSpawner BE
- world/structure/template.go:62,75,86 — rawEntity{Pos,BlockPos,NBT} + rawTemplate.Entities + StructureTemplate.entities are ALREADY parsed + stored at load (the runtime .nbt parser keeps them per the comment "kept so 16-02/v3 can find them"). So the village entity list is NOT a net-new extraction — it is ALREADY in StructureTemplate.entities; Task 3 only emits SpawnRequests from the stored list.
- data/entity — Witch/Cat/Villager/Silverfish entity ids
</read_first>

<interfaces>
From world/worker.go (current):
  type ChunkResult struct { Pos level.ChunkPos; Chunk *level.Chunk; Err error }   // ADD: Spawns []structure.SpawnRequest

From world/structure/piece.go (current + 20-02's extension):
  type WorldGenView interface {
    SetBlock(wx, wy, wz int, st block.StateID)
    GetBlock(wx, wy, wz int) block.StateID
    // 20-02 added (confirm exact signature in 20-02-SUMMARY): SetBlockEntity(...) for chests
    // THIS plan adds: RecordSpawn(req SpawnRequest)   // off-tick record, never a live entity
  }

From world/structure/place.go (current):
  func placeInChunk(start *StructureStart, view WorldGenView, writableBox BoundingBox, chunkPos level.ChunkPos, worldSeed int64)
  func (c *Cache) PlaceStructures(view WorldGenView, center level.ChunkPos, worldSeed int64, minY, height int)
  // PostProcess RECORDS spawns through the view; place.go/the Neighborhood buffers them; placeStructures forwards them.

From world/noisegen.go (current):
  func (g *NoiseGenerator) placeStructures(view *Neighborhood)   // :383 — forward the collected spawns onto the emitted ChunkResult
  func (g *NoiseGenerator) Decorate(view *Neighborhood)          // :293 — placeStructures runs at the END (:346)

From server/tick.go (current — the drain site):
  func (r chunkReady) applyTo(t *TickLoop) { /* t.world.Insert(...); t.postProcessChunkFluids(...) */ }
  // ADD: drain r.res.Spawns -> for each: t.idAlloc.AllocID() + build Entity + t.entities.add(e)

From server/entity_store.go + block_drop.go (the store-add pattern to mirror):
  func (a *EntityIDAllocator) AllocID() int32
  func (s *entityStore) add(e *Entity)
  func NewEntity(id int32, t entity.Type, x,y,z float64) *Entity

From world/structure/template.go (ALREADY stores template entities — NOT net-new):
  type StructureTemplate struct { /* ... */ entities []rawEntity }   // populated at LoadTemplate
  type rawEntity struct { Pos []float64; BlockPos [3]int; NBT nbt.RawMessage }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChunkResult.Spawns seam + WorldGenView.RecordSpawn + the tick-side drain (off-tick -> tick handoff)</name>
  <files>world/worker.go, world/structure/spawn.go, world/structure/piece.go, world/structure/place.go, world/structure/cache.go, world/noisegen.go, server/structure_spawn.go, server/structure_spawn_test.go</files>
  <read_first>
    - 20-02-SUMMARY.md — the exact WorldGenView method 20-02 added (extend the SAME interface; do NOT clobber it)
    - world/worker.go:22 ChunkResult + the emit/handleTerrain/tryEmit flow (Spawns must ride the SAME immutable handoff as Chunk)
    - world/structure/piece.go:16 WorldGenView + :802 PostProcess; world/structure/place.go:25,53; world/noisegen.go:383 placeStructures
    - server/tick.go:77 chunkReady.applyTo + entity_store.add + AllocID + block_drop.NewEntity
  </read_first>
  <behavior>
    - Test (TestStructureSpawnSeam): a ChunkResult carrying N SpawnRequests, drained by applyTo, results in N entities added to the store with allocated ids
    - Test (-race): the worker RECORDS requests off-tick and the tick ADDS them on the owner — no off-tick entity_store mutation (Pitfall 5)
    - Test: a SPAWNER-block silverfish is NOT a SpawnRequest (it is a block, placed in the chunk) — assert it does not reach the store
    - Test: PostProcess calling view.RecordSpawn(req) buffers the request, and placeStructures forwards the buffer onto ChunkResult.Spawns (the full path: PostProcess -> place.go collect -> noisegen.go forward -> ChunkResult)
  </behavior>
  <action>
    Define structure.SpawnRequest{EntityType entityid; X,Y,Z float64; Finalize <minimal one-shot data, e.g. variant/persistence flag>} in world/structure/spawn.go (the off-tick carrier — a value, no live pointer, mirroring ChunkResult's single-owner discipline).
    EXTEND WorldGenView (world/structure/piece.go) with `RecordSpawn(req SpawnRequest)` ALONGSIDE 20-02's SetBlockEntity (do NOT replace it — read 20-02-SUMMARY for its exact signature). PostProcess records spawns through the view instead of touching any store.
    In place.go, thread the recorded spawns through placeInChunk/PlaceStructures: the *world.Neighborhood (the WorldGenView impl, package world) buffers RecordSpawn calls; PlaceStructures drives StartsForChunk (cache.go) as today. In world/noisegen.go placeStructures (:383), after the PlaceStructures call, COLLECT the buffered spawns and attach them to the emitted ChunkResult (the worker.go emit path) — Spawns rides the EXACT same immutable handoff as Chunk.
    Add `Spawns []SpawnRequest` to world.ChunkResult (worker.go). In server (structure_spawn.go), extend chunkReady.applyTo (server/tick.go) to DRAIN r.res.Spawns on the owner: for each request AllocID() + build the Entity (mirror NewItemEntity: NewEntity + metadata) + t.entities.add(e). The tracker broadcasts AddEntity next tick (no new tracker code). finalizeSpawn attribute/variant reads stub behind a CITED vanilla-default constant (CLAUDE.md) structured to become real later.
    NOTE: this plan's piece.go + noisegen.go edits are sequential AFTER 20-02 (piece.go) and 20-05 (noisegen.go) — read their SUMMARYs first so you extend, not overwrite, their changes.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/... ./server/ && go vet ./world/ ./world/structure/ ./server/ && go test ./server/ -run TestStructureSpawnSeam</automated>
  </verify>
  <done>ChunkResult carries Spawns; PostProcess records via WorldGenView.RecordSpawn; place.go/noisegen.go forward the buffer onto the immutable ChunkResult; the tick drains them on the owner into the store; no off-tick store mutation (-race clean); the seam test green; CGO=0 build clean. Never gopls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swamp hut witch+cat + stronghold silverfish SPAWNER + reload guard</name>
  <files>world/structure/swamp_hut.go, world/structure/stronghold_spawner.go</files>
  <read_first>
    - javap SwampHutPiece (postProcess tail spawnedWitch/spawnedCat/spawnCat) + StrongholdPieces (Blocks.SPAWNER + SpawnerBlockEntity.setEntityId SILVERFISH, hasPlacedSpawner)
    - world/structure/swamp_hut.go:190 (the deferred tail) + the stronghold chamber piece
    - 20-03-SUMMARY.md (the persisted spawnedWitch/spawnedCat/hasPlacedSpawner guard fields)
    - level/block/blockentities.go MobSpawner BE
  </read_first>
  <behavior>
    - Test (TestSwampHutSpawns): SwampHutPiece.PostProcess records exactly one witch + one cat SpawnRequest (via view.RecordSpawn), guarded by spawnedWitch/spawnedCat (a second PostProcess call records none — one-shot)
    - Test (TestStrongholdSpawner): the stronghold chamber places a SPAWNER block-entity set to silverfish (a BLOCK in the chunk, NOT a SpawnRequest), guarded by hasPlacedSpawner
    - Test: the guards persist (read via 20-03's piece NBT) so a reloaded piece skips re-spawn (Pitfall 6)
  </behavior>
  <action>
    FILL swamp_hut.go:190's deferred tail per SwampHutPiece.postProcess: guard on !spawnedWitch -> record a WITCH SpawnRequest at the jar-exact (x,y,z) with setPersistenceRequired + finalizeSpawn data via view.RecordSpawn, set spawnedWitch=true; then spawnCat -> guard !spawnedCat -> record a CAT SpawnRequest, set spawnedCat=true. The guards are the persisted fields (20-03). Use data/entity Witch/Cat ids. For the stronghold: in the chamber piece, place a Blocks.SPAWNER block + a MobSpawner block-entity with setEntityId(SILVERFISH) (a BLOCK, NO SpawnRequest — Pitfall: silverfish is not a live entity), guarded by hasPlacedSpawner. Cite every javap. The reload-no-double-spawn property: a region-hit chunk bypasses PostProcess (worker.go:182 emits region hits directly) AND the persisted guard reads true if recompute ever re-runs — belt and suspenders.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/structure/ && go test ./world/structure/ -run 'TestSwampHutSpawns|TestStrongholdSpawner'</automated>
  </verify>
  <done>Swamp hut records witch+cat requests (one-shot); stronghold places a silverfish SPAWNER block (one-shot); guards persist; tests green; CGO=0 build clean. Never gopls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Village template entity spawns (villagers + cat) — from the ALREADY-STORED template entity list</name>
  <files>world/structure/village_entities.go</files>
  <read_first>
    - javap StructureTemplate.placeEntities / entityInfoList / createEntityIgnoreException
    - world/structure/template.go:62,75,86 — rawEntity + rawTemplate.Entities + StructureTemplate.entities are ALREADY parsed + stored at LoadTemplate (the runtime .nbt parser keeps them). The list is in hand — Task 3 reads it, it does NOT re-extract.
    - world/structure/village.go — where the village template is placed (the position transform to apply to each stored entity)
    - data/entity Villager/Cat ids
  </read_first>
  <behavior>
    - Test (TestVillageSpawns): a village house template with a stored entity (villager) records a SpawnRequest at the template-transformed position (read from StructureTemplate.entities)
    - Test: the village cat (where present in the stored entity list) records a SpawnRequest
    - Test: a template with an empty entity list records none (no panic)
  </behavior>
  <action>
    Port StructureTemplate.placeEntities reading the ALREADY-PARSED StructureTemplate.entities (template.go stores rawEntity{Pos,BlockPos,NBT} at load — this is NOT a net-new offline extraction; the runtime parser already keeps the list per its own comment). For each stored rawEntity, emit a SpawnRequest at the rotation/mirror-transformed world position (createEntityIgnoreException -> a SpawnRequest via view.RecordSpawn, NOT a live entity). Apply the SAME transform template.go uses for blocks (transformPos / calculateRelativePosition) to the entity Pos. finalizeSpawn variant/profession reads stub behind a CITED vanilla-default constant if the subsystem is incomplete. Read the entity NBT (rawEntity.NBT) for the entity id / variant.
    SCOPE NOTE (A4 resolved): because template.go ALREADY parses + stores Entities, there is NO offline extraction step here — Task 3 is purely the transform + RecordSpawn emit over the stored list. If a specific village .nbt lacks an entities tag, StructureTemplate.entities is empty -> no requests (the no-panic test).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/structure/ && go test ./world/structure/ -run TestVillageSpawns</automated>
  </verify>
  <done>Village templates record villager+cat spawn requests at transformed positions from the stored template entity list; an empty list is a no-op; tests green; CGO=0 build clean. Never gopls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-tick worker -> tick entity store | The worker MUST NOT mutate the tick-owned store; spawns cross only via the immutable ChunkResult.Spawns |
| persisted spawn guard -> reload | A reloaded piece must read its one-shot guard to avoid double-spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | (data race) | entity_store off-tick | mitigate | ChunkResult.Spawns handoff; the tick performs the only add; `go test -race` gate (Pitfall 5) |
| T-20-11 | T (Tampering) | reload double-spawn | mitigate | One-shot guards persisted in piece NBT (20-03) + region-hit bypasses PostProcess (Pitfall 6) |
| T-20-12 | D (DoS) | SpawnRequest list size | mitigate | Bound the per-chunk spawn-request count (a structure emits a small fixed set; cap defensively) |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./world/... ./server/` clean (NEVER gopls).
- `go vet ./world/ ./world/structure/ ./server/` clean.
- `go test ./world/structure/ -run 'TestSwampHutSpawns|TestStrongholdSpawner|TestVillageSpawns'` + `go test ./server/ -run TestStructureSpawnSeam` green.
- Docker `-race` over ./server/ ./world/ green — the spawn handoff is the off-tick->tick boundary; -race MUST be clean (TICK-05 / Pitfall 5).
- CAPTURE-DIFF FLAG: spawned entities + the silverfish spawner block-entity touch the chunk-wire entity/BlockEntity list. Re-run the worldgen capture-diff goldens; re-seal if the framing changed (document in SUMMARY), else confirm green.
- SEQUENCING: this plan edits world/structure/piece.go (after 20-02 in Wave 2) and world/noisegen.go (after 20-05 in Wave 2). Confirm the build is clean ON TOP of both — read 20-02-SUMMARY + 20-05-SUMMARY before editing those two files so you extend, never clobber.
</verification>

<success_criteria>
- Swamp hut records witch+cat; stronghold places a silverfish SPAWNER block; village records villagers+cat from the stored template entity list — all as SpawnRequests (or a block for silverfish), never off-tick live entities.
- Requests reach the tick store via chunkReady.applyTo; the tracker broadcasts AddEntity for free.
- No double-spawn on reload (persisted guards + region-hit bypass). -race clean.
- The full spawn data path (piece.go RecordSpawn -> place.go collect -> noisegen.go forward -> worker.go ChunkResult.Spawns -> server drain) is wired end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-04-SUMMARY.md`.
Stage files BY NAME. Commit message: `feat(20): structure inhabitant spawns via ChunkResult.Spawns + silverfish spawner (STRUCT-POLISH-02)` — NO Co-Authored-By / NO Claude attribution. Push: development.
Record in the SUMMARY: the SpawnRequest struct fields, the WorldGenView.RecordSpawn signature (alongside 20-02's SetBlockEntity), that the village entity list came from template.go's ALREADY-STORED entities (NOT a net-new extraction), the finalizeSpawn stub constants used, and whether the capture-diff needed a re-seal.
</output>
