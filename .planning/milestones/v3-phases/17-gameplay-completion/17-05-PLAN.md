---
phase: 17-gameplay-completion
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [GAMEPLAY-07]
must_haves:
  truths:
    - "Two real vanilla 26.2 clients see each other move in-world (avatars render + track position)"
    - "A player's position and inventory survive a disconnect + reconnect"
    - "An attack deals damage; lethal damage shows the death screen; respawn returns the player to a fresh world"
    - "Fall damage applies on a hard landing"
    - "Water flows after a disturbance and slows/floats a swimming player"
    - "A broken block drops a visible, pickable item"
  artifacts: []
  key_links:
    - from: "two real vanilla 26.2 clients"
      to: "the running cmd/sulfur server (local or demo.trysulfur.net)"
      via: "live multiplayer session"
      pattern: "human-verified"
---

<objective>
GAMEPLAY-07: the closing VISUAL GATE. A real, unmodified vanilla 26.2 (protocol 776) client — two players — confirms end-to-end multiplayer gameplay: players see each other move, positions/inventory survive a reconnect, attacks deal damage + death/respawn, water flows + affects movement, and broken blocks drop pickable items. This is the human-confirmed threshold the prior milestones used (Phases 5/13/16).

Purpose: The six seams (17-01..04) closed on automated tests. Like every prior gate, the authoritative proof of "actually playable on a real client" is a human running an unmodified vanilla client — not a self-test. This gate exercises all six requirements together against real network framing.
Output: User confirmation that v3's playability claim is real, with any defects surfaced for a `--gaps` follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gameplay-completion/17-RESEARCH.md
@.planning/phases/17-gameplay-completion/17-01-SUMMARY.md
@.planning/phases/17-gameplay-completion/17-02-SUMMARY.md
@.planning/phases/17-gameplay-completion/17-03-SUMMARY.md
@.planning/phases/17-gameplay-completion/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, run the full automated backstop, and start a local server for the gate</name>
  <files>(no source edits — build + run only)</files>
  <read_first>
    - .planning/HANDOFF.md "INFRA (live)" + "STANDING CONSTRAINTS" (the local server run + the -race Docker command + the CGO_ENABLED=0 build + kill the bg server on :25565 before rebuilding)
    - all four 17-0x-SUMMARY.md (what each plan shipped + any documented limitation feeding the gate, e.g. the player-physics integration note, the waterlogged simplification, the v1 1:1 drop map)
  </read_first>
  <action>
    1. Kill any background server holding :25565 (per HANDOFF: `Get-NetTCPConnection -LocalPort 25565 | Stop-Process` on Windows, or the equivalent — confirm nothing is listening before rebuild).
    2. Full backstop: `cd /d/ender && go build ./... && go test ./...` — must be green. Then the race gate over the touched packages: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race -timeout 1800s ./server/...` — must be clean (CGO_ENABLED=0 host means -race needs Docker). If anything fails, STOP and fix before the gate (do not present a gate over a red build).
    3. Build the server binary (CGO_ENABLED=0, pure Go static) and start it locally on :25565 with a known seed (e.g. seed 25, matching the prior gates) so the operator can connect two clients. Confirm it listens on proto 776 and logs the join path.
    4. Verify the six seams are wired with a quick grep sanity pass: `grep -n "entities.add(p.playerEntity)" server/tick.go`, `grep -n "spawnBlockDrop" server/block_interact.go`, `grep -n "ServerboundInteract" server/subtick.go`, `grep -rn "getNewLiquid" server/fluid.go`, `grep -n "NOT applied for v1" server/gameplay_tick.go` (must be EMPTY), `grep -n "syncJoinInventories" server/tick_phases.go`.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 and `go test ./...` is green
    - the Docker -race over ./server/... is clean
    - the server is running on :25565 (proto 776) ready for two clients
    - all six seam greps confirm the wiring (and the "NOT applied for v1" grep is empty)
  </acceptance_criteria>
  <done>Automated backstop green + race clean; a local server is up for the human gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: VISUAL GATE — two real vanilla 26.2 clients confirm all six GAMEPLAY requirements</name>
  <action>
    Automation is complete (Task 1 built + tested + started the server). PAUSE here for the human gate: the operator connects two real vanilla 26.2 clients and verifies the six requirements per <how-to-verify>. Do not proceed until the operator returns "approved" or a defect list. This task makes no code edits — it is the human confirmation step that closes the phase (mirroring the Phase 5/13/16 gates).
  </action>
  <what-built>
    The six unwired gameplay seams are now connected (17-01..04): player visibility + tab-list broadcast (GAMEPLAY-01), persisted-position load (02), inventory join-sync (03), attack/interact damage + fall damage (04 in the requirement numbering / plan 17-03), vanilla fluid simulation + player fluid physics (05 / plan 17-02), and block-break item drops (06 / plan 17-04). The server is running on localhost:25565 (proto 776, seed 25), automated tests + Docker -race are green.
  </what-built>
  <how-to-verify>
    Connect TWO unmodified vanilla 26.2 (protocol 776) clients (PrismLauncher) to the running server — `localhost:25565` (or `demo.trysulfur.net:25565` if testing the deployed build). Then confirm each requirement:

    1. GAMEPLAY-01 (players visible): with both clients in-world, move client A — client B sees A's avatar MOVE in real time (not frozen, not invisible), and A appears in B's tab list (and vice versa). Walk apart and back together — avatars despawn/respawn at range.
    2. GAMEPLAY-02 (position persists): on client A, walk far from spawn, then disconnect and reconnect — A respawns AT the position it left (not back at spawn), with no "Loading terrain" hang and no rubber-band.
    3. GAMEPLAY-03 (inventory): the inventory window opens populated (not an empty/broken window); in creative, set a hotbar item and confirm it sticks; a survival client can hold + place the item.
    4. GAMEPLAY-04 (damage): client A attacks client B — B's health drops (hearts decrease). Take B to 0 HP — the death screen appears; click Respawn — B returns to a fresh world at spawn with full hearts. Then jump off a tall cliff — fall damage applies on landing.
    5. GAMEPLAY-05 (water): break a block next to existing water (or place water near a drop) — the water FLOWS into the opening and settles (does not flow forever or flicker). Swim into deep water — movement is slowed and the player floats/swims (buoyancy), not sinking like on land.
    6. GAMEPLAY-06 (drops): break a block (e.g. stone/dirt) in survival — a visible item entity pops out on the ground (NOT invisible). Walk over it (pickup is a later concern if not wired — confirm at minimum it RENDERS as a dropped item).

    Report PASS for each, or describe the exact defect (which requirement, what you saw vs expected) so a `/gsd-plan-phase 17 --gaps` follow-up can target it.
  </how-to-verify>
  <resume-signal>Type "approved" if all six pass, or describe the defects (per requirement) for a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- Automated backstop green (`go test ./...`) + Docker -race over ./server/... clean BEFORE the human gate
- The human gate confirms all six GAMEPLAY requirements end-to-end on two real vanilla 26.2 clients
- Any defect is captured per-requirement for a `--gaps` follow-up (mirroring the Phase 5/13/16 gate discipline)
</verification>

<success_criteria>
- A real, unmodified vanilla 26.2 client (two players) confirms: players see each other move, positions/inventory survive a reconnect, attacks deal damage + death/respawn, water flows + affects movement, and broken blocks drop visible items.
- v3's "actually playable on a real multiplayer client" claim is human-confirmed (the prior milestones' gate standard).
</success_criteria>

<output>
After completion, create `.planning/phases/17-gameplay-completion/17-05-SUMMARY.md` recording the gate outcome: which requirements passed on the real client, any defects + their disposition (fixed inline / deferred to --gaps / documented limitation), the seed + build used, and the user's confirmation quote (as the prior gates did). If the gate fails, the SUMMARY lists the gaps for `/gsd-plan-phase 17 --gaps`.
</output>
