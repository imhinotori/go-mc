---
phase: 05-player-session-in-world-first-playable
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/play_join.go
  - server/gameplay_tick.go
  - server/play_join_test.go
autonomous: true
requirements: [PLAY-01, PLAY-05, PLAY-02, PLAY-03]
user_setup: []

must_haves:
  truths:
    - "sendPlayBootstrap APPENDS the early-Play tail (PlayerAbilities → SetHeldSlot → PlayerInfoUpdate(self) → SetDefaultSpawnPosition) AFTER the existing three bootstrap packets (Login → GameEvent → PlayerPosition) and still BEFORE loop.register — preserving the proven FIFO-before-register ordering invariant; it does NOT rebuild the Login/position scaffolding"
    - "writePlayerInfoUpdateAdd encodes the 776 8-action set as a 1-byte mask (ADD_PLAYER|UPDATE_GAME_MODE|UPDATE_LISTED) followed by a count-prefixed single self-entry (UUID, name, 0 properties, gameMode VarInt, listed Boolean) so the joining player appears in its own tab list (PLAY-05)"
    - "The bootstrap issues an INCREMENTING per-player teleport id (not the const initialTeleportID=1), threads it into the bootstrap PlayerPosition AND into the new tickPlayer.awaitingTeleport, so the Plan-05-01 teleport gate can match the client's Confirm Teleportation echo (PLAY-02)"
    - "AcceptPlayer threads the real per-player identity (the login name + uuid already passed to AcceptPlayer, plus a per-player entity id) into the bootstrap tail and the tickPlayer, replacing the const joinEntityID=1 placeholder for the self tab-list entry"
    - "PlayerAbilities and SetHeldSlot encode their jar-confirmed simple layouts (Byte flags + Float flySpeed + Float walkSpeed; VarInt slot); SetDefaultSpawnPosition encodes the 26.x RespawnData/GlobalPos shape (sealed by the Plan-05-03 capture-diff)"
  artifacts:
    - path: "server/play_join.go"
      provides: "early-Play tail builders (writePlayerAbilities, writeSetHeldSlot, writePlayerInfoUpdateAdd, writeSetDefaultSpawnPosition) + incrementing-teleport-id bootstrap"
      contains: "writePlayerInfoUpdateAdd"
    - path: "server/gameplay_tick.go"
      provides: "AcceptPlayer threads real name/uuid/entityID + the issued teleport id into the bootstrap tail and tickPlayer.awaitingTeleport"
      contains: "awaitingTeleport"
    - path: "server/play_join_test.go"
      provides: "wire-layout + ordering tests for the tail (abilities/heldslot/playerinfo/spawnpos) and the incrementing teleport id"
      contains: "TestPlayerInfoUpdateWire"
  key_links:
    - from: "server/gameplay_tick.go AcceptPlayer"
      to: "server/play_join.go sendPlayBootstrap"
      via: "pass the player's real name/uuid/entityID + the issued incrementing teleport id"
      pattern: "sendPlayBootstrap"
    - from: "server/play_join.go sendPlayBootstrap"
      to: "tickPlayer.awaitingTeleport (Plan 05-01 field)"
      via: "the issued teleport id is returned/recorded so AcceptPlayer sets awaitingTeleport before register"
      pattern: "awaitingTeleport"
    - from: "server/play_join.go writePlayerInfoUpdateAdd"
      to: "the client tab list"
      via: "1-byte action mask + count-prefixed self-entry (UUID/name/gameMode/listed)"
      pattern: "ClientboundPlayerInfoUpdate"
---

<objective>
Send the early-Play packet set a real client needs to fully enter the world and appear in the tab list (PLAY-01, PLAY-05), and wire the incrementing teleport id the Plan-05-01 gate matches (PLAY-02) — by EXTENDING the existing bootstrap (server/play_join.go, server/gameplay_tick.go), not rebuilding it. Today `sendPlayBootstrap` sends exactly three packets (Login → GameEvent → PlayerPosition) with a const teleport id of 1 and a const entity id of 1, and no abilities/held-slot/tab-list/spawn-pos. This plan APPENDS the early-Play tail (PlayerAbilities → SetHeldSlot → PlayerInfoUpdate(self) → SetDefaultSpawnPosition) after those three and still before `loop.register` (preserving the FIFO-before-register invariant), threads the real login name/uuid + a per-player entity id into the tab-list entry, and issues an incrementing teleport id stored in `tickPlayer.awaitingTeleport`.

Purpose: PLAY-01 hardening (the full early-Play set so the client is a real, listed player, not just a camera on solid ground), PLAY-05 (the player appears in its own tab list), and the PLAY-02 producer side (the incrementing id the Plan-05-01 gate validates). Three of the four new encoders are trivial/jar-confirmed; SetDefaultSpawnPosition (and the PlayerInfoUpdate entry sub-encoding) are sealed by the Plan-05-03 capture-diff.
Output: the four tail builders + the incrementing-teleport-id bootstrap in play_join.go, the AcceptPlayer threading of real identity + awaitingTeleport in gameplay_tick.go, and the wire/ordering tests. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-player-session-in-world-first-playable/05-RESEARCH.md
@.planning/phases/05-player-session-in-world-first-playable/05-VALIDATION.md
@.planning/phases/05-player-session-in-world-first-playable/05-01-PLAN.md

<interfaces>
<!-- VERIFIED this session from the actual code. Use these directly. -->

From server/play_join.go (EXTEND — the existing bootstrap; do NOT rebuild these):
```go
const joinEntityID = 1        // placeholder — replace with a real per-player id for the tab entry
const initialTeleportID = 1   // const — replace with an incrementing per-player id
const gameEventLevelChunksLoadStart = 13
const gameModeSurvival = 0
func writeLoginPacket(viewDist int) pk.Packet                      // jar-verified — reuse as-is
func writeGameEventPacket(eventID int, param float32) pk.Packet    // reuse as-is
func writePlayerPositionPacket(teleportID int, x, y, z float64, yaw, pitch float32) pk.Packet // reuse; drive with incrementing tpID
func sendPlayBootstrap(c *Client, viewDist int, center level.ChunkPos, surfaceY int) // APPEND the tail here + thread identity/tpID
```

From server/gameplay_tick.go (EXTEND — AcceptPlayer; thread real identity + awaitingTeleport):
```go
func (g *gameTick) AcceptPlayer(name string, id uuid.UUID, profilePubKey *user.PublicKey, properties []user.Property, protocol int32, conn *net.Conn)
// today: builds tickPlayer{client, keep, keepalive, center, viewDist, sentChunks, secs} and calls sendPlayBootstrap(c, viewDist, spawnCenter, g.spawnSurfaceY) BEFORE g.loop.register <- player.
// name + id are ALREADY available here (login profile) — currently unused; thread them into the tail + tickPlayer.
const overworldSections = 24
```

From server/tick.go (Plan 05-01 ADDED these fields — set awaitingTeleport here):
```go
type tickPlayer struct {
    // ... existing ...
    x, y, z float64; yaw, pitch float32; onGround bool   // Plan 05-01
    awaitingTeleport int                                  // Plan 05-01 — SET it from the incrementing bootstrap id
    confirmedTeleport bool
}
```

From net/packet (primitives — all exist):
```go
type Byte int8; type Float float32; type VarInt int32; type UUID uuid.UUID; type String string; type Boolean bool
func Marshal[ID ~int32|int](id ID, fields ...FieldEncoder) Packet
// For the PlayerInfoUpdate collection, write the count + entries inline (a custom FieldEncoder) like levelsEncoder/commonPlayerSpawnInfoEncoder in play_join.go.
```

From data/packetid/packetid.go (all IDs already generated — VERIFIED present):
```go
ClientboundPlayerAbilities, ClientboundSetHeldSlot, ClientboundPlayerInfoUpdate, ClientboundSetDefaultSpawnPosition
```
</interfaces>

<key_facts>
<!-- VERIFIED against 05-RESEARCH this session. -->
- EXTEND, DO NOT REBUILD (the STATE.md Deferred mandate): writeLoginPacket + writePlayerPositionPacket are jar-verified and covered by TestLoginPacketWireLayout / TestPlayerPositionPacketWireLayout. Do NOT touch their bodies. APPEND the tail in sendPlayBootstrap; keep all sends BEFORE loop.register (the FIFO-before-register invariant proven by TestJoinSequenceOrdering — a tail packet enqueued before register still lands before any chunk packet).
- THE EARLY-PLAY TAIL (05-RESEARCH Pattern 5), appended after the existing 3:
    PlayerAbilities(flags=0, flySpeed=0.05, walkSpeed=0.1) — jar-confirmed: Byte flags(0x01 invuln|0x02 flying|0x04 canFly|0x08 instabuild) + Float flySpeed + Float walkSpeed. Survival default = all-false flags.
    SetHeldSlot(0) — jar-confirmed: a single VarInt slot index.
    PlayerInfoUpdate(self) — the tab-list entry (PLAY-05).
    SetDefaultSpawnPosition(...) — 26.x RespawnData = GlobalPos + Float + Float (CAPTURE-DIFF candidate; encode the jar shape, seal bytes in Plan 05-03).
- PlayerInfoUpdate (PLAY-05, 05-RESEARCH Pattern 4 — jar-confirmed action set, MEDIUM-confidence entry sub-encoding):
    8 actions (added UPDATE_LIST_ORDER + UPDATE_HAT vs the old 6): ADD_PLAYER=0x01, INITIALIZE_CHAT=0x02, UPDATE_GAME_MODE=0x04, UPDATE_LISTED=0x08, UPDATE_LATENCY=0x10, UPDATE_DISPLAY_NAME=0x20, UPDATE_LIST_ORDER=0x40, UPDATE_HAT=0x80. 8 actions still fit in 1 byte, so vanilla's writeEnumSet == a single pk.Byte mask (the fork has FixedBitSet but no writeEnumSet helper; a 1-byte mask is byte-identical — Assumption A3, sealed by the capture-diff).
    Self-entry minimal action set: ADD_PLAYER|UPDATE_GAME_MODE|UPDATE_LISTED (0x01|0x04|0x08 = 0x0D). Wire: pk.Byte(mask), then writeCollection = VarInt(1) count, then the entry = UUID(id), [ADD_PLAYER] String(name) + VarInt(0) propertyCount, [UPDATE_GAME_MODE] VarInt(gameMode), [UPDATE_LISTED] Boolean(true). Entry bodies are written in ENUM ORDER for the present actions.
    Capture-diff the property-list sub-encoding (Boolean signed + optional String signature) even though offline sends 0 properties — the count-prefix shape must match (Plan 05-03).
- INCREMENTING TELEPORT ID (PLAY-02 producer, 05-RESEARCH Pattern 3): replace the const initialTeleportID=1 with a per-player incrementing id. Issue it in AcceptPlayer (a simple per-gameTick atomic counter, or derive from the entity id for v1 — document the choice), pass it to writePlayerPositionPacket, AND set tickPlayer.awaitingTeleport to the SAME id so the Plan-05-01 dispatch gate matches the client's echo. The bootstrap is sent off-tick in AcceptPlayer BEFORE register; the tickPlayer{awaitingTeleport: tpID} is constructed there, so the tick knows what to match (05-RESEARCH note under Pattern 3).
- REAL IDENTITY: AcceptPlayer already receives the login `name string, id uuid.UUID` — thread them into writePlayerInfoUpdateAdd(id, name, gameModeSurvival) and into the tickPlayer. Assign a per-player entity id (for v1 a simple incrementing counter on gameTick, or reuse joinEntityID's slot replaced by the counter) — document the choice; the self tab-list entry uses the UUID, the Login playerId uses the entity id (writeLoginPacket currently hard-codes joinEntityID — for v1 leaving Login's entity id as the const is acceptable IF the tab entry's UUID is the real one; prefer threading a real entity id if low-cost, but the load-bearing tab-list key is the UUID).
- SetDefaultSpawnPosition (05-RESEARCH Pitfall 5, Open Question 3): 26.x RespawnData = GlobalPos(dimension ResourceKey + BlockPos) + Float + Float. Encode the jar shape (a custom FieldEncoder mirroring commonPlayerSpawnInfoEncoder). It is RECOMMENDED (compass/respawn) but its absence likely does not kick (A5) — the Plan-05-03 capture-diff + real-client test are the arbiter. SetTime is OPTIONAL for walking (A2) — do NOT add it here unless the Plan-05-03 real-client test shows a kick without it.
- SINGLE-OWNER (TICK-05): sendPlayBootstrap runs off-tick in AcceptPlayer and mutates NO tick-owned state — it only moves bytes through the connection's own bounded queue (exactly like the existing 3 bootstrap packets). The tickPlayer (incl. awaitingTeleport) is constructed off-tick and handed to the owner via loop.register; the owner does the insert (drainRegistrations). No new boundary crossing.
- STALE-LSP CAVEAT: gopls/Serena may show bogus "undefined: awaitingTeleport" or "undefined: recenterRing" mid-edit because Plan 05-01's edits + this plan's edits land across files. TRUST `go build ./...`. If 05-01 is merged (this plan depends_on 01), the fields exist.
- Serena guidance: add the 4 builder funcs + action-mask consts to server/play_join.go (mirror the existing levelsEncoder/commonPlayerSpawnInfoEncoder custom-FieldEncoder style for the PlayerInfoUpdate collection and the RespawnData record). Change sendPlayBootstrap's signature to accept the identity + teleport id (or read them from a passed struct). Thread them from AcceptPlayer in server/gameplay_tick.go and set tickPlayer.awaitingTeleport. Extend server/play_join_test.go.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (accept goroutine) → untrusted client (clientbound Play packets) | The early-Play tail is server-authored output. The risk is wire-correctness (a malformed packet kicks the client) and identity-spoofing surface in the tab-list entry, not untrusted input. The teleport id is the one value the client echoes back (validated in Plan 05-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Spoofing | the issued teleport id (producer side) | mitigate | The id is SERVER-issued and incrementing (never client-chosen), recorded in tickPlayer.awaitingTeleport; Plan 05-01 only confirms on an exact echo match. An attacker cannot pre-guess-and-confirm a future id without observing the sent PlayerPosition (and even then only confirms its own join). Asserted by TestBootstrapTeleportID + the Plan-05-01 TestTeleportGate. |
| T-5-07 | Tampering | malformed early-Play tail packet (wrong action mask / field order) kicks the real client | mitigate | The action set is the jar-confirmed 8-action 1-byte mask; the entry sub-encoding and SetDefaultSpawnPosition (RespawnData/GlobalPos) are capture-diffed against a real vanilla 26.2 server (Plan 05-03) before sign-off — the producer-side wire-correctness gate, mirroring Phase 2/4. |
| T-5-08 | Information disclosure | the tab-list self-entry leaking another player's identity | accept | v1 sends only the joining player's OWN UUID/name (single self-entry); multi-player broadcast (and its visibility rules) is Phase-6-adjacent. No cross-player data is in the v1 entry. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The early-Play tail builders (PlayerAbilities, SetHeldSlot, PlayerInfoUpdate self-entry, SetDefaultSpawnPosition) — PLAY-01/05</name>
  <files>server/play_join.go, server/play_join_test.go</files>
  <read_first>
    - .planning/phases/05-player-session-in-world-first-playable/05-RESEARCH.md — Pattern 4 (PlayerInfoUpdate self-entry), Pattern 5 (the tail), Code Examples (PlayerAbilities, SetHeldSlot), Pitfall 4 (8-action mask), Pitfall 5 (RespawnData/GlobalPos).
    - server/play_join.go — levelsEncoder + commonPlayerSpawnInfoEncoder (the custom-FieldEncoder pattern to mirror for the PlayerInfoUpdate collection + RespawnData record).
    - server/play_join_test.go — TestLoginPacketWireLayout (the field-by-field decode pattern to mirror).
  </read_first>
  <behavior>
    PLAY-05 (tab-list self-entry — 8-action mask):
    - TestPlayerInfoUpdateWire: writePlayerInfoUpdateAdd(uuid, "Steve", 0) → id == ClientboundPlayerInfoUpdate; first byte == 0x0D (ADD_PLAYER|UPDATE_GAME_MODE|UPDATE_LISTED); collection count == 1; entry decodes as UUID, String "Steve", VarInt 0 (properties), VarInt 0 (gameMode), Boolean true (listed) in that order.
    PLAY-01 (the simple tail encoders):
    - TestPlayerAbilitiesWire: writePlayerAbilities(false,false,false,false,0.05,0.1) → id ClientboundPlayerAbilities; Byte 0x00; Float 0.05; Float 0.1.
    - TestSetHeldSlotWire: writeSetHeldSlot(0) → id ClientboundSetHeldSlot; VarInt 0.
    - TestSetDefaultSpawnPositionWire: writeSetDefaultSpawnPosition(...) → id ClientboundSetDefaultSpawnPosition; the RespawnData/GlobalPos field framing decodes (dimension Identifier + BlockPos Long + Float + Float) — assert the framing; exact bytes are sealed by Plan 05-03's capture-diff.
  </behavior>
  <action>
    1. server/play_join.go — add the action-mask consts (piuAddPlayer=0x01, piuUpdateGameMode=0x04, piuUpdateListed=0x08) and four builders:
       - writePlayerAbilities(invuln, flying, canFly, instabuild bool, flySpeed, walkSpeed float32) pk.Packet — Byte flags + Float + Float (05-RESEARCH Code Examples).
       - writeSetHeldSlot(slot int32) pk.Packet — VarInt slot.
       - writePlayerInfoUpdateAdd(id uuid.UUID, name string, gameMode int32) pk.Packet — pk.Byte(mask) + a custom collection FieldEncoder (VarInt(1) + the self-entry in enum order) mirroring levelsEncoder. Comment the entry sub-encoding as a capture-diff candidate.
       - writeSetDefaultSpawnPosition(...) pk.Packet — a custom RespawnData FieldEncoder (GlobalPos = dimension Identifier + BlockPos Long; then two Floats) mirroring commonPlayerSpawnInfoEncoder. Comment it as a 26.x-restructure capture-diff candidate.
    2. server/play_join_test.go — add the four wire tests in <behavior>, decoding field-by-field like TestLoginPacketWireLayout.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestPlayerInfoUpdateWire|TestPlayerAbilitiesWire|TestSetHeldSlotWire|TestSetDefaultSpawnPositionWire' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The four tail builders exist; PlayerInfoUpdate writes the 1-byte 8-action mask (0x0D) + a count-prefixed self-entry in enum order; abilities/held-slot encode their jar-confirmed simple layouts; SetDefaultSpawnPosition encodes the RespawnData/GlobalPos framing.
    - Each wire test decodes the packet field-by-field and asserts the id + values/framing.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The early-Play tail encoders exist and round-trip on the wire — the player can be made a real, listed player (PLAY-05) with abilities/held-slot/spawn-pos (PLAY-01). The uncertain entry sub-encoding + RespawnData bytes are flagged for the Plan-05-03 capture-diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append the tail + issue the incrementing teleport id in the bootstrap; thread real identity from AcceptPlayer (PLAY-01/02/03/05)</name>
  <files>server/play_join.go, server/gameplay_tick.go, server/play_join_test.go</files>
  <read_first>
    - .planning/phases/05-player-session-in-world-first-playable/05-RESEARCH.md — Pattern 3 (incrementing teleport id threaded into awaitingTeleport), Pattern 5 (tail ordering before register).
    - server/play_join.go — sendPlayBootstrap (the 3-packet send to extend) + the const initialTeleportID/joinEntityID to replace.
    - server/gameplay_tick.go — AcceptPlayer (where name/id arrive and the tickPlayer is built + registered) + the existing TestJoinSequenceOrdering invariant.
    - server/tick.go — tickPlayer.awaitingTeleport (Plan 05-01 field) to set.
  </read_first>
  <behavior>
    PLAY-01/05 (tail ordering): sendPlayBootstrap enqueues Login → GameEvent → PlayerPosition → PlayerAbilities → SetHeldSlot → PlayerInfoUpdate(self) → SetDefaultSpawnPosition, IN THAT ORDER, all before loop.register.
    - TestBootstrapTailOrdering: drive sendPlayBootstrap against a piped client (or the existing AcceptPlayer harness), drain the clientbound packets, and assert the exact 7-packet order (the 3 existing + the 4 tail), with the tail after PlayerPosition and the whole set before any chunk packet.
    PLAY-02 (incrementing id):
    - TestBootstrapTeleportID: two successive joins issue DISTINCT incrementing teleport ids; each player's tickPlayer.awaitingTeleport equals the id carried in its bootstrap PlayerPosition; a subsequent ServerboundAcceptTeleportation echoing that id confirms (drives the Plan-05-01 gate) and a different id does not.
    PLAY-03 (completeness — already built in Phase 4, confirm not regressed): the bootstrap still sends the GameEvent LEVEL_CHUNKS_LOAD_START before the chunk stream, and the Phase-4 streamer still fills the radius>=2 ring; the existing TestJoinSequenceOrdering (Login + LOAD_START precede SetChunkCacheCenter/LevelChunkWithLight) stays green after the tail is appended.
  </behavior>
  <action>
    1. server/play_join.go — change sendPlayBootstrap to (a) accept the player's real name/uuid (and entity id / game mode) + the issued teleport id (extend the signature or pass a small bootstrapParams struct), (b) drive writePlayerPositionPacket with the issued incrementing id (not initialTeleportID), (c) APPEND writePlayerAbilities → writeSetHeldSlot → writePlayerInfoUpdateAdd(uuid, name, gameModeSurvival) → writeSetDefaultSpawnPosition AFTER the existing PlayerPosition send. Keep the const initialTeleportID only if still referenced (prefer removing it once the incrementing id is wired; leave joinEntityID for Login unless you thread a real entity id).
    2. server/gameplay_tick.go — in AcceptPlayer: issue the incrementing teleport id (a per-gameTick atomic counter — document the choice), pass name/id + that id into sendPlayBootstrap, and set the constructed tickPlayer.awaitingTeleport to the SAME id (and confirmedTeleport=false). Thread name/id (already received) through; optionally assign a per-player entity id. Keep the send BEFORE g.loop.register <- player (the FIFO invariant). Mutate no tick-owned state off-tick (the tickPlayer is handed over via register).
    3. server/play_join_test.go — add TestBootstrapTailOrdering + TestBootstrapTeleportID. For the teleport-id test, reuse the Plan-05-01 dispatch path (wire the player into clientIndex, call dispatch with an AcceptTeleportation echoing awaitingTeleport, assert confirmedTeleport flips only on a match).
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestBootstrapTailOrdering|TestBootstrapTeleportID|TestJoinSequenceOrdering|TestTeleportGate' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - sendPlayBootstrap enqueues the 7-packet sequence in order (3 existing + 4 tail), all before loop.register; TestJoinSequenceOrdering still passes (Login still precedes chunks).
    - The bootstrap issues an incrementing per-player teleport id, threads it into the PlayerPosition AND tickPlayer.awaitingTeleport; distinct joins get distinct ids; the Plan-05-01 gate confirms only on a matching echo.
    - Real login name/uuid feed the self tab-list entry; no tick-owned state is mutated off-tick (TICK-05 preserved).
    - PLAY-03 not regressed: LEVEL_CHUNKS_LOAD_START still precedes the chunk stream and the radius>=2 ring still streams (TestJoinSequenceOrdering green) after the tail is appended.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>A joining player now receives the full early-Play set (PLAY-01), appears in its own tab list (PLAY-05), and carries a server-issued incrementing teleport id the movement gate validates (PLAY-02) — all by extending the proven bootstrap before register, not rebuilding it. Run the Docker -race gate over ./server/... before marking the wave done.</done>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestPlayerInfoUpdateWire|TestPlayerAbilitiesWire|TestSetHeldSlotWire|TestSetDefaultSpawnPositionWire|TestBootstrapTailOrdering|TestBootstrapTeleportID' -count=1` passes.
- `go test ./server/ -run 'TestJoinSequenceOrdering|TestLoginPacketWireLayout|TestPlayerPositionPacketWireLayout|TestTeleportGate' -count=1` passes (the existing bootstrap is extended, not regressed; the incrementing id drives the Plan-05-01 gate).
- Docker `-race` gate over `./server/...` is clean (the bootstrap is sent off-tick through the bounded queue; the tickPlayer crosses via register — TICK-05).
- `go vet ./... && go build ./...` clean; zero new dependencies.
</verification>

<success_criteria>
- PLAY-01 (early-Play tail): a joining client receives PlayerAbilities → SetHeldSlot → PlayerInfoUpdate(self) → SetDefaultSpawnPosition after the existing Login/GameEvent/PlayerPosition, all before register (FIFO invariant preserved).
- PLAY-05 (tab list): the joining player appears in its own tab list via the 8-action-mask self-entry keyed on the real UUID/name.
- PLAY-02 (producer): the bootstrap issues an incrementing per-player teleport id threaded into awaitingTeleport, so the Plan-05-01 gate validates the client's Confirm Teleportation echo.
- The uncertain encoders (PlayerInfoUpdate entry, SetDefaultSpawnPosition RespawnData) are flagged for the Plan-05-03 capture-diff; SetTime is left out unless the real-client test demands it.
</success_criteria>

<output>
After completion, create `.planning/phases/05-player-session-in-world-first-playable/05-02-SUMMARY.md`.
</output>
