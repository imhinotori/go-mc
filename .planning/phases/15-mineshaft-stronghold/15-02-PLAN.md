---
phase: 15-mineshaft-stronghold
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - world/structure/concentric_rings.go
  - world/structure/concentric_rings_test.go
  - world/structure/structure_set.go
  - world/levelgen/data/embed.go
  - tools/extract_worldgen.go
  - world/noisegen.go
requirements: [STRUCT-04]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The concentric_rings placement is GLOBAL, not per-chunk (Pitfall #5 — the architectural risk this plan isolates). A ConcentricRingsStructurePlacement (ported javap -c net.minecraft.world.level.levelgen.structure.placement.ConcentricRingsStructurePlacement against temp/cache/26.2-inner.jar) parses the strongholds structure_set (world/levelgen/data/structure_set/strongholds.json — VERIFIED on disk: type concentric_rings, count 128, distance 32, spread 3, preferred_biomes #minecraft:stronghold_biased_to, salt 0) and computes the ~128 ring chunk positions ONCE for the world (NOT a per-chunk decision — you CANNOT decide is-this-a-stronghold-chunk locally). The positions are a spiral over rings: ported generateRingPositions (javap -c net.minecraft.world.level.chunk.ChunkGeneratorStructureState.generateRingPositions) — for each ring r in [0,distance): place `count`-distributed positions at increasing radius, each position's angle advanced by a seeded RNG, the chunk pos = (round(cos*dist), round(sin*dist)) snapped, then ADJUSTED to the nearest valid biome (next truth). The exact ring/count distribution + the radius/angle math + the rounding is bit-exact (a draw-order or rounding divergence moves every stronghold)."
    - "The ring RNG is seeded from a DEDICATED concentricRingsSeed (NOT the structure salt path the temples/mineshaft use). Port the exact derivation (javap -c ChunkGeneratorStructureState — the WorldgenRandom seeded via setLargeFeatureSeed/a dedicated seed derived from the world seed for the ring spiral; vanilla uses a RandomState/WorldgenRandom.fromHashOf or the world seed directly into the ring RNG — port the EXACT seeding call the bytecode shows). The seed derivation is pinned by a test: a known world seed → the exact ~128 ring positions (derived FROM generateRingPositions, the reference list computed by the algorithm in-test, not eyeballed)."
    - "Each ring position is BIOME-VALIDATED against #minecraft:stronghold_biased_to (Pitfall #5): the ring spiral, at each candidate position, queries the biome source for a valid biome in the preferred set and ADJUSTS the position to the nearest one within a search radius (port the jar's biome-search loop — getBiome over a spiral/grid of sample points, accept the first whose biome ∈ the tag set). This makes ring generation DEPEND on the (already-ported) multi-noise biome source — thread biomeAt through. The #stronghold_biased_to tag is a NEW embed: it is NOT currently embedded (only has_structure/stronghold is — VERIFIED: world/levelgen/data/tags/worldgen/biome/has_structure/ has stronghold but the extractor only pulls has_structure/, not the bare stronghold_biased_to tag at data/minecraft/tags/worldgen/biome/stronghold_biased_to.json which EXISTS in server/registrydata). Extend tools/extract_worldgen.go's prefix list to pull tags/worldgen/biome/ (the bare biome tags incl. stronghold_biased_to) + extend embed.go + the loader to expose it as a biome-name set (the 38-biome list VERIFIED in server/registrydata/tags/worldgen/biome/stronghold_biased_to.json)."
    - "The ring positions are CACHED at worldgen-state init (computed ONCE, not per chunk) and exposed as isPlacementChunk(pos) = the precomputed ring-list contains pos (ported ConcentricRingsStructurePlacement.isPlacementChunk → getRingPositionsFor().contains). DECISION (the concentric-rings worldgen-state home): the ring list lives in a per-world StrongholdRingState struct held by world/structure (constructed once, lazily-but-once via sync.Once so the ~128-position biome-validated spiral — which is EXPENSIVE: many GetBiome calls — runs a single time for the world, not per chunk and not per worker goroutine). The NoiseGenerator OWNS the StrongholdRingState (constructed in NewNoiseGenerator alongside the structCache, fed the world seed + the biomeAt seam + the embedded stronghold_biased_to set), keeping world→world/structure one-directional (no cycle, the established Phase-14 dependency direction). The state is goroutine-safe (the cached []ChunkPos is immutable after the sync.Once init; reads are lock-free). isPlacementChunk is an O(rings) or O(1)-via-set membership test over the cached list."
    - "The stronghold StartGenerator placement HALF is wired (the PLACEMENT decision only — the pieces are 15-03): a strongholdStartGen.GenerateStarts gates on StrongholdRingState.isPlacementChunk(pos) (the ring-list contains test) INSTEAD of the per-chunk spacing/frequency math the temples/mineshaft use. On a ring chunk it produces a start ANCHOR (Structure minecraft:stronghold, ChunkPos pos, the surface-sampled Y, the start RNG seeded via SetLargeFeatureSeed) with the piece assembly STUBBED (an empty/placeholder piece set this plan, FILLED by 15-03); on a non-ring chunk it produces nil. This plan proves the GLOBAL placement model in isolation (the architectural risk) before the recursive pieces hang on it — mirroring how 14-01 landed the pipeline before any geometry."
    - "Determinism + isolation hold: the ring list is pure over (worldSeed) (same seed → same ~128 positions → same isPlacementChunk answers); the strongholdStartGen is pure over (seed,pos). The existing temple+mineshaft acceptance + the 5x5 reorder/emit-once gates stay green WITH the stronghold placement registered (the empty-piece stub writes NO blocks this plan — same byte-inert discipline 14-01 used for the empty PLACE hook)."
  artifacts:
    - path: "world/structure/concentric_rings.go"
      provides: "ConcentricRingsStructurePlacement (count/distance/spread/salt parse) + generateRingPositions (the seeded biome-validated spiral) + StrongholdRingState (the sync.Once-cached per-world ring list + isPlacementChunk contains-test) + the placement-half strongholdStartGen (ring-gate + anchor start, piece set stubbed)"
      contains: "generateRingPositions"
      min_lines: 150
    - path: "world/structure/concentric_rings_test.go"
      provides: "the ring acceptance: a known world seed → the exact ~128 ring positions (derived from the algorithm) + isPlacementChunk contains/not-contains + the biome-validation adjustment (a position moved to the nearest #stronghold_biased_to biome) + the sync.Once single-compute + the placement purity"
      contains: "TestConcentricRings"
      min_lines: 100
    - path: "world/structure/structure_set.go"
      provides: "the concentric_rings structure_set parse (count/distance/spread/preferred_biomes/salt) + the stronghold_biased_to biome-tag loader (the NEW bare-biome-tag embed)"
      contains: "concentric_rings"
    - path: "tools/extract_worldgen.go"
      provides: "the extractor prefix extension pulling tags/worldgen/biome/ (the bare biome tags incl. stronghold_biased_to) so the preferred_biomes set is embeddable"
      contains: "stronghold_biased_to"
    - path: "world/noisegen.go"
      provides: "the StrongholdRingState constructed ONCE in NewNoiseGenerator (world seed + biomeAt + the embedded preferred set) + the strongholdStartGen registered into the CompositeStartGenerator (placement-half, byte-inert pieces this plan)"
      contains: "StrongholdRingState"
  key_links:
    - from: "world/structure/concentric_rings.go generateRingPositions"
      to: "world/levelgen/biome/source.go MultiNoiseBiomeSource.GetBiome (via the biomeAt seam)"
      via: "each ring position is adjusted to the nearest #stronghold_biased_to biome — ring gen DEPENDS on the multi-noise biome source (Pitfall #5)"
      pattern: "biomeAt|GetBiome"
    - from: "world/structure/concentric_rings.go StrongholdRingState"
      to: "sync.Once (the per-world single-compute)"
      via: "the expensive ~128-position biome-validated spiral runs ONCE for the world, not per chunk/per worker — cached immutable []ChunkPos read lock-free"
      pattern: "sync.Once"
    - from: "world/structure/concentric_rings.go isPlacementChunk"
      to: "the cached ring-position list"
      via: "is-this-a-stronghold-chunk = the precomputed list contains pos (GLOBAL, not the per-chunk spacing math)"
      pattern: "isPlacementChunk|contains"
    - from: "world/noisegen.go NewNoiseGenerator"
      to: "world/structure/concentric_rings.go NewStrongholdRingState"
      via: "the NoiseGenerator owns the ring state (constructed once, fed seed+biomeAt+preferred-set), keeping world→world/structure one-directional"
      pattern: "StrongholdRingState"
---

<objective>
Port the STRONGHOLD PLACEMENT (STRUCT-04, the architectural-risk half) in ISOLATION — the unique `concentric_rings` model — before the recursive pieces (15-03) hang on it. This mirrors 14-01's discipline (land the pipeline before geometry). Pitfall #5 is the whole job:

1. **Global, not per-chunk**: the ~128 ring chunk positions are computed ONCE for the world via a seeded spiral (`generateRingPositions`), not by a local is-this-a-stronghold-chunk test. `isPlacementChunk(pos)` = the precomputed list contains `pos`.

2. **A dedicated ring seed + biome validation**: the spiral RNG is seeded from a dedicated derivation (not the temple/mineshaft salt path), and EACH ring position is adjusted to the nearest `#stronghold_biased_to` biome — making ring gen DEPEND on the already-ported multi-noise biome source. The `#stronghold_biased_to` tag is a NEW embed (only `has_structure/stronghold` is currently embedded).

3. **The worldgen-state home (the decision to lock)**: the ring list lives in a per-world `StrongholdRingState` (held by `world/structure`, owned + constructed ONCE by the `NoiseGenerator` via `sync.Once`, fed the seed + biomeAt + preferred set). The expensive biome-validated spiral runs a single time for the world — not per chunk, not per worker. `world→world/structure` stays one-directional (no cycle). The stronghold StartGenerator gates on `isPlacementChunk` with the PIECE assembly STUBBED (byte-inert this plan; 15-03 fills it).

Purpose: de-risk the architecturally-unique placement model in isolation, fully tested + deterministic, so 15-03's recursive pieces (the "more of the same" half) hang on a proven global-ring foundation.
Output: world/structure/concentric_rings.go (placement + ring state + stub generator) + the acceptance test + the structure_set/biome-tag loader + the extractor/embed extension + the noisegen ring-state wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v2-structures.md

# Phase-14 pipeline (the StartGenerator + cache + biome seam this hangs on):
@.planning/phases/14-structure-pipeline-temples/14-01-SUMMARY.md
# 15-01 (the mineshaft — read for the StartGenerator registration + the surface-sampler/biome-gate pattern this reuses):
@.planning/phases/15-mineshaft-stronghold/15-01-PLAN.md

<interfaces>
<!-- The seams this plan threads. -->

From world/structure/cache.go (the StartGenerator + biome seam — the stronghold gate replaces the spacing math with ring membership):
```go
type BiomeAt func(wx, wy, wz int) levelbiome.Type
type SurfaceSampler interface { SampleSurfaceY(wx, wz int) int }
type StartGenerator interface {
    GenerateStarts(seed int64, pos level.ChunkPos, sampler SurfaceSampler, biomeAt BiomeAt) []*StructureStart
}
// NewCache(sampler, biomeAt) — constructed in NewNoiseGenerator; the cache memoizes GenerateStarts pure over (seed,pos)
```

From world/structure/structure_set.go + world/levelgen/data/embed.go:
```go
func LoadStructureSet(id string) (StructureSet, error)        // "strongholds" already embedded (concentric_rings — extend the parse)
func HasStructureBiomes(structureID string) (map[string]bool, error)  // biome-id-name set
// NEW: a stronghold_biased_to loader returning the preferred-biome name set (extend embed + extractor)
// HasStructureBiomeTag(id) accessor pattern (embed.go:208) — mirror it for the bare biome tag
```

From world/levelgen/biome/source.go (the multi-noise source the ring validation queries — via the biomeAt seam):
```go
func (s *MultiNoiseBiomeSource) GetBiome(x, y, z int) levelbiome.Type   // public; NoiseGenerator threads it as biomeAt
```

From level/biome/list.go (Type → id name, for tag membership):
```go
func (t Type) String() string   // "minecraft:plains" etc — test membership: preferredSet[biomeAt(x,64,z).String()]
```

From world/levelgen/random.go (RNG seeders — port the EXACT ring-seed derivation the jar uses):
```go
func (w *WorldgenRandom) SetLargeFeatureSeed(worldSeed int64, chunkX, chunkZ int)
func (w *WorldgenRandom) SetSeed(seed int64)
// the ring spiral uses a DEDICATED seed — port the exact ChunkGeneratorStructureState derivation (NOT the structure salt)
```

From world/structure/start.go:
```go
type StructureStart struct { Structure string; ChunkPos level.ChunkPos; Pieces []Piece; BBox BoundingBox; References int }
type BoundingBox struct { MinX,MinY,MinZ,MaxX,MaxY,MaxZ int }
```

From world/noisegen.go (the wiring site — the NoiseGenerator owns the structCache + biomes; ADD the ring state here):
```go
// NewNoiseGenerator(seed,secs,minY): builds router → biomes (MultiNoiseBiomeSource) → structCache (NewCache(sampler,biomeAt))
//   → CompositeStartGenerator(desert,jungle,igloo,swamp, [15-01: mineshaft]) → registered as structGen.
// ADD: ringState := structure.NewStrongholdRingState(seed, biomeAt, preferredSet) constructed ONCE here;
//      strongholdGen := structure.NewStrongholdStartGen(ringState, ...); append to the composite.
```
</interfaces>

# The embedded data (VERIFIED on disk):
@world/levelgen/data/structure_set/strongholds.json
@world/levelgen/data/structure/stronghold.json
# The preferred-biome tag content (VERIFIED present in server registrydata; NOT YET embedded — this plan embeds it):
@server/registrydata/tags/worldgen/biome/stronghold_biased_to.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the stronghold_biased_to embed + the concentric_rings structure_set parse + generateRingPositions (the seeded biome-validated spiral)</name>
  <files>tools/extract_worldgen.go, world/levelgen/data/embed.go, world/structure/structure_set.go, world/structure/concentric_rings.go, world/structure/concentric_rings_test.go</files>
  <behavior>
    - Test: the strongholds structure_set parses as concentric_rings with count 128, distance 32, spread 3, salt 0, preferred_biomes #stronghold_biased_to (assert each field vs the embedded JSON).
    - Test: the stronghold_biased_to tag loads as the 38-biome name set (assert membership: plains/desert/taiga IN, ocean/nether biome NOT).
    - Test: generateRingPositions(knownSeed, biomeStub) → the exact ~128 ring positions; the reference list computed BY the algorithm in-test (radius/angle/round math), asserted bit-equal (a draw-order/rounding divergence fails). With a biome stub that rejects the first N sample points, a position is ADJUSTED to the nearest accepted one (the biome-search loop fires).
    - Test: the seed derivation is the dedicated ring seed (NOT the structure salt) — pinned by the exact derived value.
  </behavior>
  <action>FIRST extend tools/extract_worldgen.go's worldgenZipPrefixes to pull `data/minecraft/tags/worldgen/biome/` (the bare biome tags — the current list only pulls has_structure/; add the parent biome-tag dir so stronghold_biased_to is extracted) and extend world/levelgen/data/embed.go to embed + expose it (mirror the HasStructureBiomeTag accessor at embed.go:208 for a bare biome tag). Add a loader in structure_set.go returning the stronghold_biased_to biome-name set (map[string]bool over the 38 entries). Extend the structure_set parse to handle type concentric_rings (count/distance/spread/preferred_biomes/salt) alongside random_spread. Then port generateRingPositions (javap -c net.minecraft.world.level.chunk.ChunkGeneratorStructureState.generateRingPositions + ConcentricRingsStructurePlacement): seed a WorldgenRandom via the DEDICATED ring-seed derivation (port the EXACT seeding call — decompile to confirm whether it's setLargeFeatureSeed, a fromHashOf, or the world seed directly), then the spiral: for each ring, distribute `count`-apportioned positions at increasing radius with a seeded angle advance, round each to a ChunkPos, and ADJUST to the nearest #stronghold_biased_to biome via a biomeAt search loop (port the jar's sample-search). Take biomeAt as a parameter (the seam) so the test can stub it. Return []level.ChunkPos. Pin all of it with the known-seed reference + the biome-adjustment + the dedicated-seed tests.</action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestConcentricRingsParse|TestStrongholdBiasedTo|TestGenerateRingPositions|TestRingSeed' -count=1 && cd D:/ender && go build ./tools/...</automated>
  </verify>
  <done>The stronghold_biased_to tag is embedded + loaded; the concentric_rings set parses; generateRingPositions produces the exact ~128 biome-validated positions for a known seed via the dedicated ring seed, pinned by the algorithm-derived reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the StrongholdRingState (sync.Once per-world cache) + isPlacementChunk + the placement-half strongholdStartGen (piece-stubbed) + the noisegen ring-state wiring</name>
  <files>world/structure/concentric_rings.go, world/structure/concentric_rings_test.go, world/noisegen.go</files>
  <behavior>
    - Test: StrongholdRingState computes the ring list EXACTLY ONCE (sync.Once) even under concurrent isPlacementChunk calls from many goroutines (a compute-count probe asserts 1; -race clean); the cached []ChunkPos is immutable + read lock-free.
    - Test: isPlacementChunk(pos) is true for every ring position, false for a non-ring chunk (the contains-test); pure over (worldSeed).
    - Test: strongholdStartGen.GenerateStarts on a ring chunk → one anchor start (Structure minecraft:stronghold, ChunkPos==pos, empty/placeholder Pieces — byte-inert this plan); on a non-ring chunk → nil; pure over (seed,pos).
    - Test (in ./world/): the 5x5 reorder + emit-once + the temple/mineshaft acceptance stay byte-identical WITH the stronghold placement registered (the stubbed pieces write NO blocks).
  </behavior>
  <action>Implement StrongholdRingState in concentric_rings.go: a struct holding the world seed, the biomeAt seam, the preferred-biome set, a sync.Once, and the cached []level.ChunkPos. NewStrongholdRingState(seed, biomeAt, preferredSet) constructs it WITHOUT computing (lazy); the first isPlacementChunk (or an explicit Ensure()) runs generateRingPositions ONCE under the sync.Once (the expensive biome-validated spiral — many GetBiome calls — must not run per chunk or per worker). isPlacementChunk(pos) = the cached list contains pos (use a packed-pos set for O(1) membership if the ~128 linear scan shows in profiling; a set built once in the Once is simplest + lock-free). Implement strongholdStartGen implementing StartGenerator: GenerateStarts gates on ringState.isPlacementChunk(pos) INSTEAD of any spacing/frequency math; on a ring chunk, seed the start RNG via SetLargeFeatureSeed(seed,pos.X,pos.Z), sample the surface Y, and return a single anchor StructureStart with Structure "minecraft:stronghold", ChunkPos pos, and a STUBBED/empty piece set (15-03 fills it — document the stub inline, mirroring 14-01's empty PLACE hook); else nil. Wire world/noisegen.go: in NewNoiseGenerator construct `ringState := structure.NewStrongholdRingState(seed, biomeAt, structure.LoadStrongholdBiasedTo())` ONCE (after biomeAt is built), construct strongholdGen over it, and append it to the CompositeStartGenerator (alongside the temples + the 15-01 mineshaft). Keep world→world/structure one-directional. Add the once/concurrency/contains/purity tests + re-run the ./world/ determinism gate.</action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestStrongholdRingState|TestIsPlacementChunk|TestStrongholdStart' -count=1 && go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce|TestStructuresPipelineAcceptance|TestMineshaft' -count=1</automated>
  </verify>
  <done>The ring list computes exactly once per world (sync.Once, -race clean); isPlacementChunk is the global contains-test; the stronghold placement-half emits an anchor start on ring chunks (pieces stubbed, byte-inert); the determinism + temple/mineshaft gates stay green with the stronghold placement registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structure_set JSON → ring computation | the concentric_rings params (count/distance/spread/seed) drive WHERE all ~128 strongholds are; a divergence moves every stronghold (non-reproducible vs vanilla) |
| biome source → ring validation | each ring position depends on GetBiome (the preferred-biome adjustment) — a wrong tag/search desyncs positions |
| sync.Once cache → many worker goroutines | the expensive spiral must run once, immutably, read concurrently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | generateRingPositions diverging from vanilla (wrong radius/angle/round math, wrong dedicated-seed derivation, wrong count distribution) → every stronghold in the wrong place | mitigate | The spiral + the dedicated ring seed are javap -c transcribed; TestGenerateRingPositions pins the exact ~128 positions for a known seed (the reference computed by the algorithm, not eyeballed); TestRingSeed pins the derivation. |
| T-15-08 | Tampering | the biome-validation adjustment wrong (wrong tag, wrong search loop) → a ring position not snapped to a #stronghold_biased_to biome | mitigate | The stronghold_biased_to tag is embedded + loaded (the 38-biome set pinned); the biome-search loop is ported javap -c; TestGenerateRingPositions with a rejecting biome stub asserts the position adjusts to the nearest accepted one. |
| T-15-09 | DoS | the expensive biome-validated spiral re-running per chunk or per worker goroutine (the ~128-position search is many GetBiome calls) | mitigate | StrongholdRingState computes ONCE via sync.Once; TestStrongholdRingState pins compute-count==1 under concurrent access; the cached []ChunkPos is immutable + read lock-free; -race clean. |
| T-15-10 | Integrity (non-reproducible world) | the same seed yielding different ring positions / placement answers | mitigate | The ring list is pure over (worldSeed); the strongholdStartGen is pure over (seed,pos); TestIsPlacementChunk + TestStrongholdStart pin purity; the 5x5/emit-once gates re-run byte-identical with the placement registered. |
| T-15-11 | Tampering | a world↔world/structure import cycle from putting the ring state on the NoiseGenerator | mitigate | StrongholdRingState lives in world/structure; the NoiseGenerator constructs it via NewStrongholdRingState(seed, biomeAt func, preferredSet) — the dependency is one-directional (world→world/structure), the established Phase-14 direction. go build proves no cycle. |
</threat_model>

<verification>
- `go test ./world/structure/ -run 'TestConcentricRings|TestGenerateRingPositions|TestStrongholdBiasedTo|TestStrongholdRingState|TestIsPlacementChunk|TestStrongholdStart|TestRingSeed'` green: the concentric_rings parse + the exact ~128 biome-validated ring positions + the sync.Once single-compute + isPlacementChunk contains + the placement-half anchor + purity.
- `go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce|TestStructuresPipelineAcceptance|TestMineshaft'` green: the determinism + temple/mineshaft gates stay byte-identical WITH the stronghold placement registered (pieces stubbed → byte-inert).
- `go build ./tools/...` green (the extractor extension compiles); the stronghold_biased_to tag embeds.
- Docker `-race` clean across `./world/... ./world/structure/...` on golang:1.26 (CGO_ENABLED=0): the sync.Once ring compute is race-free under concurrent worker access.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` + `go vet ./world/...` clean; NO new deps; NO import cycle (world→world/structure only); NO encoder/packet/chunk-wire/golden file touched.
</verification>

<success_criteria>
- STRUCT-04 success criterion 2: the stronghold's concentric_rings placement is ported — the ~128 biome-validated ring positions precomputed once at worldgen-state init, isPlacementChunk = ring-list contains.
- The ring computation is GLOBAL (not per-chunk), seeded from the dedicated ring seed, biome-validated against #stronghold_biased_to (the multi-noise biome source threaded through).
- The worldgen-state home is locked: StrongholdRingState in world/structure, owned + constructed ONCE (sync.Once) by the NoiseGenerator, one-directional dependency.
- The placement is proven in isolation (pieces stubbed, byte-inert) — the architectural risk de-risked before 15-03's pieces hang on it.
- The 5x5 determinism + emit-once stay byte-identical; Docker -race clean. NO human-verify gate.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mineshaft-stronghold/15-02-SUMMARY.md` documenting: the dedicated ring-seed derivation (the exact call), the generateRingPositions math + the biome-validation search, the StrongholdRingState worldgen-state home + the sync.Once decision, the stronghold_biased_to embed extension, and the strongholdStartGen anchor (the piece-stub seam 15-03 fills).
</output>
