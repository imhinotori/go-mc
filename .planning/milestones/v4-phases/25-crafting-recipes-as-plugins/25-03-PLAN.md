---
phase: 25-crafting-recipes-as-plugins
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - server/cooking_block.go
  - server/stonecutter_menu.go
  - server/cooking_test.go
  - .planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md
autonomous: true
requirements: [PLUGIN-05]

must_haves:
  truths:
    - "Each cooking/stonecutting recipe type (smelting, blasting, smoking, campfire_cooking, stonecutting) is AUDITED for build-or-defer against what its block/menu/tick subsystem needs versus what exists in the codebase"
    - "Where a block+menu+tick is faithfully buildable in budget, the minimal 1:1 slice is BUILT (the block opens its menu, the matcher drives the result, the cooking tick / single-input transform runs)"
    - "Where too large, the matcher (already shipped + tested in Plan 01) is kept, and the BLOCK/menu is DEFERRED with a CITED reason in deferred-blocks.md (jar class + missing subsystem + codebase evidence) — no fake furnace"
    - "Every per-type decision (build vs defer) is recorded with evidence; no recipe type is silently dropped"
  artifacts:
    - path: ".planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md"
      provides: "the per-type build-or-defer decision log with jar-class + missing-subsystem + codebase-evidence citations for every deferral"
      contains: "## Per-Type Decisions"
  key_links:
    - from: "server/cooking_block.go (IF built)"
      to: "plugin/host.Manager.Match (smelting/cooking matcher)"
      via: "the cooking tick drives the smelting matcher result"
      pattern: "plugins\\.Match|deferred-blocks"
---

<objective>
Resolve the operator's "todos los tipos incl smelting" mandate for the BLOCK side: audit each cooking/stonecutting recipe type and, per the no-built-but-unwired rule, BUILD the minimal 1:1 block+menu+tick where feasible within budget, or SHIP-the-matcher-and-DEFER-the-block with a cited reason. The recipe DATA + MATCH for all these types already shipped + tested in Plan 01 — this plan is the BLOCK/menu/tick decision and any feasible builds.

Purpose: The operator wants as much of the full crafting system as can be built 1:1 this phase. Crafting-grid (shaped/shapeless) is done in Plan 02. Smelting/cooking needs a furnace block-entity + a fuel+progress cooking tick (a NEW subsystem); stonecutting needs the stonecutter block + a single-input menu. This plan decides each ON EVIDENCE (read what each needs vs what exists), builds the buildable, and cites every deferral — never fakes a furnace.

Output: any built cooking/stonecutter block+menu+tick slices (1:1, jar-cited, tested) AND deferred-blocks.md recording every per-type decision with citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-crafting-recipes-as-plugins/25-RESEARCH.md
@.planning/phases/25-crafting-recipes-as-plugins/25-CONTEXT.md
@.planning/phases/25-crafting-recipes-as-plugins/25-VALIDATION.md
@CLAUDE.md
@.planning/phases/25-crafting-recipes-as-plugins/25-01-SUMMARY.md
@.planning/phases/25-crafting-recipes-as-plugins/25-02-SUMMARY.md

# The menu scaffolding to reuse for a stonecutter / furnace menu (read in full):
@server/chest_open.go
@server/crafting_menu.go

<interfaces>
<!-- Contracts the executor needs, from Plan 01 + Plan 02. -->
```go
// From plugin/host (Plan 01): the matchers for cooking/stonecutting already ship + are tested.
func (m *Manager) Match(grid starlark.Value) (id, count int, ok bool)
// level/recipe (Plan 01) already parses smelting/blasting/smoking/campfire_cooking/stonecutting.

// From server (Plan 02): the menu scaffolding to clone for a stonecutter/furnace menu.
type openContainer struct { windowID int; chestPos pk.Position; kind /* chest|crafting */; craftGrid [9]component.SlotData }
func (p *tickPlayer) nextContainerCounter() int
func menuTypeID(reg []string, name string) int32   // registryid.Menu indices
func (t *TickLoop) useBlockInteraction(...) bool    // extend for a new openable block
// registryid.Menu has "minecraft:stonecutter", "minecraft:furnace", "minecraft:blast_furnace", "minecraft:smoker".
// registryid.Block has "minecraft:furnace", "minecraft:blast_furnace", "minecraft:smoker", "minecraft:campfire", "minecraft:stonecutter".
```

The existing scheduled-block-tick subsystem is level/ticks/ (LevelTicks, ScheduledTick — see STATE.md
v3.1 SUB-BLOCKTICK). A furnace cooking tick (burn-time/cook-time progress) is the candidate subsystem
to evaluate: does it need NEW block-entity machinery (fuel slot, progress fields, lit state) that does
not exist, or can it ride the existing scheduled-tick / block-entity seams?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit each cooking/stonecutting type — build-or-defer decision with evidence</name>
  <files>.planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md</files>
  <read_first>
    - JAVAP the block/menu subsystems to size them:
      `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.block.entity.AbstractFurnaceBlockEntity` (the cooking tick: litTime/cookingProgress/cookingTotalTime, the fuel burn, serverTick) — size the NEW subsystem
      `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.inventory.StonecutterMenu` (the single-input menu — likely SMALLER, no tick)
      `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.inventory.FurnaceMenu` + `...CampfireBlockEntity`
    - server/crafting_menu.go + server/chest_open.go (the menu scaffolding a stonecutter/furnace menu would clone)
    - level/ticks/ (the existing scheduled-block-tick subsystem — does the furnace cooking tick fit it, or need new block-entity machinery? read STATE.md SUB-BLOCKTICK)
    - server/chest_open.go createBlockEntityOnPlace + decodeChestBE (the block-entity-on-place + decode pattern a furnace BE would need)
    - 25-CONTEXT.md "Recipe scope" (the no-built-but-unwired rule, the build-or-defer-per-block mandate) + 25-RESEARCH.md "State of the Art" (smelting/furnace deferral note)
  </read_first>
  <action>
    For EACH of the 5 types (smelting, blasting, smoking, campfire_cooking, stonecutting), determine ON EVIDENCE (the planner has NO authority to defer for "too hard" — only the 3 legitimate reasons: >50% context cost, missing information, dependency conflict; see CLAUDE.md + the planner authority rule):

    1. **What the block needs:** the block (exists in registryid.Block ✓), the block-entity (does a furnace/campfire BE exist? javap AbstractFurnaceBlockEntity to size its fields: fuel slot, lit state, cookingProgress, cookingTotalTime), the menu (clone crafting_menu.go / chest_open.go), and — for cooking — the COOKING TICK (burn-time/cook-time progress: a NEW subsystem, or does it ride level/ticks?).
    2. **What exists:** the matcher (✓ Plan 01), the menu scaffolding (✓ Plan 02 openContainer/sendContent/click engine), block-entity-on-place (✓ chest pattern createBlockEntityOnPlace), the scheduled-tick seam (✓ level/ticks).
    3. **The gap + the cost:** for stonecutting — a single-input menu + an immediate transform (NO tick, NO fuel) — likely a SMALL build (clone the menu, single-input -> Match -> result). For smelting/blasting/smoking/campfire — a furnace/campfire BLOCK-ENTITY with fuel + a progress cooking tick — size whether it exceeds ~50% of one agent's context (the legitimate context-cost reason to split/defer).

    Write `deferred-blocks.md` with a `## Per-Type Decisions` section: a row per type with **decision (BUILD | DEFER)**, **jar class(es)** cited (the BlockEntity/Menu javap'd), **the missing subsystem** (e.g. "furnace fuel+progress cooking tick — not in level/ticks; AbstractFurnaceBlockEntity.serverTick needs litTime/cookingProgress block-entity fields that do not exist"), **codebase evidence** (file:line of what exists / what's missing), and **the cost reason** for any defer (context-cost / missing-info / dependency — never "too hard"). The matcher-ships-block-defers split MUST be explicit: the recipe matcher (Plan 01) is ready + tested; the deferral is the BLOCK/menu/tick only.

    Recommended default (the executor confirms against the javap sizing, does NOT rubber-stamp): stonecutting is BUILDABLE (small, no tick) -> BUILD in Task 2; smelting/blasting/smoking/campfire DEFER the furnace/campfire block (the fuel+progress cooking-tick block-entity subsystem is a legitimate context-cost split, AND it's arguably a separate feature per ROADMAP "smelting needs a furnace block+menu+cook-tick subsystem") -> the matcher ships, the block defers WITH the citation. If the javap sizing shows the furnace tick fits in budget, BUILD the minimal slice instead — the operator wants as much built 1:1 as feasible. Record the actual decision.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md && grep -q '## Per-Type Decisions' .planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md && grep -vc '^#' .planning/phases/25-crafting-recipes-as-plugins/deferred-blocks.md</automated>
  </verify>
  <done>deferred-blocks.md records a build-or-defer decision for all 5 cooking/stonecutting types, each with a jar-class citation, the missing subsystem (for defers), codebase evidence, and a legitimate cost reason — no type silently dropped, no "too hard" justification. Acceptance: the file exists with `## Per-Type Decisions` and a row for each of smelting/blasting/smoking/campfire_cooking/stonecutting (`grep -v '^#' deferred-blocks.md | grep -c 'smelting\|blasting\|smoking\|campfire\|stonecutting'` >= 5).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the feasible block(s) (stonecutter and/or furnace per the Task-1 audit) — 1:1, jar-cited</name>
  <files>server/stonecutter_menu.go, server/cooking_block.go, server/cooking_test.go</files>
  <read_first>
    - The Task-1 deferred-blocks.md decision (build ONLY what Task 1 marked BUILD; do NOT speculatively build a deferred block — the no-built-but-unwired rule)
    - JAVAP the class(es) for the block(s) being built (StonecutterMenu and/or AbstractFurnaceBlockEntity per the audit) — match to bytecode
    - server/crafting_menu.go (the menu clone source: openContainer.kind, sendCraftingContent, the click engine, useBlockInteraction extension)
    - server/crafting_click.go (the single/grid -> Match -> result + onTake consume pattern to adapt for a single-input transform)
    - 25-RESEARCH.md "Pattern 5" (the chest/crafting menu clone) + the build-or-defer rule
  </read_first>
  <behavior>
    - IF stonecutting is BUILT (TestStonecutterMenu): right-clicking a stonecutter opens its single-input menu (openScreen minecraft:stonecutter); placing a stonecuttable input (e.g. stone) drives the matcher; selecting/taking the result transforms 1 input -> the result; close returns the input to the player. The result comes from Manager.Match (the plugin path), 1:1 with the jar single-input transform.
    - IF a furnace/cooking block is BUILT (TestCookingTick): the block opens its menu, fuel + input drive the cooking tick (litTime/cookingProgress per AbstractFurnaceBlockEntity.serverTick), the smelting matcher produces the result on cook completion, 1:1 with the javap'd cook tick.
    - IF a type is DEFERRED: a test asserts its MATCHER still works headless (TestSmeltingMatcherShips: feed the smelting matcher an iron_ore, assert iron_ingot) — proving the deferral is the BLOCK only, the recipe logic ships. (This test may live in level/recipe from Plan 01; if so, reference it; else add it here.)
  </behavior>
  <action>
    Build ONLY the block(s) Task 1 marked BUILD. For each, JAVAP the class first and match to bytecode; cite every method.

    **If stonecutting BUILD** (the likely small build): write `server/stonecutter_menu.go` cloning crafting_menu.go — `isStonecutterBlock`, extend useBlockInteraction, `openStonecutter` (openScreen minecraft:stonecutter), a single-input + result menu, the input feeds Manager.Match (the stonecutting matcher from Plan 01), a take transforms 1 input -> result (the 1:1 StonecutterMenu transform — note stonecutting consumes 1 input per result, no shrink-mirror complexity), close returns the input. Reuse the Plan-02 openContainer.kind discriminator (add a stonecutter kind).

    **If a cooking block BUILD** (only if the Task-1 audit sized it within budget): write `server/cooking_block.go` — the minimal 1:1 furnace/campfire block-entity + fuel/progress cooking tick (AbstractFurnaceBlockEntity.serverTick: litTime decrement, cookingProgress increment to cookingTotalTime, on completion run the smelting matcher result), riding level/ticks or a new minimal block-entity field set per the audit. Cite AbstractFurnaceBlockEntity.serverTick.

    **For every DEFERRED type:** add NO server code (the matcher already ships in Plan 01). Add the matcher-ships test (TestSmeltingMatcherShips etc.) if not already covered in level/recipe — feed the deferred type's matcher a known input, assert the jar-correct result, proving the deferral is the block/menu only.

    If Task 1's audit deferred ALL block builds (stonecutter included), this task reduces to the matcher-ships tests + a note — that is a VALID outcome (every block deferred with a cited reason, all matchers tested). Do not build a block the audit deferred.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestStonecutter|TestCooking|TestSmeltingMatcherShips' -v && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./server/</automated>
  </verify>
  <done>Every type Task 1 marked BUILD has a 1:1 jar-cited block+menu(+tick) that crafts through the plugin Match path and is tested; every DEFERRED type has a matcher-ships test proving its recipe logic works headless. No deferred block is speculatively built. Acceptance: `CGO_ENABLED=0 go test ./server/ -run 'TestStonecutter|TestCooking|TestSmeltingMatcherShips'` exits 0 (the run matches whatever was built/deferred per the audit); `CGO_ENABLED=0 go build ./...` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cooking/stonecutter menu (IF built) | The client sends ContainerClick; the input + fuel are server-authoritative; a forged windowId/slot is rejected (resend), reusing the Plan-02 menu discipline. |
| smelting matcher → result | The result comes from Manager.Match over server-built input — a client cannot inject a smelt result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-10 | Tampering | stonecutter/cooking consume (IF built) | mitigate | The single-input transform consumes exactly 1 input per result (1:1 StonecutterMenu / AbstractFurnaceBlockEntity); the result comes from Manager.Match, not the client. Tested by TestStonecutter/TestCooking. |
| T-25-11 | Availability | input items lost on close (IF built) | mitigate | The menu close returns the input/fuel to the player (reusing Plan-02's close-returns-grid), NOT silent deletion. |
| T-25-12 | Tampering | a deferred block silently dropping a recipe type | mitigate | Every deferred type keeps its matcher (tested headless, TestSmeltingMatcherShips) and is recorded in deferred-blocks.md with a cited reason — the recipe logic is never silently dropped, only the block UI is deferred. |
| T-25-13 | DoS | cooking tick on the hot path (IF a furnace built) | accept-or-mitigate | If a cooking block is built, the cook tick rides the existing scheduled/block-entity tick seam (level/ticks) — bounded, not a per-tick scan. If deferred, N/A. Documented in the audit. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` clean (static).
- `CGO_ENABLED=0 go vet ./server/` clean.
- `CGO_ENABLED=0 go test ./server/` green (built blocks + matcher-ships tests).
- deferred-blocks.md exists with a per-type decision + jar citations for every deferral.
- Any built block carries jar citations (StonecutterMenu / AbstractFurnaceBlockEntity).
- Docker -race (CGO=1) over ./server/ clean if a cooking tick (cross-tick state) was built.
- IGNORE stale gopls "undefined" diagnostics — gate ONLY on go build/vet/test.
</verification>

<success_criteria>
- Every cooking/stonecutting type has a build-or-defer decision recorded with jar-class + missing-subsystem + codebase evidence (no "too hard", no silent drop).
- The feasible block(s) (stonecutter and/or furnace per the evidence-based audit) are built 1:1, jar-cited, and craft through the plugin Match path.
- Every deferred type keeps its (Plan-01) matcher, tested headless — the deferral is the block/menu only.
- CGO=0 build/vet/test clean; -race clean for any cross-tick cooking state built.
</success_criteria>

<output>
After completion, create `.planning/phases/25-crafting-recipes-as-plugins/25-03-SUMMARY.md` recording: the per-type build-or-defer decisions (mirroring deferred-blocks.md), what was built (the jar citations + the menu/tick deltas), what was deferred and WHY (the missing subsystem + the cost reason), and the matcher-ships proof for each deferred type. This closes PLUGIN-05's "all types incl smelting" mandate — as much built 1:1 as feasible, every deferral cited.
</output>
