---
phase: 34-new-passive-mobs
plan: 01
type: execute
wave: 2
depends_on: [34-00]
files_modified:
  - plugins/vanilla_cow/main.star
  - plugins/vanilla_cow/plugin.toml
  - server/assets/vanilla_cow/main.star
  - server/assets/vanilla_cow/plugin.toml
  - server/attack_dispatch.go
  - server/cow_test.go
autonomous: true
requirements: [MOB-PASS-01]
must_haves:
  truths:
    - "A vanilla_cow plugin declares the 8-goal Cow.registerGoals set (FloatGoal@0, PanicGoal@1, BreedGoal@2, TemptGoal@3 cow_food, FollowParentGoal@4, Stroll@5, LookAtPlayer@6, LookAround@7) reusing the proven Go goal runtime"
    - "A cow renders as entity.Cow.ID with max_health 10.0 and movement_speed 0.2"
    - "Right-clicking an adult cow with an empty bucket consumes 1 bucket, gives a milk_bucket, and emits the entity.cow.milk sound; a baby cow does not milk (falls through to the feed path)"
  artifacts:
    - path: "plugins/vanilla_cow/main.star"
      provides: "Cow declare_mob with the 8-goal subset (no carrot Tempt)"
      contains: "declare_mob"
    - path: "server/assets/vanilla_cow/main.star"
      provides: "byte-identical embed copy of the cow plugin"
      contains: "declare_mob"
    - path: "server/attack_dispatch.go"
      provides: "tryMilkCow milking interact (bucket->milk_bucket)"
      contains: "func (t *TickLoop) tryMilkCow"
    - path: "server/cow_test.go"
      provides: "cow boot-load + behavior + milk tests"
      contains: "func TestMilkCow"
  key_links:
    - from: "server/attack_dispatch.go handleInteract"
      to: "tryMilkCow"
      via: "cow + held-bucket gate before tryFeedAnimal"
      pattern: "tryMilkCow"
    - from: "plugins/vanilla_cow/main.star tempt"
      to: "nearest_player_holding_food handle"
      via: "entity.nearest_player_holding_food(\"cow_food\", range) (from 34-00)"
      pattern: "nearest_player_holding_food"
    - from: "plugins/vanilla_cow/main.star"
      to: "server/assets/vanilla_cow/main.star"
      via: "byte-identical copy"
      pattern: "declare_mob"
---

<objective>
Cow as a jar-faithful Starlark plugin (MOB-PASS-01): the 8-goal Cow.registerGoals set reusing the
proven Go goal runtime, plus the cow-only milking interact (Cow.mobInteract: empty bucket -> milk_bucket).

Depends on 34-00 (the parameterized nearest_player_holding_food handle the cow TemptGoal uses).

Purpose: First of the three new passive mobs. Proves the pig `.star` template copies cleanly to a new
mob with retuned attributes and a per-mob interact extra, without touching the pig (the oracle stays green).
Output: vanilla_cow plugin pair (byte-identical), tryMilkCow in attack_dispatch.go, cow_test.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-new-passive-mobs/34-CONTEXT.md
@.planning/phases/34-new-passive-mobs/34-JARNOTES.md
@.planning/phases/34-new-passive-mobs/34-PATTERNS.md
@.planning/phases/34-new-passive-mobs/34-00-SUMMARY.md

<interfaces>
<!-- The cow .star is a SUBSET of the pig .star — copy verbatim, delete the carrot Tempt, retune. -->
<!-- The goal CALLBACKS (float_*, panic_*, breed_*, follow_*, stroll_*, look_*, around_*) are -->
<!-- mob-agnostic: COPY VERBATIM. Only constants + the declare_mob() block + the tempt food handle change. -->

baseTypeByName (server/plugin_mob_decl.go:108-121) ALREADY maps "cow" -> entity.Cow (Width 0.9, Height
1.4). spawnDeclaredMob (plugin_mob_decl.go:386) handles it unchanged (attrs seeded, hitbox auto).

THE FOOD HANDLE (from 34-00): the cow TemptGoal can_use calls
  entity.nearest_player_holding_food("cow_food", TEMPT_RANGE)
(the parameterized handle 34-00 added; the cow_food item tag is CONFIRMED in data/tag/tags.go,
34-JARNOTES.md:54). This REPLACES the pig's hardcoded nearest_player_holding_pig_food in the copied tempt
callbacks.

Cow milk data (verified):
  item.Bucket.ID = 1040 ("bucket"), item.MilkBucket.ID = 1046 ("milk_bucket") ; soundid 449 = "entity.cow.milk"

handleInteract (server/attack_dispatch.go:725-764) calls tryFeedAnimal unconditionally at :763.
tryFeedAnimal (attack_dispatch.go:801-847) is the FEED-path sibling analog (held read + shrinkHeldItem
consume + the no-op playEatingSound seam at :791-793 left for a sound-overriding animal).
shrinkHeldItem (server/block_interact.go:339). encodeSoundEntity (entity_encode.go:793) +
broadcastToTrackers (entity_events.go:28) — the playSound model (combat_mob.go).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vanilla_cow plugin pair (8-goal subset, byte-identical embed)</name>
  <read_first>
    - plugins/vanilla_pig/main.star (the canonical template — copy wholesale, then trim/retune)
    - plugins/vanilla_pig/plugin.toml (copy verbatim, change only name)
    - server/vanilla_pig_embed.go:25-34 (the embed-pair discipline: repo-root + server/assets/ MUST be byte-identical)
    - .planning/phases/34-new-passive-mobs/34-00-SUMMARY.md (the nearest_player_holding_food signature)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:8-19 (Cow.registerGoals @0..7) + :70,224 (Cow attrs 10.0/0.2)
    - .planning/phases/34-new-passive-mobs/34-PATTERNS.md:39-95 (the .star copy + declare_mob retune guidance)
  </read_first>
  <action>
Create plugins/vanilla_cow/main.star by COPYING plugins/vanilla_pig/main.star verbatim, then:
  1. DELETE the two carrot Tempt blocks (tempt_carrot_can_use/tick/stop/continue AND the first @4 goal()
     entry). Cow has ONLY ONE Tempt (the food tag), no carrot_on_a_stick.
  2. In the remaining (pig_food) tempt callbacks, REPLACE the handle call
     entity.nearest_player_holding_pig_food(TEMPT_RANGE) with
     entity.nearest_player_holding_food("cow_food", TEMPT_RANGE) (the 34-00 parameterized handle). Rename
     the callbacks/state keys to tempt_food_* for clarity (optional, but keep them consistent).
  3. Retune the declare_mob block per Cow.registerGoals (34-JARNOTES.md:8-19), USING THE JAR INDICES:
       name = "vanilla_cow", base_type = "cow",
       attributes = { "max_health": 10.0, "movement_speed": 0.2 },
       goals: @0 FloatGoal[JUMP], @1 PanicGoal[MOVE] (PanicGoal(2.0); the 2.0 speedModifier is
         cited-deferred like the pig's 1.25), @2 BreedGoal[MOVE,LOOK], @3 TemptGoal[MOVE,LOOK] (cow_food),
         @4 FollowParentGoal[], @5 WaterAvoidingRandomStroll[MOVE], @6 LookAtPlayer[LOOK],
         @7 RandomLookAround[MOVE,LOOK].
     NOTE: cow's BreedGoal is @2 (not @3 as the pig) because cow has no carrot goal. Cite
     net.minecraft.world.entity.animal.cow.AbstractCow.registerGoals in a header comment.
  4. Copy plugins/vanilla_cow/main.star verbatim to server/assets/vanilla_cow/main.star (BYTE-IDENTICAL).
Create plugins/vanilla_cow/plugin.toml (copy the pig's, name "vanilla_cow"). Caps stay
["entities.read", "entities.write", "world.read", "nav"] (milking is HOST-side). Copy verbatim to
server/assets/vanilla_cow/plugin.toml.
  </action>
  <verify>
    <automated>diff plugins/vanilla_cow/main.star server/assets/vanilla_cow/main.star</automated>
    <automated>diff plugins/vanilla_cow/plugin.toml server/assets/vanilla_cow/plugin.toml</automated>
  </verify>
  <acceptance_criteria>
    - `diff plugins/vanilla_cow/main.star server/assets/vanilla_cow/main.star` produces NO output
    - `diff plugins/vanilla_cow/plugin.toml server/assets/vanilla_cow/plugin.toml` produces NO output
    - `grep -c 'def tempt_carrot' plugins/vanilla_cow/main.star` returns 0
    - `grep 'nearest_player_holding_food("cow_food"' plugins/vanilla_cow/main.star` matches
    - `grep -c 'declare_mob' plugins/vanilla_cow/main.star` returns 1
    - `grep 'base_type = "cow"' plugins/vanilla_cow/main.star` matches
    - `grep '"max_health": 10.0' plugins/vanilla_cow/main.star` matches AND `grep '"movement_speed": 0.2' plugins/vanilla_cow/main.star` matches
    - `grep -v '^#' plugins/vanilla_cow/main.star | grep -c 'priority = '` returns 8
  </acceptance_criteria>
  <done>vanilla_cow plugin pair exists, byte-identical, 8 goals at jar indices, cow_food tempt via the parameterized handle, carrot Tempt removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tryMilkCow interact (bucket -> milk_bucket) + cow tests</name>
  <read_first>
    - server/attack_dispatch.go:725-847 (handleInteract dispatcher + tryFeedAnimal analog + the no-op playEatingSound seam at :791-793)
    - server/block_interact.go:339 (shrinkHeldItem)
    - server/combat_mob.go (the playSound -> encodeSoundEntity -> broadcastToTrackers model)
    - server/entity_encode.go:793 (encodeSoundEntity signature)
    - server/entity.go (the baseType field name — grep baseType)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:134-147 (AbstractCow.mobInteract verbatim bytecode)
    - server/plugin_pig_test.go:35 (TestPluginPigBootLoads — the boot-load + goal-count test shape to mirror)
  </read_first>
  <behavior>
    - TestCowBootLoads: a spawned cow renders as entity.Cow.ID and its AI has 8 goals at the jar priorities.
    - TestMilkCow: an adult cow + a player holding 1 bucket -> after handleInteract, the held slot is
      milk_bucket (1046), the bucket is consumed, and an entity.cow.milk sound (449) was broadcast.
    - TestMilkCowBabyFallsThrough: a BABY cow + a player holding a bucket -> NO milk; falls through to feed.
    - TestCowBehavior: a cow spawns, walks, and is CREATURE (categoryOf -> CREATURE).
  </behavior>
  <action>
Add func (t *TickLoop) tryMilkCow(p *tickPlayer, mob *Entity) bool to server/attack_dispatch.go,
porting net.minecraft.world.entity.animal.cow.AbstractCow.mobInteract VERBATIM (34-JARNOTES.md:134-147):
  // if (itemStack.is(Items.BUCKET) && !isBaby()) { playSound(COW_MILK,1,1);
  //   r = ItemUtils.createFilledResult(itemStack, player, MILK_BUCKET.getDefaultInstance());
  //   setItemInHand(hand, r); return SUCCESS; } return super.mobInteract(...);
  func (t *TickLoop) tryMilkCow(p *tickPlayer, mob *Entity) bool {
      inv := ensureInventory(p)
      held := inv.get(heldWindowSlot(inv.heldSlot))
      if slotIsEmpty(held) { return false }
      if int32(held.ItemID) != int32(item.Bucket.ID) { return false }   // 1040 ; not a bucket -> super
      if mob.isBaby() { return false }                                  // !isBaby gate -> super (feed)
      t.broadcastToTrackers(mob.id, encodeSoundEntity(449, soundSourceNeutral, mob.id, 1.0, 1.0, 0))  // COW_MILK, NO RNG
      // ItemUtils.createFilledResult: consume 1 bucket, give milk_bucket. If the held stack was size 1,
      // replace it with milk_bucket; else shrink 1 and add milk_bucket to the inventory (or drop). Use the
      // existing give/replace + shrinkHeldItem helpers (grep tryFeedAnimal/shrinkHeldItem for the path).
      return true
  }
Verify soundSourceNeutral + encodeSoundEntity against the files in read_first; use the constant
combat_mob.go's playSound uses if it differs.

Wire into handleInteract (attack_dispatch.go:763): replace the unconditional t.tryFeedAnimal(p, mob) with:
      if mob.baseType == entity.Cow && t.tryMilkCow(p, mob) { return }
      t.tryFeedAnimal(p, mob)
(read the Entity baseType field name from entity.go; milk is tried first for a cow; a non-bucket / baby
returns false and falls through to feed.)

Create server/cow_test.go with the four <behavior> tests. Mirror plugin_pig_test.go:35 for the boot-load
+ goal-count assertion (load the cow registry via the same test harness the pig test uses). For TestMilkCow
assert the held slot id becomes 1046 and a sound packet was queued to the tracker.
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./...</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestCow|TestMilkCow' -v</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` exits 0
    - `grep -c 'func (t \*TickLoop) tryMilkCow' server/attack_dispatch.go` returns 1
    - `grep 'tryMilkCow' server/attack_dispatch.go` shows the handleInteract call site gating on entity.Cow
    - `grep '449' server/attack_dispatch.go` matches (the entity.cow.milk sound id), cited COW_MILK
    - `CGO_ENABLED=0 go test ./server/ -run 'TestCowBootLoads|TestMilkCow|TestMilkCowBabyFallsThrough|TestCowBehavior' -v` exits 0
    - `grep -c 'func TestMilkCow' server/cow_test.go` returns 1
  </acceptance_criteria>
  <done>tryMilkCow ports AbstractCow.mobInteract 1:1 (bucket->milk_bucket + COW_MILK sound, baby gated), wired into handleInteract before feed, all four cow tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ServerboundInteract -> handleInteract | untrusted target id + hand; held item read server-side (never from packet) |
| plugin .star -> host handles | sandboxed Starlark, least-privilege caps from plugin.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Spoofing | handleInteract target id | mitigate | tryMilkCow runs after handleInteract re-resolves the mob through owningRegion (:741-748); a forged/non-cow id falls through harmlessly |
| T-34-02 | Tampering | held item from packet | mitigate | the held item is read server-side from the player's selected hand (inv.get), never trusted from the Interact payload (the tryFeedAnimal precedent) |
| T-34-03 | Elevation | cow plugin caps | accept | cow plugin.toml carries the same least-privilege caps as the pig; milking is host-side Go, no widened cap |
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... clean; go vet ./server/ clean
- Both .star and both .toml copies byte-identical
- Cow tests green: boot-load (8 goals), milk (bucket->milk_bucket+sound), baby-fall-through, behavior
- The pig oracle is UNTOUCHED (no edit to vanilla_pig.go / newPigAI / vanilla_pig/main.star / pig food handles)
</verification>

<success_criteria>
- Cow declares the 8-goal Cow.registerGoals set, renders as entity.Cow.ID, attrs 10.0/0.2 (MOB-PASS-01)
- Milking ports AbstractCow.mobInteract 1:1: empty bucket + adult -> milk_bucket + COW_MILK sound; baby -> feed path
- Both .star and both .toml copies byte-identical; the pig oracle stays green
</success_criteria>

<output>
After completion, create `.planning/phases/34-new-passive-mobs/34-01-SUMMARY.md`
</output>
