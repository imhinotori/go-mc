---
phase: 24-vanilla-mobs-as-plugins
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - server/plugin_mob_ai.go
  - server/plugin_mob_decl.go
  - server/vanilla_pig.go
  - server/vanilla_pig_embed.go
  - server/plugin_pig_test.go
  - server/plugin_pig_race_test.go
  - server/async.go
  - server/debug.go
  - cmd/sulfur/main.go
  - plugins/vanilla_pig/plugin.toml
  - plugins/vanilla_pig/main.star
autonomous: true
requirements: [PLUGIN-04]

must_haves:
  truths:
    - "vanilla_pig.star declares 3 goals at the jar priorities (RandomStroll@6 MOVE, LookAtPlayer@7 LOOK, RandomLookAround@8 MOVE|LOOK), each ported 1:1 and citing its net.minecraft.world.entity.ai.goal.* class"
    - "Each NEW pig goal (Float@0, Panic@1, Breed@3, Tempt@4, FollowParent@5) is BUILT 1:1 if its prerequisite is faithfully buildable now, else DEFERRED with a CITED reason in the backlog (never silently skipped, never faked)"
    - "The vanilla_pig plugin BOOT-LOADS: the host loads it with the declare_mob/goal builtins injected, and the TickLoop holds the resulting registry before the first pig can spawn"
    - "newPigAI is replaced at BOTH call sites (async.go natural spawn, debug.go SULFUR_DEBUG spawn) by the plugin-driven build — the plugin pig is the only pig"
    - "The existing pig-AI suite passes against the plugin pig UNCHANGED (or updated only to the seeded-source expectation); TestPluginPigEqualsGoNativePig proves behavior-identity"
    - "The swapped plugin pig ticking (look/nearest-player/rand seams + nav) is Docker -race clean; the per-entity rng has no shared-global race"
  artifacts:
    - path: "plugins/vanilla_pig/main.star"
      provides: "the 1:1 Pig AI plugin (3 ported goals, each citing its jar FQCN)"
      contains: "declare_mob"
    - path: "plugins/vanilla_pig/plugin.toml"
      provides: "the vanilla_pig manifest (least-privilege caps: entities.read/write, world.read, nav)"
      contains: "capabilities"
    - path: "server/vanilla_pig.go"
      provides: "spawnVanillaPig + the boot-load/registry wiring the swap sites call"
      contains: "func (t *TickLoop) spawnVanillaPig"
    - path: "server/plugin_pig_test.go"
      provides: "TestVanillaPigDeclaresGoalSet, TestVanillaPigStrollSetsTarget, TestVanillaPigLooksAtPlayer, TestPluginPigEqualsGoNativePig, TestPluginPigBootLoads"
      contains: "TestPluginPigEqualsGoNativePig"
  key_links:
    - from: "server/async.go (spawnCandidatesReady.applyTo)"
      to: "server/vanilla_pig.go spawnVanillaPig"
      via: "the swap: pig.ai = newPigAI() replaced by the plugin-driven spawn"
      pattern: "spawnVanillaPig"
    - from: "cmd/sulfur/main.go"
      to: "the vanilla_pig registry on the TickLoop"
      via: "boot-load the vanilla_pig plugin with declare_mob/goal injected, hand the registry to the tick"
      pattern: "vanilla_pig|SetMobRegistry"
    - from: "plugins/vanilla_pig/main.star look_tick"
      to: "entity.set_look (Plan 01)"
      via: "the LOOK goal aims via the new seam"
      pattern: "set_look"
---

<objective>
Re-express the vanilla Pig AI AS a Starlark plugin (the FIRST 1:1 dogfood), boot-load it, and SWAP it in as the only pig — proving the Phase-23 API (extended in Plan 01) expresses real vanilla AI. The existing pig-AI tests become the behavior-identical contract.

This plan delivers:
1. The vanilla_pig.star plugin — the 3 EXISTING passive goals (RandomStroll@6, LookAtPlayer@7, RandomLookAround@8) re-expressed 1:1 against bytecode, each consuming the Plan-01 seams (set_look, nearest_player, rand_int/rand_float).
2. The per-new-goal build-OR-defer audit: for Float@0, Panic@1, Breed@3, Tempt@4, FollowParent@5 — build the minimal faithful slice + port the goal IF its prerequisite exists now, else DEFER with a cited reason (no-built-but-unwired governs each).
3. The boot-load wiring (the Phase-23-deferred follow-up) so spawnDeclaredMob has the vanilla_pig declaration live at boot.
4. The SWAP at both newPigAI sites + TWO API-fidelity fixes the dogfood surfaces (requiresUpdateEveryTick for @8, per-mob goal scratch state across the frozen boundary).
5. The behavior-identical GATE (TestPluginPigEqualsGoNativePig + the existing suite green against the plugin pig) and the swapped-pig Docker -race.

Purpose: the validation phase — close the API gaps with a real 1:1 vanilla mob, behavior-identical to the Go pig it replaces.
Output: the plugin, the boot-load/registry wiring, the swap, the fidelity fixes, the gate tests, and the -race test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# The phase artifacts (read in full)
@.planning/phases/24-vanilla-mobs-as-plugins/24-CONTEXT.md
@.planning/phases/24-vanilla-mobs-as-plugins/24-RESEARCH.md
@.planning/phases/24-vanilla-mobs-as-plugins/24-VALIDATION.md

# Plan 01's output (the seams this plan consumes)
@.planning/phases/24-vanilla-mobs-as-plugins/24-01-SUMMARY.md

# The Phase-23 API being dogfooded + the swap sites + the boot wiring
@server/plugin_mob_decl.go
@server/plugin_mob_ai.go
@server/plugin_mob_test.go
@server/ai_mob.go
@server/ai_goals_passive.go
@server/ai_goal.go
@cmd/sulfur/main.go

<interfaces>
<!-- Contracts the executor needs. Extracted this session. -->

From server/plugin_mob_decl.go — the declare-once registry + spawn path (REUSE unchanged where possible):
```go
type mobDecl struct { name string; baseType entity.Entity; attrs map[string]float64; goals []goalDecl; caps capSet }
type mobRegistry struct { byName map[string]*mobDecl; caps capSet }
func newMobRegistry() *mobRegistry
func (r *mobRegistry) setLoadCaps(c capSet)
func (r *mobRegistry) declareMobBuiltin() *starlark.Builtin   // inject as extra["declare_mob"]
func (r *mobRegistry) goalBuiltin() *starlark.Builtin         // inject as extra["goal"]
func (t *TickLoop) spawnDeclaredMob(decl *mobDecl, x, y, z float64) *Entity  // NewEntity + seedAttributes + buildAIFromDecl + entities.add
// goal() builtin signature: goal(priority, flags, tick, can_use=None, start=None, stop=None) — tick is REQUIRED today (errors if nil)
```

From server/plugin_mob_ai.go — the goal adapter (note the two fidelity gaps):
```go
type starlarkGoal struct { baseGoal; t *TickLoop; caps capSet; name string; canUseFn,tickFn,startFn,stopFn starlark.Callable }
func buildAIFromDecl(t *TickLoop, decl *mobDecl) *mobAI
// FIDELITY GAP 1: starlarkGoal embeds baseGoal -> requiresUpdateEveryTick() returns false.
//   RandomLookAround@8 is requiresUpdateEveryTick=true in the jar (ai_goals_passive.go confirms).
// FIDELITY GAP 2: starlarkGoal carries NO per-goal Starlark scratch state; lookAt/lookAround need a
//   per-mob countdown + captured target (Go goals keep these as struct fields).
```

From server/async.go:311-312 and server/debug.go:166-167 — the TWO swap sites:
```go
// async.go (spawnCandidatesReady.applyTo):
pig := NewEntity(t.idAlloc.AllocID(), entity.Pig, float64(c.x)+0.5, float64(c.y), float64(c.z)+0.5)
pig.ai = newPigAI()
t.entities.add(pig)
// debug.go (SULFUR_DEBUG spawn):
pig := NewEntity(id, entity.Pig, px, py, pz)
pig.ai = newPigAI()
t.entities.add(pig)
```

From cmd/sulfur/main.go ~line 288 — the EXISTING plugin load to extend with the mob registry:
```go
const pluginsDir = "plugins"
mgr := host.New()
mgr.LoadDir(pluginsDir)        // loads event plugins WITHOUT declare_mob/goal — vanilla_pig needs LoadDirWith with them injected
tick.SetPlugins(mgr)
```

From server/plugin_mob_test.go — the load harness pattern to mirror at boot:
```go
r := newMobRegistry()
extra := starlark.StringDict{"declare_mob": r.declareMobBuiltin(), "goal": r.goalBuiltin()}
m.LoadDirWith(root, extra)     // captures into r
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-new-goal build-OR-defer audit (javap each jar goal) + the vanilla_pig.star (3 existing goals 1:1) + the two API-fidelity fixes</name>
  <files>plugins/vanilla_pig/main.star, plugins/vanilla_pig/plugin.toml, server/plugin_mob_ai.go, server/plugin_mob_decl.go, .planning/phases/24-vanilla-mobs-as-plugins/deferred-goals.md</files>
  <action>
    THE 1:1 MANDATE FULLY APPLIES. For EVERY ported goal/value: `javap -c -p -classpath temp/cache/26.2-inner.jar <FQCN>` FIRST, match priority/flags/canUse/canContinueToUse/start/stop/tick/constants/draw-order to the bytecode, and CITE the class in the .star. Do NOT trust this plan's intermediate numbers — re-disassemble.

    PART A — the new-goal build-OR-defer audit (CONTEXT decision 4 / the no-built-but-unwired rule). javap `net.minecraft.world.entity.animal.pig.Pig.registerGoals` to confirm the exact set @0..@8. For EACH new goal, decide build-now-vs-defer by reading what it ACTUALLY needs against the Sulfur codebase (verify, do not assume):
      - FloatGoal@0: needs mob isInWater AND setJumping/jumpControl. The existing eyeInWater/fluidAt seams are PLAYER-specific (*tickPlayer), and ai_mob.go documents jumpControl as DEFERRED (no jump control on mobAI). FloatGoal's behavior IS jump-when-in-water — with no jumpControl the goal would be built-but-unwired. Confirm via javap (FloatGoal.tick calls mob.getJumpControl().jump()) + grep the codebase for a mob jump seam. If genuinely absent → DEFER with the cited reason. If a minimal faithful mob isInWater + a jump-impulse slice is small enough to build 1:1, build it and port the goal — your call after verifying.
      - PanicGoal@1: needs a mob "was hurt recently" source. The on_damage event (combat.go) fires ONLY for players (*tickPlayer / p.entityID); mobs have no applyDamage path → no mob hurt source exists. Confirm via grep (is there any mob-damage seam?). If absent → DEFER with the cited reason (do NOT fake a hurt flag).
      - BreedGoal@3 / TemptGoal@4 (×2) / FollowParentGoal@5: need entity aging + breeding state + the PIG_FOOD/CARROT_ON_A_STICK held-item tag read. These are large unbuilt subsystems → almost certainly DEFER, each with a cited reason. Confirm no aging/held-item seam exists before deferring.
    Record EVERY decision in .planning/phases/24-vanilla-mobs-as-plugins/deferred-goals.md: the goal, its jar FQCN, the exact prerequisite it needs, the codebase evidence it's absent, and "DEFER (cited)" or "BUILT (slice X)". This file is the explicit-justification artifact CONTEXT decision 4 demands. The OUTCOME: as much real vanilla pig AI as can be ported 1:1 this phase, every deferral justified — never a silent skip.

    PART B — the two API-fidelity fixes the dogfood surfaces (resolve before writing the goals):
      1. requiresUpdateEveryTick (FIDELITY GAP 1): RandomLookAround@8 is requiresUpdateEveryTick=true (confirm via javap RandomLookAroundGoal.requiresUpdateEveryTick + ai_goals_passive.go). starlarkGoal embeds baseGoal → returns false, so @8's tick would not run when the selector isn't simulating, breaking behavior-identity. Add an optional `requires_update_every_tick=False` kwarg to the goal() builtin (plugin_mob_decl.go) captured into goalDecl, and override starlarkGoal.requiresUpdateEveryTick() to return it (plugin_mob_ai.go). Faithful + minimal.
      2. Per-mob goal scratch (FIDELITY GAP 2 / RESEARCH Pitfall 3, Open-Q §6): lookAt needs a countdown + captured target; lookAround needs a heading + countdown. Starlark module globals freeze on load → a module dict can't be mutated at tick time. Stroll needs NO extra state (its want-target lives on the mob nav via nav.path_to). For lookAt/lookAround, add a minimal per-mob mutable scratch the host provides — recommended: an `entity.state` dict-like seam on entityHandle backed by a per-entity map the host owns (tick-owned), OR typed scratch fields on starlarkGoal the seams read/write. Pick the minimal faithful option, implement it, and cite that it replaces the Go goal's struct fields (lookTime/lookX/Y/Z, relX/relZ/lookTime). Keep it tick-owned + -race-safe (id-keyed, owner-only).

    PART C — write plugins/vanilla_pig/main.star: declare_mob("vanilla_pig", base_type="pig", attributes={"max_health":10.0,"movement_speed":0.25}, goals=[...]) with the 3 goals re-expressed 1:1 (RESEARCH §The vanilla-pig declaration + ai_goals_passive.go as the oracle):
      - @6 MOVE WaterAvoidingRandomStrollGoal: can_use rolls entity.rand_int(STROLL_INTERVAL)!=0 GATE then the 3 offset draws (the bytecode order), stashes the want via nav (or scratch), start → nav.path_to, continue → nav.has_path, stop → nav.stop/clear. Its vanilla tick is empty → use the `tick` optionality (PART B is NOT about tick; goal() requires tick today — either pass a no-op tick OR relax goal() to make tick optional when start/continue carry the behavior, RESEARCH Open-Q §7; pick the minimal faithful option and cite it).
      - @7 LOOK LookAtPlayerGoal: can_use rolls entity.rand_float()<0.02 then world.nearest_player(entity.x, eye_y, entity.z, 6.0); start sets lookTime=40+entity.rand_int(40) into scratch; tick entity.set_look(yaw_toward(...)) + decrement; continue within distance² && lookTime>0; stop clears scratch.
      - @8 MOVE|LOOK RandomLookAroundGoal: can_use rolls entity.rand_float()<0.02; start picks heading via entity.rand_float()/nextDouble (2π·rnd → relX/relZ) + lookTime=20+entity.rand_int(20); requires_update_every_tick=True; tick entity.set_look(yaw_toward(relX,relZ)) + decrement; continue lookTime>=0.
      Each goal block opens with `# ports net.minecraft.world.entity.ai.goal.<Class>` (no GPL paste). plugin.toml declares least-privilege caps: entities.read, entities.write, world.read, nav (NOT world.write — a pig never sets blocks; RESEARCH Security Domain).

    Do NOT port any DEFERRED goal. Do NOT renumber/merge priorities or flags from the jar.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./server/ -run 'TestVanillaPigDeclaresGoalSet|TestVanillaPigGoalsPorted' && grep -q 'net.minecraft.world.entity.ai.goal' plugins/vanilla_pig/main.star && test -f .planning/phases/24-vanilla-mobs-as-plugins/deferred-goals.md</automated>
  </verify>
  <done>vanilla_pig.star declares 3 goals at @6/@7/@8 with the jar priorities+flags, each citing its ai.goal.* class; the draw order matches the bytecode; requires_update_every_tick is honored for @8; per-mob scratch works for lookAt/lookAround; goal() tick-optionality (or a no-op tick) handles stroll's empty tick; deferred-goals.md records each new goal's build/defer decision with a cited reason and codebase evidence. The manifest declares least-privilege caps without world.write.</done>
</task>

<task type="auto">
  <name>Task 2: Boot-load the vanilla_pig plugin + SWAP newPigAI at both sites + the behavior-identical gate</name>
  <files>server/vanilla_pig.go, server/vanilla_pig_embed.go, server/async.go, server/debug.go, cmd/sulfur/main.go, server/plugin_pig_test.go</files>
  <action>
    BOOT-LOAD (RESEARCH Pitfall 4 — the Phase-23-deferred follow-up; the SWAP requires the registry live at boot). The vanilla_pig plugin MUST always be present (the swap makes it the only pig), so do NOT rely on an operator-managed plugins/ dir alone. Recommended (CONTEXT decision 5 leaves the bundling to the planner): `//go:embed` the vanilla_pig plugin.toml + main.star into server (server/vanilla_pig_embed.go), and on boot load it through the host with the declare_mob/goal builtins injected via LoadDirWith into a server-owned mobRegistry — mirroring the plugin_mob_test.go harness. Materialize the embedded plugin to a temp dir for LoadDirWith, OR add a host seam that loads from an fs.FS — pick the minimal faithful approach. Stamp the registry caps from the manifest (setLoadCaps). Hand the populated registry to the TickLoop (add a field + SetMobRegistry, set BEFORE tick.Run, tick-owned thereafter). If the vanilla_pig declaration is missing after load → FAIL LOUDLY at boot (not silently at spawn — Pitfall 4). Keep the existing event-plugin LoadDir("plugins") path working (it loads WITHOUT declare_mob/goal — do not break it).

    spawnVanillaPig (server/vanilla_pig.go): `func (t *TickLoop) spawnVanillaPig(x, y, z float64) *Entity` → look up the "vanilla_pig" decl in the tick's registry and call t.spawnDeclaredMob(decl, x,y,z) (the Phase-23 path: NewEntity entity.Pig → real pig attrs → buildAIFromDecl → entities.add). It renders as entity.Pig.ID (custom = behavior, not a new wire id). Guard a nil registry/decl with a loud error.

    SWAP (CONTEXT decision 3 / RESEARCH Pattern 3): replace BOTH `pig.ai = newPigAI()` blocks:
      - server/async.go (spawnCandidatesReady.applyTo ~line 311): replace the NewEntity+newPigAI+add with `pig := t.spawnVanillaPig(...)`.
      - server/debug.go (SULFUR_DEBUG spawn ~line 166): same.
    After the swap there must be NO `newPigAI()` call in async.go or debug.go. Keep newPigAI + the 3 Go goal structs as the behavior-identical ORACLE (CONTEXT/RESEARCH Open-Q §2 recommend KEEP for the phase — TestPluginPigEqualsGoNativePig asserts the plugin pig == the Go pig; do NOT delete them this phase).

    THE GATE (server/plugin_pig_test.go) — the behavior-identical proof (24-VALIDATION 24-02-04, RESEARCH "the signature test"):
      - TestPluginPigBootLoads: the boot-load wiring populates the registry with "vanilla_pig"; spawnVanillaPig returns a live pig with e.typ == entity.Pig.ID and a non-nil ai with 3 goals at @6/@7/@8.
      - TestVanillaPigStrollSetsTarget / TestVanillaPigLooksAtPlayer: the plugin-driven goals set a wantTarget within ±10/±7 (stroll) and face the nearest player via set_look (lookAt) — the plugin analogues of the existing TestRandomStrollSetsTarget / TestLookAtPlayerFacesNearest.
      - TestPluginPigEqualsGoNativePig: spawn a Go-native pig (newPigAI) and a plugin pig (spawnVanillaPig) with the SAME seeded RNG seed + the SAME world/player inputs, drive both through serverAiStep for N ticks, assert IDENTICAL observable output (wantTarget sequence, facing yaw, position). This passes ONLY if the goals are ported 1:1, the draw order matches, and set_look writes exactly what the Go goal wrote.
      - Confirm the EXISTING pig-AI suite passes against the swapped path: run TestServerAiStep*, TestPigGoalSetRegistered, the navigation follow tests, async_stress_test, spawner_test. Update an existing test ONLY where its expectation depended on the OLD global-rand behavior (to the seeded-source expectation — the seeded source is the 1:1-correct one). RESEARCH Pitfall 6: TestServerAiStepWalksToGoalTarget is a PRE-EXISTING async-timing flake — on a full-suite failure, re-run it in isolation (`-run TestServerAiStepWalksToGoalTarget -count=1`) before attributing it to this phase; do NOT mask a real regression as "the known flake".
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && go vet ./server/ ./cmd/... && CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigBootLoads|TestVanillaPig|TestPluginPigEqualsGoNativePig|TestServerAiStep|TestPigGoalSetRegistered' && ! grep -nE 'newPigAI\(\)' server/async.go server/debug.go</automated>
  </verify>
  <done>vanilla_pig boot-loads into a tick-owned registry (loud failure if missing); spawnVanillaPig builds the plugin pig; both newPigAI call sites now call spawnVanillaPig (no newPigAI in async.go/debug.go); TestPluginPigEqualsGoNativePig proves the plugin pig == the Go pig; the existing pig-AI suite passes against the plugin path; newPigAI + the Go goals remain as the oracle.</done>
</task>

<task type="auto">
  <name>Task 3: The swapped-pig Docker -race gate + full-suite + CGO=0 ship build</name>
  <files>server/plugin_pig_race_test.go</files>
  <action>
    The swap put untrusted-plugin-driven goal callbacks (set_look / nearest_player / rand seams + nav) on the live tick. Per CLAUDE.md + RESEARCH Pitfall 5: -race needs CGO=1 (Docker); the ship binary stays CGO=0. These are two gates, not in conflict.

    Write server/plugin_pig_race_test.go (TestPluginPigRace): spawn one or more plugin pigs via spawnVanillaPig, drive them through serverAiStep/tickOnce for many ticks alongside a player so all 3 goals fire (forcing the look + nearest_player + rand seams), and assert no panic/divergence. The seams are tick-owned by construction (id-handles re-resolved on the owner; the per-entity rng created in buildAIFromDecl and drawn only on the tick goroutine — no shared global), so -race must come back clean.

    Run the gates:
      - Ship build: `CGO_ENABLED=0 go build ./...` clean.
      - Full quick suite: `CGO_ENABLED=0 go test ./server/ ./plugin/... ./level/attribute/` green.
      - Docker -race (CGO=1): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ ./plugin/...` green — the swapped plugin pig tick + the seeded-RNG determinism are only meaningful under -race with the fixed seed.
    On a TestServerAiStepWalksToGoalTarget failure under -race, re-run it in isolation before attributing it to this phase (RESEARCH Pitfall 6 — the pre-existing async-timing flake); the Plan-01 seeded source should REDUCE it, but a residual async-pool timing flake is not a Phase-24 regression. Record the -race outcome + any isolation re-run in the SUMMARY.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./server/ ./plugin/... ./level/attribute/</automated>
  </verify>
  <done>TestPluginPigRace exists and drives all 3 plugin-pig goals; CGO=0 go build ./... clean; the quick suite green; the Docker -race over ./server/ ./plugin/... green (recorded in the SUMMARY, with any flake-isolation re-run noted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vanilla_pig.star goal callback → tick-owned mob/world state | the bundled plugin's goal logic crosses into the live tick via the Plan-01 seams every tick a goal runs |
| boot load → tick registry | the declared mob registry is written once at boot and read at spawn on the tick goroutine |
| swapped spawn path → live pig | every naturally/debug-spawned pig is now plugin-driven on the hot tick path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Tampering / correctness | a mis-ported goal diverges from vanilla (wrong priority/flag/draw-order) | mitigate | Each goal javap-verified against the jar bytecode FIRST + cited; TestPluginPigEqualsGoNativePig asserts the plugin pig == the Go-native pig under the same seed (a divergence fails the gate, not ships silently). |
| T-24-06 | Denial of service | a vanilla_pig goal callback hangs/errors and stalls the tick (disconnecting every player) | mitigate | Phase-21/23 carry: every callback runs on a fresh budget-bounded thread (SetMaxExecutionSteps → EvalError), starlarkGoal.call logs+absorbs the error, the tickOnce recover backstop is the second line. The swapped pig inherits this isolation unchanged. |
| T-24-07 | Elevation of privilege | the vanilla_pig plugin (or a tampered copy) griefs blocks via world.write | mitigate | The manifest declares NO world.write (least-privilege: entities.read/write, world.read, nav only); the Phase-23 capability gate denies set_block. The embedded copy is the source of truth (a swapped-on-disk plugins/ copy cannot widen caps beyond its manifest). |
| T-24-08 | Tampering / data-race | the swapped plugin pig tick leaks a live *Entity or shares the per-entity rng across goroutines | mitigate | Handles re-resolve by id on the tick goroutine (Phase-23); the per-entity rng is created in buildAIFromDecl and drawn only on the tick goroutine (no shared global). Docker -race (Task 3) covers the live swapped tick. |
| T-24-09 | Spoofing / availability | the vanilla_pig declaration is missing at boot → nil-deref or pigless world | mitigate | The boot-load fails LOUDLY if "vanilla_pig" is absent after load (not a silent spawn-time nil); the //go:embed copy guarantees presence in the binary. |
</threat_model>

<verification>
- `cd /d/ender && CGO_ENABLED=0 go build ./...` clean (ship binary stays CGO=0).
- `go vet ./server/ ./cmd/...` clean.
- `CGO_ENABLED=0 go test ./server/ ./plugin/... ./level/attribute/` green (incl. the existing pig-AI suite against the plugin path).
- `! grep -nE 'newPigAI\(\)' server/async.go server/debug.go` (both swap sites replaced).
- `grep -q 'net.minecraft.world.entity.ai.goal' plugins/vanilla_pig/main.star` (each goal cites its jar class).
- `test -f .planning/phases/24-vanilla-mobs-as-plugins/deferred-goals.md` (every new goal's build/defer decision is recorded with a cited reason).
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ ./plugin/...` green.
- TestPluginPigEqualsGoNativePig green (the behavior-identical proof).
- javap evidence for EACH ported goal + the deferral rationale recorded in the SUMMARY (the 1:1 anchor).
- IGNORE stale gopls "undefined" diagnostics (gate on go build/vet/test only).
</verification>

<success_criteria>
- vanilla_pig.star ports the 3 passive goals 1:1 (@6/@7/@8, jar-cited, bytecode-matched draw order), consuming the Plan-01 seams.
- Every NEW pig goal is built-1:1 (if buildable) or deferred-with-a-cited-reason in deferred-goals.md — never silently skipped, never faked.
- The vanilla_pig plugin boot-loads into a tick-owned registry (loud failure if missing).
- newPigAI is replaced at both async.go + debug.go; the plugin pig is the only pig; it renders as entity.Pig.ID.
- The two API-fidelity gaps (requiresUpdateEveryTick, per-mob scratch) are closed faithfully.
- The existing pig-AI suite passes against the plugin pig (updated only to the seeded-source expectation where needed); TestPluginPigEqualsGoNativePig proves identity.
- Docker -race over the swapped pig tick is clean; CGO=0 ship build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/24-vanilla-mobs-as-plugins/24-02-SUMMARY.md` recording: the boot-load/bundling approach chosen (embed vs dir), the two API-fidelity fixes, the per-new-goal build/defer outcomes (pointing to deferred-goals.md), which existing tests were updated to the seeded-source expectation, the TestPluginPigEqualsGoNativePig design, and the Docker -race result (with any flake-isolation re-run noted). State whether newPigAI + the Go goals are retained as the oracle or scheduled for retirement.
</output>
