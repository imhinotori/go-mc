---
phase: 06-entities-physics-interaction
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/tracker.go
  - server/entity_encode.go
  - server/tick_phases.go
  - server/tick.go
  - server/tracker_test.go
  - server/entity_encode_test.go
  - .planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md
autonomous: true
requirements: [ENT-01]
user_setup: []

must_haves:
  truths:
    - "A synchronous entityTracker (server/tracker.go) is assigned to TickLoop.tracker (replacing noopTracker) WITHOUT changing the tracker interface or the t.tracker.Tick() call site at tick_phases.go:26 — the Phase-3 seam is FILLED, not modified (so Phase 8 can later swap the executor off-tick behind the same seam)"
    - "Each tick, the tracker diffs per-player visibility against the entity store's near() broad-phase: newly-visible entities get ClientboundAddEntity (+ ClientboundSetEntityData if metadata is non-default); still-visible-and-moved entities get a delta ClientboundMoveEntityPos/PosRot/Rot or a ClientboundTeleportEntity for large deltas (+ ClientboundRotateHead); no-longer-visible entities are batched into ONE ClientboundRemoveEntities — per-player tracked-set bookkeeping decides each"
    - "ClientboundAddEntity encodes the JAR-DERIVED 26.2 wire order (VarInt id, UUID, VarInt typeId, Double x/y/z, Vec3.LP low-precision SHORT movement — NOT 3 Doubles — Byte xRot/yRot/yHeadRot, VarInt data) with the LP short scaling settled by decompiling the jar in Task 0; ClientboundSetEntityData encodes the indexed-entry list (Byte index, VarInt serializerId, codec value) terminated by a single 0xFF byte"
    - "tickEntities (server/tick_phases.go) is filled to advance entity state ages/positions for the tick (the per-tick entity step the tracker reads); it runs on the tick goroutine over the tick-owned store (TICK-05). This plan's tickEntities may be minimal (no physics yet — that is Plan 06-03) but MUST keep the per-section buckets consistent if an entity's position changed"
    - "The tracker runs ON the tick goroutine (synchronous) and emits via player.client.Send (the bounded queue; the writeLoop stays the sole socket writer); NO goroutines, NO xsync/ants/conc — the synchronous-not-async constraint is explicit (Phase 8 / OPT-02 moves it off-tick via applyAsyncResults)"
  artifacts:
    - path: "server/tracker.go"
      provides: "entityTracker{loop *TickLoop} implementing Tick(): per-player visibility diff → Add/SetEntityData/Move/RotateHead/RemoveEntities; per-player tracked-set bookkeeping"
      contains: "func (et *entityTracker) Tick()"
    - path: "server/entity_encode.go"
      provides: "jar-derived encoders: AddEntity, SetEntityData (indexed entries + 0xFF terminator), MoveEntityPos/PosRot/Rot, TeleportEntity, RotateHead, SetEntityMotion, RemoveEntities"
      contains: "func encodeAddEntity"
    - path: "server/tick.go"
      provides: "TickLoop.tracker is set to a real entityTracker (NewTickLoop or a SetTracker seam); tickPlayer gains a tracked map[int32]bool"
      contains: "entityTracker"
    - path: "server/tick_phases.go"
      provides: "tickEntities filled (per-tick entity step + bucket consistency) — keyed on the tick-owned store"
      contains: "func (t *TickLoop) tickEntities"
    - path: ".planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md"
      provides: "the jar-derived LP short scaling + SetEntityData 0xFF framing record (sealed byte-level by Plan 06-07)"
      contains: "Vec3.LP"
  key_links:
    - from: "server/tracker.go entityTracker.Tick"
      to: "server/entity_store.go entityStore.near"
      via: "the per-player visible set comes from the grid broad-phase query"
      pattern: "near"
    - from: "server/tracker.go entityTracker.Tick"
      to: "server/entity_encode.go encodeAddEntity"
      via: "newly-visible entities emit the jar-derived AddEntity (+ SetEntityData)"
      pattern: "encodeAddEntity"
    - from: "server/tick.go TickLoop.tracker"
      to: "server/tick_phases.go t.tracker.Tick()"
      via: "the UNCHANGED Phase-3 seam now drives the real entityTracker"
      pattern: "tracker.Tick"
---

<objective>
Fill the Phase-3 `tracker.Tick()` seam with a SYNCHRONOUS `entityTracker` (ENT-01) that makes spawned entities VISIBLE to nearby players. Today `t.tracker` is `noopTracker{}` and `tick_phases.go:26` calls `t.tracker.Tick()` — a no-op. This plan assigns a real `entityTracker` (without touching the interface or the call site, so Phase 8 / OPT-02 can later swap the executor off-tick behind the same seam) that, each tick, diffs each player's visible set (from the store's `near()` grid broad-phase, Plan 06-01) against a per-player tracked set: newly-visible → `ClientboundAddEntity` (+ `SetEntityData` if metadata), moved → a delta `MoveEntity*`/`TeleportEntity` (+ `RotateHead`), gone → one batched `RemoveEntities`. It also fills the `tickEntities()` stub with the per-tick entity step and the jar-derived clientbound encoders.

The two MEDIUM wire surfaces — the `Vec3.LP` low-precision SHORT movement scaling in `AddEntity`/`SetEntityMotion`, and the `SetEntityData` indexed-entry + `0xFF` terminator framing — are jar-derived in Task 0 (and byte-sealed by Plan 06-07's capture-diff). Everything is SYNCHRONOUS, tick-owned, single-owner (TICK-05): NO goroutines, NO xsync/ants/conc.

Purpose: ENT-01's "a synchronous entity tracker sends spawn/move/despawn to nearby players behind an async-shaped tracker.Tick() interface" is THE behavior of this slice. Filling the seam (not changing it) is the load-bearing Phase-8-readiness contract.
Output: server/tracker.go (the synchronous tracker), server/entity_encode.go (the jar-derived encoders), the filled tickEntities, the tracker assignment + the per-player tracked set, and the 06-CAPTURE-DIFF.md LP/0xFF record. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-entities-physics-interaction/06-RESEARCH.md
@.planning/phases/06-entities-physics-interaction/06-VALIDATION.md
@.planning/phases/06-entities-physics-interaction/06-01-PLAN.md

<interfaces>
<!-- VERIFIED this session. The tracker consumes Plan 06-01's store. -->

From server/tick.go (the seam — DO NOT change the interface or the call site):
```go
type tracker interface{ Tick() }
type noopTracker struct{}
func (noopTracker) Tick() {}
// TickLoop.tracker is set to noopTracker{} in NewTickLoop today. Assign a real entityTracker instead
// (or via a SetTracker seam) — the t.tracker.Tick() call at tick_phases.go:26 is UNCHANGED.
type tickPlayer struct {
    client *Client
    x, y, z float64; yaw, pitch float32; onGround bool // Phase-5 position (tick-owned)
    center level.ChunkPos; viewDist int
    // ADD: tracked map[int32]bool (the per-player tracked entity-id set; tick-owned)
}
```

From server/tick_phases.go (the entity phase — FILL tickEntities; the call order is fixed):
```go
func (t *TickLoop) tickEntities() { t.trace("tickEntities") } // STUB — fill it
// tickOnce order: resolveSubtickInputs → tickWorld → tickChunks → tickEntities → tickAI →
//   tickPhysics → applyAsyncResults → tracker.Tick() → flushOutbound
// The tracker runs AFTER tickEntities/tickPhysics and applyAsyncResults (06-RESEARCH Pattern 1).
```

From server/entity_store.go (Plan 06-01 — the store + broad phase):
```go
type Entity struct { /* id, typ entity.ID, uuid, x/y/z, vx/vy/vz, yaw/pitch/headYaw, onGround, width/height, metadata */ }
func (s *entityStore) near(x, z float64, rangeChunks int) []*Entity
func (s *entityStore) get(id int32) (*Entity, bool)
type EntityIDAllocator struct{ /* AllocID() int32 */ }
```

From data/packetid/packetid.go (all IDs generated — VERIFIED present per 06-RESEARCH):
```go
ClientboundAddEntity, ClientboundSetEntityData, ClientboundMoveEntityPos, ClientboundMoveEntityPosRot,
ClientboundMoveEntityRot, ClientboundTeleportEntity, ClientboundEntityPositionSync, ClientboundRotateHead,
ClientboundSetEntityMotion, ClientboundRemoveEntities
```

From net/packet (primitives — all exist):
```go
type Double float64; type Float float32; type Short int16; type Byte int8; type UnsignedByte uint8
type VarInt int32; type UUID uuid.UUID; type Angle int8 // byte-angle
func Marshal[ID ~int32|int](id ID, fields ...FieldEncoder) Packet
func (p Packet) Scan(fields ...FieldDecoder) error
```

From server/play_join.go (the byte-angle helper precedent — pk.Angle from a degree float):
```go
// degrees → byte angle: round(deg * 256 / 360) as an int8/Angle. Mirror the existing precedent if present.
```
</interfaces>

<key_facts>
<!-- VERIFIED against 06-RESEARCH this session. -->
- FILL THE SEAM, DO NOT CHANGE IT (06-RESEARCH Pattern 1 + Anti-Patterns): keep `type tracker interface{ Tick() }` and the `t.tracker.Tick()` call site at tick_phases.go:26 UNCHANGED. Assign a real `entityTracker{loop: t}` to t.tracker (in NewTickLoop or a SetTracker method). Phase 8 (OPT-02) swaps the executor off-tick behind this exact seam — that is why the interface must not change.
- SYNCHRONOUS ONLY (06-RESEARCH Anti-Patterns): the tracker runs ON the tick goroutine, reads tick-owned entity state, emits via player.client.Send. NO goroutines, NO xsync/ants/conc, NO async tracker. CLAUDE.md "Stack Patterns by Variant" — do NOT introduce those deps; no async subsystem exists to optimize until Phase 8.
- VISIBILITY DIFF (06-RESEARCH Pattern 1, steps 2-4): for each player, visible := store.near(p.x, p.z, trackRange). For each visible e: if !p.tracked[e.id] → Send(encodeAddEntity(e)); if metadata non-default → Send(encodeSetEntityData(e)); if moving → optionally Send(encodeSetEntityMotion(e)); set p.tracked[e.id]=true. Else (already tracked) → Send a delta encodeMove*(e) or a teleport for a large delta + encodeRotateHead. After the loop, any id in p.tracked NOT in visible → batch into ONE encodeRemoveEntities(ids) and delete from p.tracked. Use a single conservative trackRange for v1 (06-RESEARCH A5: players 48 / mobs ~80; a single range is acceptable, tunable).
- JAR-DERIVE IN TASK 0 (06-RESEARCH Open Questions 1-2 / A2-A3): decompile to settle (a) the Vec3.LP_STREAM_CODEC short scaling for AddEntity/SetEntityMotion movement (historically velocity×8000 clamped to short — confirm the 776 factor); (b) the SynchedEntityData$DataValue.write framing (Byte id, VarInt serializerId, codec value) + the pack() 0xFF (255) terminator; (c) whether the v1 test entity renders from AddEntity + the 0xFF-only metadata or needs a non-default entry. Record findings inline in server/entity_encode.go + in 06-CAPTURE-DIFF.md. Do NOT guess. `javap -p -c -classpath temp/cache/26.2-inner.jar net.minecraft.network.protocol.game.ClientboundAddEntityPacket` and `...ClientboundSetEntityDataPacket` + `net.minecraft.network.syncher.SynchedEntityData`.
- ADDENTITY WIRE (06-RESEARCH Code Example, jar-derived): VarInt id, UUID uuid, VarInt entityTypeId, Double x/y/z, Short LP-movement vx/vy/vz (NOT 3 Doubles — the LP low-precision short), Byte xRot, Byte yRot, Byte yHeadRot, VarInt data. The Short LP scaling is the Task-0 finding.
- SETENTITYDATA WIRE (06-RESEARCH Pitfall 1): an OPEN-ENDED list of entries, each = Byte index, VarInt serializerTypeId, serializer-specific value; the list TERMINATES with a single Byte 0xFF (255). For v1 emit the MINIMUM metadata — often just the 0xFF terminator (no entries) or a single shared-flags index-0 entry. Omitting 0xFF desyncs the stream and the client drops the entity.
- DELTA vs TELEPORT (06-RESEARCH Pattern 1): MoveEntityPos/PosRot encode a SHORT delta (the (curr-prev)*scale short, vanilla uses *4096 for the position delta — confirm in Task 0 if you encode deltas; otherwise use TeleportEntity/EntityPositionSync with absolute Doubles for v1 simplicity and correctness, and note the choice). For v1 it is acceptable to favor TeleportEntity (absolute) for moved entities to avoid the delta-overflow edge — document the choice; deltas are a later optimization.
- tickEntities (06-RESEARCH Pattern 1 + the pipeline): fill the stub to do the per-tick entity step — for THIS plan that is at minimum keeping the bucket consistent for any entity whose position changed and ageing/bookkeeping; the physics that MOVES entities is Plan 06-03. Keep it on the tick goroutine over the tick-owned store.
- SINGLE-OWNER (TICK-05): tracker, tickEntities, the tracked-set mutation all run on the tick goroutine. player.client.Send goes through the bounded queue (writeLoop stays sole socket writer).
- A v1 TEST ENTITY: this plan should be able to spawn ONE test entity into the store (a test helper or a tiny debug spawn) so the tracker has something to emit and the real-client check (Plan 06-07) sees something. Use entity.SulfurCube or a simple visible mob; assign its id from the allocator. Keep any debug spawn behind a clear flag/helper so it does not pollute production unintentionally — the real-client gate needs a visible entity.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/tracker.go (entityTracker + Tick), server/entity_encode.go (the encoders), server/tracker_test.go + server/entity_encode_test.go. Add the tracked map to tickPlayer + assign the real tracker (symbol-level edits in server/tick.go). Fill tickEntities in server/tick_phases.go. Write the Task-0 findings into 06-CAPTURE-DIFF.md.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sulfur entity encoders → vanilla 26.2 client | A mis-framed AddEntity (wrong LP movement codec) or SetEntityData (missing 0xFF terminator, wrong serializer codec) desyncs the client's entity stream — the entity appears then vanishes, or the client logs "unknown data value id". The authoritative check is the jar-derive + the Plan-06-07 capture-diff + the real-client visual. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering (data integrity) | AddEntity LP movement / SetEntityData framing diverging from vanilla (self-round-trips green, client drops the entity) | mitigate | Jar-derive the Vec3.LP short scaling + the SetEntityData 0xFF terminator framing (Task 0); for v1 emit minimal metadata (0xFF-only) so there is no serializer-codec surface to get wrong; byte-seal both in Plan 06-07's capture-diff against a real vanilla server + the real-client visual. |
| T-6-08 | Tampering/race | the tracker mutating entity/tracked state off the tick goroutine | mitigate | The tracker runs SYNCHRONOUSLY on the tick goroutine (the Phase-3 seam is filled, not made async); all reads of the store + writes of the tracked set happen on the owner; Send goes through the bounded queue. Proven by the Docker -race gate. |
| T-6-10 | Denial of Service | a player's tracked set or a RemoveEntities batch growing unbounded | accept | The visible set is bounded by store.near()'s clamped trackRange; the tracked set is bounded by the number of entities in range; entity spawning is server-driven (not client-driven), so the client cannot inflate either. Mob-spawn caps are Phase 7. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Jar-derive the Vec3.LP short scaling + the SetEntityData 0xFF framing (Wave 0)</name>
  <files>server/entity_encode.go, .planning/phases/06-entities-physics-interaction/06-CAPTURE-DIFF.md</files>
  <read_first>
    - .planning/phases/06-entities-physics-interaction/06-RESEARCH.md — Open Questions 1-2, Assumptions A2-A3, Pitfall 1, the AddEntity Code Example.
    - server/play_join.go — the existing pk.Marshal encoder style + any byte-angle helper to mirror.
  </read_first>
  <behavior>
    ENT-01 (settle the two MEDIUM wire surfaces before encoding):
    - The Vec3.LP_STREAM_CODEC movement short scaling for AddEntity/SetEntityMotion is decompiled and recorded (the ×factor and clamp).
    - The SynchedEntityData$DataValue.write framing (Byte id, VarInt serializerId, codec value) + the pack() 0xFF terminator is decompiled and recorded.
    - 06-CAPTURE-DIFF.md records the findings + whether the v1 test entity needs a non-default metadata entry (try 0xFF-only first).
  </behavior>
  <action>
    1. Decompile and record (inline code comments in server/entity_encode.go + a section in 06-CAPTURE-DIFF.md):
       `javap -p -c -classpath temp/cache/26.2-inner.jar net.minecraft.network.protocol.game.ClientboundAddEntityPacket`
       `javap -p -c -classpath temp/cache/26.2-inner.jar net.minecraft.world.phys.Vec3` (the LP_STREAM_CODEC scaling)
       `javap -p -c -classpath temp/cache/26.2-inner.jar net.minecraft.network.protocol.game.ClientboundSetEntityDataPacket`
       `javap -p -c -classpath temp/cache/26.2-inner.jar net.minecraft.network.syncher.SynchedEntityData`
       Settle: the LP movement short factor + clamp; the DataValue.write field order; the pack() terminator byte (expect 255/0xFF).
    2. Create server/entity_encode.go with the package header + a documented `func lpShort(v float64) pk.Short` helper encoding the jar-derived LP scaling, and a documented `degToByteAngle(deg float32) pk.Angle` helper — with the javap-derived facts in comments. (The full encoders are Task 1; this task establishes the jar-derived primitives + the record so the encoders are not guessed.)
    3. Begin 06-CAPTURE-DIFF.md: the capture method (mirror 05-CAPTURE-DIFF.md), the LP-scaling finding, the SetEntityData framing finding, and the v1-metadata decision (0xFF-only first). Mark the byte-level seal as deferred to Plan 06-07.
  </action>
  <verify>
    <automated>go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Vec3.LP short scaling and the SetEntityData 0xFF framing are jar-derived (javap output recorded in code comments + 06-CAPTURE-DIFF.md) — not guessed.
    - lpShort + degToByteAngle helpers exist with the jar-derived scaling; the v1-metadata decision is recorded.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The two MEDIUM entity wire surfaces are jar-derived and the encoding primitives exist — Task 1 can build AddEntity/SetEntityData on settled bytes, and Plan 06-07 byte-seals them against a real vanilla server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Jar-derived entity encoders (AddEntity, SetEntityData, Move*, Teleport, RotateHead, SetEntityMotion, RemoveEntities) (ENT-01)</name>
  <files>server/entity_encode.go, server/entity_encode_test.go</files>
  <read_first>
    - .planning/phases/06-entities-physics-interaction/06-RESEARCH.md — Pattern 1, Pitfall 1, the AddEntity Code Example, the ENT-01 packet-id table.
    - server/entity_encode.go — the Task-0 lpShort/degToByteAngle helpers + the jar-derived facts.
    - data/packetid/packetid.go — the Clientbound* entity ids.
  </read_first>
  <behavior>
    ENT-01 (the jar-derived clientbound encoders):
    - Test 1 (TestAddEntityWire): encodeAddEntity(e) has id ClientboundAddEntity and a body that decodes VarInt id, UUID, VarInt typeId, Double x/y/z, Short vx/vy/vz (LP), Byte xRot/yRot/yHeadRot, VarInt data back to the entity's values (LP movement round-trips through lpShort).
    - Test 2 (TestSetEntityDataWire): encodeSetEntityData with no entries emits exactly the single 0xFF terminator byte after the entity id; with one entry it emits Byte index, VarInt serializerId, value, then 0xFF. The terminator is ALWAYS present.
    - Test 3 (TestMoveAndRemoveWire): encodeTeleportEntity / encodeMoveEntityPos / encodeRotateHead encode the jar-derived layout; encodeRemoveEntities([]int32{a,b,c}) emits a VarInt-count-prefixed id list under ClientboundRemoveEntities.
  </behavior>
  <action>
    1. server/entity_encode.go — implement, each with a jar-derived field-order comment:
       - `encodeAddEntity(e *Entity) pk.Packet`: pk.Marshal(ClientboundAddEntity, VarInt id, UUID, VarInt(typ), Double x/y/z, lpShort(vx/vy/vz), degToByteAngle(pitch/yaw/headYaw), VarInt data).
       - `encodeSetEntityData(e *Entity) pk.Packet`: id + the indexed-entry list (for v1, minimal — 0xFF-only unless Task 0 found a required entry) + the mandatory 0xFF terminator. Use a small entries-encoder type or inline write.
       - `encodeSetEntityMotion(e *Entity) pk.Packet`: VarInt id + lpShort(vx/vy/vz).
       - `encodeMoveEntityPos/PosRot/Rot` (delta) AND `encodeTeleportEntity` (absolute Double x/y/z + byte angles + onGround) — for v1 the tracker MAY favor TeleportEntity for moved entities (document the choice; deltas are an optimization). `encodeRotateHead(id, headYaw)`.
       - `encodeRemoveEntities(ids []int32) pk.Packet`: VarInt count + N VarInt ids.
    2. server/entity_encode_test.go (package server) — the 3 tests in <behavior>, asserting the packet id + the jar-derived body round-trip (esp. the 0xFF terminator always present and the LP short scaling).
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestAddEntityWire|TestSetEntityDataWire|TestMoveAndRemoveWire' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All entity encoders use the jar-derived field order; AddEntity uses the LP short movement (not 3 Doubles); SetEntityData ALWAYS emits the 0xFF terminator.
    - RemoveEntities is a VarInt-count-prefixed id list; the move/teleport/rotate encoders match the jar.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The jar-derived clientbound entity encoders exist and round-trip — the tracker (Task 2) can emit spawn/move/despawn packets a real client will accept (byte-sealed in Plan 06-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronous entityTracker behind tracker.Tick() + fill tickEntities + assign the tracker (ENT-01)</name>
  <files>server/tracker.go, server/tick.go, server/tick_phases.go, server/tracker_test.go</files>
  <read_first>
    - .planning/phases/06-entities-physics-interaction/06-RESEARCH.md — Pattern 1 (the full tracker), Anti-Patterns (synchronous only; do not change the seam), A5 (track range).
    - server/tick.go — the tracker interface + noopTracker + NewTickLoop (assign the real tracker) + tickPlayer (add tracked).
    - server/tick_phases.go — tickEntities stub + the t.tracker.Tick() call site (UNCHANGED) + the fixed phase order.
    - server/entity_store.go (Plan 06-01) — near()/get(); server/entity_encode.go (Task 1) — the encoders.
  </read_first>
  <behavior>
    ENT-01 (the synchronous tracker — the phase's headline behavior):
    - Test 1 (TestVisibilityDiff): a player at a position with a test entity in range → on the FIRST Tick the player receives ClientboundAddEntity (+ SetEntityData) and p.tracked[id] becomes true; a SECOND Tick with the entity unmoved sends NO new Add (no duplicate spawn).
    - Test 2 (TestTrackerMove): the entity moves within range → the next Tick sends a Move/Teleport (+ RotateHead) for the tracked entity (not a re-Add).
    - Test 3 (TestTrackerRemove): the entity moves out of range (or is removed) → the next Tick sends exactly ONE ClientboundRemoveEntities containing the id and deletes it from p.tracked; a re-entry re-Adds it.
    - Test 4 (TestTrackerSynchronousNoChangeToSeam): the tracker interface + the t.tracker.Tick() call site are unchanged (the entityTracker satisfies the existing one-method interface); no goroutine is spawned by Tick().
  </behavior>
  <action>
    1. server/tick.go — add `tracked map[int32]bool` to tickPlayer (lazy-init in the tracker; tick-owned, the per-player tracked entity-id set). Assign a real tracker: in NewTickLoop set `t.tracker = &entityTracker{loop: t}` (or add `func (t *TickLoop) useEntityTracker()` and call it where appropriate) WITHOUT changing the interface or the call site. Document the swap-point for Phase 8.
    2. server/tracker.go — `type entityTracker struct { loop *TickLoop }`; `func (et *entityTracker) Tick()` per 06-RESEARCH Pattern 1: for each p in loop.players, visible := loop.entities.near(p.x, p.z, trackRange); diff against p.tracked → Add(+SetEntityData[+SetEntityMotion]) for new, Move/Teleport(+RotateHead) for moved-and-tracked, batch RemoveEntities for gone; all via p.client.Send. Define a single conservative trackRange const (A5). Guard nil p.client / nil store. NO goroutines.
    3. server/tick_phases.go — fill tickEntities: the per-tick entity step (age/bookkeeping + keep the store's buckets consistent for any entity whose position changed this tick). Keep it minimal (physics that MOVES entities is Plan 06-03) and on the tick goroutine over the tick-owned store. Do NOT modify the t.tracker.Tick() call site.
    4. server/tracker_test.go (package server) — the 4 tests in <behavior>. Construct a TickLoop, spawn a test entity into loop.entities via the allocator, register a tickPlayer at a known position, call loop.tracker.Tick() (or the entityTracker directly), and assert the drained client packets per scenario (reuse the pipe/drain helpers from the existing server tests).
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestVisibilityDiff|TestTrackerMove|TestTrackerRemove|TestTrackerSynchronousNoChangeToSeam|TestTickPhaseOrder' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The entityTracker is assigned to TickLoop.tracker WITHOUT changing the tracker interface or the t.tracker.Tick() call site (Phase-8-ready seam preserved); Tick() spawns no goroutine.
    - Per tick: newly-visible → Add(+SetEntityData), moved-and-tracked → Move/Teleport(+RotateHead), gone → ONE batched RemoveEntities; the per-player tracked set bookkeeps correctly (no duplicate spawns, clean re-entry).
    - tickEntities is filled (per-tick step + bucket consistency); the fixed phase order (TestTickPhaseOrder) still holds.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>A synchronous entityTracker behind the unchanged tracker.Tick() seam sends spawn/move/despawn to nearby players — ENT-01's headline behavior is implemented (Phase-8-swappable). Run the Docker -race gate over ./server/... before marking the wave done (the tracker mutates only tick-owned state on the owner goroutine — TICK-05 / T-6-08).</done>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestAddEntityWire|TestSetEntityDataWire|TestMoveAndRemoveWire|TestVisibilityDiff|TestTrackerMove|TestTrackerRemove|TestTrackerSynchronousNoChangeToSeam' -count=1` passes.
- The fixed phase order (TestTickPhaseOrder) and the existing server tests still pass — the seam is filled, not changed.
- Docker `-race` gate over `./server/...` is clean (the tracker is synchronous on the tick goroutine; the store + tracked set cross no boundary mutably — TICK-05 / T-6-08).
- `go vet ./... && go build ./...` clean; zero new dependencies (NO xsync/ants/conc).
</verification>

<success_criteria>
- ENT-01 (headline): a SYNCHRONOUS entityTracker behind the unchanged tracker.Tick() seam diffs per-player visibility (from the store's grid broad-phase) and emits AddEntity(+SetEntityData) / Move-or-Teleport(+RotateHead) / one batched RemoveEntities — nearby players see entities spawn, move, and despawn.
- The AddEntity LP short movement + the SetEntityData 0xFF framing are jar-derived (not guessed) and recorded in 06-CAPTURE-DIFF.md for Plan 06-07's byte-seal.
- The Phase-3 seam is FILLED, not modified (Phase-8-swappable); NO async, NO new deps; single-owner / -race clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-entities-physics-interaction/06-02-SUMMARY.md`.
</output>
</content>
