---
phase: 35-hostiles
plan: 05
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - server/ai_goals_attack.go
  - plugins/vanilla_spider/main.star
  - plugins/vanilla_spider/plugin.toml
  - server/assets/vanilla_spider/main.star
  - server/assets/vanilla_spider/plugin.toml
  - server/spider_test.go
must_haves:
  truths:
    - "vanilla_spider declares Spider.registerGoals: goalSelector FloatGoal@1 + LeapAtTargetGoal@3 + SpiderAttackGoal@4 + WaterAvoidingRandomStroll@5 + LookAtPlayer@6 + LookAround@6; targetSelector HurtByTargetGoal@1 + SpiderTargetGoal<Player>@2"
    - "LeapAtTargetGoal draws nextFloat (lockstep) and sets a velocity impulse toward the target; SpiderAttackGoal does not attack in daylight (the gametime proxy)"
    - "the spider spawns, acquires a player target, leaps/paths to it, and deals melee damage"
    - "the repo-root and embed copies of vanilla_spider are byte-identical"
  artifacts:
    - path: "server/ai_goals_attack.go"
      provides: "leapAtTargetGoal (nextFloat impulse) + spiderAttackGoal daylight gate"
      contains: "leapAtTargetGoal"
    - path: "plugins/vanilla_spider/main.star"
      provides: "the jar-faithful spider declaration"
      contains: "declare_mob"
    - path: "server/assets/vanilla_spider/main.star"
      provides: "the byte-identical embed copy"
      contains: "declare_mob"
    - path: "server/spider_test.go"
      provides: "the per-hostile behavior + the leap nextFloat RNG gate"
      contains: "func TestSpider"
  key_links:
    - from: "plugins/vanilla_spider/main.star LeapAtTargetGoal tick"
      to: "the velocity impulse (setDeltaMovement)"
      via: "entity.rand_float() gate"
      pattern: "rand_float"
    - from: "plugins/vanilla_spider/main.star SpiderAttackGoal"
      to: "isDarkEnoughToSpawn / the day proxy"
      via: "the daylight no-attack gate"
      pattern: "daylight|dark"
requirements: [MOB-HOST-03]
autonomous: true
---

<objective>
Build the spider as a jar-faithful dogfood plugin (MOB-HOST-03, SC#2) — Spider.registerGoals re-expressed as vanilla_spider. The spider is the differentiated hostile: a FloatGoal@1 (copied from cow), a LeapAtTargetGoal@3 with its OWN nextFloat-gated velocity impulse (the one NEW RNG goal in this plan, ported into ai_goals_attack.go), a daylight-gated SpiderAttackGoal@4, the shared passive goals, and the 35-01 target goals. The spider hunts the player at night, leaps, and melees (but not in daylight).

Purpose: The third hostile, exercising the LeapAtTargetGoal impulse goal (the ONE goal that impulses rather than paths) + the daylight aggression gate — the spider's differentiators. The leap nextFloat is lockstep-tested.
Output: the leapAtTargetGoal + spiderAttackGoal daylight gate in ai_goals_attack.go (the only shared Go change in Wave 2 — confined to the file 35-01 created), the vanilla_spider pair (byte-identical), + a behavior + leap-RNG test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-hostiles/35-JARNOTES.md
@.planning/phases/35-hostiles/35-PATTERNS.md
@.planning/phases/35-hostiles/35-CONTEXT.md
@./CLAUDE.md
@plugins/vanilla_cow/main.star
@plugins/vanilla_cow/plugin.toml
@server/cow_test.go

<interfaces>
<!-- builtins + host handles as 35-03/35-04, plus the leap impulse seam. -->

declare_mob/goal builtins + flags routing — see plugins/vanilla_cow/main.star + 35-03. base_type "spider"
already exists. FloatGoal callbacks COPY VERBATIM from vanilla_cow (float_can_use/float_tick,
vanilla_cow/main.star:78-85, the nextFloat()<0.8 swim-jump). The shared stroll/look/around COPY VERBATIM.

The leap impulse: combat_mob.go knockbackEntity (lines 460-466) sets a velocity impulse via setDeltaMovement
— the EXACT shape LeapAtTargetGoal uses (it impulses, it does NOT path — 35-PATTERNS.md:161-164, 456-460).
The leap nextFloat is the same shape as FloatGoal.tick (entity.rand_float(), vanilla_cow/main.star:84).

The daylight gate: the spider's SpiderAttackGoal "won't attack in daylight" — gate on the SAME day/night
proxy the spawn gate uses (35-02's t.isDarkEnoughToSpawn / the t.gametime night window).

Spider attrs (35-JARNOTES.md:77): max_health 16.0, movement_speed 0.3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: leapAtTargetGoal (nextFloat impulse) + spiderAttackGoal daylight gate in ai_goals_attack.go</name>
  <read_first>
    - server/ai_goals_attack.go (the 35-01 meleeAttackGoal base — spiderAttackGoal subclasses it)
    - server/combat_mob.go (knockbackEntity setDeltaMovement lines 460-466 — the impulse seam)
    - server/ai_goals_passive.go (mobRandom(e) helper + the nextFloat draw shape)
    - server/spawner.go (the 35-02 isDarkEnoughToSpawn / t.gametime proxy — the daylight gate source)
    - .planning/phases/35-hostiles/35-JARNOTES.md (LeapAtTargetGoal line 127,140,161-164 + SpiderAttackGoal line 63,175 + MeleeAttackGoal 166-175)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the LeapAtTargetGoal + SpiderAttackGoal entries lines 161-168, 456-460)
  </read_first>
  <behavior>
    - Test: leapAtTargetGoal.canUse: with a target acquired + the entity on-ground, the nextFloat gate (the
      vanilla LeapAtTargetGoal canUse probability check) fires exactly one rand_float; when the gate passes it
      returns true. The draw is exactly one rand_float per canUse.
    - Test: leapAtTargetGoal start/tick sets a velocity impulse toward the target (vertical + horizontal
      delta), NOT a nav path.
    - Test: spiderAttackGoal does not perform the melee hit when it is daytime (isDarkEnoughToSpawn false);
      it does at night.
  </behavior>
  <action>
Port net.minecraft.world.entity.ai.goal.LeapAtTargetGoal + SpiderAttackGoal into ai_goals_attack.go (the file
35-01 created). VERIFY each against the jar at exec (the OPEN decompile list, 35-JARNOTES:194-198) before
writing.

1. leapAtTargetGoal (cite LeapAtTargetGoal):
   canUse(): target := e.ai.getTarget(); if target == 0 { return false }; require on-ground; the vanilla
     canUse draws getRandom().nextFloat() as part of its gate (the leap probability) — draw it via
     mobRandom(e).nextFloat() in the EXACT bytecode position (cite the LeapAtTargetGoal.canUse line). Return
     per the gate + the distance band.
   start()/tick(): compute the horizontal delta toward the target + the vertical leap component and set the
     velocity impulse via the setDeltaMovement seam (the knockbackEntity shape, combat_mob.go:460-466) — the
     leap IMPULSES, it does NOT setWantTarget (the ONE goal exception, 35-PATTERNS.md:456-460). Cite the exact
     LeapAtTargetGoal vector math (the (dx,dz) normalized * 0.4 + the y component) from the decompile.

2. spiderAttackGoal (cite SpiderAttackGoal) — subclass meleeAttackGoal with the daylight delta: it will NOT
   perform the attack in daylight. Gate the checkAndPerformAttack (or canContinueToUse, per the bytecode) on
   t.isDarkEnoughToSpawn() (the 35-02 proxy — the same gate the spawn pass uses). When daytime, the spider
   still targets/paths but does not deal the melee hit (faithful to "won't attack in daylight"). Cite
   SpiderAttackGoal's canContinueToUse/getAttackReachSqr daylight check + the brightness read it replaces
   (proxied by the gametime window, consistent with the 35-02 forced decision).

3. Extend ai_goals_target_test.go (or add to spider_test.go in Task 3) — but the GO-LEVEL leap/daylight unit
   tests live here: TestLeapAtTargetGateRNG (exactly one rand_float in canUse), TestLeapSetsImpulse (the delta
   is set, no nav path), TestSpiderAttackDaylightGate (no melee hit when isDarkEnoughToSpawn false).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./server/... && go test ./server/ -run "TestLeapAtTargetGateRNG|TestLeapSetsImpulse|TestSpiderAttackDaylightGate" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "leapAtTargetGoal" server/ai_goals_attack.go` AND `grep -n "spiderAttackGoal" server/ai_goals_attack.go` return.
    - `grep -n "nextFloat" server/ai_goals_attack.go` returns in the leap canUse (the lockstep draw).
    - `grep -n "setDeltaMovement\|deltaMovement\|setDelta" server/ai_goals_attack.go` returns (the impulse, NOT a nav path).
    - `grep -n "isDarkEnoughToSpawn" server/ai_goals_attack.go` returns (the spider daylight gate).
    - `cd D:/ender && go test ./server/ -run "TestLeapAtTargetGateRNG|TestLeapSetsImpulse|TestSpiderAttackDaylightGate" -count=1` exits 0.
  </acceptance_criteria>
  <done>LeapAtTargetGoal draws one nextFloat and impulses toward the target; SpiderAttackGoal gates the melee on the daylight proxy; the Go unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: vanilla_spider plugin pair (FloatGoal + LeapAtTarget + daylight melee, repo-root + byte-identical embed)</name>
  <read_first>
    - plugins/vanilla_cow/main.star (the template — incl. float_can_use/float_tick lines 78-85 to COPY VERBATIM)
    - plugins/vanilla_cow/plugin.toml + server/assets/vanilla_cow/* (the manifest + embed sibling)
    - server/ai_goals_attack.go (the Task-1 leapAtTargetGoal + spiderAttackGoal — the host-handle/goal-kind they expose)
    - .planning/phases/35-hostiles/35-JARNOTES.md (Spider.registerGoals lines 57-71 + attributes line 77)
    - .planning/phases/35-hostiles/35-CONTEXT.md (Spider goal set line 57-59 + AvoidArmadillo deferral)
    - .planning/phases/35-hostiles/35-01-SUMMARY.md (the target/melee goal integration shape)
  </read_first>
  <behavior>
    - The vanilla_spider declaration loads through the host (no parse error).
    - The goal set + priorities + flags match Spider.registerGoals (v1 subset, AvoidArmadillo deferred).
    - The repo-root and server/assets copies are byte-identical.
  </behavior>
  <action>
Create plugins/vanilla_spider/main.star — a 1:1 vanilla-Spider dogfood, citing
net.minecraft.world.entity.monster.spider.Spider.registerGoals. Copy the vanilla_cow header/structure style.

GOAL SET (35-CONTEXT:57-59, 35-JARNOTES:57-71 — the v1 subset; AvoidEntityGoal<Armadillo>@2 DEFERRED):
  goalSelector:
    @1 FloatGoal(mob)                            flags=["JUMP"]         — COPY VERBATIM from vanilla_cow (float_can_use/float_tick, requires_update_every_tick=True)
    @3 LeapAtTargetGoal(mob, 0.4)                flags=["MOVE"]         — the Task-1 leapAtTargetGoal (nextFloat impulse, leap height 0.4)
    @4 SpiderAttackGoal(mob)                     flags=["MOVE","LOOK"]  — the Task-1 spiderAttackGoal (daylight-gated melee)
    @5 WaterAvoidingRandomStrollGoal(mob, 0.8)   flags=["MOVE"]         — COPY VERBATIM from vanilla_cow stroll
    @6 LookAtPlayerGoal(Player, 8.0)             flags=["LOOK"]         — COPY from vanilla_cow look
    @6 RandomLookAroundGoal                       flags=["MOVE","LOOK"]  — COPY VERBATIM from vanilla_cow around
  targetSelector:
    @1 HurtByTargetGoal(mob)                      flags=["TARGET"]       — the 35-01 hurtByTargetGoal
    @2 SpiderTargetGoal<Player>(mob)              flags=["TARGET"]       — a daylight-gated NearestAttackableTargetGoal
                                                                           subclass: the 35-01 nearestAttackableTargetGoal
                                                                           with the daylight aggression gate (35-JARNOTES:69)

The shared passive callbacks (float/stroll/look/around) COPY VERBATIM from vanilla_cow. The leap + spider-melee
goals reference Task-1's leapAtTargetGoal/spiderAttackGoal (wire per their exposed handle/goal-kind). The
SpiderTargetGoal is the 35-01 nearestAttackableTargetGoal with the daylight gate (cite SpiderTargetGoal extends
NearestAttackableTargetGoal, daylight-gated). The leap rand_float + the target rand_int(10) gates are the
lockstep draws (cite each). Document the DEFERRED AvoidEntityGoal<Armadillo>@2 + the IronGolem target variant.

ATTRIBUTES dict (35-CONTEXT:65, 35-JARNOTES:77): max_health 16.0, movement_speed 0.3. Each cited to
Spider.createAttributes (Monster + MAX_HEALTH 16.0 + MOVEMENT_SPEED 0.3).

base_type = "spider".

Create plugins/vanilla_spider/plugin.toml — clone vanilla_cow/plugin.toml's least-privilege cap set (the
leap impulse + melee are host-side ops). Create the BYTE-IDENTICAL embed copies at server/assets/vanilla_spider/*.
  </action>
  <verify>
    <automated>cd D:/ender && diff plugins/vanilla_spider/main.star server/assets/vanilla_spider/main.star && diff plugins/vanilla_spider/plugin.toml server/assets/vanilla_spider/plugin.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "declare_mob" plugins/vanilla_spider/main.star` AND `grep -n "base_type = \"spider\"" plugins/vanilla_spider/main.star` return.
    - `grep -c "\"TARGET\"" plugins/vanilla_spider/main.star` >= 2.
    - `grep -n "rand_float" plugins/vanilla_spider/main.star` returns (the leap gate) AND `grep -n "rand_int(10)" plugins/vanilla_spider/main.star` returns AND `grep -c "rand_int(5)" plugins/vanilla_spider/main.star` == 0.
    - `grep -ci "DEFERRED\|Armadillo" plugins/vanilla_spider/main.star` >= 1.
    - `diff plugins/vanilla_spider/main.star server/assets/vanilla_spider/main.star` EMPTY.
    - `diff plugins/vanilla_spider/plugin.toml server/assets/vanilla_spider/plugin.toml` EMPTY.
  </acceptance_criteria>
  <done>The vanilla_spider pair declares the FloatGoal + LeapAtTarget + daylight-melee goal set with TARGET routing + both lockstep gates + the Armadillo deferral; both copies byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: spider_test.go — spawn + goal-set + leap + daylight-melee behavior + the leap RNG gate</name>
  <read_first>
    - server/cow_test.go (the loadVanillaCowRegistry harness lines 36-60)
    - server/zombie_test.go (the sibling per-hostile test shape from 35-03)
    - plugins/vanilla_spider/main.star (the declaration under test)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the hostiles_test.go entry)
  </read_first>
  <behavior>
    - TestSpiderBootLoads: the registry loads + the goal count (6 goalSelector + 2 targetSelector) + the 2
      TARGET goals route into the targetSelector.
    - TestSpiderBehavior: spawn a spider + a player at night within FOLLOW_RANGE; tick; assert attackTargetID
      == player, a leap impulse fires (the delta is set) at the gated nextFloat, and the melee hit drops player
      health at night; assert NO melee hit when forced to daytime.
    - TestSpiderLeapGateRNG: the LeapAtTargetGoal canUse draws exactly one rand_float.
  </behavior>
  <action>
Create server/spider_test.go — clone the cow_test.go harness as loadVanillaSpiderRegistry. Clone the
zombie_test.go shapes plus the spider-specific leap/daylight assertions:
  - TestSpiderBootLoads: assert name "vanilla_spider", goal count (6 + 2), and the TARGET routing.
  - TestSpiderBehavior: spawn + player at night; seed the gates (nextInt(10)==0, the leap nextFloat passes);
    tick; assert attackTargetID == player + a leap velocity impulse set + the melee hit at night; flip to
    daytime + assert NO melee hit (the daylight gate).
  - TestSpiderLeapGateRNG: assert exactly one rand_float draw in the LeapAtTargetGoal canUse.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./server/ -run "TestSpiderBootLoads|TestSpiderBehavior|TestSpiderLeapGateRNG" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func TestSpiderBootLoads" server/spider_test.go` AND `grep -n "func TestSpiderBehavior" server/spider_test.go` AND `grep -n "func TestSpiderLeapGateRNG" server/spider_test.go` all return.
    - `grep -n "loadVanillaSpiderRegistry" server/spider_test.go` returns.
    - `cd D:/ender && go test ./server/ -run "TestSpiderBootLoads|TestSpiderBehavior|TestSpiderLeapGateRNG" -count=1` exits 0.
  </acceptance_criteria>
  <done>The spider boot-loads with the FloatGoal/LeapAtTarget/daylight-melee routing, leaps + melees the player at night (not in daylight), and the leap gate draws exactly rand_float.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vanilla_spider .star -> host buildAIFromDecl | the declared goal set + attributes cross into the host |
| leapAtTargetGoal -> entity velocity | a goal sets a velocity impulse (a movement vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Tampering | leapAtTargetGoal velocity impulse | mitigate | the impulse is host-computed from the acquired target + the jar-cited 0.4 vector (not plugin-supplied); bounded by the same setDeltaMovement path knockback uses — no unbounded velocity. |
| T-35-11 | Elevation | spiderAttackGoal melee -> player damage | mitigate | routes through the shared 35-01 host attack handle (attacker = mob id); additionally daylight-gated, so the attack surface is strictly narrower than the zombie's. |
</threat_model>

<verification>
- `go build ./server/...` clean; `diff` of both copies empty; `go test` of the spider + leap/daylight tests green.
- The goal set matches Spider.registerGoals (v1 subset); both lockstep gates (rand_int(10) + rand_float).
- The daylight gate suppresses the melee in daytime.
</verification>

<success_criteria>
- SC#2 (spider half): the spider acquires + leaps + melees the player at night; maps to MONSTER.
- MOB-HOST-03: the jar-faithful Spider.registerGoals plugin with its target selector + the wall-climb/light-gated-aggression differentiators (light-gated aggression shipped; wall-climb cite-deferred if no climb subsystem — record it).
</success_criteria>

<output>
After completion, create `.planning/phases/35-hostiles/35-05-SUMMARY.md` (record the deferred AvoidArmadillo, the IronGolem target, and the wall-climb if deferred; note the leap nextFloat lockstep + the daylight-melee gate).
</output>
