---
phase: 29-damage-keystone-s2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/java/GenTags.java
  - tools/gen_tags.go
  - tools/main.go
  - data/tag/tags.go
  - data/tag/tags_test.go
autonomous: true
requirements: [MOB-SUB-03]

must_haves:
  truths:
    - "The codegen pipeline (`cd tools && go run . --version 26.2`) emits data/tag/tags.go with real damage-type AND item tag membership maps."
    - "`tag.DamageTypeTags[\"bypasses_armor\"]` and `tag.DamageTypeTags[\"panic_causes\"]` are non-empty and contain the exact damage-type ids the jar JSON lists (recursively flattened)."
    - "Nested `#`-tag refs inside panic_causes.json (e.g. #minecraft:is_player_attack) are flattened so panic_causes ⊇ is_player_attack members — NOT left as a literal '#...' string."
    - "Item tags (pig_food, carrot_on_a_stick, cow_food, wolf_food) are generated into the SAME package (tag.ItemTags) — generated-but-unwired in P29, consumer lands P32."
  artifacts:
    - path: "tools/java/GenTags.java"
      provides: "ZipFile jar-read extractor for data/minecraft/tags/{damage_type,item}/ with recursive nested-tag flatten, emits tags.json"
      contains: "ZipFile"
    - path: "tools/gen_tags.go"
      provides: "Generator: reads tags.json, emits data/tag/tags.go (DamageTypeTags + ItemTags maps)"
      contains: "func genTags"
    - path: "data/tag/tags.go"
      provides: "Generated tag membership tables"
      contains: "var DamageTypeTags"
    - path: "data/tag/tags_test.go"
      provides: "Membership + nested-flatten golden assertions vs javap"
      contains: "func TestTagMembership"
  key_links:
    - from: "tools/main.go"
      to: "genTags"
      via: "generators slice entry {\"tags\", genTags}"
      pattern: "\\{\"tags\", genTags\\}"
    - from: "data/tag/tags.go"
      to: "jar JSON data/minecraft/tags/damage_type/"
      via: "GenTags.java extractor → tags.json → gen_tags.go"
      pattern: "var DamageTypeTags = map\\[string\\]map\\[int32\\]bool"
---

<objective>
Build the shared codegen tag tooling that emits a genuine, jar-extracted damage-type tag membership table (and, front-loaded, the item-food tag table) into a new `data/tag/` package. This is the keystone's REAL deliverable for MOB-SUB-03 — it makes `source.is("bypasses_armor")` / `source.is("panic_causes")` real reads against jar data, NOT `const false` (PITFALLS Pitfall 3). Phase 29 CONSUMES only the damage-type tags (Plan 02/03); item tags are generated-but-unwired here (consumer is Phase 32 TemptGoal) — intentional shared-tooling front-loading per the locked CONTEXT decision (Grey Area 1), NOT a no-built-but-unwired violation (the data is real + tested headless).

Purpose: Without a real tag table, the mob `actuallyHurt` armor-bypass branch and (Phase 31) PanicGoal's panic-causing gate would be faked. This plan ships the data so they are genuine.
Output: `tools/java/GenTags.java`, `tools/gen_tags.go`, the `{"tags", genTags}` registration in `tools/main.go`, the generated `data/tag/tags.go`, and `data/tag/tags_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-damage-keystone-s2/29-CONTEXT.md
@.planning/phases/29-damage-keystone-s2/29-RESEARCH.md
@.planning/phases/29-damage-keystone-s2/29-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase this session — use directly, no exploration needed. -->

The generator framework (tools/main.go:34-45) — register a new generator here:
```go
var generators = []generator{
    // ... existing entries ...
    {"itemfood", genItemFood},
    {"registryid", genRegistryID},
}
```
A `generator` is `struct{ name string; fn func(jsonDir, goMCRoot string) error }`. Each fn runs in order (main.go:127).

The generator helper API (tools/helpers.go):
```go
func generatedHeader(generatorFile string, sourceFiles ...string) string  // l.22 — "// Code generated by ...; DO NOT EDIT."
func readJSON(path string, v any) error                                     // l.57
func writeFile(path string, data []byte) error                             // l.69
```

The generator pattern to MIRROR (tools/gen_item_food.go:64-147 — genItemFood):
```go
func genItemFood(jsonDir, goMCRoot string) error {
    // readJSON(filepath.Join(jsonDir, "items.json"), &report)
    // build sorted rows (sort.Slice for deterministic output — gen_item_food.go:97)
    // buf.WriteString(generatedHeader("gen_item_food.go", "items.json"))
    // buf.WriteString("\npackage food\n\n")  -> for us: "package tag"
    // emit the map; format.Source([]byte(buf.String())); writeFile(out, formatted)
}
```

The Java extractor pattern to MIRROR (tools/java/ExtractAll.java) — ZipFile jar-read:
```java
// open the inner jar with new ZipFile(path), iterate zip.entries(),
// match e.getName().startsWith("data/minecraft/tags/damage_type/"),
// zip.getInputStream(e) -> read the JSON body.
```
And the hand-written JSON output pattern (tools/java/GenEntities.java:48-68) — PrintWriter + a jsonStr helper, NO external JSON dependency.

The generated-package OUTPUT shape to MIRROR (data/item/food.go):
```go
// Code generated by tools/gen_item_food.go from ...; DO NOT EDIT.
package food
var Food = map[string]ItemFood{ ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Write the tag membership test scaffold</name>
  <read_first>
    - data/item/food.go (the generated-package shape: header + `package` + a typed `var ... = map[...]`)
    - JAR DATA to javap-verify membership against (BEFORE asserting): unzip `temp/cache/26.2-inner.jar` and read `data/minecraft/tags/damage_type/bypasses_armor.json`, `panic_causes.json`, `is_fire.json`, `is_player_attack.json` — these are the golden subsets. Also read `data/minecraft/tags/item/pig_food.json` for the item-tag side.
  </read_first>
  <behavior>
    - Test TestTagMembership: tag.DamageTypeTags["bypasses_armor"] contains the FALL/MAGIC/DROWN-class damage-type ids the jar JSON lists, and does NOT contain the player_attack id.
    - Test TestNestedTagFlatten: tag.DamageTypeTags["panic_causes"] is a SUPERSET of tag.DamageTypeTags["is_player_attack"] members (proving the nested `#minecraft:is_player_attack` ref was recursively flattened, not stored as a literal string).
    - Test TestItemTagsGenerated (front-load): tag.ItemTags["pig_food"] is non-empty (carrot/potato/beetroot ids) — proves the item-tag family generated too.
  </behavior>
  <action>
    Create `data/tag/tags_test.go` (`package tag`). Encode the golden subsets read from the jar JSON in read_first — assert exact damage-type-id membership for bypasses_armor (positive: a fall/magic id; negative: the player_attack id), the panic_causes ⊇ is_player_attack superset relation (nested-flatten proof), and that ItemTags["pig_food"] is non-empty. Use small explicit golden id sets derived from the jar JSON, NOT a tautological "len > 0" only — at least one POSITIVE and one NEGATIVE membership per damage-type assertion. The map types are `var DamageTypeTags = map[string]map[int32]bool` and `var ItemTags = map[string]map[int32]bool` (the contract Task 2 emits). This test MUST compile against (and initially FAIL on a missing) `data/tag/tags.go` until Tasks 1-2 generate it.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go vet ./data/tag/ 2>&1 | head; echo "expected: build error / red until tags.go is generated"</automated>
  </verify>
  <acceptance_criteria>
    - data/tag/tags_test.go exists (package tag) with TestTagMembership, TestNestedTagFlatten, TestItemTagsGenerated.
    - Each damage-type assertion has at least one POSITIVE and one NEGATIVE membership (not a tautological len>0) using golden ids derived from the jar JSON.
    - References both DamageTypeTags and ItemTags (the contract Task 2 emits).
    - Red until data/tag/tags.go is generated (expected at this task).
  </acceptance_criteria>
  <done>data/tag/tags_test.go exists with TestTagMembership (positive+negative), TestNestedTagFlatten (superset), TestItemTagsGenerated; references DamageTypeTags + ItemTags; red until generated.</done>
</task>

<task type="auto">
  <name>Task 1: GenTags.java — ZipFile jar-read extractor with recursive nested-tag flatten</name>
  <read_first>
    - tools/java/ExtractAll.java (the ZipFile jar-read pattern: `new ZipFile(...)`, `zip.entries()` iteration, `zip.getInputStream(e)`, the `e.getName().startsWith(...)` match)
    - tools/java/GenEntities.java:48-68 (the hand-written JSON output: PrintWriter + the jsonStr helper, no external JSON dep)
    - JAR to javap/unzip BEFORE writing: `temp/cache/26.2-inner.jar` entries `data/minecraft/tags/damage_type/*.json` (35 tags) and `data/minecraft/tags/item/*.json` (224 tags). Read `panic_causes.json` to confirm the nested `#`-ref format vanilla TagLoader.build resolves.
  </read_first>
  <action>
    Create `tools/java/GenTags.java` mirroring ExtractAll.java's ZipFile read. Open the inner jar via `new ZipFile(jarPath)`, iterate `zip.entries()`, and collect every entry whose name starts with `data/minecraft/tags/damage_type/` OR `data/minecraft/tags/item/`. For each, read the JSON body via `zip.getInputStream(e)` and parse its `"values":[...]` array. CRITICAL — recursive nested-tag flatten exactly as `net.minecraft.tags.TagLoader.build` does: a value that starts with `#` (e.g. `#minecraft:is_player_attack`) is a REFERENCE to another tag whose members must be recursively expanded and merged (NOT stored as the literal string). Resolve the tag name from the file path (`damage_type/bypasses_armor.json` → tag `bypasses_armor`). Handle the `{ "id": "...", "required": false }` object form of a value as well as the bare-string form (per the datapack value schema). Emit a flat membership JSON `tags.json` to the extractor output dir using the hand-written PrintWriter + jsonStr pattern from GenEntities.java — shape: `{ "damage_type": { "bypasses_armor": ["minecraft:fall", ...], ... }, "item": { "pig_food": ["minecraft:carrot", ...], ... } }` (member values as their resource-location strings; the Go generator resolves them to numeric ids). Cite the source class (`net.minecraft.tags.TagLoader.build`) in a header comment. If GenTags must be invoked from ExtractAll.java's extractor list, append `"GenTags"` to ExtractAll.java's runner list (l.246-area) — but prefer a standalone ZipFile reader (it needs no Bootstrap binding since it reads JSON, not reflection).
  </action>
  <verify>
    <automated>cd /d/ender/tools && go run . --version 26.2 2>&1 | grep -iE "tags|GenTags|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tools/java/GenTags.java exists and contains `ZipFile`.
    - GenTags.java contains a recursive flatten branch keyed on a value starting with `#` (grep `startsWith("#")` or equivalent).
    - Running the pipeline produces an intermediate `tags.json` containing both a `damage_type` and an `item` section; `panic_causes` lists member ids that ALSO appear in `is_player_attack` (nested flatten worked — verify by inspecting the emitted tags.json, NOT by trusting the string).
    - GenTags.java header cites `net.minecraft.tags.TagLoader.build` and the 26.2 jar.
  </acceptance_criteria>
  <done>GenTags.java reads both tag families from the jar zip, recursively flattens nested `#`-refs like TagLoader.build, and emits a flat tags.json with damage_type + item sections.</done>
</task>

<task type="auto">
  <name>Task 2: gen_tags.go generator + register in main.go → emit data/tag/tags.go</name>
  <read_first>
    - tools/gen_item_food.go:64-147 (the full genX(jsonDir, goMCRoot) shape: readJSON → sorted rows → generatedHeader → package → emit map → format.Source → writeFile)
    - tools/gen_registryid.go:13-68 (the sorted-keys / multi-output discipline; also how it resolves resource-location strings → numeric ids via the registryid data)
    - tools/main.go:34-45 (the generators slice — add `{"tags", genTags}` after `{"itemfood", genItemFood}`)
    - tools/helpers.go:22,57,69 (generatedHeader, readJSON, writeFile)
    - data/item/food.go (the OUTPUT package shape) and data/registryid/ (to resolve `minecraft:fall` damage-type strings and `minecraft:carrot` item strings → int32 ids — confirm the damage_type + item registry id maps exist; gen_registryid.go:76 already emits the damage_type registry per RESEARCH)
  </read_first>
  <action>
    Create `tools/gen_tags.go` mirroring `genItemFood`. Signature `func genTags(jsonDir, goMCRoot string) error`. readJSON the `tags.json` (Task 1 output) into a typed struct `{ DamageType map[string][]string; Item map[string][]string }`. Resolve each member resource-location string (e.g. `minecraft:fall`, `minecraft:carrot`) to its numeric id via the same registry-id data gen_registryid.go uses (damage_type registry for the damage tags, item registry for the item tags) — strip the `minecraft:` prefix as the existing generators do. Build SORTED rows (sort.Slice over tag names, and sort member ids) for deterministic output. Emit `data/tag/tags.go` (`package tag`): `var DamageTypeTags = map[string]map[int32]bool{ ... }` and `var ItemTags = map[string]map[int32]bool{ ... }`, each tag → set of member ids → true. Use `generatedHeader("gen_tags.go", "tags.json")`, then `format.Source`, then `writeFile`. Register in `tools/main.go` generators slice: add `{"tags", genTags}` right after `{"itemfood", genItemFood}` (l.44). Run `cd tools && go run . --version 26.2` to regenerate. NOTE the front-load: ItemTags is GENERATED but has NO consumer in Phase 29 (consumer is Phase 32) — that is intentional per CONTEXT Grey Area 1; document it in the generator's header comment.
  </action>
  <verify>
    <automated>cd /d/ender/tools && go run . --version 26.2 2>&1 | grep -iE "genTags|tags:" | head; cd /d/ender && CGO_ENABLED=0 go build ./data/tag/ && CGO_ENABLED=0 go test ./data/tag/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tools/gen_tags.go contains `func genTags(jsonDir, goMCRoot string) error`.
    - tools/main.go generators slice contains `{"tags", genTags}` (grep `\{"tags", genTags\}`).
    - data/tag/tags.go exists, begins with the generated-code header, `package tag`, contains `var DamageTypeTags = map[string]map[int32]bool` and `var ItemTags = map[string]map[int32]bool`.
    - `CGO_ENABLED=0 go test ./data/tag/` exits 0 (TestTagMembership + TestNestedTagFlatten + TestItemTagsGenerated all green).
    - Output is deterministic (re-running `go run . --version 26.2` produces a byte-identical tags.go — sorted rows).
  </acceptance_criteria>
  <done>data/tag/tags.go generated with both tag families; registered in main.go; ./data/tag/ tests green; deterministic output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time jar → embedded Go data | The jar JSON is the authoritative source; codegen runs offline. No runtime/client input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | generated tag table (data/tag/tags.go) | mitigate | Data is build-time-only from the frozen jar; tags_test.go asserts membership against a jar-derived golden subset so a wrong/empty flatten is caught at test time (defends against the `const false` / unflattened-`#`-ref failure mode that PITFALLS Pitfall 3 names). |
| T-29-04 | Information disclosure | item-food tags generated-but-unwired | accept | Generated data has no runtime consumer in P29; it is inert embedded data (no exposure surface). Documented front-load per CONTEXT Grey Area 1. |

No web/auth/network threats apply — this is offline build tooling emitting static data.
</threat_model>

<verification>
- `cd tools && go run . --version 26.2` runs clean; regenerates data/tag/tags.go deterministically.
- `CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./...` clean.
- `CGO_ENABLED=0 go test ./data/tag/` green.
- `go list -deps ./... | grep -i gopy` EMPTY (no cgo leak; no new Go deps added).
- Pig oracle untouched (no server code changed this plan) — `CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig` stays green.
</verification>

<success_criteria>
- MOB-SUB-03 data foundation shipped: DamageTypeTags has genuine, jar-flattened membership for bypasses_armor + panic_causes (+ the rest) with nested `#`-refs resolved.
- ItemTags front-loaded into the same package (unwired, documented).
- All ops jar-sourced (TagLoader.build cited); no hand-authored tag data; no new deps; CGO=0 clean.
</success_criteria>

<output>
After completion, create `.planning/phases/29-damage-keystone-s2/29-01-SUMMARY.md`.
</output>
