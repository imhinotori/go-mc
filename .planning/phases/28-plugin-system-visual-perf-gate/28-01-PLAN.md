---
phase: 28-plugin-system-visual-perf-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plugin_perf_bench_test.go
  - server/perf_gate_test.go
  - server/assets/wandermob/plugin.toml
  - server/assets/wandermob/main.star
  - server/wandermob_embed.go
  - server/test_kit.go
  - server/item_use.go
  - cmd/sulfur/main.go
autonomous: true
requirements: [PLUGIN-07]

must_haves:
  truths:
    - "go test -bench BenchmarkPluginPigVsGoNative ./server/ runs and reports a go-native sub-bench and a plugin sub-bench (the delta = the isolated plugin overhead)"
    - "BenchmarkTickEmitOverhead reports ~0 allocs/op for the zero-subscriber sub-bench (the zero-subscriber guard is free)"
    - "go test ./server/ -run TestPerfGate exits 0 — the plugin per-mob-tick delta is within an ABSOLUTE ns cap AND a relative % cap, both set FROM a real baseline run (not guessed)"
    - "A custom wander-mob declaration boot-loads into the LIVE registry (server/wandermob_embed.go, //go:embed like vanilla_pig_embed.go), NOT the isolated testdata root"
    - "A SULFUR_TEST_KIT-gated spawn trigger spawns the custom wander mob via spawnDeclaredMob when the operator/bot uses the spawn item in-game"
    - "The default CGO_ENABLED=0 go build ./cmd/sulfur succeeds and the test-kit trigger is a no-op when SULFUR_TEST_KIT != 1 (no prod leak)"
  artifacts:
    - path: "server/plugin_perf_bench_test.go"
      provides: "BenchmarkPluginPigVsGoNative (A/B over newPigAI oracle vs the plugin pig) + BenchmarkTickEmitOverhead + BenchmarkServerAiStepPluginVsGo"
      contains: "func BenchmarkPluginPigVsGoNative"
    - path: "server/perf_gate_test.go"
      provides: "TestPerfGate — the hard, CI-repeatable threshold assertion (absolute ns + relative %)"
      contains: "func TestPerfGate"
    - path: "server/wandermob_embed.go"
      provides: "loadWanderMobRegistry / LoadWanderMobRegistry boot-load of the embedded custom wander mob into the live registry"
      contains: "go:embed"
    - path: "server/assets/wandermob/main.star"
      provides: "the custom (non-vanilla) wander mob declaration (1 MOVE goal that nav-targets a nearby point)"
      contains: "declare_mob"
    - path: "server/test_kit.go"
      provides: "the custom-mob spawn item in the kit + handleGateSpawnEgg trigger"
      contains: "spawnDeclaredMob"
  key_links:
    - from: "server/perf_gate_test.go::TestPerfGate"
      to: "server/ai_mob.go::newPigAI + server/vanilla_pig.go::spawnVanillaPigWithID"
      via: "the retained Phase-24 A/B oracle pair driven through serverAiStep + drainPendingPath"
      pattern: "newPigAI|spawnVanillaPigWithID"
    - from: "server/item_use.go (use seam)"
      to: "server/plugin_mob_decl.go::spawnDeclaredMob"
      via: "the test-kit spawn item's use handler (gated by testKitEnabled)"
      pattern: "spawnDeclaredMob"
    - from: "cmd/sulfur/main.go boot"
      to: "server/wandermob_embed.go::LoadWanderMobRegistry"
      via: "boot-load the custom decl into the live mob registry alongside vanilla_pig"
      pattern: "LoadWanderMobRegistry|SetMobRegistry"
---

<objective>
Build the AUTOMATED PERF GATE and the in-game CUSTOM-MOB SPAWN TRIGGER for PLUGIN-07 (closes v4).

This plan ships the server-side half of the gate, with NO client involvement:
1. Promote the EXISTING Phase-24 A/B test (`TestPluginPigEqualsGoNativePig`, which drives the retained `newPigAI` Go oracle against the plugin pig) into an A/B BENCHMARK + a hard `TestPerfGate` whose threshold is set FROM a real baseline run.
2. Add the per-tick `Emit` overhead bench (zero-subscriber ≈ free).
3. Boot-load a CUSTOM (non-vanilla) wander-mob declaration into the LIVE registry (Phase 23 left it in an isolated testdata root) + add a SULFUR_TEST_KIT spawn trigger so Plan 02's bot can spawn it.

Purpose: the perf-pass is one of PLUGIN-07's two proofs (the other is Plan 02's bot run). The custom-mob trigger is the seam the bot needs for checklist item #1.

Output: `server/plugin_perf_bench_test.go`, `server/perf_gate_test.go`, the embedded wander-mob plugin + its boot-load, and the test-kit spawn trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-plugin-system-visual-perf-gate/28-CONTEXT.md
@.planning/phases/28-plugin-system-visual-perf-gate/28-RESEARCH.md
@CLAUDE.md

# THE PERF ORACLE — read first, this is the harness to promote to a bench:
@server/plugin_pig_test.go

# THE A/B SPAWN SEAMS + the retained oracle:
@server/vanilla_pig.go
@server/ai_mob.go

# THE BOOT-LOAD PATTERN to mirror for the custom wander mob (//go:embed + materialize + LoadDirWith):
@server/vanilla_pig_embed.go

# THE PHASE-23 WANDER DECL to promote out of the isolated testdata root into a live embed:
@server/testdata/mobplugins/wandermob/main.star
@server/testdata/mobplugins/wandermob/plugin.toml

# THE SPAWN PATH the trigger calls:
@server/plugin_mob_decl.go

# THE TEST-KIT to extend + the use seam to wire the trigger through:
@server/test_kit.go
@server/item_use.go

# THE EMIT host to bench (zero-subscriber fast path):
@plugin/host/emit.go

# THE BENCH STYLE to match (testing.B + b.ReportAllocs + b.ResetTimer, no new dep):
@world/noisegen_bench_test.go

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->

From server/plugin_pig_test.go (the A/B harness to reuse):
```go
// newPhysicsLoop() (*TickLoop, *ChunkManager) — installs the vanilla_pig registry.
// drainPendingPath(loop *TickLoop, e *Entity) — block-drains the async A* so the two paths
//   adopt their (identical) path the SAME tick (removes scheduler jitter from the bench).
// Build pattern (per sub-bench): create loop, fill floor chunks, spawn N mobs, warm up, ResetTimer.
goPig := NewEntity(pigID, entity.Pig, 8.5, float64(floorY+1), 8.5)
goPig.ai = newPigAI()             // the RETAINED Go-native oracle (DO NOT delete it)
reseedMobAI(goPig.ai, goPig.id)
goLoop.only().entities.add(goPig)
// plugin pig with a pinned id (same reseed seed):
plPig := plLoop.spawnVanillaPigWithID(pigID, 8.5, float64(floorY+1), 8.5)
// drive: pig.ai.serverAiStep(loop, pig); then drainPendingPath(loop, pig)
```

From server/vanilla_pig.go (boot-load + spawn API — mirror for the wander mob):
```go
func LoadVanillaPigRegistry() (*mobRegistry, error)
func (t *TickLoop) SetMobRegistry(r *mobRegistry)
func (t *TickLoop) spawnDeclaredMob(decl *mobDecl, x, y, z float64) *Entity  // plugin_mob_decl.go
```

From server/test_kit.go:
```go
func testKitEnabled() bool             // os.Getenv("SULFUR_TEST_KIT") == "1"
type testKitStack struct { slot int16; id item.ID; count int32 }
var testKit = []testKitStack{ ... }    // add the custom-mob spawn item here
```

From plugin/host/emit.go (the bench target):
```go
func (m *Manager) Emit(evt EventType, payload Event)  // len(hooks)==0 → one map read, 0 alloc
// host.EventTick, host.TickEvent{Tick: n}
```

From server/wandermob_embed.go (NEW — model on vanilla_pig_embed.go exactly):
```go
const wanderMobName = "wanderer"   // matches declare_mob(name="wanderer") in the embedded main.star
func LoadWanderMobRegistry() (*mobRegistry, error)  // or merge into the existing registry — see action
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the custom wander mob to a LIVE boot-loaded embed + add the test-kit spawn trigger</name>
  <files>server/assets/wandermob/plugin.toml, server/assets/wandermob/main.star, server/wandermob_embed.go, server/test_kit.go, server/item_use.go, cmd/sulfur/main.go</files>
  <action>
    Read server/vanilla_pig_embed.go, server/testdata/mobplugins/wandermob/{main.star,plugin.toml}, server/test_kit.go, server/item_use.go, and the cmd/sulfur boot path FIRST.

    Step A — embed the custom decl into the binary (mirror vanilla_pig_embed.go EXACTLY):
    - Copy server/testdata/mobplugins/wandermob/{plugin.toml,main.star} into server/assets/wandermob/ (the //go:embed root must live under the package dir, like assets/vanilla_pig/). Keep the decl as-is: a CUSTOM mob (base_type pig → renders as the pig wire id; "custom" = the BEHAVIOR), one MOVE goal that nav-targets a nearby point so it WALKS via the Go nav. It is free of the 1:1 mandate (custom, not vanilla).
    - Create server/wandermob_embed.go modeled on vanilla_pig_embed.go: `//go:embed assets/wandermob/plugin.toml assets/wandermob/main.star`, a `const wanderMobName = "wanderer"` (MUST match the declare_mob name in main.star), and a `LoadWanderMobRegistry()` that materializes + LoadDirWith's the embedded plugin with the `declare_mob`/`goal` builtins injected, failing loudly if the "wanderer" decl is absent. Per CONTEXT D-2: this BOOT-LOADS the custom decl into the LIVE registry (Phase 23 deliberately left it in an isolated testdata root). Reuse the existing materialize/caps helpers' pattern; do NOT duplicate vanilla_pig's helpers verbatim if you can parameterize, but a parallel set of functions is acceptable.
    - Wire the boot-load in cmd/sulfur/main.go: after the existing vanilla_pig SetMobRegistry, ALSO register the wanderer decl into the SAME tick-owned registry so both vanilla_pig AND wanderer are spawnable. Simplest: add a registry-merge — load the wander registry and copy its `byName["wanderer"]` decl into the installed registry's `byName` before tick.Run (single-threaded boot, TICK-05 safe). If a clean merge helper does not exist, add a small `(t *TickLoop) registerDeclaredMob(decl)` or extend SetMobRegistry's caller to populate both. Cite the single-owner-at-boot discipline in a comment.

    Step B — the SULFUR_TEST_KIT spawn trigger (CONTEXT D-2: spawn-egg item OR debug command → spawnDeclaredMob):
    - In server/test_kit.go: add a custom-mob spawn item to the kit (a spawn-egg-style item, e.g. item.PigSpawnEgg.ID, into a free hotbar slot — slot 35/menu hotbar; pick a slot the existing kit does not use). Add a `const gateSpawnEggID` so the use handler can match it. Document it is a GATE-ONLY re-skin of a spawn egg, no-op unless SULFUR_TEST_KIT=1.
    - Add `func (t *TickLoop) handleGateSpawnEgg(p *tickPlayer)`: guard on `testKitEnabled() && t.mobRegistry != nil`, look up the "wanderer" decl in the registry, and call `t.spawnDeclaredMob(decl, p.x, p.y, p.z)` (or 1-2 blocks in front so it does not spawn inside the player). Owner-goroutine only (called from the tick-side use path), no locks (TICK-05).
    - Wire it into the use seam: in server/item_use.go's handleUseItem (right-click air) — after the hand/validity decode, BEFORE the food path, if testKitEnabled() and the held item's ItemID == gateSpawnEggID, call handleGateSpawnEgg(p) and return. This makes the gate egg right-clickable to spawn the custom mob. Keep the food path untouched for non-egg items. Cite that this is gate-only (the default prod join has the vanilla empty inventory; testKitEnabled() false → unreachable).

    CRITICAL: the default CGO_ENABLED=0 `go build ./cmd/sulfur` MUST stay green, and with SULFUR_TEST_KIT unset the trigger must be a no-op (no prod leak — threat T-28-03).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./cmd/sulfur ./server/ && go vet ./server/ ./cmd/sulfur</automated>
  </verify>
  <done>
    The wander mob boot-loads into the live registry (server/wandermob_embed.go exists with //go:embed + wanderMobName="wanderer"); cmd/sulfur registers both vanilla_pig and wanderer; the test-kit has a gate spawn egg whose use handler calls spawnDeclaredMob gated by testKitEnabled(); CGO=0 build + vet green.
  </done>
</task>

<task type="auto">
  <name>Task 2: The A/B perf bench + the Emit-overhead bench + TestPerfGate (threshold set FROM a real baseline)</name>
  <files>server/plugin_perf_bench_test.go, server/perf_gate_test.go</files>
  <action>
    Read server/plugin_pig_test.go (the TestPluginPigEqualsGoNativePig harness + drainPendingPath), world/noisegen_bench_test.go (the bench style), and plugin/host/emit.go FIRST.

    Create server/plugin_perf_bench_test.go (package server), matching the noisegen_bench_test.go style (testing.B + b.ReportAllocs() + b.ResetTimer(), NO new dep):

    1. `BenchmarkPluginPigVsGoNative(b *testing.B)` with TWO sub-benches (b.Run):
       - "go-native": build a loop (newPhysicsLoop + fill a 3×3 floor like the equality test), spawn N=100 Go-native pigs via `NewEntity(id, entity.Pig, ...); pig.ai = newPigAI(); reseedMobAI(pig.ai, id); loop.only().entities.add(pig)` with distinct ids (1000+i). Add a player east (within look range) so the lookAt goal fires for both. WARM UP a handful of ticks (e.g. 10) with serverAiStep + drainPendingPath BEFORE b.ResetTimer() (Pitfall 2 — first-call bias). Then `for n := 0; n < b.N; n++ { for _, p := range pigs { p.ai.serverAiStep(loop, p); drainPendingPath(loop, p) } }`.
       - "plugin": identical EXCEPT spawn via `loop.spawnVanillaPigWithID(int32(1000+i), ...)`.
       - b.ReportAllocs() in both. The plugin sub-bench ns/op MINUS the go-native ns/op = the isolated plugin overhead (both run the IDENTICAL Go nav/physics — Pitfall 1: report the DELTA, never the absolute).
    2. `BenchmarkTickEmitOverhead(b *testing.B)` with two sub-benches over a host.Manager:
       - "zero-subscribers": a Manager with no hooks; `m.Emit(host.EventTick, host.TickEvent{Tick: n})` in the loop — hits the len==0 fast path (~0 allocs/op, a map read). Build the Manager with host.New() (check plugin/host for the no-hooks constructor; if Emit needs a tick payload only, EventTick + TickEvent suffices).
       - "one-subscriber": a Manager with ONE trivial on_tick hook registered (load a tiny inline .star with `register("on_tick", lambda ...)` via the existing test harness, OR reuse an existing emit_test.go helper that registers a no-op hook — read plugin/host/emit_test.go for the pattern). Emit in the loop. This documents the SUBSCRIBED cost (not on the default unsubscribed path).
       - b.ReportAllocs() in both.
    3. `BenchmarkServerAiStepPluginVsGo(b *testing.B)` — the per-serverAiStep delta for ONE plugin pig vs ONE Go pig (same structure, N=1), for an isolated per-mob number.

    Create server/perf_gate_test.go (package server) with `TestPerfGate(t *testing.T)` — the HARD, CI-repeatable assertion:
    - Factor a helper `avgTickNs(t, spawn func() (*TickLoop, []*Entity), warmup, ticks int) float64` that builds the world, spawns the mobs, warms up, then times `ticks` iterations of (serverAiStep + drainPendingPath) over all mobs with time.Now() deltas, returning ns per (mob·tick).
    - goNs := avgTickNs over N go-native pigs; plNs := avgTickNs over N plugin pigs (N=100, warmup=50, ticks=2000 per RESEARCH Code Examples).
    - deltaNs = plNs - goNs; deltaPct = 100*deltaNs/goNs.
    - THRESHOLD (CONTEXT D-3 — set FROM the baseline, do NOT guess): FIRST run `go test -bench BenchmarkPluginPigVsGoNative ./server/ -benchmem -count=3` and `go test -run TestPerfGate ./server/ -v` to read the REAL measured delta on this machine. Then set BOTH caps as consts in the test: `maxDeltaNsPerMobTick` = the measured per-mob-tick delta × a safety margin (1.5–2×), and `maxDeltaPct` = a relative cap comfortably above the measured % (e.g. round the measured % up to the next sensible bound, with margin). t.Fatalf if either is exceeded. t.Logf the measured go-native/plugin/delta numbers. DOCUMENT in a comment the baseline run that produced the numbers (date + machine note) so the threshold is auditable.
    - Per the bot-observes-is-the-proof + oracle-already-exists facts: a comment must note this bench REUSES the Phase-24 retained oracle (newPigAI) and the drainPendingPath discipline; do NOT delete the Go oracle.

    Note in a comment: -race needs CGO=1 (Docker); benchmarks themselves run CGO=0 host; the equality path is already -race-verified in Phase 24.
  </action>
  <verify>
    <automated>go test -run TestPerfGate ./server/ -v && go test -bench 'BenchmarkPluginPigVsGoNative|BenchmarkTickEmitOverhead|BenchmarkServerAiStepPluginVsGo' -benchmem ./server/ -benchtime=20x</automated>
  </verify>
  <done>
    BenchmarkPluginPigVsGoNative runs with go-native + plugin sub-benches; BenchmarkTickEmitOverhead's zero-subscribers sub-bench reports ~0 allocs/op; TestPerfGate exits 0 with an absolute ns/(mob·tick) cap AND a relative % cap, both set from the documented baseline run; a comment records the baseline numbers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → server boot | SULFUR_TEST_KIT toggles the gate-only spawn item + trigger; must be inert in prod |
| perf bench → CI verdict | the threshold value gates whether a regression passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | TestPerfGate threshold | mitigate | Set the threshold FROM a real baseline run (documented in-file) with a bounded safety margin (1.5–2× absolute, a tight relative %). A too-loose cap lets a regression pass — keep the margin small + record the measured baseline so the value is auditable. |
| T-28-03 | Elevation of Privilege | the test-kit spawn egg / trigger | mitigate | The spawn item is added to the kit ONLY when testKitEnabled() (SULFUR_TEST_KIT=1); handleGateSpawnEgg early-returns when the env is unset. Default prod join keeps the vanilla empty inventory — no spawn-egg, no trigger reachable. Verified by the no-op-when-unset done criterion. |
| T-28-04 | Denial of Service | unbounded custom-mob spawning via the egg | accept | The egg is a gate-only kit item behind SULFUR_TEST_KIT; the bot spawns a bounded count. The plugin-facing rate-limited spawn builtin remains out of scope (carries Phase-23 T-23-11 accept). Not exposed to untrusted plugins. |
| T-28-05 | Spoofing | a custom mob masquerading as vanilla on the wire | accept | base_type pig → the wander mob renders as the pig wire id BY DESIGN (custom = behavior, not a new wire type); the bot distinguishes it by its movement pattern (Go-nav wander), not its wire type. Documented in the decl. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./cmd/sulfur ./server/` green (default static binary preserved).
- `go vet ./server/ ./cmd/sulfur` clean.
- `go test -run TestPerfGate ./server/ -v` exits 0.
- `go test -bench 'BenchmarkPluginPigVsGoNative|BenchmarkTickEmitOverhead' -benchmem ./server/ -benchtime=20x` runs; both sub-benches of the A/B report numbers; zero-subscriber Emit ~0 allocs/op.
- The wander mob boot-loads (a smoke test or the existing physics-loop harness can confirm `registry.byName["wanderer"]` is present after LoadWanderMobRegistry).
- Docker -race over ./server/ stays clean (the bench/trigger add no cross-tick state; the spawn path is tick-owned). Run if the environment allows CGO=1.
</verification>

<success_criteria>
- The Phase-24 A/B oracle is promoted to a bench; the per-mob-tick plugin delta is measured + asserted within a baseline-derived absolute + relative cap (TestPerfGate green).
- The zero-subscriber Emit bench proves the unsubscribed hot path is allocation-free.
- A CUSTOM wander mob boot-loads into the LIVE registry and is spawnable in-game via a SULFUR_TEST_KIT-gated spawn trigger (the seam Plan 02's bot needs).
- The default CGO=0 binary builds; the trigger is inert without SULFUR_TEST_KIT.
</success_criteria>

<output>
After completion, create `.planning/phases/28-plugin-system-visual-perf-gate/28-01-SUMMARY.md` recording: the measured perf baseline (go-native ns, plugin ns, delta ns + %), the chosen threshold caps, the wander-mob embed/boot-load wiring, and the test-kit trigger shape (which item id, which slot, which use seam).
</output>
