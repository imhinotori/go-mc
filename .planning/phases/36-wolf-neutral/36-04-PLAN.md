---
phase: 36-wolf-neutral
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - server/wolf_test.go
  - server/hostiles_embed_test.go
  - server/vanilla_mob_test.go
autonomous: false
requirements: [MOB-NEUT-01, MOB-NEUT-02]

must_haves:
  truths:
    - "The wolf embeds and boot-loads from the real //go:embed (TestWolfBootLoads)"
    - "A fed BONE eventually tames a wolf (the 1/3 chance), which then sits and follows its owner"
    - "A wild wolf hit by a player becomes angry (angerTime = 400 + nextInt(381)) and targets the attacker"
    - "The tame nextInt(3) and anger UniformInt(400,780)=400+nextInt(381) draws are exercised by focused RNG tests"
    - "TestPluginPigEqualsGoNativePig stays byte-identical (the pig oracle is untouched)"
    - "Docker -race + strictRegion is clean over the wolf + mob packages"
  artifacts:
    - path: "server/wolf_test.go"
      provides: "the wolf boot-load + behavior + RNG test suite"
      contains: "func TestWolfBootLoads"
  key_links:
    - from: "server/wolf_test.go"
      to: "the real //go:embed wolf"
      via: "loadVanillaWolfRegistry (temp-dir from ../plugins/vanilla_wolf) + the embed assertion"
      pattern: "loadVanillaWolfRegistry"
    - from: "server/wolf_test.go TestWolfTame"
      to: "tryWolfInteract nextInt(3)"
      via: "the focused tame RNG test"
      pattern: "nextInt(3)"
---

<objective>
The GATE. Build the wolf test suite (boot-load + per-mob behavior + focused RNG tests), add the wolf to the embed/boot-load assertions, prove TestPluginPigEqualsGoNativePig stays byte-identical, run Docker -race + strictRegion clean, and do the live bot verify (a wolf tames on BONE and sits; a hit wild wolf attacks back).

Purpose: This is the Phase-36 acceptance gate (SC#3). It depends on Plan B (the taming interact) and Plan C (the .star plugin) being live. It is test-only Go + the live-verify checkpoint, so it cannot collide with the foundation/interact/plugin plans.
Output: A green wolf test suite, a byte-identical pig oracle, a -race-clean build, and operator confirmation of taming/sitting/anger-on-hit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/36-wolf-neutral/36-JARNOTES.md
@.planning/phases/36-wolf-neutral/36-PATTERNS.md
@.planning/phases/36-wolf-neutral/36-01-SUMMARY.md
@.planning/phases/36-wolf-neutral/36-02-SUMMARY.md
@.planning/phases/36-wolf-neutral/36-03-SUMMARY.md

<interfaces>
<!-- The test-harness analogs the executor mirrors directly. -->

cow_test.go harness (the template to mirror):
  loadVanillaCowRegistry(t) *mobRegistry   (36 — temp-dir from ../plugins/vanilla_cow)
  cowLoop(t) (*TickLoop, int, *fakeClock)  (71)
  spawnCow(loop, x,y,z) *Entity            (85)
  TestCowBootLoads(t)                      (110)
  TestCowBehavior(t)                       (284)

The pig oracle: TestPluginPigEqualsGoNativePig (server/plugin_pig_test.go:267) — MUST stay byte-identical;
the wolf is a separate mob, so this test is UNTOUCHED. Run it to PROVE no regression.

The real-embed boot-load assertions: hostiles_embed_test.go (the //go:embed boot-load) + vanilla_mob_test.go.

From Plan B (36-02): tryWolfInteract draws mobRandom(mob).nextInt(3); ==0 tames (DATA_FLAGS tame+sit,
MAX_HEALTH 40). From Plan A (36-01): the anger draw 400 + mobRandom(e).nextInt(381) at the combat store-point;
the sit/follow_owner/owner_hurt goals. mobRandom(e) is the per-entity seeded stream (reseeded per id at spawn).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The wolf test suite (boot-load + behavior + focused RNG) + the embed assertions</name>
  <files>server/wolf_test.go, server/hostiles_embed_test.go, server/vanilla_mob_test.go</files>
  <read_first>
    - server/cow_test.go (loadVanillaCowRegistry 36, cowLoop 71, spawnCow 85, TestCowBootLoads 110, TestCowBehavior 284 — the EXACT harness to mirror)
    - server/zombie_test.go + server/skeleton_test.go (the combat-mob behavior-test precedent)
    - server/hostiles_embed_test.go + server/vanilla_mob_test.go (the real //go:embed boot-load assertions to extend)
    - server/plugin_pig_test.go (267 — TestPluginPigEqualsGoNativePig, to confirm it is untouched)
    - .planning/phases/36-wolf-neutral/36-JARNOTES.md (62-69 tryToTame nextInt(3); 139-143 anger UniformInt(400,780)=400+nextInt(381); 131-137 sit canUse; 162-173 follow_owner)
    - .planning/phases/36-wolf-neutral/36-PATTERNS.md (the per-mob behavior test section 379-388)
    - .planning/phases/36-wolf-neutral/36-01-SUMMARY.md + 36-02-SUMMARY.md + 36-03-SUMMARY.md (the verified indices, the RNG seams, the final goal-list shape)
  </read_first>
  <behavior>
    - TestWolfBootLoads: the real //go:embed wolf registry loads; the wolf spawns with base_type wolf, category CREATURE, MAX_HEALTH 8.0, ATTACK_DAMAGE 4.0, and the declared goal count matches Wolf.registerGoals (minus the cited-deferred goals).
    - TestWolfTame: feeding BONE to an untamed non-angry wolf draws exactly one nextInt(3); with a seeded stream forced to 0, the wolf becomes tame (DATA_FLAGS 0x4), owner is set, MAX_HEALTH becomes 40 + health 40, orderedToSit true, hearts event 7 broadcast; forced to !=0, it stays untamed, smoke event 6, bone still consumed.
    - TestWolfSit: a tamed orderedToSit wolf parks (the SitWhenOrderedToGoal claims MOVE/JUMP, inSittingPose true, navigation stopped).
    - TestWolfFollowOwner: a tamed wolf beyond 10² blocks from its owner paths toward it (moveTo issued); within 2² it stops; orderedToSit suppresses follow.
    - TestWolfAngerOnHit: a wild wolf hit by a player sets angerTime = 400 + nextInt(381) (one draw, exact range 400..780), angerTarget = the attacker, and isAngryAt(attacker) becomes true; ResetUniversalAnger/decrement counts it down to 0.
    - TestWolfTameRNG + TestWolfAngerRNG: focused draw-order tests pinning nextInt(3) for tame and 400+nextInt(381) for anger against a seeded mobRandom stream (the lockstep discipline, even without a full oracle).
  </behavior>
  <action>
    Create server/wolf_test.go mirroring cow_test.go's harness: loadVanillaWolfRegistry(t) (temp-dir copied from ../plugins/vanilla_wolf), wolfLoop(t), spawnWolf(loop,x,y,z), then the behavior tests above. Drive the tame/anger RNG via the seeded mobRandom(e) stream (force the seed so nextInt(3) hits 0 and !=0 deterministically; assert the anger draw is in [400,780] and equals 400 + the stream's nextInt(381)). Use the zombie/skeleton behavior-test pattern for the combat/target assertions (the wolf reuses those Go-native goals).

    Add the wolf to hostiles_embed_test.go's //go:embed boot-load assertion list and to vanilla_mob_test.go's boot-load assertions (mirror how cow/zombie/skeleton are asserted there).

    Do NOT touch plugin_pig_test.go — TestPluginPigEqualsGoNativePig must stay byte-identical (run it to confirm).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestWolf|TestPluginPigEqualsGoNativePig' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "func TestWolfBootLoads" server/wolf_test.go is non-empty AND grep -n "func loadVanillaWolfRegistry" server/wolf_test.go is non-empty
    - grep -nE "func TestWolfTame|func TestWolfSit|func TestWolfFollowOwner|func TestWolfAngerOnHit" server/wolf_test.go shows all four behavior tests
    - grep -nE "nextInt\(3\)|nextInt\(381\)" server/wolf_test.go shows both focused RNG assertions
    - CGO_ENABLED=0 go test ./server/ -run 'TestWolf' -count=1 exits 0
    - CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -count=1 exits 0 (the pig oracle is byte-identical)
    - grep -n "wolf" server/hostiles_embed_test.go is non-empty (the boot-load assertion)
  </acceptance_criteria>
  <done>The wolf boot-loads from the real embed, tames/sits/follows/angers in the behavior tests, the tame nextInt(3) + anger 400+nextInt(381) draws are pinned, and the pig oracle stays byte-identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Docker -race + strictRegion gate</name>
  <files>server/wolf_test.go</files>
  <read_first>
    - .planning/STATE.md (the standing Docker -race + strictRegion gate convention used by every prior phase)
    - the prior phase SUMMARYs' gate sections (how 34/35 ran the Docker -race over the mob packages)
    - 36-01/02/03-SUMMARY.md (any cross-tick state introduced — the anger timer + owner bookkeeping are tick-owned)
  </read_first>
  <action>
    Run the standing Docker -race gate over the wolf + mob packages exactly as Phase 34/35 did (the containerized
    `go test -race` with strictRegion enabled). Confirm the new wolf state (angerTime/owner bookkeeping, the
    tame/sit DATA_FLAGS broadcast) is tick-owned and race-free — only immutable snapshots cross the tick→broadcast
    seam (the TICK-05 discipline). If the carryover flaky TestBehaviorRegressionMobSpawns surfaces (STATE.md:19-28),
    note it as the pre-existing carryover (NOT a wolf regression) and confirm the wolf tests themselves are -race clean
    in isolation. Fix any genuine wolf-introduced race before closing.
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/... -race -run 'TestWolf|TestPluginPigEqualsGoNativePig' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./server/... -race -run 'TestWolf|TestPluginPigEqualsGoNativePig' -count=1 exits 0 (no race in the wolf path or the pig oracle)
    - the Docker -race + strictRegion run is clean over the wolf/mob packages (recorded in SUMMARY with the command + result)
    - any surfaced flake is identified as the documented carryover (TestBehaviorRegressionMobSpawns), not a wolf regression
  </acceptance_criteria>
  <done>The wolf subsystem is -race clean (Docker + strictRegion); the only acceptable flake is the documented spawner carryover.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live bot verify — tame on BONE + sit + anger-on-hit</name>
  <what-built>
    The complete wolf: boots from the embed, spawns via /dbg wolf, tames on a BONE feed (1/3 chance), sits, follows
    its owner, and a hit wild wolf angers and attacks back. The pig oracle stayed byte-identical and the build is -race clean.
  </what-built>
  <how-to-verify>
    1. Build CGO_ENABLED=0 and start the server (the standing local run, localhost:25565).
    2. Connect a client (or the testbot). Run `/dbg wolf` near you — a wolf spawns (eid printed in chat).
    3. Hold BONEs. Right-click the untamed wolf repeatedly — expect smoke puffs (tame fail, status 6) until eventually
       hearts (status 7) and the wolf becomes yours (the 1/3 chance — a few bones). The tamed wolf should sit (parked).
    4. Right-click your tamed wolf with an empty hand — it toggles between sitting (parked) and standing/following.
       Walk away >10 blocks — a standing tamed wolf paths to follow you; within ~2 blocks it stops.
    5. Spawn a fresh wild wolf (`/dbg wolf`), then hit it once — it should turn and attack you back (anger-on-hit),
       and stay angry for ~20-39s.
    6. (optional) Confirm a wild wolf attacks a nearby skeleton (`/dbg` a skeleton near a wild wolf).
  </how-to-verify>
  <resume-signal>Type "approved" if taming/sitting/following/anger-on-hit all work, or describe what misbehaved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tick→broadcast | the tame/sit DATA_FLAGS + anger state cross from the tick goroutine to trackers as immutable snapshots (TICK-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Tampering | the embed/repo-root drift | mitigate | the byte-identical diff (Plan C) + the real //go:embed boot-load test here prove the shipped wolf matches the repo-root plugin |
| T-36-10 | Denial of Service | the anger/owner cross-tick state | accept | tick-owned + snapshot-only across the broadcast seam (verified -race clean); bounded per-entity, no shared mutable hot-path state |
</threat_model>

<verification>
- CGO_ENABLED=0 go test ./server/ -run 'TestWolf' -count=1 exits 0
- CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -count=1 exits 0 (pig oracle byte-identical)
- go test ./server/... -race clean over the wolf/mob packages (Docker + strictRegion)
- go.mod unchanged (no new dep); CGO_ENABLED=0 preserved
- Live verify: tame-on-BONE + sit + follow + anger-on-hit confirmed by the operator
</verification>

<success_criteria>
- The wolf boot-loads, tames, sits, follows, and angers-on-hit (SC#1 + SC#2 proven end-to-end).
- TestPluginPigEqualsGoNativePig is byte-identical (SC#3 the pig oracle gate).
- Docker -race + strictRegion clean (SC#3); no new Go dep; CGO=0 (SC#3).
- The DEFER list (armor/dye/variants/beg/llama-avoid/prey-hunting/teleport/heal-feed) is recorded across the SUMMARYs, not silently dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/36-wolf-neutral/36-04-SUMMARY.md` — record the test results, the Docker -race command + result, the pig-oracle byte-identity confirmation, the operator live-verify outcome, and a consolidated DEFER list for the phase. This is the LAST v5 phase — note v5 milestone completion readiness.
</output>
