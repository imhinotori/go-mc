---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - server/ai_goals_breed.go
  - server/ai_goals_follow.go
  - server/ai_mob.go
  - server/entity.go
  - server/ai_mob_test.go
autonomous: true
requirements: [MOB-SUB-09, MOB-GATE-01]
tags: [breeding, breed-goal, follow-parent, goals, pig-parity]

must_haves:
  truths:
    - "The Go-native pig has 9 goals {0,1,3,4,4,5,6,7,8}: BreedGoal@3 (speed 1.0, {MOVE,LOOK}) + FollowParentGoal@5 (speed 1.1, EMPTY flags)"
    - "Two in-love same-region pigs find each other (getFreePartner: same-class && canMate && !isPanicking, nearest within range 8.0), navigate, and breed at loveTime>=adjustedTickDelay(60)=60 && distSqr<9"
    - "breed() spawns a baby (breedAge=-24000, half-scale) via spawnVanillaPig, sets both parents to cooldown (breedAge=6000), resets inLove, awards XP orb (1+nextInt(7)), draws the variant nextBoolean(), emits hearts"
    - "A baby (breedAge<0) follows the nearest adult same-class within 3..16 blocks (re-path every adjustedTickDelay(10)=10, NO RNG)"
    - "adjustedTickDelay(n)=n at 20 TPS (NOT reducedTickDelay's ceil(n/2)); isPanicking reads the running PanicGoal (faithful, NOT a hurtTime proxy)"
    - "The Go-native goal-count test (TestPigGoalSetRegistered) asserts 9 goals; the breed/follow RNG is confined to dormant canUse-gated paths"
  artifacts:
    - path: "server/ai_goals_breed.go"
      provides: "breedGoal (getFreePartner scan + navigate + breed()) + adjustedTickDelay + isPanicking"
      contains: "func newBreedGoal"
    - path: "server/ai_goals_follow.go"
      provides: "followParentGoal (nearest-adult scan + navigate, NO RNG)"
      contains: "func newFollowParentGoal"
    - path: "server/ai_mob.go"
      provides: "BreedGoal@3 + FollowParentGoal@5 in newPigAI"
      contains: "newBreedGoal"
    - path: "server/entity.go"
      provides: "canMate helper"
      contains: "func (e *Entity) canMate"
  key_links:
    - from: "server/ai_mob.go newPigAI"
      to: "newBreedGoal / newFollowParentGoal"
      via: "m.goals.addGoal(3, ...) / addGoal(5, ...)"
      pattern: "m.goals.addGoal\\(3|m.goals.addGoal\\(5"
    - from: "server/ai_goals_breed.go breed()"
      to: "spawnVanillaPig + awardExperienceOrbs + refreshDimensions"
      via: "child spawn (breedAge=-24000 + refreshDimensions) + XP orb + variant + parent cooldown"
      pattern: "spawnVanillaPig|breedAge = -24000"
---

<objective>
Build the GO-NATIVE half of the pig's last two deferred goals — BreedGoal@3 (speed 1.0, {MOVE,LOOK}) and FollowParentGoal@5 (speed 1.1, EMPTY flags) — the two new goal files, the same-class scan + canMate + getFreePartner + breed() (child spawn via spawnVanillaPig at breedAge=-24000 + refreshDimensions, both parents to breedAge=6000, inLove reset, XP orb 1+nextInt(7), variant nextBoolean(), hearts) + the nearest-adult-parent follow scan + the `adjustedTickDelay` identity helper + the faithful `isPanicking` read. Register both goals in newPigAI and update the Go-native goal-count test.

Purpose: This is C1 — the Go-native goals (split from the .star lockstep + the 4 plugin-side tests, which land in 33-04, to protect quality on the highest-oracle-risk surface). It MUST keep the breed/follow RNG confined: BreedGoal.canUse gates on isInLove (false on the un-fed oracle → zero draws, even the XP/variant draws fire only mid-breeding); FollowParentGoal gates on isBaby (false on the adult oracle) and has NO RNG.
Output: two goal files + canMate + adjustedTickDelay + isPanicking + newPigAI registration + the Go-native goal-count test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-02-SUMMARY.md

<oracle_gate_strategy>
THE MAKE-OR-BREAK CONSTRAINT (this is the goal-adding plan — highest oracle risk):
The oracle pig is an un-fed lone adult in a world with NO partner/food/baby. For THIS plan:
- BreedGoal.canUse: `if !isInLove() return false` on LINE 1 → the un-fed oracle (inLove==0) → ZERO draws.
  breed()'s child-spawn + XP nextInt(7) + variant nextBoolean() fire ONLY mid-breeding (the dormant oracle never reaches it).
- FollowParentGoal.canUse: `if breedAge >= 0 return false` → the adult oracle → zero effect (+ NO RNG anyway).
- The same-class scan uses the IN-REGION near() (the documented same-region breeding cut; cross-region deferred).
- LOCKSTEP NOTE: this plan adds the GO-NATIVE goals + their RNG draws (XP nextInt(7) + variant nextBoolean()).
  33-04 mirrors them onto BOTH .star pigs. Until 33-04 lands, the Go pig has 9 goals and the plugin pig has 7,
  so the plugin-vs-native oracle `TestPluginPigEqualsGoNativePig` will go RED here — that is EXPECTED and 33-04/33-05
  close it. The gate for THIS plan is the GO-NATIVE goal-count test (9 goals) + the unchanged goals' tests staying green.
  Record the observed TestPluginPigEqualsGoNativePig behavior in the SUMMARY so 33-04 knows the exact divergence.
GATE for THIS plan: TestPigGoalSetRegistered (Go-native, 9 goals) passes; the breed/follow RNG is confined to the dormant canUse-gated paths.
</oracle_gate_strategy>

<interfaces>
<!-- Verified from the codebase + Plans A/B. Use directly. -->

From Plans A/B (server/entity.go):
```go
breedAge int; inLove int
func (e *Entity) isBaby() bool ; func (e *Entity) isInLove() bool ; func (e *Entity) canFallInLove() bool ; func (e *Entity) setInLove()
func (e *Entity) refreshDimensions()   // Plan A — call after setting a child's breedAge=-24000 (baby half-scale)
```

From server/ai_goals_passive.go (the temptGoal SKELETON to copy — scan-then-moveTo, NO RNG):
```go
type temptGoal struct { baseGoal; ... }                                     // :450
func newTemptGoal(speedModifier float64, pred func(int32) bool, canScare bool) *temptGoal  // :464
// canUse :478 (scan) ; canContinueToUse :497 ; start :508 ; tick :517 (setWantTarget/moveTo) ; stop :535 (clearWantTarget)
func reducedTickDelay(ticks int) int { return (ticks + 1) / 2 }             // :79 — Goal.reducedTickDelay = ceil(n/2). DO NOT use this for breed/follow.
```

From the entity scan + spawn + XP seams:
```go
func (s *entityStore) near(x, z float64, rangeChunks int) []*Entity        // entity_store.go:168 — IN-REGION, UNFILTERED; filter by e.typ
e.typ                                                                        // entity.go:49 — the wire type id (same-class filter)
func (t *TickLoop) spawnVanillaPig(x, y, z float64) *Entity                 // vanilla_pig.go:36 — breed() child spawn
func (t *TickLoop) awardExperienceOrbs(e *Entity, value int)               // death_mob.go:347 — the breed XP orb
func (t *TickLoop) broadcastHearts(e *Entity)                              // combat_mob.go (Plan B) — breed hearts
func (t *TickLoop) broadcastBabyFlag(e *Entity)                            // combat_mob.go (Plan A) — push DATA_BABY_ID=true after child.breedAge=-24000
```

From the goal selector + the panic running-state (for isPanicking — the FAITHFUL read):
```go
func newBaseGoal(flags goalFlag) baseGoal   // breed: flagMove|flagLook ; follow: 0 (empty — verified handled, ai_goal.go §6)
// e.ai.goals.goals is []*wrappedGoal (ai_goal.go:141); each wrappedGoal has `running bool` (:109) + `g Goal` (the panicGoal is *panicGoal, ai_goals_panic.go:67).
// isPanicking(e) = for _, wg := range e.ai.goals.goals { if _, ok := wg.g.(*panicGoal); ok { return wg.running } } ; return false   — reachable from any goal callback.
```

From 33-JARNOTES.md (the values — port verbatim):
```
BreedGoal: range 8.0; canUse isInLove→getFreePartner; canContinue partner alive && inLove && loveTime<60 && !isPanicking;
  tick lookAt + moveTo(1.0) + ++loveTime; breed at loveTime >= adjustedTickDelay(60)=60 && distSqr < 9.0;
  getFreePartner: nearest same-class in inflate(8.0), canMate && !isPanicking. priority 3, speed 1.0, {MOVE,LOOK}.
FollowParentGoal: canUse age<0 → nearest ADULT (age>=0) in inflate(8,4,8), skip if distSqr<9; continue 9<d<256;
  tick recalc every adjustedTickDelay(10)=10, moveTo(parent,1.1). priority 5, speed 1.1, EMPTY flags. NO RNG.
adjustedTickDelay(n)=n at 20 TPS (NOT reducedTickDelay's ceil(n/2)). canMate = other!=this && same-class && both isInLove.
finalizeSpawnChildFromBreeding: child setAge(-24000); parents setAge(6000); reset inLove; XP 1+nextInt(7); variant nextBoolean().
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: adjustedTickDelay + canMate + isPanicking + the two goal files (breed/follow + breed())</name>
  <read_first>
    - server/ai_goals_passive.go:72-79 (reducedTickDelay — the WRONG helper to avoid) + :450-542 (temptGoal skeleton) + :266-340 (lookAtPlayerGoal)
    - server/entity_store.go:168-183 (near — IN-REGION, unfiltered) ; server/entity.go:49 (e.typ)
    - server/ai_goal.go:103-142 (wrappedGoal.running :109 + goals []*wrappedGoal :141) + :60-130 (Goal interface) + :182-207 (empty-flag handling — CONFIRMED no change)
    - server/ai_goals_panic.go:67 (panicGoal struct) ; server/ai_mob.go:29 (mobAI.goals)
    - server/vanilla_pig.go:36 (spawnVanillaPig) ; server/death_mob.go:347 (awardExperienceOrbs)
    - 33-JARNOTES.md: the BreedGoal + FollowParentGoal + Animal.canMate + finalizeSpawnChildFromBreeding + "adjustedTickDelay" + "isPanicking" + "breed() RNG draws" sections
    - 33-PATTERNS.md §7 (the two goal files + same-class scan) + §8 (child spawn) + §9 (breed XP orb)
  </read_first>
  <files>server/ai_goals_breed.go, server/ai_goals_follow.go, server/entity.go</files>
  <action>
    0. WARNING-2 FIX — adjustedTickDelay: add `func adjustedTickDelay(n int) int { return n }` (Goal.adjustedTickDelay
       returns n at 20 TPS — identity; cite 33-JARNOTES.md "adjustedTickDelay" section). Put it in ai_goals_breed.go or
       a shared goal file. Do NOT reuse reducedTickDelay (ceil(n/2)) — that would give 30/5, WRONG. Use
       adjustedTickDelay(60)=60 (breed threshold) and adjustedTickDelay(10)=10 (follow re-path), or inline 60/10 with a cite.
    1. server/entity.go — `func (e *Entity) canMate(other *Entity) bool` (Animal.canMate):
       `return other != e && other.typ == e.typ && e.isInLove() && other.isInLove()`.
    2. WARNING-4 FIX — isPanicking (the FAITHFUL read, NOT a proxy):
       `func (t *TickLoop) isPanicking(e *Entity) bool { if e.ai == nil { return false }; for _, wg := range e.ai.goals.goals { if _, ok := wg.g.(*panicGoal); ok { return wg.running } }; return false }`
       (Mob.isPanicking = the running PanicGoal). Cite ai_goal.go wrappedGoal.running + ai_goals_panic.go panicGoal.
       Do NOT use hurtTime>0 (decays fast; fires for non-panic damage — NOT faithful).
    3. server/ai_goals_breed.go — breedGoal (copy temptGoal's structure):
       - `func newBreedGoal(speed float64) *breedGoal` → baseGoal: newBaseGoal(flagMove | flagLook), speedModifier, partner *Entity, loveTime int.
       - getFreePartner(t, e): t.cur().entities.near(e.x, e.z, 1) filtered to `other.typ==e.typ && e.canMate(other) && !t.isPanicking(other)`; restrict to range 8.0 (distSqr<=64.0); pick NEAREST. (in-region near = the same-region cut.)
       - canUse: `if !e.isInLove() return false; g.partner = getFreePartner(t,e); return g.partner != nil`.
       - canContinueToUse: partner alive && partner.isInLove() && g.loveTime < 60 && !t.isPanicking(partner).
       - start: loveTime=0 + setWantTarget. stop: partner=nil; loveTime=0; clearWantTarget.
       - tick: lookAt(partner) + setWantTarget(partner pos, 1.0) + `g.loveTime++`; if `g.loveTime >= adjustedTickDelay(60) && distSqr(partner) < 9.0` → t.breed(e, g.partner).
       - `func (t *TickLoop) breed(e, partner *Entity)`: spawn child at the midpoint via t.spawnVanillaPig(midX,midY,midZ);
         `child.breedAge = -24000; child.refreshDimensions(); t.broadcastBabyFlag(child)` (baby half-scale + render);
         `e.breedAge = 6000; partner.breedAge = 6000; e.inLove = 0; partner.inLove = 0`;
         the RNG draws — port finalizeSpawnChildFromBreeding ORDER (javap-verify XP-vs-variant order FIRST):
           XP: `t.awardExperienceOrbs(e, 1 + t.rng(e).IntN(7))` ; variant: a nextBoolean() draw picking which parent's
           variant the child inherits (Pig.setVariant — child.variant = (draw ? e : partner).variant). USE THE SAME
           per-mob RNG source the goals use (cite); BOTH draws + their ORDER MUST be mirrored host-side in 33-04's .star
           breed path identically (the lockstep rule). Cite finalizeSpawnChildFromBreeding + the "breed() RNG draws" jarnotes section.
         `t.broadcastHearts(e)`.
    4. server/ai_goals_follow.go — followParentGoal (copy temptGoal, NO RNG):
       - `func newFollowParentGoal(speed float64) *followParentGoal` → baseGoal: newBaseGoal(0) (EMPTY flags), speedModifier, parent *Entity, timeToRecalcPath int.
       - canUse: `if !e.isBaby() return false`; near() filtered to `other.typ==e.typ && other.breedAge >= 0` (ADULT), nearest within inflate(8,4,8) (range 1 + filter distSqr); `if closest==nil return false`; `if closestDistSqr < 9.0 return false` (3²); set g.parent; return true.
       - canContinueToUse: `if !e.isBaby() return false`; parent alive; `9.0 < distSqr(parent) && distSqr(parent) < 256.0`.
       - start: timeToRecalcPath=0. stop: parent=nil.
       - tick (port jarnotes EXACTLY): `if --g.timeToRecalcPath > 0 { return }; g.timeToRecalcPath = adjustedTickDelay(10); setWantTarget(parent pos, 1.1)`. PURE INT — NO RNG.
       Both goals use the existing setWantTarget/clearWantTarget nav seam (ai_mob.go:179-198), like tempt.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func adjustedTickDelay" server/*.go` returns 1; `grep -c "reducedTickDelay" server/ai_goals_breed.go server/ai_goals_follow.go` returns 0 (the WRONG helper is NOT used in the new goals).
    - `grep -c "func (e \*Entity) canMate" server/entity.go` returns 1; `grep -c "func (t \*TickLoop) isPanicking" server/*.go` returns 1.
    - `grep -c "wg.g.(\*panicGoal)" server/*.go` returns 1 (isPanicking reads the running PanicGoal, NOT hurtTime).
    - `grep -c "func newBreedGoal" server/ai_goals_breed.go` and `grep -c "func newFollowParentGoal" server/ai_goals_follow.go` each return 1.
    - `grep -c "breedAge = -24000\|breedAge = 6000\|child.refreshDimensions" server/ai_goals_breed.go` returns >= 3 (child baby + half-scale + parent cooldown).
    - `grep -c "IntN(7)" server/ai_goals_breed.go` returns >= 1 AND the variant nextBoolean() draw is present + cited (grep the variant/Bool draw).
    - `CGO_ENABLED=0 go build ./server/` exits 0.
  </acceptance_criteria>
  <done>adjustedTickDelay (identity) + canMate + faithful isPanicking + breedGoal (getFreePartner + breed() with both RNG draws + child half-scale) + followParentGoal (NO RNG) exist; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register BreedGoal@3 + FollowParentGoal@5 in newPigAI + the Go-native goal-count test</name>
  <read_first>
    - server/ai_mob.go:210-269 (newPigAI registration :235-267; the DEFERRED doc :212-234, :232-233 — flip to PORTED)
    - server/ai_goal.go:149 (addGoal insertion-sort) + :182-207 (empty-flag goal handled — no change)
    - server/ai_mob_test.go:137-191 (TestPigGoalSetRegistered: !=7 → !=9; byPriority block :166-190)
    - 33-PATTERNS.md §5 (newPigAI +2) + §12 (the Go-native test — the other 3 tests are 33-04)
  </read_first>
  <files>server/ai_mob.go, server/ai_mob_test.go</files>
  <action>
    1. server/ai_mob.go newPigAI: add `m.goals.addGoal(3, newBreedGoal(1.0))` and `m.goals.addGoal(5, newFollowParentGoal(1.1))`
       (jarnotes). Match the jar order for readability (addGoal insertion-sorts). Flip the DEFERRED doc comment
       (:212-234, :232-233) to PORTED — the Go-native pig now has all 9 goals {0,1,3,4,4,5,6,7,8}. NO change to
       serverAiStep (the goals reuse setWantTarget/clearWantTarget).
    2. Update ONLY the Go-native goal-count test (the 3 plugin-side tests are 33-04):
       ai_mob_test.go:137 `!= 7` → `!= 9`; in the byPriority block (:166-190) add `byPriority[3].(*breedGoal)` with
       flags `flagMove|flagLook` and `byPriority[5].(*followParentGoal)` with flags `0`. Update the Fatalf message to 9.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestPigGoalSetRegistered' -timeout 120s 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "m.goals.addGoal(3, newBreedGoal\|m.goals.addGoal(5, newFollowParentGoal" server/ai_mob.go` returns 2.
    - `grep -c "!= 9" server/ai_mob_test.go` returns >= 1.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPigGoalSetRegistered'` PASSES (the Go-native pig has 9 goals with the right types/flags).
    - `CGO_ENABLED=0 go vet ./server/` exits 0; `CGO_ENABLED=0 go build ./...` exits 0.
    - NOTE: TestPluginPigEqualsGoNativePig may go RED here (Go pig 9 goals, plugin pig 7 until 33-04) — EXPECTED, closed by 33-04/33-05. Record the observed behavior in the SUMMARY for 33-04.
  </acceptance_criteria>
  <done>newPigAI registers 9 goals; TestPigGoalSetRegistered passes; the plugin-vs-native gate is wired to close in 33-04/33-05 (documented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tick RNG stream → oracle | the breed() XP nextInt(7) + variant nextBoolean() draws must be mirrored identically onto both .star pigs (33-04) or the 9v9 gate (33-05) diverges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | a new goal/RNG draw diverging the oracle | mitigate | BreedGoal.canUse isInLove-gated (dormant on the un-fed oracle); FollowParentGoal isBaby-gated + NO RNG; the XP + variant draws confined to mid-breeding; mirrored onto both .star pigs in 33-04, gated 9v9 in 33-05. |
| T-33-08 | Tampering | cross-region breeding mutating a foreign region's mob | accept | v5 ships the SAME-REGION cut (in-region near() scan — the documented ROADMAP deviation; rolled into 33-deviations.md in the gate plan). |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0; `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestPigGoalSetRegistered|TestAging|TestBabyHitbox|TestInLove|TestFeed|TestTempt|TestPanic|TestFloat|TestPigStrolls' -timeout 240s -v` PASS (the full 9v9 TestPluginPigEqualsGoNativePig gate is closed in 33-05).
- gopls is STALE — trust the compiler.
</verification>

<success_criteria>
- breedGoal@3 (getFreePartner + breed() with XP + variant draws + child half-scale) + followParentGoal@5 (NO RNG) exist as Go-native goals.
- adjustedTickDelay (identity, NOT reducedTickDelay) + canMate + the FAITHFUL isPanicking (running PanicGoal) are built.
- newPigAI registers 9 goals; TestPigGoalSetRegistered passes; the plugin-vs-native divergence is documented and slated to close in 33-04/33-05.
- CGO=0 clean; no new Go deps.
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-03-SUMMARY.md`
</output>
