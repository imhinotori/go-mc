---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/entity.go
  - server/combat_mob.go
  - server/tick_phases.go
  - server/entity_encode.go
  - server/aging_mob_test.go
autonomous: true
requirements: [MOB-SUB-08]
tags: [aging, ageable-mob, metadata, pig-parity]

must_haves:
  truths:
    - "A baby pig (breedAge < 0) ticks its breedAge up by +1 each tick toward 0 (becomes an adult)"
    - "An adult pig on breeding cooldown (breedAge > 0) ticks breedAge down by -1 each tick toward 0"
    - "A lone un-fed adult pig (breedAge == 0) is a pure-int no-op: breedAge stays 0 every tick, zero RNG draws"
    - "isBaby() == (breedAge < 0); the DATA_BABY_ID wire metadata reflects it so the client renders a small pig"
    - "The aging tick runs OUTSIDE serverAiStep (in the tickMobIFrames per-mob loop) so the pig oracle RNG stream is unperturbed"
  artifacts:
    - path: "server/entity.go"
      provides: "breedAge int field on Entity (distinct from the existing item/orb `age int`)"
      contains: "breedAge int"
    - path: "server/combat_mob.go"
      provides: "tickMobAging — the pure-int per-tick breedAge decrement/increment toward 0"
      contains: "func (t *TickLoop) tickMobAging"
    - path: "server/entity_encode.go"
      provides: "babyDataEntry + boolSerializerID + dataBabyIndex (DATA_BABY_ID BOOLEAN metadata)"
      contains: "func babyDataEntry"
    - path: "server/aging_mob_test.go"
      provides: "aging tests (baby ticks to adult, cooldown decays, age-0 no-op, isBaby)"
      contains: "func TestAging"
  key_links:
    - from: "server/tick_phases.go"
      to: "tickMobAging"
      via: "the per-mob loop that already calls tickMobIFrames at tick_phases.go:324-326"
      pattern: "tickMobAging\\("
    - from: "server/entity_encode.go babyDataEntry"
      to: "Entity.breedAge"
      via: "isBaby = breedAge < 0 → BOOLEAN metadata entry index 16"
      pattern: "babyDataEntry"
---

<objective>
Build the AgeableMob aging subsystem: a signed-int `breedAge` machine on Entity (baby <0 ticks up to 0; adult cooldown >0 ticks down to 0; ==0 = adult-ready), the RNG-free per-tick `tickMobAging` (twin of `tickMobIFrames`, running OUTSIDE serverAiStep), and the DATA_BABY_ID BOOLEAN wire metadata so the client renders a small pig.

Purpose: This is the foundation subsystem for breeding (Plan B feeds it inLove, Plan C breeds using it). It is the FIRST plan of the dogfood-gate phase and MUST keep `TestPluginPigEqualsGoNativePig` byte-identical: the aging tick is PURE INT (no RNG) and a lone un-fed adult has breedAge==0, so the tick is a no-op on the oracle pig.
Output: `breedAge` field + `tickMobAging` + DATA_BABY_ID BOOLEAN metadata + aging tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md

<oracle_gate_strategy>
THE MAKE-OR-BREAK CONSTRAINT (every plan in this phase honors it):
The oracle pig (`TestPluginPigEqualsGoNativePig`) is an un-fed lone adult: breedAge==0, inLove==0,
no partner/food/baby near. For THIS plan:
- `tickMobAging` is PURE INT (breedAge += sign toward 0) — NO RNG. Adding it to the per-mob loop
  perturbs NOTHING in the per-mob RNG stream the oracle pins.
- It is STATEFUL, so the oracle pig's breedAge MUST initialize IDENTICALLY (0) on both the Go-native
  and the plugin pig spawn. The Entity zero-value (breedAge 0) gives this for free on NewEntity —
  confirm no spawn path sets it.
- It runs in the SAME per-mob loop as tickMobIFrames (tick_phases.go:324-326), structurally OUTSIDE
  serverAiStep's goal callbacks / navigation.tick (combat_mob.go:644-646 explains WHY).
GATE: run `TestPluginPigEqualsGoNativePig` at the end of this plan — it MUST stay green.
</oracle_gate_strategy>

<interfaces>
<!-- Verified from the codebase. Use directly, no exploration needed. -->

From server/entity.go (the COLLISION to avoid — do NOT reuse this field):
```go
// age is ItemEntity.age: ticks since spawn. ... The XP-orb tick reuses this field with the
// SAME semantics (ExperienceOrb.age, discard at 6000) ...
age int   // entity.go:119 — ItemEntity/XP-orb, NOT the AgeableMob breed-age. Name the new field breedAge.
```

From server/combat_mob.go:647-654 (the EXACT twin to mirror):
```go
func (t *TickLoop) tickMobIFrames(e *Entity) {
	if e.hurtTime > 0 { e.hurtTime-- }
	if e.invulnerableTime > 0 { e.invulnerableTime-- }
}
```

From server/tick_phases.go:324-326 (the per-mob loop — add the tickMobAging call here):
```go
for _, e := range t.cur().entities.byID {
	t.tickMobIFrames(e)
}
```

From server/entity_encode.go (the metadata builders + serializer consts — mirror airDataEntry, swap INT→BOOLEAN):
```go
const intSerializerID int32 = 1    // entity_encode.go:284 — EntityDataSerializers.INT
const byteSerializerID int32 = 0   // entity_encode.go:317 — EntityDataSerializers.BYTE
func airDataEntry(air int32) entityDataEntry {        // entity_encode.go:290
	return entityDataEntry{index: dataAirSupplyIndex, serializerID: intSerializerID, value: pk.VarInt(air)}
}
// entityDataEntry framing (entity_encode.go:186): Byte(index) + VarInt(serializerID) + value
func encodeSetEntityDataByID(entityID int32, entries ...entityDataEntry) pk.Packet  // entity_encode.go:423
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add breedAge field + tickMobAging (the pure-int aging tick)</name>
  <read_first>
    - server/entity.go:108-160 (the item-`age` field at :119 — the COLLISION; and the per-entity int block where breedAge goes)
    - server/combat_mob.go:636-654 (tickMobIFrames — the exact twin + the WHY-outside-serverAiStep comment)
    - server/tick_phases.go:310-340 (the per-mob loop calling tickMobIFrames at :324-326)
    - 33-JARNOTES.md:34-42 (AgeableMob: BABY_START_AGE -24000; age<0 baby→0; age>0 cooldown→0; ==0 adult; isBaby age<0; per-tick ageUp = setAge(age + (age<0?+1:age>0?-1:0)))
    - 33-PATTERNS.md §1 (the field placement) + §2 (the tick analog + insertion point)
  </read_first>
  <files>server/entity.go, server/combat_mob.go, server/tick_phases.go</files>
  <action>
    1. In server/entity.go, add a `breedAge int` field to the Entity struct in the per-entity int block
       (place it right after the i-frame/death block, ~entity.go:238 after `deathTime int32`).
       Do NOT reuse the existing `age int` (entity.go:119, ItemEntity/XP-orb). Doc-comment it in the
       i-frame field style: AgeableMob.age — `breedAge < 0` = baby (ticks toward 0); `> 0` = breeding
       cooldown (ticks toward 0); `== 0` = adult-ready. Tick-owned plain int, zero for non-animals,
       snapshot-friendly (TICK-05 contract — plain value, not a pointer/map). Cite AgeableMob + BABY_START_AGE -24000.
    2. Add an `isBaby()` helper method on *Entity: `return e.breedAge < 0` (AgeableMob.isBaby, jarnotes:40).
       Place it near the field or with the other Entity predicate methods.
    3. In server/combat_mob.go, add `func (t *TickLoop) tickMobAging(e *Entity)` as a sibling of
       tickMobIFrames (right after it, ~combat_mob.go:654). Body — PURE INT, port AgeableMob per-tick
       ageUp (jarnotes:39): `if e.breedAge < 0 { e.breedAge++ } else if e.breedAge > 0 { e.breedAge-- }`.
       Carry the same doc-comment discipline: cite AgeableMob.aging/setAge, note it is PURE INTEGER MATH
       (no RNG draw) so it cannot perturb the per-mob RNG stream the pig oracle pins, and that it runs in
       the dedicated per-mob loop in tickAI structurally OUTSIDE serverAiStep's goal callbacks.
    4. In server/tick_phases.go, in the per-mob loop at :324-326 that calls `t.tickMobIFrames(e)`, add
       `t.tickMobAging(e)` on the next line (same loop body, same OUTSIDE-serverAiStep position).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "breedAge int" server/entity.go` shows the new field (1 match in the struct).
    - `grep -c "func (t \*TickLoop) tickMobAging" server/combat_mob.go` returns 1.
    - `grep -v '^[[:space:]]*//' server/tick_phases.go | grep -c "t.tickMobAging(e)"` returns 1 (the call is live, not commented).
    - `CGO_ENABLED=0 go build ./server/` exits 0.
  </acceptance_criteria>
  <done>breedAge field + isBaby() + tickMobAging exist, wired into the per-mob OUTSIDE-serverAiStep loop; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DATA_BABY_ID BOOLEAN metadata (the small-pig wire flag)</name>
  <read_first>
    - server/entity_encode.go:180-360 (entityDataEntry framing :186-212; itemDataEntry :250; airDataEntry :290 with intSerializerID :284; livingEntityFlagsEntry :331 with byteSerializerID :317; the player-customisation block :350-360; encodeSetEntityDataByID :423)
    - 33-JARNOTES.md:119-130 (the DATA_BABY_ID accessor-index derivation: Entity 0-7, LivingEntity 8-14, Mob 15, AgeableMob 16 = DATA_BABY_ID BOOLEAN — EXECUTOR MUST javap-confirm)
    - 33-PATTERNS.md §3 (the BOOLEAN serializer gap + the index walk + the babyDataEntry builder)
  </read_first>
  <files>server/entity_encode.go</files>
  <action>
    1. JAVAP-CONFIRM FIRST (do NOT guess):
       - `javap -c -p net.minecraft.network.syncher.EntityDataSerializers` from temp/cache/26.2-inner.jar
         (CFR/javap): read the static{} registerSerializer order to pin the BOOLEAN serializer id.
         The documented order is 0=BYTE,1=INT,2=LONG,3=FLOAT,4=STRING,5=COMPONENT,6=OPTIONAL_COMPONENT,
         7=ITEM_STACK,8=BOOLEAN (entity_encode.go:237-242 documents this) → expect 8, but VERIFY.
       - `javap -c -p net.minecraft.world.entity.Mob` (count defineId calls — DATA_MOB_FLAGS is one → index 15)
         and `javap -c -p net.minecraft.world.entity.AgeableMob` (DATA_BABY_ID is its first defineId → index 16).
         Walk the hierarchy EXACTLY like the Avatar=16 derivation documented at entity_encode.go:339-347.
       - Confirm `pk.Boolean` exists in net/packet (the BOOLEAN value codec = a single byte 0/1, ByteBufCodecs.BOOL).
         If pk.Boolean is absent, use `pk.UnsignedByte(0|1)` — check net/packet first; cite which you used.
    2. Add two consts near intSerializerID/byteSerializerID (entity_encode.go:284/317):
       `const boolSerializerID int32 = <verified, expect 8>` and `const dataBabyIndex int32 = <verified, expect 16>`,
       each with a doc comment citing the javap output (EntityDataSerializers order + the AgeableMob defineId walk).
    3. Add the builder after the player-customisation block (~entity_encode.go:360), mirroring airDataEntry:
       ```go
       func babyDataEntry(isBaby bool) entityDataEntry {
           return entityDataEntry{index: dataBabyIndex, serializerID: boolSerializerID, value: pk.Boolean(isBaby)}
       }
       ```
       (substitute the verified value codec). Doc-comment: DATA_BABY_ID — Byte(16) + VarInt(boolSerializerID) +
       Boolean(isBaby); the client renders a small pig when true. Cite AgeableMob.DATA_BABY_ID.
    4. Wire the baby flag onto the spawn metadata path: find where the pig's spawn metadata / SetEntityData is
       built (grep `airDataEntry\|livingEntityFlagsEntry\|encodeSetEntityDataByID` for the mob-spawn carry site)
       and include `babyDataEntry(e.isBaby())` so a spawned baby renders small. If the spawn metadata is carried
       via Entity.metadata, follow the playerSkinMetadata precedent (entity_encode.go:366-373); otherwise push it
       via encodeSetEntityDataByID at spawn. ALSO push a DATA_BABY_ID update via encodeSetEntityDataByID when
       breedAge crosses 0 (baby→adult) — but defer the cross-0 push to Plan C's breed/age-up flow if no broadcast
       seam exists yet; for THIS plan, the spawn-time carry is the minimum (the oracle pig is always an adult →
       isBaby false → entry value false → no behavior change, gate-safe).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func babyDataEntry" server/entity_encode.go` returns 1.
    - `grep -c "boolSerializerID\|dataBabyIndex" server/entity_encode.go` returns >= 3 (2 consts + >=1 use).
    - The const doc comments cite the javap-confirmed EntityDataSerializers order and the AgeableMob defineId index.
    - `CGO_ENABLED=0 go build ./server/` exits 0.
  </acceptance_criteria>
  <done>babyDataEntry + boolSerializerID + dataBabyIndex exist (javap-confirmed), wired onto the spawn metadata; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aging tests + the oracle gate</name>
  <read_first>
    - server/combat_mob_test.go:240-270 (the tickMobIFrames test pattern — spawn an entity, set the int, call the tick, assert the decrement)
    - server/plugin_pig_test.go (the spawnVanillaPig helper usage + TestPluginPigEqualsGoNativePig at :251)
    - server/vanilla_pig.go:36 (spawnVanillaPig signature)
    - 33-PATTERNS.md §12 (the oracle stays green only if breed/follow are dormant — for THIS plan, aging is a pure-int no-op on age-0)
  </read_first>
  <files>server/aging_mob_test.go</files>
  <action>
    Create server/aging_mob_test.go mirroring the tickMobIFrames test (combat_mob_test.go:240-270):
    1. `TestAgingBabyTicksToAdult`: spawn a pig (or NewEntity), set breedAge = -3, call `loop.tickMobAging(e)`
       three times, assert breedAge == 0 and `e.isBaby() == false` after; assert isBaby() == true while < 0.
    2. `TestAgingCooldownDecays`: set breedAge = +3, call tickMobAging three times, assert breedAge == 0 (cooldown decays toward 0, never below).
    3. `TestAgingAdultIsNoOp`: set breedAge = 0, call tickMobAging 10 times, assert breedAge stays 0 (the
       oracle's lone-adult no-op — PURE INT, zero change).
    4. Optionally assert babyDataEntry(true)/babyDataEntry(false) build the expected index/serializer entry
       (a direct builder unit check) — keep it lightweight if the entry fields are unexported-but-package-visible.
    Use the existing test harness (newTestTickLoop / spawnVanillaPig — match combat_mob_test.go's setup).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestAging|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestPigGoalSet' -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go vet ./server/` exits 0.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestAging' -v` shows TestAgingBabyTicksToAdult, TestAgingCooldownDecays, TestAgingAdultIsNoOp all PASS.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -timeout 120s` PASSES (THE GATE — byte-identical, the aging tick is a pure-int no-op on the un-fed lone adult).
  </acceptance_criteria>
  <done>Aging tests pass AND the pig oracle stays byte-identical green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server tick → client (metadata wire) | DATA_BABY_ID metadata is server-authored and broadcast; client only renders. No untrusted input crosses inbound here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | tickMobAging perturbing the oracle RNG stream | mitigate | tickMobAging is PURE INT (no RNG); runs in the tickMobIFrames loop OUTSIDE serverAiStep. Gated by TestPluginPigEqualsGoNativePig (Task 3). |
| T-33-02 | Information disclosure | DATA_BABY_ID metadata leaking server state | accept | The baby flag is intentionally public (the client renders it); no PII, vanilla-faithful wire. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0.
- `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestAging|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestPigGoalSet|TestVanillaPig' -timeout 240s -v` all PASS.
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/` clean.
- gopls is STALE — trust the compiler, not the language server.
</verification>

<success_criteria>
- breedAge int (distinct from item `age`) + isBaby() + tickMobAging (pure-int, OUTSIDE serverAiStep) exist and are wired into the per-mob loop.
- DATA_BABY_ID BOOLEAN metadata (javap-confirmed index + serializer) renders a small pig on spawn.
- Aging tests pass; the pig oracle stays byte-identical (the gate).
- CGO=0 build/vet clean; no new Go deps; Docker -race clean.
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md`
</output>
