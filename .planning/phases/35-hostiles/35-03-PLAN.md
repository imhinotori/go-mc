---
phase: 35-hostiles
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - plugins/vanilla_zombie/main.star
  - plugins/vanilla_zombie/plugin.toml
  - server/assets/vanilla_zombie/main.star
  - server/assets/vanilla_zombie/plugin.toml
  - server/zombie_test.go
autonomous: true
requirements: [MOB-HOST-01]
must_haves:
  truths:
    - "vanilla_zombie declares Zombie.registerGoals: goalSelector ZombieAttackGoal@3 + WaterAvoidingRandomStroll@7 + LookAtPlayer@8 + LookAround@8; targetSelector HurtByTargetGoal@1 + NearestAttackableTargetGoal<Player>@2"
    - "the zombie spawns, acquires a player target via NearestAttackableTargetGoal, paths to it, and deals melee damage through ZombieAttackGoal"
    - "the TARGET-flagged goals carry flags=[\"TARGET\"] so buildAIFromDecl routes them into the targetSelector"
    - "the repo-root and embed copies of vanilla_zombie are byte-identical"
  artifacts:
    - path: "plugins/vanilla_zombie/main.star"
      provides: "the jar-faithful zombie declaration"
      contains: "declare_mob"
    - path: "server/assets/vanilla_zombie/main.star"
      provides: "the byte-identical embed copy"
      contains: "declare_mob"
    - path: "server/zombie_test.go"
      provides: "the per-hostile behavior + RNG gate"
      contains: "func TestZombie"
  key_links:
    - from: "plugins/vanilla_zombie/main.star NearestAttackableTargetGoal goal"
      to: "the targetSelector (via buildAIFromDecl)"
      via: "flags=[\"TARGET\"]"
      pattern: "TARGET"
    - from: "plugins/vanilla_zombie/main.star ZombieAttackGoal tick"
      to: "the melee damage keystone"
      via: "the host attack handle"
      pattern: "ZombieAttackGoal|melee"
---

<objective>
Build the zombie as a jar-faithful dogfood plugin (MOB-HOST-01, SC#2) — Zombie.registerGoals re-expressed as vanilla_zombie. It reuses the shared passive goals (WaterAvoidingRandomStroll/LookAtPlayer/LookAround — copied VERBATIM from vanilla_cow) and the NEW target/attack goals (HurtByTargetGoal, NearestAttackableTargetGoal, ZombieAttackGoal) from Plan 35-01, routed by flags. The zombie hunts the player at night and melees through the keystone.

Purpose: The first hostile. It proves the targetSelector subsystem end-to-end (acquire -> path -> melee) and is the template for skeleton/spider. The DOGFOOD pattern continues — vanilla mobs ARE jar-faithful plugins.
Output: the vanilla_zombie plugin pair (repo-root + byte-identical embed) + a per-hostile behavior test + the focused RNG assertion. NO Go-file change (all shared Go is in 35-01/35-02; the embed wiring is in the gate plan 35-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-hostiles/35-JARNOTES.md
@.planning/phases/35-hostiles/35-PATTERNS.md
@.planning/phases/35-hostiles/35-CONTEXT.md
@./CLAUDE.md
@plugins/vanilla_cow/main.star
@plugins/vanilla_cow/plugin.toml
@server/cow_test.go

<interfaces>
<!-- The .star builtins + host handles the executor declares against — DO NOT re-explore. -->

declare_mob(name, base_type, attributes, goals) + goal(priority, flags, can_use, start, tick, stop,
can_continue, requires_update_every_tick) — both shown in plugins/vanilla_cow/main.star.

flags routing: flags=["TARGET"] -> buildAIFromDecl (35-01) routes the goal into m.targetSelector;
flags=["MOVE"]/["MOVE","LOOK"]/["LOOK"]/["JUMP"] -> m.goals. "TARGET" already maps to flagTarget
(plugin_mob_decl.go:198). base_type "zombie" already exists (baseTypeByName lines 113-116).

RNG draw seams: entity.rand_int(bound) (= e.ai.rng.nextInt) and entity.rand_float() — used by the pig/cow
stroll/look goals (vanilla_cow/main.star:284,323). The NearestAttackableTargetGoal gate uses
entity.rand_int(10) (the LOCKSTEP full 10, 35-JARNOTES.md:159-164).

host target/attack handles (from 35-01's goals): the .star target/attack callbacks invoke the host handles
the Go goals in ai_goals_target.go / ai_goals_attack.go expose — confirm their exact builtin names at exec
(e.g. entity.nearest_attackable_player(range), entity.set_attack_target(id), entity.melee_attack()). If
35-01 ported these as Go-native goals (not .star callbacks), the declaration references them by a goal kind
rather than callbacks — CONFIRM the integration shape from 35-01-SUMMARY.md before writing.

attributes seeding: seedAttributes/attrAlias (35-01 added follow_range/attack_damage/armor aliases).
Zombie attrs (35-JARNOTES.md:75): max_health 20 (Mob base), movement_speed 0.23, follow_range 35.0,
attack_damage 3.0, armor 2.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vanilla_zombie plugin pair (repo-root + byte-identical embed)</name>
  <read_first>
    - plugins/vanilla_cow/main.star (the FULL template — the goal callbacks, the declare_mob block, the draw-order cite style)
    - plugins/vanilla_cow/plugin.toml (the manifest + the least-privilege cap set)
    - server/assets/vanilla_cow/main.star + plugin.toml (the byte-identical embed sibling)
    - .planning/phases/35-hostiles/35-JARNOTES.md (Zombie.registerGoals lines 18-35 + attributes line 75 + NearestAttackableTargetGoal lines 149-164 + MeleeAttackGoal lines 166-175)
    - .planning/phases/35-hostiles/35-CONTEXT.md (Zombie goal set line 51-53 + attributes line 63 + the cite-deferred goals)
    - .planning/phases/35-hostiles/35-01-SUMMARY.md (the EXACT host-handle/goal-kind integration shape for the target/attack goals)
  </read_first>
  <behavior>
    - The vanilla_zombie declaration loads through the host with declare_mob/goal builtins (no parse error).
    - The goal set + priorities + flags match Zombie.registerGoals EXACTLY (the v1 subset).
    - The repo-root and server/assets copies are byte-for-byte identical.
  </behavior>
  <action>
Create plugins/vanilla_zombie/main.star — a 1:1 vanilla-Zombie dogfood, citing
net.minecraft.world.entity.monster.zombie.Zombie.registerGoals. Copy the vanilla_cow header/structure style.

GOAL SET (35-CONTEXT:51-53, 35-JARNOTES:18-35 — the v1 subset; the spear/turtle-egg/village goals are
CITE-DEFERRED, document each in a header comment):
  goalSelector:
    @3 ZombieAttackGoal(mob, 1.0, false)         flags=["MOVE","LOOK"]  — MeleeAttackGoal subclass, plain melee
    @7 WaterAvoidingRandomStrollGoal(mob, 1.0)   flags=["MOVE"]         — COPY VERBATIM from vanilla_cow stroll
    @8 LookAtPlayerGoal(Player, 8.0)             flags=["LOOK"]         — COPY from vanilla_cow look (lookDist 8.0 NOT 6.0)
    @8 RandomLookAroundGoal                       flags=["MOVE","LOOK"]  — COPY VERBATIM from vanilla_cow around
  targetSelector (flags=["TARGET"] -> routed to m.targetSelector by buildAIFromDecl):
    @1 HurtByTargetGoal(mob)                      flags=["TARGET"]       — the 35-01 hurtByTargetGoal
    @2 NearestAttackableTargetGoal<Player>(mob, mustSee)  flags=["TARGET"]  — the 35-01 nearestAttackableTargetGoal

The shared passive goal callbacks (stroll/look/around) are mob-agnostic — COPY VERBATIM from vanilla_cow,
adjusting ONLY the LookAtPlayer lookDistance constant (8.0 for the zombie vs the cow's 6.0). The target/attack
goals reference 35-01's ported goals — wire them per the integration shape recorded in 35-01-SUMMARY.md (host
handle names or goal-kind). The NearestAttackableTargetGoal RNG gate uses entity.rand_int(10) (the LOCKSTEP
full 10 — cite 35-JARNOTES.md:159-164: "DO NOT halve to 5"). The ZombieAttackGoal melee cooldown is the
RNG-free 20-tick gate (MeleeAttackGoal, 35-JARNOTES:166-175) — draw NO RNG in the melee callback.

ATTRIBUTES dict (35-CONTEXT:63, 35-JARNOTES:75): max_health 20.0, movement_speed 0.23, follow_range 35.0,
attack_damage 3.0, armor 2.0. Each cited to Monster.createMonsterAttributes + Zombie's createAttributes adds.

base_type = "zombie". Document the CITE-DEFERRED goals in the header (SpearUseGoal@2, ZombieAttackTurtleEggGoal,
MoveThroughVillageGoal, the IronGolem/Villager/Turtle NearestAttackableTarget variants, reinforcement spawns)
— each a one-line "DEFERRED: <jar goal> — <why> (no weapon/village/turtle subsystem)" so nothing is silent.

Create plugins/vanilla_zombie/plugin.toml — clone vanilla_cow/plugin.toml; keep the least-privilege cap set.
The melee player-damage is a HOST-side op (like the cow's milking) — confirm it routes through a host handle,
NOT a new plugin capability (prefer least-privilege). Cite the same reasoning the cow manifest does.

Create the BYTE-IDENTICAL embed copies at server/assets/vanilla_zombie/main.star + plugin.toml (copy the
repo-root files verbatim — the embed copy is the SWAP source-of-truth, vanilla_pig_embed.go:26-30 invariant).
  </action>
  <verify>
    <automated>cd D:/ender && diff plugins/vanilla_zombie/main.star server/assets/vanilla_zombie/main.star && diff plugins/vanilla_zombie/plugin.toml server/assets/vanilla_zombie/plugin.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "declare_mob" plugins/vanilla_zombie/main.star` returns AND `grep -n "base_type = \"zombie\"" plugins/vanilla_zombie/main.star` returns.
    - `grep -c "\"TARGET\"" plugins/vanilla_zombie/main.star` >= 2 (HurtByTarget + NearestAttackableTarget).
    - `grep -n "rand_int(10)" plugins/vanilla_zombie/main.star` returns AND `grep -c "rand_int(5)" plugins/vanilla_zombie/main.star` == 0.
    - `grep -ci "DEFERRED" plugins/vanilla_zombie/main.star` >= 3 (spear/turtle-egg/village/variants documented).
    - `diff plugins/vanilla_zombie/main.star server/assets/vanilla_zombie/main.star` produces EMPTY output.
    - `diff plugins/vanilla_zombie/plugin.toml server/assets/vanilla_zombie/plugin.toml` produces EMPTY output.
  </acceptance_criteria>
  <done>The vanilla_zombie pair declares the jar-faithful goal set with TARGET routing + the lockstep rand_int(10) gate + the cited deferrals; both copies are byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: zombie_test.go — spawn + goal-set + target-acquire + melee-hit behavior + RNG gate</name>
  <read_first>
    - server/cow_test.go (the loadVanillaCowRegistry temp-dir harness lines 36-60 + the spawn/goal-set/behavior assertions)
    - server/ai_goals_target_test.go (the focused RNG-gate test shape from 35-01)
    - plugins/vanilla_zombie/main.star (the declaration under test)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the hostiles_test.go entry lines 428-443)
  </read_first>
  <behavior>
    - TestZombieBootLoads: the vanilla_zombie registry loads + the declaration has the expected goal count +
      the 2 TARGET goals route into the targetSelector, the rest into the goalSelector.
    - TestZombieBehavior: spawn a zombie + a player in range at night; tick; assert the zombie acquires the
      player as attackTargetID, paths toward it, and (in melee reach) the player takes ATTACK_DAMAGE (3.0).
    - TestZombieTargetGateRNG: the NearestAttackableTargetGoal canUse gate draws exactly one rand_int(10).
  </behavior>
  <action>
Create server/zombie_test.go — clone the cow_test.go loadVanillaCowRegistry harness as loadVanillaZombieRegistry
(materialize ../plugins/vanilla_zombie to a temp dir, LoadDirWith the declare_mob/goal builtins). This is a
LIGHTER per-mob gate (spawn + goal-set + behavior), NOT a bit-exact Go-vs-plugin oracle (the pig proved the
subsystems — 35-CONTEXT:91-92).

Tests:
  - TestZombieBootLoads: load the registry; assert the declaration name "vanilla_zombie", the goal count
    (4 goalSelector + 2 targetSelector), and that the 2 TARGET-flagged goals were routed into the
    targetSelector (assert m.targetSelector has 2 goals, m.goals has 4) via buildAIFromDecl.
  - TestZombieBehavior: spawn a vanilla_zombie + a player within FOLLOW_RANGE at a gametime in the night
    window; force the rand_int(10) gate to fire (seed so nextInt(10)==0); tick the AI; assert
    e.ai.attackTargetID == the player's id, the nav want points toward the player, and after melee reach +
    the 20-tick cooldown the player's health dropped by ATTACK_DAMAGE 3.0 (through the keystone).
  - TestZombieTargetGateRNG: seed the entity rng; assert NearestAttackableTargetGoal.canUse draws exactly one
    rand_int with bound 10.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./server/ -run "TestZombieBootLoads|TestZombieBehavior|TestZombieTargetGateRNG" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func TestZombieBootLoads" server/zombie_test.go` AND `grep -n "func TestZombieBehavior" server/zombie_test.go` AND `grep -n "func TestZombieTargetGateRNG" server/zombie_test.go` all return.
    - `grep -n "loadVanillaZombieRegistry" server/zombie_test.go` returns (the temp-dir harness).
    - `cd D:/ender && go test ./server/ -run "TestZombieBootLoads|TestZombieBehavior|TestZombieTargetGateRNG" -count=1` exits 0.
  </acceptance_criteria>
  <done>The zombie boot-loads with the right goal routing, acquires + melees the player at night, and the target gate draws exactly rand_int(10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vanilla_zombie .star -> host buildAIFromDecl | the declared goal set + attributes cross into the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Tampering | the zombie attribute dict (attack_damage 3.0) | mitigate | seedAttributes seeds only known aliased attributes through the host (35-01); an unknown attr key is silently skipped, not injected — the host owns the attribute supplier. |
| T-35-07 | Elevation | the melee callback -> player damage | mitigate | the melee routes through the shared 35-01 host attack handle (attacker = mob id, host-set); the plugin cannot forge damage or target a non-acquired entity. |
</threat_model>

<verification>
- `diff` of both copies empty; `go test` of the 3 zombie tests green.
- The goal set matches Zombie.registerGoals (v1 subset); TARGET goals route into the targetSelector.
- The lockstep rand_int(10) gate; the deferrals documented.
</verification>

<success_criteria>
- SC#2 (zombie half): the zombie acquires a player target, paths to it, deals melee damage through the keystone; maps to MONSTER (via 35-02).
- MOB-HOST-01: the jar-faithful Zombie.registerGoals plugin with ZombieAttackGoal + target selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/35-hostiles/35-03-SUMMARY.md` (record the cite-deferred zombie goals: SpearUseGoal, turtle-egg, village, IronGolem/Villager/Turtle targets, reinforcement spawns, day/night burn if deferred).
</output>
