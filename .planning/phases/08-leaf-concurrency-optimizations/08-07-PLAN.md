---
phase: 08-leaf-concurrency-optimizations
plan: 07
type: execute
wave: 5
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - server/async_stress_test.go
  - save/region/linear_bench_test.go
autonomous: true
requirements: [OPT-06]
user_setup: []

must_haves:
  truths:
    - "A combined concurrent stress test exercises ALL async subsystems AT ONCE under the Docker -race gate (OPT-06): a running TickLoop with players + mobs + a loaded world drives async pathfinding (OPT-01), the async tracker (OPT-02), and the async spawn scan (OPT-03) simultaneously while paths/diffs/spawns are submitted and rejoined — giving the race detector the full pool↔tick boundary to inspect. The gate is `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... ./world/... ./save/... -count=10` and it is GREEN"
    - "A behavior-regression check proves the optimizations did NOT change observable behavior, only WHEN work happens: (a) async paths still ARRIVE and are followed (1+ tick late tolerated) — a mob still navigates around an obstacle; (b) the async tracker still sends spawn/move/despawn to nearby players (matching the synchronous golden); (c) mobs still spawn under the cap; (d) a .linear region round-trips. Each is an automatable assertion over the post-swap server"
    - "This phase adds NO new wire surface (it is internal optimization — no new packets, no changed byte layouts), so the final gate is an AUTOMATABLE -race + behavior-regression gate, NOT a blocking human-verify and NOT a capture-diff — the existing Phase 5/6/7 real-client behavior simply still works (the prior capture-diff fixtures + golden tests remain green). This is confirmed: no encoder/packet file is modified by any Phase-8 plan"
    - "The full existing test suite (all Phase 1-7 tests + the Phase-8 per-subsystem tests) passes under -race at -count=10 — the single-owner discipline makes every async subsystem -race clean BY CONSTRUCTION (the snapshot-on-owner → compute-off-tick → apply-on-owner rejoin), and this gate proves it across the whole server/world/save surface"
    - "A SUMMARY documents the phase result: every OPT-01..06 swap is behind its named existing seam (no rewrite), the rejoin validates target existence, xsync is justified per-use, and the -race gate is green — closing Phase 8 as additive optimization, not a rewrite"
  artifacts:
    - path: "server/async_stress_test.go"
      provides: "the combined all-subsystems concurrent stress test (paths + tracker + spawns under one running tick) — the OPT-06 -race target + the behavior-regression assertions"
      contains: "TestAllAsyncSubsystemsRaceClean"
    - path: "save/region/linear_bench_test.go"
      provides: "the .linear round-trip + size-savings behavior check (OPT-05 still works post-integration)"
      contains: "TestLinearRoundTripIntegration"
  key_links:
    - from: "server/async_stress_test.go"
      to: "the pathPool + trackerPool + spawnPool + applyAsyncResults (all subsystems)"
      via: "a running TickLoop drives all three async swaps simultaneously under -race"
      pattern: "advance"
    - from: "server/async_stress_test.go (behavior regression)"
      to: "the post-swap path-arrival / tracker-send / spawn / linear-roundtrip behaviors"
      via: "automatable assertions that observable behavior is unchanged (only timing shifted)"
      pattern: "TestBehaviorRegression"
    - from: "the Docker -race gate (-count=10)"
      to: "./server/... ./world/... ./save/..."
      via: "the phase-gate command proving every async subsystem -race clean by construction"
      pattern: "go test -race"
---

<objective>
OPT-06: the final acceptance gate for Phase 8 — prove EVERY async subsystem is `-race` clean and that the optimizations changed only WHEN work happens, not WHAT the server does. This wave runs after all of OPT-01/02/03/04/05 have landed (08-02 through 08-06). It ships (1) a combined concurrent stress test that drives async pathfinding + the async tracker + the async spawn scan SIMULTANEOUSLY under one running `TickLoop`, giving the Docker `-race` detector the full pool↔tick boundary to inspect, and (2) a behavior-regression check proving paths still arrive (1+ tick late tolerated), the tracker still sends spawn/move/despawn, mobs still spawn, and a `.linear` region round-trips.

The phase makes NO new wire surface (no new packets, no changed byte layouts — confirmed: no Phase-8 plan touches an encoder/packet file), so the final gate is an AUTOMATABLE `-race` + behavior-regression gate, NOT a blocking human-verify and NOT a capture-diff. The existing Phase 5/6/7 real-client behavior simply still works — the prior capture-diff fixtures and golden tests remain green. The single-owner discipline (snapshot-on-owner → compute-off-tick → apply-on-owner) makes every subsystem `-race` clean BY CONSTRUCTION; this gate proves it across the whole `server/world/save` surface at `-count=10`.

Purpose: OPT-06 ("the server passes -race clean on every async subsystem") — the non-negotiable Phase-8 acceptance bar, applied as the cross-cutting final gate that every async wave already partially proved, now run combined.
Output: server/async_stress_test.go (the combined stress + behavior regression), save/region/linear_bench_test.go (the .linear integration round-trip), the green Docker -race gate, and the phase SUMMARY. New external deps: none.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-leaf-concurrency-optimizations/08-RESEARCH.md
@.planning/phases/08-leaf-concurrency-optimizations/08-01-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-02-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-04-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-05-PLAN.md

<interfaces>
<!-- VERIFIED this session. The running-loop seam the combined stress test drives. -->

From server/tick.go + tick_phases.go (the loop to drive all subsystems through):
```go
func NewTickLoop(clock Clock) *TickLoop // wires pathPool/trackerPool/spawnPool + asyncTracker + asyncIn2
func (t *TickLoop) SetWorld(mgr *world.ChunkManager, worker *world.Worker) // wires the chunk subsystem
func (t *TickLoop) advance(now time.Time) int       // drive logical ticks (test seam)
func (t *TickLoop) applyAsyncResults()              // drains asyncIn + asyncIn2 on the owner
func (t *TickLoop) Close()                           // releases the ants pools (08-01)
// register a player, add a mob (entities.add + newPigAI), load a column — then advance() to drive
// pathfinding + tracker + spawn all at once.
```

From server/clock.go (the injectable test clock to advance the loop deterministically):
```go
// a fake/manual Clock so the stress test drives advance() without real time (existing Phase-3 seam)
```

From the LANDED Phase-8 subsystems (the behaviors to regression-check):
```go
// OPT-01: a mob with a groundNavigation requests a path; after advance() ticks it follows the path.
// OPT-02: asyncTracker sends AddEntity/TeleportEntity/RemoveEntities to a near player (test Client sink).
// OPT-03: naturalSpawn (every spawnInterval) submits a scan; applyTo adds a Pig under cap.
// OPT-05: save/region.WriteLinear/ReadLinear round-trips a region.
```
</interfaces>

<key_facts>
<!-- VERIFIED this session against 08-RESEARCH Validation + the constraints. -->
- THE PHASE GATE COMMAND (08-RESEARCH Validation / STATE.md:148): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... ./world/... ./save/... -count=10`. The host is CGO_ENABLED=0 (no C compiler) so -race MUST run in the golang:1.26 Docker image — the established Phase 2-7 path. -count=10 stresses the scheduler to surface intermittent races.
- COMBINED STRESS, NOT PER-SUBSYSTEM (08-RESEARCH Wave 0 Gaps — "a concurrent stress test that submits many paths/spawns while the tick runs"): the per-subsystem tests (08-02/04/05) each proved their boundary; this wave runs ALL THREE under ONE running loop simultaneously — many mobs pathing + the tracker diffing + the spawner scanning while advance() ticks — so the race detector sees cross-subsystem interleavings the isolated tests can't. Build a small world, register a couple of players, add several mobs with AI, and advance() many ticks while the pools churn.
- BEHAVIOR REGRESSION = SAME WHAT, DIFFERENT WHEN (the ROADMAP "additive optimization, not a rewrite" goal): assert (a) a mob given an obstacle still reaches the far side (path arrived + followed, 1+ tick late OK); (b) a near player's test Client sink received AddEntity then movement then RemoveEntities for a mob entering/moving/leaving range (tracker still works); (c) under cap, a Pig is added within a bounded number of spawn cycles (spawner still works); (d) a .linear region round-trips byte-identically. These are automatable — no human, no capture-diff.
- NO NEW WIRE SURFACE → AUTOMATABLE GATE (the constraint, planner's call confirmed): grep-confirm that NO Phase-8 plan modified an encoder/packet file (entity_encode.go, world/packet.go, the play_join/login/configuration encoders). Since the bytes are unchanged, the existing capture-diff golden tests (TestPlayBytesVsVanillaCapture, the entity capture tests) remain the wire authority and stay green — this wave does NOT add a new capture-diff or a human-verify. Document this reasoning in the SUMMARY (why OPT-06 is automatable, not a real-client gate).
- THE EXISTING SUITE MUST STAY GREEN: the full Phase 1-7 test suite + the Phase-8 per-subsystem tests all pass under -race -count=10. A regression in any prior test means an OPT swap changed behavior (a bug) — fix the swap, not the test.
- -RACE CLEAN BY CONSTRUCTION (STATE.md / 08-RESEARCH Summary): the single-owner rejoin (snapshot-on-owner → off-tick compute → apply-on-owner) is why this passes. If -race flags something, it means a worker captured live state (Pitfall 3) or sent off-tick (Pitfall 5) — trace it to the offending closure in the named subsystem and fix the capture/emission, not the test.
- STALE-LSP CAVEAT (STATE.md): trust `go build ./...` + the Docker -race over gopls phantom errors.
- Serena guidance: create server/async_stress_test.go (the combined running-loop stress + the 4 behavior-regression assertions) and save/region/linear_bench_test.go (the .linear integration round-trip + size check). Run the full Docker -race -count=10 gate. Write the phase SUMMARY documenting the OPT-01..06 coverage + the automatable-gate rationale.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All off-tick pool workers ↔ the single tick owner (combined) | This wave validates the WHOLE Phase-8 boundary at once: every worker reads only its immutable snapshot and rejoins via applyAsyncResults; the owner is the sole mutator. The -race gate is the proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-24 | Tampering/race | a cross-subsystem race only visible when path + tracker + spawn run together | mitigate | The combined stress test drives all three under -race -count=10, surfacing interleavings the isolated tests miss; single-owner makes them clean by construction. |
| T-8-25 | Tampering | an OPT swap silently changing observable behavior (a rewrite, not an optimization) | mitigate | The behavior-regression assertions (path arrives, tracker sends, mob spawns, linear round-trips) + the unchanged capture-diff goldens prove WHAT is unchanged; only WHEN shifted. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Combined all-subsystems concurrent stress test + behavior-regression assertions under -race (OPT-06)</name>
  <files>server/async_stress_test.go, save/region/linear_bench_test.go</files>
  <read_first>
    - .planning/phases/08-leaf-concurrency-optimizations/08-RESEARCH.md — the Validation section (the phase-gate command, the Wave 0 stress-test gap), the Summary (-race clean by construction).
    - the LANDED 08-02/03/04/05/06 SUMMARYs — the exact behaviors to regression-check + the test seams each established (test Client sink, the world/mob builders).
    - server/tick.go + tick_phases.go + clock.go — NewTickLoop / SetWorld / advance / applyAsyncResults / Close (the running-loop seam) + the injectable test clock.
    - server/async_test.go (08-01) + tracker_test.go (08-04) + spawner_test.go (08-05) — reuse their world/mob/player builders so the combined test does not reinvent the scaffold.
  </read_first>
  <behavior>
    OPT-06 (combined race-cleanliness + behavior regression):
    - Test 1 (TestAllAsyncSubsystemsRaceClean): build a small loaded world, register 2 players, add several mobs with newPigAI, then advance() many ticks (e.g. a few hundred) while pathfinding submits, the async tracker diffs, and the spawner scans simultaneously — the test passes cleanly and (under the Docker -race gate) the race detector finds nothing. Call t.Close() to release pools at the end.
    - Test 2 (TestBehaviorRegressionPathArrives): a mob with an obstacle between it and a target still reaches the far side after enough advance() ticks (the async path arrived 1+ tick late and was followed).
    - Test 3 (TestBehaviorRegressionTrackerSends): a test Client sink for a near player receives AddEntity (mob enters range) → movement packets → RemoveEntities (mob leaves range) across advance() ticks (the async tracker still emits the visibility lifecycle).
    - Test 4 (TestBehaviorRegressionMobSpawns): under cap with a loaded world + a player, within a bounded number of spawnInterval cycles a Pig is added to the store (the async spawner still populates).
    - Test 5 (TestLinearRoundTripIntegration, in linear_bench_test.go): a region written via WriteLinear and read via ReadLinear round-trips byte-identically and is smaller than the .mca equivalent (OPT-05 still works post-integration).
  </behavior>
  <action>
    1. server/async_stress_test.go (package server) — TestAllAsyncSubsystemsRaceClean (the combined running-loop stress) + the 3 behavior-regression tests, reusing the Phase-8 test builders (world, mob+AI, player+Client sink) from the per-subsystem test files. Drive the loop via the injectable clock's advance(); drain via applyAsyncResults each tick (or rely on advance()'s tickOnce which calls it). Release pools with Close().
    2. save/region/linear_bench_test.go — TestLinearRoundTripIntegration: the .linear write→read round-trip + the size-vs-mca assertion (reusing the 08-03 codec).
    3. Grep-confirm no Phase-8 plan modified an encoder/packet file; document in the SUMMARY that OPT-06 is therefore an automatable gate (no new capture-diff / no human-verify), and that the existing capture-diff goldens remain the wire authority and stay green.
  </action>
  <verify>
    <automated>go test ./server/... ./world/... ./save/... -count=1 && go vet ./... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The combined stress test drives all async subsystems under one running loop and passes; the behavior-regression assertions prove paths arrive, the tracker sends, mobs spawn, and .linear round-trips (WHAT unchanged, WHEN shifted).
    - No Phase-8 plan modified an encoder/packet file (grep-confirmed); the existing capture-diff goldens remain green — OPT-06 is an automatable gate, not a human-verify/capture-diff.
    - `go test ./server/... ./world/... ./save/...` (non-race), `go vet`, `go build` all clean.
  </acceptance_criteria>
  <done>The combined stress + behavior-regression tests exist and pass; the automatable-gate rationale (no new wire surface) is established. THE PHASE GATE: run `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... ./world/... ./save/... -count=10` and confirm it is GREEN before writing the SUMMARY.</done>
</task>

</tasks>

<verification>
- `go test ./server/... ./world/... ./save/... -count=1` passes (combined stress + behavior regression + all prior tests).
- **THE PHASE GATE (OPT-06, non-negotiable):** `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... ./world/... ./save/... -count=10` is GREEN — every async subsystem -race clean by construction.
- The existing Phase 5/6/7 capture-diff goldens (TestPlayBytesVsVanillaCapture, the entity capture tests) still pass — no wire surface changed; the real-client behavior still works.
- `go vet ./... && go build ./...` clean under CGO_ENABLED=0; no new external deps.
</verification>

<success_criteria>
- OPT-06: the server passes -race clean on EVERY async subsystem — pathfinding, tracker, spawner — proven by the combined Docker -race -count=10 gate over ./server/... ./world/... ./save/...
- A behavior-regression check proves the optimizations are additive (paths still arrive 1+ tick late, the tracker still sends spawn/move/despawn, mobs still spawn, .linear round-trips) — the WHAT is unchanged, only the WHEN shifted off-tick.
- The phase has no new wire surface, so the gate is automatable (not a human-verify / capture-diff); the existing capture-diff goldens stay green; Phase 8 closes as additive optimization, not a rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/08-leaf-concurrency-optimizations/08-07-SUMMARY.md` — and document the phase-level result: each OPT-01..06 swap behind its named seam, the rejoin-validates-existence discipline, the per-use xsync justification, and the green -race gate.
</output>
</content>
