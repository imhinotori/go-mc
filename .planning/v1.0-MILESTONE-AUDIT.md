# Milestone v1.0 — Audit

**Date:** 2026-06-24
**Status:** `passed`
**Scope:** All 9 phases (GEN, NET, TICK, WORLD, PLAY, ENT, AI, CMD, OPT) + PARITY-01 (Phase 9 stretch).

## Verdict: PASS

The milestone achieved its definition of done. Requirements coverage is complete, all
cross-phase seams resolve to wired end-to-end flows, build/test/-race are green.

## 1. Requirements coverage

| Group | IDs | Status |
|-------|-----|--------|
| Foundation/Codegen | GEN-01..04 | ✅ 4/4 |
| Net & Protocol | NET-01..07 | ✅ 7/7 |
| Tick Loop | TICK-01..06 | ✅ 6/6 |
| World & Chunks | WORLD-01..05 | ✅ 5/5 |
| Player Session | PLAY-01..06 | ✅ 6/6 |
| Entities/Physics/Interaction | ENT-01..06 | ✅ 6/6 |
| AI & Pathfinding | AI-01..03 | ✅ 3/3 |
| Commands & Chat | CMD-01..02 | ✅ 2/2 |
| Leaf Async | OPT-01..06 | ✅ 6/6 |
| **v1 total** | **45** | **✅ 45/45 (100%)** |
| Stretch (Phase 9) | PARITY-01 | ✅ Done (visual gate approved) |

Deferred to v2 (tracked, NOT in v1 scope): ONLINE-01, ONLINE-02, REGION-01.

## 2. Cross-phase integration (gsd-integration-checker, evidence-based read of the live source)

**All seams CONNECTED. No orphaned seam, no stub in a critical path, no disconnected flow.**

- **applyAsyncResults seam** — CONNECTED. Drains BOTH `asyncIn` (Phase-4 chunkReady, `server/tick_phases.go:67-76` via the SetWorld adapter `tick.go:521-535`) and `asyncIn2` (Phase-8 path/tracker/spawn, `tick_phases.go:78-92`) on the owner goroutine in the unchanged pipeline slot. Submit sites live: `navigation.go:132-137`, `tracker.go:171-181`, `spawner.go:306-316`. Pipeline order matches TICK-01 (`tick_phases.go:32-41`, asserted by `tick_test.go`).
- **tracker.Tick() swap** — CONNECTED, no ambiguity. Single swap-point `tick.go:474` (`t.tracker = &asyncTracker{...}`); synchronous `entityTracker` retained ONLY as the golden reference (`TestAsyncTrackerMatchesSync`), never assigned.
- **Generator seam** — CONNECTED. `cmd/sulfur/main.go:145-158` wires `world.NewNoiseGenerator` (Phase 9) into the Phase-4 worker; `SULFUR_SUPERFLAT=1` is the documented fallback. Compile-time assertion `_ Generator = (*NoiseGenerator)(nil)` (`world/noisegen.go:296`).
- **E2E join flow** — CONNECTED at every handoff: handshake/login/config/play (`server/server.go:72-124`) → Play bootstrap (`gameplay_tick.go:143-271`, `play_join.go:480-502`) → first chunks (`tick_phases.go:123-137,268-315`) → movement gated on teleport-id (`tick.go:766-817`) → entities/mobs (`tick_phases.go:188-211`, asyncTracker `tracker.go:137-183`).
- **Worldgen wired** — `Generate` runs fill→surface→carve (the surface-before-carve fidelity fix, `world/noisegen.go:139,158-159,165`); spawn-Y derives from the real generated `WorldSurface` heightmap (`main.go:151-152` → `noisegen.go:192-202`), not a flat constant; per-marker interpolation present (`world/levelgen/noisechunk/interpolator.go`).

## 3. Build / Test / Race

- `go build ./...` — green (exit 0).
- `go test ./...` — green (every package ok).
- `go test -race ./server/... ./world/... ./save/...` (Docker golang:1.26, host CGO_ENABLED=0) — **green**, all packages ok (incl. the Phase-9 fidelity-fix packages: noisechunk, density, world).

## 4. Gaps

None. The integration check traced every export → import → live call site → owner-side apply with no break.

## Post-gate note

After the Phase-9 real-client visual gate was approved, a fidelity audit vs the
unobfuscated 26.2 jar found + fixed 2 structural worldgen port divergences (per-marker
interpolation, surface-before-carve; commit `aa619b81`) — recorded in STATE.md. These
sharpened terrain to true 1:1 vanilla and are included in this milestone.
