---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/entity.go
  - server/combat_mob.go
  - server/tick_phases.go
  - server/entity_encode.go
  - server/aging_mob_test.go
autonomous: true
requirements: [MOB-SUB-08]
tags: [aging, ageable-mob, metadata, hitbox, pig-parity]

must_haves:
  truths:
    - "A baby pig (breedAge < 0) ticks its breedAge up by +1 each tick toward 0 (becomes an adult)"
    - "An adult pig on breeding cooldown (breedAge > 0) ticks breedAge down by -1 each tick toward 0"
    - "A lone un-fed adult pig (breedAge == 0) is a pure-int no-op: breedAge stays 0 every tick, zero RNG draws"
    - "isBaby() == (breedAge < 0); a baby pig's AABB is HALF-SCALE (0.45×0.45, adult×0.5) — the load-bearing hitbox the goal distSqr checks read"
    - "When breedAge crosses -1→0 (grows up), the AABB is RESTORED to adult dims AND a DATA_BABY_ID=false update is broadcast to trackers (the client re-renders full size)"
    - "The DATA_BABY_ID wire metadata reflects isBaby on spawn (a baby renders small client-side)"
    - "The aging tick runs OUTSIDE serverAiStep (in the tickMobIFrames per-mob loop) so the pig oracle RNG stream is unperturbed"
  artifacts:
    - path: "server/entity.go"
      provides: "breedAge int field + isBaby() + the baby/adult dimension toggle (setBabyDimensions/restoreAdultDimensions)"
      contains: "breedAge int"
    - path: "server/combat_mob.go"
      provides: "tickMobAging — pure-int breedAge tick + the cross-0 transition (AABB restore + DATA_BABY_ID broadcast)"
      contains: "func (t *TickLoop) tickMobAging"
    - path: "server/entity_encode.go"
      provides: "babyDataEntry + boolSerializerID + dataBabyIndex (DATA_BABY_ID BOOLEAN metadata)"
      contains: "func babyDataEntry"
    - path: "server/aging_mob_test.go"
      provides: "aging tests (baby→adult, cooldown decays, age-0 no-op, isBaby, half-scale AABB, cross-0 restore)"
      contains: "func TestAging"
  key_links:
    - from: "server/tick_phases.go"
      to: "tickMobAging"
      via: "the per-mob loop that already calls tickMobIFrames at tick_phases.go:324-326"
      pattern: "tickMobAging\\("
    - from: "server/combat_mob.go tickMobAging cross-0"
      to: "Entity.width/height AABB + DATA_BABY_ID broadcast"
      via: "breedAge -1→0 → restore adult dims + broadcast DATA_BABY_ID=false"
      pattern: "babyDataEntry|restoreAdult"
---

<objective>
Build the AgeableMob aging subsystem: a signed-int `breedAge` machine on Entity (baby <0 ticks up to 0; adult cooldown >0 ticks down to 0; ==0 = adult-ready), the RNG-free per-tick `tickMobAging` (twin of `tickMobIFrames`, running OUTSIDE serverAiStep), the **baby HALF-SCALE hitbox** (the server-side AABB shrink — a SEPARATE deliverable from the render flag, and the load-bearing input to the breed/follow distSqr checks), and the DATA_BABY_ID BOOLEAN wire metadata + its **cross-0 broadcast** so a baby that grows up re-renders full size.

Purpose: This is the foundation subsystem for breeding (Plan B feeds it inLove, Plan C/D breed using it). MOB-SUB-08 + ROADMAP SC#1 EXPLICITLY name the "baby half-scale hitbox" — it is built here, not dropped. It is the FIRST plan of the dogfood-gate phase and MUST keep `TestPluginPigEqualsGoNativePig` byte-identical: the aging tick is PURE INT (no RNG) and a lone un-fed adult has breedAge==0 (full-size, no transition), so the tick is a no-op on the oracle pig.
Output: `breedAge` + isBaby() + the baby/adult dimension toggle + `tickMobAging` (with the cross-0 transition) + DATA_BABY_ID metadata + the cross-0 broadcast + aging/AABB tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md

<oracle_gate_strategy>
THE MAKE-OR-BREAK CONSTRAINT (every plan in this phase honors it):
The oracle pig (`TestPluginPigEqualsGoNativePig`) is an un-fed lone adult: breedAge==0, inLove==0, FULL-SIZE.
For THIS plan:
- `tickMobAging` is PURE INT (breedAge += sign toward 0) — NO RNG. Adding it to the per-mob loop perturbs
  NOTHING in the per-mob RNG stream the oracle pins.
- The baby-hitbox toggle + the cross-0 broadcast fire ONLY on a baby (breedAge<0) or a -1→0 transition.
  The oracle pig is breedAge==0 from spawn → it is NEVER a baby → the toggle/broadcast never fire → no-op.
- It is STATEFUL, so the oracle pig's breedAge MUST initialize IDENTICALLY (0) on both halves. The Entity
  zero-value (breedAge 0, full-size dims from the spawn table) gives this for free on NewEntity — confirm
  no spawn path sets breedAge.
- It runs in the SAME per-mob loop as tickMobIFrames (tick_phases.go:324-326), structurally OUTSIDE
  serverAiStep's goal callbacks / navigation.tick (combat_mob.go:644-646 explains WHY).
GATE: run `TestPluginPigEqualsGoNativePig` at the end of this plan — it MUST stay green.
</oracle_gate_strategy>

<interfaces>
<!-- Verified from the codebase. Use directly, no exploration needed. -->

From server/entity.go (the COLLISION to avoid + the dimension fields the hitbox toggles):
```go
age int   // entity.go:119 — ItemEntity/XP-orb, NOT the AgeableMob breed-age. Name the new field breedAge.
// width, height are the AABB footprint, COPIED from the data/entity table at spawn (NewEntity). Plain values, snapshot-friendly.
width, height float64   // entity.go:82 — adult pig = 0.9 x 0.9 from the table
func (e *Entity) AABB() bvh.AABB[float64, bvh.Vec3[float64]] {   // entity.go:353 — feet-anchored
	hw := e.width / 2
	return bvh.AABB[...]{ Lower:{e.x-hw, e.y, e.z-hw}, Upper:{e.x+hw, e.y+e.height, e.z+hw} }
}
```

From 33-JARNOTES.md (BABY_DIMENSIONS — javap-confirmed):
```
Pig.BABY_DIMENSIONS = EntityType.PIG.getDimensions().scale(0.5f).withEyeHeight(0.40625f)
  == scalable(0.45, 0.45)   // adult 0.9x0.9 → baby = adult × 0.5
getDefaultDimensions(pose) = isBaby() ? BABY_DIMENSIONS : super.getDefaultDimensions(pose)
Eye height (baby 0.40625): our Entity has NO eye-height field → cite-defer (not load-bearing for goal distSqr).
```

From server/combat_mob.go:647-654 (the EXACT twin to mirror) + tick_phases.go:324-326 (the per-mob loop):
```go
func (t *TickLoop) tickMobIFrames(e *Entity) { if e.hurtTime > 0 { e.hurtTime-- }; if e.invulnerableTime > 0 { e.invulnerableTime-- } }
for _, e := range t.cur().entities.byID { t.tickMobIFrames(e) }   // tick_phases.go:324-326 — add tickMobAging here
```

From server/entity_encode.go (metadata builders + serializer consts + the broadcast seam):
```go
const intSerializerID int32 = 1   // :284 ; const byteSerializerID int32 = 0  // :317
func airDataEntry(air int32) entityDataEntry { return entityDataEntry{index: dataAirSupplyIndex, serializerID: intSerializerID, value: pk.VarInt(air)} }  // :290
// entityDataEntry framing (:186): Byte(index) + VarInt(serializerID) + value
func encodeSetEntityDataByID(entityID int32, entries ...entityDataEntry) pk.Packet  // :423 — the cross-0 broadcast
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: breedAge + isBaby + the baby/adult dimension toggle + tickMobAging (with the cross-0 transition)</name>
  <read_first>
    - server/entity.go:78-120 (width,height :82 + the item-`age` :119 collision) + :345-359 (AABB feet-anchored) + the per-entity int block (~:238 after deathTime)
    - server/combat_mob.go:636-654 (tickMobIFrames — the twin + the WHY-outside-serverAiStep comment)
    - server/tick_phases.go:310-340 (the per-mob loop calling tickMobIFrames at :324-326)
    - 33-JARNOTES.md "BABY half-scale HITBOX" section (BABY_DIMENSIONS scalable(0.45,0.45) = adult×0.5; getDefaultDimensions) + the "adjustedTickDelay" + the AgeableMob aging section (BABY_START_AGE -24000; age<0 baby→0; >0 cooldown→0; isBaby age<0)
    - 33-PATTERNS.md §1 (field placement) + §2 (the tick analog + insertion point)
  </read_first>
  <files>server/entity.go, server/combat_mob.go, server/tick_phases.go</files>
  <action>
    1. server/entity.go: add `breedAge int` to the per-entity int block (~:238 after deathTime). Do NOT reuse
       `age int` (:119, ItemEntity/XP-orb). Doc-comment in the i-frame field style: AgeableMob.age — `<0` baby
       (ticks toward 0), `>0` breeding cooldown (ticks toward 0), `==0` adult-ready. Tick-owned plain int, zero
       for non-animals, snapshot-friendly (TICK-05). Cite AgeableMob + BABY_START_AGE -24000.
    2. Add `func (e *Entity) isBaby() bool { return e.breedAge < 0 }` (AgeableMob.isBaby).
    3. THE BABY HITBOX (the BLOCKER fix — port Pig.getDefaultDimensions + BABY_DIMENSIONS faithfully):
       - Add a baby-scale constant: `const babyDimensionScale = 0.5` (Pig.BABY_DIMENSIONS = adult.scale(0.5f);
         derive the baby dims as adult × scale so non-pig mobs (Phase 34) reuse the scale, NOT a hardcoded 0.45).
       - Add `func (e *Entity) refreshDimensions()` (port getDefaultDimensions): if e.isBaby(), set
         e.width,e.height to the ADULT (spawn-table) dims × babyDimensionScale; else set them to the adult dims.
         To know the adult dims, capture them at spawn: add an `adultWidth, adultHeight float64` pair (or a
         `spawnWidth/spawnHeight`) set in NewEntity from the data-table width/height, so refreshDimensions has
         the un-scaled source. (Alternative if cleaner: store only the scaled current dims + recompute from the
         entity-type table — but the captured-adult-dims approach avoids a re-lookup. Pick one; cite it.)
       - Call refreshDimensions() whenever breedAge is set/changes (see Task: the cross-0 transition below, and
         Plan C's breed() child-spawn at breedAge=-24000 will call it too).
       Cite Pig.BABY_DIMENSIONS + getDefaultDimensions (33-JARNOTES.md). Eye height (baby 0.40625): our Entity
       has no eye-height field — leave a `// eye-height baby 0.40625 cite-deferred (no eye-height field; not
       load-bearing for goal distSqr — Pig.BABY_DIMENSIONS.withEyeHeight)` note (Plan D rolls it into 33-deviations.md).
    4. server/combat_mob.go: add `func (t *TickLoop) tickMobAging(e *Entity)` after tickMobIFrames (~:654).
       Body — port AgeableMob per-tick ageUp (PURE INT) WITH the cross-0 transition:
       ```
       if e.breedAge < 0 {
           e.breedAge++
           if e.breedAge == 0 { t.onGrewUp(e) }   // -1 → 0 transition: restore adult dims + broadcast DATA_BABY_ID=false
       } else if e.breedAge > 0 {
           e.breedAge--
       }
       ```
       Add `func (t *TickLoop) onGrewUp(e *Entity)` that calls `e.refreshDimensions()` (restore adult AABB) and
       the DATA_BABY_ID=false broadcast (the broadcast body is wired in Task 2 — stub the call now: `t.broadcastBabyFlag(e)`).
       Doc-cite AgeableMob.aging + that this is PURE INT (the broadcast is a tracker fan-out, not an RNG draw) and
       runs OUTSIDE serverAiStep (the per-mob loop) so the oracle stream is preserved.
    5. server/tick_phases.go: in the per-mob loop at :324-326 (after `t.tickMobIFrames(e)`), add `t.tickMobAging(e)`.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "breedAge int" server/entity.go` returns 1; `grep -c "func (e \*Entity) isBaby\|func (e \*Entity) refreshDimensions" server/entity.go` returns 2.
    - `grep -c "babyDimensionScale" server/entity.go` returns >= 1 (derived scale, not a hardcoded 0.45).
    - `grep -c "func (t \*TickLoop) tickMobAging\|func (t \*TickLoop) onGrewUp" server/combat_mob.go` returns 2.
    - `grep -v '^[[:space:]]*//' server/tick_phases.go | grep -c "t.tickMobAging(e)"` returns 1.
    - `CGO_ENABLED=0 go build ./server/` exits 0 (broadcastBabyFlag stub resolves in Task 2 — Task 1+2 compile together; the executor may do Task 2 first).
  </acceptance_criteria>
  <done>breedAge + isBaby() + refreshDimensions (baby half-scale hitbox) + tickMobAging (with the -1→0 onGrewUp transition) exist, wired into the OUTSIDE-serverAiStep loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DATA_BABY_ID BOOLEAN metadata + the spawn-time carry + the cross-0 broadcast</name>
  <read_first>
    - server/entity_encode.go:180-360 (entityDataEntry framing :186-212; itemDataEntry :250; airDataEntry :290 + intSerializerID :284; livingEntityFlagsEntry :331 + byteSerializerID :317; player-customisation :350-360; encodeSetEntityDataByID :423) + the spawn-metadata carry site (grep airDataEntry/livingEntityFlagsEntry usage)
    - 33-JARNOTES.md:119-130 (DATA_BABY_ID accessor index 16 BOOLEAN — javap-confirm) + the BABY_DIMENSIONS section
    - 33-PATTERNS.md §3 (the BOOLEAN serializer gap + the index walk + babyDataEntry)
  </read_first>
  <files>server/entity_encode.go, server/combat_mob.go</files>
  <action>
    1. JAVAP-CONFIRM FIRST (do NOT guess):
       - `javap -c -p net.minecraft.network.syncher.EntityDataSerializers` — the registerSerializer order pins
         BOOLEAN's id (documented order at :237-242 → expect 8; VERIFY).
       - `javap -c -p net.minecraft.world.entity.Mob` + `net.minecraft.world.entity.AgeableMob` — walk the
         defineId hierarchy like the Avatar=16 derivation at :339-347 → DATA_BABY_ID = index 16 (VERIFY).
       - Confirm `pk.Boolean` exists in net/packet (BOOLEAN codec = single byte 0/1, ByteBufCodecs.BOOL); if
         absent use `pk.UnsignedByte(0|1)` — cite which.
    2. Add `const boolSerializerID int32 = <verified, expect 8>` + `const dataBabyIndex int32 = <verified, expect 16>`
       near :284/:317, each citing its javap source.
    3. Add the builder after :360, mirroring airDataEntry:
       `func babyDataEntry(isBaby bool) entityDataEntry { return entityDataEntry{index: dataBabyIndex, serializerID: boolSerializerID, value: pk.Boolean(isBaby)} }`
       (substitute the verified codec). Doc-comment: DATA_BABY_ID — Byte(16) + VarInt(boolSerializerID) + Boolean(isBaby);
       the client renders a small pig when true. Cite AgeableMob.DATA_BABY_ID.
    4. SPAWN-TIME CARRY: at the mob-spawn metadata site, include `babyDataEntry(e.isBaby())` so a spawned baby
       renders small (follow the airDataEntry/livingEntityFlagsEntry carry pattern; if metadata rides Entity.metadata,
       follow playerSkinMetadata :366-373; else push via encodeSetEntityDataByID at spawn).
    5. THE CROSS-0 BROADCAST (Warning 3 — firm owner): implement `func (t *TickLoop) broadcastBabyFlag(e *Entity)`
       in server/combat_mob.go (the stub onGrewUp called in Task 1): build `encodeSetEntityDataByID(e.id, babyDataEntry(e.isBaby()))`
       and broadcast it to e's trackers using the SAME tracker fan-out combat_mob uses for hurt/death sounds/metadata
       (grep the existing SetEntityData/sound broadcast in combat_mob for the fan-out helper). This pushes DATA_BABY_ID=false
       when a baby grows up (-1→0 via onGrewUp), so the client re-renders full size — the Plan-D "grows to full size"
       live criterion depends on it. (It also serves the breed() child-spawn baby→small path: Plan C can reuse
       broadcastBabyFlag right after setting child.breedAge=-24000 to push DATA_BABY_ID=true.)
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func babyDataEntry" server/entity_encode.go` returns 1; `grep -c "func (t \*TickLoop) broadcastBabyFlag" server/combat_mob.go` returns 1.
    - `grep -c "boolSerializerID\|dataBabyIndex" server/entity_encode.go` returns >= 3 (2 consts + >=1 use).
    - The const doc comments cite the javap-confirmed EntityDataSerializers order + the AgeableMob defineId index.
    - `grep -c "babyDataEntry(e.isBaby())" server/*.go` returns >= 1 (spawn carry or broadcast uses the live flag).
    - `CGO_ENABLED=0 go build ./server/` exits 0.
  </acceptance_criteria>
  <done>babyDataEntry + boolSerializerID + dataBabyIndex (javap-confirmed) exist; the spawn-time carry + the cross-0 broadcastBabyFlag (firm owner) are wired; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aging + half-scale-AABB + cross-0 tests + the oracle gate</name>
  <read_first>
    - server/combat_mob_test.go:240-270 (the tickMobIFrames test pattern)
    - server/entity.go:353 (AABB — assert width=0.45 → Upper.X-Lower.X == 0.45 for a baby)
    - server/plugin_pig_test.go (spawnVanillaPig usage + TestPluginPigEqualsGoNativePig :251)
    - server/vanilla_pig.go:36 (spawnVanillaPig)
  </read_first>
  <files>server/aging_mob_test.go</files>
  <action>
    Create server/aging_mob_test.go:
    1. `TestAgingBabyTicksToAdult`: set breedAge=-3, call refreshDimensions (or spawn as baby), call tickMobAging
       3×, assert breedAge==0 and isBaby()==false; assert isBaby()==true while <0.
    2. `TestAgingCooldownDecays`: breedAge=+3, tickMobAging 3×, assert breedAge==0 (never below).
    3. `TestAgingAdultIsNoOp`: breedAge=0, tickMobAging 10×, assert breedAge stays 0 (the oracle's lone-adult no-op).
    4. `TestBabyHitboxHalfScale` (the BLOCKER coverage): spawn a pig, set breedAge=-100 + refreshDimensions,
       assert the AABB is half-scale — e.g. for the pig `(e.AABB().Upper.X - e.AABB().Lower.X) ≈ 0.45` (adult 0.9 × 0.5)
       and the height span ≈ 0.45; then assert an ADULT pig (breedAge=0) AABB span ≈ 0.9 (full). Use an epsilon compare.
    5. `TestGrowUpRestoresHitbox` (the cross-0 coverage): set breedAge=-1 + refreshDimensions (half-scale), call
       tickMobAging once → breedAge==0, assert the AABB restored to full (0.9 span) — proving onGrewUp ran
       refreshDimensions. (The broadcast itself is exercised at the integration level in Plan D; here assert the dims.)
    Use the existing harness (newTestTickLoop / spawnVanillaPig — match combat_mob_test.go).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestAging|TestBabyHitbox|TestGrowUp|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestPigGoalSet' -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go vet ./server/` exits 0.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestAging|TestBabyHitbox|TestGrowUp' -v` shows all aging + the half-scale-AABB + the cross-0-restore tests PASS.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -timeout 120s` PASSES (THE GATE — the oracle pig is a full-size adult; aging is a pure-int no-op; the hitbox toggle/broadcast never fire → byte-identical).
  </acceptance_criteria>
  <done>Aging + half-scale-AABB + cross-0-restore tests pass AND the pig oracle stays byte-identical green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server tick → client (metadata wire) | DATA_BABY_ID metadata + the cross-0 update are server-authored and broadcast; client only renders. No untrusted input inbound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | tickMobAging (+ the hitbox toggle/broadcast) perturbing the oracle RNG stream | mitigate | tickMobAging is PURE INT (no RNG); the broadcast is a tracker fan-out, not a draw; both fire only on a baby/transition (the oracle is a full-size adult). Runs OUTSIDE serverAiStep. Gated by TestPluginPigEqualsGoNativePig (Task 3). |
| T-33-02 | Information disclosure | DATA_BABY_ID metadata leaking server state | accept | The baby flag is intentionally public (the client renders it); no PII, vanilla-faithful wire. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0; `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestAging|TestBabyHitbox|TestGrowUp|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestPigGoalSet|TestVanillaPig' -timeout 240s -v` all PASS.
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/` clean.
- gopls is STALE — trust the compiler.
</verification>

<success_criteria>
- breedAge int (distinct from item `age`) + isBaby() + tickMobAging (pure-int, OUTSIDE serverAiStep) exist and are wired.
- The baby HALF-SCALE hitbox (refreshDimensions, adult×0.5 = 0.45 for the pig) is built — the load-bearing AABB the breed/follow distSqr checks read (MOB-SUB-08 + SC#1).
- The -1→0 grow-up transition restores the adult AABB AND broadcasts DATA_BABY_ID=false (the client re-renders full size).
- DATA_BABY_ID BOOLEAN metadata (javap-confirmed) renders a small pig on spawn.
- Aging + half-scale-AABB + cross-0 tests pass; the pig oracle stays byte-identical (the gate); CGO=0 clean; no new Go deps; Docker -race clean.
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md`
</output>
