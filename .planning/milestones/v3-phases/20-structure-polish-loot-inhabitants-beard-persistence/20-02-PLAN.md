---
phase: 20-structure-polish-loot-inhabitants-beard-persistence
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - server/block_drop.go
  - server/chest_loot.go
  - server/chest_loot_test.go
  - world/structure/piece.go
autonomous: true
requirements: [STRUCT-POLISH-01]

must_haves:
  truths:
    - "Block break drops are produced by the SHARED level/loot evaluator (not the hardcoded blockDropTable map)"
    - "A generated structure chest stores {LootTable, LootTableSeed} in its block-entity NBT and rolls LAZILY on first open (vanilla unpackLootTable), not at gen-time"
    - "createChest draws the seed via the PIECE's RandomSource.nextLong() so chest contents are deterministic per (worldseed, chunk)"
    - "The block-table delta (alternatives, match_tool, survives_explosion, apply_bonus, explosion_decay) is ported for the faithful GAMEPLAY-06 path"
  artifacts:
    - path: "server/block_drop.go"
      provides: "blockDropFor rewired onto level/loot.Roll over the embedded block table"
      contains: "loot\\."
    - path: "server/chest_loot.go"
      provides: "lazy unpackLootTable on chest open + the chest BE roll"
      contains: "unpackLootTable|LootTableSeed"
    - path: "world/structure/piece.go"
      provides: "createChest emits a chest BlockEntity carrying {LootTable, LootTableSeed}"
      contains: "NextLong"
  key_links:
    - from: "server/block_drop.go"
      to: "level/loot.Roll"
      via: "evaluator replaces blockDropTable map"
      pattern: "loot\\.Roll"
    - from: "world/structure/piece.go createChest"
      to: "chest BlockEntity NBT"
      via: "rng.NextLong() -> {LootTable, LootTableSeed}"
      pattern: "LootTableSeed"
    - from: "server/chest_loot.go"
      to: "level/loot.Roll"
      via: "lazy roll on first open"
      pattern: "loot\\.Roll"
---

<objective>
Wire the shared evaluator (20-01's `level/loot`) into the two consumers that make it the SINGLE loot evaluator: (1) REPLACE `server/block_drop.go`'s hardcoded `blockDropTable` map with `loot.Roll` over the embedded block table, and (2) make `world/structure/piece.go` `createChest` write a real chest BlockEntity carrying `{LootTable, LootTableSeed}` (seed drawn from the PIECE's RandomSource.nextLong() at gen, matching vanilla `setLootTable`), then roll LAZILY on first open via an `unpackLootTable` analogue in a tick-side chest-open path. This is the second half of STRUCT-POLISH-01.

Purpose: faithful per-seed chest loot + faithful block drops, both through one evaluator (the research's "shared evaluator" mandate; block_drop.go Assumption A1 explicitly flags its map as "superseded by STRUCT-POLISH-01's loot evaluator"). Vanilla rolls chests LAZILY (Pitfall 2) — store table+seed, roll on open, never at gen-time.

Output: block drops + structure chests both produce vanilla loot; the v1 blockDropTable map is gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-RESEARCH.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-01-SUMMARY.md
@CLAUDE.md

<read_first>
- .planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-01-SUMMARY.md — the exact level/loot.Roll API surface (do NOT re-explore the package)
- temp/cache/26.2-inner.jar (decompile before writing):
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.block.entity.RandomizableContainerBlockEntity` (setLootTable / unpackLootTable / getItem seam)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.StructurePiece` (createChest -> setLootTable(key, random.nextLong()))
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.storage.loot.functions.ApplyBonusCount` + `ApplyExplosionDecay` (the fortune/explosion block functions)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.storage.loot.predicates.MatchTool` + `LootItemRandomChanceCondition` (survives_explosion family)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.storage.loot.entries.AlternativesEntry` (first-passing child wins)
- server/block_drop.go — the map to replace (blockDropFor / blockDropTable); spawnBlockDrop already runs on the tick (TICK-05 safe) and uses component.SlotData
- world/structure/piece.go:721 createChest + the LootChest record (X,Y,Z,LootTable) — EXTEND to draw nextLong() + emit a chest BlockEntity
- level/chunk.go BlockEntity (Data dynbt) + level/component/containerloot_gen.go (the minecraft:container_loot component) + level/block/blockentities.go — the chest BE carrier
- temp/cache/26.2-datagen/.../loot_table/blocks/{stone,grass_block,oak_log,diamond_ore}.json — the block tables (verify the embedded tree from 20-01 includes blocks/)
</read_first>

<interfaces>
<!-- From 20-01-SUMMARY.md (authoritative). Re-confirm exact names there. -->
  loot.Roll(table *loot.LootTable, seed int64, ctx *loot.LootContext) []component.SlotData
  loot.LoadTable(id string) (*loot.LootTable, error)  // resolves "minecraft:blocks/stone" / "minecraft:chests/simple_dungeon" from the embed
  loot.NewContext(rng levelgen.RandomSource /*, luck */) *loot.LootContext

From server/block_drop.go (current, to be rewired):
  func blockDropFor(broken block.StateID) (component.SlotData, bool)   // returns the v1 map drop
  func (t *TickLoop) spawnBlockDrop(p *tickPlayer, pos pk.Position, brokenState block.StateID)  // tick-owned

From world/structure/piece.go (current):
  func (p *StructurePiece) createChest(view WorldGenView, box BoundingBox, x,y,z int, lootTable string, lootChests *[]LootChest) bool
  type LootChest struct { X,Y,Z int; LootTable string }   // EXTEND with LootTableSeed; emit a real BlockEntity
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire block drops onto the shared evaluator + port the block-table delta</name>
  <files>server/block_drop.go</files>
  <read_first>
    - 20-01-SUMMARY.md (the Roll/LoadTable/NewContext API)
    - javap ApplyBonusCount, ApplyExplosionDecay, MatchTool, LootItemRandomChanceCondition, AlternativesEntry
    - server/block_drop.go (the existing blockDropFor + spawnBlockDrop tick seam)
  </read_first>
  <behavior>
    - Test (TestBlockDropViaLoot): breaking stone yields cobblestone VIA loot.Roll over minecraft:blocks/stone (not the map); grass_block -> dirt; oak_log -> oak_log
    - Test: a block with a `survives_explosion`/`match_tool`-gated table behaves per the condition (alternatives first-passing child wins)
    - Test: air/unknown still drops nothing (no-op)
  </behavior>
  <action>
    REPLACE blockDropFor's `blockDropTable` map lookup with `loot.Roll(loot.LoadTable("minecraft:blocks/"+name), seed, ctx)` where name = Block.ID() sans namespace and seed is the block-break RNG (the existing math/rand/v2 path — keep the draw-order documented). DELETE blockDropTable + the old map (it is superseded; CLAUDE.md says never leave a placeholder masquerading as the real thing).
    Port the block-table delta IN level/loot (data files added there) but the CONDITION/FUNCTION bodies the block tables need — AlternativesEntry (first child whose conditions pass), MatchTool (silk-touch/tool predicate — stub the enchantment/tool read behind a CITED vanilla-default constant if the tool subsystem is incomplete, structured to become a real read), survives_explosion (LootItemRandomChanceCondition family), ApplyBonusCount (fortune ore formula), ApplyExplosionDecay — must be implemented so blocks/*.json evaluate. If a block function/condition is genuinely unbuildable without a not-yet-ported subsystem, stub behind a cited constant equal to the vanilla default (never bake away). spawnBlockDrop stays tick-owned; if Roll returns multiple stacks, spawn one Item entity per stack (popResource per drop) preserving the existing jitter/toss draw order.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ ./level/loot/ && go vet ./server/ && go test ./server/ -run TestBlockDropViaLoot</automated>
  </verify>
  <done>Block drops route through loot.Roll; blockDropTable is deleted; stone->cobblestone via the table; TestBlockDropViaLoot green; CGO=0 build clean. Never gopls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createChest writes a {LootTable, LootTableSeed} BlockEntity (gen-time seed draw)</name>
  <files>world/structure/piece.go</files>
  <read_first>
    - javap StructurePiece.createChest (setLootTable(key, random.nextLong())) + RandomizableContainerBlockEntity.setLootTable
    - world/structure/piece.go:721 createChest + the LootChest record
    - level/chunk.go BlockEntity (Data) + level/component/containerloot_gen.go
  </read_first>
  <behavior>
    - Test (TestCreateChestEmitsBE): createChest draws rng.NextLong() ONCE per chest and emits a chest BlockEntity whose NBT carries LootTable (id) + LootTableSeed (the drawn long)
    - Test: the seed is deterministic per (worldseed, chunk) — same piece RNG state -> same LootTableSeed
    - Test: NO item list is written at gen-time (Pitfall 2 — lazy roll; the BE holds only table+seed)
  </behavior>
  <action>
    EXTEND createChest (piece.go:721) per StructurePiece.createChest: after placing the chest block, draw `seed := rng.NextLong()` on the PIECE's RandomSource (already threaded into PostProcess — the determinism keystone, Pitfall 1) and emit a chest `level.BlockEntity` into the chunk via WorldGenView carrying the NBT `{LootTable: lootTable, LootTableSeed: seed}` (the minecraft:container_loot component / RandomizableContainerBlockEntity keys — cite the exact NBT key names from javap). Extend LootChest with LootTableSeed (or replace it with the real BE — keep whichever the village/temple callers already consume; minimize churn). Do NOT roll loot here (lazy). Confirm WorldGenView can write a BlockEntity; if the off-tick WorldGenView lacks a SetBlockEntity method, ADD it (the chunk already carries a BlockEntity list) — keep it pure (off-tick worker writes into the immutable-on-emit chunk, same discipline as block writes).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/... && go vet ./world/structure/ && go test ./world/structure/ -run TestCreateChestEmitsBE</automated>
  </verify>
  <done>createChest emits a chest BE with {LootTable, LootTableSeed}; the seed is the piece-RNG nextLong(); no items at gen; test green; CGO=0 build clean. Never gopls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lazy roll on chest open (unpackLootTable) — the tick-side container path</name>
  <files>server/chest_loot.go, server/chest_loot_test.go</files>
  <read_first>
    - javap RandomizableContainerBlockEntity.unpackLootTable / getItem (the lazy-roll seam)
    - 20-01-SUMMARY.md (Roll/NewContext API)
    - the existing chest-open / container-menu path (ENT-04 InventoryMenu) — find where a block-entity container is opened (Open Question 4: this open path may be net-new)
  </read_first>
  <behavior>
    - Test (TestChestLazyRoll): opening a chest BE that carries {LootTable, LootTableSeed} fills it via loot.Roll(table, LootTableSeed, ctx) and CLEARS the table (so a re-open does not re-roll)
    - Test: the rolled contents match the seed-reproduction golden for that table+seed (ties to 20-01's TestLootSeedReproduces)
    - Test: a chest with no LootTable (plain/older chest) opens empty (no panic) — backward tolerant
  </behavior>
  <action>
    Port unpackLootTable: on the first container access of a chest BE that has LootTable != "", build a LootContext (LegacyRandomSource(LootTableSeed) via loot.NewContext), call loot.Roll(table, LootTableSeed, ctx), place the rolled stacks into the container slots, then CLEAR LootTable (one-shot). This runs TICK-side (the container-open path is tick-owned).
    FIRST, during read_first, DETERMINE whether Sulfur has a block-entity chest-open path today: the ENT-04 InventoryMenu exists, but the path that OPENS a *block-entity container* (UseItemOn a chest block -> open a container menu backed by the chest BE) may be net-new (Open Question 4, RESOLVED: this plan owns it). 
    CASE A — the open seam EXISTS: hang unpackLootTable on it (resolve the chest BE on open, roll, clear), nothing more.
    CASE B — the open seam is NET-NEW (no block-entity-container open path): wire the MINIMAL seam only — when a player UseItemOn a chest block, resolve the chest BE, unpackLootTable, and present its contents through the EXISTING ENT-04 container menu. The EXPLICIT FALLBACK CEILING is exactly this menu hookup; do NOT build a general container/interaction system here. 
    W2 SPLIT NOTE: if Case B's open-seam wiring is larger than a menu hookup (e.g. it needs new block-interaction dispatch, slot sync, or a windowId allocator that does not exist), STOP and flag it to the orchestrator to SPLIT the chest-open seam into its own plan — do NOT let this task silently balloon past the unpackLootTable + menu-present scope. Keep the roll on the tick (no off-tick mutation) in every case.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ && go test ./server/ -run TestChestLazyRoll</automated>
  </verify>
  <done>Opening a generated chest rolls loot once (lazily) and matches the golden for table+seed; re-open does not re-roll; an empty/older chest is tolerated; CGO=0 build clean. Never gopls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chest BE NBT (on-disk) -> unpackLootTable | A persisted chest may carry an absent/garbled LootTable; must open empty, never panic |
| player block-interact -> chest open | Untrusted client triggers the open; the roll is server-authoritative (seed is server-stored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T (Tampering) | chest_loot.go unpackLootTable | mitigate | Tolerate absent/garbled LootTable -> open empty (no trust-and-crash); the seed is server-stored, never client-supplied |
| T-20-05 | E (Elevation) | server-authoritative roll | mitigate | The roll uses the SERVER-stored LootTableSeed; a client cannot influence contents (no client-supplied seed) |
| T-20-06 | D (DoS) | block_drop multi-stack spawn | accept | A block table yields a bounded stack list; the existing entity-store add is tick-bounded |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./server/ ./world/... ./level/loot/` clean (NEVER gopls).
- `go vet ./server/ ./world/structure/` clean.
- `go test ./server/ -run 'TestBlockDropViaLoot|TestChestLazyRoll'` + `go test ./world/structure/ -run TestCreateChestEmitsBE` green.
- blockDropTable is DELETED (grep -c 'blockDropTable' server/block_drop.go == 0, excluding comments: `grep -v '^//' server/block_drop.go | grep -c blockDropTable` == 0).
- Docker `-race` over ./server/ ./world/structure/ green (the lazy roll runs on the tick — TICK-05 safe).
- CAPTURE-DIFF FLAG: this plan adds chest BlockEntities to chunks, touching the chunk-wire BlockEntity list. Re-run the worldgen capture-diff goldens; if the BlockEntity-list framing changed, RE-SEAL the goldens (document the re-seal in the SUMMARY). If unchanged, note the goldens stay green.
</verification>

<success_criteria>
- Block drops and structure chests BOTH produce vanilla loot through the single level/loot evaluator.
- Chests store table+seed and roll lazily on first open (matching vanilla); the seed is the piece-RNG nextLong().
- The v1 blockDropTable map is gone. The block-table delta (alternatives/match_tool/survives_explosion/apply_bonus/explosion_decay) is ported.
- Capture-diff goldens addressed (re-sealed or confirmed green).
</success_criteria>

<output>
After completion, create `.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-02-SUMMARY.md`.
Stage files BY NAME. Commit message: `feat(20): shared evaluator wired to block drops + lazy chest loot (STRUCT-POLISH-01)` — NO Co-Authored-By / NO Claude attribution. Push: development.
Record in the SUMMARY: whether the capture-diff goldens needed a re-seal (and why), and the final chest-BE NBT key names used.
</output>
