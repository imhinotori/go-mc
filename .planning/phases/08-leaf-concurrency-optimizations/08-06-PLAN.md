---
phase: 08-leaf-concurrency-optimizations
plan: 06
type: execute
wave: 4
depends_on: [02, 04, 05]
files_modified:
  - server/async.go
  - server/collections_audit.go
  - server/collections_audit_test.go
autonomous: true
requirements: [OPT-04, OPT-06]
user_setup: []

must_haves:
  truths:
    - "A documented contention audit (server/collections_audit.go header + the SUMMARY) records, for EVERY tick-owned collection (entityStore.byID, entityStore.buckets, ChunkManager.columns, clientIndex, players, p.tracked), whether it is read by an OPT-01/02/03 worker LIVE or only via an owner-built snapshot — and concludes SNAPSHOT-AND-STAY-PLAIN as the default: because OPT-01 (snapshotRegion), OPT-02 (position+tracked copy), and OPT-03 (solidity snapshot) all COPY their input ON the owner before submitting, NO worker reads a live tick-owned collection, so EVERY one of these collections stays a PLAIN map (single-owner = faster — 08-RESEARCH Pitfall 1 / Open Question 1). The audit JUSTIFIES each plain-vs-xsync decision with its contention rationale"
    - "xsync/v4 is introduced ONLY where a design genuinely forces a cross-async-boundary read of a LIVE collection — and the audit confirms the candidate: the entity-ID space is the one place the async boundary is genuinely crossed (idAlloc is already an atomic.Int32 read off-tick by the accept goroutine AND on-tick by spawning). The audit either (a) confirms idAlloc's atomic is already the correct lock-free primitive (no xsync needed — an atomic counter IS the lock-free variant), OR (b) if 01-05 revealed an actual live shared map, converts THAT ONE collection to xsync.Map with a contention rationale. The default outcome is: NO blanket xsync; the audit names the single justified use (or documents that the atomic already satisfies OPT-04)"
    - "At least ONE concrete xsync/v4 usage is demonstrated and tested to satisfy OPT-04's literal text ('hot-path collections use lock-free/specialized variants (xsync/v4)') — either the genuinely-contended collection identified above, OR (if the snapshot discipline leaves none) a documented, justified xsync.Counter for a metric BOTH a pool worker and the tick touch (e.g. an async-work-submitted counter), proving the xsync/v4 dependency is exercised, not just declared. The choice is justified by contention, NOT 'xsync everything'"
    - "The ants/v2 pools (one per async subsystem: pathPool, trackerPool, spawnPool) are confirmed as the OPT-04 'worker pools use ants/v2' deliverable — already wired in 08-01 and used by 02/04/05 — and the audit records their sizing rationale (pathfinding ~NumCPU, tracker/spawner small) and their non-blocking drop-on-overload discipline as the bounded-pool guarantee"
    - "The audit is ENFORCED by a test/grep gate: a test asserts no OPT pool closure captures a live tick-owned collection (e.g. a documented invariant + a grep gate that no `t.entities`/`t.world`/`p.tracked` is captured by reference inside a Submit closure — the snapshot discipline), so a future change that reintroduces a live cross-boundary read fails the gate (08-RESEARCH Pitfall 1 warning sign)"
  artifacts:
    - path: "server/collections_audit.go"
      provides: "the documented per-collection contention audit (plain-vs-xsync with rationale) + the single justified xsync/v4 usage (the contended collection or a justified xsync.Counter) + the ants-pool sizing rationale"
      contains: "xsync"
    - path: "server/collections_audit_test.go"
      provides: "a test exercising the chosen xsync/v4 type (round-trip / concurrent access under -race) + the no-live-capture enforcement gate"
      contains: "TestHotCollections"
  key_links:
    - from: "server/collections_audit.go"
      to: "github.com/puzpuzpuz/xsync/v4"
      via: "the ONE justified xsync/v4 usage (contended collection or xsync.Counter) — exercising the dep"
      pattern: "xsync."
    - from: "server/collections_audit.go (the audit)"
      to: "entityStore.byID / ChunkManager.columns / p.tracked (all PLAIN)"
      via: "the snapshot-and-stay-plain conclusion: no worker reads them live, so they stay plain maps"
      pattern: "snapshot-and-stay-plain"
    - from: "server/collections_audit_test.go (the gate)"
      to: "the OPT pool closures (no live tick-owned collection captured)"
      via: "a grep/test invariant enforcing the snapshot discipline"
      pattern: "Submit"
---

<objective>
OPT-04: finalize the hot-path collection strategy with a CONTENTION AUDIT — the deliberate "decide per-collection by contention, not xsync-everything" plan. This wave runs AFTER OPT-01/02/03 (08-02/04/05) have landed and REVEALED the real contention points. The audit's load-bearing finding, grounded in those three swaps: because every OPT snapshots its input ON the owner before submitting (OPT-01 `snapshotRegion`, OPT-02 the position+tracked copy, OPT-03 the solidity snapshot), NO pool worker ever reads a live tick-owned collection — so `entityStore.byID`, `entityStore.buckets`, `ChunkManager.columns`, `clientIndex`, `players`, and `p.tracked` ALL stay PLAIN maps (single-owner = faster). This is the honest 08-RESEARCH Open Question 1 resolution: snapshot-and-stay-plain is the default; xsync is introduced ONLY where a design genuinely forces a live cross-boundary read.

The audit then satisfies OPT-04's literal requirement ("hot-path collections use lock-free/specialized variants (xsync/v4); worker pools use ants/v2") concretely: (1) confirm the ants/v2 pools (pathPool/trackerPool/spawnPool from 08-01) ARE the "worker pools use ants/v2" deliverable, with their sizing + non-blocking rationale documented; (2) introduce ONE justified xsync/v4 usage — the single genuinely-contended primitive the async boundary crosses (or, if the snapshot discipline leaves no contended map, a justified `xsync.Counter` that both a pool worker and the tick touch), so the dependency is EXERCISED with a contention rationale, never blanket-applied. (3) ENFORCE the snapshot discipline with a gate so no future change reintroduces a live cross-boundary read.

Purpose: OPT-04 ("hot-path collections use lock-free/specialized variants (xsync/v4); worker pools use ants/v2") — delivered as a justified, audited decision rather than a reflexive swap, plus its slice of OPT-06 (the chosen xsync type is -race tested). This is where the "xsync is justified per-use, not blanket" self-check is satisfied.
Output: collections_audit.go (the audit + the one justified xsync usage + the pool rationale), collections_audit_test.go (the xsync exercise + the no-live-capture gate). New external deps: none (xsync/v4 landed in 08-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-leaf-concurrency-optimizations/08-RESEARCH.md
@.planning/phases/08-leaf-concurrency-optimizations/08-01-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-02-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-04-PLAN.md
@.planning/phases/08-leaf-concurrency-optimizations/08-05-PLAN.md

<interfaces>
<!-- VERIFIED this session. The tick-owned collections the audit classifies — ALL plain today. -->

From server/entity_store.go (PLAIN maps — the audit confirms they stay plain):
```go
type entityStore struct {
    byID    map[int32]*Entity              // tick-owned; read via near() snapshot, not live by workers
    buckets map[level.ChunkPos][]*Entity   // tick-owned; near() copies out a fresh slice
}
type EntityIDAllocator struct { next atomic.Int32 } // ALREADY lock-free; read off-tick (accept goroutine) + on-tick
func (a *EntityIDAllocator) AllocID() int32 { return a.next.Add(1) } // the genuine cross-boundary primitive
```

From world/manager.go (PLAIN map — stays plain):
```go
type ChunkManager struct { columns map[level.ChunkPos]*holder } // tick-owned; workers read solidity SNAPSHOTS
```

From server/tick.go (PLAIN collections — stay plain; the pools = the OPT-04 'ants' deliverable):
```go
type TickLoop struct {
    players     []*tickPlayer                 // tick-owned slice
    clientIndex map[*Client]*tickPlayer       // tick-owned map
    pathPool, trackerPool, spawnPool *ants.Pool // 08-01 — the 'worker pools use ants/v2' deliverable
    asyncIn2    chan asyncResult
}
// tickPlayer.tracked map[int32]bool — copied into the OPT-02 snapshot; stays plain
```

From github.com/puzpuzpuz/xsync/v4 (the lock-free types — use ONE, justified; VERIFY API via Context7 if unsure):
```go
import "github.com/puzpuzpuz/xsync/v4"
m := xsync.NewMap[int32, *Entity]() // CLHT: obstruction-free reads, lock-sharded writes (v4 generics-only)
c := xsync.NewCounter()             // a sharded counter both a worker + the tick can touch
```
</interfaces>

<key_facts>
<!-- VERIFIED this session against 08-RESEARCH Open Question 1 + the live collections. -->
- SNAPSHOT-AND-STAY-PLAIN IS THE DEFAULT (08-RESEARCH Open Question 1 / Pitfall 1 / Critical observation): every OPT copies its input on the owner, so workers read copies, never the live store/world/tracked. Therefore entityStore.byID, entityStore.buckets, ChunkManager.columns, clientIndex, players, p.tracked ALL stay PLAIN. CLAUDE.md is explicit: "a tick-only map stays a plain map (single-owner = faster)." The audit must STATE this per-collection with the rationale, NOT swap them.
- THE ONE GENUINE CROSS-BOUNDARY PRIMITIVE: idAlloc (atomic.Int32) is the single thing read from two goroutines (the accept goroutine claims a player id off-tick; the tick claims entity ids; and now spawnCandidatesReady.applyTo claims on the owner). It is ALREADY lock-free (an atomic counter). The audit's honest conclusion: an atomic.Int32 IS the lock-free/specialized variant for a counter — xsync is not needed to "upgrade" it. Document this so the audit does not manufacture a contended map that doesn't exist.
- SATISFY OPT-04'S LITERAL TEXT WITH ONE JUSTIFIED xsync USE: OPT-04 says "hot-path collections use lock-free/specialized variants (xsync/v4)". To exercise the dep with a real contention rationale (not theater), introduce ONE justified usage: PREFER a collection that 01-05 actually revealed as cross-boundary if any exists; OTHERWISE use an xsync.Counter for a metric BOTH a pool worker and the tick legitimately touch — e.g. an `asyncSubmits`/`asyncDrops` counter incremented by the pool-submit path (which may run in a worker callback) and read by the tick/Stats. This is a genuine multi-writer counter → xsync.Counter is the correct lock-free primitive (vs racing a plain int). Justify it in the audit. Do NOT convert a tick-only map to xsync to "use the dep."
- THE ants POOLS ARE THE OTHER HALF OF OPT-04: pathPool/trackerPool/spawnPool (one per subsystem, 08-01) satisfy "worker pools use ants/v2". The audit records: sizing (pathfinding ~runtime.NumCPU per the heavy A*; tracker/spawner small) and the non-blocking drop-on-overload discipline (Pitfall 4) as the bounded guarantee. No new pool is created here — they are confirmed + documented.
- ENFORCE THE DISCIPLINE (08-RESEARCH Pitfall 1 warning sign): add a gate that a Submit closure never captures a live tick-owned collection. A practical gate: a grep-style test (or a documented invariant test) over server/*.go asserting that no `t.pathPool.Submit`/`t.trackerPool.Submit`/`t.spawnPool.Submit` closure body references `t.entities`/`t.world`/`p.tracked` directly (they must read a snapshot value). Use a comment-filtered grep (grep -v '^\s*//' then grep the pattern) so doc prose doesn't false-positive. This guards the snapshot-and-stay-plain conclusion against regression.
- xsync v4 IS GENERICS-ONLY (08-RESEARCH State of the Art): MapOf aliases Map; use xsync.NewMap[K,V]()/xsync.NewCounter(). Confirm the exact constructor names via Context7 (the README) before writing.
- STALE-LSP CAVEAT (STATE.md): trust `go build ./...` over gopls phantom errors on the xsync generics.
- Serena guidance: create server/collections_audit.go (the documented audit table in the header + the ONE justified xsync usage — wired into the async-submit path or a Stats counter — + the pool rationale). Create server/collections_audit_test.go (exercise the xsync type concurrently under -race + the no-live-capture grep/invariant gate). Use Context7 to confirm xsync.NewCounter/NewMap.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Off-tick pool worker ↔ tick goroutine (any shared metric/collection) | The audit's job is to prove the ONLY shared state crossing the boundary is the immutable channel message + the already-lock-free idAlloc atomic (+ the one justified xsync metric). No live tick-owned map is shared. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-21 | Tampering/race | a future change capturing a live tick-owned collection in a pool closure (reintroducing a data race) | mitigate | The no-live-capture gate (grep/invariant test) fails such a change; the audit documents the snapshot discipline as the contract (Pitfall 1 warning sign). |
| T-8-22 | Tampering/race | a multi-writer metric counter (worker + tick) racing as a plain int | mitigate | The one justified xsync.Counter (or the contended collection) is lock-free; exercised under -race by TestHotCollections. |
| T-8-23 | Performance regression | xsync-ing a tick-only map "to be safe" (slower, pointless) | accept (prevented) | The audit explicitly KEEPS tick-only maps plain with a per-collection rationale (single-owner = faster); the gate prevents a needless swap masquerading as safety. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the contention audit (snapshot-and-stay-plain per collection) + the ants-pool rationale, and introduce the ONE justified xsync/v4 usage (OPT-04)</name>
  <files>server/collections_audit.go, server/async.go</files>
  <read_first>
    - .planning/phases/08-leaf-concurrency-optimizations/08-RESEARCH.md — Open Question 1, Pitfall 1, the Critical observation for OPT-04, the Standard Stack (xsync/v4 + ants/v2), the OPT-04 Code Example.
    - the LANDED 08-02/04/05 SUMMARYs (or PLANs) — what each swap actually snapshots, confirming no live cross-boundary read exists.
    - server/entity_store.go + world/manager.go + server/tick.go — the live collections to classify (all plain) + idAlloc (the atomic).
    - Context7: github.com/puzpuzpuz/xsync/v4 — confirm NewCounter / NewMap constructors + the v4 generics API.
  </read_first>
  <action>
    1. server/collections_audit.go (package server) — a file-header AUDIT TABLE: each tick-owned collection (entityStore.byID, entityStore.buckets, ChunkManager.columns, clientIndex, players, p.tracked) → "read live by a worker? NO (snapshot on owner)" → DECISION: stay PLAIN, with the per-collection rationale citing which OPT snapshots it (OPT-01 snapshotRegion / OPT-02 tracked copy / OPT-03 solidity snapshot). Record idAlloc as the one genuine cross-boundary primitive, ALREADY lock-free (atomic.Int32 = the correct specialized variant; no xsync needed). Record pathPool/trackerPool/spawnPool as the "worker pools use ants/v2" deliverable with sizing + non-blocking rationale.
    2. server/collections_audit.go + server/async.go — introduce the ONE justified xsync/v4 usage: an xsync.Counter (e.g. asyncSubmits / asyncDrops) that the async-submit path (OPT-01/02/03 Submit callers + the overload-drop path) increments and the tick/Stats reads — a genuine multi-writer counter where xsync.Counter is the correct lock-free primitive. Wire the increments at the existing Submit/drop sites (a small, additive change) and expose a read (e.g. via Stats or a getter). Document WHY this is the justified use and why no map needed conversion.
    3. Confirm `go build ./...` under CGO_ENABLED=0 with the xsync import.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && go vet ./server/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The audit documents EVERY tick-owned collection's plain-vs-xsync decision with a contention rationale; the conclusion is snapshot-and-stay-plain (no map converted needlessly).
    - The ants pools are confirmed as the OPT-04 'worker pools use ants/v2' deliverable with sizing + non-blocking rationale.
    - Exactly ONE justified xsync/v4 usage is introduced (a genuine multi-writer counter or a real contended collection), exercising the dep with a rationale — not blanket xsync.
    - `CGO_ENABLED=0 go build ./...` clean.
  </acceptance_criteria>
  <done>OPT-04 is delivered as an audited decision: snapshot-and-stay-plain for every tick-only collection (justified per-collection), the ants pools confirmed, and one justified xsync/v4 usage exercising the dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the xsync/v4 usage under -race + enforce the no-live-capture snapshot gate (OPT-04 / OPT-06)</name>
  <files>server/collections_audit_test.go</files>
  <read_first>
    - .planning/phases/08-leaf-concurrency-optimizations/08-RESEARCH.md — Pitfall 1 (the warning sign: a worker capturing a live collection) + the Validation section (TestHotCollections).
    - server/collections_audit.go (Task 1) — the xsync.Counter usage to exercise + the Submit sites to gate.
  </read_first>
  <behavior>
    OPT-04 (the xsync exercise + the discipline gate):
    - Test 1 (TestHotCollectionsXsyncConcurrent): the chosen xsync type (Counter/Map) is exercised by N concurrent goroutines incrementing/reading, asserting the final value is exact — proving the lock-free primitive is correct under contention (the -race target).
    - Test 2 (TestNoLiveCollectionCapture): a grep/invariant gate over server/*.go (comment-filtered) asserting no `Submit(` closure body references `t.entities`/`t.world`/`p.tracked` directly — the snapshot discipline is enforced, so a regression that reintroduces a live cross-boundary read fails this test.
    - Test 3 (TestTickOnlyMapsStayPlain): a documented assertion that entityStore.byID / ChunkManager.columns / clientIndex are plain maps (not xsync types) — the snapshot-and-stay-plain conclusion is pinned so a future "xsync everything" change is caught.
  </behavior>
  <action>
    1. server/collections_audit_test.go (package server) — TestHotCollectionsXsyncConcurrent: spin N goroutines hammering the xsync.Counter/Map and assert the exact final count/state (run under -race in the Docker gate). TestNoLiveCollectionCapture: read the server/*.go sources, strip comment lines (a regex filtering `^\s*//` and block comments, or use a comment-aware scan), then assert no Submit-closure region references the forbidden live-collection tokens — fail with a clear message naming the offending file/line. TestTickOnlyMapsStayPlain: a compile/type assertion (or reflection check) that the named collections are Go maps, not xsync types.
    2. Ensure the gate is robust against the audit's own doc prose (comment-filter so the audit table text does not false-positive).
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestHotCollections|TestNoLiveCollectionCapture|TestTickOnlyMapsStayPlain' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The xsync/v4 usage is exercised by a concurrent test (exact result under contention) — the -race target for OPT-04.
    - The no-live-capture gate enforces the snapshot discipline (a reintroduced live cross-boundary read fails the test); it is robust against the audit's own comment prose.
    - The tick-only-maps-stay-plain assertion pins the snapshot-and-stay-plain conclusion against a future blanket-xsync regression.
  </acceptance_criteria>
  <done>OPT-04 is enforced + tested: the one justified xsync/v4 use is -race clean under contention, and the snapshot discipline (no live cross-boundary collection read) is gated against regression. Run the Docker -race gate over ./server/... before marking the wave done: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... -count=1`.</done>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestHotCollections|TestNoLiveCollectionCapture|TestTickOnlyMapsStayPlain' -count=1` passes.
- Docker `-race` over `./server/...` clean (the xsync counter under concurrent access; the snapshot discipline holds) — `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./server/... -count=1`.
- `CGO_ENABLED=0 go build ./... && go vet ./...` clean; no new external deps (xsync/v4 landed in 08-01).
</verification>

<success_criteria>
- OPT-04: the hot-path collection strategy is delivered as an audited, justified decision — snapshot-and-stay-plain for every tick-only collection (single-owner = faster), with the contention rationale per collection, NOT blanket xsync.
- The ants/v2 pools (one per subsystem) are confirmed as the 'worker pools use ants/v2' deliverable; exactly ONE justified xsync/v4 usage exercises the dep with a real contention rationale.
- A gate enforces the snapshot discipline (no live cross-boundary collection read) so the -race-clean-by-construction property cannot silently regress; the chosen xsync type is -race tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-leaf-concurrency-optimizations/08-06-SUMMARY.md`.
</output>
</content>
