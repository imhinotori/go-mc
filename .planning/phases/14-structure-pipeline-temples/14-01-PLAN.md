---
phase: 14-structure-pipeline-temples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/extract_worldgen.go
  - world/levelgen/data/embed.go
  - world/structure/placement.go
  - world/structure/placement_test.go
  - world/structure/start.go
  - world/structure/cache.go
  - world/structure/cache_test.go
  - world/structure/surface_sampler.go
  - world/noisegen.go
  - world/worker.go
  - world/worker_structure_test.go
requirements: [STRUCT-01]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The placement math is ported BIT-EXACT from the jar (javap -c net.minecraft.world.level.levelgen.structure.placement.RandomSpreadStructurePlacement / StructurePlacement against temp/cache/26.2-inner.jar): getPotentialStructureChunk uses Math.floorDiv(chunkX, spacing) (NOT Go truncating '/' — an explicit floorDiv for negatives), seeds a WorldgenRandom(LegacyRandomSource(0)) via the ALREADY-PORTED SetLargeFeatureWithSalt(worldSeed, regionX, regionZ, salt), then evaluates the spread offset per axis via the SpreadType (LINEAR = nextInt(spacing-separation) ONE draw; TRIANGULAR = (nextInt(b)+nextInt(b))/2 TWO draws — the exact draw count or the whole stream desyncs; LINEAR is the RandomSpreadStructurePlacement DEFAULT when the structure_set JSON omits the spread_type field, which all four temple sets do — the loader supplies LINEAR explicitly from the field initializer, javap cite), yielding startChunk = (regX*spacing+ox, regZ*spacing+oz); isStructureChunk(cx,cz) = getPotentialStructureChunk(cx,cz) == (cx,cz). The four probabilityReducer variants (probabilityReducer, legacyProbabilityReducerWithDouble, legacyArbitrarySaltProbabilityReducer, legacyPillagerOutpostReducer) are ported (the latter three are Phase-15 mineshaft/outpost paths but ported now for jar completeness + the structure_set frequency_reduction_method field selects which) — a golden test pins a known (worldSeed, salt, spacing, separation) → the exact start chunk + the exact draw sequence."
    - "A world/structure package owns the StructureStart cache: a concurrent xsync.Map[int64,*StructureStart] (packed-pos keyed, the same packing world.packPos uses) holding Starts (starts OWNED by that chunk, computed in the STARTS phase, pure over (seed,chunkPos)) + References (for chunk C, the packed keys of neighbor chunks whose start bbox reaches C, computed in the REFERENCES phase). The STARTS computation is PURE over (worldSeed, chunkPos) (WORLD-04 purity mandate) so the cache is a MEMOIZATION not shared mutable state, deduped via golang.org/x/sync/singleflight exactly like chunk-gen — two workers computing the same chunk's starts get byte-identical results. NO new deps (xsync/v4 + singleflight already vendored per CLAUDE.md)."
    - "STRUCTURE_REFERENCES is ported as an 8-chunk-radius bbox-intersect scan (jar-confirmed ±8 in ChunkGenerator.createReferences): for chunk C, scan every chunk in [C.x-8..C.x+8]x[C.z-8..C.z+8] and, for each scanned cell, COMPUTE-STARTS-ON-DEMAND (ComputeStarts(seed, cell, gen) — pure over (seed,cell) + singleflight-deduped, so it is cheap, order-independent, and memoized) rather than reading only already-cached starts; for every owned StructureStart whose bounding box intersects C's 16x16 column, record the owner's packed key in C's References list — this is how a multi-chunk structure owned by a neighbor up to 8 chunks away is FOUND from C. The scan radius (±8) and the start-compute radius are THE SAME (both 8) — a radius-8 scan reading radius-1 data would silently truncate any start owned ≥2 chunks out (latent for ≤2-chunk temples, a LIVE bug the moment Phase-15 mineshafts reuse this), so REFERENCES drives its own ComputeStarts per scanned cell. 8 is the max structure radius the engine assumes."
    - "The heightmap-at-STARTS column sampler is exposed (Pitfall #9): a cheap 'sample surface Y at world (x,z)' that does NOT require the owner chunk's blocks to exist — it samples the noise router's PreliminarySurfaceLevel density node directly (world/levelgen/noisechunk preliminarySurfaceLevel reads ONLY router.NoiseRouter.PreliminarySurfaceLevel.Compute, NOT the filled density field), matching vanilla's getBaseHeight/preliminary-surface sampling (NOT placed blocks). Scattered temples project_start_to_heightmap WORLD_SURFACE_WG read this at STARTS time before that chunk generates. DECISION DOCUMENTED inline: sample the router's preliminary surface (one density compute per quart cell) over generating the owner chunk — same value, ~1/700 the cost."
    - "The two-phase worker seam is ported (research 'Brownfield: two-pass in the worker, recommended'): the worker pipeline becomes fill→surface→carve→[compute+cache STARTS for C]→[compute REFERENCES for C — which itself on-demand-computes STARTS over C's full ±8 scan, each pure (seed,pos) singleflight-deduped, so radius-8 references read radius-8 data not radius-1]→[PLACE pieces into C — wired in 14-02]→finish. The STARTS step has NO block writes (pure geometry over the surface sampler), so it does not disturb the existing fill/surface/carve ordering; PLACE runs LAST (vanilla's FEATURES step is after carve) so structures overwrite terrain. This plan lands STARTS+REFERENCES + the PLACE HOOK (an empty placeStructures(view) call site in Decorate that 14-02 fills); the 5x5 determinism (TestDecorationReorderIdentical) + emit-once gates STAY green with STARTS computing (no blocks written yet)."
    - "The biome-check DATA + SEAM for the temple placement (the load-bearing half of 'vanilla positions') is established: the embedded has_structure biome-tag allow-lists (desert_pyramid→[desert], igloo→[snowy_taiga,snowy_plains,snowy_slopes], jungle_temple→[bamboo_jungle,jungle], swamp_hut→[swamp] — VERIFIED in the jar) load + parse to biome name-sets; and the NoiseGenerator threads its MultiNoiseBiomeSource (world/levelgen/biome/source.go GetBiome(x,y,z) levelbiome.Type, public) into the StartGenerator context (a BiomeAt(wx,wy,wz) levelbiome.Type func or the source itself) so 14-02/14-03 can gate each temple on a REAL GetBiome-at-origin ∈ allow-set test. The StartGenerator interface carries the biome lookup alongside the SurfaceSampler — NO accept-by-default escape; a temple whose origin biome is not in its allow-set produces NO start (vanilla behavior)."
  artifacts:
    - path: "world/structure/placement.go"
      provides: "RandomSpreadStructurePlacement (getPotentialStructureChunk floorDiv+SetLargeFeatureWithSalt+SpreadType) + isStructureChunk + the 4 probabilityReducer variants + the structure_set JSON model (spacing/separation/salt/spread_type/frequency_reduction_method)"
      contains: "func.*getPotentialStructureChunk"
      min_lines: 80
    - path: "world/structure/cache.go"
      provides: "the concurrent StructureStart cache (xsync.Map[int64,*StructureStart], Starts + References) + singleflight-deduped pure STARTS memoization + the 8-radius References scan"
      contains: "xsync.Map"
    - path: "world/structure/start.go"
      provides: "StructureStart (piece list + bounding box + chunkPos + reference count) + BoundingBox (Intersects/IsInside/the writable-area helper) — the start/bbox types 14-02's pieces fill"
      contains: "type StructureStart"
    - path: "world/structure/surface_sampler.go"
      provides: "the heightmap-at-STARTS column sampler: SurfaceSampler interface { SampleSurfaceY(wx,wz) int } satisfied by the router's PreliminarySurfaceLevel (no owner chunk needed)"
      contains: "SampleSurfaceY"
    - path: "world/worker.go"
      provides: "the two-pass seam: STARTS computed+cached for C and its 8 neighbors after carve, REFERENCES for C, the placeStructures hook (empty here, filled 14-02), gated before emit"
      contains: "structure"
    - path: "world/structure/placement_test.go"
      provides: "the bit-exact placement golden: a known (worldSeed,salt,spacing,separation) → the exact start chunk + draw sequence for LINEAR + TRIANGULAR + isStructureChunk + floorDiv-for-negatives"
      contains: "TestGetPotentialStructureChunk"
  key_links:
    - from: "world/structure/placement.go getPotentialStructureChunk"
      to: "world/levelgen/random.go WorldgenRandom.SetLargeFeatureWithSalt"
      via: "the placement RNG is seeded via the ALREADY-PORTED salt-in-seed helper (Phase 10), then SpreadType draws nextInt"
      pattern: "SetLargeFeatureWithSalt"
    - from: "world/structure/cache.go STARTS memoization"
      to: "golang.org/x/sync/singleflight"
      via: "two workers computing the same chunk's starts collapse to one pure computation (like chunk-gen dedup)"
      pattern: "singleflight"
    - from: "world/worker.go placeStructures hook"
      to: "world/structure/cache.go References + Starts"
      via: "the PLACE pass (14-02) gathers C's own + referenced starts from the cache; this plan lands the hook + STARTS/REFERENCES population"
      pattern: "References|startsForChunk"
    - from: "world/structure/surface_sampler.go SampleSurfaceY"
      to: "world/levelgen/noisechunk preliminarySurfaceLevel (router.NoiseRouter.PreliminarySurfaceLevel)"
      via: "the STARTS heightmap query samples the router's preliminary surface (no owner chunk), matching vanilla getBaseHeight"
      pattern: "PreliminarySurfaceLevel"
---

<objective>
Land the STRUCTURE PIPELINE (STRUCT-01) — the integration-risk half of Phase 14 — in isolation, well-tested, before any structure geometry exists. Four pieces:

1. **The placement math (bit-exact)**: `getPotentialStructureChunk` (floorDiv + the already-ported `SetLargeFeatureWithSalt` + the `SpreadType` LINEAR/TRIANGULAR draw counts), `isStructureChunk`, and the four `probabilityReducer` variants — ported javap -c from the jar, pinned by a golden test deriving the expected start chunk + draw sequence from the algorithm.

2. **The StructureStart cache (`world/structure` package)**: a concurrent `xsync.Map[int64,*StructureStart]` holding `Starts` (owned, pure over (seed,pos)) + `References` (neighbor starts reaching C), memoized + `singleflight`-deduped like chunk-gen. NO new deps.

3. **STRUCTURE_REFERENCES**: the 8-chunk-radius (±8, jar-confirmed) bbox-intersect scan that records, for chunk C, which neighbor starts reach into it — the cross-chunk discovery mechanism.

4. **The heightmap-at-STARTS column sampler (Pitfall #9) + the two-pass worker seam**: a cheap `SampleSurfaceY(wx,wz)` from the router's `PreliminarySurfaceLevel` (no owner chunk needed — vanilla's `getBaseHeight` approach), and the worker pipeline extended to `fill→surface→carve→[STARTS for C+8 neighbors]→[REFERENCES for C]→[PLACE hook]→finish`. STARTS writes NO blocks, so the 5x5 determinism + emit-once gates stay green; PLACE is an empty hook 14-02 fills.

This plan deliberately isolates the worker-seam + placement math (the integration risk) into its own well-tested unit, with ZERO blocks placed — the desert pyramid shakedown (14-02) lands on a proven, byte-stable pipeline.

Purpose: a deterministic, cross-chunk-correct structure-decision pipeline that any structure can hang geometry on.
Output: the `world/structure` package (placement + cache + start/bbox + surface sampler) + the worker two-pass seam + the embedded structure/structure_set JSON. ZERO new deps, ZERO blocks placed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-structures.md
@.planning/phases/10-worldgen-foundation/10-03-SUMMARY.md

<interfaces>
<!-- ALL VERIFIED on disk 2026-06-25. Re-read world/worker.go + world/noisegen.go + world/levelgen/random.go BEFORE coding. Trust `go build ./...` over gopls/Serena phantom errors. -->

THE ALREADY-PORTED SEED HELPERS (world/levelgen/random.go, VERIFIED Phase 10 — do NOT re-port):
```go
func NewWorldgenRandom(seed int64) *WorldgenRandom
// placement decision: seed = x*341873128712 + z*132897987541 + worldSeed + salt; PURE, no draws
func (w *WorldgenRandom) SetLargeFeatureWithSalt(worldSeed int64, x, z, salt int)
// piece RNG (used by 14-02): re-seed worldSeed, draw a,b, seed (cx*a)^(cz*b)^worldSeed
func (w *WorldgenRandom) SetLargeFeatureSeed(worldSeed int64, chunkX, chunkZ int)
// LCG draws (java.util.Random-exact, VERIFIED): NextIntN(bound) (modulo-bias loop), NextFloat, NextLong
```
WorldgenRandom embeds *LegacyRandomSource → it IS a levelgen.RandomSource with NextIntN/NextFloat/NextLong.

THE HEIGHTMAP-AT-STARTS SAMPLER SOURCE (world/levelgen/noisechunk/noisechunk.go, VERIFIED): preliminarySurfaceLevel(x,z) reads ONLY `nc.router.NoiseRouter.PreliminarySurfaceLevel.Compute(density.Context{X:qx,Y:0,Z:qz})` (qx=(x>>2)<<2, floored) — NOT the filled density field. So a router-only column sampler needs NO NoiseChunk fill, NO owner chunk: `floor(router.NoiseRouter.PreliminarySurfaceLevel.Compute(...))`. This is vanilla's getBaseHeight/preliminary-surface (Pitfall #9). NewNoiseChunk(router,pos) does the FULL ~768-corner fill (~116ms/chunk, STATE.md 09-09) — do NOT use it for the per-column STARTS query; sample the router node directly.

THE WORKER SEAM (world/worker.go, VERIFIED Phase 10 — the scheduler goroutine owns staging/all-decoration, no locks): tryDecorate(center) builds a Neighborhood over the 9 carved chunks and calls g.gen.Decorate(view). The STARTS+REFERENCES+PLACE hook slots into this single-owner scheduler path (research "two-pass in the worker"). packPos(p)=int64(p[0])<<32|int64(uint32(p[1])) is the cache key packing. The 5x5 reorder + emit-once gates are TestDecorationReorderIdentical / TestEmitOnce in world/worker_seam_test.go — re-read for the AS-BUILT shape; they MUST stay green (STARTS writes nothing).

THE DECORATE SEAM (world/noisegen.go, VERIFIED): Decorate(view *Neighborhood) runs the post-carve 3x3 pass; g.seed is the worldSeed, g.router the bound graph, g.Dims()=(minY, secs*16). The placeStructures hook goes at the END of Decorate (after applyBiomeDecoration — vanilla FEATURES step) so structures overwrite terrain+features. The center chunk is view.chunks[packPos(view.center)].

THE NEIGHBORHOOD WRITE PROXY (world/neighborhood.go, VERIFIED): view.SetBlock(wx,wy,wz,stateID) / view.GetBlock — cross-chunk (blockStateWriteRadius=1, out-of-3x3 DROPPED) + heightmap-live. 14-02's pieces write through it; this plan only WIRES the hook.

THE EMBED PATTERN (tools/extract_worldgen.go + world/levelgen/data/embed.go, VERIFIED): worldgenZipPrefixes is a []{jarPrefix,destSub}; ADD {"data/minecraft/worldgen/structure/","structure"} (34 JSON) + {"data/minecraft/worldgen/structure_set/","structure_set"} (20 JSON) — pure-unzip, no binary. Desert/jungle/igloo/swamp_hut ship 0 .nbt (code-assembled), so NO binary extraction this phase (that is Phase-16's village problem). Then extend embed.go's //go:embed line with `structure structure_set`. Run the extractor: `cd tools && go run . --version 26.2` (or the documented gen command — confirm in tools/).

THE STRUCTURE_SET SALTS (research, jar-confirmed): desert_pyramid spacing 32 / separation 8 / salt 14357617; igloo salt 14357618; swamp_hut salt 14357620; jungle_pyramid shares the desert set's machinery. spread_type: linear (temples). LOAD these from the embedded structure_set JSON, do NOT hardcode (the JSON is the authority; cross-check the ported constants against it).

The Docker -race command (STATE.md): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...`.
</interfaces>

<key_facts>
<!-- PORT the placement math + cache + references + sampler from jar bytecode (cite the class). EMBED structure/structure_set JSON. Idiomatic Go, no GPL paste. -->
- PORT TARGETS (javap -c, cite each): net.minecraft.world.level.levelgen.structure.placement.RandomSpreadStructurePlacement (getPotentialStructureChunk, SpreadType.LINEAR/TRIANGULAR.evaluate), StructurePlacement (isStructureChunk, the 4 probabilityReducer static methods), ChunkGenerator.createReferences (the ±8 scan), ChunkGenerator.createStructures/tryGenerateStructure (the STARTS driver — the per-structure findGenerationPoint is 14-02; this plan lands the DRIVER + an EMPTY start for the shakedown to hang on).
- floorDiv DISCIPLINE (Pitfall #3 + #8): Math.floorDiv(chunkX, spacing) floors toward negative infinity; Go `/` truncates toward zero. Implement floorDiv(a,b) explicitly (the same discipline random.go's mthGetSeed documents for Java >> vs >>>). regX=floorDiv(chunkX,spacing). Region/seed math is Java long overflow — explicit int64.
- SpreadType DRAW COUNT (Pitfall #3): LINEAR.evaluate(rng,bound)=rng.nextInt(bound) ONE draw; TRIANGULAR.evaluate(rng,bound)=(rng.nextInt(bound)+rng.nextInt(bound))/2 (or the exact jar form — TRANSCRIBE it) TWO draws. Get the draw count EXACT or every later piece-placement draw desyncs vs a vanilla seed. The salt is added INSIDE SetLargeFeatureWithSalt's seed (already correct), NOT XORed after.
- THE STARTS PURITY (WORLD-04): the start decision is pure over (worldSeed, chunkPos) — same inputs → same start chunk → same (eventual) piece tree. So the cache is a MEMOIZATION (singleflight-deduped, two concurrent computers of the same chunk get identical results), NOT dangerous shared mutable state. The xsync.Map + singleflight are ALREADY vendored (go.mod) — NO new deps (CLAUDE.md: xsync/v4 for concurrent maps, singleflight for gen dedup).
- THE 8-RADIUS REFERENCES (Pitfall #2): a structure owned by chunk N writes into C. If PLACE only saw C's own starts, multi-chunk structures truncate. createReferences scans ±8 (jar-confirmed) and records every neighbor start whose bbox intersects C. 8 = the max structure radius the engine assumes. This is what makes the cross-chunk PLACE (14-02's placeInChunk clip) find N's start from C.
- THE HEIGHTMAP-AT-STARTS DECISION (Pitfall #9, DOCUMENT inline): scattered temples (project_start_to_heightmap WORLD_SURFACE_WG) need terrain height at STARTS time, BEFORE that chunk's blocks exist. Vanilla samples the noise generator's getBaseHeight (the preliminary surface), NOT placed blocks. Sulfur exposes SampleSurfaceY(wx,wz) = floor(router.NoiseRouter.PreliminarySurfaceLevel.Compute(quart-snapped (wx,wz))) — ONE density compute, no chunk fill. CHOSEN over "generate the owner chunk first" (same value, ~700x cheaper, and avoids a STARTS→Generate cycle). Note the divergence honestly: this is the PRELIMINARY surface (pre-carve/surface-rule), exactly what vanilla's structure placement uses, NOT the final placed-block heightmap.
- THE TWO-PASS WORKER SEAM (research, MEDIUM/INFERRED — design choice): extend the scheduler so for a wanted center C it (a) ensures STARTS computed+cached for C AND its 8 neighbors (each pure (seed,pos), singleflight-deduped — cheap, geometry-only), (b) runs the existing terrain+Decorate, (c) the PLACE pass (14-02) writes pieces into C from C's own+referenced starts BEFORE emit. This plan lands (a)+(b)+the REFERENCES + an EMPTY placeStructures(view) hook. The single-owner ChunkResult handoff is preserved — STARTS/REFERENCES run on the SAME scheduler goroutine (no locks; the cache's xsync.Map is the ONLY cross-goroutine state and it is a pure memoization).
- ZERO BLOCKS PLACED this plan: STARTS is pure geometry (no SetBlock), the placeStructures hook is empty, so the emitted chunk bytes are IDENTICAL to Phase 13. The 5x5 reorder (TestDecorationReorderIdentical) + emit-once gates MUST stay byte-identical — re-run them. The structure pipeline EXISTS + is tested in isolation; the first BLOCKS come in 14-02.
- NO visual gate this phase (that is Phase 16) — acceptance is automated. Do NOT add an autonomous:false task.
- STALE-LSP caveat: trust `go build ./...` over gopls/Serena phantom errors.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the placement math (getPotentialStructureChunk + isStructureChunk + the 4 probabilityReducers) + the embedded structure/structure_set JSON</name>
  <files>tools/extract_worldgen.go, world/levelgen/data/embed.go, world/structure/placement.go, world/structure/placement_test.go</files>
  <behavior>
    - getPotentialStructureChunk(worldSeed, chunkX, chunkZ, spacing, separation, salt, spreadType) returns the start ChunkPos: regX=floorDiv(chunkX,spacing), regZ=floorDiv(chunkZ,spacing), a fresh WorldgenRandom(0).SetLargeFeatureWithSalt(worldSeed,regX,regZ,salt), ox=spreadType.evaluate(rng, spacing-separation), oz=spreadType.evaluate(rng, spacing-separation), start=(regX*spacing+ox, regZ*spacing+oz) — JAR-EXACT draw order.
    - LINEAR.evaluate draws nextInt(bound) ONCE; TRIANGULAR draws nextInt(bound) TWICE then averages (jar form). A golden vector with a known (worldSeed,salt,spacing,separation) reproduces the exact start chunk for BOTH spread types.
    - isStructureChunk(worldSeed,cx,cz,...) == (getPotentialStructureChunk(...) == (cx,cz)).
    - floorDiv handles negative chunk coords correctly (floor toward -inf, NOT Go truncation): a golden with chunkX=-1, spacing=32 lands in region -1, not 0.
    - The 4 probabilityReducer variants reproduce their jar nextFloat()<frequency gates (legacyArbitrarySaltProbabilityReducer is the mineshaft path, ported now for completeness; a golden pins each).
    - The structure_set JSON (desert_pyramid spacing 32/sep 8/salt 14357617, igloo salt 14357618, swamp_hut salt 14357620) loads from the embedded FS and its constants MATCH the ported defaults.
  </behavior>
  <action>
tools/extract_worldgen.go: ADD to worldgenZipPrefixes: {"data/minecraft/worldgen/structure/","structure"} + {"data/minecraft/worldgen/structure_set/","structure_set"} (pure-unzip JSON, NO binary — temples ship 0 .nbt) AND the biome-tag tree {"data/minecraft/tags/worldgen/biome/has_structure/","tags/worldgen/biome/has_structure"} (the per-structure biome allow-lists — VERIFIED present in the jar: desert_pyramid.json=[minecraft:desert], igloo.json=[snowy_taiga,snowy_plains,snowy_slopes], jungle_temple.json=[bamboo_jungle,jungle], swamp_hut.json=[minecraft:swamp]). Run the extractor (`cd tools && go run . --version 26.2` — confirm the exact gen command in tools/) so the 34 structure + 20 structure_set + the has_structure biome tags land under world/levelgen/data/. Log the counts.
world/levelgen/data/embed.go: extend the //go:embed line to include `structure structure_set` AND add `tags/worldgen/biome/has_structure` (or widen the existing `tags` embed to cover the worldgen biome tags — re-read the AS-BUILT directive lines 53-55; tags/block is currently embedded, the worldgen/biome/has_structure tree is NEW) so the biome allow-lists are in FS for the 14-02/14-03 temple biome check.
world/structure/placement.go (NEW package): port (javap -c, cite RandomSpreadStructurePlacement + StructurePlacement) — floorDiv(a,b int) (explicit floor-toward-neg-inf), SpreadType (Linear/Triangular with evaluate(rng RandomSource, bound int) int — the EXACT draw counts), RandomSpreadStructurePlacement{spacing,separation,salt,spreadType} + getPotentialStructureChunk + isStructureChunk, the 4 probabilityReducer static funcs (probabilityReducer / legacyProbabilityReducerWithDouble / legacyArbitrarySaltProbabilityReducer / legacyPillagerOutpostReducer), and the structure_set JSON model + a loader over the embedded data/structure_set (parse spacing/separation/salt/spread_type/frequency_reduction_method; the placement union is "random_spread" here, "concentric_rings" is Phase-15 — parse the type tag, only random_spread bodies needed now). The spread_type field is ABSENT in all four temple structure_sets (VERIFIED) — the loader MUST default it to LINEAR (the RandomSpreadStructurePlacement field initializer default, javap cite), NOT error or leave it zero-value-ambiguous; make the default explicit + tested. Cite the seed via the already-ported WorldgenRandom.SetLargeFeatureWithSalt (do NOT re-port). No GPL paste.
world/structure/placement_test.go: TestGetPotentialStructureChunk (a known worldSeed+desert salt/spacing/separation → derive the expected start chunk + draw sequence FROM the algorithm, assert exact) for LINEAR + a TRIANGULAR case; TestFloorDivNegative (chunkX=-1,spacing=32 → region -1); TestIsStructureChunk (the start chunk reports true, a neighbor reports false); TestProbabilityReducers (each variant's nextFloat gate at a known seed); TestStructureSetJSONMatchesConstants (the embedded desert_pyramid/igloo/swamp_hut structure_set salts+spacing == the ported values, AND the absent spread_type field → the loader yields LINEAR); TestHasStructureBiomeTagsLoad (the embedded has_structure/desert_pyramid|igloo|jungle_temple|swamp_hut tags parse to their expected biome name-sets — the data the 14-02/14-03 biome check reads).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestGetPotentialStructureChunk|TestFloorDiv|TestIsStructureChunk|TestProbabilityReducers|TestStructureSetJSON' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The placement math is bit-exact: getPotentialStructureChunk (floorDiv + the already-ported salt-in-seed + LINEAR/TRIANGULAR draw counts), isStructureChunk, and the 4 probabilityReducers reproduce the jar draw sequence for a known seed; the structure/structure_set JSON + the has_structure biome-tag allow-lists are embedded, the absent spread_type defaults to LINEAR, and the salts match the ported constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the StructureStart cache + bbox/start types + the 8-radius REFERENCES scan + the heightmap-at-STARTS sampler</name>
  <files>world/structure/start.go, world/structure/cache.go, world/structure/cache_test.go, world/structure/surface_sampler.go</files>
  <behavior>
    - StructureStart holds a piece list (filled by 14-02), a bounding box (union of piece bboxes), the owning chunkPos, and a reference count; an EMPTY start (no pieces) reports its zero bbox and is treated as "no structure here".
    - BoundingBox supports Intersects(other) + IsInside(wx,wy,wz) + a writableArea(chunkPos, minY, height) helper (the 16x16 column: minX..minX+15, full height, minZ..minZ+15 — jar getWritableArea).
    - The cache stores Starts[packedPos]=[]*StructureStart (owned, computed pure over (seed,pos)) + References[packedPos]=[]int64 (neighbor keys reaching C); computeStarts(seed,pos) is singleflight-deduped — two concurrent calls collapse to one and return identical results.
    - computeReferences(seed, C, gen) scans [C±8]x[C±8] and COMPUTES STARTS ON DEMAND for each scanned cell (ComputeStarts(seed, cell, gen) — pure + singleflight-deduped, never read-only-cached), then for every owned start whose bbox intersects C's writable column, appends the owner's packed key to C's References (dedup) — a start owned by N up to 8 chunks out is found from C (NOT just radius-1 neighbors).
    - SampleSurfaceY(wx,wz) returns floor(router.NoiseRouter.PreliminarySurfaceLevel.Compute(quart-snapped)) WITHOUT any chunk fill — pure over (router/seed, wx, wz); two calls at the same (x,z) return the same Y.
  </behavior>
  <action>
world/structure/start.go: StructureStart{Pieces []Piece /*the Piece interface 14-02 defines — declare a minimal placeholder interface { BoundingBox() BoundingBox } HERE so the cache compiles, 14-02 extends it*/, BBox BoundingBox, ChunkPos level.ChunkPos, References int} + BoundingBox{MinX,MinY,MinZ,MaxX,MaxY,MaxZ int} with Intersects/IsInside/Encapsulate + WritableArea(pos level.ChunkPos, minY, height int) BoundingBox (the 16x16xH column, jar getWritableArea). Port from StructureStart + BoundingBox (javap -c, cite). An empty Pieces start = no structure.
world/structure/cache.go: Cache{Starts *xsync.Map[int64,[]*StructureStart], References *xsync.Map[int64,[]int64], sf singleflight.Group} (xsync/v4 + x/sync/singleflight, already vendored). ComputeStarts(seed int64, pos level.ChunkPos, gen StartGenerator) []*StructureStart — singleflight-deduped over the packed key, memoized; StartGenerator is an interface { GenerateStarts(seed int64, pos level.ChunkPos, sampler SurfaceSampler, biomeAt func(wx,wy,wz int) levelbiome.Type) []*StructureStart } 14-02's structures implement (the biomeAt func is the REAL biome-check seam — BLOCKER fix, no accept-by-default; this plan supplies a NO-OP/empty generator stub so the pipeline runs end-to-end with zero starts). ComputeReferences(seed, C, gen) the ±8 scan that for EACH scanned cell calls ComputeStarts(seed, cell, gen) (compute-on-demand, pure + singleflight-deduped — NOT a read of only already-cached starts, which would silently miss any start owned ≥2 chunks from C) and populates References from the intersecting owned starts. The scan radius and the compute radius are THE SAME 8. StartsForChunk(C) []*StructureStart = C's own Starts + the starts named in C's References (the gather PLACE will use). All cache reads/writes via xsync (the ONLY cross-goroutine state; pure memoization). Use world.packPos's packing (replicate the int64(p[0])<<32|int64(uint32(p[1])) form — document it matches the worker's).
world/structure/surface_sampler.go: SurfaceSampler interface { SampleSurfaceY(wx,wz int) int } + a routerSurfaceSampler{router *router.Router} impl computing floor(router.NoiseRouter.PreliminarySurfaceLevel.Compute(density.Context{X:(wx>>2)<<2, Y:0, Z:(wz>>2)<<2})). Cite the Pitfall #9 decision inline (preliminary surface, no chunk fill, vanilla getBaseHeight).
world/structure/cache_test.go: TestComputeStartsSingleflight (concurrent same-(seed,pos) → one computation, identical results, -race); TestComputeStartsPure (same inputs → identical starts); TestReferences8Radius (a start owned by a chunk at distance ≥2 (NOT just radius-1) with a bbox reaching C → C.References contains its owner key — proving compute-on-demand reaches the full ±8, not just the pre-cached ring; a start NOT reaching C → absent); TestBoundingBoxIntersects + TestWritableArea (the 16x16 column bounds); TestSampleSurfaceYDeterministic (same (x,z) → same Y, no chunk fill). Use a fake StartGenerator emitting a fixed bbox start so the cache+references are testable WITHOUT 14-02's geometry.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestComputeStarts|TestReferences|TestBoundingBox|TestWritableArea|TestSampleSurfaceY' -count=1 -race && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The world/structure cache holds singleflight-deduped pure-memoized Starts + the 8-radius References scan computes starts on-demand per scanned cell (not read-only-cached) so it finds neighbor starts reaching C from the FULL ±8 (incl. ≥2-chunk-out owners, future-proofing Phase-15 mineshafts); BoundingBox Intersects/WritableArea + the StructureStart type are ready for 14-02's pieces; the SampleSurfaceY column sampler returns the router's preliminary surface with no chunk fill — all -race clean.</done>
</task>

<task type="auto">
  <name>Task 3: the two-pass worker seam — STARTS+REFERENCES for C and its 8 neighbors after carve, the placeStructures hook (empty), the determinism gates stay green</name>
  <files>world/noisegen.go, world/worker.go, world/worker_structure_test.go</files>
  <action>
First re-read world/worker.go (tryDecorate/processRing — the scheduler-goroutine path) + world/noisegen.go (Decorate) for the AS-BUILT shape.
world/noisegen.go: give NoiseGenerator a *structure.Cache (built in NewNoiseGenerator, one per world) + the routerSurfaceSampler (over g.router) + a biome-lookup seam threaded from g.biomes (the existing *biome.MultiNoiseBiomeSource — its GetBiome(x,y,z) levelbiome.Type is public) into the StartGenerator context (define the StartGenerator interface to take BOTH a SurfaceSampler AND a BiomeAt(wx,wy,wz) levelbiome.Type func, so 14-02/14-03 temples gate on a REAL biome test — NO accept-by-default) + the StartGenerator set (an EMPTY/no-op set this plan — 14-02 registers the desert pyramid). At the END of Decorate (AFTER applyBiomeDecoration — vanilla FEATURES order, so structures overwrite terrain+features): ensure STARTS are computed+cached for the center C (cache.ComputeStarts(g.seed, C, gen) — pure geometry, singleflight-deduped, NO block writes), then compute REFERENCES for C (cache.ComputeReferences(g.seed, C, gen) — which itself computes-on-demand the STARTS for every cell in C's ±8 scan, so the radius-8 references read radius-8 data, not radius-1; the per-cell ComputeStarts is pure + singleflight-deduped so it is cheap + order-independent), then call an EMPTY placeStructures(view, C) hook (a method that 14-02 fills: gather StartsForChunk(C), placeInChunk each with C's writable box). Document the two-pass ordering inline (fill→surface→carve→STARTS(C)→REFERENCES(C) [which on-demand-computes STARTS over C's ±8 scan]→PLACE(C)→finish). Because the StartGenerator is empty + placeStructures is empty, ZERO blocks are written — the chunk bytes stay identical to Phase 13.
world/worker.go: the STARTS+REFERENCES run inside Decorate (called from tryDecorate on the scheduler goroutine) so NO worker structural change is needed beyond confirming the single-owner path is preserved — but VERIFY + document that the cache (the only cross-goroutine state) is the xsync.Map memoization and the ±8 on-demand STARTS computes are pure geometry (the surface sampler reads the router, NOT the chunk) so they do NOT trigger neighbor chunk GENERATION. If a minimal hook is needed in the scheduler to thread the cache, add it minimally; do NOT touch the ChunkResult handoff / Generator interface / chunk wire.
world/worker_structure_test.go: TestStartsCachedForNeighborhood (decorating C populates STARTS for C + every cell in its ±8 references scan in the cache, pure (seed,pos) — incl. a ≥2-chunk-out cell, proving the references compute-on-demand reaches the full radius); TestReferencesPopulatedOnDecorate (C's References reflect a fake neighbor start); TestStructurePipelineNoBlocksYet (with the empty StartGenerator, the decorated chunk bytes are IDENTICAL to a no-structure-pipeline run — the pipeline is wired but inert). Then assert the EXISTING TestDecorationReorderIdentical (5x5) + TestEmitOnce STILL pass byte-identical with STARTS computing. Run Docker -race.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/ -run 'TestStartsCachedForNeighborhood|TestReferencesPopulatedOnDecorate|TestStructurePipelineNoBlocksYet|TestDecorationReorderIdentical|TestEmitOnce' -count=1 && go build ./... && CGO_ENABLED=0 go build ./... && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... ./world/structure/...</automated>
  </verify>
  <done>The two-pass seam is wired: Decorate computes+caches STARTS for C + REFERENCES for C (which on-demand-computes STARTS over the full ±8 scan, pure + singleflight-deduped, no neighbor chunk generation, no block writes) + an empty placeStructures hook 14-02 fills; the chunk bytes stay identical to Phase 13 and the 5x5 reorder-determinism + emit-once + Docker -race gates stay green — the pipeline exists, tested, inert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → placement math | The structure/structure_set JSON is the pinned 26.2 jar (pure-unzipped); the placement constants (salts/spacing) are cross-checked against the embedded JSON. No client input reaches the STARTS computation — the only inputs are (worldSeed, chunkPos), both server-derived. |
| STARTS cache → worker scheduler goroutine | The xsync.Map cache is the ONLY cross-goroutine state; it is a PURE memoization (same (seed,pos) → same start), singleflight-deduped. STARTS/REFERENCES run on the single scheduler goroutine; the cache crossing is a read-mostly concurrent map, not shared mutable game state. |
| surface sampler → router | SampleSurfaceY reads the immutable bound router (NewRouter is pure per seed); no chunk, no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | getPotentialStructureChunk draw-order / floorDiv divergence (wrong start positions vs a vanilla seed) | mitigate | The placement math is transcribed javap -c + pinned by TestGetPotentialStructureChunk (the exact start chunk + draw sequence for LINEAR + TRIANGULAR derived from the algorithm) + TestFloorDivNegative; a reorder or a Go-truncating `/` fails the golden. |
| T-14-02 | Tampering | wrong SpreadType draw COUNT (TRIANGULAR's two draws) desyncing the whole later piece stream | mitigate | TRIANGULAR.evaluate is transcribed with the exact two-draw form; the golden asserts the post-evaluate rng fingerprint so an extra/missing draw fails. |
| T-14-03 | Integrity (non-reproducible world) | the STARTS cache returning different starts for the same (seed,pos) under concurrency | mitigate | ComputeStarts is pure over (seed,pos) + singleflight-deduped; TestComputeStartsSingleflight (-race) pins one-computation-identical-results; the xsync.Map is read-mostly memoization, no mutable shared state. |
| T-14-04 | Tampering | the 8-radius References scan missing a neighbor start (truncated cross-chunk structures, LIVE the moment Phase-15 multi-chunk mineshafts reuse this) | mitigate | ComputeReferences COMPUTES STARTS ON-DEMAND per scanned cell (ComputeStarts(seed,cell,gen), pure + singleflight-deduped) so the radius-8 scan reads radius-8 data, NOT only the radius-1 pre-cached ring; TestReferences8Radius pins a start owned ≥2 chunks out (beyond radius 1) reaching C is found + one not reaching C is absent; the scan radius and compute radius are the SAME 8. |
| T-14-05 | Integrity | the STARTS heightmap sampler reading placed blocks (a STARTS→Generate cycle / wrong height vs vanilla) | accept | SampleSurfaceY samples the router's PRELIMINARY surface (one density compute, no chunk), exactly vanilla's getBaseHeight — NOT placed blocks. Documented inline; no cycle, no block read. The preliminary-vs-final divergence is the same one vanilla's structure placement uses. |
| T-14-06 | Tampering | the structure pipeline perturbing the byte-stable chunk output (a determinism regression before any geometry) | mitigate | STARTS is pure geometry (no SetBlock); the placeStructures hook is empty; TestStructurePipelineNoBlocksYet asserts byte-identity vs a no-pipeline run + the 5x5 reorder + emit-once gates re-run green. The Docker -race gate proves the cache crossing is clean. |
</threat_model>

<verification>
- `go test ./world/structure/ -run 'TestGetPotentialStructureChunk|TestFloorDiv|TestIsStructureChunk|TestProbabilityReducers|TestStructureSetJSON' -count=1` green: placement math bit-exact, structure_set JSON embedded + salts match.
- `go test ./world/structure/ -run 'TestComputeStarts|TestReferences|TestBoundingBox|TestWritableArea|TestSampleSurfaceY' -count=1 -race` green: the cache (pure+singleflight), the 8-radius references, the bbox helpers, the column sampler — race-clean.
- `go test ./world/ -run 'TestStartsCachedForNeighborhood|TestReferencesPopulatedOnDecorate|TestStructurePipelineNoBlocksYet|TestDecorationReorderIdentical|TestEmitOnce' -count=1` green: STARTS+REFERENCES wired into Decorate, ZERO blocks placed, the 5x5 reorder + emit-once stay byte-identical.
- Docker `-race` over `./world/... ./world/structure/...` clean (CGO golang:1.26 — the intended race-gate toolchain; the go.mod targets the go 1.25.0 floor and golang:1.26 is a strict superset, so no go.mod bump is needed).
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; NO new deps (xsync/v4 + singleflight already vendored — grep go.mod); NO encoder/packet/chunk-wire file touched.
</verification>

<success_criteria>
- STRUCT-01 (pipeline half): the two-phase pipeline EXISTS — STRUCTURE_STARTS (the placement math + a pure singleflight-memoized StructureStart cache in a new world/structure package keyed by packed chunk pos), STRUCTURE_REFERENCES (the ±8 bbox-intersect scan), and the PLACE HOOK (the empty placeStructures call site 14-02 fills) — wired into the worker's two-pass seam after carve.
- The placement math is bit-exact: getPotentialStructureChunk (floorDiv + the already-ported SetLargeFeatureWithSalt + LINEAR/TRIANGULAR draw counts), the spread types, the four probabilityReducer variants, isStructureChunk — pinned by a golden derived from the algorithm.
- The heightmap-at-STARTS column sampler is exposed (the router's preliminary surface, no owner chunk — Pitfall #9, decision documented), and the two-pass worker seam (fill→surface→carve→STARTS(C+8)→REFERENCES(C)→PLACE-hook→finish) lands with ZERO blocks placed — the 5x5 reorder-determinism + emit-once + Docker -race gates stay green. The integration risk is isolated + tested before any structure geometry.
</success_criteria>

<output>
After completion, create `.planning/phases/14-structure-pipeline-temples/14-01-SUMMARY.md`
</output>
