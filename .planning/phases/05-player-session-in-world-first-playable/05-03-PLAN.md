---
phase: 05-player-session-in-world-first-playable
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - server/play_join_capture_test.go
  - .planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md
  - .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-player-info-update.bin
  - .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-set-default-spawn-position.bin
  - .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-forget-level-chunk.bin
autonomous: false
requirements: [PLAY-01, PLAY-02, PLAY-05, PLAY-06]
user_setup: []

must_haves:
  truths:
    - "A capture-diff test (server/play_join_capture_test.go) byte-compares Sulfur's ClientboundPlayerInfoUpdate (self-entry), ClientboundSetDefaultSpawnPosition (RespawnData/GlobalPos), and ClientboundForgetLevelChunk encoders against real vanilla 26.2 server packets captured from temp/cache/26.2-server.jar"
    - "The vanilla golden packets are captured and committed as fixtures so CI byte-diffs without booting Java every run; the test skips gracefully with a clear message if a fixture is absent"
    - "05-CAPTURE-DIFF.md records the capture method, the per-field diff (PlayerInfoUpdate 8-action mask + entry sub-encoding incl. the property-list count-prefix; SetDefaultSpawnPosition RespawnData GlobalPos field order; the jar-derived ForgetLevelChunk layout) and resolves the Open Questions (ForgetLevelChunk layout, PlayerInfoUpdate entry sub-encoding, SpawnPos/SetTime necessity, ServerboundPlayerLoaded requirement)"
    - "If a capture reveals a divergence, the fix is applied to the owning plan's encoder (server/play_join.go or world/packet.go) and the affected tests re-run before sign-off"
    - "An unmodified vanilla 26.2 client connects to cmd/sulfur, logs in, and WALKS AROUND: the view-distance ring FOLLOWS the player (no void at the edges), the player appears in its own tab list, and there is no kick/hang — the PLAY-06 first-playable milestone, confirmed by a human"
  artifacts:
    - path: "server/play_join_capture_test.go"
      provides: "TestPlayBytesVsVanillaCapture: the authoritative PLAY-01/02/05 byte-diff against vanilla golden fixtures"
      contains: "func TestPlayBytesVsVanillaCapture"
    - path: ".planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md"
      provides: "The vanilla 26.2 capture-diff record + the resolution of the Open Questions + the PLAY-06 human sign-off"
      contains: "PlayerInfoUpdate"
    - path: ".planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-player-info-update.bin"
      provides: "The committed golden vanilla ClientboundPlayerInfoUpdate self-entry packet"
      contains: ""
  key_links:
    - from: "server/play_join_capture_test.go"
      to: ".planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-player-info-update.bin"
      via: "load the golden vanilla packet and byte-diff Sulfur's encoder output"
      pattern: "vanilla-player-info-update"
    - from: "05-CAPTURE-DIFF.md"
      to: "the real vanilla 26.2 walk-around"
      via: "the human confirms the ring follows + tab list + no kick (PLAY-06)"
      pattern: "walk"
---

<objective>
Seal the uncertain Phase-5 wire layouts against the ONLY authoritative source for proto 776 — a byte-for-byte capture-diff of Sulfur's `ClientboundPlayerInfoUpdate`, `ClientboundSetDefaultSpawnPosition`, and `ClientboundForgetLevelChunk` against a real vanilla 26.2 server — and then confirm the PLAY-06 first-playable milestone: an unmodified vanilla 26.2 client connects, logs in, and WALKS AROUND a world whose ring FOLLOWS it, appears in the tab list, and is not kicked. This mirrors Phase 4's 04-04 gate: a self-round-trip cannot prove vanilla-correctness for a protocol no spec covers (the wiki documents ≤773; the PlayerInfoUpdate entry sub-encoding and the 26.x RespawnData/WorldClock restructures are jar-confirmed in shape but their exact bytes drift between versions). The capture-diff is the producer-side proof; the real-client walk-around is the consumer-side proof.

The executor FIRST does the automatable capture/diff work (stand up the cached vanilla jar, capture the three Play packets, commit them as golden fixtures, byte-diff against Sulfur's encoders, write 05-CAPTURE-DIFF.md, resolve the Open Questions, apply any fix to the owning plan's encoder and re-run), THEN PAUSES for a BLOCKING human-verify checkpoint: the user runs an unmodified vanilla 26.2 client against cmd/sulfur and confirms walk-around / ring-follows / tab-list / no-kick.

Purpose: PLAY-01/02/05 wire-correctness (the uncertain encoders) and PLAY-06 (the milestone) are the phase's highest-risk requirements — "first playable" is only true when a real client renders, walks, and is listed. This plan is autonomous:false because the decisive check — a real client walking around a following world — cannot be self-approved.
Output: server/play_join_capture_test.go (the golden byte-diff), the committed vanilla golden .bin fixtures, and a signed-off 05-CAPTURE-DIFF.md resolving the Open Questions. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-player-session-in-world-first-playable/05-RESEARCH.md
@.planning/phases/05-player-session-in-world-first-playable/05-VALIDATION.md
@.planning/phases/05-player-session-in-world-first-playable/05-01-PLAN.md
@.planning/phases/05-player-session-in-world-first-playable/05-02-PLAN.md
@.planning/phases/04-world-chunk-system/04-04-PLAN.md
@.planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md

<interfaces>
<!-- VERIFIED this session. The capture-diff consumes the encoders from Plans 05-01/05-02. -->

From server/play_join.go (Plan 05-02 — the encoders under test):
```go
func writePlayerInfoUpdateAdd(id uuid.UUID, name string, gameMode int32) pk.Packet // the self tab-list entry
func writeSetDefaultSpawnPosition(...) pk.Packet                                    // RespawnData/GlobalPos
func writePlayerAbilities(invuln, flying, canFly, instabuild bool, flySpeed, walkSpeed float32) pk.Packet
func writeSetHeldSlot(slot int32) pk.Packet
```

From world/packet.go (Plan 05-01 — the jar-derived encoder under test):
```go
func ForgetLevelChunk(cx, cz int32) pk.Packet // jar-derived 26.2 layout
```

From the bot harness + the Phase-4 capture method (reuse — VERIFIED present):
```go
// Point the fork bot/ client at the VANILLA server (offline login -> config -> play), capture the raw bytes
// of ClientboundPlayerInfoUpdate / ClientboundSetDefaultSpawnPosition / ClientboundForgetLevelChunk.
// Alternatively use a logging TCP proxy between a vanilla client and the vanilla server.
// The exact method is documented in .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md.
```

Environment (VERIFIED available per 05-RESEARCH Environment Availability):
```
temp/cache/26.2-server.jar  (sha1 823e2250…, the real vanilla 26.2 server)
temp/cache/26.2-inner.jar   (javap source for any remaining jar-derive)
Java (Zulu/Temurin) 25      (run the vanilla server; javap)
golang:1.26 Docker          (-race, host CGO_ENABLED=0)
```
</interfaces>

<key_facts>
<!-- VERIFIED against 05-RESEARCH this session. -->
- THE AUTHORITATIVE GATE (same lesson as Phase 2 NET-04 + Phase 4 WORLD-02/03): a Go self-round-trip CANNOT prove the uncertain encoders are vanilla-correct. The PlayerInfoUpdate entry sub-encoding (the property-list count-prefix), the 26.x SetDefaultSpawnPosition (RespawnData = GlobalPos + Float + Float), and the ForgetLevelChunk layout are jar-confirmed in SHAPE but their exact bytes are MEDIUM-confidence (05-RESEARCH Assumptions A3/A4/A5). Only a byte-diff vs the real vanilla 26.2 server proves them. (PlayerAbilities + SetHeldSlot are HIGH-confidence jar-confirmed simple layouts — diff them too if captured, but they are not the risk.)
- Capture method (mirror Phase 4 WORLD-CAPTURE-DIFF.md): boot temp/cache/26.2-server.jar offline + flat with a fixed seed/spawn so the captured packets are deterministic; capture the join-time ClientboundPlayerInfoUpdate (the server's own self/op entry), ClientboundSetDefaultSpawnPosition, and a ClientboundForgetLevelChunk (walk to force a chunk drop, or move the spawn) — via the fork bot/ client pointed at vanilla (dump raw packet bytes) OR a logging proxy. Commit each as a golden .bin under fixtures/.
- Sulfur side: produce the same packets with the same inputs (same UUID/name/gameMode for PlayerInfoUpdate; same spawn for SetDefaultSpawnPosition; same chunk coords for ForgetLevelChunk) and byte-diff. NOTE: exact byte-equality for PlayerInfoUpdate requires matching vanilla's action set + entry order; if vanilla's join entry carries MORE actions than Sulfur's minimal ADD_PLAYER|UPDATE_GAME_MODE|UPDATE_LISTED, diff the FRAMING (1-byte action mask, count-prefix, per-action body order) and document the action-set difference — the load-bearing assertions are the mask width (1 byte for ≤8 actions), the count-prefix shape, and the per-action field order, which are what cause a client reject.
- Test TestPlayBytesVsVanillaCapture: load each fixture, parse it, and assert Sulfur's encoder produces a structurally-matching packet (action mask is 1 byte; the collection is VarInt-count-prefixed; the ADD_PLAYER entry has String name + VarInt(0) property count; UPDATE_GAME_MODE/UPDATE_LISTED bodies present in enum order; SetDefaultSpawnPosition's RespawnData GlobalPos = dimension Identifier + BlockPos Long + two Floats; ForgetLevelChunk matches the jar-derived layout). t.Skip with a message pointing at 05-CAPTURE-DIFF.md if a fixture is absent (native CI stays green; the capture is the gate).
- Resolve the Open Questions IN 05-CAPTURE-DIFF.md from the capture/real-client run:
    1. ClientboundForgetLevelChunk 26.2 layout — confirmed jar-derived in Plan 05-01 Task 0; the capture-diff is the byte-level confirmation.
    2. PlayerInfoUpdate entry sub-encoding (property-list Boolean signed + optional String signature) — confirm the count-prefix shape matches even with 0 properties.
    3. SetDefaultSpawnPosition / SetTime necessity — the real-client test is the arbiter: if the client kicks WITHOUT SetDefaultSpawnPosition, keep it (capture-diff'd); if it kicks without SetTime, ADD SetTime (capture-diff it then) — otherwise leave SetTime out (A2).
    4. ServerboundPlayerLoaded requirement — confirm the client walks without the server blocking on it (routed as a no-op in Plan 05-01); note if withholding any clientbound packet until it arrives matters.
- If ANY divergence is found, fix it in the OWNING plan's encoder (server/play_join.go for the tail; world/packet.go for ForgetLevelChunk), re-run that plan's wire tests + this capture-diff, and record the fix in 05-CAPTURE-DIFF.md before sign-off.
- THE PLAY-06 real-client walk-around (the human-verify): connect an unmodified vanilla 26.2 client (PrismLauncher) to cmd/sulfur. WALK >2 chunks in each direction and confirm the ring FOLLOWS (new chunks appear at the edges; no void; no "walking off the world" — this is Plan 05-01's recenterRing working end-to-end). Open the tab list and confirm the player's name is listed (Plan 05-02's PlayerInfoUpdate). Confirm NO kick / no "Loading terrain…" hang. This is the decisive PLAY-04/05/06 check; it cannot be self-approved.
- ZERO new dependencies. Pure stdlib testing + the existing bot/ harness + the cached jar.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/play_join_capture_test.go (package server) + the fixtures/ dir + .bin files. Write 05-CAPTURE-DIFF.md. If a fix is needed, make a precise symbol-level edit in the owning encoder and re-run. Read 04-04-PLAN.md's Task 2 for the exact capture-diff + human-verify checkpoint shape to mirror.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sulfur Play-packet encoders → vanilla 26.2 client | The client silently mis-renders or rejects (kicks on) a Play packet whose byte framing diverges from vanilla. The only authoritative validation is a byte-diff vs the real server + a real-client run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07 | Tampering (data integrity) | A wire-framing divergence (wrong action-mask width, missing property count-prefix, wrong RespawnData/GlobalPos order, wrong ForgetLevelChunk layout) that self-round-trips green but the client rejects | mitigate | Byte-diff against the real vanilla 26.2 server output (the golden fixtures) + the real-client walk-around — the framing assertions (1-byte mask, count-prefix, per-action body order, GlobalPos order, jar-derived forget) are exactly the divergences that kick the client. Any divergence is fixed in the owning plan and re-diffed before sign-off. |
| T-5-04 | Denial of Service | Decode of a vanilla golden packet over-allocating from an attacker-controlled length | accept | The golden fixtures are trusted committed artifacts captured from the real vanilla server (not attacker input); parsing them uses bounded pk.Scan with no unbounded make-before-check. Low risk for trusted test fixtures. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture vanilla 26.2 Play packets + the byte-diff test (automatable prep)</name>
  <files>server/play_join_capture_test.go, .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-player-info-update.bin, .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-set-default-spawn-position.bin, .planning/phases/05-player-session-in-world-first-playable/fixtures/vanilla-forget-level-chunk.bin, .planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md</files>
  <read_first>
    - .planning/phases/05-player-session-in-world-first-playable/05-RESEARCH.md — the Capture-diff harness Code Example, Pitfalls 4-6, the Open Questions, Environment Availability.
    - .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md — the proven capture method to mirror (the exact server.properties, the bot/proxy dump, the golden .bin commit).
    - .planning/phases/04-world-chunk-system/04-04-PLAN.md — Task 1 (the automatable capture-diff prep) + Task 2 (the human-sign-off shape).
    - server/play_join.go + world/packet.go — the encoders under test (Plans 05-02 / 05-01).
  </read_first>
  <behavior>
    PLAY-01/02/05 (authoritative byte-diff — automatable portion):
    - Capture: boot temp/cache/26.2-server.jar offline + flat + fixed seed; capture ClientboundPlayerInfoUpdate (the join self/op entry), ClientboundSetDefaultSpawnPosition, and ClientboundForgetLevelChunk; commit each raw packet as a golden .bin under fixtures/.
    - Test TestPlayBytesVsVanillaCapture: load each fixture; parse + assert the vanilla framing (PlayerInfoUpdate: 1-byte action mask, VarInt-count collection, ADD_PLAYER entry = UUID + String name + VarInt(0) properties, per-action bodies in enum order; SetDefaultSpawnPosition: RespawnData GlobalPos = dimension Identifier + BlockPos Long + two Floats; ForgetLevelChunk: the jar-derived layout). Then generate Sulfur's packet for the same inputs and assert the SAME framing. Skip with a clear message if a fixture is absent.
  </behavior>
  <action>
    1. Capture (document every step in 05-CAPTURE-DIFF.md so it is reproducible — mirror WORLD-CAPTURE-DIFF.md):
       - Start the vanilla server: `java -jar temp/cache/26.2-server.jar` in a temp dir with server.properties = flat level-type, fixed level-seed, online-mode=false, eula accepted. Record the exact server.properties.
       - Capture the three packets: point the fork bot/ client at the vanilla server (offline login → config → play) and dump the raw bytes of ClientboundPlayerInfoUpdate (the join-time entry), ClientboundSetDefaultSpawnPosition, and a ClientboundForgetLevelChunk (move the player / view to force a chunk drop). Save each to fixtures/vanilla-player-info-update.bin, fixtures/vanilla-set-default-spawn-position.bin, fixtures/vanilla-forget-level-chunk.bin (document whether the id byte is included).
    2. Write server/play_join_capture_test.go (package server): TestPlayBytesVsVanillaCapture as in <behavior>. Load each fixture with os.ReadFile (t.Skip if missing). Parse + assert the vanilla framing, then assert Sulfur's encoder matches the framing. Keep the assertions on the load-bearing framing (mask width, count-prefix, per-action order, GlobalPos order, forget layout); if vanilla's join entry carries a different action SET than Sulfur's minimal one, document it and assert the framing, not byte-equality of the differing actions.
    3. Begin 05-CAPTURE-DIFF.md: record the capture method, the exact server.properties/seed/inputs, the per-field diff table, and a section resolving the Open Questions (ForgetLevelChunk layout confirmed; PlayerInfoUpdate property count-prefix; SpawnPos/SetTime necessity — deferred to the Task-2 real-client run; ServerboundPlayerLoaded). If a divergence is found, FIX it in the owning plan's encoder and re-run that plan's wire tests + this test; record the fix.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestPlayBytesVsVanillaCapture|TestPlayerInfoUpdateWire|TestSetDefaultSpawnPositionWire' -count=1 && go test ./world/ -run 'TestForgetLevelChunkWire' -count=1 && go vet ./... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The three golden fixtures are captured from the real vanilla 26.2 server and committed.
    - TestPlayBytesVsVanillaCapture loads the fixtures and asserts Sulfur's encoders match the vanilla framing (1-byte action mask, count-prefix, per-action order; RespawnData GlobalPos order; jar-derived forget); it skips cleanly if a fixture is absent.
    - 05-CAPTURE-DIFF.md records the reproducible capture method, the per-field diff, and the resolution of the Open Questions (SpawnPos/SetTime necessity deferred to the real-client run); any divergence found is fixed in the owning plan and re-diffed.
    - `go build ./...`, `go vet ./...` clean; zero new deps.
  </acceptance_criteria>
  <done>Sulfur's PlayerInfoUpdate / SetDefaultSpawnPosition / ForgetLevelChunk wire is byte-diffed against a real vanilla 26.2 server: the golden fixtures are committed, the diff test asserts the vanilla framing, the Open Questions are resolved (SpawnPos/SetTime deferred to the real-client run), and any divergence is fixed — the automatable half of the authoritative PLAY-01/02/05 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real vanilla 26.2 client WALKS AROUND + human sign-off (PLAY-04/05/06 milestone gate)</name>
  <files>.planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md</files>
  <action>
    This is a human-verification checkpoint, not an autonomous code task. The capture-diff (Task 1) proves the bytes match vanilla; this proves a real client WALKS AROUND a following world and is listed — the decisive PLAY-04/05/06 milestone that cannot be self-approved (mirrors Phase 4's 04-04 / WORLD-05 sign-off). The executor must FIRST ensure Task 1 is complete (fixtures committed, diff green, Open Questions resolved, any divergence fixed and re-diffed), finalize 05-CAPTURE-DIFF.md, then PAUSE and present it. Do not self-approve.

    Automatable preparation (do before pausing):
    1. Confirm Task 1's TestPlayBytesVsVanillaCapture is green and 05-CAPTURE-DIFF.md resolves the Open Questions with any divergence fixed + re-diffed.
    2. Run the full suite + the Docker -race gate over ./server/... ./world/... to confirm the movement/tick/streaming seam is race-clean under load (Plans 05-01/05-02).
    3. Run cmd/sulfur (which now: sends the early-Play tail before register, gates movement on the confirmed teleport id, decodes movement into tick-owned position, and re-centers the ring on a boundary crossing). Confirm it builds and listens on proto 776.
  </action>
  <what-built>
    The full Phase 5 player session: movement decode (the jar-confirmed packed flags Byte, not a Boolean) into tick-owned position with the teleport-id gate (Plan 05-01), the ring re-center so the world FOLLOWS the walking player (Plan 05-01 recenterRing + the jar-derived ForgetLevelChunk), and the early-Play tail — PlayerAbilities → SetHeldSlot → PlayerInfoUpdate(self tab-list) → SetDefaultSpawnPosition — with the incrementing teleport id (Plan 05-02). 05-CAPTURE-DIFF.md + the golden fixtures are the audit artifacts proving Sulfur's uncertain Play encoders match a real vanilla 26.2 server's — the authoritative correctness check for a protocol no spec covers.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/05-player-session-in-world-first-playable/05-CAPTURE-DIFF.md.
    2. Confirm the per-field diff shows Sulfur's encoders match vanilla framing: PlayerInfoUpdate's 1-byte action mask + count-prefixed self-entry (UUID/name/0-properties/gameMode/listed in enum order); SetDefaultSpawnPosition's RespawnData GlobalPos order; the jar-derived ForgetLevelChunk layout.
    3. Confirm the Open Questions are resolved (ForgetLevelChunk layout; PlayerInfoUpdate property count-prefix; whether SetDefaultSpawnPosition/SetTime are needed; ServerboundPlayerLoaded routed as a no-op without blocking streaming).
    4. The decisive check — connect an UNMODIFIED vanilla 26.2 client (PrismLauncher) to cmd/sulfur and confirm:
       a. WALK AROUND: move >2 chunks in each direction; new chunks appear at the edges and the ring FOLLOWS — NO void at the edges, NO "walking off the world" (Plan 05-01's recenterRing working end-to-end).
       b. TAB LIST: open the player list and confirm the player's name is listed (Plan 05-02's PlayerInfoUpdate self-entry).
       c. NO KICK / NO HANG: the client does not get kicked (malformed packet) and does not hang on "Loading terrain…".
  </how-to-verify>
  <verify>Human sign-off — the user types the resume signal after confirming the capture-diff and that a real client walks around a following world, appears in the tab list, and is not kicked.</verify>
  <done>The user has reviewed 05-CAPTURE-DIFF.md, confirmed Sulfur's uncertain Play encoders match vanilla 26.2 (PlayerInfoUpdate mask + entry, SetDefaultSpawnPosition RespawnData, ForgetLevelChunk), and confirmed an unmodified vanilla 26.2 client connects, logs in, WALKS AROUND a world whose ring follows it, appears in its own tab list, and is not kicked — then typed "approved". The PLAY-01..06 first-playable milestone is met.</done>
  <resume-signal>Type "approved" if the capture-diff matches vanilla and a real client walks around a following world (ring follows, tab list shows the player, no kick), or describe the divergence (ring does not follow / void at edges, player missing from tab list, kick on join citing a malformed packet, walking off the world) to fix.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestPlayBytesVsVanillaCapture' -count=1` + `go test ./world/ -run 'TestForgetLevelChunkWire' -count=1` pass (the byte-diff asserts the vanilla framing; the jar-derived forget round-trips).
- The three golden fixtures are committed; 05-CAPTURE-DIFF.md records the diff + resolves the Open Questions + is signed off.
- An unmodified vanilla 26.2 client connected to cmd/sulfur WALKS AROUND a world whose ring follows it, appears in the tab list, and is not kicked (human-confirmed).
- Docker `-race` over `./server/... ./world/...` clean; `go build ./...` clean; any divergence found in the diff was fixed in the owning plan and re-diffed; zero new dependencies.
</verification>

<success_criteria>
- PLAY-01/02/05 (authoritative): Sulfur's PlayerInfoUpdate / SetDefaultSpawnPosition / ForgetLevelChunk bytes match a real vanilla 26.2 server — the action mask, the entry count-prefix + per-action order, the RespawnData GlobalPos order, and the jar-derived forget layout all align (the framing that kicks the client is correct).
- PLAY-04/06 (real-client milestone): an unmodified vanilla 26.2 client connects, logs in, and WALKS AROUND a world whose view-distance ring FOLLOWS it (no void at the edges), appears in its own tab list, and is not kicked — the first-playable milestone.
- The Open Questions are resolved from the capture + real-client run and recorded (ForgetLevelChunk layout, PlayerInfoUpdate entry sub-encoding, SpawnPos/SetTime necessity, ServerboundPlayerLoaded); any divergence is fixed in the owning plan and re-diffed before sign-off.
- The capture-diff + real-client walk-around are the authoritative gate (a self-round-trip is insufficient by design — the Phase-2/4 symmetric-bug lesson).
</success_criteria>

<output>
After completion, create `.planning/phases/05-player-session-in-world-first-playable/05-03-SUMMARY.md`.
</output>
