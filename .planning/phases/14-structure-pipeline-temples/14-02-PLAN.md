---
phase: 14-structure-pipeline-temples
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - world/structure/piece.go
  - world/structure/piece_test.go
  - world/structure/place.go
  - world/structure/place_test.go
  - world/structure/desert_pyramid.go
  - world/structure/desert_pyramid_test.go
  - world/noisegen.go
  - world/worker_structure_test.go
requirements: [STRUCT-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The StructurePiece bounding-box-tree machinery is ported (javap -c net.minecraft.world.level.levelgen.structure.StructurePiece + ScatteredFeaturePiece against temp/cache/26.2-inner.jar): a Go Piece interface { BoundingBox() BoundingBox; PostProcess(view, writableBox, chunkPos, rng) } + a base StructurePiece struct holding the bbox + orientation/rotation/mirror + genDepth; addChildren(parent, accessor, rng) recursion (single-piece temples barely recurse — the hook + findCollisionPiece(list,box) collision check exist for Phase-15 mineshaft reuse); the block helpers placeBlock (local→world via getWorldPos applying rotation/mirror/orientation, then CLIP to the piece bbox AND the writableBox via box.IsInside, then SetBlock — the cross-chunk clip), generateBox/generateAirBox (fill a sub-box), generateMaybeBox (probabilistic), fillColumnDown (place down until a solid block / the world floor), maybeGenerateBlock (per-cell probability), createChest (chest block + a loot-table TAG marker — LOOT DEFERRED to v3, documented). Rotation/mirror transform the local coords AND the placed block state (state.Rotate/Mirror — temples are NONE-rotation so the identity path is exercised; the transform machinery is ported for Phase-16 jigsaw reuse)."
    - "StructureStart.placeInChunk is ported (javap -c StructureStart.placeInChunk): for a start touching chunk C, iterate ALL pieces, and for each piece whose BoundingBox Intersects C's writable box (the 16x16xH column from 14-01's WritableArea), call piece.PostProcess(view, writableBox, C.pos, rng) — which writes blocks CLIPPED to the writableBox via placeBlock's box.IsInside guard. A multi-chunk structure is placed ONCE PER OVERLAPPING CHUNK, each call writing only that chunk's 16x16 slice, IDEMPOTENT (the placement RNG is re-derivable from (worldSeed,chunkX,chunkZ) via the already-ported SetLargeFeatureSeed, and writes are position-clipped). AfterPlace (the terrain-beard) is STUBBED (v3 deferral, documented)."
    - "The desert pyramid generates in vanilla positions as the pipeline shakedown (javap -c DesertPyramidStructure findGenerationPoint + DesertPyramidPiece — single piece, hardcoded geometry, 0 .nbt): the StartGenerator for the desert_pyramid structure_set decides WHERE via 14-01's getPotentialStructureChunk (salt 14357617, spacing 32, separation 8) + a REAL biome check at the chosen point (biomeAt(originX, surfaceY, originZ) ∈ the embedded has_structure/desert_pyramid allow-set = {minecraft:desert}; a non-desert origin → NO start — vanilla behavior, NO accept-by-default), samples the surface Y via 14-01's SampleSurfaceY (project_start_to_heightmap WORLD_SURFACE_WG), builds a DesertPyramidPiece at that origin with its bbox, and wraps it in a StructureStart cached by 14-01's pipeline; DesertPyramidPiece.PostProcess writes the FULL hardcoded pyramid geometry (the sandstone/chiseled-sandstone/cut-sandstone stepped pyramid + the orange/blue terracotta floor mosaic + the hidden TNT-trap chamber with the 4 chests + the pressure-plate trigger — chests as blocks + tag, TNT as the tnt block, loot deferred) via generateBox/placeBlock, JAR-EXACT block-by-block + draw order. A known worldSeed → a desert pyramid at the expected chunk (derived FROM the placement algorithm) with the expected blocks (a fingerprint of the placed StateIDs)."
    - "The cross-chunk references+clip works end-to-end: a desert pyramid whose bbox spans two chunks places IDEMPOTENTLY — once per overlapping chunk, each chunk getting exactly its 16x16 slice (the same piece placed from C's PLACE pass and from N's PLACE pass produces identical, non-duplicated blocks because placeBlock clips to each chunk's writableBox + the piece RNG is re-derivable). A test placing a chunk-edge-spanning pyramid from both overlapping chunks asserts the union is the complete pyramid + no double-writes / no truncation (Pitfall #2)."
    - "STARTS is pure over (seed,pos): the desert pyramid's start (which chunk owns it, the piece origin, the bbox) is identical regardless of which chunk's decoration computed it first; and with the desert pyramid now PLACING blocks, the Phase-11 5x5 reorder-determinism (TestDecorationReorderIdentical) + emit-once gates STAY byte-identical (the cross-chunk clip + re-derivable RNG make placement order-independent), and the Docker -race gate (CGO golang:1.26) over ./world/... ./world/structure/... is clean."
  artifacts:
    - path: "world/structure/piece.go"
      provides: "the Piece interface + base StructurePiece (bbox/orientation/rotation/mirror/genDepth) + addChildren/findCollisionPiece + placeBlock(clip to bbox+writableBox)/generateBox/generateAirBox/generateMaybeBox/fillColumnDown/maybeGenerateBlock/createChest(block+tag, loot deferred) + the rotation/mirror transform"
      contains: "func.*placeBlock"
      min_lines: 120
    - path: "world/structure/place.go"
      provides: "StructureStart.placeInChunk (the per-chunk piece loop, intersect-then-PostProcess, clip-to-writableBox) + the AfterPlace stub + the placeStructures(view, C) driver gathering StartsForChunk(C)"
      contains: "func.*placeInChunk"
    - path: "world/structure/desert_pyramid.go"
      provides: "the desert_pyramid StartGenerator (getPotentialStructureChunk salt 14357617 + biome/surface check) + DesertPyramidPiece + its hardcoded PostProcess geometry (stepped sandstone pyramid + terracotta mosaic + TNT-trap chamber + 4 chests as blocks, loot deferred), registered into the NoiseGenerator's StartGenerator set"
      contains: "DesertPyramidPiece"
      min_lines: 250  # the stepped body + 2 towers + terracotta mosaic + TNT chamber + 4 chests is plausibly 250-400+ lines; min_lines is only a floor — the REAL completeness gate is the TestDesertPyramidPlaces block-fingerprint oracle (a truncated geometry fails the fingerprint, not the line count)
    - path: "world/structure/desert_pyramid_test.go"
      provides: "the shakedown + THE completeness gate: a known worldSeed → a desert pyramid at the EXPECTED chunk (derived from the placement algorithm) + the FULL expected block fingerprint (the oracle that catches truncated/wrong geometry — the real gate, not min_lines); the cross-chunk-span idempotence test"
      contains: "TestDesertPyramidPlaces"
    - path: "world/structure/place_test.go"
      provides: "placeInChunk intersect+clip unit tests (a piece spanning 2 chunks placed from each → exactly its slice, idempotent, no double-write/truncation)"
      contains: "TestPlaceInChunkCrossChunk"
  key_links:
    - from: "world/structure/desert_pyramid.go DesertPyramidPiece.PostProcess"
      to: "world/structure/piece.go placeBlock/generateBox"
      via: "the pyramid geometry is written via the base-piece block helpers, clipped to the writableBox"
      pattern: "placeBlock|generateBox"
    - from: "world/structure/place.go placeStructures(view, C)"
      to: "world/structure/cache.go StartsForChunk(C)"
      via: "the PLACE pass gathers C's own + referenced starts and placeInChunk-es each into C's writable box (the 14-01 hook, now filled)"
      pattern: "StartsForChunk"
    - from: "world/noisegen.go Decorate placeStructures hook (14-01, empty)"
      to: "world/structure/place.go placeStructures"
      via: "the 14-01 empty hook now dispatches the real PLACE pass at the end of Decorate (after applyBiomeDecoration — structures overwrite terrain+features)"
      pattern: "placeStructures"
    - from: "world/structure/desert_pyramid.go StartGenerator"
      to: "world/structure/placement.go getPotentialStructureChunk + surface_sampler SampleSurfaceY"
      via: "the desert pyramid decides WHERE via the salt-14357617 placement math + the preliminary-surface Y at STARTS time"
      pattern: "getPotentialStructureChunk|SampleSurfaceY"
---

<objective>
Land the FIRST true structure end-to-end (STRUCT-02): the `StructurePiece` bounding-box-tree machinery + `StructureStart.placeInChunk` + the **desert pyramid** as the pipeline shakedown, hung on 14-01's proven (and so-far inert) pipeline.

1. **The StructurePiece machinery**: the `Piece` interface + base `StructurePiece` (bbox/orientation/rotation/mirror/genDepth), `addChildren`/`findCollisionPiece` (the recursion hook + collision check — single-piece temples barely recurse, but Phase-15 mineshaft reuses it), and the block helpers — `placeBlock` (the cross-chunk clip: local→world via rotation/mirror, then `box.IsInside` against the piece bbox AND the chunk's writable box, then `SetBlock`), `generateBox`/`generateAirBox`/`generateMaybeBox`, `fillColumnDown`, `maybeGenerateBlock`, `createChest` (block + loot-table tag, loot deferred v3).

2. **`StructureStart.placeInChunk`**: for a start touching chunk C, loop all pieces, and for each whose bbox intersects C's writable box, `PostProcess` it — writing blocks clipped to C's 16×16 slice. A multi-chunk structure places once per overlapping chunk, idempotent (re-derivable RNG + position-clipped writes). This fills 14-01's empty `placeStructures` hook.

3. **The desert pyramid shakedown**: the `desert_pyramid` `StartGenerator` (salt 14357617 placement + desert biome check + the preliminary-surface Y at STARTS) + `DesertPyramidPiece` with its fully-hardcoded `PostProcess` geometry (stepped sandstone pyramid + terracotta mosaic + the TNT-trap chamber + 4 chests as blocks, loot deferred). It exercises STARTS→cache→REFERENCES→PLACE→postProcess end-to-end with ONE piece — the ideal shakedown. A known worldSeed → a pyramid at the expected chunk (derived from the algorithm) with the expected block fingerprint.

This is the first BLOCKS the structure pipeline places. The cross-chunk references+clip (Pitfall #2) and the order-independent determinism (the 5×5 reorder + emit-once gates stay byte-identical, now WITH a pyramid placing) are the load-bearing proofs.

Purpose: prove the whole pipeline on the simplest true structure before the cheap temple follow-ons (14-03).
Output: `world/structure/piece.go` + `place.go` + `desert_pyramid.go` + the shakedown/cross-chunk tests + the filled `placeStructures` hook. ZERO new deps, NO visual gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-structures.md
@.planning/phases/14-structure-pipeline-temples/14-01-PLAN.md

<interfaces>
<!-- 14-01's types are the contracts. Re-read the AS-BUILT world/structure/{placement,cache,start,surface_sampler}.go + world/neighborhood.go BEFORE coding. Trust `go build ./...` over gopls/Serena phantom errors. -->

THE 14-01 PIPELINE TYPES (world/structure/, the contracts this plan fills):
```go
// start.go (VERIFY the AS-BUILT field names)
type StructureStart struct { Pieces []Piece; BBox BoundingBox; ChunkPos level.ChunkPos; References int }
type BoundingBox struct { MinX,MinY,MinZ,MaxX,MaxY,MaxZ int }
func (b BoundingBox) Intersects(o BoundingBox) bool
func (b BoundingBox) IsInside(wx,wy,wz int) bool
func WritableArea(pos level.ChunkPos, minY, height int) BoundingBox  // the 16x16xH column
type Piece interface { BoundingBox() BoundingBox /* + PostProcess — this plan EXTENDS the interface */ }
// cache.go
func (c *Cache) StartsForChunk(C level.ChunkPos) []*StructureStart  // own + referenced
type StartGenerator interface { GenerateStarts(seed int64, pos level.ChunkPos, sampler SurfaceSampler, biomeAt func(wx,wy,wz int) levelbiome.Type) []*StructureStart }  // biomeAt = the REAL biome-check seam (14-01), satisfied by the NoiseGenerator's MultiNoiseBiomeSource.GetBiome
// placement.go
func getPotentialStructureChunk(worldSeed int64, chunkX,chunkZ, spacing,separation,salt int, st SpreadType) level.ChunkPos
func isStructureChunk(...) bool
// surface_sampler.go
type SurfaceSampler interface { SampleSurfaceY(wx,wz int) int }
```
NOTE: 14-01 declared Piece as a minimal `{ BoundingBox() BoundingBox }` placeholder so the cache compiled. THIS plan extends it to add `PostProcess(view *world.Neighborhood, box BoundingBox, chunkPos level.ChunkPos, rng levelgen.RandomSource)` — re-read the AS-BUILT 14-01 Piece + adjust. (If a world→world/structure import cycle threatens — the Neighborhood lives in package world — pass an interface (a WorldGenView { SetBlock/GetBlock }) the Neighborhood satisfies, defined in world/structure, so the dependency points world→world/structure only. DECIDE + document; the cache already lives in world/structure so this is the cleaner direction.)

THE WRITE PROXY (world/neighborhood.go, VERIFIED): SetBlock(wx,wy,wz, block.StateID) / GetBlock(wx,wy,wz) block.StateID — cross-chunk (blockStateWriteRadius=1, out-of-3x3 DROPPED) + heightmap-live. placeBlock writes ONLY through it (via the WorldGenView interface). The temple blocks are placed as block.StateIDs from block.ToStateID.

THE SEED HELPERS (world/levelgen/random.go, VERIFIED Phase 10): the PIECE rng uses WorldgenRandom.SetLargeFeatureSeed(worldSeed, chunkX, chunkZ) (re-seed worldSeed, draw a,b, seed (cx*a)^(cz*b)^worldSeed) — GenerationContext.makeRandom, the determinism hinge. All piece placement (geometry, chest positions, the mosaic) draws from THIS. WorldgenRandom IS a RandomSource (NextIntN/NextFloat/NextLong).

THE TEMPLE BLOCKS (level/block/blocks.go, VERIFIED present): block.ToStateID[block.Sandstone{}], ChiseledSandstone{}, CutSandstone{}, SmoothSandstone{}, SandstoneStairs{Facing:..., Half:..., Shape:...} (a struct with props — set the facing fields), OrangeTerracotta{}, BlueTerracotta{}, Cobblestone{}, Tnt{Unstable:...} (a struct), Chest{Facing:..., Type:...}. Resolve via block.ToStateID (the map is keyed by the block VALUE incl. its props — match the exact prop set the jar places). Stairs/chests have a Facing prop the pyramid orientation sets.

THE 14-01 HOOK (world/noisegen.go, VERIFIED 14-01): Decorate ends with an EMPTY placeStructures(view, C) call after applyBiomeDecoration. THIS plan fills it: placeStructures gathers cache.StartsForChunk(C) + placeInChunk each into C's WritableArea. The NoiseGenerator holds the cache + the StartGenerator set (empty in 14-01) — REGISTER the desert_pyramid StartGenerator here.

THE DETERMINISM GATES (world/worker_seam_test.go, VERIFIED): TestDecorationReorderIdentical (5x5, two request orders → byte-identical) + TestEmitOnce. They MUST stay green now WITH a pyramid placing (the cross-chunk clip + re-derivable RNG make placement order-independent). Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... ./world/structure/...`.
</interfaces>

<key_facts>
<!-- PORT StructurePiece + placeInChunk + DesertPyramidPiece from jar bytecode (cite each class). Hardcoded geometry, 0 .nbt. Idiomatic Go, no GPL paste. Loot DEFERRED v3 (chests = block+tag). -->
- PORT TARGETS (javap -c, cite each): net.minecraft.world.level.levelgen.structure.StructurePiece (placeBlock, generateBox, generateAirBox, generateMaybeBox, fillColumnDown, maybeGenerateBlock, createChest, getWorldPos/getWorldX/Y/Z, addChildren, findCollisionPiece, the rotation/mirror handling); StructureStart.placeInChunk; net.minecraft.world.level.levelgen.structure.structures.DesertPyramidStructure (findGenerationPoint) + DesertPyramidPiece (the constructor bbox + postProcess geometry); ScatteredFeaturePiece (the shared scattered-temple base — moveBelowSeaLevel/the height offset).
- placeBlock IS the cross-chunk clip (Pitfall #2, jar-exact): getWorldPos(x,y,z) (local→world applying orientation/rotation/mirror), then `if !box.IsInside(worldPos) return` (clip to the writableBox — THIS is what makes a chunk-spanning piece write only its slice), then canBeReplaced, then mirror/rotate the STATE, then view.SetBlock. The box passed to PostProcess IS the chunk's writableBox. Idempotent: the piece RNG is re-derivable (SetLargeFeatureSeed over (worldSeed,ownerChunkX,ownerChunkZ)) so placing the same piece from C's pass and N's pass draws the SAME sequence + clips to each chunk's slice → identical non-duplicated blocks.
- DESERT PYRAMID GEOMETRY (DesertPyramidPiece.postProcess, TRANSCRIBE block-by-block + draw order): the stepped sandstone pyramid body (generateBox layers of sandstone/cut/chiseled), the entrance + the two towers, the orange/blue terracotta floor MOSAIC (the X pattern — placed per-cell), the hidden lower chamber with the TNT trap (the tnt block + the suspended-sand/pressure-plate trigger — place the tnt + the trigger blocks; the REDSTONE/entity logic is a runtime concern, the BLOCKS are the deliverable), and the 4 CHESTS in the chamber corners (createChest → chest block + a loot-table tag marker, LOOT DEFERRED v3). The EXACT block sequence + any nextInt draws (e.g. the random sandstone variation, if the jar does it) are the determinism contract.
- THE DESERT PYRAMID START (DesertPyramidStructure.findGenerationPoint): getPotentialStructureChunk(worldSeed, cx, cz, 32, 8, 14357617, LINEAR) decides the start chunk; a REAL biome check at the chosen point — biomeAt(originX, surfaceY, originZ) (the threaded MultiNoiseBiomeSource.GetBiome) ∈ the embedded has_structure/desert_pyramid allow-set {minecraft:desert}; if not in the set, NO start (vanilla); SampleSurfaceY(originX, originZ) for the WORLD_SURFACE_WG project height (the pyramid sits ON the surface); build DesertPyramidPiece at (originX, surfaceY, originZ) with its bbox; the piece RNG seeded via SetLargeFeatureSeed(worldSeed, startChunkX, startChunkZ). Wrap in a StructureStart. The StartGenerator runs per-chunk in the STARTS phase (14-01's ComputeStarts) — only the OWNING chunk (isStructureChunk true) produces a non-empty start.
- THE WORLD↔WORLD/STRUCTURE BOUNDARY (import-cycle discipline, CLAUDE.md): the Neighborhood is in package world; the structure pipeline is package world/structure (so the cache could land there in 14-01). PostProcess must NOT import package world (cycle: world→world/structure→world). Define a WorldGenView interface { SetBlock(wx,wy,wz int, st block.StateID); GetBlock(wx,wy,wz int) block.StateID } in world/structure; *world.Neighborhood satisfies it structurally; placeStructures (called from world.Decorate) passes the view as WorldGenView. DECIDE + document (the dependency points world→world/structure only). The biomeAt func (BLOCKER-1 biome-check seam) is likewise passed as a plain func(wx,wy,wz int) levelbiome.Type from package world into world/structure — levelbiome is level/biome (no cycle); the StartGenerator never imports package world.
- AfterPlace (the terrain beard) is STUBBED (research Deferrals — v3): scattered temples set no beard, so this is a no-op for desert pyramid anyway; the stub is documented for the jigsaw (Phase 16) that DOES beard.
- LOOT DEFERRAL (documented, matches the REQUIREMENTS.md v3 chest deferral + the Phase-13 dungeon precedent): createChest places the chest BLOCK + a loot-table tag/NBT marker; no loot is rolled (block entities / loot tables are a v3 subsystem). The VISIBLE pyramid (sandstone + mosaic + TNT + chest blocks) is fully delivered.
- ORDER-INDEPENDENT DETERMINISM (the load-bearing proof): the desert pyramid's start is pure over (seed,pos) (which chunk owns it, the origin, the bbox are identical regardless of compute order); placeInChunk's clip + the re-derivable piece RNG make the PLACED blocks identical regardless of which overlapping chunk placed first. So the 5x5 reorder (TestDecorationReorderIdentical) + emit-once gates STAY byte-identical now WITH a pyramid placing. Re-run them.
- NO visual gate this phase (Phase 16). Acceptance is automated: the placement math → the expected chunk (derive from the algorithm, do NOT eyeball), the postProcess → the expected block fingerprint, the cross-chunk idempotence, the 5x5 determinism, Docker -race. Do NOT add an autonomous:false task.
- STALE-LSP caveat: trust `go build ./...` over gopls/Serena phantom errors.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the StructurePiece machinery (Piece interface + base + placeBlock-with-clip + generateBox/fillColumnDown/createChest + rotation/mirror) + placeInChunk</name>
  <files>world/structure/piece.go, world/structure/piece_test.go, world/structure/place.go, world/structure/place_test.go</files>
  <behavior>
    - placeBlock(view, state, x,y,z, box): getWorldPos applies orientation/rotation/mirror to (x,y,z); if !box.IsInside(worldPos) the write is DROPPED (the cross-chunk clip); else the state is mirror/rotate-transformed and view.SetBlock-ed. A piece spanning two chunks placed against chunk C's writableBox writes ONLY C's slice.
    - generateBox(view, box, x1,y1,z1, x2,y2,z2, state) fills the local sub-box (each cell via placeBlock → clipped). generateAirBox fills air. fillColumnDown(view, state, x,z, fromY) places downward until a non-air/solid block or the floor. maybeGenerateBlock places per a probability draw. createChest places the chest block + a loot tag marker (loot deferred), returns true on a successful (in-box) placement.
    - rotation/mirror NONE is the identity (temples) — getWorldPos returns local+offset unchanged, the state is untransformed; a ROT_90/MIRROR_LEFT_RIGHT unit test exercises the transform path (for Phase-16 reuse).
    - findCollisionPiece(pieces, box) returns the first piece whose bbox intersects box (nil if none) — addChildren uses it (single temples don't, but the hook exists).
    - placeInChunk(start, view, C): for each piece whose BoundingBox Intersects WritableArea(C), PostProcess it with that writableBox; pieces not intersecting C are skipped. AfterPlace is a documented stub.
  </behavior>
  <action>
First re-read the AS-BUILT 14-01 world/structure/{start,cache,surface_sampler}.go (the Piece placeholder, BoundingBox, WritableArea, StartsForChunk).
world/structure/piece.go: EXTEND the Piece interface with PostProcess(view WorldGenView, box BoundingBox, chunkPos level.ChunkPos, rng levelgen.RandomSource). Define WorldGenView interface { SetBlock(wx,wy,wz int, st block.StateID); GetBlock(wx,wy,wz int) block.StateID } (the *world.Neighborhood satisfies it — import-cycle discipline, document the world→world/structure direction). Port (javap -c, cite StructurePiece) the base StructurePiece struct { bbox BoundingBox; orientation Direction; rotation Rotation; mirror Mirror; genDepth int } + getWorldPos/getWorldX/Y/Z (the local→world transform), placeBlock (the clip: box.IsInside guard + state mirror/rotate + SetBlock), generateBox/generateAirBox/generateMaybeBox, fillColumnDown, maybeGenerateBlock, createChest (block + loot tag, DEFERRED), findCollisionPiece, addChildren (the recursion hook + accessor). Rotation/Mirror enums + the state transform (NONE = identity). No GPL paste.
world/structure/place.go: port StructureStart.placeInChunk(start, view, C) (javap -c, cite) — iterate start.Pieces, intersect each bbox with WritableArea(C, minY, height), PostProcess the intersecting ones; AfterPlace stub (documented v3). placeStructures(view, C, cache, minY, height): gather cache.StartsForChunk(C) + placeInChunk each (the driver world.Decorate's 14-01 hook calls).
world/structure/piece_test.go: TestPlaceBlockClips (a placeBlock outside the box → dropped; inside → SetBlock'd); TestGenerateBox (fills the sub-box, edges clipped); TestFillColumnDown; TestRotationMirrorIdentity (NONE = local unchanged) + TestRotationTransform (ROT_90 maps coords + state); TestFindCollisionPiece.
world/structure/place_test.go: TestPlaceInChunkCrossChunk — a fake 2-chunk-spanning piece placed against chunk C's writableBox writes ONLY C's slice; placed against N's writableBox writes ONLY N's slice; the UNION = the full piece, no overlap/double-write/truncation (idempotent, Pitfall #2). TestPlaceInChunkSkipsNonIntersecting.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestPlaceBlock|TestGenerateBox|TestFillColumnDown|TestRotation|TestFindCollisionPiece|TestPlaceInChunk' -count=1 -race && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The StructurePiece machinery is ported: placeBlock clips to the writableBox (the cross-chunk mechanism), generateBox/fillColumnDown/createChest (loot deferred)/findCollisionPiece/addChildren work, rotation/mirror NONE=identity + the transform path is exercised, and placeInChunk places a chunk-spanning piece once-per-chunk idempotently (no double-write/truncation) — all -race clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the desert pyramid — the StartGenerator (salt 14357617 + biome/surface) + DesertPyramidPiece hardcoded geometry, wired into the pipeline</name>
  <files>world/structure/desert_pyramid.go, world/structure/desert_pyramid_test.go, world/noisegen.go, world/worker_structure_test.go</files>
  <behavior>
    - The desert_pyramid StartGenerator: for chunk pos, isStructureChunk(worldSeed,pos.x,pos.z, 32,8,14357617,LINEAR) gates ownership; the OWNING chunk samples SampleSurfaceY(originX,originZ) for the WORLD_SURFACE_WG project height + a REAL desert biome check (biomeAt(origin) ∈ {minecraft:desert} from the embedded has_structure tag — a non-desert origin yields NO start, NO accept-by-default), builds a DesertPyramidPiece at (originX,surfaceY,originZ) with its bbox, and returns a StructureStart wrapping it (the piece RNG seeded via SetLargeFeatureSeed(worldSeed, startChunkX, startChunkZ)); a non-owning chunk returns no start.
    - DesertPyramidPiece.PostProcess writes the JAR-EXACT pyramid: the stepped sandstone body (sandstone/cut/chiseled layers via generateBox), the entrance + towers, the orange/blue terracotta floor mosaic (per-cell X pattern), the hidden TNT-trap chamber + the 4 chests (chest block + loot tag, loot deferred) + the tnt block — block-by-block + draw order pinned.
    - A known worldSeed → a desert pyramid at the EXPECTED chunk (computed FROM getPotentialStructureChunk in the test, not eyeballed) with the EXPECTED block fingerprint (a hash/sample of the placed StateIDs).
    - With the pyramid now placing, the 5x5 reorder-determinism + emit-once stay byte-identical (order-independent placement).
  </behavior>
  <action>
First re-read the AS-BUILT 14-01 placement.go (getPotentialStructureChunk/SpreadType) + cache.go (StartGenerator/ComputeStarts) + the desert_pyramid structure + structure_set JSON (the embedded salt/spacing/biome — cross-check the constants).
world/structure/desert_pyramid.go: port (javap -c, cite DesertPyramidStructure.findGenerationPoint + DesertPyramidPiece + ScatteredFeaturePiece) — desertPyramidStartGen implementing StartGenerator.GenerateStarts (isStructureChunk gate via salt 14357617/spacing 32/sep 8/LINEAR loaded from the structure_set JSON; SampleSurfaceY for the project height; the REAL desert biome check at the point — biomeAt(originX, surfaceY, originZ) ∈ the embedded has_structure/desert_pyramid allow-set (load the tag from the 14-01 FS, parse to a levelbiome.Type set, compare via .String()/the name lookup); a non-desert origin returns NO start (vanilla — there is NO accept-by-default escape; the biomeAt func is threaded in from the NoiseGenerator per 14-01's StartGenerator interface); build DesertPyramidPiece + StructureStart). DesertPyramidPiece struct (extends the base StructurePiece) + its constructor (the bbox from the origin + the fixed pyramid dimensions, the ScatteredFeaturePiece height offset) + PostProcess (TRANSCRIBE the full geometry: sandstone body layers, towers, terracotta mosaic, TNT-trap chamber, 4 chests via createChest [loot deferred], the tnt block — exact block sequence + any nextInt draws). Resolve all StateIDs via block.ToStateID (match the exact props — stairs facing, chest facing). No GPL paste; document the loot/redstone-logic v3 deferrals inline.
world/noisegen.go: REGISTER desertPyramidStartGen into the NoiseGenerator's StartGenerator set (the empty set 14-01 created) so ComputeStarts runs it; CONFIRM the 14-01 placeStructures hook (now filled by Task 1's place.go) dispatches the PLACE pass at the end of Decorate. Thread the NoiseGenerator's g.biomes (*biome.MultiNoiseBiomeSource — GetBiome(x,y,z) levelbiome.Type is public) into the StartGenerator call as the biomeAt func (the 14-01 StartGenerator interface already takes it); the desert StartGenerator uses it for the REAL biome check. No accept-by-default fallback anywhere.
world/structure/desert_pyramid_test.go: TestDesertPyramidExpectedChunk (a known worldSeed → assert the owning chunk == getPotentialStructureChunk(seed,...,14357617,...) computed in-test); TestDesertPyramidPlaces (place the pyramid into its owning chunk's 3x3 → assert the sandstone shell + the mosaic + the TNT chamber + the 4 chest blocks appear at the expected local coords, FULL-fingerprint-pinned vs an oracle — this fingerprint is THE completeness gate that catches truncated/missing geometry, NOT the min_lines floor; the oracle must cover the whole pyramid, not a corner sample); TestDesertPyramidStartPure (same (seed,pos) → identical start: owner/origin/bbox).
world/worker_structure_test.go: TestDesertPyramidCrossChunkIdempotent (a pyramid whose bbox spans 2 chunks → placed from each overlapping chunk's PLACE pass, the union is the complete pyramid, no double-write/truncation); assert TestDecorationReorderIdentical (5x5) + TestEmitOnce STILL byte-identical with the pyramid placing. Run Docker -race.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestDesertPyramid' -count=1 -race && go test ./world/ -run 'TestDesertPyramidCrossChunkIdempotent|TestDecorationReorderIdentical|TestEmitOnce' -count=1 && go build ./... && CGO_ENABLED=0 go build ./... && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... ./world/structure/...</automated>
  </verify>
  <done>The desert pyramid generates in vanilla positions as the pipeline shakedown: a known worldSeed → a pyramid at the expected chunk (derived from getPotentialStructureChunk) with the expected hardcoded geometry (sandstone body + terracotta mosaic + TNT chamber + 4 chests as blocks, loot deferred); the cross-chunk references+clip places a chunk-spanning pyramid idempotently once-per-chunk; STARTS is pure over (seed,pos); and the 5x5 reorder-determinism + emit-once + Docker -race gates stay green WITH the pyramid placing — STRUCT-01+STRUCT-02 are proven end-to-end on one structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → DesertPyramidPiece geometry | The pyramid geometry is hardcoded in the ported postProcess (0 .nbt); the desert_pyramid structure/structure_set is the pinned 26.2 jar. No client input reaches PostProcess — it runs off-tick on the single scheduler goroutine over (worldSeed, chunkPos). |
| piece PostProcess → cross-chunk WorldGenView | placeBlock writes through the WorldGenView (the Neighborhood, blockStateWriteRadius=1) clipped to the piece bbox AND the chunk's writableBox; out-of-slice writes are dropped. |
| world/structure ↔ world | The structure pipeline is package world/structure; PostProcess takes a WorldGenView interface (not *world.Neighborhood) so the dependency points world→world/structure only — no import cycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | DesertPyramidPiece.postProcess block/draw-order divergence (wrong geometry vs a vanilla seed) | mitigate | The geometry is transcribed javap -c + pinned by TestDesertPyramidPlaces (the exact block fingerprint at the expected local coords) + TestDesertPyramidExpectedChunk (the owning chunk derived from getPotentialStructureChunk); a reorder/wrong-block fails the test. |
| T-14-08 | Tampering | the cross-chunk clip failing — a chunk-spanning pyramid double-writing or truncating (Pitfall #2) | mitigate | placeBlock clips to box.IsInside(writableBox); placeInChunk only PostProcesses intersecting pieces; TestPlaceInChunkCrossChunk + TestDesertPyramidCrossChunkIdempotent pin once-per-chunk slice writes with the union = the complete pyramid, no overlap. |
| T-14-09 | Integrity (non-reproducible world) | the same seed producing a different pyramid position/shape (placement-order or RNG-derivation divergence) | mitigate | The start is pure over (seed,pos); the piece RNG is re-derivable via SetLargeFeatureSeed(worldSeed,startChunkX,startChunkZ); TestDesertPyramidStartPure + the 5x5 reorder-determinism + emit-once gates re-run byte-identical WITH the pyramid placing. |
| T-14-10 | Tampering | a world↔world/structure import cycle forcing a structural compromise | mitigate | PostProcess takes a WorldGenView interface defined in world/structure (the Neighborhood satisfies it structurally); the dependency is one-directional (world→world/structure). go build proves no cycle. |
| T-14-11 | Information disclosure | the pyramid chests/TNT exposing loot or a redstone-trap surface | accept | Loot is a v3 deferral — chests place as the chest BLOCK + a loot-table tag marker, no loot rolled; the TNT trap places as the tnt + trigger BLOCKS, no redstone/entity logic. No data surface. Documented inline. |
| T-14-12 | DoS | a runaway addChildren recursion (a malformed piece tree) | accept | The desert pyramid is SINGLE-PIECE (no recursion); addChildren is the ported hook with the genDepth bound for Phase-15 mineshaft reuse but is not exercised here. No unbounded growth this phase. |
</threat_model>

<verification>
- `go test ./world/structure/ -run 'TestPlaceBlock|TestGenerateBox|TestFillColumnDown|TestRotation|TestFindCollisionPiece|TestPlaceInChunk' -count=1 -race` green: the StructurePiece machinery + the cross-chunk clip + placeInChunk idempotence.
- `go test ./world/structure/ -run 'TestDesertPyramid' -count=1 -race` green: the pyramid lands at the expected chunk (derived from the placement algorithm) with the expected hardcoded geometry; the start is pure over (seed,pos).
- `go test ./world/ -run 'TestDesertPyramidCrossChunkIdempotent|TestDecorationReorderIdentical|TestEmitOnce' -count=1` green: the chunk-spanning pyramid places idempotently; the 5x5 reorder + emit-once stay byte-identical WITH the pyramid placing.
- Docker `-race` over `./world/... ./world/structure/...` clean (CGO golang:1.26 — the intended race-gate toolchain; go.mod targets the go 1.25.0 floor, golang:1.26 is a strict superset, no go.mod bump needed).
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; NO new deps; NO import cycle (world→world/structure only); NO encoder/packet/chunk-wire file touched.
</verification>

<success_criteria>
- STRUCT-02: the StructurePiece bounding-box-tree machinery is ported (addChildren recursion + findCollisionPiece + postProcess + placeBlock/generateBox/fillColumnDown with rotation/mirror), and the desert pyramid (single-piece, hardcoded geometry, 0 .nbt) generates in vanilla positions as the pipeline shakedown — a known worldSeed → a pyramid at the expected chunk with the expected blocks.
- STRUCT-01 proven end-to-end: STARTS→cache→REFERENCES→PLACE→postProcess runs on one structure; the cross-chunk references+clip places a chunk-spanning pyramid idempotently once-per-chunk (Pitfall #2); placeInChunk fills 14-01's empty hook.
- The determinism contract holds with blocks placing: STARTS is pure over (seed,pos), the 5x5 reorder-determinism + emit-once stay byte-identical, Docker -race clean — the pipeline is shaken down, ready for the cheap temple follow-ons (14-03).
</success_criteria>

<output>
After completion, create `.planning/phases/14-structure-pipeline-temples/14-02-SUMMARY.md`
</output>
