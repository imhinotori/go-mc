---
phase: 09-stretch-online-worldgen-regions
plan: 07
type: execute
wave: 5
depends_on: [05]
files_modified:
  - world/levelgen/biome/climate.go
  - world/levelgen/biome/source.go
  - world/levelgen/surface/rules.go
  - world/levelgen/surface/system.go
  - world/levelgen/biome/climate_test.go
  - world/levelgen/surface/surface_test.go
autonomous: true
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "world/levelgen/biome/climate.go + source.go PORT the multi-noise biome source (Climate + MultiNoiseBiomeSource) from the jar — REAL biome diversity, NOT a single-biome plains stub: the 6-D Climate sampler (temperature/humidity/continentalness/erosion/depth/weirdness) computed from the Wave-3 router functions (temperature/vegetation/continents/erosion/depth/ridges) + the Climate$ParameterList nearest-match over the overworld biome parameter boxes (extracted in Wave 1 from OverworldBiomes.class as biome_parameters.json) → a biome id per (x,y,z). So plains/forest/desert/ocean/mountains vary across the world by climate, and the surface rules + surface blocks key on the actual biome — full biome-varied terrain"
    - "world/levelgen/surface/rules.go + system.go PORT the FULL SurfaceSystem + SurfaceRules from the jar (NOT a minimal grass/dirt stub): the surface_rule sequence from overworld.json (extracted Wave 1) is parsed into the ported ConditionSource + RuleSource node set (the full vocabulary verified this session: sequence, condition, block, bandlands, biome, stone_depth, vertical_gradient, water, y_above, above_preliminary_surface, hole, steep, not, temperature, noise_threshold — plus the SurfaceRules$Context conditions). SurfaceSystem.buildSurface walks each column top-down applying the rule sequence so the top blocks are biome-correct: grass+dirt in plains, sand+sandstone in deserts/beaches, gravel, podzol, terracotta bands in badlands, etc. — the REAL surface, not a uniform grass cap"
    - "the surface pass writes the biome-correct top blocks AND the chunk biome containers AND the 3 CLIENT heightmaps: SurfaceSystem.buildSurface (driven per-column from the filled+carved terrain of Waves 5/6) computes the top solid + applies the surface rule sequence (which reads the biome from the multi-noise source + the noise conditions); the biome source fills the per-section biome paletted containers with the real (varied) biome ids; the 3 CLIENT heightmaps (WorldSurface/MotionBlocking/MotionBlockingNoLeaves) are written from the computed top (the Superflat heightmap discipline, Pitfall 6) — a wrong heightmap mis-renders/mis-spawns"
    - "Everything is PURE + deterministic over the seed (PARITY-01 / WORLD-04 / Pitfall 7): the climate sampler + the surface noise all seed from the world seed via the Wave-2/3 chain — NO math/rand, NO global state, NO map-iteration-order over the biome boxes (deterministic nearest-match); same seed → identical biomes + surfaces. ZERO new dependencies, ZERO concurrency"
  artifacts:
    - path: "world/levelgen/biome/climate.go"
      provides: "the ported Climate sampler (6-D TargetPoint from the router functions) + Climate$ParameterList nearest-match (the RTree/linear search over the biome boxes)"
      contains: "Climate"
    - path: "world/levelgen/biome/source.go"
      provides: "MultiNoiseBiomeSource: getBiome(x,y,z) → biome id via the climate sample + the Wave-1 biome_parameters boxes"
      contains: "MultiNoiseBiomeSource"
    - path: "world/levelgen/surface/rules.go"
      provides: "the parsed surface_rule sequence → the ported ConditionSource + RuleSource node set (the full vocabulary)"
      contains: "RuleSource"
    - path: "world/levelgen/surface/system.go"
      provides: "SurfaceSystem.buildSurface — the top-down column walk applying the rule sequence + writing the biome-correct surface + the 3 CLIENT heightmaps"
      contains: "buildSurface"
  key_links:
    - from: "world/levelgen/biome/climate.go Climate sampler"
      to: "world/levelgen/router.go bound temperature/vegetation/continents/erosion/depth/ridges functions (Plan 09-03)"
      via: "the 6-D climate target point is computed from the router climate functions"
      pattern: "Climate"
    - from: "world/levelgen/biome/source.go"
      to: "world/levelgen/data BiomeParameters (Plan 09-01)"
      via: "the nearest-match runs over the embedded overworld biome parameter boxes"
      pattern: "BiomeParameters"
    - from: "world/levelgen/surface/system.go buildSurface"
      to: "world/levelgen/biome/source.go getBiome + the surface_rule sequence"
      via: "the surface rules read the biome (and noise conditions) to pick the biome-correct top blocks"
      pattern: "getBiome"
---

<objective>
Port the FULL SurfaceSystem/SurfaceRules + the multi-noise Climate biome source — the two systems that turn the filled+carved terrain (Waves 5/6) into a biome-VARIED world with biome-correct surfaces. This is pure full-parity scope (the original T0 plan used a single-biome plains stub + a minimal grass/dirt surface): full parity ports the real multi-noise biome source (so plains/forest/desert/ocean/badlands/mountains vary by climate) and the full surface_rule sequence (so deserts get sand, badlands get terracotta bands, beaches get gravel/sand, plains get grass+dirt — keyed on the actual biome). The biome parameter boxes were extracted in Wave 1 (`biome_parameters.json` from `OverworldBiomes.class`), and the `surface_rule` sequence is in the embedded `overworld.json` (Wave 1).

This plan delivers: (1) `world/levelgen/biome/climate.go` — the ported `Climate` sampler (the 6-D TargetPoint computed from the Wave-3 router climate functions) + `Climate$ParameterList` nearest-match; (2) `world/levelgen/biome/source.go` — `MultiNoiseBiomeSource.getBiome(x,y,z)` over the Wave-1 biome boxes; (3) `world/levelgen/surface/rules.go` — the parsed `surface_rule` sequence → the ported `ConditionSource`+`RuleSource` node set (the full vocabulary verified this session: `sequence, condition, block, bandlands, biome, stone_depth, vertical_gradient, water, y_above, above_preliminary_surface, hole, steep, not, temperature, noise_threshold`); (4) `world/levelgen/surface/system.go` — `SurfaceSystem.buildSurface` (the top-down column walk applying the rule sequence + writing the biome-correct surface + the biome containers + the 3 CLIENT heightmaps, the Pitfall-6 discipline).

Everything seeds from the world seed (Pitfall 7) and is deterministic. ZERO new dependencies, ZERO concurrency.

Purpose: biome-varied surfaces are the visible "this looks like Minecraft, not a uniform grass slab" parity signal; the surface rules are what put the right block on top. The Generator (Wave 8) runs buildSurface after the fill+carve, and uses the biome source to fill the biome containers.
Output: world/levelgen/biome/{climate,source}.go, world/levelgen/surface/{rules,system}.go (+ tests). ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md
@.planning/phases/09-stretch-online-worldgen-regions/09-05-PLAN.md

<interfaces>
<!-- VERIFIED via the jar (the surface_rule vocabulary + the biome/surface class inventory) + the upstream plans this session. -->

From world/levelgen/data (Plan 09-01):
```go
func NoiseSettings(id string) ([]byte, error)  // overworld.json carries the surface_rule sequence
func BiomeParameters() ([]byte, error)          // the overworld biome climate boxes (from OverworldBiomes.class)
```
From world/levelgen/router.go (Plan 09-03 — the bound climate functions + the surface inputs):
```go
// Router exposes the bound climate functions (the Climate 6-D inputs):
//   Temperature, Vegetation(humidity), Continents(continentalness), Erosion, Depth, Ridges(weirdness) density.Function
//   + PreliminarySurfaceLevel (the surface-rule above_preliminary_surface condition) + SeaLevel(63)
// + RandomState (the surface noise seed)
```
From world/levelgen/aquifer.go (Plan 09-05) / the filled+carved terrain:
```go
// the Wave-8 Generator supplies, per column, the filled+carved block column (post-fill, post-carve) the surface walks top-down
```
REUSE — level + block + biome:
```go
func level.NewBiomesPaletteContainer(length int, defaultValue BiomesState) *PaletteContainer[BiomesState]
func level.NewBitStorage(bits, length int, data []uint64) *BitStorage   // the 3 CLIENT heightmaps (Superflat discipline)
biome.Type  // the biome registry — UnmarshalText("minecraft:desert") etc.; confirm the registry covers the overworld biomes
block.ToStateID[block.GrassBlock{}/Dirt{}/Sand{}/Sandstone{}/Gravel{}/Podzol{}/CoarseDirt{}/Terracotta variants/Stone{}/Water{}]
```

The Java sources to port (javap temp/cache/26.2-inner.jar — VERIFIED present this session):
```
biome.Climate / $TargetPoint / $ParameterPoint / $ParameterList / $RTree — the 6-D sampler + the nearest-biome search
biome.MultiNoiseBiomeSource                       — getNoiseBiome(x,y,z) → the climate sample → the biome
data.worldgen.biome.OverworldBiomes               — the baked climate params (extracted Wave 1 to biome_parameters.json)
levelgen.SurfaceSystem                            — buildSurface(...): the per-column top-down walk + the surface noises
levelgen.SurfaceRules                             — $SequenceRuleSource, $ConditionSource (Biome/NoiseThreshold/VerticalGradient/
                                                     StoneDepth/Water/Steep/Hole/AbovePreliminarySurface/Temperature/Not/YBlockCheck),
                                                     $RuleSource ($BlockRuleSource/$Bandlands), $Context
SurfaceRules$Context                              — the column state (stoneDepthAbove/Below, the biome, the surface level)
```
NOTE: the multi-noise nearest-match uses an RTree over the 6-D boxes — a linear nearest-match (squared-distance to each box) is a CORRECT (if slower) substitute for the RTree; port the linear version first (correctness), note the RTree as a perf refinement. Cite each .class.
</interfaces>

<key_facts>
<!-- VERIFIED against the jar (the surface_rule vocabulary walk + the biome/surface class inventory) + 09-RESEARCH this session. -->
- REAL MULTI-NOISE BIOMES, NOT A PLAINS STUB (full-parity — the T0 plan stubbed a single biome): port Climate (the 6-D TargetPoint temperature/humidity/continentalness/erosion/depth/weirdness from the router functions) + Climate$ParameterList (the nearest-match over the biome boxes) + MultiNoiseBiomeSource. The biome boxes are the Wave-1 biome_parameters.json (extracted from OverworldBiomes.class). getBiome(x,y,z) → a varied biome id. The surface rules + the biome containers key on this real biome.
- THE FULL SURFACE_RULE VOCABULARY (verified this session by walking overworld.json's surface_rule): sequence, condition, block, bandlands, biome, stone_depth, vertical_gradient, water, y_above, above_preliminary_surface, hole, steep, not, temperature, noise_threshold. Port the ConditionSource set (Biome/NoiseThreshold/VerticalGradient/StoneDepth/Water/Steep/Hole/AbovePreliminarySurface/Temperature/Not/YBlockCheck) + the RuleSource set (BlockRuleSource + Bandlands [the badlands terracotta bands]) + the SequenceRuleSource. NOT a minimal stub — the FULL sequence so deserts/badlands/beaches/plains all get their correct surface.
- SURFACESYSTEM.buildSurface (Pitfall 6 — render correctness): walk each column TOP-DOWN from the filled+carved terrain; maintain the SurfaceRules$Context (stoneDepthAbove/Below, the biome from getBiome, the surface level, the surface noise); apply the rule sequence to pick each surface block (grass on the exposed plains top, dirt below, sand in deserts, gravel beaches, etc.); WRITE the 3 CLIENT heightmaps (WorldSurface/MotionBlocking/MotionBlockingNoLeaves) from the top solid (the Superflat discipline). A wrong heightmap → client lighting/spawn glitches.
- THE BIOME CONTAINERS (the per-section biome palette): fill NewBiomesPaletteContainer with the REAL biome ids from getBiome (biomes are stored per 4×4×4 sub-cell — the noise-biome resolution; confirm the container granularity matches the Phase-4 wire). The varied biome is what the client renders (grass color, fog, etc.) + what the surface rules key on.
- SEED EVERYTHING FROM THE WORLD SEED (Pitfall 7): the surface noise (the SurfaceSystem's surfaceNoise/secondary) + the climate sampler all seed from the Wave-2/3 chain. NO math/rand, NO global. The nearest-match over the boxes is deterministic (a stable squared-distance + a stable tiebreak — no map iteration). Same seed → identical biomes + surfaces.
- CONFIRM THE BIOME REGISTRY COVERS THE OVERWORLD BIOMES: biome.Type must resolve all the overworld biome ids the parameter list references (plains/forest/desert/ocean/badlands/etc.). They are in the generated biome registry (Phase 2 sent the full registry to the client); confirm the Go-side biome.Type covers them. If an id is missing, surface it (do not silently map to plains).
- ZERO NEW DEPS, ZERO CONCURRENCY: stdlib only; no Generator/worker/tick touched here (Wave 8 drives buildSurface + the biome fill). NO xsync/ants/conc.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create world/levelgen/biome/climate.go (the Climate sampler + nearest-match) + source.go (MultiNoiseBiomeSource) + climate_test.go, then world/levelgen/surface/rules.go (parse the surface_rule into the ported node set) + system.go (buildSurface + heightmaps) + surface_test.go. Cite each .class. Do NOT touch the router/aquifer/noisechunk packages (consume them), world/generator.go, or cmd/sulfur.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-clamped chunk position → buildSurface/getBiome (off-tick) | The surface + biome systems read only the build-trusted DATA (surface_rule + biome boxes) + the bound router + the clamped position; no client data reaches them. Integrity = determinism; availability = the per-column surface-rule + the nearest-match cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-19 | Denial of Service | the per-block nearest-match over all biome boxes making getBiome too slow | mitigate | The biome is sampled at the noise-biome resolution (per 4×4×4 cell, not per block — vanilla caches the climate); the linear nearest-match over a bounded box count is O(boxes) per cell. The worker's bounded queue drops on overload. Asserted by the surface/biome test completing fast. |
| T-9-20 | Integrity (mis-render) | wrong heightmaps / wrong surface block → client lighting/spawn glitches or wrong biome colors | mitigate | buildSurface computes the top solid per column + writes the 3 CLIENT heightmaps from it (Pitfall 6); the biome containers carry the real biome ids; asserted by a test (valid heightmaps, biome-correct surface blocks in plains vs desert). |
| T-9-21 | Integrity (world corruption) | non-deterministic biome/surface (leaky RNG) breaking same-seed→same-world | mitigate | The climate + surface noise seed from the world seed (Pitfall 7); the nearest-match is deterministic (no map iteration). Asserted by TestBiomeSurfaceDeterministic. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the multi-noise Climate biome source (real biome diversity) (PARITY-01)</name>
  <files>world/levelgen/biome/climate.go, world/levelgen/biome/source.go, world/levelgen/biome/climate_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Tier F (the multi-noise Climate source + the OverworldBiomes baked params).
    - The jar (javap): `javap -p net.minecraft.world.level.biome.Climate` + `$TargetPoint` + `$ParameterPoint` + `$ParameterList` + `$RTree` (the 6-D sampler + the nearest-match), `javap -p net.minecraft.world.level.biome.MultiNoiseBiomeSource` (getNoiseBiome). Read the 6-D quantization + the distance metric constant-for-constant. Record divergence.
    - world/levelgen/data/embed.go (Plan 09-01) — BiomeParameters() (the biome boxes).
    - world/levelgen/router.go (Plan 09-03) — the bound Temperature/Vegetation/Continents/Erosion/Depth/Ridges functions (the climate inputs).
    - level/biome/list.go — confirm biome.Type covers the overworld biome ids the param list references.
  </read_first>
  <behavior>
    PARITY-01 (multi-noise biomes):
    - Test 1 (TestClimateTargetPoint): the 6-D TargetPoint (temperature/humidity/continentalness/erosion/depth/weirdness) is computed from the router functions at fixed coords, deterministically (the quantization matches the ported math).
    - Test 2 (TestBiomeParametersParse): BiomeParameters() parses into the box list (each box = the 6-D Param ranges + offset → a biome id), covering the overworld biomes.
    - Test 3 (TestNearestBiomeVaries): getBiome over a spread of (x,z) returns MORE THAN ONE distinct biome (real diversity — not uniform plains); the nearest-match picks the box with the minimum 6-D squared distance + the documented tiebreak.
    - Test 4 (TestBiomeDeterministic): getBiome at fixed coords is identical across two sources from the same seed (Pitfall 7).
  </behavior>
  <action>
    1. world/levelgen/biome/climate.go — port Climate$TargetPoint (the 6-D point + the quantizeCoord) + Climate$ParameterPoint (a box: the 6-D Param ranges + offset) + the nearest-match (port a LINEAR fitness search — minimize the 6-D squared distance with the offset term — over the box list; note the RTree as a perf refinement). Compute the TargetPoint from the router's Temperature/Vegetation/Continents/Erosion/Depth/Ridges at (x,y,z). Cite Climate.class.
    2. world/levelgen/biome/source.go — parse BiomeParameters() into the box list; `func NewMultiNoiseBiomeSource(r *Router) *MultiNoiseBiomeSource` + `func (s *MultiNoiseBiomeSource) getBiome(x, y, z int) biome.Type` (sample the climate → nearest box → biome id). Cite MultiNoiseBiomeSource.class.
    3. world/levelgen/biome/climate_test.go (package biome) — the 4 tests in <behavior>.
  </action>
  <verify>
    <automated>go test ./world/levelgen/biome/ -run 'TestClimateTargetPoint|TestBiomeParametersParse|TestNearestBiomeVaries|TestBiomeDeterministic' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Climate 6-D sampler + the nearest-match over the Wave-1 biome boxes are ported (Climate.class + MultiNoiseBiomeSource.class); getBiome returns VARIED biomes across the world (not uniform plains).
    - Deterministic over the seed (Pitfall 7); the nearest-match has no map-iteration nondeterminism; cites the .class; no GPL paste.
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The multi-noise Climate biome source is ported — biomes vary by climate across the world; Task 2's surface rules key on the real biome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the FULL SurfaceSystem/SurfaceRules + buildSurface (biome-correct surface + heightmaps) (PARITY-01)</name>
  <files>world/levelgen/surface/rules.go, world/levelgen/surface/system.go, world/levelgen/surface/surface_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Tier E (SurfaceSystem/SurfaceRules subset, now FULL), Pitfall 6 (heightmap/surface two-pass).
    - The jar (javap): `javap -p net.minecraft.world.level.levelgen.SurfaceRules` + the nested $ classes ($SequenceRuleSource, $ConditionSource impls [Biome/NoiseThreshold/VerticalGradient/StoneDepth/Water/Steep/Hole/AbovePreliminarySurface/Temperature/Not/YBlockCheck], $BlockRuleSource, $Bandlands), `javap -p net.minecraft.world.level.levelgen.SurfaceSystem` (buildSurface + the surface noises), `javap -p net.minecraft.world.level.levelgen.SurfaceRules$Context` (stoneDepthAbove/Below + the biome + the surface level). Read each condition/rule constant-for-constant. Record divergence.
    - world/levelgen/data/embed.go (Plan 09-01) — NoiseSettings("minecraft:overworld") (the surface_rule sequence is in it).
    - world/levelgen/biome/source.go (Task 1) — getBiome (the surface rules' biome condition).
    - world/levelgen/router.go (Plan 09-03) — PreliminarySurfaceLevel (the above_preliminary_surface condition) + SeaLevel.
    - world/generator.go — the Superflat heightmap discipline (NewBitStorage, the 3 CLIENT heightmaps) to MIRROR.
  </read_first>
  <behavior>
    PARITY-01 (the full surface + heightmaps):
    - Test 1 (TestParseSurfaceRuleSequence): the surface_rule from overworld.json parses into the ported node tree (sequence/condition/block/biome/stone_depth/vertical_gradient/water/bandlands/...) without error — the full vocabulary supported (an unsupported surface-rule type errors loudly).
    - Test 2 (TestSurfaceBiomeCorrect): buildSurface over a plains column places grass+dirt; over a desert column (a biome the climate gives sand) places sand+sandstone; over a beach (near sea level) gravel/sand — the surface is biome-correct (not a uniform grass cap).
    - Test 3 (TestHeightmapsWritten): buildSurface writes the 3 CLIENT heightmaps from the computed top solid per column; no column reports a bogus height (Pitfall 6).
    - Test 4 (TestBiomeSurfaceDeterministic): the biome containers + the surface blocks + the heightmaps are identical across two runs from the same seed (Pitfall 7).
  </behavior>
  <action>
    1. world/levelgen/surface/rules.go — parse the surface_rule sequence into the ported ConditionSource set (Biome/NoiseThreshold/VerticalGradient/StoneDepth/Water/Steep/Hole/AbovePreliminarySurface/Temperature/Not/YBlockCheck) + the RuleSource set (BlockRuleSource + Bandlands) + SequenceRuleSource. An unsupported type errors loudly. Cite SurfaceRules.class + the nested $.
    2. world/levelgen/surface/system.go — port SurfaceSystem: the surface noise(s) (seeded from the world seed) + `buildSurface(column, pos, getBiome, set, writeHeightmaps)` — walk each column top-down maintaining the SurfaceRules$Context (stoneDepthAbove/Below, the biome from getBiome, the surface level, the noise), apply the parsed rule sequence to pick each surface block, and write the 3 CLIENT heightmaps (WorldSurface/MotionBlocking/MotionBlockingNoLeaves) from the top solid (mirror Superflat's NewBitStorage). Cite SurfaceSystem.class + SurfaceRules$Context.
    3. world/levelgen/surface/surface_test.go (package surface) — the 4 tests in <behavior>.
  </action>
  <verify>
    <automated>go test ./world/levelgen/surface/ -run 'TestParseSurfaceRuleSequence|TestSurfaceBiomeCorrect|TestHeightmapsWritten|TestBiomeSurfaceDeterministic' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The FULL surface_rule sequence parses into the ported ConditionSource/RuleSource node set (the full vocabulary incl. bandlands/biome/stone_depth/vertical_gradient/...); an unsupported type errors loudly.
    - buildSurface places biome-correct surface blocks (grass/dirt plains, sand/sandstone desert, gravel beaches, terracotta bands badlands — keyed on getBiome) + writes the 3 CLIENT heightmaps from the top (Pitfall 6); the biome containers carry the varied biomes.
    - Deterministic over the seed (Pitfall 7); cites the .class; no GPL paste.
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The FULL SurfaceSystem/SurfaceRules + the multi-noise biomes are ported — surfaces are biome-correct + varied (not a uniform grass slab), the biome containers carry real biomes, and the heightmaps are written; Wave 8's Generator runs buildSurface after the fill+carve.</done>
</task>

</tasks>

<verification>
- `go test ./world/levelgen/biome/ -run 'TestClimateTargetPoint|TestBiomeParametersParse|TestNearestBiomeVaries|TestBiomeDeterministic' -count=1` passes.
- `go test ./world/levelgen/surface/ -run 'TestParseSurfaceRuleSequence|TestSurfaceBiomeCorrect|TestHeightmapsWritten|TestBiomeSurfaceDeterministic' -count=1` passes.
- The Wave-1..6 tests still pass.
- `go vet ./... && go build ./...` clean; ZERO new dependencies; world/generator.go, world/worker.go, the tick, and cmd/sulfur are UNTOUCHED.
</verification>

<success_criteria>
- PARITY-01 (surface + biomes, FULL parity): the ported multi-noise Climate biome source gives VARIED biomes by climate (plains/forest/desert/ocean/badlands/mountains — not a single-biome stub), and the FULL ported SurfaceSystem/SurfaceRules sequence places biome-correct surface blocks (sand deserts, gravel beaches, terracotta badlands, grass plains) + writes the 3 CLIENT heightmaps + fills the biome containers with real biomes.
- Idiomatic Go, no GPL paste, javap-cited; deterministic over the seed (Pitfall 7); zero new dependencies, zero concurrency; the Generator (Wave 8) drives buildSurface + the biome fill after the fill+carve.
</success_criteria>

<output>
After completion, create `.planning/phases/09-stretch-online-worldgen-regions/09-07-SUMMARY.md`.
</output>
