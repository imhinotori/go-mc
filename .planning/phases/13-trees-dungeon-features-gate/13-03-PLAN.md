---
phase: 13-trees-dungeon-features-gate
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - world/levelgen/feature/tree_special.go
  - world/levelgen/feature/tree_special_test.go
  - world/levelgen/feature/provider.go
  - world/levelgen/feature/provider_test.go
  - world/feature_tree.go
  - world/feature_tree_test.go
autonomous: true
requirements: [FEAT-04]
user_setup: []

must_haves:
  truths:
    - "The RootPlacer subsystem is ported (net.minecraft.world.level.levelgen.feature.rootplacers.MangroveRootPlacer.placeRoots + the RootPlacer base, javap -c against temp/cache/26.2-inner.jar): the 13-01 TreeConfiguration.rootPlacer optional field is now filled by a real `mangrove_root_placer` (root_provider + above_root_placement{chance, provider} + mangrove_root_placement{max_root_length/width, muddy_roots_in/provider, can_grow_through tag, random_skew_chance} + trunk_offset_y IntProvider). TreeFeature.place calls rootPlacer.placeRoots BEFORE the trunk when non-nil — the roots grow down from the trunk base through the can-grow-through set, shifting the trunk origin up by trunk_offset_y — JAR-EXACT draw order. (mangrove_swamp generates [14 climate cells, verified]; this is the only overworld root_placer.)"
    - "The special-biome TRUNK placers are ported jar-exact: cherry_trunk_placer (cherry_grove — the trunk + the weighted branch_count branches with branch_horizontal_length/branch_start_offset/branch_end_offset draws), upwards_branching_trunk_placer (mangrove — the per-log place_branch_per_log_probability branch with extra_branch_steps/length, can_grow_through gating), bending_trunk_placer (lush_caves rooted_azalea_tree — the trunk that bends by bend_length in a random horizontal direction after min_height_for_leaves). (pale_oak REUSES dark_oak_trunk_placer from 13-02 — no new trunk.) Each on the 13-01 TrunkPlacer base."
    - "The special-biome FOLIAGE placers are ported jar-exact: cherry_foliage_placer (the cherry canopy with corner_hole_chance/wide_bottom_layer_hole_chance/hanging_leaves_chance/hanging_leaves_extension_chance draws), random_spread_foliage_placer (mangrove + azalea — the leaf_placement_attempts random scatter of foliage over a radius/foliage_height). (pale_oak REUSES dark_oak_foliage_placer from 13-02.) Each on the 13-01 FoliagePlacer base + its own shouldSkipLocation/scatter logic."
    - "The special-biome DECORATORS + the deferred provider are ported: `randomized_int_state_provider` (a provider that wraps a source provider + randomizes one int property [e.g. mangrove_propagule `age` 0-4] via an IntProvider draw — the 12-01 ParseProvider deferred type, now real) for mangrove's `attached_to_leaves`; AttachedToLeavesDecorator (mangrove — attach the propagule under leaf blocks per probability/directions/exclusion_radius/required_empty_blocks), PaleMossDecorator (pale_garden — ground_probability/leaves_probability/trunk_probability pale moss + carpet), CreakingHeartDecorator (pale_oak_creaking — the creaking_heart block in the trunk). (leave_vine/beehive reused from 13-02.) Each TreeDecorator.place draws JAR-EXACT on the threaded rng."
    - "The 13-02 ParseTrunkPlacer/ParseFoliagePlacer + ParseTreeDecorator + the 13-01 ParseRootPlacer + the 12-01 ParseProvider are extended so EVERY special-biome placer/foliage/decorator/root/provider type resolves (the prior 'ported in 13-03' loud-errors are now real cases) — no remaining loud-error path for any generatable overworld `tree` config. The tree body's rootPlacer hook (13-01) now runs the real mangrove roots; the decorator loop (13-02) now runs the special decorators."
    - "TestAllOverworldTreeConfigsDecode is the COMPLETENESS GUARD (the objective acceptance): it iterates EVERY embedded `tree` configured_feature whose biome is generatable from biome_parameters.json (cherry_grove 60 / mangrove_swamp 14 / pale_garden 40 / lush_caves 1 climate cells ALL included — verified present) and asserts ParseTreeConfiguration succeeds for ALL of them, so the FULL vanilla overworld tree set (oak/birch/spruce/pine/jungle/acacia/dark_oak/cherry/mangrove/pale_oak/azalea + mega variants) parses with real placers+foliage+decorators+root_placer. The tree body's decode-or-skip path NEVER fires for a production overworld tree — 13-01/13-02's deferred unported-placer skip is now CLOSED for the entire generatable overworld."
    - "Deterministic placement tests prove each special placer/foliage/decorator/root draws jar-exact for a known seed (the cherry branches + canopy, the mangrove upwards-branching trunk + roots + propagules, the azalea bending trunk + random-spread foliage, the pale_oak pale_moss/creaking_heart), pinned vs an in-test oracle; and a per-special-biome integration smoke (cherry_grove->cherry, mangrove_swamp->mangrove with roots+propagules, pale_garden->pale_oak with pale_moss, lush_caves->azalea) resolves through the selector recursion to real trees."
  artifacts:
    - path: "world/levelgen/feature/tree_special.go"
      provides: "the special-biome trunk/foliage placers (cherry/upwards_branching/bending + cherry_foliage/random_spread) + the RootPlacer subsystem (MangroveRootPlacer) + the special decorators (AttachedToLeaves/PaleMoss/CreakingHeart), each on the 13-01 bases, registered in the Parse* dispatch"
      contains: "MangroveRootPlacer"
      min_lines: 250
    - path: "world/levelgen/feature/provider.go"
      provides: "randomized_int_state_provider ported (the 12-01-deferred provider mangrove's attached_to_leaves needs), the ParseProvider error arm replaced with a real case"
      contains: "randomized_int_state_provider"
    - path: "world/feature_tree.go"
      provides: "the tree body's rootPlacer hook running the real mangrove roots + the decorator loop running the special decorators"
      contains: "rootPlacer"
    - path: "world/levelgen/feature/tree_special_test.go"
      provides: "per-special-placer/foliage/decorator/root draw-order tests + TestAllOverworldTreeConfigsDecode (the FULL generatable-overworld completeness guard, cherry/mangrove/pale_oak/azalea included)"
      contains: "TestAllOverworldTreeConfigsDecode"
  key_links:
    - from: "world/levelgen/feature/tree.go TreeFeature.place rootPlacer hook (13-01)"
      to: "world/levelgen/feature/tree_special.go MangroveRootPlacer.placeRoots"
      via: "the optional root_placer field (13-01) is now filled by the real mangrove root body, called before the trunk"
      pattern: "placeRoots|rootPlacer"
    - from: "world/levelgen/feature/tree_special.go AttachedToLeavesDecorator"
      to: "world/levelgen/feature/provider.go randomized_int_state_provider"
      via: "the mangrove propagule age is randomized via the now-ported provider"
      pattern: "randomized_int_state_provider"
    - from: "world/levelgen/feature/tree_special_test.go TestAllOverworldTreeConfigsDecode"
      to: "world/levelgen/feature/embed.go the embedded tree configs"
      via: "every generatable-overworld tree config decodes end-to-end (no loud-error path remains)"
      pattern: "TestAllOverworldTreeConfigsDecode"
---

<objective>
Close FEAT-04 to the FULL vanilla overworld tree set: port the SPECIAL-biome trees the four extra generatable biomes need — cherry_grove (cherry), mangrove_swamp (mangrove + the **RootPlacer subsystem**), lush_caves (azalea), pale_garden (pale_oak) — so EVERY biome the multi-noise source can produce grows its correct vanilla tree. The completeness guard `TestAllOverworldTreeConfigsDecode` (every generatable-overworld `tree` config decodes) is the objective acceptance; it closes 13-01/13-02's deferred unported-placer skip for the entire overworld.

These four biomes ARE generatable (verified on disk: `world/levelgen/data/biome_parameters.json` has cherry_grove 60 / mangrove_swamp 14 / pale_garden 40 / lush_caves 1 climate cells), so a player reaches them — the milestone mandate ("full vanilla per-biome vegetation, 1:1 from the jar") requires PORTING them, not deferring. The new work, verified against the real configs this session:
- **The RootPlacer subsystem** (research Wave D item 26) — 13-01 added the optional `root_placer` FIELD + the `TreeFeature.place` hook; this plan ports the real `mangrove_root_placer` body (mangrove.json — the only overworld root_placer; azalea uses a bending trunk + below_trunk_provider rooted_dirt, NO root_placer, verified).
- **Special trunk placers**: `cherry_trunk_placer` (cherry), `upwards_branching_trunk_placer` (mangrove), `bending_trunk_placer` (azalea). pale_oak REUSES `dark_oak_trunk_placer` (13-02).
- **Special foliage placers**: `cherry_foliage_placer` (cherry), `random_spread_foliage_placer` (mangrove + azalea). pale_oak REUSES `dark_oak_foliage_placer` (13-02).
- **Special decorators + the deferred provider**: `randomized_int_state_provider` (the 12-01-deferred provider mangrove's `attached_to_leaves` needs), `attached_to_leaves` (mangrove propagules), `pale_moss` (pale_garden), `creaking_heart` (pale_oak_creaking — referenced by the generatable `pale_oak_creaking_checked` placed_feature, verified).

All placers/foliage/decorators plug into 13-01's pure bases (the setBlock/read callback seam — no `world` import); the root body fills 13-01's optional field; the decorators run through 13-02's decorator loop. This plan depends on 13-02 (the common roster + the decorator loop + ParseTreeDecorator) and extends the Parse* dispatch + the body's rootPlacer hook — SEQUENCED after 13-02 (wave 3), sharing `tree.go`/`feature_tree.go`/`provider.go`.

Purpose: the FULL generatable-overworld tree set renders 1:1 — every biome a player can reach grows its correct vanilla tree, deterministic per seed, with the completeness guard proving no gaps.
Output: tree_special.go (the special placers/foliage/root/decorators) + the randomized_int_state_provider port + the extended dispatch/body, with per-special draw-order tests + THE all-overworld-tree-configs-decode completeness guard. ZERO new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/phases/13-trees-dungeon-features-gate/13-01-PLAN.md
@.planning/phases/13-trees-dungeon-features-gate/13-02-PLAN.md

<interfaces>
<!-- VERIFIED on disk this session (the four special configs read). Re-read 13-01-SUMMARY (RootPlacer field + hook) + 13-02-SUMMARY (the decorator loop + ParseTreeDecorator) BEFORE coding. -->

THE 13-01 ROOT HOOK + FIELD (world/levelgen/feature/tree.go, BUILT by 13-01):
```go
// TreeConfiguration.rootPlacer RootPlacer (OPTIONAL, nil for non-mangrove)
type RootPlacer interface { placeRoots(setBlock setBlockFn, read readFn, rng levelgen.RandomSource, pos BlockPos, cfg *TreeConfiguration) (trunkOrigin BlockPos, ok bool) }
// ParseRootPlacer(raw) -> RootPlacer  (13-01: stub erroring on mangrove_root_placer -> THIS plan fills it)
// TreeFeature.place calls cfg.rootPlacer.placeRoots BEFORE placeTrunk when non-nil (shifts the trunk origin up by trunk_offset_y).
```

THE 13-02 BASES + DECORATOR LOOP (world/levelgen/feature/tree_placers.go + tree_decorator.go + feature_tree.go, BUILT by 13-02):
```go
// TrunkPlacer/FoliagePlacer bases + ParseTrunkPlacer/ParseFoliagePlacer (common ported; special arms error "ported in 13-03")
// TreeDecorator interface { place(ctx *DecoratorContext) } + DecoratorContext{logs,leaves,roots []BlockPos; rng; setBlock; read}
// ParseTreeDecorator(raw) (common ported; attached_to_leaves/pale_moss/creaking_heart error "ported in 13-03")
// the tree body collects placed log/leaf positions + runs the decorator loop after foliage
```
CONFIRM the AS-BUILT signatures from 13-01/13-02 SUMMARY. This plan replaces the remaining "ported in 13-03" error arms with real cases.

THE DEFERRED PROVIDER (world/levelgen/feature/provider.go, Phase 12 — the error arm to replace):
```go
// ParseProvider default arm currently: "...randomized_int_state_provider deferred to Phase 13" (provider.go:366, VERIFIED)
// randomized_int_state_provider = RandomizedIntStateProvider{ source BlockStateProvider; property string; values IntProvider }
//   getState: s := source.GetState(rng, x,y,z); v := values.Sample(rng); return s with `property` set to v  (e.g. mangrove_propagule age 0..4)
```
Port it here (mangrove's attached_to_leaves block_provider needs it). The IntProvider sample is the 12-03 minimal in-package IntProvider pattern (constant/uniform) — the values here are `uniform{0,4}`. Setting a StateID's int property requires re-resolving the {Name,Properties} with the property overridden (use the existing resolveBlockState path with a property map mutation, or enumerate the source block's states by the property value).

THE REAL SPECIAL CONFIGS (VERIFIED on disk world/levelgen/data/configured_feature/):
```
cherry.json:  cherry_trunk_placer{base_height:7, branch_count: weighted_list[1,2,3], branch_horizontal_length: uniform{2,4}, branch_start_offset_from_top:{-4,-3}, branch_end_offset_from_top: uniform{-1,0}} + cherry_foliage_placer{radius:4,height:5, corner_hole_chance:0.25, wide_bottom_layer_hole_chance:0.25, hanging_leaves_chance:0.1667, hanging_leaves_extension_chance:0.3333} + two_layers_feature_size{upper_size:2} + decorators:[] (cherry_bees_005 adds beehive)
mangrove.json: upwards_branching_trunk_placer{base_height:2, height_rand_a:1, height_rand_b:4, extra_branch_steps: uniform{1,4}, extra_branch_length: uniform{0,1}, place_branch_per_log_probability:0.5, can_grow_through:#mangrove_logs_can_grow_through} + random_spread_foliage_placer{radius:3, foliage_height:2, leaf_placement_attempts:70} + mangrove_root_placer{...} + decorators:[leave_vine{0.125}, attached_to_leaves{block_provider: randomized_int_state_provider(mangrove_propagule age uniform{0,4}), directions:[down], probability:0.14, exclusion_radius_xz:1, required_empty_blocks:2}, beehive{0.01}] + two_layers_feature_size{limit:2, upper_size:2}
azalea_tree.json: bending_trunk_placer{base_height:4, height_rand_a:2, bend_length: uniform{1,2}, min_height_for_leaves:3} + random_spread_foliage_placer{radius:3, foliage_height:2, leaf_placement_attempts:50} + below_trunk_provider: simple rooted_dirt (NO root_placer) + foliage_provider: weighted{azalea_leaves:3, flowering_azalea_leaves:1} + decorators:[]
pale_oak.json: dark_oak_trunk_placer (REUSE 13-02) + dark_oak_foliage_placer (REUSE 13-02) + three_layers_feature_size{upper_size:2} + decorators:[pale_moss{ground_probability:0.8, leaves_probability:0.15, trunk_probability:0.4}]
pale_oak_creaking.json: same as pale_oak + decorators:[pale_moss, creaking_heart]  (referenced by the generatable pale_oak_creaking_checked placed_feature)
```
DERIVE the exact draw order from the bytecode (javap -c, cite each class) — the configs give the params, the bytecode gives the draw sequence.

THE RNG (world/levelgen/random.go — VERIFIED): NextIntN/NextFloat/NextBoolean/NextInt on the threaded LegacyRandomSource. All special placers/foliage/root/decorators draw on the SAME threaded rng. DO NOT fork a source.
</interfaces>

<key_facts>
<!-- PORT from jar bytecode; cite each class. The four special configs are VERIFIED on disk. No GPL paste. -->
- THE FOUR BIOMES ARE GENERATABLE (verified): biome_parameters.json has cherry_grove(60)/mangrove_swamp(14)/pale_garden(40)/lush_caves(1) climate cells -> the multi-noise source produces them -> the milestone mandate requires PORTING their trees, not deferring.
- RootPlacer (mangrove_root_placer, javap -c cite MangroveRootPlacer.placeRoots + RootPlacer base): grow root columns down from the trunk base through #mangrove_roots_can_grow_through (max_root_length 15 / max_root_width 8), swap mud for muddy_mangrove_roots (muddy_roots_in/provider), optionally place above_root (moss_carpet) per above_root_placement_chance, with random_skew_chance, then return the trunk origin shifted up by trunk_offset_y (uniform{1,3}). The DRAW ORDER is the determinism contract. azalea has NO root_placer (uses bending trunk + below_trunk_provider rooted_dirt — verified).
- cherry_trunk_placer (javap -c): the main trunk + `branch_count` (weighted_list[1,2,3]) branches, each at a `branch_start_offset_from_top` height, `branch_horizontal_length` long, ending `branch_end_offset_from_top` — transcribe the branch direction + curve draws. branch_count is a `weighted_list` IntProvider (the 12-03 minimal in-package IntProvider — port the weighted_list kind here in feature, or reuse if 13-02 added it).
- upwards_branching_trunk_placer (javap -c): the column + per-log `place_branch_per_log_probability` nextFloat branch of `extra_branch_steps`/`extra_branch_length`, gated by can_grow_through. Returns the foliage attachments at the branch tips.
- bending_trunk_placer (javap -c): a straight trunk to min_height_for_leaves then a bend of `bend_length` in a random horizontal direction (Direction.getRandom draw).
- cherry_foliage_placer (javap -c): the cherry canopy + the corner-hole / wide-bottom-hole / hanging-leaves probability draws (each a nextFloat gate per the config chances).
- random_spread_foliage_placer (javap -c): `leaf_placement_attempts` tries, each a random offset within radius x foliage_height (the scatter draws), placing a leaf if free.
- randomized_int_state_provider (javap -c RandomizedIntStateProvider.getState): source.getState then randomize one int property via the IntProvider (mangrove_propagule age uniform{0,4}). Replace the 12-01 ParseProvider deferred arm (provider.go:366). Setting the property = re-resolve the source state's {Name,Properties} with `property`=v.
- Decorators (javap -c cite each): attached_to_leaves (attach the randomized propagule under leaf blocks per probability/directions/exclusion_radius/required_empty_blocks), pale_moss (PaleMossDecorator: ground/leaves/trunk pale-moss + carpet per the three probabilities), creaking_heart (CreakingHeartDecorator: a creaking_heart block in the trunk). leave_vine/beehive are 13-02's (reused).
- minimum_size: cherry/mangrove use parameterized two_layers (upper_size:2 / limit:2); pale_oak uses three_layers (13-02 added ThreeLayersFeatureSize). Confirm getSizeAtLayer.
- THE COMPLETENESS GUARD (the objective gate): TestAllOverworldTreeConfigsDecode iterates EVERY embedded `tree` config for a generatable biome (the full set incl. cherry/mangrove/tall_mangrove/azalea_tree/pale_oak/pale_oak_creaking) and asserts ParseTreeConfiguration succeeds for ALL — the analogue of 12-02's all-ore-configs-decode guard. After this plan there is NO loud-error path for any generatable overworld tree; 13-01/13-02's decode-or-skip never fires in production.
- ZERO new deps; single-threaded; the Docker -race gate is 13-04's. STALE-LSP: trust `go build`.
- This plan SHARES tree.go/feature_tree.go/provider.go with 13-01/13-02 — wave 3 depends_on [13-02], NOT parallel. The special placer/foliage/root/decorator BODIES live in tree_special.go; only the Parse* arms + the body's rootPlacer hook + the provider.go arm touch the shared files.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the RootPlacer subsystem + the special-biome trunk/foliage placers (cherry/mangrove/azalea)</name>
  <files>world/levelgen/feature/tree_special.go, world/levelgen/feature/tree_special_test.go, world/levelgen/feature/tree.go</files>
  <behavior>
    - MangroveRootPlacer.placeRoots grows the jar-exact root columns down through the can-grow-through set (muddy-roots swap, above-root moss carpet per chance, random skew), returns the trunk origin shifted by trunk_offset_y — draw-pinned; the 13-01 rootPlacer field now resolves it.
    - cherry_trunk_placer places the trunk + the weighted branch_count branches (start/horizontal-length/end offsets) jar-exact; upwards_branching_trunk_placer places the column + per-log probability branches; bending_trunk_placer bends by bend_length in a random direction after min_height_for_leaves — each draw-pinned.
    - cherry_foliage_placer (corner/wide-bottom/hanging-leaves probability draws) + random_spread_foliage_placer (leaf_placement_attempts scatter) place jar-exact for a seed.
    - ParseTrunkPlacer/ParseFoliagePlacer/ParseRootPlacer now resolve cherry/upwards_branching/bending/cherry_foliage/random_spread/mangrove_root_placer (the 13-01/13-02 "ported in 13-03" arms become real cases).
  </behavior>
  <action>
tree_special.go: port (javap -c, cite each class) MangroveRootPlacer.placeRoots + the RootPlacer base; cherry_trunk_placer / upwards_branching_trunk_placer / bending_trunk_placer; cherry_foliage_placer / random_spread_foliage_placer — each on the 13-01 TrunkPlacer/FoliagePlacer/RootPlacer base (the setBlock/read callback seam). Port the weighted_list + uniform IntProviders the cherry branch_count / trunk_offset_y / bend_length need (the 12-03 minimal in-package IntProvider pattern, or reuse if 13-02 added them). In tree.go: replace the special-biome "ported in 13-03" arms in ParseTrunkPlacer/ParseFoliagePlacer with real cases + fill ParseRootPlacer's mangrove_root_placer case.
tree_special_test.go: TestMangroveRootPlacer (root columns + trunk_offset_y shift, draw-pinned). TestCherryTrunkPlacer (branch_count branches + offsets). TestUpwardsBranchingTrunkPlacer (per-log branches). TestBendingTrunkPlacer (the bend direction + length). TestCherryFoliagePlacer (the hole/hanging probability draws). TestRandomSpreadFoliagePlacer (the attempts scatter). Each pinned to an oracle.
  </action>
  <verify>
    <automated>cd D:/ender && go build ./world/levelgen/feature/ && go test ./world/levelgen/feature/ -run 'TestMangroveRootPlacer|TestCherryTrunkPlacer|TestUpwardsBranchingTrunkPlacer|TestBendingTrunkPlacer|TestCherryFoliagePlacer|TestRandomSpreadFoliagePlacer' -count=1</automated>
  </verify>
  <done>The RootPlacer subsystem (MangroveRootPlacer) + the special-biome trunk/foliage placers (cherry/upwards_branching/bending + cherry_foliage/random_spread) place jar-exact for known seeds (draw-pinned), and the Parse* dispatch resolves them all — the 13-01/13-02 special-biome error arms are closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: randomized_int_state_provider + the special decorators + the FULL completeness guard + per-special-biome smokes</name>
  <files>world/levelgen/feature/provider.go, world/levelgen/feature/provider_test.go, world/levelgen/feature/tree_special.go, world/levelgen/feature/tree_special_test.go, world/feature_tree.go, world/feature_tree_test.go</files>
  <behavior>
    - randomized_int_state_provider (provider.go) draws the source state then randomizes the configured int property via the IntProvider (mangrove_propagule age uniform{0,4}) — the 12-01 ParseProvider deferred arm is now a real case, draw-pinned.
    - AttachedToLeavesDecorator attaches the randomized propagule under leaf blocks per probability/directions/exclusion_radius/required_empty_blocks; PaleMossDecorator places pale moss/carpet per ground/leaves/trunk probabilities; CreakingHeartDecorator places a creaking_heart in the trunk — each draw-exact via the 13-02 decorator loop.
    - ParseTreeDecorator resolves attached_to_leaves/pale_moss/creaking_heart (the 13-02 "ported in 13-03" arms become real); the tree body's rootPlacer hook runs the real mangrove roots.
    - TestAllOverworldTreeConfigsDecode: EVERY embedded `tree` config for a generatable biome (cherry/mangrove/tall_mangrove/azalea_tree/pale_oak/pale_oak_creaking + all common) decodes end-to-end — NO loud-error path remains.
    - Per-special-biome smoke: cherry_grove->cherry, mangrove_swamp->mangrove (roots+propagules), pale_garden->pale_oak (pale_moss), lush_caves->azalea resolve through the selector to real trees.
  </behavior>
  <action>
provider.go: replace the ParseProvider `randomized_int_state_provider` deferred error arm (provider.go:366) with a real RandomizedIntStateProvider{source, property, values IntProvider} — getState draws source.GetState then values.Sample and sets the int property (re-resolve the source {Name,Properties} with the property overridden). Port the in-package IntProvider (uniform) it needs.
tree_special.go: port (javap -c, cite each) AttachedToLeavesDecorator / PaleMossDecorator / CreakingHeartDecorator on the 13-02 DecoratorContext. In tree.go (ParseTreeDecorator) replace the 13-02 special-arm errors with real cases. In feature_tree.go: confirm the rootPlacer hook (13-01) now runs the real mangrove roots in the body's place sequence (before the trunk).
provider_test.go: TestRandomizedIntStateProvider (source + property-randomize draw, mangrove_propagule age).
tree_special_test.go: TestAttachedToLeavesDecorator / TestPaleMossDecorator / TestCreakingHeartDecorator (draw-pinned) + **TestAllOverworldTreeConfigsDecode** (iterate every generatable-overworld `tree` config -> ParseTreeConfiguration succeeds; explicitly include cherry/mangrove/tall_mangrove/azalea_tree/pale_oak/pale_oak_creaking).
feature_tree_test.go (extend): TestCherryTree / TestMangroveTreeWithRoots (roots + propagules placed) / TestPaleOakWithPaleMoss / TestAzaleaTree (bending + random_spread) + TestPerSpecialBiomeSmoke (the four biomes resolve through the selector to real trees).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/levelgen/feature/ -run 'TestRandomizedIntStateProvider|TestAttachedToLeaves|TestPaleMoss|TestCreakingHeart|TestAllOverworldTreeConfigsDecode' -count=1 && go test ./world/ -run 'TestCherryTree|TestMangroveTreeWithRoots|TestPaleOakWithPaleMoss|TestAzaleaTree|TestPerSpecialBiomeSmoke' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>randomized_int_state_provider + the special decorators (attached_to_leaves/pale_moss/creaking_heart) are ported, the mangrove root hook runs in the body, and TestAllOverworldTreeConfigsDecode proves EVERY generatable-overworld tree config (cherry/mangrove/pale_oak/azalea included) decodes end-to-end — 13-01/13-02's deferred unported-placer skip is fully closed; the four special biomes resolve to real trees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → special placer/root/decorator/provider decode | The four special configs (from the pinned 26.2 jar) are build-time-trusted. The bodies run off-tick on the single worker scheduler goroutine; no client input reaches them. |
| root/decorator → cross-chunk Neighborhood | The mangrove roots / propagules / pale-moss write through the same bctx.view (blockStateWriteRadius=1) as the trunk/foliage; out-of-3x3 writes are dropped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering | RootPlacer / special-placer draw-order divergence (mangrove roots, cherry branches, bending trunk) | mitigate | Each is transcribed javap -c + pinned by a geometry+draw-order test vs an oracle; the root-then-trunk ordering (placeRoots before placeTrunk, shifting the origin) is pinned. A reorder fails the test. |
| T-13-15 | Tampering | randomized_int_state_provider setting the wrong property/value (mangrove_propagule age) | mitigate | The provider re-resolves the source {Name,Properties} with the property overridden + the IntProvider draw pinned; TestRandomizedIntStateProvider asserts the exact age value + the post-draw rng fingerprint. |
| T-13-16 | Spoofing/Integrity (incomplete vegetation set — the milestone risk) | a generatable biome's tree silently skipped → cherry/mangrove/pale_oak/azalea missing | mitigate | TestAllOverworldTreeConfigsDecode iterates EVERY generatable-overworld tree config (the four special biomes explicitly included, verified present in biome_parameters.json) + asserts decode; no loud-error/skip path remains. This is the objective acceptance for the milestone "full 1:1 vegetation set" mandate. |
| T-13-17 | Denial of Service | the mangrove root recursion (max_root_length 15 / width 8) writing many cross-chunk blocks | accept | Bounded by max_root_length/width + the can-grow-through set; out-of-3x3 writes dropped by the proxy; the worker queue is bounded (Phases 4/8). No new control needed. |
</threat_model>

<verification>
- `go test ./world/levelgen/feature/ -run 'TestMangroveRootPlacer|TestCherryTrunkPlacer|TestUpwardsBranchingTrunkPlacer|TestBendingTrunkPlacer|TestCherryFoliagePlacer|TestRandomSpreadFoliagePlacer|TestRandomizedIntStateProvider|TestAttachedToLeaves|TestPaleMoss|TestCreakingHeart|TestAllOverworldTreeConfigsDecode' -count=1` green: the special placers/root/foliage/decorators/provider are jar-exact + draw-pinned, and EVERY generatable-overworld tree config decodes (the completeness guard).
- `go test ./world/ -run 'TestCherryTree|TestMangroveTreeWithRoots|TestPaleOakWithPaleMoss|TestAzaleaTree|TestPerSpecialBiomeSmoke' -count=1` green: the four special biomes resolve through the selector to real trees.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps; feature does NOT import placement or world (acyclic).
</verification>

<success_criteria>
- FEAT-04 (FULL criterion 1, 1:1): EVERY generatable overworld biome grows its correct vanilla tree set — oak/birch (13-01) + spruce/pine/jungle/acacia/dark_oak/mega (13-02) + cherry/mangrove/pale_oak/azalea (here, with the RootPlacer subsystem + the special foliage/decorators/provider), deterministic per seed. The milestone "full vanilla per-biome vegetation, 1:1 from the jar" mandate is honored — no biome's tree deferred.
- The completeness guard PASSES: TestAllOverworldTreeConfigsDecode iterates every generatable-overworld `tree` config (cherry/mangrove/pale_oak/azalea explicitly included, verified present in biome_parameters.json) and decodes them all — 13-01/13-02's deferred unported-placer skip is fully closed; no loud-error path remains for any production overworld tree.
- The determinism contract holds across the full roster: each special placer/root/foliage/decorator/provider draw order is pinned to an oracle; the root-before-trunk ordering is correct; everything writes through the cross-chunk Neighborhood on the threaded rng.
</success_criteria>

<output>
After completion, create `.planning/phases/13-trees-dungeon-features-gate/13-03-SUMMARY.md`
</output>
