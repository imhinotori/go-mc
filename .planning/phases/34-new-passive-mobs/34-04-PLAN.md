---
phase: 34-new-passive-mobs
plan: 04
type: execute
wave: 3
depends_on: [34-00, 34-01, 34-02, 34-03]
files_modified:
  - server/vanilla_pig_embed.go
  - server/vanilla_pig.go
  - cmd/sulfur/main.go
  - server/commands_dbg.go
  - server/async.go
  - server/vanilla_mob_test.go
autonomous: false
requirements: [MOB-PASS-01, MOB-PASS-02, MOB-PASS-03]
must_haves:
  truths:
    - "The boot-load materializes + loads ALL FOUR vanilla mob plugins (pig/cow/sheep/chicken) into the ONE mobRegistry; a missing declaration fails loudly"
    - "spawnVanillaMob(name, x, y, z) spawns any declared mob by name; spawnVanillaPig stays a thin wrapper so the pig oracle call sites are byte-identical"
    - "/dbg cow|sheep|chicken each spawn the right mob at the player"
    - "Natural creature spawning picks among the 4 CREATURE mobs (not pig-hardcoded)"
    - "TestPluginPigEqualsGoNativePig stays byte-identical (the pig oracle is GREEN through the generalization)"
  artifacts:
    - path: "server/vanilla_pig_embed.go"
      provides: "LoadVanillaMobRegistry loading all 4 embeds"
      contains: "LoadVanillaMobRegistry"
    - path: "server/vanilla_pig.go"
      provides: "spawnVanillaMob(name,...) generic spawn + spawnVanillaPig thin wrapper"
      contains: "func (t *TickLoop) spawnVanillaMob"
    - path: "server/commands_dbg.go"
      provides: "/dbg cow|sheep|chicken cases"
      contains: "case \"cow\""
    - path: "server/async.go"
      provides: "natural spawn picks among the 4 CREATURE mobs"
      contains: "spawnVanillaMob"
  key_links:
    - from: "cmd/sulfur/main.go boot"
      to: "LoadVanillaMobRegistry"
      via: "the renamed loader call"
      pattern: "LoadVanillaMobRegistry"
    - from: "server/async.go naturalSpawn apply"
      to: "spawnVanillaMob"
      via: "name pick among CREATURE mobs"
      pattern: "spawnVanillaMob"
    - from: "server/vanilla_pig.go spawnVanillaPig"
      to: "spawnVanillaMob"
      via: "thin wrapper (oracle call sites unchanged)"
      pattern: "spawnVanillaMob(vanillaPigMobName"
---

<objective>
THE GATE (MOB-PASS-01/02/03 close): generalize the pig-hardcoded boot-load + spawn helpers to load and
spawn all four vanilla mobs from the one registry, wire the spawn levers (/dbg + natural spawn + main.go
boot), and prove the pig oracle stays byte-identical AND the three new mobs spawn + behave live.

Purpose: The new mobs (Plans A/B/C) declare their plugins, but nothing loads or spawns them yet. This
plan makes them live: extend the boot-load to all 4, generalize spawnVanillaPig -> spawnVanillaMob(name),
add the dbg + natural-spawn levers, and run the full verification gate (build + vet + the pig oracle +
the per-mob tests + Docker -race + a live bot pass).
Output: generalized boot-load/spawn, /dbg cow|sheep|chicken, natural-spawn mob pick, the pig oracle green.

THE PIG ORACLE IS THE STANDING GATE: vanilla_pig.go's spawnVanillaPig / spawnVanillaPigWithID and both
vanilla_pig/main.star copies stay UNTOUCHED in behavior. spawnVanillaPig becomes a thin wrapper over
spawnVanillaMob(vanillaPigMobName, ...) so the oracle test (TestPluginPigEqualsGoNativePig) is byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-new-passive-mobs/34-CONTEXT.md
@.planning/phases/34-new-passive-mobs/34-PATTERNS.md
@.planning/phases/34-new-passive-mobs/34-01-SUMMARY.md
@.planning/phases/34-new-passive-mobs/34-02-SUMMARY.md
@.planning/phases/34-new-passive-mobs/34-03-SUMMARY.md

<interfaces>
<!-- Generalize the pig-specific boot-load + spawn. The registry byName map already holds all 4 (it is -->
<!-- map[string]*mobDecl); the work is loading all 4 embeds into it + a name-parameterized spawn. -->

server/vanilla_pig_embed.go (CURRENT — generalize):
  //go:embed assets/vanilla_pig/plugin.toml assets/vanilla_pig/main.star
  var vanillaPigFS embed.FS
  const vanillaPigMobName = "vanilla_pig"
  func loadVanillaPigRegistry() (*mobRegistry, error)   // materializes ONE plugin, loads, asserts ONE decl
  func materializeVanillaPig() (string, error)          // writes assets/vanilla_pig/{toml,star} to temp
  func vanillaPigCaps() (capSet, error)                 // parses assets/vanilla_pig/plugin.toml caps

server/vanilla_pig.go (CURRENT — generalize):
  func LoadVanillaPigRegistry() (*mobRegistry, error) { return loadVanillaPigRegistry() }   // exported boot entry
  func (t *TickLoop) SetMobRegistry(r *mobRegistry)                                          // UNCHANGED
  func (t *TickLoop) spawnVanillaPig(x, y, z float64) *Entity {                              // -> thin wrapper
      decl := t.mobRegistry.byName[vanillaPigMobName]; return t.spawnDeclaredMob(decl, x, y, z) }
  func (t *TickLoop) spawnVanillaPigWithID(id int32, x, y, z float64) *Entity                // UNCHANGED (oracle)

cmd/sulfur/main.go:347 (CURRENT):
  if reg, err := server.LoadVanillaPigRegistry(); err != nil { log.Fatalf(...) } else {
      tick.SetMobRegistry(reg); log.Printf("vanilla_pig: ... boot-loaded ...") }

server/commands_dbg.go:14-31: the "pig" case -> t.spawnVanillaPig(p.x,p.y,p.z); the default usage string.

server/async.go:334-338 (CURRENT natural spawn): the swap call site hardcodes spawnVanillaPig. The
naturalSpawner counts categoryCreature (async.go:314); cow/sheep/chicken are all CREATURE (categoryOf,
34-CONTEXT.md:44) sharing the same cap.

mobRegistry (plugin_mob_decl.go:80-95): byName map[string]*mobDecl + caps + setLoadCaps (per-load).
The 4 mob names: "vanilla_pig", "vanilla_cow", "vanilla_sheep", "vanilla_chicken".

DOCKER -race GATE (34-JARNOTES.md:288-291 VERBATIM — the repo's existing -race invocation, lifted from
33-05; do NOT re-derive or "grep the Makefile"):
  MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/
  Append ` 2>&1 | tail -20` for the verify. TestRegionPanicIsolated prints a scary recovered-panic stack
  in a full run — that is a KNOWN false alarm; judge the FINAL ok/FAIL line, not the mid-run stack.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize boot-load (all 4 embeds) + spawnVanillaMob(name) + main.go + /dbg</name>
  <read_first>
    - server/vanilla_pig_embed.go (the full file — generalize loadVanillaPigRegistry/materialize/caps to a per-mob loop)
    - server/vanilla_pig.go (the full file — generalize spawnVanillaPig -> spawnVanillaMob + keep the pig wrapper + spawnVanillaPigWithID UNCHANGED)
    - cmd/sulfur/main.go:347-357 (the boot-load call + log line)
    - server/commands_dbg.go:13-32 (the /dbg dispatcher + the pig case + usage string)
    - server/plugin_mob_decl.go:80-95 (mobRegistry byName + setLoadCaps — load each mob into the shared registry with its own caps)
    - .planning/phases/34-new-passive-mobs/34-PATTERNS.md:149-228 (the boot-load + spawn + main.go + dbg generalization guidance)
  </read_first>
  <action>
1. server/vanilla_pig_embed.go — generalize to load ALL FOUR mobs into one registry:
   - Extend the embed glob to all 4 plugin dirs:
       //go:embed assets/vanilla_pig assets/vanilla_cow assets/vanilla_sheep assets/vanilla_chicken
     (embedding the dirs covers plugin.toml + main.star for each). Rename the var to vanillaMobFS (keep
     a vanillaPigFS alias if other code references it, OR update references).
   - Add name constants: vanillaCowMobName = "vanilla_cow", vanillaSheepMobName = "vanilla_sheep",
     vanillaChickenMobName = "vanilla_chicken" (keep vanillaPigMobName).
   - Add a slice vanillaMobNames = []string{vanillaPigMobName, vanillaCowMobName, vanillaSheepMobName,
     vanillaChickenMobName}.
   - Replace loadVanillaPigRegistry() with loadVanillaMobRegistry() that LOOPS over vanillaMobNames:
     for each name, materialize its embed to a temp dir (parameterize materializeVanillaPig ->
     materializeVanillaMob(name) using the "assets/<name>/" prefix), parse its caps
     (vanillaPigCaps -> vanillaMobCaps(name)), r.setLoadCaps(caps) BEFORE that mob's LoadDirWith
     (per-mob caps, since setLoadCaps is per-load — plugin_mob_decl.go:95), and LoadDirWith into the
     SAME registry r. After the loop, assert EACH of the 4 names is present in r.byName (keep the LOUD
     per-mob failure — vanilla_pig_embed.go:69-73, now per name). Cite the per-mob load discipline.
     IMPORTANT: create ONE mobRegistry r = newMobRegistry() before the loop; all 4 load into it.

2. server/vanilla_pig.go — generalize the spawn:
   - Rename the exported LoadVanillaPigRegistry -> LoadVanillaMobRegistry (delegates to
     loadVanillaMobRegistry()). Keep a LoadVanillaPigRegistry alias ONLY if needed by tests; prefer
     updating callers.
   - Add func (t *TickLoop) spawnVanillaMob(name string, x, y, z float64) *Entity: look up
     t.mobRegistry.byName[name], LOUD panic if registry nil or name missing (mirror spawnVanillaPig's
     guards, parameterized by name), delegate to t.spawnDeclaredMob(decl, x, y, z).
   - Make spawnVanillaPig a thin wrapper: return t.spawnVanillaMob(vanillaPigMobName, x, y, z). Its
     guards now live in spawnVanillaMob. Keep spawnVanillaPigWithID UNCHANGED (the oracle test helper —
     do NOT route it through the generic path; it pins an id and stays byte-identical).

3. cmd/sulfur/main.go:347 — rename the call: server.LoadVanillaMobRegistry(). Update the log line to
   "vanilla mobs: bundled 1:1 pig/cow/sheep/chicken plugins boot-loaded". Keep the log.Fatalf-on-error
   (a missing mob is a broken server).

4. server/commands_dbg.go — add cases to runDbgCommand:
       case "cow":     e := t.spawnVanillaMob(vanillaCowMobName, p.x, p.y, p.z) ; ...broadcast...
       case "sheep":   e := t.spawnVanillaMob(vanillaSheepMobName, p.x, p.y, p.z) ; ...
       case "chicken": e := t.spawnVanillaMob(vanillaChickenMobName, p.x, p.y, p.z) ; ...
   Each mirrors the pig case (broadcastSystemChat with eid + pos). Update the default usage string to
   "/dbg pig | cow | sheep | chicken | water | pig-in-water".
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./...</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go vet ./server/ ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` exits 0
    - `CGO_ENABLED=0 go vet ./server/ ./cmd/...` exits 0
    - `grep -c 'func loadVanillaMobRegistry' server/vanilla_pig_embed.go` returns 1
    - `grep 'assets/vanilla_cow' server/vanilla_pig_embed.go` matches AND `grep 'assets/vanilla_sheep' server/vanilla_pig_embed.go` matches AND `grep 'assets/vanilla_chicken' server/vanilla_pig_embed.go` matches
    - `grep -c 'func (t \*TickLoop) spawnVanillaMob' server/vanilla_pig.go` returns 1
    - `grep 'spawnVanillaMob(vanillaPigMobName' server/vanilla_pig.go` matches (the thin wrapper)
    - `grep 'LoadVanillaMobRegistry' cmd/sulfur/main.go` matches
    - `grep 'case "cow"' server/commands_dbg.go` matches AND `grep 'case "sheep"' server/commands_dbg.go` matches AND `grep 'case "chicken"' server/commands_dbg.go` matches
    - `grep 'spawnVanillaPigWithID' server/vanilla_pig.go` still present and unchanged in signature
  </acceptance_criteria>
  <done>Boot-load loads all 4 embeds into one registry (loud per-mob failure); spawnVanillaMob(name) is the generic spawn; spawnVanillaPig is a thin wrapper; spawnVanillaPigWithID untouched; main.go + /dbg wired.</done>
</task>

<task type="auto">
  <name>Task 2: Natural-spawn mob pick + the gate verification (pig oracle + all mobs + Docker -race)</name>
  <read_first>
    - server/async.go:300-340 (the naturalSpawn apply: the cap re-check + the spawnVanillaPig swap call site at :334-338)
    - server/vanilla_pig.go (the new spawnVanillaMob from Task 1)
    - server/plugin_pig_test.go:267 (TestPluginPigEqualsGoNativePig — must stay green; do NOT edit it)
    - .planning/phases/34-new-passive-mobs/34-PATTERNS.md:231-246 (the natural-spawn generalization guidance)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:288-291 (the Docker -race command — lift VERBATIM)
  </read_first>
  <action>
1. server/async.go (the naturalSpawn apply, :334-338) — generalize the pig-hardcoded spawn to a name
   pick among the 4 CREATURE mobs. Replace t.spawnVanillaPig(...) with a pick:
       name := t.pickNaturalCreatureMob()   // returns one of the 4 vanilla mob names
       t.withRegion(dest, func() { t.spawnVanillaMob(name, float64(c.x)+0.5, float64(c.y), float64(c.z)+0.5) })
   Add pickNaturalCreatureMob(): a deterministic-seedable pick among {vanilla_pig, vanilla_cow,
   vanilla_sheep, vanilla_chicken}. Use a SEEDED source (NOT the unseeded global rand.IntN — note the
   STATE.md carryover flake at async_stress_test about the unseeded global spawner RNG). Prefer a
   per-TickLoop seeded *rand.Rand the spawner owns, or pick by (gametime/column) hash so the choice is
   deterministic + test-stable. Document the pick policy (uniform among the 4 for v1; vanilla biome
   spawn weights are a cited deferral). The per-tick throttle (one placement per apply, :338) and the
   cross-region cap re-check (:308-317, categoryCreature shared cap) are UNCHANGED — all 4 are CREATURE.
   Do NOT change the cap math (the /289 divisor, creatureCap).

2. Create server/vanilla_mob_test.go:
   - TestAllFourMobsBootLoad: loadVanillaMobRegistry() returns a registry with all 4 names present
     (pig/cow/sheep/chicken), each with the right goal count (pig 9, cow 8, sheep 9, chicken 8).
   - TestSpawnVanillaMobByName: spawnVanillaMob("vanilla_cow"/...) spawns each as the right entity type.
   - TestPigOracleStillGreen: a thin assertion that spawnVanillaPig still routes to the pig decl and
     spawnVanillaPigWithID is unchanged (or just rely on TestPluginPigEqualsGoNativePig — see below).
   - TestNaturalSpawnPicksAmongFour: pickNaturalCreatureMob over many seeds returns each of the 4 names
     (no mob is unreachable) and only the 4 valid names.

3. Run the FULL gate verification (the standing constraints):
   - CGO_ENABLED=0 go build ./...
   - CGO_ENABLED=0 go vet ./server/
   - The PIG ORACLE: CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig -v (MUST be
     byte-identical green — the generalization must not have perturbed the pig path).
   - All per-mob + new tests:
     CGO_ENABLED=0 go test ./server/ -run 'TestCow|TestSheep|TestChicken|TestEatBlock|TestMilk|TestEggLay|TestSlowFall|TestShear|TestAllFourMobs|TestSpawnVanillaMob|TestNaturalSpawn|TestVanillaPig|TestPigGoalSet|TestPluginPig' -v
   - Docker -race + strictRegion (the standing constraint) — run the repo's existing -race invocation
     VERBATIM (34-JARNOTES.md:288-291), over ./server/, and judge the FINAL ok/FAIL line:
       MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ 2>&1 | tail -20
     NOTE: TestRegionPanicIsolated prints a scary recovered-panic stack mid-run — a KNOWN false alarm.
     The gate is the FINAL "ok ... / FAIL" line (must be ok, no real data race), NOT the mid-run stack.
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./server/</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig|TestAllFourMobsBootLoad|TestSpawnVanillaMobByName|TestNaturalSpawnPicksAmongFour' -v</automated>
    <automated>MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` exits 0 AND `CGO_ENABLED=0 go vet ./server/` exits 0
    - `grep -c 'func (t \*TickLoop) pickNaturalCreatureMob' server/async.go` returns 1
    - `grep 'spawnVanillaMob' server/async.go` matches (the natural-spawn call uses the generic spawn)
    - `grep -v '^#' server/async.go | grep -c 'rand.IntN'` for the new pick does NOT use the unseeded global (use a seeded source) — verify the pick reads a seeded *rand.Rand or a deterministic hash
    - `CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig -v` exits 0 (THE GATE — byte-identical)
    - `CGO_ENABLED=0 go test ./server/ -run 'TestCow|TestSheep|TestChicken|TestEatBlock|TestMilk|TestEggLay|TestSlowFall|TestShear|TestAllFourMobs|TestSpawnVanillaMob|TestNaturalSpawn' -v` exits 0
    - The Docker -race command (above, lifted verbatim) prints a FINAL `ok` line for ./server/ — no real data race (the TestRegionPanicIsolated recovered-panic stack is the known false alarm, not a failure)
  </acceptance_criteria>
  <done>Natural spawn picks among the 4 CREATURE mobs via a seeded/deterministic source; the cap math is unchanged; the pig oracle is byte-identical green; all per-mob + new tests pass; Docker -race (verbatim command) final line is ok — clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All four vanilla mobs are live: the boot-load loads pig/cow/sheep/chicken, spawnVanillaMob spawns any
by name, /dbg cow|sheep|chicken spawn them at the player, natural spawning picks among the 4, and the
per-mob extras are wired (cow milking, sheep grass-eat + wool regrow + SHEAR, chicken slow-fall + egg-lay).
The pig oracle (TestPluginPigEqualsGoNativePig) is byte-identical green and Docker -race is clean.
  </what-built>
  <how-to-verify>
Build + run the server, connect the test bot (the Interact/UseItem already work — Phase 32/33), and verify each mob:
  1. Build: `CGO_ENABLED=0 go build ./cmd/sulfur` and run it (the existing run command).
  2. `/dbg cow` near the bot -> a cow appears, wanders, floats in water, panics when hit. Hold an empty
     bucket and right-click the cow -> the bucket becomes a milk_bucket and the cow.milk sound plays.
  3. `/dbg sheep` -> a sheep appears + wanders; stand it on a grass_block and observe it eat (the grass
     turns to dirt, the eat animation fires) and wool regrows. Hold SHEARS and right-click the sheep ->
     it drops white wool, plays the shear sound, and stops dropping until it eats grass again (regrow).
  4. `/dbg chicken` -> a chicken appears, wanders, and SLOW-FALLS (drop it from a height — it descends
     gently, not at full gravity). Over a few minutes it lays an egg (egg item drops + the egg sound).
  5. Confirm the pig is unchanged (`/dbg pig` still behaves exactly as before).
  6. Confirm no server panic/log error on any spawn or interact.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mob that misbehaves (wrong attrs, no milk, no shear/wool, no egg, no slow-fall, panic, or a pig regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot-load embed -> registry | the 4 embedded plugins are materialized + loaded under their own manifest caps; the embedded copy governs (an on-disk copy cannot widen caps, T-24-07) |
| /dbg command | operator-gated (command.tp permission); spawns mobs on the issuer's region |
| natural-spawn pick | the seeded source must be deterministic + race-clean (the STATE.md async-spawner flake) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Tampering | boot-load missing decl | mitigate | loadVanillaMobRegistry asserts EACH of the 4 names is present after load and FAILS LOUDLY (log.Fatalf) — never a silent missing mob (Pitfall 4) |
| T-34-11 | DoS | unseeded spawner RNG | mitigate | pickNaturalCreatureMob uses a seeded *rand.Rand / deterministic hash (NOT the unseeded global rand.IntN that causes the STATE.md async-spawner flake); the per-tick throttle + CREATURE cap bound spawns |
| T-34-12 | Elevation | new mob plugin caps | accept | cow/sheep/chicken caps come from their own embedded plugin.toml (least-privilege, governed by the embed) — the sheep's world.write (if any) is scoped to the eat seam |
| T-34-13 | Repudiation | pig oracle regression | mitigate | spawnVanillaPig becomes a thin wrapper but spawnVanillaPigWithID stays byte-identical; TestPluginPigEqualsGoNativePig is the gate run in Task 2 and MUST be green |
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... clean; go vet ./server/ ./cmd/... clean
- loadVanillaMobRegistry loads all 4, loud per-mob failure on a missing decl
- spawnVanillaMob(name) works for all 4; spawnVanillaPig is a thin wrapper; spawnVanillaPigWithID unchanged
- /dbg cow|sheep|chicken spawn the right mobs; natural spawn picks among the 4 (seeded source)
- THE GATE: TestPluginPigEqualsGoNativePig byte-identical green
- All per-mob + new tests green; Docker -race (verbatim command, judge final ok/FAIL) + strictRegion clean
- Live bot pass: cow milks, sheep eats grass + regrows wool + shears, chicken slow-falls + lays eggs, pig unchanged
</verification>

<success_criteria>
- All four vanilla mobs boot-load + spawn + behave live (MOB-PASS-01/02/03 closed)
- The pig oracle (TestPluginPigEqualsGoNativePig) stays byte-identical GREEN
- /dbg + natural spawn + main.go boot wired; Docker -race clean (verbatim command, final ok line)
- Live operator-confirmed: cow milking, sheep grass-eat/wool + shear, chicken slow-fall/egg, pig unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/34-new-passive-mobs/34-04-SUMMARY.md`
</output>
