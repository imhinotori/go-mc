---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - server/dogfood_gate_test.go
  - .planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-deviations.md
autonomous: false
requirements: [MOB-GATE-01, MOB-GATE-02]
tags: [dogfood-gate, oracle, pig-parity, race, live-test]

must_haves:
  truths:
    - "The full 9-goal pig oracle TestPluginPigEqualsGoNativePig passes byte-identical over 500 ticks with ALL goals {0,1,3,4,4,5,6,7,8} live (plugin pig vs Go-native pig)"
    - "The full scenario suite passes: aging (baby→adult, DATA_BABY_ID flips, half-scale AABB), feed-sets-love (adult), feed-ages-up (baby), breed (two in-love pigs → baby + cooldown + inLove reset + XP orb + variant), follow-parent (baby paths to adult)"
    - "Boot-load asserts the pig loads with exactly 9 goals"
    - "Docker -race over ./server/ is clean with all goals + aging + breeding live"
    - "The same-region breeding cut + the baby eye-height defer + the isPanicking read are documented as accepted v5 deviations (not silent)"
    - "A live vanilla-client/bot pass confirms: feed two pigs → a small baby spawns + the baby follows an adult + grows to full size"
  artifacts:
    - path: "server/dogfood_gate_test.go"
      provides: "the consolidated gate suite (the full 9-goal oracle assertion + the end-to-end aging→feed→breed→follow scenario + boot-load-9)"
      contains: "func TestDogfoodGate"
    - path: ".planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-deviations.md"
      provides: "the documented same-region cut + baby eye-height defer + isPanicking + any RNG/sound/persist deviations"
      contains: "same-region"
  key_links:
    - from: "server/dogfood_gate_test.go"
      to: "TestPluginPigEqualsGoNativePig"
      via: "the 9-goal byte-identical oracle assertion over 500 ticks"
      pattern: "TestPluginPigEqualsGoNativePig|byteIdentical"
---

<objective>
Close THE HARD GATE (MOB-GATE-02): prove the full 9-goal pig oracle stays byte-identical over 500 ticks with every goal live (plugin pig vs Go-native pig, now BOTH at 9 goals after 33-03/33-04), AND that the observable dogfood works — two fed pigs breed and the small baby follows its parent and grows up. Consolidate the end-to-end scenario suite (aging → feed → breed → follow), assert boot-load-9, run Docker -race clean, document the accepted deviations (same-region cut, baby eye-height, isPanicking), and gate on a live vanilla-client/bot pass.

Purpose: This is the dogfood gate that MUST be green before any new mob (Phases 34–36). It proves S1–S4 are 1:1 by exercising the full pig with every subsystem. Plans A/B built the subsystem; 33-03 built the Go-native goals; 33-04 mirrored them onto both .star pigs (closing the 9v9 oracle); this plan is the FORMAL gate + the observable end-to-end proof + the human live-verify.
Output: the consolidated gate test suite + the deviations doc + the live-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-02-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-03-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-04-SUMMARY.md

<oracle_gate_strategy>
THE GATE ITSELF:
The oracle pig (`TestPluginPigEqualsGoNativePig`, plugin_pig_test.go:251) drives a Go-native pig vs the
plugin pig byte-identical over 500 ticks. After 33-03 (Go pig 9 goals) + 33-04 (plugin pig 9 goals, lockstep),
BOTH are at 9 goals. It stays green ONLY because:
- BreedGoal.canUse is isInLove-gated → the un-fed lone-adult oracle never enters breeding → zero draws.
- FollowParentGoal.canUse is isBaby-gated → the adult oracle never follows → zero effect (+ no RNG anyway).
- The aging + inLove tick is pure-int + dormant on the stable age-0 / inLove-0 oracle pig; the hitbox toggle/broadcast never fire.
The oracle world spawns ONE pig with no partner/food/baby (confirm the build closure at plugin_pig_test.go:258+).
If the gate is RED, the divergence is an un-mirrored RNG draw — find it (breed XP nextInt(7)? variant nextBoolean()?
a heart offset?) and ensure it is identical on BOTH the Go pig AND both .star pigs (re-open 33-03/33-04, do NOT weaken the gate).
</oracle_gate_strategy>

<interfaces>
<!-- From Plans A/B/C1/C2. Use directly. -->
```go
// Plan A (33-01): breedAge int, isBaby(), refreshDimensions (baby half-scale), tickMobAging + onGrewUp (cross-0 restore + broadcast), babyDataEntry/broadcastBabyFlag
// Plan B (33-02): inLove int, setInLove/canFallInLove/isInLove, handleInteract FEED path, encodeLevelParticles
// 33-03 (C1): breedGoal@3, followParentGoal@5, canMate, isPanicking, adjustedTickDelay, breed() (XP + variant draws), newPigAI 9 goals
// 33-04 (C2): both .star copies (9 goals, lockstep), 5 host handles, the 3 plugin-side goal-count tests (7→9), breed/follow scenario tests
// The oracle: TestPluginPigEqualsGoNativePig (plugin_pig_test.go:251) — drives both pigs byte-identical (9v9 after 33-04)
// spawnVanillaPig(x,y,z) (vanilla_pig.go:36) — the scenario-test spawn helper
```
The live-bot: the testbot's UseItemOn/Interact (right-click a pig with a carrot held, slot 38 in the test kit)
sends ServerboundInteract → handleInteract (Plan B feed path). Carrot id 887 / pig_food tag.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The consolidated dogfood-gate test suite (9-goal oracle + end-to-end scenario + boot-load-9)</name>
  <read_first>
    - server/plugin_pig_test.go:251+ (TestPluginPigEqualsGoNativePig — the oracle harness + the build closure :258+; confirm ONE lone pig)
    - server/breed_follow_test.go (33-04's scenario tests — reuse) + server/aging_mob_test.go (Plan A: TestBabyHitbox/TestGrowUp) + server/inlove_feed_test.go (Plan B)
    - server/plugin_pig_test.go:55-66 (the boot-load test — 9 after 33-04)
    - 33-PATTERNS.md §12 (the oracle stays green; the gate)
  </read_first>
  <files>server/dogfood_gate_test.go</files>
  <action>
    Create server/dogfood_gate_test.go — the consolidated gate suite (a thin orchestration over the pieces
    Plans A/B/C1/C2 already test; this plan proves they compose end-to-end):
    1. `TestDogfoodGateOracleNineGoals`: assert the pig loads with exactly 9 goals {0,1,3,4,4,5,6,7,8} (boot-load-9),
       then run the SAME byte-identical comparison TestPluginPigEqualsGoNativePig runs (or call/extend it) over
       500 ticks — assert byte-identical. A focused wrapper that ALSO asserts len==9 up front so a regression to
       <9 goals is caught here.
    2. `TestDogfoodGateEndToEnd`: the full observable dogfood in ONE scenario:
       a. spawn two ADULT pigs in range; feed both (drive the feed path with a pig_food item) → assert both inLove==600;
       b. drive the tick loop → assert a baby (breedAge==-24000, HALF-SCALE AABB ≈0.45 span) spawns, both parents breedAge==6000, inLove reset, XP orb awarded;
       c. drive more ticks → assert the baby ages up (breedAge increments toward 0) AND, with the parent nearby, the baby's FollowParentGoal sets a nav want target at the adult (3..16 blocks, re-path every 10 ticks);
       d. drive the baby fully to breedAge==0 → assert isBaby() flips false, the AABB restored to full (≈0.9 span), and the DATA_BABY_ID cross-0 broadcast fired (babyDataEntry(e.isBaby()) == false).
    3. Keep these as orchestration tests (reuse spawnVanillaPig + the Plan A/B/C harnesses) — do NOT re-implement the subsystem logic; assert the COMPOSED behavior.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestDogfoodGate|TestPluginPigEqualsGoNativePig|TestBreed|TestFollowParent|TestAging|TestBabyHitbox|TestGrowUp|TestInLove|TestFeed|TestPluginPigBootLoads|TestVanillaPig|TestPigGoalSet|TestTempt|TestPanic|TestFloat|TestPigStrolls' -timeout 240s 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func TestDogfoodGate" server/dogfood_gate_test.go` returns >= 2 (the oracle + the end-to-end).
    - `CGO_ENABLED=0 go test ./server/ -run 'TestDogfoodGate' -v` shows TestDogfoodGateOracleNineGoals + TestDogfoodGateEndToEnd PASS.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -timeout 240s` PASSES (THE GATE — 9 goals, byte-identical, 500 ticks).
    - The full -run regex above passes with NO failures (every named test green together).
  </acceptance_criteria>
  <done>The consolidated gate suite passes: the 9-goal oracle is byte-identical AND the end-to-end aging→feed→breed→follow scenario (incl. half-scale baby + grow-up restore) works.</done>
</task>

<task type="auto">
  <name>Task 2: Docker -race + the deviation doc (same-region cut + baby eye-height + isPanicking)</name>
  <read_first>
    - .planning/ROADMAP.md:227 (the accepted v5 deviation: same-region breeding cut)
    - .planning/REQUIREMENTS.md:28 (MOB-SUB-09 — "Cross-region breeding ships the same-region cut")
    - server/ai_goals_breed.go (33-03 — the in-region near() scan + isPanicking) + 33-JARNOTES.md (the BABY_DIMENSIONS eye-height defer)
    - the Plan A/B/C1/C2 SUMMARYs (deferred persist / RNG / sound / eye-height notes to roll up)
  </read_first>
  <files>.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-deviations.md</files>
  <action>
    1. Run the Docker -race gate over ./server/ with ALL goals + aging + breeding live:
       `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/`
       It MUST be clean. (Note the pre-existing flaky TestBehaviorRegressionMobSpawns / TestServerAiStepWalksToGoalTarget
       carryover from STATE.md — if EITHER flakes, confirm it is the SAME documented spawner-RNG/AI-RNG flake and NOT
       a new breeding/aging race; a NEW race in the breed/follow/aging/hitbox code is a BLOCKER and must be fixed, owned here.)
    2. Write 33-deviations.md documenting the accepted v5 deviations (CLAUDE.md mandate — never silent):
       - SAME-REGION BREEDING CUT: partners must share a region (the in-region near() scan); the barrier-resolved
         cross-region match is DEFERRED (ROADMAP:227). State what is reduced + why + the path to full fidelity.
       - BABY EYE-HEIGHT DEFER: the AABB half-scale (0.45×0.45) IS shipped; the baby eye-height 0.40625 (Pig.BABY_DIMENSIONS
         .withEyeHeight) is cite-deferred because our Entity has no eye-height field and it is not load-bearing for the goal
         distSqr checks. Cite Pig.BABY_DIMENSIONS; note it scales 0.40625/adult when an eye-height field lands.
       - isPANICKING: shipped as the FAITHFUL running-PanicGoal read (Mob.isPanicking) — note it here only if any edge
         case (e.ai nil mobs) was guarded; it is NOT a deviation if the faithful read shipped.
       - Any RNG-source / sound-id / heart-offset / NBT-persist deviations rolled up from the SUMMARYs (e.g. Age/InLove
         NBT persist deferred per PATTERNS §13). Each entry: vanilla vs v5 + why + the path to full fidelity. Cite the jar class/method.
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The Docker -race run reports no DATA RACE in the breed/follow/aging/feed/hitbox code (a documented pre-existing spawner/AI flake is acceptable if confirmed unchanged; a NEW race is a blocker).
    - `33-deviations.md` exists and documents the same-region cut + the baby eye-height defer + (if applicable) isPanicking, each with the vanilla-vs-v5 delta + the path to full fidelity.
    - `grep -c "same-region\|eye-height" .planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-deviations.md` returns >= 2.
  </acceptance_criteria>
  <done>Docker -race is clean (no new race); the same-region cut + baby eye-height + isPanicking deviations are documented, not silent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live vanilla-client/bot dogfood verification (the observable gate)</name>
  <what-built>
    The full 9-goal pig {0,1,3,4,4,5,6,7,8} with aging + breeding + feeding + the baby half-scale hitbox live.
    The byte-identical oracle gate is green, the end-to-end scenario test (aging→feed→breed→follow→grow) passes,
    and Docker -race is clean. This checkpoint is the OBSERVABLE dogfood: a real vanilla 26.2 client (or the
    testbot) must confirm the behavior in-world before the gate formally closes and Phase 34 (new mobs) may begin.
  </what-built>
  <action>Present the verification steps below to the user, wait for the resume-signal, and (on failure) fix the failing seam before closing the gate. This is a blocking human-verify checkpoint — do NOT auto-pass.</action>
  <how-to-verify>
    1. Build + run the server with the test kit so a carrot is in the join inventory:
       `CGO_ENABLED=0 go build -o sulfur.exe ./cmd/sulfur && SULFUR_TEST_KIT=1 ./sulfur.exe -seed 777`
       (the test kit puts a carrot in slot 38; default prod join is the vanilla empty inventory, untouched.)
    2. Connect an unmodified vanilla 26.2 client (PrismLauncher) to localhost:25565, OR run the testbot:
       `./testbot -mode wander` (use its UseItemOn/Interact to right-click a pig with the carrot held).
    3. Find two adult pigs. With the carrot selected, RIGHT-CLICK each pig once. EXPECTED: each fed pig shows
       HEART particles (it fell in love), and the held carrot count decreases by 1 per feed.
    4. With both pigs in love and near each other, WAIT (~3s / 60 ticks). EXPECTED: a visibly SMALL baby pig
       (the half-scale hitbox + DATA_BABY_ID flag) spawns between them; a few XP orbs drop; the parents stop hearts.
    5. Watch the baby. EXPECTED: the small baby pig PATHS TOWARD the nearest adult (FollowParentGoal), staying
       within a few blocks, and over time GROWS to full size (aging to breedAge 0 — the DATA_BABY_ID cross-0 broadcast).
    6. Confirm nothing crashes and other players see the same (if a second client is available).
  </how-to-verify>
  <resume-signal>
    Type "approved" if: hearts on feed, a SMALL baby spawns from two fed pigs, the baby follows an adult, and the
    baby grows to full size. Otherwise describe exactly what failed (no hearts? no baby? baby full-size not small?
    baby doesn't follow? doesn't grow? crash?) so the failing seam can be fixed before the gate closes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (live feed) | the live-verify exercises the real ServerboundInteract feed path end-to-end (mitigated in Plan B). |
| tick RNG → oracle | the gate is the formal proof the 9-goal RNG stream stays byte-identical (the test is the boundary check). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Tampering | a regression dropping the pig below 9 goals or diverging the oracle | mitigate | TestDogfoodGateOracleNineGoals asserts len==9 + byte-identical over 500 ticks (Task 1); the gate blocks Phase 34. |
| T-33-10 | Denial of service | a data race in the new breed/follow/aging/hitbox concurrent paths | mitigate | Docker -race over ./server/ with all goals live (Task 2); a new race is a blocker owned here. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0; `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestDogfoodGate|TestBreed|TestFollowParent|TestAging|TestBabyHitbox|TestGrowUp|TestInLove|TestFeed|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestVanillaPig|TestPigGoalSet|TestTempt|TestPanic|TestFloat|TestPigStrolls' -timeout 240s -v` all PASS.
- `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` EMPTY.
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/` clean (no new race).
- The live vanilla-client/bot pass (the human-verify checkpoint) confirms feed→love→small-baby→follow→grow.
- gopls is STALE — trust the compiler.
</verification>

<success_criteria>
- THE GATE: the full 9-goal pig oracle TestPluginPigEqualsGoNativePig passes byte-identical over 500 ticks (MOB-GATE-02).
- The end-to-end scenario (aging→feed→breed→follow→grow, incl. half-scale baby + grow-up restore) + boot-load-9 pass.
- Docker -race clean (no new race); the same-region cut + baby eye-height + isPanicking deviations documented.
- The live vanilla-client/bot pass confirms the observable dogfood (feed two pigs → small baby spawns + follows + grows).
- MOB-GATE-01 + MOB-GATE-02 formally closed — Phase 34 (new mobs) may begin.
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-05-SUMMARY.md`
</output>
