---
phase: 30.1-faithful-randomstrollgoal-target-selection
verified: 2026-06-30T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 30.1: Faithful RandomStrollGoal Target Selection (BUGFIX) Verification Report

**Phase Goal:** A wandering pig picks a REACHABLE stroll target every time, so it ambles freely instead of walking a few blocks then jamming against a block forever. Re-port vanilla `LandRandomPos`/`WaterAvoidingRandomStrollGoal.getPosition` (best-of-10 weighted + x/y/z draw order + movePosUpOutOfSolid ground-snap + isStableDestination/water/malus rejection), jar-faithful, oracle stays byte-identical.
**Verified:** 2026-06-30
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A pig on flat ground strolls continuously, never motionless > ~3×interval (TestPigStrollsWithoutWedging GREEN) | ✓ VERIFIED | `CGO_ENABLED=0 go test -run TestPigStrollsWithoutWedging` PASS (0.04s). Test log: longest motionless run = 93 (≤ 360), distinct quarter-positions = 189 (≥ 8). The pig visibly walks across many wantX/Y/Z columns over 600 ticks. This test FAILED before the fix. |
| 2 | Plugin pig and Go-native pig stay byte-identical over 500 ticks with the new 30-draw stroll stream (TestPluginPigEqualsGoNativePig GREEN) | ✓ VERIFIED | `CGO_ENABLED=0 go test -run TestPluginPigEqualsGoNativePig` PASS (0.05s). The bit-fragile lockstep oracle is green with the gate `nextInt(60)` + probability `nextFloat()` + 30 `nextInt` candidate draws identical across the Go goal and both `.star` copies. |
| 3 | getPosition draws EXACTLY the gate + probability + 30 nextInt (10 candidates × x,y,z) in x/y/z order on BOTH the Go goal AND both .star copies | ✓ VERIFIED | `server/ai_goals_passive.go:188-200`: `nextFloat()` gate then `for i:=0;i<10` unconditional loop calling `generateRandomDirection`. `generateRandomDirection` (`:210-215`) draws xt(`2*h+1`), yt(`2*v+1` — y BEFORE z), zt(`2*h+1`). Both `.star` copies (`stroll_can_use:79-92`) draw `rand_int(60)` gate, `rand_float()`, then 10×(x,y,z) `rand_int`. Oracle byte-identity confirms count/order match. |
| 4 | The committed stroll want-target is always a reachable walkable column (solid floor at want_y-1), snapped up via moveUpOutOfSolid (maxY=maxBuildHeightY) — never a raw e.y+dy underground point | ✓ VERIFIED | `server/stroll_snap.go::snapStrollWant` (`:122-146`): per candidate runs isOutsideLimits→isRestricted→isStableDestination(`blockSolidAt(bx,by-1,bz)`)→moveUpOutOfSolid(maxY=`maxBuildHeightY`)→isWater/hasMalus, returns first valid as `(bx+0.5, snappedY, bz+0.5)` (atBottomCenterOf). RNG-free. Wired in `serverAiStep` (`ai_mob.go:200-207`) BEFORE requestPath (`:216`). |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `server/ai_goals_passive.go` | getPosition rewrite + generateRandomDirection + reducedTickDelay + candidate stash | ✓ VERIFIED | `getPosition(t,e)` widened, `generateRandomDirection` x/y/z, `reducedTickDelay`, `wantCandidates` field, start() handoff via `setWantCandidates`. Build+vet clean. |
| `server/stroll_snap.go` (created) | RNG-free snap/validate over 10 raw candidates | ✓ VERIFIED | `snapStrollWant` + cited helpers (isSolidBlock/isStableDestination/moveUpOutOfSolid/isOutsideLimits/isRestricted/isWaterAt/hasMalus). No RNG draws in body. Every helper cites jar class/method (RandomPos/LandRandomPos/GoalUtils/PathNavigation). |
| `server/ai_mob.go` | setWantCandidates + snapStrollWant call before requestPath | ✓ VERIFIED | `wantCands`/`hasWantCands` carrier (`:47-56`), `setWantCandidates` (`:134`), snap block (`:200-207`) precedes requestPath (`:216`). |
| `server/plugin_entity.go` | path_to overloaded (3 floats | 30 floats), no new handle | ✓ VERIFIED | `case 3:`→setWantTarget, `case 30:`→setWantCandidates (`:859-883`). AttrNames unchanged `["path_to","has_path","stop","jump"]` (`:769`) — no new method. |
| `plugins/vanilla_pig/main.star` | stroll_can_use draws 30 rand_int x/y/z, commits via path_to(30 floats) | ✓ VERIFIED | `stroll_can_use:79-92` — gate 60, rand_float probability, 10×(x,y,z), `nav.path_to(*flat)`. No set_state list. |
| `server/assets/vanilla_pig/main.star` | byte-identical copy | ✓ VERIFIED | `diff` exits 0 (byte-identical). |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| getPosition | setWantCandidates | start() hands 10 raw candidates `[10][3]float64` | ✓ WIRED | `ai_goals_passive.go:234` calls `e.ai.setWantCandidates(arr, g.wantLandMode)`. |
| serverAiStep | blockSolidAt | snapStrollWant reads world solidity before requestPath | ✓ WIRED | `stroll_snap.go` helpers all route through `t.blockSolidAt`; snap at `ai_mob.go:200` runs before requestPath at `:216`. |
| .star stroll_can_use | plugin_entity pathTo (30-float) → setWantCandidates | `nav.path_to(*flat)` 30 flat floats | ✓ WIRED | `main.star:92` → `plugin_entity.go:865 case 30` → `setWantCandidates(cands, false)` (`:883`). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Wedge fixed | `go test -run TestPigStrollsWithoutWedging` | PASS, longest motionless 93 ≤ 360, 189 distinct positions | ✓ PASS |
| Oracle byte-identical | `go test -run TestPluginPigEqualsGoNativePig` | PASS (500 ticks byte-equal) | ✓ PASS |
| Full regression | `go test ./server/ ./data/tag/ ./level/loot/` | all `ok` | ✓ PASS |
| Build clean | `CGO_ENABLED=0 go build ./...` | exit 0 | ✓ PASS |
| Vet clean | `CGO_ENABLED=0 go vet ./server/` | exit 0 | ✓ PASS |
| .star copies identical | `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` | empty, exit 0 | ✓ PASS |
| Race clean (gate tests) | Docker `go test -race -run 'TestPig...|TestPluginPig...|TestNavigation|TestFloatGoal'` | `ok` 2.9s, exit 0 | ✓ PASS |
| Race clean (full pkg) | Docker `go test -race ./server/` | `ok` 13.5s, exit 0, 0 DATA RACE | ✓ PASS |
| No new Go deps | git diff go.mod | no change | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| MOB-GATE-01 | 30.1-01-PLAN | Pig's goals wired 1:1 (incremental across Phases 30-33; RandomStrollGoal hardened to 1:1 here) | ✓ SATISFIED | RandomStrollGoal target selection is now a faithful port of WaterAvoidingRandomStrollGoal.getPosition (best-of-10, x/y/z draw order, moveUpOutOfSolid snap, isStableDestination/water/malus rejection), oracle byte-identical — a verified step toward the Phase-33 pig-parity dogfood gate. Formally closed at Phase 33. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | No TODO/FIXME/stub/list-scratch found in modified files | — | The faithful-scope stubs (isWater/isOutsideLimits/isRestricted/hasMalus == vanilla default, getWalkTargetValue==0.0, home no-op) are CITED with jar references and upgrade paths per the CLAUDE.md mandate — not baked-away placeholders. |

### Human Verification Required

None required for goal verification. The headless `TestPigStrollsWithoutWedging` (full-tick repro driving serverAiStep+advance, longest motionless 93 ≤ 360, 189 distinct positions over 600 ticks) is strong evidence the in-game wedge is fixed. The user will play-test in-game regardless, but no automated gap forces human verification — every success criterion is programmatically green.

### Gaps Summary

No gaps. All four ROADMAP success criteria are met:
1. Pig strolls continuously (wedge fixed) — TestPigStrollsWithoutWedging GREEN.
2. Stroll target is always a reachable walkable column — snapStrollWant ports the full validate+snap chain, jar-cited, RNG-free.
3. RNG lockstep holds — 30-draw stream added to the Go goal AND both .star copies in the same plan; TestPluginPigEqualsGoNativePig byte-identical.
4. Standing — every op faithful + cited; CGO=0; no new Go deps; Docker -race clean (full package, 0 DATA RACE).

The deferred secondary (`canContinueToUse` stuck-timeout hardening) was correctly NOT done — `canContinueToUse` still keys purely off `hasTarget` (`ai_goals_passive.go:220-222`), matching vanilla `RandomStrollGoal.canContinueToUse = !navigation.isDone()`. The faithful target selection + the navigation arrival re-roll fix remove the observed wedge trigger.

Note on plan deviation: the executor ported the FULL `WaterAvoidingRandomStrollGoal.getPosition` (probability nextFloat() gate + DefaultRandomPos/LandRandomPos branch) rather than the plan's bare `LandRandomPos.getPos`, and corrected the gate to `reducedTickDelay(120)==60`. Both changes are MORE faithful to the jar (verified via javap/CFR), preserve identical draw counts (oracle stays green), and serve the same goal. This is a faithfulness improvement within scope, not a scope reduction — all must_have truths are still satisfied.

---

_Verified: 2026-06-30_
_Verifier: Claude (gsd-verifier)_
