---
phase: 35-hostiles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mob_category.go
  - server/spawner.go
  - server/async.go
  - server/spawner_monster_test.go
autonomous: true
requirements: [MOB-SUB-11]
must_haves:
  truths:
    - "categoryOf returns categoryMonster for zombie, skeleton, and spider"
    - "a monsterCap function gives MONSTER 70 / spawnableChunkCount / 289 (the /289 divisor kept, 70 cap)"
    - "countByCategory tallies MONSTER once categoryOf returns it (zero counting-code change)"
    - "isDarkEnoughToSpawn is the FORCED gametime-darkness proxy over t.gametime — NEVER a silent daylight flood"
    - "the natural-spawn MONSTER pass is gated by isDarkEnoughToSpawn AND the monster cap re-check"
  artifacts:
    - path: "server/mob_category.go"
      provides: "the 3 MONSTER categoryOf cases"
      contains: "return categoryMonster"
    - path: "server/spawner.go"
      provides: "monsterCap + isDarkEnoughToSpawn proxy + the MONSTER natural-spawn pass"
      contains: "func (t *TickLoop) isDarkEnoughToSpawn"
    - path: "server/async.go"
      provides: "pickNaturalMonsterMob + the MONSTER apply-time cap re-check"
      contains: "pickNaturalMonsterMob"
  key_links:
    - from: "server/spawner.go naturalSpawn MONSTER pass"
      to: "isDarkEnoughToSpawn() + monsterCap()"
      via: "the dark gate + the cap gate"
      pattern: "isDarkEnoughToSpawn"
    - from: "server/async.go applyTo MONSTER re-check"
      to: "countByCategoryAcrossRegions()[categoryMonster]"
      via: "the cross-region monster cap"
      pattern: "categoryMonster"
---

<objective>
Gate hostile spawning (MOB-SUB-11, SC#3 + SC#4) — the per-category MONSTER cap + the FORCED day/night light-gate proxy. Three MONSTER categoryOf cases, a monsterCap (70 / 289), the natural-spawn MONSTER pass (dark gate + cap re-check), and the isDarkEnoughToSpawn proxy over t.gametime (NO light engine exists — the decision is PRE-MADE in CONTEXT + JARNOTES; do NOT re-open it).

Purpose: A survival night needs hostiles to spawn only at night and only under a cap. This is pure-additive over the existing CREATURE spawn machinery — countByCategory already tallies by category, the cap formula is identical (only the category + divisor-fed value differ), so the pig/cow/sheep/chicken CREATURE accounting is untouched.
Output: the 3 categoryOf cases, monsterCap, isDarkEnoughToSpawn (the cited proxy), the naturalSpawn MONSTER pass, pickNaturalMonsterMob + the apply-time MONSTER cap re-check, and a spawner test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-hostiles/35-JARNOTES.md
@.planning/phases/35-hostiles/35-PATTERNS.md
@.planning/phases/35-hostiles/35-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Existing seams the executor builds against — DO NOT re-explore the codebase. -->

mob_category.go: maxInstancesPerChunk() already returns 70 for categoryMonster (lines 55-56) and 10 for
categoryCreature (line 58). categoryOf(t entity.ID) (lines 82-104) switches on entity id; the cow case
(lines 86-91) is the EXACT additive template. entity.Zombie/Skeleton/Spider all exist with Type "monster".

spawner.go: spawnMagicNumber = 289 (line 80). creatureCap(spawnableChunkCount) =
categoryCreature.maxInstancesPerChunk() * spawnableChunkCount / spawnMagicNumber (lines 88-90) — clone shape.
countByCategory (lines 97-106) buckets by categoryOf(e.typ) — ALREADY tallies MONSTER once categoryOf returns
it. countByCategoryAcrossRegions (lines 108+) is the cross-region variant. The naturalSpawn CREATURE gate
(lines 315-395) is the pass template: compute cap, read live count, return if at/over; findStandableY
(lines 185-197) is the ON_GROUND standable check to REUSE. t.gametime is the only day/night clock
(block_break.go:230 reads it the same way) — no separate dayTime/timeOfDay is sent to the client.

async.go: spawnCandidatesReady.applyTo (lines 294-344) re-reads the live count + drops at/over cap
(lines 313-317). naturalCreatureMobNames (lines 352-357) is the explicit CREATURE name slice;
pickNaturalCreatureMob (lines 359+) draws uniform-random from the per-region seeded levelRandom.
mobNearAcrossRegions packing guard (line 323) + spawnVanillaMob(name,...) placement (line 340) are
category-agnostic — reuse verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the 3 MONSTER categoryOf cases + monsterCap + the isDarkEnoughToSpawn gametime proxy</name>
  <read_first>
    - server/mob_category.go (categoryOf lines 82-104 — the cow case 86-91; maxInstancesPerChunk lines 53-74)
    - server/spawner.go (spawnMagicNumber line 80, creatureCap lines 88-90, countByCategory lines 97-106)
    - server/block_break.go (the t.gametime read at line 230 — the day/night clock)
    - .planning/phases/35-hostiles/35-JARNOTES.md (the spawn-rules section + isDarkEnoughToSpawn bytecode lines 177-192 — the SKY/BLOCK light reads that DON'T EXIST yet, confirming the proxy)
    - .planning/phases/35-hostiles/35-CONTEXT.md (THE FORCED LIGHT-GATE DECISION lines 38-48)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the mob_category.go + spawner.go entries)
  </read_first>
  <behavior>
    - Test: categoryOf(entity.Zombie.ID) == categoryMonster (and skeleton, spider).
    - Test: monsterCap(n) == 70 * n / 289 (e.g. monsterCap(289) == 70).
    - Test: isDarkEnoughToSpawn() returns true when t.gametime % 24000 is in the night window, false in daytime.
  </behavior>
  <action>
Add the MONSTER spawn accounting + the FORCED light-gate proxy. Cite the jar for each.

1. mob_category.go categoryOf — add 3 cases byte-for-byte the cow shape (35-PATTERNS.md:213-221), each cited:
     case entity.Zombie.ID:    return categoryMonster   // MOB-HOST-01: EntityType.ZOMBIE = MobCategory.MONSTER
     case entity.Skeleton.ID:  return categoryMonster   // MOB-HOST-02: EntityType.SKELETON = MobCategory.MONSTER
     case entity.Spider.ID:    return categoryMonster   // MOB-HOST-03: EntityType.SPIDER = MobCategory.MONSTER
   PURE additive — the pig/cow/sheep/chicken CREATURE arms are untouched. maxInstancesPerChunk already returns
   70 for MONSTER — NO cap-table change.

2. spawner.go — clone creatureCap to monsterCap (cite NaturalSpawner$SpawnState.canSpawnForCategoryGlobal):
     func monsterCap(spawnableChunkCount int) int {
         return categoryMonster.maxInstancesPerChunk() * spawnableChunkCount / spawnMagicNumber
     }
   Same /289 divisor; only the category (70 vs 10) differs.

3. spawner.go — add the isDarkEnoughToSpawn PROXY (THE FORCED DECISION, 35-CONTEXT:38-48, 35-PATTERNS.md:248-261).
   Cite Monster.isDarkEnoughToSpawn and document the deferral LOUDLY:
     // isDarkEnoughToSpawn — THE FORCED PROXY (35-CONTEXT SC#4): no light engine exists (v1 is flat-stone
     // superflat, no light propagation — node_evaluator.go:27 / path_region.go:29), so the real
     // Monster.isDarkEnoughToSpawn SKY/BLOCK-light read (35-JARNOTES.md:177-192) is unavailable. Gate hostile
     // spawns on the day/night gametime window instead — EQUALS the vanilla night default and becomes a real
     // sky-light read when the lighting engine lands. NEVER a silent daylight flood. DEFERRED: full
     // light-propagation + cave/block-light spawning + the light-test RNG (nextInt(32) + monsterSpawnLightTest
     // UniformInt sample) — recorded here + in the SUMMARY. Cite Monster.isDarkEnoughToSpawn.
     func (t *TickLoop) isDarkEnoughToSpawn() bool {
         const dayLengthTicks = 24000
         const nightStartTicks = 13000   // vanilla: monsters spawn from dusk
         const nightEndTicks = 23000     // to dawn
         dayTime := t.gametime % dayLengthTicks
         return dayTime >= nightStartTicks && dayTime < nightEndTicks
     }
   CONFIRM at exec that t.gametime % 24000 IS the only time-of-day signal (no separate dayTime field) — if a
   real, trivially-available sky-light read exists, CONTEXT permits taking it instead; otherwise the proxy is
   the accepted default. Do NOT bake the value away — keep it structured as a real-read seam.
  </action>
  <verify>
    <automated>cd D:/ender && go build ./server/... && go test ./server/ -run "TestCategoryOfMonster|TestMonsterCap|TestIsDarkEnoughToSpawn" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return categoryMonster" server/mob_category.go` >= 3.
    - `grep -n "func monsterCap" server/spawner.go` returns AND the body uses spawnMagicNumber (`grep -n "spawnMagicNumber" server/spawner.go` includes the monsterCap site).
    - `grep -n "func (t \*TickLoop) isDarkEnoughToSpawn" server/spawner.go` returns AND `grep -n "t.gametime" server/spawner.go` shows the proxy reads gametime.
    - `grep -ci "FORCED PROXY\|gametime" server/spawner.go` >= 1 (the deferral is documented, not silent).
    - `cd D:/ender && go test ./server/ -run "TestCategoryOfMonster|TestMonsterCap|TestIsDarkEnoughToSpawn" -count=1` exits 0.
  </acceptance_criteria>
  <done>The 3 hostiles map to MONSTER; monsterCap gives 70/289; isDarkEnoughToSpawn is the cited gametime proxy with the deferral recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the natural-spawn MONSTER pass (dark + cap gate) + pickNaturalMonsterMob + apply-time cap re-check</name>
  <read_first>
    - server/spawner.go (the naturalSpawn CREATURE gate lines 315-395, findStandableY lines 185-197, the single-in-flight/snapshot machinery lines 358-395)
    - server/async.go (spawnCandidatesReady.applyTo lines 294-344, the cap re-check lines 313-317, naturalCreatureMobNames lines 352-357, pickNaturalCreatureMob lines 359+)
    - server/spawner.go countByCategory (lines 97-106) + countByCategoryAcrossRegions (lines 108+)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the spawner.go MONSTER-pass + async.go entries lines 230-292)
    - .planning/phases/35-hostiles/35-CONTEXT.md (Spawn gating SC#3 lines 82-84)
  </read_first>
  <behavior>
    - Test: with isDarkEnoughToSpawn() false (daytime), the naturalSpawn MONSTER pass adds NO hostile.
    - Test: with the live MONSTER count >= monsterCap(...), the MONSTER pass (and the apply-time re-check)
      adds no hostile.
    - Test: pickNaturalMonsterMob returns one of {vanilla_zombie, vanilla_skeleton, vanilla_spider} from the
      seeded levelRandom.
  </behavior>
  <action>
Add the MONSTER natural-spawn pass alongside the CREATURE one. The single-in-flight + snapshot + off-tick scan
machinery is category-agnostic — REUSE it; the only deltas are monsterCap, the dark gate, and the monster
species picker. Cite NaturalSpawner.spawnCategoryForPosition + checkMonsterSpawnRules (the v1 collapse:
isDarkEnoughToSpawn proxy + the existing findStandableY ON_GROUND check).

1. spawner.go naturalSpawn — add a sibling MONSTER pass after the CREATURE gate (lines 328-350 are the
   template): compute monsterCap(spawnableChunkCount), read counts[categoryMonster], return early if at/over.
   ADD the dark gate: `if !t.isDarkEnoughToSpawn() { skip the monster pass }`. REUSE findStandableY verbatim
   for the standable column (checkMonsterSpawnRules / checkSurfaceMonstersSpawnRules collapse for v1 to: this
   dark gate + the ON_GROUND standable check — 35-PATTERNS.md:264-266). The candidate (x,z) selection +
   single-in-flight machinery (lines 358-395) is reused unchanged.

2. async.go — clone naturalCreatureMobNames to an EXPLICIT naturalMonsterMobNames slice
   {vanilla_zombie, vanilla_skeleton, vanilla_spider} (its own list so a non-monster bundled mob is never
   dragged in — 35-PATTERNS.md:282-284). Clone pickNaturalCreatureMob to pickNaturalMonsterMob over that slice,
   drawing from the same per-region seeded levelRandom (same uniform-pick shape, lines 359-380).

3. async.go applyTo — the apply-time cap re-check (lines 313-317) must use the RIGHT cap for the category
   being placed. Carry the category/picker on the spawnCandidatesReady message (CREATURE is currently
   implicit) — add a category (or picker) field so applyTo re-checks countByCategoryAcrossRegions()[categoryMonster]
   vs monsterCap(...) for a monster candidate, and the existing CREATURE check for a creature candidate
   (35-PATTERNS.md:287-291). The mobNearAcrossRegions packing guard (line 323) + spawnVanillaMob(name,...)
   placement (line 340) are reused verbatim.

4. checkDespawn port (SC#3) — the cap bounds SPAWNS not population. If a per-mob checkDespawn distance/age
   despawn (Mob.checkDespawn) is feasible without new subsystems, port it citing Mob.checkDespawn; otherwise
   record the deferral explicitly (the cap re-check already bounds spawns). Do NOT fake a despawn.

5. spawner_monster_test.go (NEW) — clone the existing spawner/async test harness:
   - TestMonsterPassDaytimeNoSpawn: isDarkEnoughToSpawn false -> no monster added.
   - TestMonsterCapBlocks: count >= monsterCap -> no monster added (pass + apply-time re-check).
   - TestPickNaturalMonsterMob: returns a member of naturalMonsterMobNames.
  </action>
  <verify>
    <automated>cd D:/ender && go build ./server/... && go test ./server/ -run "TestMonsterPassDaytimeNoSpawn|TestMonsterCapBlocks|TestPickNaturalMonsterMob" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func pickNaturalMonsterMob\|pickNaturalMonsterMob" server/async.go` returns.
    - `grep -n "naturalMonsterMobNames" server/async.go` returns AND lists the 3 vanilla_ hostile names.
    - `grep -n "isDarkEnoughToSpawn()" server/spawner.go` returns in the naturalSpawn MONSTER pass.
    - `grep -n "monsterCap" server/async.go` returns (the apply-time re-check uses the monster cap).
    - `grep -n "categoryMonster" server/async.go` returns (the cross-region monster re-check).
    - `cd D:/ender && go test ./server/ -run "TestMonsterPassDaytimeNoSpawn|TestMonsterCapBlocks|TestPickNaturalMonsterMob" -count=1` exits 0.
  </acceptance_criteria>
  <done>Hostiles spawn only at night (the proxy) and only under monsterCap, picked from the explicit monster slice, with a cross-region apply-time re-check; checkDespawn is ported or its deferral recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| natural spawner -> world | the spawner adds entities (a flood vector if the cap is bypassed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | DoS | the MONSTER natural-spawn pass | mitigate | monsterCap (70/289) bounds the global MONSTER count + the apply-time cross-region re-check (countByCategoryAcrossRegions) closes the snapshot-staleness window — the same anti-flood discipline as the CREATURE cap (Pitfall 3). |
| T-35-05 | Tampering | isDarkEnoughToSpawn proxy | accept | the proxy reads only host-owned t.gametime (not plugin/client-controllable); a wrong window only changes WHEN hostiles spawn, never bypasses the cap. The full light read is the deferred follow-up. |
</threat_model>

<verification>
- `go build ./server/...` + `go vet ./server/...` clean.
- categoryOf returns MONSTER for the 3; monsterCap = 70/289; the proxy gates on gametime.
- The MONSTER pass + apply-time re-check respect the dark gate and the cap (tests green).
</verification>

<success_criteria>
- SC#3: per-category MONSTER cap (70, /289), MONSTER tallied in countByCategory, checkDespawn ported or deferral recorded.
- SC#4: the FORCED gametime-darkness proxy ships, the deferral recorded, NEVER a silent daylight flood.
</success_criteria>

<output>
After completion, create `.planning/phases/35-hostiles/35-02-SUMMARY.md` (record the light-engine deferral + the checkDespawn disposition explicitly).
</output>
