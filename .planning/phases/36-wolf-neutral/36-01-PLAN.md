---
phase: 36-wolf-neutral
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/entity.go
  - server/entity_encode.go
  - server/ai_goals_sit.go
  - server/ai_goals_owner.go
  - server/ai_goals_target.go
  - server/plugin_mob_ai.go
  - server/plugin_mob_decl.go
  - server/mob_category.go
  - server/combat_mob.go
  - level/attribute/defaults.go
  - server/vanilla_pig_embed.go
  - server/commands_dbg.go
autonomous: true
requirements: [MOB-NEUT-01, MOB-NEUT-02]

must_haves:
  truths:
    - "A 'wolf' base_type resolves to entity.Wolf (ID 149) and categoryOf(entity.Wolf.ID) returns categoryCreature"
    - "wolfSupplier exists with MOVEMENT_SPEED 0.3 + MAX_HEALTH 8.0 + ATTACK_DAMAGE 4.0 on the Animal base"
    - "The wolf carries tame/orderedToSit/inSittingPose/ownerUUID/angerEndTime/angerTarget state fields, zero for the pig"
    - "DATA_FLAGS (index 18) + owner-UUID (index 19) metadata entries encode/broadcast for the wolf"
    - "buildNativeGoal routes kind=sit/follow_owner/owner_hurt_by/owner_hurt/angry_player_target/skeleton_target to the new Go-native goals"
    - "nearestAttackableTargetGoal carries a targetClass (PLAYER|SKELETON) + an optional angerGate; the bare nearest_attackable_target keeps the un-gated PLAYER behavior (Phase-35 UNCHANGED)"
    - "A wild un-hit wolf does NOT acquire a player target (angerGate holds); a wolf with a live angerEndTime DOES (isAngryAt)"
    - "A wolf can acquire a nearby skeleton target (skeleton_target scans entity.Skeleton.ID within FOLLOW_RANGE)"
    - "A wolf hit by a player sets angerEndTime = gameTime + 400 + nextInt(381) at the combat_mob.go store-point (wolf-gated); isAngry is a gametime-endpoint check (no per-tick decrement)"
    - "The wolf embeds and boot-loads; /dbg wolf spawns one"
  artifacts:
    - path: "server/ai_goals_sit.go"
      provides: "SitWhenOrderedToGoal port (flags JUMP|MOVE, no RNG)"
      contains: "func (g *sitWhenOrderedToGoal) canUse"
    - path: "server/ai_goals_owner.go"
      provides: "FollowOwnerGoal + OwnerHurtByTargetGoal + OwnerHurtTargetGoal ports"
      contains: "func newFollowOwnerGoal"
    - path: "server/ai_goals_target.go"
      provides: "nearestAttackableTargetGoal parameterized with targetClass + angerGate; nearestEntityOfTypeAt scan; the angry_player_target + skeleton_target ctors"
      contains: "func nearestEntityOfTypeAt"
    - path: "level/attribute/defaults.go"
      provides: "wolfSupplier registered in suppliers map"
      contains: "func wolfSupplier"
  key_links:
    - from: "server/plugin_mob_ai.go buildNativeGoal"
      to: "ai_goals_sit.go / ai_goals_owner.go / ai_goals_target.go ctors"
      via: "kind switch cases sit/follow_owner/owner_hurt_by/owner_hurt/angry_player_target/skeleton_target"
      pattern: "case \"sit\""
    - from: "server/ai_goals_target.go nearestAttackableTargetGoal.findTarget"
      to: "nearestPlayerIDAt (PLAYER) / nearestEntityOfTypeAt(entity.Skeleton.ID) (SKELETON)"
      via: "targetClass switch"
      pattern: "targetClassSkeleton"
    - from: "server/ai_goals_target.go nearestAttackableTargetGoal.canUse"
      to: "the wolf isAngryAt gate"
      via: "angerGate predicate (nil for the hostiles, non-nil for angry_player_target)"
      pattern: "angerGate"
    - from: "server/combat_mob.go flag2 store-point"
      to: "wolf angerEndTime field"
      via: "startPersistentAngerTimer = angerEndTime = gameTime + 400 + nextInt(381) (wolf-gated)"
      pattern: "entity.Wolf.ID"
    - from: "server/mob_category.go categoryOf"
      to: "categoryCreature"
      via: "entity.Wolf.ID arm"
      pattern: "entity.Wolf.ID"
---

<objective>
Build the entire SHARED Go foundation for the wolf: the TamableAnimal/NeutralMob STATE (tame/owner/sit/anger fields), the DATA_FLAGS + owner-UUID metadata, the NEW goal CLASSES (SitWhenOrderedToGoal, FollowOwnerGoal, OwnerHurtByTargetGoal, OwnerHurtTargetGoal), the PARAMETERIZED target goal (targetClass PLAYER|SKELETON + an optional anger gate — so the wolf can target the player ONLY when angry and target skeletons, while the hostiles keep the un-gated player target unchanged), the goal-kind seam extension (sit/follow_owner/owner_hurt_by/owner_hurt/angry_player_target/skeleton_target), the wolfSupplier, categoryOf→CREATURE, the gametime-endpoint anger model set at the combat store-point, plus the wolf base_type / embed / boot-load / /dbg wiring.

Purpose: This is the Wave-1 foundation that Plan B (taming interact) and Plan C (.star plugin) both build on. All shared Go edits are HOISTED here so B (attack_dispatch.go only) and C (.star only) stay disjoint and parallel in Wave 2, and D (the gate) stays test-only.
Output: All wolf Go state, metadata, goals, the parameterized target seam (angry_player_target + skeleton_target kinds), supplier, category, anger trigger, and wiring — but NO taming interact (Plan B) and NO .star plugin (Plan C).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/36-wolf-neutral/36-JARNOTES.md
@.planning/phases/36-wolf-neutral/36-PATTERNS.md

<interfaces>
<!-- Key seams the executor uses directly — no codebase exploration needed. -->

buildNativeGoal switch (server/plugin_mob_ai.go:271-290) — current valid kinds:
  nearest_attackable_target, hurt_by_target, melee_attack, spider_attack, leap_at_target, float
The panic message at line 208 lists the valid kinds; the flags() assertion at 215-216 verifies
declared flags agree with the Go goal's own flags. ADD the 6 new cases (sit, follow_owner,
owner_hurt_by, owner_hurt, angry_player_target, skeleton_target) + update both lists.

nativeKind validation (server/plugin_mob_decl.go:289-307) — a kind-goal must NOT also supply a
callback or requires_update_every_tick. No change needed beyond the switch addition.

baseTypeByName resolver (server/plugin_mob_decl.go:120-133) + loud-error allowed list (~355).

mobRandom(e) per-entity RNG stream — ai_goals_target.go:91 (mobRandom(e).nextInt(...)); the SAME
seeded per-entity stream the combat kinds draw from (reseeded per entity id at spawn).

THE TARGET-GOAL SEAM (server/ai_goals_target.go) — the file the B1/B2 fix parameterizes:
  - nearestAttackableTargetGoal struct (51-63): baseGoal + randomInterval + target int32 + forceTrigger.
    newNearestAttackableTargetGoal() (67-72) builds it PLAYER-only, no anger gate.
  - canUse (87-98): the nextInt(10) gate, then findTarget, return target != 0.
  - findTarget (119-129): hardcoded to nearestPlayerIDAt(t, e.x, e.y, e.z, follow) — the PLAYER branch only.
  - nearestPlayerIDAt (298-312): the entity-id player scan over t.players within maxDist.
  - hurtByTargetGoal (184-290) — UNCHANGED by this fix.
  The wolf NEEDS (a) a player target gated on isAngryAt (a wild un-hit wolf must NOT aggro players) and
  (b) a SKELETON target (a mob-class scan). The fix parameterizes this goal — see Task 2.

The entity-scan seam (the SKELETON branch source): t.cur().entities.near(e.x, e.z, rangeChunks)
returns a fresh copy of every entity in the candidate columns (entity_store.go:near 168). The breed goal
(ai_goals_breed.go:130-147 findFreePartner) and the follow goal (ai_goals_follow.go:105) both use it with
an `other.typ == <wanted-id>` filter + an entityDistSqr nearest-wins loop. near() takes a CHUNK range, so
convert FOLLOW_RANGE (blocks) to columns via ceil(follow/16) and re-check the precise block distance inside
the loop. entityDistSqr(e, other) is the squared-distance helper the breed scan uses.

createAnimalAttributes() + suppliers map (level/attribute/defaults.go:165-262) — cowSupplier is the
exact analog: createAnimalAttributes().AddValue(...).Build(); register "wolf": wolfSupplier().

categoryOf (server/mob_category.go:87-91) — the cow arm returns categoryCreature.

entity_encode.go index-derivation (428-436) + woolDataEntry builder (450-456) + encodeSetEntityDataByID
(506) + broadcastBabyFlag pattern (combat_mob.go:786-796).

combat_mob.go flag2 store-point (110-126): sets e.lastDamageSource / e.lastHurtByMob /
e.lastHurtByMobTimestamp on a fresh hit. The anger timer is the additive sibling here.

The gametime seam: combat_mob.go runs on the tick goroutine; the MeleeAttackGoal cooldown already reads
the current gametime (the same value isAngry compares against — find the existing gametime accessor the
combat/tick code uses and reuse it; do NOT invent a new clock).

entity.Wolf.ID = 149, Wolf.Type == "creature" (data/entity/entity.go:1362,1368).
entity.Skeleton.ID (data/entity/entity.go:1055 — the SKELETON-target class for skeleton_target).
WOLF_FOOD tag = data/tag/tags.go:375.

vanilla_pig_embed.go: //go:embed directive (32), vanillaWolfMobName const + vanillaMobNames list (38-70).
commands_dbg.go: the /dbg cow arm (20-24) + usage string (60).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wolf state fields + DATA_FLAGS/owner metadata + wolfSupplier + categoryOf + base_type/embed/dbg wiring</name>
  <files>server/entity.go, server/entity_encode.go, level/attribute/defaults.go, server/mob_category.go, server/plugin_mob_decl.go, server/vanilla_pig_embed.go, server/commands_dbg.go</files>
  <read_first>
    - server/entity.go (the breedAge/inLove/sheared/eggTime field cluster, ~270-337 — the sheared bool at 315-325 is the closest single template)
    - server/entity_encode.go (babyDataEntry 384-417, woolDataEntry 428-456, the dataWoolIndex=18 derivation, encodeSetEntityDataByID 506)
    - level/attribute/defaults.go (cowSupplier 165-170, catSupplier 110-116, the suppliers map 246-262)
    - server/mob_category.go (the cow arm 87-91)
    - server/plugin_mob_decl.go (baseTypeByName 120-133, the loud-error allowed list ~355)
    - server/vanilla_pig_embed.go (the //go:embed directive 32, the mob-name const + vanillaMobNames 38-70)
    - server/commands_dbg.go (the /dbg cow arm 20-24, the usage string 60)
    - .planning/phases/36-wolf-neutral/36-JARNOTES.md (lines 109-118 TamableAnimal DATA_FLAGS 0x1 sit/0x4 tame + DATA_OWNERUUID; 206-216 the CONFIRMED index 18/19 derivation; 239-247 the W6-DISSOLVED gametime-endpoint anger model; 157-163 attributes + applyTamingSideEffects 8→40)
    - .planning/phases/36-wolf-neutral/36-PATTERNS.md (the entity.go, entity_encode.go, defaults.go, mob_category.go sections)
  </read_first>
  <action>
    Add the wolf's TamableAnimal/NeutralMob STATE to server/entity.go, following the sheared-bool documented pattern (jar-cite + "zero is the pig default" + "tick-owned" note on each). Fields (cite net.minecraft.world.entity.TamableAnimal + NeutralMob, JARNOTES:109-118,239-247):
      - `tame bool`           // DATA_FLAGS bit 0x4 (TamableAnimal.isTame). FALSE for the pig.
      - `orderedToSit bool`   // the plain orderedToSit field (setOrderedToSit). FALSE for the pig.
      - `inSittingPose bool`  // DATA_FLAGS bit 0x1 (setInSittingPose). FALSE for the pig.
      - `ownerUUID` (store the owner entity id/thin-id + UUID, mirroring how the codebase already stores entity refs; DATA_OWNERUUID_ID = Optional<EntityReference<LivingEntity>>, JARNOTES:73,113). Zero/empty for the pig.
      - `angerEndTime int64`  // the GAMETIME-ENDPOINT of the persistent-anger timer (NeutralMob, W6-DISSOLVED). NOT a counter+decrement: isAngry() == angerEndTime > 0 && (angerEndTime - gameTime) > 0 (JARNOTES:239-247). 0 for the pig — the anger EXPIRES automatically by gametime, NO per-tick decrement, NO ResetUniversalAngerTargetGoal.
      - `angerTarget` (persistentAngerTarget UUID/thin-id). Empty for the pig.
      - `lastHurtMob` + `lastHurtMobTimestamp` (the OWNER-SIDE bookkeeping OwnerHurtTargetGoal reads — mirror the existing lastHurtByMob/lastHurtByMobTimestamp set in combat_mob.go:124-125; JARNOTES:149-156). Set wherever an entity deals damage (the attacker side) — wolf-gated reads, zero for the pig.
    REUSE the existing lastHurtByMob/lastHurtByMobTimestamp for the inbound side (no new inbound field).

    In server/entity_encode.go, add the DATA_FLAGS byte + owner-UUID metadata following woolDataEntry EXACTLY. The accessor indices are CONFIRMED via javap (JARNOTES:206-216 — no longer open):
      - `const dataWolfFlagsIndex uint8 = 18` and `const dataWolfOwnerIndex uint8 = 19`. The chain is VERIFIED: Entity 0-7, LivingEntity 8-14, Mob 15, AgeableMob 16=DATA_BABY_ID + 17=AGE_LOCKED, Animal adds NONE, TamableAnimal.defineSynchedData = super; define(DATA_FLAGS_ID, (byte)0); define(DATA_OWNERUUID_ID, empty) → DATA_FLAGS = 18 (BYTE serializer id 0), owner-UUID = 19. (Sheep's DATA_WOOL is ALSO index 18 — fine, indices are PER-CLASS-HIERARCHY.) These are the EXPECTED constants; re-confirm the count at exec and record it in the SUMMARY (confirmation, not discovery).
      - `func wolfFlagsDataEntry(flagsByte byte) entityDataEntry` building {index: dataWolfFlagsIndex, serializerID: byteSerializerID, value: pk.Byte(int8(flagsByte))} — the woolDataEntry shape (450-456). Compute flagsByte from the wolf fields: bit 0x1 if inSittingPose, bit 0x4 if tame.
      - The owner-UUID wire broadcast DEFERS (the server-side owner ref is enough for the goals; the client owner-glow/collar render is deferred, JARNOTES:211-212,250-252). Encode DATA_FLAGS (index 18) ONLY for v1; cite the owner-metadata wire deferral. Keep the dataWolfOwnerIndex const defined for the record.
    Wolf-gate the splice: only emit wolfFlagsDataEntry for `e.typ == entity.Wolf.ID`. State the "pig oracle unperturbed" note.

    In level/attribute/defaults.go, add wolfSupplier exactly like cowSupplier (JARNOTES:39-41,157-159):
      createAnimalAttributes().AddValue(MovementSpeed, 0.3).AddValue(MaxHealth, 8.0).AddValue(AttackDamage, 4.0).Build()
    Register "wolf": wolfSupplier() in the suppliers map. (The tamed 8→40 bump is runtime applyTamingSideEffects, NOT the base supplier — Plan B owns it.)

    In server/mob_category.go, add the wolf arm: case entity.Wolf.ID: return categoryCreature (cite vanilla EntityType.WOLF / Wolf.Type=="creature").

    In server/plugin_mob_decl.go, add "wolf": entity.Wolf to baseTypeByName and add "wolf" to the loud-error allowed list (~355).

    In server/vanilla_pig_embed.go, add assets/vanilla_wolf to the //go:embed directive, add `vanillaWolfMobName = "vanilla_wolf"` const, and append vanillaWolfMobName to vanillaMobNames.

    In server/commands_dbg.go, add the `case "wolf":` arm mirroring /dbg cow (spawnVanillaMob(vanillaWolfMobName, p.x,p.y,p.z) + broadcast), and add "wolf" to the usage string (60).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./server/... ./level/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -nE "tame bool|orderedToSit bool|inSittingPose bool|angerEndTime int64" server/entity.go returns all four
    - grep -n "func wolfSupplier" level/attribute/defaults.go is non-empty AND grep -n '"wolf": wolfSupplier()' level/attribute/defaults.go is non-empty
    - grep -n "dataWolfFlagsIndex uint8 = 18" server/entity_encode.go is non-empty AND grep -n "func wolfFlagsDataEntry" server/entity_encode.go is non-empty
    - grep -n "entity.Wolf.ID" server/mob_category.go is non-empty (the categoryCreature arm)
    - grep -n '"wolf": entity.Wolf' server/plugin_mob_decl.go is non-empty
    - grep -n "vanillaWolfMobName" server/vanilla_pig_embed.go is non-empty AND the //go:embed line includes assets/vanilla_wolf
    - grep -n 'case "wolf"' server/commands_dbg.go is non-empty
    - CGO_ENABLED=0 go build ./... exits 0
  </acceptance_criteria>
  <done>Wolf state (anger held as the gametime-endpoint angerEndTime int64, not a counter), metadata (DATA_FLAGS 18; owner wire deferred), supplier, category, base_type, embed, and /dbg wiring all compile; the pig path is untouched (all wolf code is entity.Wolf.ID-gated).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The NEW goal classes (sit/follow_owner/owner_hurt) + the PARAMETERIZED target goal (angry_player_target + skeleton_target) + the kind seam + the gametime-endpoint anger trigger</name>
  <files>server/ai_goals_sit.go, server/ai_goals_owner.go, server/ai_goals_target.go, server/plugin_mob_ai.go, server/plugin_mob_decl.go, server/combat_mob.go</files>
  <read_first>
    - server/ai_goals_target.go (THE FILE THIS TASK PARAMETERIZES — read the WHOLE file: nearestAttackableTargetGoal struct+ctor 51-72, canUse 87-98, findTarget 119-129, canContinueToUse 139-154, start 161-165, stop 172-177, nearestPlayerIDAt 298-312; hurtByTargetGoal 184-290 stays UNCHANGED)
    - server/ai_goals_breed.go (findFreePartner 130-147 — the `other.typ == wanted` nearest-wins scan over t.cur().entities.near; entityDistSqr usage) and ai_goals_follow.go (105 — the same near() scan)
    - server/entity_store.go (near 168-183 — the chunk-range broad-phase; columnOf)
    - server/ai_goals_attack.go (leapAtTargetGoal struct + newLeapAtTargetGoal ctor 404, flags flagJump|flagMove; meleeAttackGoal setWantTarget/move-to shape)
    - server/plugin_mob_ai.go (buildAIFromDecl 200-216 the kind route + flags assertion + panic-list 208; buildNativeGoal switch 271-290)
    - server/plugin_mob_decl.go (the nativeKind validation 289-307 — confirm no change needed)
    - server/combat_mob.go (the flag2 store-point 110-126 where lastHurtByMob/lastDamageSource are set; the gametime accessor the MeleeAttackGoal cooldown reads; broadcastBabyFlag/broadcastMobDamageEvent 786-796)
    - .planning/phases/36-wolf-neutral/36-JARNOTES.md (131-137 SitWhenOrderedToGoal canUse; 218-237 the B1/B2 target-goal parameterization fix; 239-247 W6-DISSOLVED gametime-endpoint anger model = angerEndTime int64, isAngry, isAngryAt; 149-156 OwnerHurtBy/OwnerHurtTarget canUse; 165-190 FollowOwnerGoal canUse/canContinue/start/tick + the teleport-deferral note)
    - .planning/phases/36-wolf-neutral/36-PATTERNS.md (the ai_goals_sit.go, ai_goals_owner.go, plugin_mob_ai.go, combat_mob.go sections)
  </read_first>
  <action>
    Create server/ai_goals_sit.go — sitWhenOrderedToGoal (NO RNG, flags flagJump|flagMove). Mirror newLeapAtTargetGoal's ctor shape:
      `func newSitWhenOrderedToGoal() *sitWhenOrderedToGoal { return &sitWhenOrderedToGoal{baseGoal: newBaseGoal(flagJump | flagMove)} }`
    canUse() ports SitWhenOrderedToGoal.canUse VERBATIM (JARNOTES:133-135, cite net.minecraft.world.entity.ai.goal.SitWhenOrderedToGoal):
      if (!orderedToSit && !isTame()) return false; if (isInWater()) return false; if (!onGround()) return false;
      owner = getOwner(); if (owner==null || owner.level != mob.level) return true;
      if (distanceToSqr(owner) < 144.0 && owner.getLastHurtByMob() != null) return false;   // 144.0 = 12² exact (W4-confirmed)
      return orderedToSit
    canContinueToUse() = isOrderedToSit(). start() = navigation.stop() + setInSittingPose(true) (flip DATA_FLAGS 0x1 + broadcast via the entity_encode helper). stop() = setInSittingPose(false). Read the new entity fields (orderedToSit/tame/ownerUUID) + the owner's lastHurtByMob (the owner-side bookkeeping from Task 1).

    Create server/ai_goals_owner.go — three goals, all NO RNG, mirroring hurtByTargetGoal's TARGET-goal shape (struct+ctor+flagTarget+canUse+timestamp guard):
      - ownerHurtByTargetGoal.canUse (JARNOTES:149-154, cite OwnerHurtByTargetGoal): if (!isTame() || isOrderedToSit()) return false; owner = getOwner(); if (owner==null) return false; ownerLastHurtBy = owner.lastHurtByMob; ts = owner.lastHurtByMobTimestamp; return ts != this.timestamp && canAttack(ownerLastHurtBy) && wantsToAttack(ownerLastHurtBy, owner). On start, setTarget(ownerLastHurtBy) + stamp this.timestamp = ts.
      - ownerHurtTargetGoal.canUse (the mirror — targets whoever the OWNER attacked; reads owner.lastHurtMob / owner.lastHurtMobTimestamp from Task 1). Same gate + timestamp guard. javap OwnerHurtTargetGoal at exec to confirm the exact field reads and record in SUMMARY.
      - followOwnerGoal — `func newFollowOwnerGoal(speed float64) *followOwnerGoal` with the MOVE flag. canUse (JARNOTES:165-190, cite FollowOwnerGoal): owner = getOwner(); if (owner==null) return false; if (unableToMoveToOwner()) return false; if (distanceToSqr(owner) < startDistance²) return false [startDistance=10 → 100.0, W4-confirmed]; return true. canContinueToUse() = !unableToMoveToOwner() && distanceToSqr(owner) > stopDistance² [stopDistance=2 → 4.0, W4-confirmed] && !isOrderedToSit(). tick(): lookAt(owner); if (--timeToRecalcPath > 0) return; timeToRecalcPath = 10; navigation.moveTo(owner, speed). DEFER the teleport (tryToTeleportToOwner safe-pos scan) for v1 — the moveTo path-follow is the core observable; cite the teleport deferral in code + SUMMARY. javap FollowOwnerGoal.canUse/canContinueToUse/start at exec to confirm unableToMoveToOwner; record the verified values (the 100.0/4.0 are JARNOTES-confirmed expectations).
    Use the existing setTarget/getTarget (ai_goals_target.go:161-177) and nav-stop/setWantTarget seams.

    PARAMETERIZE server/ai_goals_target.go (the B1/B2 root fix, JARNOTES:218-237 — cite NearestAttackableTargetGoal.findTarget's getNearestEntity(getEntitiesOfClass(type,...)) branch). This is a TARGETED edit, NOT a rewrite — the hurtByTargetGoal + nearestPlayerIDAt stay byte-identical:
      - Add a target-class enum: `type nearestTargetClass int` with `targetClassPlayer` (0, the default/zero value) + `targetClassSkeleton`.
      - Add two fields to nearestAttackableTargetGoal: `targetClass nearestTargetClass` and `angerGate func(t *TickLoop, e *Entity) bool` (nil == no gate). The EXISTING newNearestAttackableTargetGoal() leaves targetClass = targetClassPlayer (zero) + angerGate = nil → byte-identical Phase-35 behavior (the hostiles are UNCHANGED — re-run the hostile tests + the pig oracle to prove it).
      - findTarget switches on targetClass: targetClassPlayer → the EXISTING nearestPlayerIDAt(t, e.x, e.y, e.z, follow) (unchanged); targetClassSkeleton → a NEW nearestEntityOfTypeAt(t, e, entity.Skeleton.ID, follow) returning the nearest entity id of that type within FOLLOW_RANGE (the mob-vs-mob branch).
      - Add `func nearestEntityOfTypeAt(t *TickLoop, e *Entity, typ int32, maxDist float64) (int32, bool)`: convert maxDist (blocks) to a chunk range via ceil(maxDist/16), scan `t.cur().entities.near(e.x, e.z, rangeChunks)`, skip `other == e || other.typ != typ`, keep the nearest by entityDistSqr within maxDist² (the findFreePartner nearest-wins shape, ai_goals_breed.go:130-147). Returns the entity id (for setTarget) — the mob-class analog of nearestPlayerIDAt. NO RNG. Cite NearestAttackableTargetGoal.findTarget non-Player branch.
      - canUse: AFTER the existing nextInt(g.randomInterval) gate + findTarget, add the anger gate — if `g.angerGate != nil && g.target != 0 && !g.angerGate(t, e)` then `g.target = 0` (drop the acquired target). The HOSTILES keep angerGate=nil so this branch never fires for them (Phase-35 UNCHANGED). Cite NeutralMob.isAngryAt gating NearestAttackableTargetGoal<Player>.
      - canContinueToUse: it currently resolves via playerByEntityID (player-only). For the SKELETON class, resolve via the entity store (t.cur().entities.get(id)) + the FOLLOW_RANGE distance instead. Branch canContinueToUse on targetClass (player → the existing player-resolve; skeleton → entity-resolve). Keep the player branch byte-identical.
      - Add TWO ctors:
          `func newAngryPlayerTargetGoal() *nearestAttackableTargetGoal` → PLAYER class + angerGate = isAngryAt (the wolf isAngryAt predicate below). flags {TARGET}.
          `func newSkeletonTargetGoal() *nearestAttackableTargetGoal` → SKELETON class + angerGate = nil. flags {TARGET}.
        Both reuse the same struct/canUse/start/stop — only targetClass + angerGate differ.
      - Add the isAngryAt predicate (port NeutralMob.isAngryAt, JARNOTES:144-145,239-247): `func isAngryAt(t *TickLoop, e *Entity, targetID int32) bool` (or the angerGate closure form). Returns true iff the wolf's anger is LIVE — `e.angerEndTime > 0 && (e.angerEndTime - <currentGameTime>) > 0` (the gametime-endpoint isAngry) AND the target matches e.angerTarget / is a valid player. The angerGate passed to newAngryPlayerTargetGoal consults the goal's acquired target id (g.target) against this. Use the SAME gametime accessor combat_mob.go uses.

    Extend buildNativeGoal (server/plugin_mob_ai.go:271-290) with the 6 cases:
      case "sit":                 return newSitWhenOrderedToGoal()
      case "follow_owner":        return newFollowOwnerGoal(declaredWalkSpeed(decl))
      case "owner_hurt_by":       return newOwnerHurtByTargetGoal()
      case "owner_hurt":          return newOwnerHurtTargetGoal()
      case "angry_player_target": return newAngryPlayerTargetGoal()
      case "skeleton_target":     return newSkeletonTargetGoal()
    Update the panic valid-kinds list at line 208 to include sit, follow_owner, owner_hurt_by, owner_hurt, angry_player_target, skeleton_target. Confirm the flags() assertion (215-216) passes for each (sit declares JUMP|MOVE, follow_owner declares MOVE, the owner_hurt* + angry_player_target + skeleton_target all declare TARGET — all flagTarget). The bare "nearest_attackable_target" stays the un-gated PLAYER goal — DO NOT change its case (the hostiles keep it).

    In server/combat_mob.go, at the flag2 store-point (110-126), add the wolf-gated anger trigger AFTER the existing lastHurtByMob write (cite NeutralMob.startPersistentAngerTimer + Wolf.PERSISTENT_ANGER_TIME = TimeUtil.rangeOfSeconds(20,39) = UniformInt(400,780), the GAMETIME-ENDPOINT model JARNOTES:139-143,239-247):
      if e.typ == entity.Wolf.ID && <src.attacker is a player> {
          e.angerEndTime = <currentGameTime> + int64(400 + mobRandom(e).nextInt(381))   // setTimeToRemainAngry: angerEndTime = gameTime + UniformInt(400,780).sample
          e.angerTarget = <the attacking player's thin-id/UUID>
      }
    Use the SAME mobRandom(e) per-entity stream the combat kinds draw from AND the SAME gametime accessor the MeleeAttackGoal cooldown reads. There is NO per-tick decrement and NO ResetUniversalAngerTargetGoal — isAngry compares angerEndTime to gametime and expires automatically (W6-DISSOLVED). The pig draws ZERO (the gate is typ == Wolf.ID). The angry_player_target goal's angerGate reads this via isAngryAt.
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./server/...</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "func (g \*sitWhenOrderedToGoal) canUse" server/ai_goals_sit.go is non-empty
    - grep -nE "144\.0|orderedToSit" server/ai_goals_sit.go shows the 144.0 distance gate
    - grep -n "func newFollowOwnerGoal" server/ai_goals_owner.go is non-empty AND grep -nE "ownerHurtByTargetGoal|ownerHurtTargetGoal" server/ai_goals_owner.go shows both
    - grep -n "func nearestEntityOfTypeAt" server/ai_goals_target.go is non-empty (the skeleton-class scan)
    - grep -nE "func newAngryPlayerTargetGoal|func newSkeletonTargetGoal" server/ai_goals_target.go shows both new ctors
    - grep -n "angerGate" server/ai_goals_target.go is non-empty (the anger-gate field + the canUse drop)
    - grep -nE "targetClassSkeleton|targetClassPlayer" server/ai_goals_target.go shows the target-class enum
    - grep -nE 'case "sit"|case "follow_owner"|case "owner_hurt_by"|case "owner_hurt"|case "angry_player_target"|case "skeleton_target"' server/plugin_mob_ai.go shows all six
    - grep -n 'case "nearest_attackable_target"' server/plugin_mob_ai.go STILL returns newNearestAttackableTargetGoal() (the hostile path UNCHANGED)
    - grep -n "entity.Wolf.ID" server/combat_mob.go shows the wolf-gated anger trigger
    - grep -vE '^[[:space:]]*//' server/combat_mob.go | grep -c "nextInt(381)" equals 1 (exactly one anger draw, not in a comment)
    - grep -nE "angerEndTime" server/combat_mob.go shows the gametime-endpoint set (gameTime + 400 + nextInt(381)), NOT a counter
    - CGO_ENABLED=0 go test ./server/ -run 'TestNearestAttackableTargetGateUsesTen|TestHurtByTarget|Hostile|Zombie|Skeleton|Spider' -count=1 exits 0 (the Phase-35 hostile/target tests UNCHANGED)
    - CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -count=1 exits 0 (the pig oracle byte-identical)
    - CGO_ENABLED=0 go build ./... exits 0
  </acceptance_criteria>
  <done>The four NEW goal classes + the parameterized target goal (angry_player_target gated on isAngryAt + skeleton_target scanning entity.Skeleton.ID) compile and route through buildNativeGoal; the bare nearest_attackable_target stays the un-gated PLAYER goal (Phase-35 hostile tests + pig oracle byte-identical); a wolf hit by a player sets angerEndTime = gameTime + 400 + nextInt(381) on its own RNG stream (gametime-endpoint, no decrement); isAngryAt is the anger gate; the pig draws zero new RNG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server interact | the BONE-feed interact (Plan B) crosses here; this plan only stores the resulting owner/tame state |
| plugin→host (kind seam) | the .star declares kinds; buildNativeGoal instantiates trusted Go goals — no plugin code on the hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | the kind seam (buildNativeGoal) | mitigate | unknown kinds panic at load (buildAIFromDecl:208) — a forged kind cannot silently no-op; the flags() assertion rejects flag/goal disagreement (all six new kinds assert their flag set) |
| T-36-02 | Denial of Service | the per-entity mobRandom anger draw | accept | one bounded nextInt(381) per fresh hit on a wolf; wolf-gated so the pig/other mobs draw zero; no unbounded loop; the anger expires by gametime (no growing timer state) |
| T-36-03 | Information disclosure | owner-UUID metadata | accept | owner-UUID WIRE broadcast is deferred (server-side ref only); when it lands the owner UUID is already client-visible vanilla metadata; no PII beyond the player's own entity ref |
| T-36-11 | Denial of Service | the skeleton_target near() scan | accept | the scan is bounded by ceil(FOLLOW_RANGE/16) columns (the same broad-phase the breed/follow goals use), gated behind the nextInt(10) acquire roll — no full-store walk, no unbounded cost |
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... exits 0
- CGO_ENABLED=0 go vet ./server/... ./level/... clean
- No new Go dependency added (go.mod unchanged) — confirm `git diff go.mod` is empty
- Every ported goal cites its jar class/method in a code comment
- The Phase-35 hostile/target tests + the pig oracle stay green/byte-identical (the parameterization is additive — the bare nearest_attackable_target is unchanged)
</verification>

<success_criteria>
- The wolf base_type, supplier, category, state fields, DATA_FLAGS metadata, four new goal classes, the parameterized target goal (angry_player_target + skeleton_target), six kind-seam cases, and the gametime-endpoint anger trigger all compile and are wolf-gated.
- The DATA_FLAGS index (18) and owner index (19) are CONFIRMED against the TamableAnimal defineSynchedData chain count (re-verified at exec, recorded in SUMMARY).
- The anger model is the gametime-endpoint angerEndTime int64 (no per-tick decrement, no ResetUniversalAngerTargetGoal — both dissolved).
- DEFERRED + RECORDED (cited): the FollowOwner teleport (tryToTeleportToOwner safe-pos scan) — only moveTo path-follow ships; the owner-UUID WIRE broadcast (server-side ref drives the goals); ResetUniversalAngerTargetGoal (dead under the default UNIVERSAL_ANGER gamerule).
- The pig path + the Phase-35 hostiles are untouched (all wolf code gated on entity.Wolf.ID / base_type "wolf"; the bare nearest_attackable_target keeps angerGate=nil + targetClassPlayer).
</success_criteria>

<output>
After completion, create `.planning/phases/36-wolf-neutral/36-01-SUMMARY.md` — record the VERIFIED DATA_FLAGS/owner accessor indices, the exec-time decompiles (FollowOwnerGoal, OwnerHurtTargetGoal, OwnerHurtByTargetGoal), the parameterized target-goal shape (targetClass + angerGate, how angry_player_target/skeleton_target were built), the gametime-endpoint anger model, and the cited deferrals (FollowOwner teleport, owner-UUID wire, ResetUniversalAngerTargetGoal).
</output>
