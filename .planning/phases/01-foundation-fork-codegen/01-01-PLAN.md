---
phase: 01-foundation-fork-codegen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - tools/go.mod
  - tools/extract.go
  - .planning/phases/01-foundation-fork-codegen/baseline-774/
autonomous: true
requirements: [GEN-01, GEN-02]
user_setup:
  - service: github
    why: "Fork Tnze/go-mc and fetch draft PR branches #294/#295/#296"
    dashboard_config:
      - task: "Fork github.com/Tnze/go-mc to your own GitHub account (github.com/<you>/go-mc)"
        location: "GitHub — Tnze/go-mc — Fork button"

must_haves:
  truths:
    - "A local git clone of the forked go-mc exists, branched off the exact pinned master commit"
    - "PRs #294, #295, #296 are merged in that order with no unresolved conflicts"
    - "The repo has two separate Go modules: the runtime module (root go.mod) and tools/go.mod"
    - "The runtime module builds with Go alone (go build ./...) on the pre-codegen fork"
    - "The 774 generated trees from PR #296 are captured as a baseline snapshot for the later GEN-04 diff"
    - "tools/extract.go targets eclipse-temurin:25-jdk, not 21-jdk"
  artifacts:
    - path: "go.mod"
      provides: "Runtime module definition with no JVM/codegen dependency"
      contains: "module "
    - path: "tools/go.mod"
      provides: "Separate codegen module isolating generator + Java-shelling deps"
      contains: "module "
    - path: "tools/extract.go"
      provides: "Extractor container config retargeted to JDK 25"
      contains: "eclipse-temurin:25-jdk"
    - path: ".planning/phases/01-foundation-fork-codegen/baseline-774/"
      provides: "Snapshot of #296's committed 774 generated trees for the GEN-04 diff"
  key_links:
    - from: "tools/go.mod"
      to: "root module"
      via: "replace directive pointing at parent (../)"
      pattern: "replace .* => \\.\\."
    - from: "git branch ender-776"
      to: "master commit 539b4a3a7f030332eb58b8a946116ae7907630d2"
      via: "git checkout -b off the pinned commit"
      pattern: "539b4a3a7f030332eb58b8a946116ae7907630d2"
---

<objective>
Establish the forked go-mc repository as the project foundation: clone the fork, branch off the exact pinned master commit, apply the three draft codegen PRs (#294 → #295 → #296) in dependency order, capture #296's committed 774 generated trees as the GEN-04 baseline, and apply the single mandatory retarget edit (JDK 21 → 25 in the extractor container). This wires the two-module separation that structurally guarantees GEN-01 (Go-only runtime) and stages the codegen pipeline for the 26.2 run in Plan 02.

Purpose: Everything downstream depends on a correct, pinned, conflict-free fork with the codegen pipeline present and the module isolation that keeps Java out of the runtime build. Getting the base commit or PR order wrong produces conflicts the drafts never anticipated (Pitfall 5).
Output: A forked go-mc repo on branch `ender-776` with #294-296 merged, a separate `tools/` module, a captured 774 baseline, and the JDK-25 retarget edit committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-fork-codegen/01-RESEARCH.md
@.planning/phases/01-foundation-fork-codegen/01-VALIDATION.md

<key_facts>
<!-- All values below are VERIFIED in 01-RESEARCH.md against live Mojang API and PR source. Use them literally. -->
- Pinned master base commit: `539b4a3a7f030332eb58b8a946116ae7907630d2` (2024-12-24; master frozen since, no drift).
- Apply PRs in order #294 → #295 → #296 (all report MERGEABLE/CLEAN against that commit):
  - #294 = tools/ codegen pipeline (separate go.mod, removes old generators)
  - #295 = hand-written framework adaptations (ProtocolVersion 774, BitStorage no length-prefix, teleport 769+ layout, component slot format, per-registry RegistryData)
  - #296 = generated 774 data (the baseline to diff 776 against)
- The mandatory retarget edit lives in tools/extract.go: `const jdkImage = "docker.io/library/eclipse-temurin:21-jdk"` → `eclipse-temurin:25-jdk` (26.2 jar is JDK-25 bytecode; running on 21 throws UnsupportedClassVersionError).
- DO NOT add a 26.2 entry to unobfuscated_versions.json — isNativelyUnobfuscated(major>=26) routes 26.x to the standard manifest (Anti-pattern in research).
- The two-module separation (tools/go.mod with a `replace` → parent) is how GEN-01 is structurally guaranteed; Java/codegen deps must never enter the runtime go.mod.
- The repo currently contains only .planning/ and CLAUDE.md — the go-mc fork does not exist yet and must be cloned INTO this working tree (or alongside, see Task 1 action).
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mojang CDN → codegen host | Server jar + lang files fetched over the network (executed in Plan 02; pinned here via PR config) |
| Upstream go-mc + draft PRs → our fork | Third-party source we execute and build; supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | Forked dependency (go-mc + #294-296) | mitigate | Pin the exact master commit `539b4a3a…` and merge specific PR branch heads; never track a moving `@master`. Record the resolved PR head SHAs in the SUMMARY for reproducibility. |
| T-1-03 | Tampering | tools/ codegen module leaking into runtime | mitigate | Enforce two-module separation (tools/go.mod separate, `replace` → parent). Runtime `go build ./...` must succeed with no codegen/Java dependency in the runtime module graph. |
| T-1-01 | Spoofing | 26.2 server jar integrity (deferred to Plan 02) | accept (here) | Jar is not fetched in this plan; integrity verification (pinned sha1 `823e2250…`) is owned by Plan 02's codegen run. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Clone the fork, branch off the pinned commit, apply PRs #294→#295→#296</name>
  <files>go.mod, tools/go.mod (and the full fork tree these PRs introduce)</files>
  <read_first>
    - .planning/phases/01-foundation-fork-codegen/01-RESEARCH.md — "Fork + apply the three PRs (pinned base, dependency order)" code block and Pitfall 5.
    - The `<key_facts>` block above for the exact commit SHA and PR order.
  </read_first>
  <action>
    The working tree (D:\ender) currently holds only .planning/ and CLAUDE.md, both already committed. Bring the forked go-mc source INTO this same repository root so the runtime module lives at the repo root alongside .planning/.

    Steps (run from D:\ender; the user has already forked Tnze/go-mc to github.com/<you>/go-mc per user_setup):

    1. Add the fork and upstream as remotes and fetch the pinned base + PR branches WITHOUT clobbering the existing .planning/ commits:
       ```bash
       git remote add upstream https://github.com/Tnze/go-mc
       # If the user's fork remote is not yet set, add it (replace <you>):
       git remote add fork https://github.com/<you>/go-mc 2>/dev/null || true
       git fetch upstream 539b4a3a7f030332eb58b8a946116ae7907630d2
       git fetch upstream pull/294/head:pr294
       git fetch upstream pull/295/head:pr295
       git fetch upstream pull/296/head:pr296
       ```
    2. Create the working branch off the pinned commit, then merge the go-mc base tree into the current root. Because .planning/ + CLAUDE.md are already committed on master and go-mc has unrelated history, merge with `--allow-unrelated-histories`:
       ```bash
       git checkout -b ender-776
       git merge --no-ff --allow-unrelated-histories 539b4a3a7f030332eb58b8a946116ae7907630d2 -m "merge: go-mc master base @539b4a3a (pinned)"
       ```
    3. Merge the three PR branches IN ORDER (each `--no-ff`), resolving any conflicts (none expected — all three are MERGEABLE/CLEAN against this base; a conflict signals the wrong base commit):
       ```bash
       git merge --no-ff pr294 -m "merge: PR #294 tools codegen pipeline"
       git merge --no-ff pr295 -m "merge: PR #295 framework adaptations (proto 774)"
       git merge --no-ff pr296 -m "merge: PR #296 generated 774 data baseline"
       ```
    4. Set the runtime module path to the fork. Edit the root `go.mod` `module` line to `github.com/<you>/go-mc` (matching the actual fork URL). Confirm `tools/go.mod` exists as a SEPARATE module with a `replace` directive pointing at the parent.

    If `git merge` reports conflicts in level/, bot/, or server/, STOP — that means the base commit is wrong (Pitfall 5). Re-verify you branched off exactly `539b4a3a7f030332eb58b8a946116ae7907630d2`.

    Record the resolved head SHAs of pr294/pr295/pr296 (from `git rev-parse pr294 pr295 pr296`) in the plan SUMMARY for reproducibility (T-1-02).
  </action>
  <verify>
    <automated>test -f go.mod && test -f tools/go.mod && grep -q "replace" tools/go.mod && grep -Eq "replace .*=> *\.\." tools/go.mod && git log --oneline | grep -q "PR #296" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` exists at repo root with a `module github.com/<you>/go-mc` line.
    - `tools/go.mod` exists as a separate module containing a `replace ... => ..` directive pointing at the parent.
    - `git log --oneline` shows the three merge commits for #294, #295, #296 in that order, and the pinned-base merge.
    - No unresolved merge conflict markers anywhere: `! grep -rl '^<<<<<<< ' --include=*.go .` returns clean.
  </acceptance_criteria>
  <done>The fork is present at the repo root on branch ender-776, branched off the pinned commit, with #294-296 merged in order and the two-module layout intact.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Go-only build of the bare fork and capture the 774 baseline</name>
  <files>.planning/phases/01-foundation-fork-codegen/baseline-774/ (new snapshot directory)</files>
  <read_first>
    - .planning/phases/01-foundation-fork-codegen/01-VALIDATION.md — Wave 0 Requirements (774 baseline capture) and the note: "If a 774 baseline cannot be generated locally (no 1.21.11 jar), capture the #296 PR's committed generated files as the baseline instead."
    - .planning/phases/01-foundation-fork-codegen/01-RESEARCH.md — Pattern 3 "Regenerate and diff" and the generated-data package table (Standard Stack) for the list of generated trees.
  </read_first>
  <action>
    Two parts:

    PART A — Prove the bare fork builds with Go alone (GEN-01 first checkpoint). From the repo root run `go build ./...` on the RUNTIME module only. It must succeed using Go alone — no Java, no Docker, no codegen. If a generated package fails to build, note it; #296 already committed valid 774 Go, so a clean build is expected.

    PART B — Capture the 774 baseline for the future GEN-04 diff. Because we likely cannot run a 1.21.11 jar locally, the baseline IS the generated files committed by PR #296 (per the VALIDATION.md note). Snapshot the current (774) state of every generated tree into a baseline directory so Plan 03 can diff the regenerated 776 trees against it. Copy these generated trees (paths per the research generated-data table) into `.planning/phases/01-foundation-fork-codegen/baseline-774/`:
      - data/packetid/
      - data/registryid/
      - data/item/
      - data/entity/
      - data/soundid/
      - level/block/ (blocks + states)
      - level/component/
      - level/biome/
      - data/lang/

    Use a copy that preserves directory structure, e.g.:
    ```bash
    mkdir -p .planning/phases/01-foundation-fork-codegen/baseline-774
    for d in data/packetid data/registryid data/item data/entity data/soundid level/block level/component level/biome data/lang; do
      [ -d "$d" ] && mkdir -p ".planning/phases/01-foundation-fork-codegen/baseline-774/$d" && cp -r "$d/." ".planning/phases/01-foundation-fork-codegen/baseline-774/$d/"
    done
    ```
    Adjust the exact path list if #296 placed any tree differently — list the actual generated dirs with `git show --stat HEAD` for the #296 merge if a path is missing, and snapshot what actually exists. The baseline must capture the same trees that Plan 02 regenerates.

    Record in the SUMMARY: which trees were captured and their 774 file/line counts (these are the "before" side of the GEN-04 enumeration).
  </action>
  <verify>
    <automated>go build ./... && test -d .planning/phases/01-foundation-fork-codegen/baseline-774/data/packetid && test -d .planning/phases/01-foundation-fork-codegen/baseline-774/data/registryid && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 on the runtime module with no Java/Docker present.
    - `.planning/phases/01-foundation-fork-codegen/baseline-774/` contains the snapshotted 774 generated trees (at minimum packetid and registryid present and non-empty).
    - The runtime `go.mod` requires no codegen/Java module: `! grep -Eiq '\bjava\b' go.mod`.
  </acceptance_criteria>
  <done>The bare fork builds with Go alone, and the 774 generated trees are snapshotted into baseline-774/ ready for the GEN-04 diff.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the mandatory JDK 21 → 25 retarget edit in tools/extract.go</name>
  <files>tools/extract.go</files>
  <read_first>
    - .planning/phases/01-foundation-fork-codegen/01-RESEARCH.md — Pitfall 1 (UnsupportedClassVersionError) and "The one mandatory retarget edit (tools/extract.go)" code block.
    - tools/extract.go — locate the `const jdkImage` line introduced by PR #294.
  </read_first>
  <action>
    PR #294 hardcodes the extractor container image. The 26.2 jar is JDK-25 bytecode (Mojang's 26.2.json declares javaVersion.majorVersion: 25), so the container must be bumped from JDK 21 to 25 — this is the single mandatory code change to retarget the pipeline.

    In tools/extract.go, change the jdkImage constant from the temurin 21 image to the temurin 25 image. Prefer pinning a digest for reproducibility:
      BEFORE: `const jdkImage = "docker.io/library/eclipse-temurin:21-jdk"`
      AFTER:  `const jdkImage = "docker.io/library/eclipse-temurin:25-jdk"`  (optionally `@sha256:<digest>` — resolve and pin the digest if Docker is available; if not, the tag form is acceptable and the SUMMARY must note the digest was not pinned).

    Do NOT touch unobfuscated_versions.json — adding a 26.2 entry there is wrong (isNativelyUnobfuscated routes 26.x to the standard manifest).

    Make exactly this one edit. Confirm tools/extract.go still compiles within the tools module (`cd tools && go build ./...`).
  </action>
  <verify>
    <automated>grep -q 'eclipse-temurin:25-jdk' tools/extract.go && ! grep -q 'eclipse-temurin:21-jdk' tools/extract.go && cd tools && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/extract.go` references `eclipse-temurin:25-jdk` and no longer references `eclipse-temurin:21-jdk`.
    - The tools module compiles: `cd tools && go build ./...` exits 0.
    - `unobfuscated_versions.json` is unchanged (no 26.2 entry added): `! grep -q '26.2' tools/unobfuscated_versions.json` (path may vary; assert no 26.2 entry wherever the file lives).
  </acceptance_criteria>
  <done>The extractor container is retargeted to JDK 25, the only mandatory code change to run codegen against the 26.2 jar.</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds on the runtime module with Go alone (GEN-01 structural checkpoint).
- `tools/go.mod` is a separate module with a `replace` → parent (Java/codegen isolation).
- `git log --oneline` shows pinned-base + #294/#295/#296 merges in order.
- `tools/extract.go` targets `eclipse-temurin:25-jdk`.
- `baseline-774/` holds the snapshotted 774 generated trees for the GEN-04 diff.
</verification>

<success_criteria>
- Forked go-mc on branch `ender-776`, branched off `539b4a3a7f030332eb58b8a946116ae7907630d2`, with #294-296 merged in dependency order and no unresolved conflicts.
- Two-module separation in place (root runtime go.mod + tools/go.mod with replace).
- Bare fork builds with `go build ./...` (Go alone, no JVM).
- 774 baseline captured under `.planning/phases/01-foundation-fork-codegen/baseline-774/`.
- JDK-25 retarget edit applied in `tools/extract.go`.
- Resolved PR head SHAs recorded in the SUMMARY (reproducibility, T-1-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fork-codegen/01-01-SUMMARY.md`.
</output>
