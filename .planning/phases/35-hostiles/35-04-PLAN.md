---
phase: 35-hostiles
plan: 04
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - plugins/vanilla_skeleton/main.star
  - plugins/vanilla_skeleton/plugin.toml
  - server/assets/vanilla_skeleton/main.star
  - server/assets/vanilla_skeleton/plugin.toml
  - server/skeleton_test.go
autonomous: true
requirements: [MOB-HOST-02]
must_haves:
  truths:
    - "vanilla_skeleton declares Skeleton.registerGoals (melee-only subset): goalSelector WaterAvoidingRandomStroll@5 + LookAtPlayer@6 + LookAround@6 + a MeleeAttackGoal; targetSelector HurtByTargetGoal@1 + NearestAttackableTargetGoal<Player>@2"
    - "the skeleton spawns, acquires a player target, paths to it, and deals melee damage through MeleeAttackGoal"
    - "the bow/RangedBowAttackGoal/reassessWeaponGoal ranged path is cite-deferred (SC#2 allows melee-only for v1) and recorded, never silently dropped"
    - "the repo-root and embed copies of vanilla_skeleton are byte-identical"
  artifacts:
    - path: "plugins/vanilla_skeleton/main.star"
      provides: "the jar-faithful melee-only skeleton declaration"
      contains: "declare_mob"
    - path: "server/assets/vanilla_skeleton/main.star"
      provides: "the byte-identical embed copy"
      contains: "declare_mob"
    - path: "server/skeleton_test.go"
      provides: "the per-hostile behavior + RNG gate"
      contains: "func TestSkeleton"
  key_links:
    - from: "plugins/vanilla_skeleton/main.star NearestAttackableTargetGoal goal"
      to: "the targetSelector (via buildAIFromDecl)"
      via: "flags=[\"TARGET\"]"
      pattern: "TARGET"
    - from: "plugins/vanilla_skeleton/main.star melee goal tick"
      to: "the melee damage keystone"
      via: "the host attack handle"
      pattern: "melee"
---

<objective>
Build the skeleton as a jar-faithful dogfood plugin (MOB-HOST-02, SC#2) — Skeleton.registerGoals re-expressed as vanilla_skeleton, MELEE-ONLY for v1 (SC#2 explicitly allows this; the bow/RangedBowAttackGoal/reassessWeaponGoal/Arrow-entity ranged path is the documented follow-up). It reuses the shared passive goals (copied from vanilla_cow) + the 35-01 target/melee goals. The skeleton hunts the player at night and melees.

Purpose: The second hostile, proving the melee-only subset of a normally-ranged mob is a clean cite-deferred port. Same template as the zombie.
Output: the vanilla_skeleton plugin pair (repo-root + byte-identical embed) + a per-hostile behavior + RNG test. NO Go-file change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-hostiles/35-JARNOTES.md
@.planning/phases/35-hostiles/35-PATTERNS.md
@.planning/phases/35-hostiles/35-CONTEXT.md
@./CLAUDE.md
@plugins/vanilla_cow/main.star
@plugins/vanilla_cow/plugin.toml
@server/cow_test.go

<interfaces>
<!-- Same builtins + host handles as vanilla_zombie (see 35-03). -->

declare_mob/goal builtins + flags=["TARGET"] routing — shown in plugins/vanilla_cow/main.star + 35-03.
base_type "skeleton" already exists (baseTypeByName). The shared passive goal callbacks (stroll/look/around)
are mob-agnostic — COPY VERBATIM from vanilla_cow. The 35-01 ported MeleeAttackGoal / NearestAttackableTargetGoal /
HurtByTargetGoal — wire per the integration shape in 35-01-SUMMARY.md. The lockstep target gate uses
entity.rand_int(10).

Skeleton attrs (35-JARNOTES.md:76): movement_speed 0.25; MAX_HEALTH 20 (Mob base); ATTACK_DAMAGE from the
Monster base (createMonsterAttributes adds ATTACK_DAMAGE — confirm the default value the melee deals).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vanilla_skeleton plugin pair (melee-only subset, repo-root + byte-identical embed)</name>
  <read_first>
    - plugins/vanilla_cow/main.star (the template — shared goal callbacks + declare_mob block + cite style)
    - plugins/vanilla_cow/plugin.toml + server/assets/vanilla_cow/* (the manifest + embed sibling)
    - .planning/phases/35-hostiles/35-JARNOTES.md (Skeleton/AbstractSkeleton.registerGoals lines 37-55 — incl. the RANGED bowGoal/meleeGoal swap to DEFER + attributes line 76)
    - .planning/phases/35-hostiles/35-CONTEXT.md (Skeleton goal set line 54-56 + the melee-only subset decision + deferred sun-avoidance)
    - .planning/phases/35-hostiles/35-01-SUMMARY.md (the host-handle/goal-kind integration shape for the melee + target goals)
  </read_first>
  <behavior>
    - The vanilla_skeleton declaration loads through the host (no parse error).
    - The melee-only goal set + priorities + flags match the AbstractSkeleton.registerGoals v1 subset.
    - The repo-root and server/assets copies are byte-identical.
  </behavior>
  <action>
Create plugins/vanilla_skeleton/main.star — a 1:1 vanilla-Skeleton dogfood (MELEE-ONLY v1), citing
net.minecraft.world.entity.monster.skeleton.AbstractSkeleton.registerGoals + Skeleton. Copy the vanilla_cow
header/structure style.

GOAL SET (35-CONTEXT:54-56, 35-JARNOTES:37-51 — the v1 MELEE subset; RestrictSun/FleeSun/AvoidWolf
DEFERRED):
  goalSelector:
    @4 MeleeAttackGoal(mob, 1.0, false)          flags=["MOVE","LOOK"]  — the v1 melee (the bowGoal swap deferred)
    @5 WaterAvoidingRandomStrollGoal(mob, 1.0)   flags=["MOVE"]         — COPY VERBATIM from vanilla_cow stroll
    @6 LookAtPlayerGoal(Player, 8.0)             flags=["LOOK"]         — COPY from vanilla_cow look (lookDist 8.0)
    @6 RandomLookAroundGoal                       flags=["MOVE","LOOK"]  — COPY VERBATIM from vanilla_cow around
  targetSelector:
    @1 HurtByTargetGoal(mob)                      flags=["TARGET"]       — the 35-01 hurtByTargetGoal
    @2 NearestAttackableTargetGoal<Player>(mob, mustSee)  flags=["TARGET"]  — the 35-01 nearestAttackableTargetGoal

NOTE: vanilla AbstractSkeleton normally swaps bowGoal<->meleeGoal in reassessWeaponGoal based on the held
item. For v1 (no weapon system, no Arrow entity) the skeleton is MELEE-ONLY — port the MeleeAttackGoal
directly and DEFER the ranged path. Document the deferral LOUDLY in the header:
"DEFERRED (SC#2-permitted, ROADMAP:240): RangedBowAttackGoal + reassessWeaponGoal + performRangedAttack +
the Arrow projectile entity — its own phase; the melee subset is the v1 ship." Also document the deferred
RestrictSunGoal/FleeSunGoal (sun-avoidance nav) + AvoidEntityGoal<Wolf> + the IronGolem/Turtle target
variants. Nothing silent.

The shared passive callbacks (stroll/look/around) COPY VERBATIM from vanilla_cow (lookDistance 8.0). The
melee + target goals reference 35-01's ports (wire per 35-01-SUMMARY.md). The NearestAttackableTargetGoal
gate uses entity.rand_int(10) (the LOCKSTEP full 10). The MeleeAttackGoal 20-tick cooldown is RNG-free.

ATTRIBUTES dict (35-CONTEXT:64, 35-JARNOTES:76): movement_speed 0.25, max_health 20.0 (Mob base). The
ATTACK_DAMAGE the melee deals comes from the Monster base createMonsterAttributes — confirm + seed the
value. Each cited.

base_type = "skeleton".

Create plugins/vanilla_skeleton/plugin.toml — clone vanilla_cow/plugin.toml's least-privilege cap set (the
melee is a host-side op). Create the BYTE-IDENTICAL embed copies at server/assets/vanilla_skeleton/*.
  </action>
  <verify>
    <automated>cd D:/ender && diff plugins/vanilla_skeleton/main.star server/assets/vanilla_skeleton/main.star && diff plugins/vanilla_skeleton/plugin.toml server/assets/vanilla_skeleton/plugin.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "declare_mob" plugins/vanilla_skeleton/main.star` AND `grep -n "base_type = \"skeleton\"" plugins/vanilla_skeleton/main.star` return.
    - `grep -c "\"TARGET\"" plugins/vanilla_skeleton/main.star` >= 2.
    - `grep -n "rand_int(10)" plugins/vanilla_skeleton/main.star` returns AND `grep -c "rand_int(5)" plugins/vanilla_skeleton/main.star` == 0.
    - `grep -ci "DEFERRED\|RangedBowAttackGoal\|reassessWeaponGoal" plugins/vanilla_skeleton/main.star` >= 1 (the ranged deferral is recorded).
    - `diff plugins/vanilla_skeleton/main.star server/assets/vanilla_skeleton/main.star` EMPTY.
    - `diff plugins/vanilla_skeleton/plugin.toml server/assets/vanilla_skeleton/plugin.toml` EMPTY.
  </acceptance_criteria>
  <done>The vanilla_skeleton pair declares the melee-only goal set with TARGET routing + the lockstep gate + the ranged deferral recorded; both copies byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: skeleton_test.go — spawn + goal-set + target-acquire + melee-hit + RNG gate</name>
  <read_first>
    - server/cow_test.go (the loadVanillaCowRegistry harness lines 36-60)
    - server/zombie_test.go (the sibling per-hostile test from 35-03 — clone its shape)
    - plugins/vanilla_skeleton/main.star (the declaration under test)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the hostiles_test.go entry)
  </read_first>
  <behavior>
    - TestSkeletonBootLoads: the registry loads + the goal count (4 goalSelector + 2 targetSelector) + the 2
      TARGET goals route into the targetSelector.
    - TestSkeletonBehavior: spawn a skeleton + a player at night within FOLLOW_RANGE; tick; assert
      attackTargetID == player, the nav want points toward it, and (in melee reach + cooldown) the player
      takes ATTACK_DAMAGE.
    - TestSkeletonTargetGateRNG: the NearestAttackableTargetGoal gate draws exactly one rand_int(10).
  </behavior>
  <action>
Create server/skeleton_test.go — clone the cow_test.go harness as loadVanillaSkeletonRegistry (materialize
../plugins/vanilla_skeleton to a temp dir, LoadDirWith the builtins). Clone the zombie_test.go test shapes:
  - TestSkeletonBootLoads: assert name "vanilla_skeleton", goal count, and the 2 TARGET goals in the
    targetSelector / 4 in the goalSelector.
  - TestSkeletonBehavior: spawn + player at night; force the gate (seed nextInt(10)==0); tick; assert
    attackTargetID == player + the nav want toward it + the melee hit drops player health by ATTACK_DAMAGE.
  - TestSkeletonTargetGateRNG: assert exactly one rand_int(10) draw in the target gate.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./server/ -run "TestSkeletonBootLoads|TestSkeletonBehavior|TestSkeletonTargetGateRNG" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func TestSkeletonBootLoads" server/skeleton_test.go` AND `grep -n "func TestSkeletonBehavior" server/skeleton_test.go` AND `grep -n "func TestSkeletonTargetGateRNG" server/skeleton_test.go` all return.
    - `grep -n "loadVanillaSkeletonRegistry" server/skeleton_test.go` returns.
    - `cd D:/ender && go test ./server/ -run "TestSkeletonBootLoads|TestSkeletonBehavior|TestSkeletonTargetGateRNG" -count=1` exits 0.
  </acceptance_criteria>
  <done>The skeleton boot-loads with the melee-only routing, acquires + melees the player at night, and the target gate draws exactly rand_int(10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vanilla_skeleton .star -> host buildAIFromDecl | the declared goal set + attributes cross into the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Elevation | the melee callback -> player damage | mitigate | routes through the shared 35-01 host attack handle (attacker = mob id, host-set); identical mitigation to the zombie (T-35-07). |
| T-35-09 | Information | the deferred ranged path | accept | the bow/Arrow path is OUT of v1 scope (no projectile surface ships); the deferral is recorded, no unwired code is shipped (the no-built-but-unwired rule). |
</threat_model>

<verification>
- `diff` of both copies empty; `go test` of the 3 skeleton tests green.
- The melee-only goal set matches the AbstractSkeleton.registerGoals subset; the ranged deferral recorded.
- The lockstep rand_int(10) gate.
</verification>

<success_criteria>
- SC#2 (skeleton half): melee-only subset acquires + melees the player; maps to MONSTER.
- MOB-HOST-02: the jar-faithful Skeleton.registerGoals plugin, melee-only for v1 with the bow deferral documented.
</success_criteria>

<output>
After completion, create `.planning/phases/35-hostiles/35-04-SUMMARY.md` (record the deferred ranged path: RangedBowAttackGoal, reassessWeaponGoal, performRangedAttack, Arrow entity; + sun-avoidance + AvoidWolf + day/night burn if deferred).
</output>
