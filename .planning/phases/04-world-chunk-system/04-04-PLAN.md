---
phase: 04-world-chunk-system
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - level/chunk_capture_test.go
  - .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md
  - .planning/phases/04-world-chunk-system/fixtures/vanilla-superflat-chunk.bin
autonomous: false
requirements: [WORLD-02, WORLD-03, WORLD-05]
user_setup: []

must_haves:
  truths:
    - "A capture-diff test (level/chunk_capture_test.go) byte-compares Sulfur's ClientboundLevelChunkWithLight encoder output against a real vanilla 26.2 superflat chunk packet captured from temp/cache/26.2-server.jar on a fixed preset/seed/coords"
    - "The vanilla golden packet is captured and committed as fixtures/vanilla-superflat-chunk.bin so CI byte-diffs without booting Java every run; the test skips gracefully with a clear message if the fixture is absent"
    - "WORLD-CAPTURE-DIFF.md records the capture method, the chosen superflat preset/seed/coords, the per-field diff (the two section shorts incl. fluidCount, palette bytes with no length prefix + LSB longs, the heightmap set = ids 1/4/5, the four light bitsets + updates lists), and the resolution of the three Open Questions"
    - "The three Open Questions are resolved from the capture: (1) fluidCount=0 renders for a fluid-free superflat, (2) ChunkBatchStart/Finished is sent (vanilla matches), (3) the empty-light masks render correctly (bitwise-complement shortcut OR presence-based fallback if the capture mismatches)"
    - "If the capture reveals a divergence, the fix is applied to the prior plans' code (level/chunk.go section/heightmap/light or world/ packet/light) and the affected tests re-run before sign-off"
    - "An unmodified vanilla 26.2 client connects to cmd/sulfur and stands on solid, visible stone ground — no void, no stripes (the real-client visual smoke), confirmed by a human"
  artifacts:
    - path: "level/chunk_capture_test.go"
      provides: "TestSectionWireVsVanillaCapture: the authoritative WORLD-02/03 byte-diff against the vanilla golden fixture"
      contains: "func TestSectionWireVsVanillaCapture"
    - path: ".planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md"
      provides: "The vanilla 26.2 capture-diff record + the resolution of the 3 open questions + human sign-off"
      contains: "fluidCount"
    - path: ".planning/phases/04-world-chunk-system/fixtures/vanilla-superflat-chunk.bin"
      provides: "The committed golden vanilla ClientboundLevelChunkWithLight packet for the fixed superflat chunk"
      contains: ""
  key_links:
    - from: "level/chunk_capture_test.go"
      to: ".planning/phases/04-world-chunk-system/fixtures/vanilla-superflat-chunk.bin"
      via: "load the golden vanilla packet and byte-diff Sulfur's encoder output"
      pattern: "vanilla-superflat-chunk"
    - from: "WORLD-CAPTURE-DIFF.md"
      to: "the two section shorts (blockCount, fluidCount)"
      via: "the diff confirms the fluid-count short is present and aligned vs vanilla"
      pattern: "fluidCount"
---

<objective>
Seal the WORLD-02/03 wire format against the ONLY authoritative source for proto 776: a byte-for-byte capture-diff of Sulfur's `ClientboundLevelChunkWithLight` against a real vanilla 26.2 server, plus a real-client visual confirmation that an unmodified client stands on solid ground (no void/stripes). This mirrors Phase 2's 02-04 capture-diff gate (the `net.Pipe` bot tolerated bugs a real client rejected) — and here the stakes are even sharper: the fork's `Section.WriteTo`/`ReadFrom` are *symmetrically* wrong about the fluid-count short, so EVERY self-round-trip test in Plans 04-01/04-02/04-03 passes on a wire the client would reject. A self-consistent green suite proves nothing about vanilla-correctness; only the capture-diff does.

The executor FIRST does the automatable capture/diff work (stand up the cached vanilla jar, capture a superflat chunk packet, commit it as a golden fixture, byte-diff against Sulfur's encoder, write WORLD-CAPTURE-DIFF.md, resolve the 3 open questions, apply any fix to the prior plans' code and re-run), THEN PAUSES for a BLOCKING human-verify checkpoint: the user runs an unmodified vanilla 26.2 client against cmd/sulfur and confirms solid visible ground.

Purpose: WORLD-02/03 are the phase's highest-risk requirements and the entire reason the failure mode is framed as "void or stripes". No published spec covers 776 chunk bytes; the wiki documents only ≤773/775. The jar is the authoritative source, and the capture-diff is how we prove our encoder matches it. This plan is autonomous:false because the decisive check — a real client rendering solid ground — cannot be self-approved.
Output: level/chunk_capture_test.go (the golden byte-diff), the committed vanilla golden .bin, and a signed-off WORLD-CAPTURE-DIFF.md resolving the 3 open questions. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-world-chunk-system/04-RESEARCH.md
@.planning/phases/04-world-chunk-system/04-VALIDATION.md
@.planning/phases/04-world-chunk-system/04-01-PLAN.md
@.planning/phases/04-world-chunk-system/04-02-PLAN.md
@.planning/phases/04-world-chunk-system/04-03-PLAN.md
@.planning/phases/02-net-protocol-state-machine/02-04-PLAN.md

<interfaces>
<!-- VERIFIED this session. The capture-diff consumes the assembled packet from Plans 04-01..03. -->

From world (Plan 04-02 — the encoder under test):
```go
func world.WriteLevelChunkWithLight(cx, cz int32, ch *level.Chunk) (pk.Packet, error) // the bytes to diff vs vanilla
func world.NewSuperflat(secs, minY, surfaceY int) *world.Superflat
func (g *world.Superflat) Generate(pos level.ChunkPos) *level.Chunk
```

From level (the chunk codec, post-04-01 fixes):
```go
func level.EmptyChunk(secs int) *level.Chunk
func (c *level.Chunk) ReadFrom(r io.Reader) (int64, error) // to parse the vanilla golden packet body for a field-by-field diff
type level.ChunkPos [2]int32
```

From the bot harness (capture the vanilla packet client-side — VERIFIED present):
```go
// bot/world/chunks.go handleLevelChunkWithLightPacket: builds EmptyChunk(dimType.Height/16) and Scans the packet.
// Point the fork bot/ client at the VANILLA server, capture the raw ClientboundLevelChunkWithLight bytes for the spawn chunk.
// Alternatively use a logging TCP proxy between a vanilla client and the vanilla server.
```

Environment (VERIFIED available per 04-RESEARCH Environment Availability):
```
temp/cache/26.2-server.jar  (sha1 823e2250…, the real vanilla 26.2 server)
Java 25 (Zulu/Temurin)      (run the vanilla server; javap)
golang:1.26 Docker          (-race, host CGO_ENABLED=0)
```
</interfaces>

<key_facts>
<!-- VERIFIED against 04-RESEARCH this session. -->
- THE AUTHORITATIVE GATE. A Go self-round-trip CANNOT prove WORLD-02 because the fork's read/write are symmetrically wrong about the fluid-count short (confirmed jar bytecode). Only a byte-diff vs the real vanilla 26.2 server output proves the wire is vanilla-correct. This is the same lesson as Phase 2 NET-04 (the net.Pipe bot tolerated the login_finished + empty-tags bugs a real client rejected).
- Capture method (mirror Phase 2): boot temp/cache/26.2-server.jar with a FIXED superflat preset (level-type=flat or the 26.2 equivalent) and a known seed, on a known spawn so the captured chunk coords are deterministic. Capture its ClientboundLevelChunkWithLight for a specific chunk (e.g. the spawn chunk 0,0) — via the fork bot/ client pointed at vanilla (dump the raw packet bytes) OR a logging proxy. Commit the raw packet bytes as fixtures/vanilla-superflat-chunk.bin.
- Sulfur side: configure Sulfur's Superflat to match the vanilla preset AS CLOSELY AS POSSIBLE for the diffed chunk (same surface height, same block = stone/the preset's block, same biome). Generate the same chunk, run world.WriteLevelChunkWithLight, capture Sulfur's bytes. NOTE: exact byte-equality may require matching vanilla's superflat layer profile; if a 1:1 block profile match is impractical, diff the STRUCTURE (field framing: two shorts present, palette format/bits, no length prefix, heightmap set ids 1/4/5, light masks + array lengths) and confirm the per-section shorts + palette framing align even if block ids differ. The load-bearing assertions are the FRAMING (fluid-count short present, no length prefix, LSB, 3 heightmaps, light field order) — those are what cause stripes/void.
- The test TestSectionWireVsVanillaCapture: load fixtures/vanilla-superflat-chunk.bin, parse its body (Int x, Int z, then level.Chunk.ReadFrom), and assert Sulfur's encoder produces a structurally-matching packet: same heightmap entry count (3) + ids (1/4/5), each section decodes with two shorts then palette (no prefix), same number of sections, light masks/arrays present with 2048-byte arrays. If the fixture is absent, t.Skip with a message pointing at WORLD-CAPTURE-DIFF.md (so native CI stays green; the capture is the gate).
- Resolve the 3 Open Questions IN WORLD-CAPTURE-DIFF.md from the capture:
    1. fluidCount: confirm vanilla writes 0 for the fluid-free superflat section and a real client renders — Sulfur sends 0 (matches).
    2. ChunkBatchStart/Finished: confirm vanilla brackets the chunk send with batch markers — Sulfur sends them (Plan 04-03). If vanilla does NOT in this flow, note it; keeping them is still safe.
    3. Empty-light masks: compare vanilla's four bitsets (skyYMask, blockYMask, emptySkyYMask, emptyBlockYMask) against Sulfur's bitSetRev shortcut. If they match for the all-present, fully-lit superflat -> shortcut is fine (research A3). If they mismatch (dark/black chunks) -> switch to presence-based masks in level/chunk.go lightData (the fix lands here, then re-run 04-01's tests).
- If ANY divergence is found, fix it in the OWNING plan's file (level/chunk.go for section/heightmap/light framing; world/packet.go or world/generator.go for assembly/light arrays), re-run that plan's tests + this capture-diff, and record the fix in WORLD-CAPTURE-DIFF.md before sign-off.
- The real-client smoke (the human-verify): connect an unmodified vanilla 26.2 client to cmd/sulfur (which now streams the superflat ring per Plan 04-03) and confirm the player stands on solid, visible stone ground — no void (fell through), no stripes (misaligned palette). This is the decisive WORLD-02/03/05 check; it cannot be self-approved.
- ZERO new dependencies. Pure stdlib testing + the existing bot/ harness + the cached jar.
- Serena guidance: create level/chunk_capture_test.go (package level) and the fixtures/ dir + .bin. Write WORLD-CAPTURE-DIFF.md. If a fix is needed, make a precise symbol-level edit in the owning file and re-run. Read 02-04-PLAN.md's Task 3 for the exact capture-diff checkpoint shape to mirror.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sulfur chunk encoder → vanilla 26.2 client | The client silently mis-renders (stripes) or rejects (void) a chunk packet whose byte framing diverges from vanilla. The only authoritative validation is a byte-diff vs the real server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-08 | Tampering (data integrity) | A wire-framing divergence (missing fluid-count short, length prefix, wrong heightmap set, wrong light masks) that self-round-trips green but the client rejects/mis-renders | mitigate | Byte-diff against the real vanilla 26.2 server output (the golden fixture) + a real-client visual smoke — the framing assertions (two shorts, no prefix, LSB, 3 heightmaps, light field order) are exactly the divergences that cause void/stripes. Any divergence is fixed in the owning plan and re-diffed before sign-off. |
| T-4-04 | Denial of Service | Decode of the vanilla golden packet over-allocating from an attacker-controlled length | accept | The golden fixture is a trusted committed artifact captured from the real vanilla server (not attacker input); parsing it via level.Chunk.ReadFrom uses the same bounded BitStorage/palette decode (no length prefix to abuse). Low risk for a trusted test fixture. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture a real vanilla 26.2 superflat chunk + the byte-diff test (automatable prep)</name>
  <files>level/chunk_capture_test.go, .planning/phases/04-world-chunk-system/fixtures/vanilla-superflat-chunk.bin, .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md</files>
  <read_first>
    - .planning/phases/04-world-chunk-system/04-RESEARCH.md — the Validation Architecture section (capture-diff command), Pitfalls 1-7 (the exact framing fields to diff), the Open Questions (the 3 to resolve), Environment Availability (the jar/Java/Docker).
    - .planning/phases/02-net-protocol-state-machine/02-04-PLAN.md — Task 3: the proven capture-diff + human-sign-off shape to mirror.
    - bot/world/chunks.go — how the bot parses a ClientboundLevelChunkWithLight (reuse to capture the vanilla packet client-side).
    - world/packet.go (Plan 04-02) — WriteLevelChunkWithLight (the encoder under test).
  </read_first>
  <behavior>
    WORLD-02/03 (authoritative byte-diff — automatable portion):
    - Capture: boot temp/cache/26.2-server.jar on a fixed superflat preset + seed; capture the spawn-chunk ClientboundLevelChunkWithLight bytes; commit them as fixtures/vanilla-superflat-chunk.bin.
    - Test TestSectionWireVsVanillaCapture: load the golden fixture; parse Int x, Int z, then level.Chunk.ReadFrom; assert the framing — exactly 3 heightmap entries with ids {1,4,5}; each section decodes as two shorts (blockCount, fluidCount) + palette (no length prefix) + biomes; the section count matches; light masks present + 2048-byte arrays. Then generate Sulfur's chunk for the same coords via world.WriteLevelChunkWithLight and assert the SAME framing (the load-bearing diff). Skip with a clear message if the fixture is absent.
  </behavior>
  <action>
    1. Capture (document every step in WORLD-CAPTURE-DIFF.md so it is reproducible):
       - Start the vanilla server: `java -jar temp/cache/26.2-server.jar` with server.properties set to a FLAT/superflat level-type, a fixed level-seed, online-mode=false, and eula accepted, in a temp dir. Record the exact server.properties used.
       - Capture the spawn chunk's ClientboundLevelChunkWithLight: point the fork bot/ client at the vanilla server (offline login -> config -> play) and dump the raw bytes of the first ClientboundLevelChunkWithLight for chunk (0,0) (or the documented spawn chunk). Save the raw packet (id + body, or just the body — document which) to .planning/phases/04-world-chunk-system/fixtures/vanilla-superflat-chunk.bin.
    2. Write level/chunk_capture_test.go (package level): TestSectionWireVsVanillaCapture as in <behavior>. Load the fixture with os.ReadFile (t.Skip if missing). Parse + assert the vanilla framing, then assert Sulfur's encoder matches the framing. Keep the assertions on the load-bearing framing (two shorts, no prefix, LSB, 3 heightmaps, light field order + array lengths); if exact block-id parity is impractical, document that in WORLD-CAPTURE-DIFF.md and assert structure, not block bytes.
    3. Begin WORLD-CAPTURE-DIFF.md: record the capture method, the exact preset/seed/coords/server.properties, the per-field diff table (the two shorts incl. fluidCount, palette bits/format + no-prefix, the heightmap set ids, the four light bitsets + updates list lengths), and a section resolving the 3 Open Questions (fluidCount=0, batch markers, empty-light masks). If a divergence is found, FIX it in the owning plan's file and re-run that plan's tests + this test; record the fix.
  </action>
  <verify>
    <automated>go test ./level/ -run 'TestSectionWireVsVanillaCapture|TestSectionRoundTrip|TestChunkHeightmapsClientSet' -count=1 && go vet ./level/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - fixtures/vanilla-superflat-chunk.bin is captured from the real vanilla 26.2 server and committed.
    - TestSectionWireVsVanillaCapture loads the fixture and asserts Sulfur's encoder matches the vanilla framing (3 heightmaps ids 1/4/5; per-section two shorts + no-prefix palette; section count; light masks + 2048-byte arrays); it skips cleanly if the fixture is absent.
    - WORLD-CAPTURE-DIFF.md records the reproducible capture method, the per-field diff, and the resolution of the 3 Open Questions; any divergence found is fixed in the owning plan and re-diffed.
    - `go build ./...`, `go vet ./level/...` clean; zero new deps.
  </acceptance_criteria>
  <done>Sulfur's chunk + light wire is byte-diffed against a real vanilla 26.2 superflat chunk: the golden fixture is committed, the diff test asserts the vanilla framing, the 3 open questions are resolved, and any divergence is fixed — the automatable half of the authoritative WORLD-02/03 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real vanilla 26.2 client renders solid ground + human sign-off (WORLD-02/03/05 authoritative gate)</name>
  <files>.planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md</files>
  <action>
    This is a human-verification checkpoint, not an autonomous code task. The capture-diff (Task 1) proves the bytes match vanilla; this proves a real client RENDERS — the decisive WORLD-02/03/05 check that cannot be self-approved (mirrors Phase 2's NET-04 02-04 sign-off). The executor must FIRST ensure Task 1 is complete (fixture committed, diff green, 3 open questions resolved, any divergence fixed and re-diffed), finalize WORLD-CAPTURE-DIFF.md, then PAUSE and present it. Do not self-approve.

    Automatable preparation (do before pausing):
    1. Confirm Task 1's TestSectionWireVsVanillaCapture is green and WORLD-CAPTURE-DIFF.md resolves the 3 open questions with any divergence fixed + re-diffed.
    2. Run cmd/sulfur (which streams the superflat ring per Plan 04-03: SetChunkCacheCenter -> ChunkBatchStart -> N LevelChunkWithLight (center-out) -> ChunkBatchFinished). Confirm it builds and listens on proto 776, and that the off-tick worker + tick are running (-race clean per Plan 04-03).
    3. Optionally run the Docker -race gate over ./world/... ./server/... once more to confirm the full pipeline is race-clean under load.
  </action>
  <what-built>
    The full Phase 4 world subsystem: the two confirmed wire fixes (fluid-count short, 3 CLIENT heightmaps — Plan 04-01), the world/ package (deterministic superflat generator, off-tick singleflight worker, ClientboundLevelChunkWithLight assembly — Plan 04-02), and the view-distance center-out ring streaming with the off-tick rejoin (Plan 04-03). WORLD-CAPTURE-DIFF.md + fixtures/vanilla-superflat-chunk.bin are the audit artifacts proving Sulfur's chunk + light bytes match a real vanilla 26.2 server's — the only authoritative correctness check for a protocol no spec covers and that the fork's symmetric read/write hides from any self-round-trip.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/04-world-chunk-system/WORLD-CAPTURE-DIFF.md.
    2. Confirm the per-field diff shows Sulfur's section framing matches vanilla: the TWO per-section shorts (blockCount, fluidCount) are present and aligned (the fluid-count fix), the palette has NO length prefix and LSB long packing, the heightmap set is exactly ids 1/4/5, and the four light bitsets + updates lists align.
    3. Confirm the 3 Open Questions are resolved (fluidCount=0 renders; ChunkBatchStart/Finished sent; empty-light masks render correctly — shortcut or presence-based fallback).
    4. The decisive check: connect an UNMODIFIED vanilla 26.2 client to cmd/sulfur and confirm the player stands on solid, visible stone ground — NO void (does not fall through), NO stripes (no misaligned palette). Walk a few blocks; the surrounding ring is loaded (the player is not on a lone island over void).
  </how-to-verify>
  <verify>Human sign-off — the user types the resume signal after confirming the capture-diff and that a real client renders solid ground (no void/stripes).</verify>
  <done>The user has reviewed WORLD-CAPTURE-DIFF.md, confirmed Sulfur's chunk + light bytes match vanilla 26.2 (fluid-count short, no prefix, LSB, 3 heightmaps, light masks) and that an unmodified vanilla 26.2 client stands on solid, visible ground (no void/stripes), and typed "approved".</done>
  <resume-signal>Type "approved" if the capture-diff matches vanilla and a real client renders solid ground, or describe the divergence (missing/misaligned section short, length prefix present, wrong heightmap set, dark/stripe chunks from light masks, falling through void) to fix.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./level/ -run 'TestSectionWireVsVanillaCapture|TestSectionRoundTrip|TestChunkHeightmapsClientSet' -count=1` passes (the byte-diff asserts the vanilla framing; the round-trip + heightmap-set regressions stay green).
- fixtures/vanilla-superflat-chunk.bin is committed; WORLD-CAPTURE-DIFF.md records the diff + resolves the 3 open questions + is signed off.
- An unmodified vanilla 26.2 client connected to cmd/sulfur stands on solid, visible ground — no void, no stripes (human-confirmed).
- `go build ./...` clean; any divergence found in the diff was fixed in the owning plan and re-diffed; zero new dependencies.
</verification>

<success_criteria>
- WORLD-02/03 (authoritative): Sulfur's ClientboundLevelChunkWithLight bytes match a real vanilla 26.2 superflat chunk — the fluid-count short, no length prefix, LSB packing, 3 CLIENT heightmaps, and the light masks/arrays all align (the framing that causes void/stripes is correct).
- WORLD-05 (real-client): an unmodified vanilla 26.2 client renders the streamed view-distance ring and stands on solid ground — it does not fall through void.
- The 3 Open Questions are resolved from the capture and recorded; any divergence is fixed in the owning plan and re-diffed before sign-off.
- The capture-diff + real-client visual check are the authoritative gate (a self-round-trip is insufficient by design — the symmetric fluid-count bug proves it).
</success_criteria>

<output>
After completion, create `.planning/phases/04-world-chunk-system/04-04-SUMMARY.md`.
</output>
