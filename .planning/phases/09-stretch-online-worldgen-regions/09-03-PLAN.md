---
phase: 09-stretch-online-worldgen-regions
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - world/levelgen/density/df.go
  - world/levelgen/density/nodes.go
  - world/levelgen/density/spline.go
  - world/levelgen/density/parse.go
  - world/levelgen/density/density_test.go
  - world/levelgen/router.go
  - world/levelgen/router_test.go
autonomous: true
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "world/levelgen/density/ PORTS the FULL density-function NODE-TYPE set from DensityFunctions$*.class (each a small pure compute(context)→double) — NOT a T0 surface subset, but EVERY node type the WHOLE overworld.json graph uses so it parses + evaluates end-to-end. The AUTHORITATIVE used-set is the 29 types derived this session by WALKING the real jar graph (overworld.json noise_router + ALL overworld density_function/**.json), port EXACTLY these: abs, add, blend_alpha, blend_density, blend_offset, cache_2d, cache_once, clamp, cube, find_top_surface, flat_cache, half_negative, interpolated, interval_select, invert, max, min, mul, noise, old_blended_noise, quarter_negative, range_choice, shift_a, shift_b, shifted_noise, spline, square, squeeze, y_clamped_gradient — plus a bare JSON number = constant. NOTE the two corrections vs the earlier ASSUMED list: find_top_surface and invert ARE used by the overworld graph (find_top_surface IS noise_router.preliminary_surface_level, consumed by 09-07's surface rules via the above_preliminary_surface condition — it MUST parse; invert is a trivial unary -x Mapped node) and MUST be ported; weird_scaled_sampler, beardifier, and cache_all_in_cell are NOT in the overworld graph (they may be defensively ported-but-marked-UNUSED, but are NOT required and MUST NOT be claimed as load-bearing). end_islands remains the ONLY density node DEFERRED (End-only, never referenced by the overworld graph). This is the KEY to full parity: the cave functions (overworld/caves/{entrances,noodle,pillars,spaghetti_2d,...}) are just MORE NODES in the SAME graph built from noise/shifted_noise/range_choice/spline — once the full set parses + evaluates, CAVES come FREE as negative-density carved regions (caves do NOT come from weird_scaled_sampler)"
    - "world/levelgen/density/parse.go PARSES the embedded density_function/noise_settings JSON (Wave 1, Plan 09-01) into the ported node tree: a parse(json)→Function dispatches on the \"type\" field; a bare number is a constant; a string ref (minecraft:overworld/offset, minecraft:overworld/caves/entrances) resolves to another parsed density_function file (a registry/HolderHolder dedup) — the graph is DATA (parsed), the node TYPES are logic (ported); the 60KB offset.json + 34KB factor.json splines + the cave functions are PARSED, never hand-typed. A node type NOT in the supported set (only end_islands for the overworld) ERRORS LOUDLY at parse naming the type — never a silent mis-evaluation"
    - "world/levelgen/router.go PORTS Tier-D: NoiseGeneratorSettings (the parsed overworld.json struct: noise{height,min_y,size_horizontal,size_vertical}, default_block, default_fluid, sea_level, aquifers_enabled:true, ore_veins_enabled:true, noise_router, surface_rule, legacy_random_source:false), NoiseRouter (ALL the named density functions — final_density + barrier + fluid_level_floodedness + fluid_level_spread + lava + vein_toggle + vein_ridged + vein_gap + continents/depth/erosion/ridges/temperature/vegetation/preliminary_surface_level, since the Aquifer (Wave 5) needs barrier/fluid_level_*/lava and the OreVeinifier (Wave 5) needs vein_*, and 09-07's surface rules need preliminary_surface_level), and RandomState — the bridge from Tier-A seeding to the graph: it holds the per-world XoroshiroPositionalRandomFactory and instantiates every router noise node's NormalNoise from the world seed via fromHashOf(noise registry name) using the Wave-1 noise params + the Wave-2 NormalNoise; a DensityFunction visitor/mapAll binds the parsed noise nodes to their seeded NormalNoise"
    - "The DATA/LOGIC split is honored end-to-end for the WHOLE graph: NewRouter(seed) parses the FULL wired graph (DATA) — final_density (incl. the cave branches) AND barrier/fluid_level_*/lava/vein_*/preliminary_surface_level (for Waves 5 + 7) — and binds it to seeded primitives (Tier A/B) producing evaluable Functions; determinism holds (same seed → identical graph evaluation, Pitfall 7); the interpolation markers are correctly preserved for Wave-4 NoiseChunk cell sampling (Pitfall 3: interpolated is NOT a no-op). The parsed graph references ONLY supported node types — TestParseFullGraphEndToEnd parses the ENTIRE noise_router (all 15 named functions, not just final_density) so any assumed-vs-actual node gap surfaces at TEST time, not runtime; if it hits end_islands or an unported type, parse fails loudly for a future fix (T-9-07)"
  artifacts:
    - path: "world/levelgen/density/df.go"
      provides: "the Function interface (Compute(ctx)→float64 + MinValue/MaxValue) + the Context (block x,y,z) the NoiseChunk supplies + the marker-kind accessor"
      contains: "type Function interface"
    - path: "world/levelgen/density/parse.go"
      provides: "parse(json, registry)→Function dispatching on \"type\" over the FULL node set (bare number→constant; string ref→another parsed density_function; unsupported type→loud error) — the data-driven graph builder"
      contains: "func Parse"
    - path: "world/levelgen/router.go"
      provides: "NoiseGeneratorSettings + NoiseRouter (ALL named functions incl. fluid_level_*/lava/vein_*/preliminary_surface_level for Waves 5+7) + RandomState (seed→positional factory→seeded NormalNoise per node) + NewRouter binding the whole parsed graph to seeded primitives"
      contains: "RandomState"
  key_links:
    - from: "world/levelgen/density/parse.go Parse"
      to: "world/levelgen/data DensityFunction/NoiseSettings accessors (Plan 09-01)"
      via: "the parser reads the embedded JSON and resolves string refs (incl. the cave functions) to other parsed density_function files"
      pattern: "data\\."
    - from: "world/levelgen/router.go RandomState"
      to: "world/levelgen/random.go XoroshiroPositionalRandomFactory + world/levelgen/synth NormalNoise (Plan 09-02)"
      via: "each noise node is seeded fromHashOf(its registry name) and bound to a NormalNoise built from the noise params"
      pattern: "FromHashOf"
    - from: "world/levelgen/density/nodes.go noise node"
      to: "world/levelgen/synth NormalNoise (Plan 09-02)"
      via: "a noise node's Compute samples its bound NormalNoise at scaled coords"
      pattern: "NormalNoise"
---

<objective>
Port the FULL density-function NODE-TYPE set + the Tier-D router/settings loader — the LOGIC that evaluates the WHOLE DATA graph from Wave 1, seeded by the Tier-A/B primitives from Wave 2. **This is the plan where caves become "free."** Because the wired graph is DATA and the evaluator is data-driven, the cave density functions (`entrances`/`noodle`/`pillars`/`spaghetti_2d`) are just MORE NODES in the SAME graph producing the SAME `final_density` — caves are NEGATIVE-density regions the noise graph itself carves (built from the `noise`/`shifted_noise`/`range_choice`/`spline` functions already in the set), NOT a separate system and NOT from `weird_scaled_sampler`. So the ONLY incremental cost over a T0 surface subset is porting the FULL node-type set (not just the surface nodes) so the whole `overworld.json` graph parses + evaluates. Once it does, hills AND caves are both produced by `final_density`.

**The complete node-type set (AUTHORITATIVE — derived this session by WALKING the real jar graph:** `overworld.json` noise_router + ALL overworld `density_function/**.json`). Port EXACTLY these 29 types: `abs, add, blend_alpha, blend_density, blend_offset, cache_2d, cache_once, clamp, cube, find_top_surface, flat_cache, half_negative, interpolated, interval_select, invert, max, min, mul, noise, old_blended_noise, quarter_negative, range_choice, shift_a, shift_b, shifted_noise, spline, square, squeeze, y_clamped_gradient` — plus a bare JSON number = `constant`. **Two corrections vs the earlier ASSUMED list:** (1) `find_top_surface` and `invert` ARE used and were MISSING — `find_top_surface` IS `noise_router.preliminary_surface_level` (it scans down a column at cell_height:8 for the first density>0; bound by the NoiseRouter AND consumed by 09-07's surface rules via the `above_preliminary_surface` condition — it MUST parse); `invert` is a trivial unary `-x` Mapped node. (2) `weird_scaled_sampler`, `beardifier`, and `cache_all_in_cell` are NOT in the overworld graph and were INVENTED — they may be defensively ported-but-marked-UNUSED, but are NOT required. `end_islands` remains the ONLY density node DEFERRED (End-only, never referenced by the overworld graph). A node type outside the supported set ERRORS LOUDLY at parse — never a silent mis-evaluation.

This plan delivers THREE things: (1) `world/levelgen/density/` — the `Function` interface + the FULL ported node types + the `parse(json,registry)→Function` builder; (2) `world/levelgen/router.go` — `NoiseGeneratorSettings` + `NoiseRouter` (ALL the named router functions, because the Aquifer in Wave 5 needs `barrier`/`fluid_level_floodedness`/`fluid_level_spread`/`lava`, the OreVeinifier needs `vein_toggle`/`vein_ridged`/`vein_gap`, and 09-07's surface rules need `preliminary_surface_level`) + `RandomState`, the bridge from Tier-A seeding to the graph; (3) the visitor/`mapAll` that binds the parsed noise nodes to their seeded noises and preserves interpolation markers.

The DATA/LOGIC split is honored end-to-end for the WHOLE graph: `NewRouter(seed)` parses the full wired graph (DATA — incl. the cave branches) and binds it to seeded primitives (Tier A/B). Determinism holds (Pitfall 7). The `interpolated` marker is preserved (NOT a no-op — Pitfall 3). ZERO new dependencies.

Purpose: PARITY-01's terrain SHAPE — hills, valleys, oceans, AND caves/ravines-via-noise — is `final_density`; the Aquifer + OreVeinifier (Wave 5) consume the `fluid_level_*`/`lava`/`vein_*` router functions and the surface rules (Wave 7) consume `preliminary_surface_level`. Without the FULL ported node set + the bound graph there is nothing to sample, and caves would be silently missing.
Output: world/levelgen/density/{df,nodes,spline,parse}.go (+ test), world/levelgen/router.go (+ test). ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md
@.planning/phases/09-stretch-online-worldgen-regions/09-01-PLAN.md
@.planning/phases/09-stretch-online-worldgen-regions/09-02-PLAN.md

<interfaces>
<!-- VERIFIED via the jar (node-type GRAPH WALK — overworld.json noise_router + ALL overworld density_function/**.json) + the upstream plans this session. This plan consumes Wave 1 (the embedded JSON incl caves) + Wave 2 (the primitives) and produces the evaluable graph. -->

From world/levelgen/data (Plan 09-01 — the embedded graph as DATA, incl. caves):
```go
func NoiseSettings(id string) ([]byte, error)    // minecraft:overworld → noise_settings/overworld.json bytes
func DensityFunction(id string) ([]byte, error)  // minecraft:overworld/offset, minecraft:overworld/caves/entrances → file bytes
func Noise(id string) ([]byte, error)            // minecraft:temperature → noise/temperature.json bytes (firstOctave+amplitudes)
```

From world/levelgen (Plan 09-02 — the seeding + primitives):
```go
type RandomSource interface { /* NextInt/NextLong/NextDouble/Fork/ForkPositional */ }
type XoroshiroPositionalRandomFactory /* At(x,y,z) RandomSource; FromHashOf(name string) RandomSource; FromSeed(seed int64) RandomSource */
```
From world/levelgen/synth (Plan 09-02):
```go
func NewNormalNoise(rs levelgen.RandomSource, firstOctave int, amplitudes []float64) *NormalNoise
func (n *NormalNoise) GetValue(x, y, z float64) float64
func NewBlendedNoise(/* the base_3d_noise scales */) *BlendedNoise   // for the old_blended_noise node
```

The Java sources to port (javap temp/cache/26.2-inner.jar — the AUTHORITATIVE source):
```
levelgen.DensityFunction (interface)        — compute(FunctionContext)→double; minValue/maxValue; mapAll(Visitor)
levelgen.DensityFunctions ($* nested)       — Constant, Noise, ShiftedNoise, ShiftA/ShiftB, Ap2(ADD/MUL/MIN/MAX),
                                              Mapped(Abs/Square/Cube/HalfNegative/QuarterNegative/Squeeze/Invert), Clamp,
                                              RangeChoice, Spline, Marker(interpolated/flat_cache/cache_2d/cache_once),
                                              BlendedNoise wrapper, BlendAlpha/BlendOffset/BlendDensity, yClampedGradient,
                                              the interval/intervalSelect helper, the preliminary_surface_level finder (find_top_surface)
levelgen.NoiseRouter                        — final_density + barrier + fluid_level_floodedness + fluid_level_spread + lava +
                                              vein_toggle + vein_ridged + vein_gap + continents/depth/erosion/ridges/temperature/vegetation/preliminary_surface_level
levelgen.NoiseSettings                      — cell dims (height 384, min_y -64, size_horizontal 1, size_vertical 2)
levelgen.RandomState                        — the per-world PositionalRandomFactory + instantiates every router noise
synth.Spline / Spline$Coordinate / $Point   — the cubic spline offset/factor/jaggedness use
```
NOTE: the ONLY deferred density node is end_islands (End dimension only — never in the overworld graph). If Parse hits an unsupported type, it errors with a clear "unsupported density node type" message naming it. find_top_surface IS ported (it is noise_router.preliminary_surface_level — bound by the router AND consumed by 09-07's surface rules; it scans down a column at cell_height:8 for the first density>0 — port it from DensityFunctions$* via javap). invert IS ported (a trivial unary Mapped node: -x). weird_scaled_sampler / beardifier / cache_all_in_cell are NOT in the overworld graph (the earlier ASSUMED list invented them); they may be defensively ported-but-marked-UNUSED, but are NOT required and the cave rationale does NOT depend on them. blend_* nodes pass through at runtime with blending disabled (no neighbor-chunk blend) — document.
</interfaces>

<key_facts>
<!-- VERIFIED against the jar GRAPH WALK (overworld.json noise_router + ALL overworld density_function/**.json) + 09-RESEARCH (Tier C/D, Pattern 1, Pitfalls 2/3/7) this session. -->
- THE GRAPH IS DATA, THE NODES ARE LOGIC, AND CAVES ARE IN THE GRAPH (the core split, full-parity): parse the density_function/noise_settings JSON into a tree of typed nodes; each node implements Compute(ctx)→float64. The TYPES are ported logic; the TREE SHAPE is parsed data. The cave functions (entrances/noodle/pillars/spaghetti_2d/spaghetti_*) are just MORE refs final_density resolves — built from noise/shifted_noise/range_choice/spline (NOT weird_scaled_sampler) — port the FULL node set so they parse + evaluate, and caves appear as negative final_density (no separate cave code). The 60KB offset.json/34KB factor.json are splines — PARSE them.
- THE FULL NODE SET (AUTHORITATIVE — the 29 types from the jar GRAPH WALK this session: overworld.json noise_router + ALL overworld density_function/**.json): abs, add, blend_alpha, blend_density, blend_offset, cache_2d, cache_once, clamp, cube, find_top_surface, flat_cache, half_negative, interpolated, interval_select, invert, max, min, mul, noise, old_blended_noise, quarter_negative, range_choice, shift_a, shift_b, shifted_noise, spline, square, squeeze, y_clamped_gradient — plus a bare number = constant. Port each Compute constant-for-constant from DensityFunctions$*.class; cite the .class. abs/square/cube/half_negative/quarter_negative/squeeze/invert are the Mapped one-arg transforms (each a simple unary fn; invert = -x). find_top_surface is the preliminary_surface_level finder (scans down a column at cell_height:8 for the first density>0) — port it (09-07's surface rules consume preliminary_surface_level via above_preliminary_surface). interval_select picks a sub-function by an input interval.
- NOT IN THE OVERWORLD GRAPH (the earlier ASSUMED list invented them): weird_scaled_sampler, beardifier, cache_all_in_cell. They are NOT required; if ported at all, port them as defensively-ported-but-UNUSED (do NOT claim them load-bearing). CRITICALLY: caves do NOT come from weird_scaled_sampler — they come from the noise/shifted_noise/range_choice/spline cave functions (overworld/caves/{entrances,noodle,pillars,spaghetti_2d,...}) already in the set.
- ONLY end_islands DEFERRED: it is End-only and never referenced by overworld.json. Parse on an unsupported type → a clear error naming it (so a future tier knows), NOT a silent value (T-9-07). T0 had a LONG defer list (caves/aquifer/vein nodes) — for FULL parity that list collapses to just end_islands, because the cave nodes are now ported.
- THE PARSER (Pattern 1): `Parse(raw json.RawMessage, reg *Registry) (Function, error)` — a bare number → constant; an object with "type" → dispatch (build the node + recursively Parse its arguments); a STRING (minecraft:overworld/offset, minecraft:overworld/caves/entrances) → reg.Resolve(id) which lazily data.DensityFunction(id)-loads + Parse-es + caches (the HolderHolder dedup — one instance per id, avoids re-parsing shared sub-graphs). Both argument shapes (inline object vs string ref) appear in the JSON.
- INTERPOLATION MARKER IS NOT A NO-OP (Pitfall 3 — load-bearing): interpolated means "sample me on the cell grid and lerp," which Wave-4 NoiseChunk orchestrates. Model it as a Marker wrapping its argument + carrying a kind so NoiseChunk detects it at cell corners. The other cache markers (flat_cache/cache_2d/cache_once) MAY be transparent pass-throughs for correctness (real caching is a perf refinement), but interpolated MUST be preserved.
- SQUEEZE + BLEND IN final_density (verified: final_density uses min/squeeze/interpolated/blend_density): port squeeze (a Mapped) exactly; blend_density/blend_alpha/blend_offset pass through at runtime with blending OFF (no neighbor-chunk blend) — document that pass-through is correct only with blending off.
- SPLINE (Pitfall 3): port Spline + Coordinate + Point + Spline.compute (the cubic) EXACTLY — offset/factor/jaggedness are spline-heavy. Wrong spline math → smooth-but-wrong terrain.
- TIER D = A DATA PARSER + THE SEEDING BRIDGE: NoiseGeneratorSettings/NoiseRouter/NoiseSettings are PARSED structs (json.Unmarshal overworld.json — incl. aquifers_enabled:true, ore_veins_enabled:true). RandomState holds the per-world XoroshiroPositionalRandomFactory (from the world seed) and instantiates every router noise node's NormalNoise via factory.FromHashOf(noise registry name) + data.Noise(id) params + synth.NewNormalNoise. The visitor/mapAll walks the parsed graph + binds each noise node to its seeded NormalNoise + preserves interpolation markers. Carry the noise's registry NAME into its seed exactly as RandomState does (Pitfall 1).
- PARSE THE FULL ROUTER (not just final_density): Wave 5's Aquifer needs the bound barrier + fluid_level_floodedness + fluid_level_spread + lava functions; Wave 5's OreVeinifier needs vein_toggle + vein_ridged + vein_gap; Wave 7's surface rules need preliminary_surface_level. So NewRouter binds ALL the named router functions (or lazily on demand), not only final_density. Expose them on the Router for Waves 5+7. TestParseFullGraphEndToEnd parses the ENTIRE noise_router (all 15 named functions) so any assumed-vs-actual node gap surfaces at TEST time, not runtime.
- DETERMINISM (Pitfall 7): NewRouter(seed) is pure — same seed → identical bound graph → identical evaluation. No map-iteration-order in the eval path; the registry cache is deterministic.
- ZERO NEW DEPS: stdlib encoding/json + math. The primitives + seeding come from Wave 2; the JSON from Wave 1.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create world/levelgen/density/df.go (Function interface + Context + marker kind), nodes.go (the FULL ported node set incl. find_top_surface + invert), spline.go (the cubic), parse.go (the dispatching parser + the registry cache), density_test.go. Then world/levelgen/router.go (NoiseGeneratorSettings/NoiseRouter (all functions)/RandomState + NewRouter) + router_test.go. Cite the javap class per node/struct. Do NOT touch synth/ or data/ (consume them) or any other runtime file.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-trusted graph JSON → parsed node tree | The density_function/noise_settings JSON parsed here comes from the Wave-1 embedded (sha1-gated-jar-sourced) DATA — build-trusted, parsed once at NewRouter construction. No runtime client input reaches the parser. The only Generate-time input is the server-clamped chunk position fed as the node Context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-05 | Denial of Service (build-time only) | unbounded recursion parsing a malformed/deeply-nested density graph | accept | The graph is the known vanilla overworld JSON from the trusted pinned jar (Wave 1), not runtime input; its depth is bounded + the registry cache dedups shared sub-graphs (one parse per ref). Parse happens once at construction. A pathological graph cannot be supplied by a client. |
| T-9-06 | Integrity (silent parity divergence) | a noise node seeded with the wrong name/factory → a different world | mitigate | RandomState seeds each node via FromHashOf(the exact registry name) ported from the jar (Pitfall 1); the determinism test asserts same-seed→identical graph evaluation. |
| T-9-07 | Integrity (wrong terrain / missing caves) | an unsupported node type silently evaluated as a no-op, OR a node the graph genuinely uses (find_top_surface/invert) being MISSING → parse error at runtime / missing surface-level / missing caves | mitigate | Parse ERRORS on an unsupported node type naming it (not a silent pass-through); the FULL jar-walked node set (29 types incl. find_top_surface + invert + the cave functions built from noise/shifted_noise/range_choice/spline) is ported, and TestParseFullGraphEndToEnd asserts the ENTIRE noise_router (all 15 named functions) parses end-to-end (hitting only supported types) so any assumed-vs-actual gap surfaces at TEST time, not runtime — caves are present, not silently dropped. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the FULL jar-walked density-function node-type set (incl. find_top_surface + invert) + the data-driven parser (PARITY-01)</name>
  <files>world/levelgen/density/df.go, world/levelgen/density/nodes.go, world/levelgen/density/spline.go, world/levelgen/density/parse.go, world/levelgen/density/density_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Tier C (the node-type table), Pattern 1 (the node interface + parser example), Pitfall 3 (interpolation markers + spline order).
    - The jar (javap): `javap -p net.minecraft.world.level.levelgen.DensityFunction` (compute/minValue/maxValue/mapAll), `javap -p net.minecraft.world.level.levelgen.DensityFunctions` + the nested $ classes (Constant/Noise/ShiftedNoise/ShiftA/ShiftB/Ap2/Mapped[Abs/Square/Cube/HalfNegative/QuarterNegative/Squeeze/Invert]/Clamp/RangeChoice/Spline/Marker/BlendedNoise/BlendAlpha/BlendOffset/BlendDensity/yClampedGradient/intervalSelect AND the preliminary_surface_level finder for find_top_surface), `javap -p net.minecraft.world.level.levelgen.synth.Spline` (+ Coordinate/Point). Read each compute() constant-for-constant. Record divergence in the SUMMARY.
    - world/levelgen/data/embed.go (Plan 09-01) — the DensityFunction(id)/Noise(id) accessors the parser reads (incl. the cave functions).
    - world/levelgen/synth/normal.go + blended.go (Plan 09-02) — the NormalNoise/BlendedNoise the noise + old_blended_noise nodes bind to.
  </read_first>
  <behavior>
    PARITY-01 (the FULL jar-walked ported node evaluators + the parser):
    - Test 1 (TestUnaryAndArithmeticNodes): the Mapped transforms (abs/square/cube/half_negative/quarter_negative/squeeze/invert) + add/mul/min/max/clamp compose correctly over child Functions (a small hand-built tree of constants), each matching the ported math (invert = -x).
    - Test 2 (TestYGradientAndRangeChoice): y_clamped_gradient ramps fromV→toV across fromY→toY (clamped); range_choice / interval_select pick the right branch by input range.
    - Test 3 (TestSplineCubic): a spline node with known control points evaluates the cubic correctly (ported Spline.compute) — interior interpolation + clamped extrapolation.
    - Test 4 (TestParseResolvesStringRef): Parse on a small JSON with a string ref resolves it (the HolderHolder dedup — same ref parsed twice returns the cached instance); Parse on the real embedded overworld/offset.json builds without error.
    - Test 5 (TestParseCaveFunctions): Parse on the real embedded overworld/caves/entrances.json (and noodle/pillars/spaghetti_2d) builds WITHOUT error — proving the FULL node set supports the cave branches (built from noise/shifted_noise/range_choice/spline, NOT weird_scaled_sampler), so caves are in the graph (the full-parity gate).
    - Test 6 (TestInterpolatedMarkerPreserved): an interpolated node is parsed as a MARKER wrapping its argument (kind=interpolated), NOT flattened — Wave-4 NoiseChunk can detect it (Pitfall 3).
    - Test 7 (TestUnsupportedNodeErrors): Parse on an unsupported type (e.g. end_islands) returns a clear error naming it (not a silent value — T-9-07).
  </behavior>
  <action>
    1. world/levelgen/density/df.go — `type Context struct { X, Y, Z int }` and `type Function interface { Compute(c Context) float64; MinValue() float64; MaxValue() float64 }`. Add the marker-kind accessor an interpolated node exposes (so NoiseChunk detects it).
    2. world/levelgen/density/nodes.go — port the FULL jar-walked node set (constant, yClampedGradient, noise [binds a *synth.NormalNoise], shiftedNoise, shiftA/shiftB, add/mul/min/max, the Mapped unary transforms abs/square/cube/halfNegative/quarterNegative/squeeze/invert [invert = -x], clamp, rangeChoice, intervalSelect, oldBlendedNoise [binds a *synth.BlendedNoise], findTopSurface [the preliminary_surface_level finder — scans down a column at cell_height:8 for the first density>0], the markers interpolated [preserved] + the cache markers flat_cache/cache_2d/cache_once [transparent pass-through], blendAlpha/blendOffset/blendDensity [pass-through, blending off]). Each Compute ported constant-for-constant from DensityFunctions$*.class; cite the .class. MinValue/MaxValue as the bytecode computes (needed for range_choice/clamp bounds). NOTE: weird_scaled_sampler / beardifier / cache_all_in_cell are NOT in the overworld graph — do NOT include them in the required set (optionally port them as defensively-marked-UNUSED with a comment, but the parse tests must NOT depend on them).
    3. world/levelgen/density/spline.go — port Spline + Coordinate + Point + Spline.compute (the cubic). Cite the .class.
    4. world/levelgen/density/parse.go — `func Parse(raw json.RawMessage, reg *Registry) (Function, error)`: bare number → constant; object with "type" → dispatch over the FULL set (build node + recursively Parse args); string → reg.Resolve(id) (lazy data.DensityFunction(id)-load + Parse + cache). An unsupported type (only end_islands for the overworld) → a clear error naming it. The Registry caches parsed refs.
    5. world/levelgen/density/density_test.go (package density) — the 7 tests in <behavior>.
  </action>
  <verify>
    <automated>go test ./world/levelgen/density/ -run 'TestUnaryAndArithmeticNodes|TestYGradientAndRangeChoice|TestSplineCubic|TestParseResolvesStringRef|TestParseCaveFunctions|TestInterpolatedMarkerPreserved|TestUnsupportedNodeErrors' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The FULL jar-walked node-type set (29 types incl. find_top_surface + invert) is ported constant-for-constant from DensityFunctions$*.class (the Mapped unary transforms incl. invert=-x, the arithmetic/range nodes, the cubic spline + squeeze, find_top_surface for preliminary_surface_level); each cites the .class; idiomatic Go, no GPL paste. weird_scaled_sampler/beardifier/cache_all_in_cell are NOT in the required set (the parse tests do not depend on them).
    - Parse dispatches on "type", resolves string refs via the data accessors with a dedup registry, and builds the real embedded overworld/offset.json AND the cave functions (entrances/noodle/pillars/spaghetti_2d — built from noise/shifted_noise/range_choice/spline) without error — caves are in the graph.
    - interpolated is preserved as a marker (Pitfall 3); an unsupported node type (end_islands) errors clearly (T-9-07).
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The FULL jar-walked Tier-C node evaluators (incl. find_top_surface + invert + the cave nodes built from the noise/spline graph) + the data-driven parser are ported — the whole graph (hills + caves) builds from the embedded JSON (DATA) into the ported node tree (logic); Task 2 seeds + binds it via RandomState.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port Tier-D NoiseGeneratorSettings/NoiseRouter (ALL functions) + RandomState → an evaluable whole graph (PARITY-01)</name>
  <files>world/levelgen/router.go, world/levelgen/router_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Tier D (NoiseGeneratorSettings/NoiseRouter/NoiseSettings/Noises/RandomState/Visitor rows), Pattern 3 (the seed→noise chain), Pitfall 1 (carry the noise name into its seed), Pitfall 7 (determinism).
    - The jar (javap): `javap -p net.minecraft.world.level.levelgen.NoiseGeneratorSettings` (incl. aquifers_enabled/ore_veins_enabled), `javap -p net.minecraft.world.level.levelgen.NoiseRouter` (ALL the named functions incl. barrier/fluid_level_*/lava/vein_*/preliminary_surface_level), `javap -p net.minecraft.world.level.levelgen.RandomState` (the positional factory + how it instantiates each noise), `javap -p net.minecraft.world.level.levelgen.DensityFunction$Visitor` / mapAll (the graph-walk binding). Record divergence.
    - world/levelgen/random.go (Plan 09-02) — the XoroshiroPositionalRandomFactory (At/FromHashOf/FromSeed).
    - world/levelgen/synth/normal.go (Plan 09-02) — NewNormalNoise(rs, firstOctave, amplitudes).
    - world/levelgen/data/embed.go (Plan 09-01) — NoiseSettings(id) + Noise(id).
    - world/levelgen/density/parse.go (Task 1) — Parse + the Registry.
  </read_first>
  <behavior>
    PARITY-01 (Tier D — the parsed settings + the seeding bridge over the WHOLE graph):
    - Test 1 (TestParseNoiseSettings): NewRouter parses overworld.json into NoiseGeneratorSettings — sea_level==63, default_block stone, default_fluid water, noise{height:384,min_y:-64,size_horizontal:1,size_vertical:2}, legacy_random_source==false, aquifers_enabled==true, ore_veins_enabled==true.
    - Test 2 (TestRandomStateSeedsNoises): RandomState from a fixed seed instantiates each noise node's NormalNoise via FromHashOf(the registry name) + the noise params — two RandomStates from the same seed produce IDENTICAL noise samples at fixed coords (Pitfall 1+7).
    - Test 3 (TestFinalDensityEvaluable): NewRouter(seed) yields an evaluable final_density Function — Compute at fixed (x,y,z) returns finite, deterministic values (same seed+coords → identical across two NewRouter calls), and the sign varies across y (solid below, air above) AND there exist (x,y,z) where final_density is negative UNDERGROUND below the surface (the noise CAVES — a sanity that the cave branches contribute carving, not just a surface).
    - Test 4 (TestParseFullGraphEndToEnd): NewRouter parses + binds the ENTIRE noise_router — ALL 15 named functions (final_density, barrier, fluid_level_floodedness, fluid_level_spread, lava, vein_toggle, vein_ridged, vein_gap, continents, depth, erosion, ridges, temperature, vegetation, preliminary_surface_level) — each evaluable + deterministic, so any assumed-vs-actual node-type gap (a node the graph uses but the set misses) surfaces HERE at TEST time, not at runtime. The whole graph parses end-to-end using only supported node types (loud error on an unsupported type — T-9-07).
    - Test 5 (TestParsePreliminarySurfaceLevel): noise_router.preliminary_surface_level (the find_top_surface node) parses AND evaluates — it MUST parse, because 09-07's surface rules consume it via the above_preliminary_surface condition.
  </behavior>
  <action>
    1. world/levelgen/router.go — define `NoiseGeneratorSettings` (the parsed overworld.json: Noise{Height,MinY,SizeHorizontal,SizeVertical}, DefaultBlock, DefaultFluid, SeaLevel, AquifersEnabled, OreVeinsEnabled, NoiseRouter (raw per-function JSON), SurfaceRule (raw — Wave 7), LegacyRandomSource), `NoiseRouter` (ALL named functions — final_density + barrier + fluid_level_floodedness + fluid_level_spread + lava + vein_toggle + vein_ridged + vein_gap + continents/depth/erosion/ridges/temperature/vegetation/preliminary_surface_level), and `RandomState` (the per-world XoroshiroPositionalRandomFactory + a method to seed+bind a NormalNoise for a noise id via FromHashOf(name) + data.Noise(id) params + synth.NewNormalNoise). Implement `func NewRouter(seed int64) (*Router, error)`: load+Unmarshal overworld.json, build RandomState, Parse the router functions (density.Parse with a Registry), and bind each noise node to its seeded NormalNoise (the visitor/mapAll — preserve interpolation markers). Expose the bound FinalDensity + the Aquifer/OreVein functions + PreliminarySurfaceLevel + the settings (SeaLevel/dims/blocks/aquifers/ore-veins). Cite the javap sources.
    2. world/levelgen/router_test.go (package levelgen) — the 5 tests in <behavior>.
  </action>
  <verify>
    <automated>go test ./world/levelgen/ -run 'TestParseNoiseSettings|TestRandomStateSeedsNoises|TestFinalDensityEvaluable|TestParseFullGraphEndToEnd|TestParsePreliminarySurfaceLevel' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - NoiseGeneratorSettings parses overworld.json (sea_level:63, default_block:stone, default_fluid:water, the dims, legacy_random_source:false, aquifers_enabled:true, ore_veins_enabled:true).
    - RandomState seeds each noise node via FromHashOf(registry name) + Wave-1 params + Wave-2 NormalNoise — same seed → identical samples (Pitfall 1+7).
    - NewRouter binds the WHOLE parsed graph: TestParseFullGraphEndToEnd parses ALL 15 named noise_router functions — final_density (with negative-density caves underground), barrier/fluid_level_*/lava (Aquifer inputs), vein_* (OreVein inputs), preliminary_surface_level (the find_top_surface node, consumed by 09-07's surface rules) — all evaluable + deterministic; the graph parses end-to-end with only supported node types (any gap fails the test, not runtime).
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The Tier-D router/settings are parsed (DATA) and RandomState bridges the Tier-A seeding to the WHOLE graph (all 15 router functions incl. final_density+caves, the Aquifer/OreVein functions, and preliminary_surface_level) — Wave 4 samples final_density per-cell; Wave 5 consumes the aquifer/vein functions; Wave 7 consumes preliminary_surface_level.</done>
</task>

</tasks>

<verification>
- `go test ./world/levelgen/density/ -run 'TestUnaryAndArithmeticNodes|TestYGradientAndRangeChoice|TestSplineCubic|TestParseResolvesStringRef|TestParseCaveFunctions|TestInterpolatedMarkerPreserved|TestUnsupportedNodeErrors' -count=1` passes (the FULL jar-walked node set + parser, incl. find_top_surface/invert + cave functions).
- `go test ./world/levelgen/ -run 'TestParseNoiseSettings|TestRandomStateSeedsNoises|TestFinalDensityEvaluable|TestParseFullGraphEndToEnd|TestParsePreliminarySurfaceLevel' -count=1` passes (Tier D — the ENTIRE noise_router, all 15 functions, bound).
- The Wave-1/Wave-2 tests still pass.
- `go vet ./... && go build ./...` clean; ZERO new dependencies.
</verification>

<success_criteria>
- PARITY-01 (Tier C + D, FULL graph): the FULL 29-type jar-walked density-function node set is ported constant-for-constant (incl. find_top_surface for preliminary_surface_level + invert=-x + the Mapped unary transforms + the cubic spline), the parser builds the WHOLE graph from the Wave-1 embedded JSON (the 60KB+ splines + the cave functions parsed not typed), and RandomState bridges the Tier-A seeding to the graph → an evaluable, deterministic final_density (with negative-density caves) + the Aquifer/OreVein router functions + preliminary_surface_level. weird_scaled_sampler/beardifier/cache_all_in_cell are NOT required (not in the overworld graph).
- CAVES COME FREE: the cave density functions parse + evaluate as part of the same final_density (negative-density carving) — built from the noise/shifted_noise/range_choice/spline functions in the set, NOT from weird_scaled_sampler — NOT a separate system; only end_islands is deferred; an unsupported node errors loudly (no silent missing caves), and TestParseFullGraphEndToEnd parses the entire noise_router so any assumed-vs-actual gap surfaces at TEST time.
- Idiomatic Go, no GPL paste, javap-cited; zero new dependencies; this is the graph Wave 4 samples + Wave 5's Aquifer/OreVeinifier consume + Wave 7's surface rules consume (preliminary_surface_level).
</success_criteria>

<output>
After completion, create `.planning/phases/09-stretch-online-worldgen-regions/09-03-SUMMARY.md`.
</output>
</output>
