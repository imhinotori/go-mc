---
phase: 13-trees-dungeon-features-gate
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - world/feature_dungeon.go
  - world/feature_dungeon_test.go
  - world/decoration_test.go
  - cmd/sulfur/main.go
autonomous: false
requirements: [FEAT-05, FEAT-06]
user_setup: []

must_haves:
  truths:
    - "MonsterRoomFeature is ported (net.minecraft.world.level.levelgen.feature.MonsterRoomFeature.place, javap -c against temp/cache/26.2-inner.jar) as the \"monster_room\" featureBody (the type is in the recognized roster, parse.go:109, currently no-op): place carves a small cobblestone room (a hollow box sized by the jar's per-axis random extents, with a cobblestone shell + air interior + a mossy-cobblestone-speckled floor) under the surface, places a SPAWNER block at the room center (the spawner block + a mob tag/NBT marker — LOOT/mob-roll DEFERRED to v3, documented), and places 1-2 CHESTS as blocks against the walls (the chest block + a loot-table tag marker, LOOT DEFERRED). It is a FEATURE (research Tier 0: NOT a structure — no StructureStart) placed via its placed_feature's count/range modifiers, registered via registerFeatureBody. The carve/place draw order is JAR-EXACT (the determinism contract)."
    - "The dungeon writes through bctx.view.SetBlock (cross-chunk + heightmap-live), so a dungeon near a chunk edge spills its cobble shell into the neighbor (the Phase-10/11 seam); the room's validity (the jar's solid-wall / open-count check that gates whether a candidate cavern fits a room) reads the live worldgen view, and the body returns false (no partial dungeon) when the candidate position is rejected — matching MonsterRoomFeature's count-of-open-sides + floor checks."
    - "The dungeon places via its EMBEDDED placed_feature (the monster_room placed_feature with its count + height_range + in_square modifiers — UNDERGROUND_DECORATION step): the existing applyBiomeDecoration pipeline (Phase 11/12) now dispatches the live monster_room body during the real decoration pass, so a real chunk's decoration includes dungeons at the jar frequency. No selector/recursion change — just the body going live."
    - "The AUTOMATED full-features acceptance gate is green: a deterministic tree-placement test (known seed → expected trunk/foliage blocks from 13-01/02/03 still placing correctly) + the forest selector resolving to REAL trees + the dungeon placing (cobble room + spawner + chests appear) + the Phase-11 5x5 reorder-determinism (TestDecorationReorderIdentical) + emit-once gates STILL byte-identical with trees+dungeon now placing, AND the full Docker -race gate over ./world/... is clean (CGO golang:1.26). go.mod/go.sum unchanged."
    - "The server is wired to expose the full features world to a real client (cmd/sulfur/main.go): the NoiseGenerator's Decorate pass (Phase 11) with ALL feature bodies live (ores+ground-cover from Phase 12, trees+dungeon from Phase 13) generates per-biome vegetation + decoration ores into the streamed chunks, behind the UNCHANGED off-tick worker + the v1-sealed chunk wire — NO new wire surface, deterministic per the fixed worldSeed. (If main.go already streams decorated chunks from Phase 12's wiring, this is a no-op verification, NOT a re-wire — confirm + document.)"
    - "THE VISUAL GATE (FEAT-06, autonomous:false, BLOCKING — the milestone-features gate, mirroring v1's 04-04/05-03/06-07/07-06/09-09): a real unmodified vanilla 26.2 client connects to the running server and CONFIRMS the FULL vanilla per-biome vegetation set — biome-correct TREES (oak/birch in plains/forest, spruce in taiga, acacia in savanna, jungle trees in jungle, dark oak in dark forest), grass + flowers + ground cover, cactus/sugar-cane/pumpkins/mushrooms where the biome dictates, and decoration ORES underground — all generated by the ported pipeline, with the SAME seed reproducing the SAME world (human-verified visual determinism, NO capture-diff — worldgen adds no new wire surface). This is the terminal task of the phase + the features half of v2."
  artifacts:
    - path: "world/feature_dungeon.go"
      provides: "the \"monster_room\" featureBody — MonsterRoomFeature.place (cobble room + spawner + chests as blocks, loot deferred), registered via registerFeatureBody, writing through bctx.view"
      contains: "registerFeatureBody(\"monster_room\""
    - path: "world/feature_dungeon_test.go"
      provides: "deterministic dungeon placement test (seed → cobble shell + spawner-at-center + chests) + cross-chunk-edge spill + validity-rejects-no-partial"
      contains: "TestMonsterRoomPlaces"
    - path: "world/decoration_test.go"
      provides: "the full-features acceptance gate: trees place + selector→real-trees + dungeon places + 5x5 reorder determinism + emit-once with ALL bodies live"
      contains: "TestFullFeaturesAcceptance"
    - path: "cmd/sulfur/main.go"
      provides: "the server wired to stream fully-decorated chunks (trees+dungeon+ores+vegetation) to a real client behind the unchanged worker (verify/no-op if Phase 12 already wired Decorate)"
      contains: "NewNoiseGenerator"
  key_links:
    - from: "world/feature_dungeon.go monster_room body"
      to: "world/neighborhood.go SetBlock"
      via: "the cobble shell + spawner + chests write through bctx.view (cross-chunk + heightmap-live)"
      pattern: "view.SetBlock|placeState"
    - from: "world/decoration.go applyBiomeDecoration (Phase 11/12)"
      to: "world/feature_dungeon.go monster_room body"
      via: "the UNDERGROUND_DECORATION-step monster_room placed_feature now dispatches the live body (no longer no-op)"
      pattern: "lookupFeatureBody"
    - from: "cmd/sulfur/main.go"
      to: "world.NewNoiseGenerator Decorate (Phase 11, all bodies live)"
      via: "the running server streams fully-decorated chunks (trees/dungeon/ores/vegetation) to the real client for the visual gate"
      pattern: "NewNoiseGenerator"
---

<objective>
Close the FEATURES half of v2 (FEAT-05 + FEAT-06). Two automated pieces then the milestone gate:

1. **The dungeon (FEAT-05)**: port `MonsterRoomFeature.place` as the `monster_room` featureBody (the type currently no-ops). It carves a small cobblestone room + a spawner + 1-2 chests AS BLOCKS (loot/mob-roll deferred to v3), placed via its `placed_feature` (count/height_range modifiers, UNDERGROUND_DECORATION step) — a FEATURE, not a structure (research Tier 0). Once registered, the existing `applyBiomeDecoration` pipeline places dungeons at the jar frequency with no pipeline change.

2. **The automated full-features acceptance**: with the FULL tree set (13-01/02/03) + the dungeon now live, re-assert the determinism contract end-to-end — deterministic tree placement, the forest selector resolving to REAL trees, the dungeon placing, the Phase-11 5×5 reorder-determinism + emit-once STILL byte-identical, and the full Docker `-race` gate clean. This is the automated backstop the visual gate stands on.

3. **THE VISUAL GATE (FEAT-06, autonomous:false, BLOCKING)** — the terminal task + the features-milestone gate, mirroring v1's visual gates (04-04/05-03/06-07/07-06/09-09). A real vanilla 26.2 client connects to the running server and confirms the FULL per-biome vegetation set: biome-correct trees, grass/flowers/ground-cover, cactus/cane/pumpkins/mushrooms, + decoration ores underground — all generated by the ported pipeline, the same seed reproducing the same world (human-verified visual determinism; NO capture-diff — worldgen adds no new wire surface, the chunk format was sealed in v1 Phase 4).

This plan depends on 13-03 (the FULL tree set incl. special biomes must be LIVE for the visual gate to show biome-correct trees). It shares `decoration_test.go` (the acceptance gate, owned here as the LAST plan so it exercises ALL bodies) + touches `main.go` (verify the server streams decorated chunks — likely a no-op if Phase 12 already wired Decorate).

Purpose: the milestone-features gate — turn "trees + dungeon + the full feature pipeline exist and tests pass" into "a real client explores a recognizable vanilla 26.2 overworld with biome-correct vegetation + ores."
Output: feature_dungeon.go (the dungeon body) + the full-features acceptance gate + the main.go verification + the BLOCKING real-client visual checkpoint. ZERO new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/research/v2-structures.md
@.planning/phases/12-core-feature-types/12-03-SUMMARY.md
@.planning/phases/13-trees-dungeon-features-gate/13-01-PLAN.md
@.planning/phases/13-trees-dungeon-features-gate/13-02-PLAN.md
@.planning/phases/13-trees-dungeon-features-gate/13-03-PLAN.md

<interfaces>
<!-- VERIFIED on disk. Re-read 13-03-SUMMARY (the FULL tree set incl. special biomes live) + the AS-BUILT decoration_test.go / main.go BEFORE coding. -->

THE BODY SEAM (world/feature_body.go, Phase 12 — register "monster_room" from init() in feature_dungeon.go; the type is in the roster, parse.go:109, currently no-op):
```go
func registerFeatureBody(t string, b featureBody)   // panics on duplicate
type featureBody func(bctx *bodyContext, cf *feature.ConfiguredFeature, ctx placement.PlacementContext, rng levelgen.RandomSource, pos placement.BlockPos) bool
func (b *bodyContext) placeState(pos placement.BlockPos, st block.StateID) bool   // view.SetBlock + heightmap-live
func (b *bodyContext) getState(pos placement.BlockPos) block.StateID
```

THE WRITE PROXY + LIVE VIEW (world/neighborhood.go, VERIFIED): bctx.view.SetBlock/GetBlock — cross-chunk (blockStateWriteRadius=1, out-of-3x3 dropped) + heightmap-live. The cobble shell/spawner/chests write ONLY through it.

THE FINAL-GATE TESTS (world/worker_seam_test.go + world/decoration_test.go, VERIFIED 12-03):
```go
// TestDecorationReorderIdentical (5x5, two request orders -> byte-identical chunks)
// TestEmitOnce / TestEmitOnceUnderHold
// TestFeatureBodiesProduceBlocks (12-03: live applyBiomeDecoration writes N feature blocks)
// NOTE (12-03-SUMMARY): the 5x5 reorder + emit-once tests live in world/worker_seam_test.go; decoration_test.go owns the live-write proof. Re-read both for the AS-BUILT shape.
```
The Docker -race command (STATE.md / 12-03-SUMMARY): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...`.

THE SERVER WIRING (cmd/sulfur/main.go, VERIFIED 09-09): NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY) behind the UNCHANGED world.NewWorker — the Decorate pass runs as part of Generate's post-carve step (Phase 11 wired applyBiomeDecoration into NoiseGenerator.Decorate). CONFIRM whether Decorate is already invoked by the worker pipeline for streamed chunks (Phase 11-03 wired it). If yes, this plan's main.go work is a VERIFICATION (the fixed worldSeed → deterministic decorated world) + the spawn-on-surface sanity, NOT a re-wire. Do NOT touch the worker/tick/Generator interface/chunk wire (v1-sealed).

MONSTERROOM CONFIG (the embedded monster_room configured_feature + placed_feature — VERIFY on disk): the configured_feature type "minecraft:monster_room" typically carries an EMPTY config (the room geometry is hardcoded in MonsterRoomFeature.place, not data); the placed_feature lists count + height_range (uniform between two anchors) + in_square + a biome filter. So the body decodes little/no config — the geometry is the port.
</interfaces>

<key_facts>
<!-- PORT MonsterRoomFeature.place from jar bytecode (cite the class). The geometry is hardcoded in the method, NOT data. No GPL paste. Loot DEFERRED to v3 (chests/spawner are blocks + tags). -->
- MonsterRoomFeature.place (javap -c, cite net.minecraft.world.level.levelgen.feature.MonsterRoomFeature.place): pick room half-extents (xr = nextInt(?)+2-ish, zr similar — TRANSCRIBE the exact bounds + draw order), scan the candidate box for a valid cavern (count open/air sides + require a solid-enough floor — the jar's accept/reject check; on reject return false, NO partial room), carve the air interior, lay the cobblestone shell, speckle the floor with mossy cobblestone (per-cell nextInt draw), place 1-2 chests against the walls (the chest-placement loop: up to 2 attempts, each nextInt tries for a wall-adjacent spot; place chest block + a loot-table TAG marker — loot rolls DEFERRED v3), and place the spawner block at the center (+ a mob marker — the mob-type roll DEFERRED v3). The EXACT draw sequence (extents → floor speckle → chest attempts → spawner) is the determinism contract.
- The dungeon is a FEATURE not a structure (research v2-structures Tier 0, HIGH confidence: MonsterRoomFeature extends Feature, place(FeaturePlaceContext), no StructureStart). It runs in the UNDERGROUND_DECORATION features step from its placed_feature — exactly like ores. Do NOT model it as a structure / touch the (not-yet-built) structure pipeline.
- Loot/spawner-mob DEFERRAL (documented, not silently dropped — matches the STRUCT chest deferrals in REQUIREMENTS.md v3): chests place as the chest BLOCK + a loot-table tag/NBT marker; the spawner places as the spawner BLOCK + a mob marker. The actual loot generation + mob spawning is v3 (block entities / loot tables are a separate subsystem). The VISIBLE result (a cobble room with a spawner + chests) is fully delivered.
- The body writes through bctx.view (cross-chunk + heightmap-live); a dungeon near a chunk edge spills its shell into the neighbor. Validity rejection returns false (no partial dungeon).
- Registering "monster_room" makes the EXISTING applyBiomeDecoration place dungeons — NO pipeline change (the placed_feature is already loaded + sorted by FeatureSorter; only the body was no-op).
- THE VISUAL GATE is autonomous:false / checkpoint:human-verify gate="blocking" — the SAME discipline as v1's gates. Worldgen adds NO new wire surface (the chunk format was capture-diff-sealed in v1 Phase 4 — grep-confirm no encoder/packet file is touched), so the gate is VISUAL determinism, NOT a capture-diff. The Phase-4/5 wire goldens stay the authority + stay green.
- The same-seed determinism is HUMAN-verified visually (re-connect with the same worldSeed → the same trees/ores in the same spots) — backed by the automated 5x5 byte-identity test, not a capture-diff.
- ZERO new deps; the dungeon body is single-threaded/pure over (rng, view); the Docker -race gate (owned HERE, the last Phase-13 plan) proves the live decoration with ALL bodies (ores/vegetation/selectors/misc/trees/dungeon) is race-clean.
- STALE-LSP caveat: trust `go build ./...` over gopls/Serena phantom errors.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the dungeon — MonsterRoomFeature.place as the "monster_room" body</name>
  <files>world/feature_dungeon.go, world/feature_dungeon_test.go</files>
  <behavior>
    - monster_room body picks the room half-extents (jar draw order), runs the cavern-validity accept/reject check over the live view (reject → return false, no partial room), carves the air interior, lays the cobblestone shell, speckles the floor with mossy cobblestone (per-cell draw), places 1-2 chests as blocks against the walls (chest + loot tag, loot deferred), and a spawner block at the center (+ mob marker, deferred) — JAR-EXACT draw order for a seed.
    - All writes go through bctx.view.SetBlock; a dungeon at a chunk edge spills its shell into the +x/+z neighbor.
    - A rejected candidate (no valid cavern / floor) returns false with NO blocks placed.
  </behavior>
  <action>
world/feature_dungeon.go: `func init(){ registerFeatureBody("monster_room", monsterRoomBody) }`. monsterRoomBody(bctx, cf, ctx, rng, pos): port MonsterRoomFeature.place (javap -c, cite the class) — transcribe the exact half-extent draws, the open-side-count + floor accept/reject check (read via bctx.view.GetBlock), the air-carve + cobblestone shell + mossy-cobble floor speckle (per-cell nextInt), the chest loop (up to 2, wall-adjacent, chest block + loot-table tag marker — loot DEFERRED), and the center spawner block (+ mob marker — DEFERRED). Resolve cobblestone/mossy_cobblestone/chest/spawner StateIDs via block.ToStateID (or feature.ResolveBlockStateJSON). All writes via bctx.view.SetBlock. Return false on reject. Document the loot/mob v3 deferral inline.
world/feature_dungeon_test.go: TestMonsterRoomPlaces (a seed over an air-cavern-in-stone → the exact cobble shell + spawner-at-center + chest blocks, draw-pinned vs an oracle). TestMonsterRoomRejectsNoPartial (a fully-solid/invalid candidate → returns false, zero blocks written). TestMonsterRoomCrossChunkEdge (a dungeon at the chunk edge spills its shell into the neighbor).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/ -run 'TestMonsterRoom' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The monster_room body carves a jar-exact cobble room + spawner + chests (loot/mob deferred to v3, documented) through the cross-chunk Neighborhood, rejects an invalid candidate with no partial room, and is registered — the existing applyBiomeDecoration now places dungeons at the jar frequency.</done>
</task>

<task type="auto">
  <name>Task 2: the full-features automated acceptance gate (trees + dungeon + selector + 5x5 determinism + emit-once + Docker -race)</name>
  <files>world/decoration_test.go, cmd/sulfur/main.go</files>
  <action>
With ALL FEAT-03/04/05 bodies now live (ore/simple_block/random_patch/selectors/misc from Phase 12; the full tree set from 13-01/02/03; dungeon from this plan), re-assert the determinism contract under the full feature set. First re-read decoration_test.go + world/worker_seam_test.go for the AS-BUILT test shapes (12-03 owns them).
1. world/decoration_test.go: add/extend TestFullFeaturesAcceptance — run the REAL applyBiomeDecoration over representative biomes (plains/forest→oak/birch, taiga→spruce, savanna→acacia, jungle, dark_forest→dark_oak) on a stone+surface 3x3 and assert: trees place (oak_log/oak_leaves + the biome-correct logs appear), the forest random_selector resolved to REAL trees (not no-op), and a dungeon (cobblestone+spawner) appears for an UNDERGROUND_DECORATION pass at a seed/biome that includes monster_room. Confirm TestDecorationReorderIdentical (5x5) + TestEmitOnce/TestEmitOnceUnderHold STILL produce byte-identical chunks with trees+dungeon now placing (the rng-draw determinism holds end-to-end).
2. cmd/sulfur/main.go: VERIFY the server streams fully-decorated chunks — confirm NewNoiseGenerator's Decorate (Phase 11, all bodies live) runs in the worker pipeline + the fixed worldSeed makes it deterministic + the player spawns on the decorated surface (not inside a tree/dungeon). If Phase 11/12 already wired Decorate into the streamed path, this is a documented no-op verification (do NOT re-wire the worker/tick/wire). Add a spawn-sanity note if needed.
3. Run the full Docker -race gate over ./world/... to prove the live decoration with ALL bodies (incl. trees+dungeon) is race-clean.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/ -run 'TestFullFeaturesAcceptance|TestDecorationReorderIdentical|TestEmitOnce|TestFeatureBodiesProduceBlocks' -count=1 && go build ./... && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...</automated>
  </verify>
  <done>The full-features acceptance is green: trees place biome-correctly, the selector resolves to real trees, the dungeon places, the 5x5 reorder determinism + emit-once stay byte-identical with ALL bodies live, the server streams deterministic fully-decorated chunks, and Docker -race over ./world/... is clean — the automated backstop for the visual gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: THE VISUAL GATE (FEAT-06) — real vanilla 26.2 client confirms full per-biome vegetation + ores, deterministic per seed (features-milestone gate)</name>
  <what-built>
    The full features pipeline is now LIVE end-to-end behind the v1-sealed chunk wire + the unchanged off-tick worker: Phase 10's legacy-LCG + cross-chunk 3x3 seam + live worldgen heightmap → Phase 11's ConfiguredFeature/PlacedFeature model + placement modifiers + FeatureSorter + applyBiomeDecoration (the 11 GenerationStep.Decoration steps over the 3x3 biome set) → Phase 12's core bodies (OreFeature decoration-ore blobs, SimpleBlock/RandomPatch ground cover, the random/simple/boolean selectors with the placeSubFeature recursion, BlockPile/FallenTree/VegetationPatch) → Phase 13's TreeFeature (StraightTrunk+Blob oak/birch [13-01], then the common Spruce/Pine/Acacia/DarkOak/Fancy/MegaJungle-trunk + jungle_foliage placers + AlterGround/Beehive/Cocoa/vine decorators [13-02], then the special-biome cherry/mangrove[+RootPlacer]/azalea/pale_oak trees [13-03]) and the MonsterRoomFeature dungeon [this plan]. The automated gate is green: trees place biome-correctly, the forest selector resolves to real trees, the dungeon places, the 5x5 reorder-determinism + emit-once stay byte-identical with all bodies live, and the full Docker -race over ./world/... is clean. Worldgen adds NO new wire surface (the chunk format was capture-diff-sealed in v1 Phase 4), so this gate — like v1's 04-04/05-03/06-07/07-06/09-09 — is a real-client VISUAL determinism check, NOT a capture-diff. DEFERRED (documented, not dropped): dungeon loot tables + spawner mob-rolls (block entities / loot, v3); the STRUCTURES half of v2 (Phases 14-16 — temples/mineshaft/stronghold/village).
  </what-built>
  <how-to-verify>
    1. Run the server: `go run ./cmd/sulfur` (listens on :25565, proto 776; the fixed worldSeed gives a deterministic world). Ensure SULFUR_DEBUG/SULFUR_SUPERFLAT are OFF (the full noise+features gen is on).
    2. Connect an unmodified vanilla 26.2 client (PrismLauncher) to `localhost:25565`. Log in (offline mode) and let the world load; walk/explore several hundred blocks across multiple biomes.
    3. Confirm ALL of:
       - BIOME-CORRECT TREES: plains/forest grow oak (+ some birch / large oak); taiga/snowy taiga grow SPRUCE (tapered cones, podzol under some); savanna grows ACACIA (flat canopies); jungle grows tall JUNGLE trees (with cocoa pods + vines); dark forest grows DARK OAK (2x2 trunks, wide canopy); and — if you reach them — cherry_grove grows pink CHERRY trees, mangrove_swamp grows MANGROVE trees on stilt ROOTS (with hanging propagules), pale_garden grows pale-oak with PALE MOSS, lush_caves grow AZALEA trees. The trees look like real Minecraft trees (trunk + leaf blob, not floating leaves or clipped trunks), and they vary by biome — NOT all-oak-everywhere.
       - GRASS + FLOWERS + GROUND COVER: the surface has tall grass + flowers (varied flower types in flower-forest), not a bare grass slab.
       - BIOME-SPECIFIC VEGETATION: desert has cactus + dead bushes; sugar cane grows beside water; pumpkins appear occasionally; mushrooms in dark/shadowed spots + swamps; the right cover for each biome.
       - DECORATION ORES: mining underground reveals scattered coal/iron/copper/etc. ore blobs (the OreFeature decoration ores, distinct from v1's noise veins) at vanilla-ish depths.
       - THE DUNGEON: exploring underground (or digging) finds at least one cobblestone monster room with a spawner + chest(s) — the cobble shell + mossy speckle + center spawner are visible (loot/mob are v3 deferrals — the chest may be empty, the spawner inert; the ROOM is the deliverable).
       - The vegetation streams continuously across chunk edges with NO clipped trees / chunk-edge-truncated foliage (the cross-chunk seam works) and no void/holes.
    4. VISUAL DETERMINISM: note a few landmarks (a specific tree cluster, an ore exposure, the dungeon location). Disconnect, RESTART the server with the SAME worldSeed, reconnect, and confirm the SAME world regenerates — the same trees/ores/dungeon in the same spots (human-verified determinism; the automated 5x5 byte-identity test backs this).
    5. If trees are missing/wrong-per-biome / leaves float or clip at chunk edges / no flowers-grass / no biome-specific cover (cactus/cane/pumpkins/mushrooms) / no decoration ores / no dungeon / the world differs across same-seed runs — describe the failure (which symptom, which biome, where) so the gap can be closed (a --gaps replan).
  </how-to-verify>
  <resume-signal>Type "approved" if a real vanilla 26.2 client sees the full vanilla per-biome vegetation set (biome-correct trees, grass/flowers, cactus/cane/pumpkins/mushrooms, decoration ores) + at least one dungeon, generated by the ported pipeline, with the same seed reproducing the same world (FEAT-06 met — the features half of v2 is closed); otherwise describe the visual failure (missing/wrong trees / clipped foliage / no vegetation / no ores / no dungeon / non-deterministic / etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted 26.2 client → server (the visual gate) | The client connects over the v1-sealed handshake→login→config→play→movement path; this plan changes only WHICH blocks the off-tick worker's Decorate pass writes (the dungeon body + the now-complete feature set), not the wire or the network boundary. No new trust boundary — the chunk position remains the only server-clamped Generate input. |
| build-data → MonsterRoomFeature place | The dungeon geometry is hardcoded in the ported method (no data); the monster_room placed_feature (count/height_range) is the pinned 26.2 jar. The body runs off-tick on the single worker scheduler goroutine; no client input reaches it. |
| dungeon → cross-chunk Neighborhood | The cobble shell/spawner/chests write through bctx.view (blockStateWriteRadius=1); out-of-3x3 writes are dropped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | MonsterRoomFeature.place draw-order divergence (extents/floor-speckle/chest/spawner) | mitigate | The place body is transcribed javap -c + pinned by TestMonsterRoomPlaces asserting the exact cobble shell + spawner + chest positions AND the post-place rng fingerprint vs an oracle — a reorder fails the test. |
| T-13-10 | Tampering | a partial/clipped dungeon from a wrong accept/reject check | mitigate | The cavern-validity check rejects an invalid candidate (return false, zero blocks) jar-faithfully; TestMonsterRoomRejectsNoPartial pins it; the cross-chunk-edge test confirms a valid edge dungeon completes into the neighbor. |
| T-13-11 | Integrity (non-reproducible world) | the same seed producing a different vegetation/dungeon layout (visual determinism regression) | mitigate | Every body is pure over (per-feature seed, pos); the per-feature seed is the global cross-biome index (Phase 11). The automated 5x5 byte-identity + emit-once gates re-run with ALL bodies live; the visual gate human-verifies same-seed reproduction. |
| T-13-12 | Tampering | live decoration data-race with the full body set under the worker | mitigate | The Decorate pass runs single-threaded on the scheduler goroutine; all bodies are pure over (rng, Neighborhood). The Docker -race gate over ./world/... (owned here, last plan) proves it with trees+dungeon+all-bodies live. |
| T-13-13 | Information disclosure | dungeon loot/spawner exposing data | accept | Loot/mob are v3 deferrals — chests/spawner are inert blocks + tag markers; no loot table is rolled, no mob is spawned. No data surface. Documented. |
</threat_model>

<verification>
- `go test ./world/ -run 'TestMonsterRoom' -count=1` green: the dungeon carves a jar-exact cobble room + spawner + chests (loot/mob deferred), rejects invalid candidates with no partial room, spills across chunk edges.
- `go test ./world/ -run 'TestFullFeaturesAcceptance|TestDecorationReorderIdentical|TestEmitOnce|TestFeatureBodiesProduceBlocks' -count=1` green: trees place biome-correctly, the selector resolves to real trees, the dungeon places, the 5x5 reorder determinism + emit-once stay byte-identical with ALL bodies live.
- Docker `-race` over `./world/...` clean (CGO golang:1.26) — the full live decoration is race-clean.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps; NO encoder/packet/capture-diff-golden file touched (the v1 wire goldens stay green — grep-confirm).
- THE VISUAL GATE (autonomous:false) is APPROVED: a real vanilla 26.2 client sees biome-correct trees + grass/flowers + biome-specific cover (cactus/cane/pumpkins/mushrooms) + decoration ores + at least one dungeon, with the same seed reproducing the same world.
</verification>

<success_criteria>
- FEAT-05: MonsterRoomFeature (a feature, not a structure) is ported and places via its placed_feature — a cobblestone room + spawner + chests as blocks (loot/mob deferred to v3, documented), deterministic per seed, through the cross-chunk Neighborhood.
- FEAT-06 (the features-milestone VISUAL GATE, autonomous:false): a real vanilla 26.2 client exploring the world sees the FULL vanilla per-biome vegetation set — biome-correct trees (oak/birch/spruce/acacia/jungle/dark_oak), grass, flowers, cactus/cane/pumpkins/mushrooms — + decoration ores underground, generated by the ported pipeline, with the same seed reproducing the same world (human-verified visual determinism, no capture-diff — worldgen adds no new wire surface).
- The automated backstop is green: trees + dungeon place, the selector resolves to real trees, the 5x5 reorder determinism + emit-once stay byte-identical with all bodies live, Docker -race clean — the features half of v2 is closed, ready for the structures half (Phases 14-16).
</success_criteria>

<output>
After completion, create `.planning/phases/13-trees-dungeon-features-gate/13-04-SUMMARY.md`
</output>
