---
phase: 13-trees-dungeon-features-gate
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - world/levelgen/feature/tree_placers.go
  - world/levelgen/feature/tree_placers_test.go
  - world/levelgen/feature/tree_decorator.go
  - world/levelgen/feature/tree_decorator_test.go
  - world/feature_tree.go
  - world/feature_tree_test.go
autonomous: true
requirements: [FEAT-04]
user_setup: []

must_haves:
  truths:
    - "The remaining overworld trunk placers are ported jar-exact (net.minecraft.world.level.levelgen.feature.trunkplacers.*, javap -c against temp/cache/26.2-inner.jar): the subset the EMBEDDED overworld configured_feature tree configs actually reference — derived from the data like Phase 12 derived providers. At minimum ForkingTrunkPlacer (fancy oak base / swamp oak), FancyTrunkPlacer (large oak, the branch-arc math), DarkOakTrunkPlacer (the 2x2 dark oak trunk), MegaJungleTrunkPlacer + GiantTrunkPlacer (mega jungle/spruce 2x2), and the spruce/acacia trunk variants the spruce/acacia/jungle configs use. Each plugs into the 13-01 TrunkPlacer base (getTreeHeight + the setBlock/read callback seam) and returns its FoliageAttachment(s) with the JAR-EXACT draw order."
    - "The remaining overworld foliage placers are ported jar-exact (net.minecraft.world.level.levelgen.feature.foliageplacers.*): the overworld subset — SpruceFoliagePlacer (the tapered spruce cone), PineFoliagePlacer (the pine tuft), AcaciaFoliagePlacer (the flat acacia canopy), BushFoliagePlacer (oak bush / jungle bush), FancyFoliagePlacer (large oak blobs), DarkOakFoliagePlacer (the dark oak canopy), MegaPineFoliagePlacer + MegaJungleFoliagePlacer (the mega-tree foliage). Each plugs into the 13-01 FoliagePlacer base (placeLeavesRow + the per-placer shouldSkipLocation) with the JAR-EXACT row/offset/corner draw order."
    - "The tree decorators the OVERWORLD actually uses are ported (net.minecraft.world.level.levelgen.feature.treedecorators.*, the inline `decorators` array): the subset the embedded tree configs reference — AlterGroundDecorator (spruce/taiga podzol+coarse-dirt under the trunk), BeehiveDecorator (the oak/birch/cherry bee-nest with a probability draw), CocoaDecorator (jungle cocoa pods on the trunk), LeaveVineDecorator + TrunkVineDecorator (jungle vines), AttachedToLeaves/PlaceOnGround as referenced. Each TreeDecorator.place runs AFTER the trunk+foliage with the placed log/leaf position lists as input (TreeDecorator.Context: logs, leaves, roots), drawing in JAR-EXACT order. A decorator type the overworld does NOT use may be deferred (documented), but an unported decorator a real overworld config references errors loudly (never silently skipped)."
    - "The 13-01 ParseTrunkPlacer/ParseFoliagePlacer dispatch is extended to recognize every newly-ported placer (the unported-error path from 13-01 now resolves them), and a new ParseTreeDecorator parses the inline `decorators[]` entries; the 13-01 tree body iterates the real decorator list AFTER placing trunk+foliage, feeding each decorator the accumulated placed-log/placed-leaf positions (the TreeDecorator context) through the same setBlock/read callback seam — so a spruce gets podzol, an oak gets a bee nest, a jungle tree gets cocoa+vines."
    - "A decode-completeness guard test confirms EVERY embedded overworld configured_feature whose type is `tree` decodes end-to-end (ParseTreeConfiguration succeeds for all of them) — so a biome's full tree set (oak/birch/spruce/pine/jungle/acacia/dark_oak/mega variants) parses with real placers+decorators, and the body's decode-or-skip path never fires for a production overworld tree. The previously-deferred 13-01 unported-placer skip is now closed for the overworld set."
    - "Deterministic placement tests prove each new placer + decorator draws jar-exact for a known seed (the spruce cone shape, the acacia canopy, the dark-oak 2x2 trunk, the fancy-oak branches; the AlterGround podzol disk, the Beehive probability + nest placement, the Cocoa pod attachment), pinned vs an in-test oracle; and a per-biome integration smoke (taiga→spruce with podzol, savanna→acacia, jungle→jungle tree with cocoa/vines, dark_forest→dark oak) resolves through the selector recursion to real trees."
  artifacts:
    - path: "world/levelgen/feature/tree_placers.go"
      provides: "the remaining overworld trunk placers (Forking/Fancy/DarkOak/MegaJungle/Giant/spruce/acacia) + foliage placers (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/MegaPine/MegaJungle), each on the 13-01 TrunkPlacer/FoliagePlacer base, registered in ParseTrunkPlacer/ParseFoliagePlacer"
      contains: "FancyTrunkPlacer"
      min_lines: 250
    - path: "world/levelgen/feature/tree_decorator.go"
      provides: "TreeDecorator interface + the overworld decorators (AlterGround/Beehive/Cocoa/LeaveVine/TrunkVine/AttachedToLeaves/PlaceOnGround) + ParseTreeDecorator + the TreeDecorator.Context (logs/leaves/roots positions)"
      contains: "AlterGroundDecorator"
    - path: "world/feature_tree.go"
      provides: "the tree body extended to iterate the real decorator list after trunk+foliage, feeding each the accumulated placed positions"
      contains: "decorator"
    - path: "world/levelgen/feature/tree_placers_test.go"
      provides: "per-placer draw-order/geometry tests (spruce cone, acacia canopy, dark-oak 2x2, fancy-oak branches) + the all-overworld-tree-configs-decode guard"
      contains: "TestAllOverworldTreeConfigsDecode"
    - path: "world/levelgen/feature/tree_decorator_test.go"
      provides: "AlterGround/Beehive/Cocoa decorator draw-order + placement tests pinned to an oracle"
      contains: "TestAlterGroundDecorator"
  key_links:
    - from: "world/levelgen/feature/tree.go ParseTrunkPlacer/ParseFoliagePlacer (13-01)"
      to: "world/levelgen/feature/tree_placers.go the new placers"
      via: "the 13-01 dispatch's unported-error arms now resolve the overworld placer roster"
      pattern: "ParseTrunkPlacer|ParseFoliagePlacer"
    - from: "world/feature_tree.go tree body"
      to: "world/levelgen/feature/tree_decorator.go ParseTreeDecorator + TreeDecorator.place"
      via: "after placing trunk+foliage the body runs each decorator with the accumulated placed-log/placed-leaf positions"
      pattern: "TreeDecorator|ParseTreeDecorator"
    - from: "world/levelgen/feature/tree_decorator.go decorators"
      to: "world/levelgen/feature/provider.go BlockStateProvider"
      via: "the bee-nest/cocoa/podzol states resolve through the existing provider/resolveBlockState path"
      pattern: "resolveBlockState|ParseProvider"
---

<objective>
Complete the overworld tree COVERAGE (FEAT-04 criterion 1, the breadth): port the remaining trunk placers (Forking/Fancy/DarkOak/MegaJungle/Giant/spruce/acacia) + foliage placers (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/MegaPine/MegaJungle) the embedded overworld tree configs reference, plus the TreeDecorator subsystem the overworld actually uses (AlterGround podzol, Beehive bee-nests, Cocoa pods, jungle vines). After 13-01 made oak/birch render, this makes EVERY overworld biome grow its correct vanilla tree: taiga spruce (with podzol), savanna acacia, jungle jungle-trees (with cocoa+vines), dark_forest dark-oak, the mega variants.

Like Phase 12 derived the provider roster from the data, this plan derives the placer/decorator roster from the EMBEDDED tree configs — port what the overworld configured_feature `tree` entries actually use, not the full theoretical hierarchy (mangrove root_placer / cherry / pale_garden are out-of-scope deferrals if no overworld config in the embed set references them; an unported placer a real config DOES reference errors loudly and gets ported here). The decode-completeness guard (every embedded `tree` config parses end-to-end) is the objective acceptance: it closes 13-01's deferred unported-placer skip for the overworld set.

The placers extend 13-01's pure `TrunkPlacer`/`FoliagePlacer` bases (the setBlock/read callback seam — no `world` import), and the decorators run AFTER trunk+foliage via 13-01's tree body, fed the accumulated placed-log/placed-leaf positions (the `TreeDecorator.Context`). This plan depends on 13-01 (the bases + the tree body + ParseTreeConfiguration) and extends `tree.go`'s dispatch + the body's decorator loop — so it is SEQUENCED after 13-01 (wave 2), sharing `tree.go`/`feature_tree.go` (the dispatch + body live there).

Purpose: full per-biome tree coverage — every overworld biome's tree set renders with the right trunk/canopy shape + decorations, deterministic per seed.
Output: tree_placers.go (the placer roster) + tree_decorator.go (the overworld decorators) + the extended dispatch/body, with per-placer/decorator draw-order tests + the all-overworld-tree-configs-decode guard. ZERO new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/phases/13-trees-dungeon-features-gate/13-01-PLAN.md

<interfaces>
<!-- VERIFIED on disk. Re-read 13-01-SUMMARY for the AS-BUILT TrunkPlacer/FoliagePlacer base + ParseTreeConfiguration + the tree body's placement loop BEFORE coding (13-01 may have adjusted the callback signatures). -->

THE 13-01 PLACER BASES (world/levelgen/feature/tree.go, BUILT by 13-01 — the new placers plug into these):
```go
// the pure callback seam (no world import): the body supplies these
type setBlockFn func(x, y, z int, st block.StateID)
type readFn     func(x, y, z int) block.StateID
// TrunkPlacer: getTreeHeight(rng) + placeTrunk(setBlock, read, rng, freeHeight, pos, cfg) []FoliageAttachment
// FoliagePlacer: createFoliage(setBlock, read, rng, attachment, cfg) + placeLeavesRow + shouldSkipLocation
// FoliageAttachment{ Pos BlockPos; RadiusOffset int; DoubleTrunk bool }
// ParseTrunkPlacer(raw)  -> TrunkPlacer  (13-01: straight ported; others error "ported in 13-02")
// ParseFoliagePlacer(raw)-> FoliagePlacer(13-01: blob ported; others error "ported in 13-02")
// ParseTreeConfiguration(resolvedConfigRaw) -> TreeConfiguration{trunkProvider,foliageProvider,dirtProvider,trunkPlacer,foliagePlacer,minimumSize,decorators,forceDirt}
```
CONFIRM the AS-BUILT names/signatures from 13-01-SUMMARY — they are this plan's hard dependency. This plan ADDS arms to ParseTrunkPlacer/ParseFoliagePlacer (the 13-01 error arms become real cases) and adds ParseTreeDecorator + the decorator list into ParseTreeConfiguration.

THE 13-01 TREE BODY (world/feature_tree.go, BUILT by 13-01 — extended here for decorators):
```go
// func init(){ registerFeatureBody("tree", treeBody) }
// treeBody: decode TreeConfiguration -> validity scan -> getTreeHeight -> placeTrunk -> createFoliage -> iterate decorators (EMPTY in 13-01)
```
13-01's body already iterates the (empty) decorators list. This plan: (a) ACCUMULATE the placed log positions + leaf positions during placeTrunk/createFoliage (the TreeDecorator.Context inputs — vanilla TreeFeature collects logsCollector/leavesCollector Sets), (b) after foliage, run each decorator.place(ctx, rng, logs, leaves, roots, setBlock). 13-01 should already collect positions for the heightmap; CONFIRM whether the collectors exist or must be added.

THE PROVIDERS + RESOLVER (world/levelgen/feature/provider.go + blockstate.go, Phase 12 — REUSE):
```go
func ParseProvider(raw json.RawMessage) (BlockStateProvider, error)
func ResolveBlockStateJSON(raw json.RawMessage) (block.StateID, error)   // {Name,Properties} -> StateID (the bee-nest/cocoa/podzol states)
```
The decorators resolve their block states (bee_nest{honey_level,facing}, cocoa{age,facing}, podzol, coarse_dirt) through these. Cocoa/Beehive have a facing/age property set — resolve via ResolveBlockStateJSON like the ore targets did.

THE TREEDECORATOR CONTEXT (port from TreeDecorator.Context, javap -c) — the inputs each decorator.place needs:
```
TreeDecorator.place(ctx): ctx exposes the LOG positions, the LEAF positions, the ROOT positions (lists collected during placeTrunk/createFoliage), the rng, and a placer (setBlock). Each decorator iterates the relevant list + draws + places its decoration blocks.
```
Port the Context as a Go struct {logs, leaves, roots []BlockPos; rng; setBlock; read}. Cite TreeDecorator + the concrete decorators.

THE RNG (world/levelgen/random.go — VERIFIED): NextIntN / NextFloat / NextBoolean / NextInt on the threaded LegacyRandomSource. The decorators draw on the SAME threaded rng (continue the sequence AFTER the foliage draws — the determinism contract). DO NOT fork a source.
</interfaces>

<key_facts>
<!-- PORT from jar bytecode; cite each class. DERIVE the roster from the EMBEDDED overworld `tree` configs (the data is authoritative, like Phase 12's providers). No GPL paste. -->
- DERIVE THE ROSTER FIRST: enumerate every embedded configured_feature whose type is `tree` (the overworld set: oak/oak_bees, birch/birch_bees/tall_birch, spruce/pine, jungle_tree/mega_jungle_tree/jungle_bush, acacia, dark_oak, mega_spruce/mega_pine, fancy_oak/fancy_oak_bees, swamp_oak, super_birch_bees, …). Read each config's trunk_placer.type / foliage_placer.type / decorators[].type. Port EXACTLY that set. A type no embedded overworld `tree` references is a documented deferral (mangrove/cherry/pale_oak if absent from the overworld embed set).
- TrunkPlacers (javap -c, cite each): ForkingTrunkPlacer (a fork branch off the column), FancyTrunkPlacer (the large-oak branch-arc math — the most complex; the trunk + 4 diagonal branches), DarkOakTrunkPlacer (a 2x2 trunk + branches), MegaJungleTrunkPlacer / GiantTrunkPlacer (2x2 mega trunks), the spruce/acacia trunk forms (StraightTrunk reused or a bending variant). Each on the 13-01 base.
- FoliagePlacers (javap -c, cite each): SpruceFoliagePlacer (the tapered cone, trunkHeight-relative + a random foliage height draw), PineFoliagePlacer (the pine tuft, height/radius draws), AcaciaFoliagePlacer (the flat 2-layer canopy + the offset-arm leaves), BushFoliagePlacer (the small bush blob), FancyFoliagePlacer (blob-per-branch), DarkOakFoliagePlacer (the wide flat canopy), MegaPine/MegaJungleFoliagePlacer (the mega-tree caps). Each on the 13-01 FoliagePlacer base + its own shouldSkipLocation.
- TreeDecorators (javap -c, cite each — the overworld `decorators[]` set): AlterGroundDecorator (replace the dirt under the trunk with podzol/coarse_dirt in a radius — spruce/taiga; the per-position provider draw), BeehiveDecorator (probability nextFloat < p, then attach a bee_nest with bees to a trunk-side at the canopy base — the facing draw), CocoaDecorator (per upper-trunk log, nextFloat < p, attach cocoa{age,facing} on a random horizontal face), LeaveVineDecorator + TrunkVineDecorator (jungle vines on leaves/trunk sides), AttachedToLeaves/PlaceOnGround if referenced. TreeDecorator.place takes the collected log/leaf positions.
- minimum_size variants: 13-01 ported TwoLayersFeatureSize; add ThreeLayersFeatureSize (mega trees) + any other size the new configs use (getSizeAtLayer feeds the foliage radius bound).
- The DECODE-COMPLETENESS GUARD is the objective gate: TestAllOverworldTreeConfigsDecode iterates EVERY embedded `tree` configured_feature and asserts ParseTreeConfiguration succeeds — so the overworld tree set is fully covered + the body's decode-or-skip never fires in production. This is the analogue of 12-02's all-ore-configs-decode guard.
- An unported placer/decorator a real OVERWORLD config references = a BLOCKER to port HERE (loud error → port it), NOT a silent skip. 13-01's skip path remains only for genuinely-out-of-scope (Nether/cave/mangrove) configs no overworld biome references.
- ZERO new deps; single-threaded; the Docker -race gate is 13-03's. STALE-LSP: trust `go build`.
- This plan SHARES tree.go + feature_tree.go with 13-01 (the dispatch + body) — that is why it is wave 2 depends_on [13-01], NOT parallel. The new placer/decorator BODIES live in their own files (tree_placers.go / tree_decorator.go) to minimize the shared-file edit surface; only the ParseTrunkPlacer/ParseFoliagePlacer arms + the body's decorator loop touch the 13-01 files.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the remaining overworld trunk + foliage placers (derived from the embedded tree configs)</name>
  <files>world/levelgen/feature/tree_placers.go, world/levelgen/feature/tree_placers_test.go, world/levelgen/feature/tree.go</files>
  <behavior>
    - Each ported trunk placer (Forking/Fancy/DarkOak/MegaJungle/Giant/spruce/acacia variants) places its jar-exact trunk geometry for a seed and returns the correct FoliageAttachment(s), draw-pinned.
    - Each ported foliage placer (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/MegaPine/MegaJungle) places its jar-exact leaf shape (cone/tuft/flat-canopy/blob) with its own shouldSkipLocation, draw-pinned.
    - ParseTrunkPlacer/ParseFoliagePlacer now resolve every newly-ported type (13-01's error arms become real cases); ThreeLayersFeatureSize (+ any new minimum_size) parses.
    - TestAllOverworldTreeConfigsDecode: EVERY embedded `tree` configured_feature decodes end-to-end (placers + size all resolve).
  </behavior>
  <action>
FIRST derive the roster: enumerate the embedded `tree` configs (the LoadAll set) + read their trunk_placer/foliage_placer/minimum_size types. Then in tree_placers.go port (javap -c, cite each class) the exact trunk set + foliage set those configs reference, each on the 13-01 TrunkPlacer/FoliagePlacer base (the setBlock/read callback seam). FancyTrunkPlacer (the branch-arc math) is the heaviest — give it real care; transcribe the branch-count/length draws exactly. In tree.go: extend ParseTrunkPlacer + ParseFoliagePlacer with the new cases (replace the "ported in 13-02" errors) + add ThreeLayersFeatureSize. 
tree_placers_test.go: per-placer geometry+draw tests (TestSpruceFoliagePlacer cone, TestAcaciaFoliagePlacer canopy, TestDarkOakTrunkPlacer 2x2, TestFancyTrunkPlacer branches, TestMegaJungleTrunkPlacer) pinned to oracles, + TestAllOverworldTreeConfigsDecode (the completeness guard).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./world/levelgen/feature/ && go test ./world/levelgen/feature/ -run 'TestSpruceFoliage|TestPineFoliage|TestAcacia|TestDarkOak|TestFancy|TestMegaJungle|TestMegaPine|TestBushFoliage|TestForkingTrunk|TestGiantTrunk|TestAllOverworldTreeConfigsDecode' -count=1</automated>
  </verify>
  <done>The overworld trunk + foliage placer roster (derived from the embedded tree configs) is ported jar-exact + draw-pinned; ParseTrunkPlacer/ParseFoliagePlacer resolve them all; EVERY embedded `tree` config decodes (the completeness guard passes), closing 13-01's unported-placer skip for the overworld.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the overworld TreeDecorator subsystem + wire it into the tree body</name>
  <files>world/levelgen/feature/tree_decorator.go, world/levelgen/feature/tree_decorator_test.go, world/feature_tree.go, world/feature_tree_test.go</files>
  <behavior>
    - AlterGroundDecorator replaces the dirt under the trunk with podzol/coarse_dirt over its radius (the per-position provider draw), AlterGround for spruce/taiga.
    - BeehiveDecorator draws nextFloat < probability, and on success attaches a bee_nest (with bees) to a trunk-side block at the canopy base with the jar facing — draw-exact.
    - CocoaDecorator iterates the upper trunk logs, per-log nextFloat < probability, attaching cocoa{age,facing} on a random horizontal face — draw-exact.
    - LeaveVine/TrunkVine attach vines to the jungle tree's leaves/trunk sides (the referenced subset).
    - The tree body, after placing trunk+foliage, runs each parsed decorator with the ACCUMULATED placed-log/placed-leaf positions (the TreeDecorator.Context) on the SAME threaded rng — so a spruce gets podzol, an oak_bees gets a nest, a jungle tree gets cocoa+vines.
    - An unported decorator referenced by a real overworld config errors loudly (decode), not silently skipped.
  </behavior>
  <action>
tree_decorator.go: TreeDecorator interface { place(ctx *DecoratorContext) } + DecoratorContext{logs, leaves, roots []placement-pos, rng, setBlock, read}. Port (javap -c, cite each) AlterGroundDecorator / BeehiveDecorator / CocoaDecorator / LeaveVineDecorator / TrunkVineDecorator (+ AttachedToLeaves/PlaceOnGround IFF an overworld config references them — confirm from the roster). ParseTreeDecorator(raw) dispatches by "type"; the bee_nest/cocoa/podzol states resolve via ResolveBlockStateJSON / ParseProvider. An unported type errors loudly. 
feature_tree.go: extend ParseTreeConfiguration's decorators decode to call ParseTreeDecorator per entry; in the tree body, COLLECT placed log positions + leaf positions during placeTrunk/createFoliage (add collectors if 13-01 didn't), and after foliage run each decorator.place(ctx) with those collectors + the threaded rng. 
tree_decorator_test.go: TestAlterGroundDecorator (podzol disk under the trunk, draw-pinned). TestBeehiveDecorator (probability draw + nest attach + facing). TestCocoaDecorator (per-log probability + cocoa face). 
feature_tree_test.go (extend): TestSpruceWithPodzol (a taiga spruce config → spruce cone + podzol under). TestOakBeesNest (oak_bees → bee_nest placed at p=1). TestJungleCocoaVines (a jungle tree → cocoa + vines). TestPerBiomeTreeSmoke (taiga→spruce, savanna→acacia, dark_forest→dark_oak, jungle→jungle tree resolve through the selector to real decorated trees).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/levelgen/feature/ -run 'TestAlterGround|TestBeehive|TestCocoa|TestLeaveVine|TestTrunkVine' -count=1 && go test ./world/ -run 'TestSpruceWithPodzol|TestOakBeesNest|TestJungleCocoaVines|TestPerBiomeTreeSmoke' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The overworld tree decorators (AlterGround/Beehive/Cocoa/vines) place jar-exact for known seeds, the tree body runs the real decorator list with the accumulated placed positions on the threaded rng, and every overworld biome's decorated tree (spruce+podzol, oak+nest, jungle+cocoa/vines, acacia, dark_oak) resolves through the selector to a real tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → placer/decorator roster decode | The placer + decorator config (from the pinned 26.2 jar embedded tree configs) is build-time-trusted. The bodies run off-tick on the single worker scheduler goroutine; no client input reaches them. |
| decorator → cross-chunk Neighborhood | Decorators write bee-nests/cocoa/podzol through the same bctx.view (blockStateWriteRadius=1) as the trunk/foliage; out-of-3x3 writes are dropped. The placed-position collectors feed the decorators, all on the single goroutine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | placer draw-order divergence across the larger roster (fancy-oak branches, spruce cone, mega trunks) | mitigate | Each placer is transcribed javap -c + pinned by a geometry+draw-order test vs an oracle; FancyTrunkPlacer (the heaviest math) gets its own branch-count/length assertion. A reorder fails the test. |
| T-13-06 | Tampering | decorator draws happening at the wrong point in the sequence (before foliage, or wrong rng) | mitigate | Decorators run AFTER trunk+foliage on the SAME threaded rng (vanilla order); the decorator tests pin the post-foliage draw count + the placed decoration. The Beehive/Cocoa probability draws are pinned for a seed. |
| T-13-07 | Spoofing/Integrity | a silently-skipped overworld placer/decorator → a biome's tree missing or wrong | mitigate | The decode-completeness guard (TestAllOverworldTreeConfigsDecode) + a loud error on any unported type a real overworld config references — the overworld set is provably fully covered, the 13-01 skip path is closed for production trees. |
| T-13-08 | Denial of Service | a pathological placer (huge fancy-oak) writing thousands of cross-chunk blocks | accept | Tree footprints are bounded by minimum_size + the world height; writes outside the 3x3 are dropped by the proxy; the worker queue is bounded (Phases 4/8). No new control needed. |
</threat_model>

<verification>
- `go test ./world/levelgen/feature/ -run 'TestSpruceFoliage|TestPineFoliage|TestAcacia|TestDarkOak|TestFancy|TestMegaJungle|TestMegaPine|TestBushFoliage|TestForkingTrunk|TestGiantTrunk|TestAllOverworldTreeConfigsDecode|TestAlterGround|TestBeehive|TestCocoa' -count=1` green: the overworld placer/decorator roster is jar-exact + draw-pinned, every embedded `tree` config decodes.
- `go test ./world/ -run 'TestSpruceWithPodzol|TestOakBeesNest|TestJungleCocoaVines|TestPerBiomeTreeSmoke' -count=1` green: every overworld biome's decorated tree resolves through the selector to a real tree.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps; feature does NOT import placement or world (acyclic).
</verification>

<success_criteria>
- FEAT-04 (breadth of criterion 1): every overworld biome grows its correct vanilla tree set — oak/birch (13-01) + spruce/pine/jungle/acacia/dark_oak/mega variants (here), each with the right trunk/canopy shape AND the decorations the overworld uses (podzol/bee-nests/cocoa/vines), deterministic per seed.
- The overworld tree set is provably COMPLETE: every embedded `tree` configured_feature decodes end-to-end (the completeness guard), closing 13-01's deferred unported-placer skip for production trees; any unported overworld placer/decorator errors loudly (never silently dropped).
- The determinism contract holds across the full roster: each placer + decorator draw order is pinned to an oracle; trees + their decorations write through the cross-chunk Neighborhood on the threaded rng.
</success_criteria>

<output>
After completion, create `.planning/phases/13-trees-dungeon-features-gate/13-02-SUMMARY.md`
</output>
