---
phase: 35-hostiles
plan: 06
type: execute
wave: 3
depends_on: [35-01, 35-02, 35-03, 35-04, 35-05]
files_modified:
  - server/vanilla_pig_embed.go
  - server/commands_dbg.go
  - server/hostiles_embed_test.go
autonomous: false
requirements: [MOB-SUB-10, MOB-SUB-11, MOB-HOST-01, MOB-HOST-02, MOB-HOST-03]
must_haves:
  truths:
    - "all 3 hostiles boot-load into the ONE registry (the //go:embed directive + vanillaMobNames + the 3 name constants grow)"
    - "/dbg zombie | skeleton | spider spawn each hostile at the player"
    - "each repo-root/embed plugin pair is byte-identical (the embed-vs-root gate asserts all 3)"
    - "the pig oracle TestPluginPigEqualsGoNativePig stays byte-identical with all 3 hostiles bundled"
    - "Docker -race + strictRegion clean; a live client/bot sees a hostile spawn at night, hunt, and attack the player"
  artifacts:
    - path: "server/vanilla_pig_embed.go"
      provides: "the 3 hostile embeds + name constants + vanillaMobNames entries"
      contains: "vanillaZombieMobName"
    - path: "server/commands_dbg.go"
      provides: "the /dbg zombie|skeleton|spider levers"
      contains: "case \"zombie\""
    - path: "server/hostiles_embed_test.go"
      provides: "the embed-vs-root byte-identity gate for all 3 + the boot-load assertion"
      contains: "func TestHostileEmbed"
  key_links:
    - from: "server/vanilla_pig_embed.go //go:embed"
      to: "the 3 server/assets/vanilla_<hostile> dirs"
      via: "the embed directive + vanillaMobNames"
      pattern: "assets/vanilla_zombie"
    - from: "server/commands_dbg.go runDbgCommand"
      to: "t.spawnVanillaMob(vanillaZombieMobName, ...)"
      via: "the /dbg cases"
      pattern: "vanillaZombieMobName"
---

<objective>
The gate (SC#1-5, all 5 requirements) — boot-load the 3 hostiles into the ONE registry, add the /dbg levers, assert the embed-vs-root byte-identity for all 3, confirm the pig oracle stays GREEN with the hostiles bundled, and live-verify that a hostile spawns at night, hunts, and attacks the player. This is the phase's integration + acceptance gate.

Purpose: 35-01..05 built the machinery + the 3 plugins in isolation (temp-dir test harnesses). This plan WIRES them into the shipped binary's boot-load + the operator /dbg path, runs the full -race integration, and does the human live-verify. The pig oracle byte-identity is the hard gate.
Output: the embed/dbg wiring + the byte-identity gate test + the Docker -race run + the human live-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-hostiles/35-PATTERNS.md
@.planning/phases/35-hostiles/35-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- The boot-load + dbg seams the executor extends — DO NOT re-explore. -->

vanilla_pig_embed.go: the //go:embed directive (line 32, currently
"assets/vanilla_pig assets/vanilla_cow assets/vanilla_sheep assets/vanilla_chicken"); the name-constant block
(lines 38-43); vanillaMobNames load order (lines 48-53); loadVanillaMobRegistry/loadOneVanillaMob (lines 66-117,
category-agnostic — handles new mobs with ZERO change) + the LOUD re-assertion (lines 84-88). This is the EXACT
extension Plan 34-04 made adding cow/sheep/chicken.

commands_dbg.go: runDbgCommand switch (lines 13-47); the cow case (lines 20-24) is the template:
  case "cow": e := t.spawnVanillaMob(vanillaCowMobName, p.x, p.y, p.z); if e != nil { broadcast... }
the default usage string (line 45). spawnVanillaMob (vanilla_pig.go:51-62) is already name-parameterized.

The byte-identity gate: there is an embed-vs-root assertion pattern (the vanilla_pig_embed.go:29 invariant
"Keep each repo-root/embed pair byte-identical") — find the existing test asserting it for pig/cow/sheep/chicken
and mirror it for the 3 hostiles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: boot-load the 3 hostiles (embed directive + name constants + vanillaMobNames) + the /dbg levers + the embed-vs-root byte-identity gate</name>
  <read_first>
    - server/vanilla_pig_embed.go (the //go:embed line 32, name constants 38-43, vanillaMobNames 48-53, the loader 66-117)
    - server/commands_dbg.go (the cow case lines 20-24, the usage string line 45)
    - server/vanilla_pig.go (spawnVanillaMob lines 51-62, name-parameterized)
    - the existing embed-vs-root byte-identity test (grep for a test reading both plugins/vanilla_cow + server/assets/vanilla_cow and asserting bytes.Equal)
    - .planning/phases/35-hostiles/35-PATTERNS.md (the vanilla_pig_embed.go + commands_dbg.go + embed-asset entries lines 306-340, 393-401)
  </read_first>
  <behavior>
    - The 3 hostiles boot-load: loadVanillaMobRegistry returns a registry whose byName has vanilla_zombie,
      vanilla_skeleton, vanilla_spider (the LOUD re-assertion passes).
    - /dbg zombie | skeleton | spider each spawn the hostile at the player.
    - For each of the 3 hostiles, the repo-root and server/assets copies are byte-identical (bytes.Equal).
  </behavior>
  <action>
Wire the 3 hostiles into the shipped boot-load + the operator /dbg path. This is the EXACT additive extension
Plan 34-04 made for cow/sheep/chicken — clone it.

1. vanilla_pig_embed.go — extend the //go:embed directive (line 32) to add the 3 hostile asset dirs:
   //go:embed assets/vanilla_pig assets/vanilla_cow assets/vanilla_sheep assets/vanilla_chicken assets/vanilla_zombie assets/vanilla_skeleton assets/vanilla_spider
   Add the 3 name constants to the const block (after line 43):
     vanillaZombieMobName   = "vanilla_zombie"
     vanillaSkeletonMobName = "vanilla_skeleton"
     vanillaSpiderMobName   = "vanilla_spider"
   Append the 3 to vanillaMobNames (lines 48-53) so all 7 boot-load into the ONE registry. The loader +
   the LOUD re-assertion are category-agnostic — ZERO other change.

2. commands_dbg.go — add 3 cases byte-for-byte the cow shape (35-PATTERNS.md:332-338):
     case "zombie":   e := t.spawnVanillaMob(vanillaZombieMobName, p.x, p.y, p.z); if e != nil { broadcast "spawned zombie eid=..." }
     case "skeleton": ... vanillaSkeletonMobName ...
     case "spider":   ... vanillaSpiderMobName ...
   Update the default usage string (line 45) to list zombie | skeleton | spider.

3. hostiles_embed_test.go (NEW) — the embed-vs-root byte-identity gate. For EACH of the 3 hostiles, read
   plugins/vanilla_<mob>/{main.star,plugin.toml} + server/assets/vanilla_<mob>/{main.star,plugin.toml} and
   assert bytes.Equal (mirror the existing pig/cow embed-vs-root test). PLUS TestHostilesBootLoad: call
   loadVanillaMobRegistry and assert byName has all 3 hostile names (the LOUD assertion path).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./server/... && go test ./server/ -run "TestHostileEmbedByteIdentical|TestHostilesBootLoad" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "assets/vanilla_zombie" server/vanilla_pig_embed.go` AND `grep -n "assets/vanilla_skeleton" server/vanilla_pig_embed.go` AND `grep -n "assets/vanilla_spider" server/vanilla_pig_embed.go` all return (the embed directive).
    - `grep -n "vanillaZombieMobName" server/vanilla_pig_embed.go` returns AND the 3 are in vanillaMobNames.
    - `grep -n "case \"zombie\"" server/commands_dbg.go` AND `grep -n "case \"skeleton\"" server/commands_dbg.go` AND `grep -n "case \"spider\"" server/commands_dbg.go` all return.
    - `cd D:/ender && go test ./server/ -run "TestHostileEmbedByteIdentical|TestHostilesBootLoad" -count=1` exits 0.
  </acceptance_criteria>
  <done>All 3 hostiles boot-load into the one registry; /dbg spawns each; the embed-vs-root byte-identity gate passes for all 3.</done>
</task>

<task type="auto">
  <name>Task 2: the full integration gate — pig oracle byte-identity + the whole hostile suite + Docker -race + strictRegion</name>
  <read_first>
    - server/vanilla_pig_embed.go + the loaded registry (the pig is still first; the hostiles are additive)
    - the Docker -race + strictRegion run recipe (grep the prior phase SUMMARYs / a Makefile / a docker compose for the exact command used in 34's gate)
    - .planning/STATE.md (the CARRYOVER flaky test note — TestBehaviorRegressionMobSpawns + TestServerAiStepWalksToGoalTarget; do NOT let a known flake mask a real regression)
  </read_first>
  <behavior>
    - TestPluginPigEqualsGoNativePig passes byte-identical with all 3 hostiles bundled (the hostiles are
      separate mobs; no draw touches the pig stream).
    - The whole hostile test suite (zombie/skeleton/spider + the target/attack/spawn tests) passes.
    - Docker -race over the server packages is clean; strictRegion is clean.
  </behavior>
  <action>
Run the full integration gate. The pig oracle is THE hard gate.

1. Run the pig oracle: `go test ./server/ -run TestPluginPigEqualsGoNativePig -count=1` — MUST be byte-identical
   (the hostiles are separate mobs, so no new draw touches the pig path). If it moves, STOP and find the leak
   (a draw added to a shared path) — do NOT proceed.

2. Run the whole Phase-35 suite: the zombie/skeleton/spider tests, the target/attack/leap/daylight tests, the
   spawn-gating tests, the embed gate — all green.

3. Run Docker -race + strictRegion using the SAME recipe the prior phase gates used (find it in the 34 gate
   SUMMARY / Makefile / compose). Distinguish the KNOWN CARRYOVER flakes (TestBehaviorRegressionMobSpawns,
   TestServerAiStepWalksToGoalTarget — STATE.md:19-28) from any NEW -race failure introduced by Phase 35. A
   known flake does not block; a new data race in the target/attack/spawn code DOES — fix it (the hostile RNG
   + bookkeeping must be tick-owned, no cross-goroutine access).

4. Confirm CGO_ENABLED=0 build is clean + no new Go dependency was added (CGO_ENABLED=0 go build; the
   go.mod/go.sum diff stays empty).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./... && go test ./server/ -run "TestPluginPigEqualsGoNativePig|TestZombie|TestSkeleton|TestSpider|TestMonster|TestNearestAttackableTarget|TestHurtByTarget|TestMeleeAttack|TestLeap|TestHostile" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd D:/ender && go test ./server/ -run TestPluginPigEqualsGoNativePig -count=1` exits 0 (byte-identical with the hostiles bundled).
    - `cd D:/ender && go test ./server/ -run "TestZombie|TestSkeleton|TestSpider|TestMonster|TestNearestAttackableTarget|TestHurtByTarget|TestMeleeAttack|TestLeap" -count=1` exits 0.
    - `cd D:/ender && CGO_ENABLED=0 go build ./...` exits 0 AND `git diff --stat go.mod go.sum` produces EMPTY output (no new dep).
    - The Docker -race + strictRegion run is clean except for the documented STATE.md carryover flakes (record any flake hit in the SUMMARY; a NEW race fails the gate).
  </acceptance_criteria>
  <done>The pig oracle is byte-identical; the whole hostile suite + the embed gate pass; CGO=0 build clean with no new dep; -race clean modulo the documented carryover flakes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The 3 hostiles (zombie/skeleton/spider) boot-load into the shipped binary, spawn naturally at night under
    the MONSTER cap (the gametime-darkness proxy), hunt the player (NearestAttackableTargetGoal), path to them,
    and deal melee damage through the Phase-29 keystone. /dbg zombie|skeleton|spider spawn them on demand.
  </what-built>
  <how-to-verify>
    1. Build + run the server (the same run recipe as the prior phase live-verifies; e.g.
       `CGO_ENABLED=0 go build -o sulfur ./cmd/sulfur && ./sulfur -seed 777`), join with a real vanilla 26.2
       client (or the testbot) on localhost:25565.
    2. /dbg zombie — confirm a zombie spawns at you, turns toward you, walks to you, and damages you (health
       drops). Repeat /dbg skeleton and /dbg spider (the spider should leap).
    3. Advance/observe gametime into the night window (or use any time command if one exists) and confirm
       hostiles spawn NATURALLY at night and do NOT spawn in daytime (the FORCED gametime-darkness proxy).
       Confirm the world is NOT flooded (the MONSTER cap holds).
    4. Confirm the spider does NOT melee in daylight (the daylight aggression gate) but does at night.
    5. Confirm the pig/cow/sheep/chicken are unchanged (still passive, no hunting).
  </how-to-verify>
  <resume-signal>Type "approved" if hostiles spawn at night, hunt, and attack (and the spider's daylight gate + the cap hold); otherwise describe what's wrong (e.g. "spawns in daytime", "no melee damage", "world flooded").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded plugin -> boot-load swap | the embedded vanilla_<hostile> manifest governs the swap's caps (T-24-07) |
| /dbg operator command -> spawn | an operator-gated lever spawns mobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Tampering | the embed-vs-root pair | mitigate | the byte-identity gate (Task 1) asserts the embedded copy == the repo-root copy; a swapped-on-disk plugins/ copy cannot widen caps (the embedded manifest governs, vanilla_pig_embed.go:28). |
| T-35-13 | Elevation | /dbg zombie|skeleton|spider | mitigate | runDbgCommand is operator-gated by the same command.tp permission (commands_dbg.go:11-12) — not a public lever; identical to the existing /dbg cow gate. |
| T-35-14 | DoS | natural hostile spawn at night | accept | the MONSTER cap (35-02) + the cross-region apply-time re-check bound the population; the live-verify confirms no flood. |
</threat_model>

<verification>
- All 3 hostiles boot-load; /dbg spawns each; the embed-vs-root gate passes for all 3.
- The pig oracle byte-identical; the whole hostile suite green; CGO=0 + no new dep; -race clean modulo documented flakes.
- The human live-verify confirms spawn-at-night + hunt + attack + the spider daylight gate + the cap.
</verification>

<success_criteria>
- SC#1-5 all satisfied: the targetSelector machinery, the 3 hostiles hunting + meleeing, the MONSTER cap, the FORCED gametime proxy, the pig oracle GREEN, -race clean.
- MOB-SUB-10, MOB-SUB-11, MOB-HOST-01, MOB-HOST-02, MOB-HOST-03 all closed.
</success_criteria>

<output>
After completion, create `.planning/phases/35-hostiles/35-06-SUMMARY.md` recording: the live-verify outcome, the FORCED gametime-darkness proxy deferral (full light-propagation cave/block-light spawning is the follow-up), the skeleton ranged-bow deferral, the per-hostile cite-deferred goals, and any -race carryover flake hit. Update STATE.md + ROADMAP.md phase 35 to complete.
</output>
