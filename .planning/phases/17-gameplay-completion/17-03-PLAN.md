---
phase: 17-gameplay-completion
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/subtick.go
  - server/attack_dispatch.go
  - server/fall_damage.go
  - server/attack_dispatch_test.go
  - server/fall_damage_test.go
autonomous: true
requirements: [GAMEPLAY-04]
must_haves:
  truths:
    - "applyInput has a ServerboundInteract(ATTACK) case that resolves the target entity id to a tickPlayer and calls applyDamage"
    - "A forged/out-of-reach/nil attack target is a silent no-op (no panic, no damage)"
    - "The attack packet only NAMES a target; the server supplies the damage amount (client never sets damage)"
    - "A player accumulates fall distance while airborne and takes floor(fallDistance-3.0) damage on landing"
    - "Lethal damage drives die(); a respawn request after death calls performRespawn"
  artifacts:
    - path: "server/attack_dispatch.go"
      provides: "ServerboundInteract decode + ATTACK action + entity-id->tickPlayer target resolution + reach gate + applyDamage call"
      min_lines: 40
    - path: "server/fall_damage.go"
      provides: "per-player fallDistance accumulation + landing-edge damage (Entity.checkFallDamage port) — OVERWRITES the 17-01 stub"
      min_lines: 35
  key_links:
    - from: "server/subtick.go applyInput"
      to: "t.applyDamage(victim, amount)"
      via: "ServerboundInteract ATTACK case"
      pattern: "ServerboundInteract|applyDamage"
    - from: "server/fall_damage.go tickFallDamage"
      to: "t.applyDamage(p, fallDmg)"
      via: "landing edge"
      pattern: "tickFallDamage"
---

<objective>
GAMEPLAY-04: wire damage dispatch + fall damage. `applyDamage`/`die`/`performRespawn`/`setHealth` are complete and tested but only reachable via SULFUR_DEBUG_DAMAGE. The attack packets already route to the subtick buffer (tick.go:771-772) but `applyInput` has no case for them — they hit the `default:` no-op. Add the real cases + an environmental fall-damage tick.

Purpose: Attacks deal no damage; there is no fall/environmental damage. Both are pure dispatch into existing, tested combat logic — plus the fall-damage accumulator.
Output: A PvP attack resolves the target and damages it (death + respawn already work); falling deals floor(fallDistance-3.0) damage on landing.

DEPENDS ON 17-01: this plan consumes the `lookupPlayerByEntityID` reverse lookup (server/player_visibility.go) and the tickPlayer fields `fallDistance`, `wasOnGround`, `lastY` — all declared by 17-01. It OVERWRITES the 17-01-created `server/fall_damage.go` stub (the tickFallDamage call site is already wired in tickEntities by 17-01). This plan touches NO shared tick-pipeline file (no tick.go, no tick_phases.go) — its files (subtick.go, attack_dispatch.go, fall_damage.go) are disjoint from 17-02 (fluid*.go) and 17-04 (block_drop.go/block_interact.go/entity_encode.go), so all three Wave-2 plans run fully parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gameplay-completion/17-RESEARCH.md
@.planning/phases/17-gameplay-completion/17-01-SUMMARY.md

<interfaces>
<!-- Verified live this planning session. -->

combat (server/combat.go) — complete + tested, just uncalled outside debug:
  func (t *TickLoop) applyDamage(p *tickPlayer, amount float32)  // lowers health, sends SetHealth, calls die() if lethal
  func (t *TickLoop) die(p *tickPlayer)                          // sends PlayerCombatKill, sets p.dead
  func (t *TickLoop) performRespawn(p *tickPlayer)               // full respawn flow (already wired to ServerboundClientCommand)

applyInput (server/subtick.go:116-206): the switch. Cases for MovePlayer*/PlayerAction/UseItemOn/Container*; the `default:` (line 203) is the no-op the Attack/Interact packets currently fall into. The teleport gate (`if !p.confirmedTeleport { return }`) is at :112 — add the new cases AFTER it.

dispatch (server/tick.go:760-798): ALREADY routes packetid.ServerboundAttack AND packetid.ServerboundInteract into the subtick buffer (lines 771-772) — so the packets ARRIVE at applyInput; they are just dropped at default.

reverse lookup (server/player_visibility.go, from 17-01):
  func (t *TickLoop) lookupPlayerByEntityID(id int32) *tickPlayer  // scans t.players, nil if not found

tickPlayer (server/tick.go, fields declared by 17-01): `fallDistance float64`, `wasOnGround bool`, `lastY float64` (this plan USES them — no tick.go edit), plus x/y/z/onGround/entityID/dead.

server/fall_damage.go: a 17-01 STUB (`func (t *TickLoop) tickFallDamage() {}`) that tickEntities (server/tick_phases.go, 17-01-owned) already calls. THIS plan OVERWRITES fall_damage.go with the real body. It does NOT edit tick_phases.go — the call site is already there. (17-02 overwrites a DIFFERENT file, fluid.go, so the two Wave-2 overwrites are disjoint.)

packetids: packetid.ServerboundInteract (255), packetid.ServerboundAttack (230) — BOTH exist (data/packetid/packetid.go). javap confirms WHICH carries the entity-attack (the 26.2 entity-interact packet is ServerboundInteractPacket = VarInt entityId + Action enum {INTERACT,ATTACK,INTERACT_AT} + optional + Boolean usingSecondaryAction).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ServerboundInteract(ATTACK) dispatch — decode, resolve target, reach-gate, applyDamage</name>
  <files>server/attack_dispatch.go, server/subtick.go, server/attack_dispatch_test.go</files>
  <read_first>
    - server/subtick.go:108-206 (the teleport gate + the switch + the default no-op — add the new case after the gate)
    - server/combat.go:64-99 (applyDamage + die — the call target; client never supplies damage, T-6-05)
    - server/player_visibility.go (lookupPlayerByEntityID from 17-01)
    - server/block_interact.go:102-105 (the existing `withinReach` reach-gate helper handlePlayerAction uses — reuse its distance approach for withinAttackReach)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 4 (the ServerboundInteract decode + ATTACK action + reverse-lookup + baseAttackDamage=1.0) + Pitfall 6 (entity-id vs tickPlayer; nil guard; reach) + Security Domain (V4/V5: server-authoritative, defensive decode)
    - javap (CITE the class — confirm WHICH packet id carries the entity attack and the exact field order):
      `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.network.protocol.game.ServerboundInteractPacket` → confirm: VarInt entityId, Action enum (0=INTERACT,1=ATTACK,2=INTERACT_AT), optional fields, Boolean usingSecondaryAction. Cite it.
  </read_first>
  <behavior>
    - TestAttackDispatchDamages: player A attacks player B (in reach) via a synthesized ServerboundInteract(ATTACK, B.entityID) -> B.health drops by baseAttackDamage(1.0) and SetHealth was sent.
    - TestAttackOutOfReach: A attacks B beyond reach -> no damage (silent no-op).
    - TestAttackForgedTarget: ATTACK with an unknown entity id -> nil victim, no panic, no damage.
    - TestAttackNonAttackAction: ServerboundInteract with action=INTERACT(0) -> no damage (only ATTACK damages).
    - TestAttackLethalDeath: B at 1 HP attacked -> health 0, die() called (p.dead true).
    - TestAttackSelf: A attacks its own entityID -> no damage (self-attack guard).
  </behavior>
  <action>
    1. server/attack_dispatch.go: `func (t *TickLoop) handleInteract(p *tickPlayer, pkt pk.Packet)`. Decode defensively (Scan-error → silent no-op, T-6-04): VarInt targetID, VarInt action (jar enum order), and the trailing fields per the javap'd ServerboundInteractPacket. Define `const interactActionAttack = 1`. If `int(action) != interactActionAttack` return. Resolve `victim := t.lookupPlayerByEntityID(int32(targetID))`; if `victim == nil || victim == p` return (no self-attack). Reach-gate: `if !t.withinAttackReach(p, victim) { return }` — add `withinAttackReach`: Euclidean distance between (p.x,p.y,p.z) and (victim.x,victim.y,victim.z) <= a const `attackReach` (vanilla creative-survival attack reach ~3.0 blocks + a small slack; cite the value). Call `t.applyDamage(victim, baseAttackDamage)` with `const baseAttackDamage float32 = 1.0` (bare-hand, cite the jar GENERIC_ATTACK_DAMAGE base). The server supplies the amount — the packet only NAMES the target (T-6-05 / V4).
    2. server/subtick.go: in applyInput, AFTER the teleport gate, add `case packetid.ServerboundInteract:` → `t.handleInteract(p, in.Packet)`. ALSO handle `case packetid.ServerboundAttack:` per what javap confirms: in 26.2 the entity-attack is carried by ServerboundInteractPacket (Action=ATTACK); if ServerboundAttack is a distinct legacy/unused id, route it to the same handler OR document it as not-the-attack-packet. Do NOT guess — the javap output decides; record the decision in a comment + the SUMMARY.
    Turn the six attack tests GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestAttack' -x 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - server/subtick.go contains `case packetid.ServerboundInteract:` calling a handler (the packet no longer hits the default no-op)
    - server/attack_dispatch.go contains `handleInteract` with a defensive Scan, an ATTACK-action guard, `lookupPlayerByEntityID`, a nil/self guard, `withinAttackReach`, and `t.applyDamage(victim, baseAttackDamage)` where `baseAttackDamage` is a server const (NOT client-supplied), with a javap citation naming `ServerboundInteractPacket`
    - all six TestAttack* tests pass
  </acceptance_criteria>
  <done>A PvP attack resolves the named target, reach-gates it, and applies server-authoritative damage into the existing applyDamage→die flow; forged/out-of-reach/self/non-attack inputs are silent no-ops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fall damage — fallDistance accumulation + landing-edge damage (OVERWRITE the 17-01 stub)</name>
  <files>server/fall_damage.go, server/fall_damage_test.go</files>
  <read_first>
    - server/fall_damage.go (the 17-01 STUB this task OVERWRITES — `func (t *TickLoop) tickFallDamage() {}`; the call site in tickEntities stays)
    - server/tick.go (the `fallDistance float64`, `wasOnGround bool`, `lastY float64` fields 17-01 declared on tickPlayer; the y/onGround fields) — confirm via 17-01-SUMMARY they exist
    - server/combat.go:64-76 (applyDamage — the call target)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 4 fall-damage paragraph (floor(fallDistance-3.0) half-hearts on landing; accumulate -vy while airborne, reset+apply on landing) + Assumption A2 (fall damage is the minimum env damage for Phase 17)
    - javap (CITE — port the vanilla fall formula): `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.entity.Entity` → checkFallDamage / `... net.minecraft.world.entity.LivingEntity` → causeFallDamage (confirm the 3.0 threshold + 1 damage per block past it).
  </read_first>
  <behavior>
    - TestFallDamageAccumulates: a player descending (y decreasing while !onGround) accumulates fallDistance == total descent.
    - TestFallDamageOnLanding: a player that fell 10 blocks then lands (onGround false→true) takes floor(10-3.0)=7 damage via applyDamage, and fallDistance resets to 0.
    - TestSmallFallNoDamage: a 2-block fall (< 3.0 threshold) deals 0 damage on landing.
    - TestStayGroundedNoDamage: a player on the ground every tick accumulates 0 and takes 0.
  </behavior>
  <action>
    OVERWRITE server/fall_damage.go (replacing the 17-01 stub) with the real body (cite the vanilla `Entity.checkFallDamage` / `LivingEntity.causeFallDamage` port — idiomatic Go, no GPL paste). Use ONLY the 17-01-declared tickPlayer fields `fallDistance`, `wasOnGround`, `lastY` — NO tick.go edit:

    `func (t *TickLoop) tickFallDamage()` — for each `p` in t.players:
    1. Accumulate while airborne: `if !p.onGround { p.fallDistance += math.Max(0, p.lastY - p.y) }` (the per-tick descent; p.lastY is last tick's y).
    2. Landing edge: `if !p.wasOnGround && p.onGround { dmg := math.Floor(p.fallDistance - 3.0); if dmg > 0 { t.applyDamage(p, float32(dmg)) }; p.fallDistance = 0 }`.
    3. End-of-loop bookkeeping: `p.wasOnGround = p.onGround` and `p.lastY = p.y` so next tick's delta + edge are correct.
    Keep the `func (t *TickLoop) tickFallDamage()` SIGNATURE identical to the 17-01 stub so the tickEntities call site (in tick_phases.go) compiles unchanged.
    Turn the four fall tests GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestFall|TestSmallFall|TestStayGrounded' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/fall_damage.go contains `func (t *TickLoop) tickFallDamage` computing `math.Floor(p.fallDistance - 3.0)` and calling `t.applyDamage` on the landing edge, using p.fallDistance/p.wasOnGround/p.lastY, with a javap citation naming `Entity.checkFallDamage` or `LivingEntity.causeFallDamage`
    - the 17-01 stub body is gone (the file now has the real implementation; the signature is unchanged)
    - all four fall tests pass
    - this plan does NOT appear in `git diff --name-only` for server/tick.go or server/tick_phases.go (no shared-pipeline edit)
  </acceptance_criteria>
  <done>Falling accumulates distance while airborne and deals floor(fallDistance-3.0) damage into the existing applyDamage flow on landing; small falls and grounded players take nothing — by overwriting the 17-01 stub, with no tick.go/tick_phases.go edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tick (attack) | The ServerboundInteract packet names a target entity id; the client must not set damage or hit out of reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering / Elevation | attack damage | mitigate | The server supplies `baseAttackDamage` (a server const); the packet only NAMES a target. The victim is resolved by entity id + reach-validated server-side; the client never claims an amount (T-6-05 / V4) |
| T-17-08 | Denial of Service | attack-rate flood | accept | Attack packets route through the existing bounded subtick buffer (cap 256, drop-oldest) — no new unbounded path (V5) |
| T-17-09 | Input Validation | ServerboundInteract decode | mitigate | Defensive Scan → silent no-op on malformed/short payload (the established applyInput contract, T-6-04); bound the target id; nil/self guard |
</threat_model>

<verification>
- `cd /d/ender && go build ./server/...` exits 0
- `go test ./server/...` passes including TestAttack*/TestFall* + existing combat_test.go + TestTickPhaseOrder (no phase reorder — fall damage runs inside tickEntities)
- `grep -n "ServerboundInteract" server/subtick.go` shows the new case (no longer default no-op)
- this plan does NOT appear in `git diff --name-only` for server/tick.go or server/tick_phases.go
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race -timeout 1800s ./server/...` clean
</verification>

<success_criteria>
- A PvP attack damages a reach-validated, server-resolved target via applyDamage; death + respawn (already wired) follow.
- Fall damage applies floor(fallDistance-3.0) on landing into the same applyDamage path.
- All inputs defensive; no client-supplied damage; no phase reorder; no shared tick-pipeline edit.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gameplay-completion/17-03-SUMMARY.md`. Document: which packet id (ServerboundInteract vs ServerboundAttack) the javap confirmed carries the entity attack and why; the baseAttackDamage + attackReach sources (bare-hand 1.0 / ~3.0 reach); that fall-damage OVERWROTE the 17-01 fall_damage.go stub and consumed the 17-01-declared lastY/wasOnGround/fallDistance fields (no tick.go/tick_phases.go edit); any non-fall environmental damage explicitly deferred (A2).
</output>
