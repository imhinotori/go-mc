---
phase: 01-foundation-fork-codegen
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md
autonomous: false
requirements: [GEN-04, GEN-01]
user_setup: []

must_haves:
  truths:
    - "The 774→776 diff is enumerated from git diff between baseline-774/ and the regenerated trees — not asserted from memory"
    - "Every enumerated change (new registries, new components, packet-ID shifts, block-state count delta, fluid-count short) traces to jar output"
    - "The handshake protocol constant reads 776 and the version string reads 26.2 (not the 774/1.21.11 values #295 shipped)"
    - "The runtime module builds with go build ./... using Go alone, with no JVM/Java/codegen dependency in the runtime go.mod"
    - "A human reviews and signs off the enumerated diff"
  artifacts:
    - path: ".planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md"
      provides: "The reviewed 774→776 byte-layout enumeration (GEN-04 deliverable)"
      contains: "774"
  key_links:
    - from: "baseline-774/"
      to: "regenerated 776 trees"
      via: "git diff / diff -r between the two trees"
      pattern: "diff"
    - from: "handshake protocol constant"
      to: "776"
      via: "grep for 774 / 1.21.11 replaced with 776 / 26.2"
      pattern: "776"
---

<objective>
Close out the phase: mechanically enumerate the 774→776 delta by diffing the Plan 01 baseline against the Plan 02 regenerated trees (GEN-04), correct the protocol/version constants #295 shipped at 774/1.21.11, and assert the final Go-only runtime build (GEN-01). The diff IS the enumeration — every changed byte layout traces to the jar, never to memory (the explicit defense against Pitfall 2 for a version that post-dates the wiki and training data).

Purpose: GEN-04 (774→776 diff enumerated and reviewed) and the final GEN-01 assertion (runtime builds with Go alone). This is the phase's audit gate: it proves the generated data is jar-derived and the runtime carries no JVM.
Output: A reviewed `774-to-776-DIFF.md` enumeration, corrected protocol constants, and a confirmed Go-only build — plus a human sign-off checkpoint on the diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-fork-codegen/01-RESEARCH.md
@.planning/phases/01-foundation-fork-codegen/01-VALIDATION.md
@.planning/phases/01-foundation-fork-codegen/01-01-SUMMARY.md
@.planning/phases/01-foundation-fork-codegen/01-02-SUMMARY.md

<key_facts>
<!-- VERIFIED in 01-RESEARCH.md. -->
- The 774 baseline lives at .planning/phases/01-foundation-fork-codegen/baseline-774/ (captured in Plan 01). The regenerated 776 trees are at data/* and level/* (Plan 02).
- GEN-04 is mechanical: diff the regenerated 776 trees against the 774 baseline; the diff enumerates the change. Load-bearing deltas to inspect: data/packetid/, data/registryid/, level/component/, level/block/ (state count), data/entity/.
- Known qualitative 776 delta (wiki-cited, jar-authoritative — confirm each against the diff, do not assert): Sulfur Cube feature set — new blocks/items (Cinnabar/Sulfur families, Music Disc "Bounce"), one new entity (Sulfur Cube) with 12 archetypes, a minecraft:sulfur_cube_content data component, a minecraft:sulfur_cube_archetype registry, five new entity attributes, a minecraft:bounce game event, a sulfur_cube_hot damage type, restructured entity predicates. Also 775 already shifted packet IDs (serverbound 50→69, clientbound 135→141) and added ~15 serverbound packets — these show up in the packetid diff.
- Pitfall 3 (Pitfalls research): #295 hand-codes ProtocolVersion = 774 (bot) and the version string "1.21.11". --version 26.2 does NOT touch this hand-written framework code. After Plan 01/02, grep bot/, server/ for `774` and `"1.21.11"` and set them to 776 / "26.2". (Phase 2 owns the handshake assertion behavior, but the constant lives in framework code applied here, so correct it now to avoid a 774-leaking-into-776 build.)
- GEN-01 final check: `go build ./...` must pass on a Go-only path; the runtime go.mod must carry no Java/codegen dependency. Verify on a Java/Docker-free path if available.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated data → runtime build | Verification-only plan: confirms the runtime consumes generated Go with zero JVM coupling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | Java/codegen leaking into the runtime module | mitigate | Final GEN-01 assertion: `go build ./...` with Go alone; assert the runtime go.mod has no Java/codegen require. Confirms the two-module isolation held end-to-end. |
| T-1-06 | Information Disclosure | Wrong protocol constant (774 leaking into a 776 build) | mitigate | Grep-and-correct the #295 hand-coded ProtocolVersion 774 / "1.21.11" to 776 / "26.2" so the build self-identifies as 776; reviewed in the diff sign-off. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 774→776 diff and correct the protocol/version constants</name>
  <files>.planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md, bot/ and server/ framework constants (ProtocolVersion / version string)</files>
  <read_first>
    - .planning/phases/01-foundation-fork-codegen/01-RESEARCH.md — Pattern 3 ("Regenerate and diff"), the generated-data delta table, and Pitfall 3 (774 leaking).
    - .planning/phases/01-foundation-fork-codegen/01-02-SUMMARY.md — the 776 per-tree counts recorded in Plan 02 (the "after" side).
    - .planning/phases/01-foundation-fork-codegen/01-01-SUMMARY.md — the 774 baseline counts (the "before" side).
  </read_first>
  <action>
    PART A — Enumerate the diff (GEN-04). Diff the regenerated 776 trees against the captured 774 baseline. Because the baseline is a copied snapshot (not the same paths), use a recursive tree diff per generated package:
    ```bash
    BASE=.planning/phases/01-foundation-fork-codegen/baseline-774
    for d in data/packetid data/registryid data/item data/entity data/soundid level/block level/component level/biome data/lang; do
      echo "=== $d ==="
      diff -ruN "$BASE/$d" "$d" || true
    done > /tmp/776.diff
    diff -rq "$BASE/data" data 2>/dev/null; diff -rq "$BASE/level" level 2>/dev/null   # per-file added/changed summary
    ```
    Write the enumeration to `.planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md` with:
      - A per-tree summary table: tree | 774 count | 776 count | delta.
      - The load-bearing deltas spelled out: new registries (expect +minecraft:sulfur_cube_archetype), new components (+minecraft:sulfur_cube_content), packet-ID shifts (data/packetid), block-state count change (level/block), new entity (Sulfur Cube), new attributes/game event/damage type.
      - For each delta, a one-line note tying it to jar output (it appears in the generated diff) — NOT asserted from memory. Flag anything in the diff that the known 776 changelog does NOT explain as "investigate" (it may be a 775 carry-over like the packet-ID reshuffle, which is expected).

    PART B — Correct the leaking 774 constants (Pitfall 3 / T-1-06). #295 hand-codes the protocol number and version string at 774 / "1.21.11"; the codegen run does not touch them. Find and fix:
    ```bash
    grep -rn '774' bot/ server/ 2>/dev/null
    grep -rn '1\.21\.11' bot/ server/ 2>/dev/null
    ```
    Set the ProtocolVersion constant to 776 and the version string to "26.2" wherever #295 hard-coded the 774/1.21.11 values. Be surgical — only the protocol/version constants, not generated tables (generated packet IDs are correct as-is; do not touch them). Confirm `go build ./...` still passes after the edit.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md && grep -q '776' .planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md && ! grep -rn '"1\.21\.11"' bot/ server/ 2>/dev/null && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `774-to-776-DIFF.md` exists with a per-tree before/after/delta table and each load-bearing delta noted as jar-traceable.
    - The ProtocolVersion constant reads 776 and the version string reads "26.2"; no `"1.21.11"` remains in bot/ or server/.
    - `go build ./...` exits 0 after the constant edits.
  </acceptance_criteria>
  <done>The 774→776 delta is enumerated from the actual generated diff and written to 774-to-776-DIFF.md, and the framework protocol/version constants read 776/26.2.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the final Go-only runtime build (GEN-01)</name>
  <files>(verification only — no source edits expected)</files>
  <read_first>
    - .planning/phases/01-foundation-fork-codegen/01-RESEARCH.md — Pitfall 6 (Java leaking into runtime) and the Validation Architecture "JVM-free build" smoke check.
    - .planning/phases/01-foundation-fork-codegen/01-VALIDATION.md — GEN-01 row: `go build ./... && ! grep -ri 'jvm|java' go.mod`.
  </read_first>
  <action>
    Final GEN-01 assertion — the runtime builds with Go alone and carries no JVM coupling:
    1. `go build ./...` and `go vet ./...` from the repo root — both must pass using Go alone.
    2. Assert the runtime go.mod declares no Java/codegen dependency: the tools/ module (and its generator/Java-shelling deps) must NOT appear in the runtime module's require graph. Confirm `go list -m all` for the runtime module does not pull the tools module, and `! grep -Eiq '\bjava\b' go.mod`.
    3. If a Go-only (Java/Docker-free) environment is available, run `go build ./...` there to prove the runtime needs neither Java nor Docker. If not available locally, document the assertion as satisfied by the require-graph check and note it for CI.
    4. Run the phase-gate suite per VALIDATION.md: `go vet ./... && go build ./... && go test ./...`.

    Record the GEN-01 evidence (build output, go.mod require graph) in the SUMMARY.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test ./... ; B=$?; ! grep -Eiq '\bjava\b' go.mod && ! go list -m all 2>/dev/null | grep -q '/go-mc/tools' && [ $B -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` exit 0 from the repo root with Go alone.
    - The runtime go.mod has no `java` reference and the require graph does not include the tools/ codegen module.
    - `go test ./...` passes (the generated-package regression net).
  </acceptance_criteria>
  <done>The runtime module builds and tests with Go alone, with no JVM/codegen dependency — GEN-01 asserted end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the 774→776 diff enumeration (GEN-04)</name>
  <files>.planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md</files>
  <action>
    This is a human-verification checkpoint, not an autonomous code task. The executor must PAUSE here and present the 774→776 diff enumeration to the user for review, then wait for the resume signal. Do not self-approve. See <what-built>, <how-to-verify>, and <resume-signal> below for the review script.
  </action>
  <what-built>
    The full Phase 1 foundation: a forked, pinned go-mc with #294-296 applied, the codegen pipeline retargeted to JDK 25, generated 776 Go data committed, and a written 774→776 enumeration. The diff is the audit artifact proving every changed byte layout traces to the 26.2 jar rather than memory.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/01-foundation-fork-codegen/774-to-776-DIFF.md`.
    2. Confirm the per-tree before/after/delta table is present and the counts match the Plan 01 (774) and Plan 02 (776) SUMMARY counts.
    3. Spot-check the load-bearing deltas against the known 776 changelog: a new `minecraft:sulfur_cube_archetype` registry, a new `minecraft:sulfur_cube_content` component, a new Sulfur Cube entity, packet-ID shifts in data/packetid, and a block-state count change in level/block. Each should be backed by a line in the actual generated diff.
    4. Confirm any diff entry flagged "investigate" is either explained (e.g. 775 packet-ID reshuffle) or accepted.
    5. Confirm the build is Go-only: `go build ./...` passes and `go.mod` carries no Java/tools dependency.
  </how-to-verify>
  <verify>Human sign-off — the user types the resume signal after reviewing the diff enumeration.</verify>
  <done>The user has reviewed 774-to-776-DIFF.md, confirmed every delta is jar-traceable and the Go-only build holds, and typed "approved".</done>
  <resume-signal>Type "approved" if the enumeration is jar-traceable and the Go-only build holds, or describe which deltas need re-investigation.</resume-signal>
</task>

</tasks>

<verification>
- `774-to-776-DIFF.md` enumerates the delta from the actual generated diff, with each change jar-traceable (GEN-04).
- ProtocolVersion / version string corrected to 776 / "26.2" (no 774/1.21.11 leak).
- `go build ./...`, `go vet ./...`, `go test ./...` pass with Go alone; runtime go.mod has no Java/tools dependency (GEN-01).
- Human sign-off recorded on the diff enumeration.
</verification>

<success_criteria>
- The 774→776 byte-layout enumeration is written, reviewed, and signed off — every delta traces to the jar (GEN-04).
- The framework self-identifies as protocol 776 / version 26.2.
- The runtime builds and tests with Go alone, no JVM in the module graph (GEN-01, final assertion).
- Phase 1 is complete: forked + pinned go-mc, retargeted codegen, committed 776 data, enumerated diff.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fork-codegen/01-03-SUMMARY.md`.
</output>
