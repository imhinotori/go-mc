---
phase: 30-jumpcontrol-fluid-s1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/fluid.go
  - server/fluid_physics.go
  - server/fluid_mob_test.go
autonomous: true
requirements: [MOB-SUB-05]

must_haves:
  truths:
    - "A mob standing in water reads mobInWater(e)==true and mobFluidHeight(e, WATER)>0"
    - "A mob standing in lava reads mobInLava(e)==true and mobFluidHeight(e, LAVA)>0"
    - "The existing water flow sim + player water predicates behave identically after the lava extension (no .isWater consumer perturbed)"
    - "getFluidJumpThreshold() returns the cited 0.0 default for the pig"
  artifacts:
    - path: "server/fluid.go"
      provides: "fluidState extended with isLava; lavaLevelOf; decodeFluid lava branch"
      contains: "isLava"
    - path: "server/fluid_physics.go"
      provides: "mobInWater / mobFluidHeight / mobInLava / getFluidJumpThreshold predicates"
      contains: "func (t *TickLoop) mobInWater("
    - path: "server/fluid_mob_test.go"
      provides: "headless predicate tests (water + lava + water-unchanged)"
      contains: "func TestMobInWater"
  key_links:
    - from: "server/fluid_physics.go mobInWater"
      to: "server/fluid.go fluidAt/decodeFluid"
      via: "AABB scan over fluidAt reading fluidState.isWater"
      pattern: "fluidAt.*\\.isWater"
    - from: "server/fluid_physics.go mobInLava"
      to: "server/fluid.go decodeFluid"
      via: "AABB scan over fluidAt reading fluidState.isLava"
      pattern: "fluidAt.*\\.isLava"
---

<objective>
Extend the fluid decode path to recognize LAVA (it is water-only today) and port the mob fluid
predicates `mobInWater` / `mobFluidHeight(tag)` / `mobInLava` / `getFluidJumpThreshold` on `*Entity`,
mirroring the existing `playerInWater` AABB scan but using the entity's width/height.

Purpose: FloatGoal (Plan 03) and the aiStep jump branch (Plan 02) both READ these predicates. Without
a real lava decode and real mob fluid reads, FloatGoal.canUse cannot be ported 1:1 (the user chose FULL
water + lava — lava must NOT be stubbed const-false).

Output: `fluidState.isLava` + `lavaLevelOf` + a `decodeFluid` lava branch (with every `.isWater`
consumer audited so water behavior is byte-identical); the four mob fluid predicates; headless tests
proving a mob reads water + lava correctly and the water sim is unperturbed. Closes MOB-SUB-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-jumpcontrol-fluid-s1/30-CONTEXT.md
@.planning/phases/30-jumpcontrol-fluid-s1/30-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing contracts the executor will EXTEND/MIRROR. Read these exact lines, do not re-explore. -->

From server/fluid.go (l.104-130) — fluidState is WATER-ONLY today; EXTEND it:
```go
type fluidState struct {
	isWater bool
	source  bool
	falling bool
	amount  int // 1..8; 8 = full source-equivalent height
}

func waterLevelOf(id block.StateID) (level int, isWater bool) {
	if id < 0 || int(id) >= len(block.StateList) {
		return 0, false
	}
	if w, ok := block.StateList[id].(block.Water); ok {
		return int(w.Level), true
	}
	return 0, false
}

func decodeFluid(id block.StateID) fluidState {
	legacy, ok := waterLevelOf(id)
	if !ok {
		return fluidState{}
	}
	switch {
	case legacy == 0:
		return fluidState{isWater: true, source: true, amount: waterSourceAmount}
	case legacy <= 7:
		return fluidState{isWater: true, amount: waterSourceAmount - legacy}
	default: // 8..15 falling
		return fluidState{isWater: true, falling: true, amount: waterSourceAmount - (legacy - 8)}
	}
}
```

From server/fluid_physics.go (l.40-61) — playerInWater is the AABB scan to MIRROR for mobs:
```go
func (t *TickLoop) playerInWater(p *tickPlayer) bool {
	if t.world() == nil {
		return false
	}
	hw := playerWidth / 2
	minX := int(math.Floor(p.x - hw)); maxX := int(math.Floor(p.x + hw))
	minY := int(math.Floor(p.y));      maxY := int(math.Floor(p.y + playerHeight))
	minZ := int(math.Floor(p.z - hw)); maxZ := int(math.Floor(p.z + hw))
	for x := minX; x <= maxX; x++ {
		for y := minY; y <= maxY; y++ {
			for z := minZ; z <= maxZ; z++ {
				if t.fluidAt(pk.Position{X: x, Y: y, Z: z}).isWater {
					return true
				}
			}
		}
	}
	return false
}
```

From server/breath.go (l.142-152) — fluidSurfaceHeight, the per-cell FlowingFluid.getHeight helper to reuse for mobFluidHeight:
```go
func (t *TickLoop) fluidSurfaceHeight(cell pk.Position, fs fluidState) float64 {
	above := t.fluidAt(pk.Position{X: cell.X, Y: cell.Y + 1, Z: cell.Z})
	if above.isWater {
		return 1.0
	}
	amount := fs.amount
	if amount <= 0 { amount = 1 }
	return float64(amount) / 9.0
}
```
NOTE: this helper's `above.isWater` check is a `.isWater` consumer you must AUDIT — for the LAVA path
it must check the SAME-fluid-above (lava above lava), not water-above. Parameterize on the fluid kind.

Entity dims: `*Entity` carries `width`/`height` (entity.go l.75, used by Phase-29 placement obstruction).
The entity's fluid origin is `e.x`/`e.y`/`e.z` with span `e.x ± e.width/2`, `e.y` .. `e.y + e.height`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fluidState + decodeFluid for lava (audit every .isWater consumer)</name>
  <read_first>
    - server/fluid.go l.78-138 (waterLevelOf, fluidState, decodeFluid, encodeFluid — the EXACT structure to extend)
    - 30-PATTERNS.md "server/fluid.go — EXTEND decodeFluid / fluidState for lava ⚠ LOAD-BEARING" (the 4-step extension recipe + the consumer-audit list)
    - jar: `javap -c -p temp/cache/26.2-inner.jar net.minecraft.world.level.material.LavaFluid` — verify getDropOff / getTickDelay / amount mapping (lava's legacy levels DIFFER from water; overworld lava drop-off is 2, nether is 1 — confirm the getDropOff bytecode and the level→amount inversion BEFORE writing)
    - Verify `block.Lava{Level}` exists: `grep -n "type Lava" level/block/*.go` (confirm the generated block.Lava struct + its Level field)
  </read_first>
  <files>server/fluid.go, server/fluid_mob_test.go</files>
  <behavior>
    - Test 1 (RED first): decodeFluid(lava source state id) returns {isLava:true, source:true, amount:8}; isWater stays false.
    - Test 2: decodeFluid(flowing lava state id) returns {isLava:true, amount:8-legacy} (using lava's verified level mapping).
    - Test 3 (water UNCHANGED): decodeFluid(water source state id) still returns {isWater:true, source:true, amount:8}, isLava:false — the existing TestFluid* / water-sim goldens stay green.
  </behavior>
  <action>
    javap-verify LavaFluid FIRST, then mirror the water decode path for lava (do NOT paraphrase — port
    the inverse-of-getLegacyLevel structure):
    1. Add `isLava bool` to `fluidState` (server/fluid.go l.104-109). Keep `isWater` — a cell is water
       XOR lava XOR neither. Cite `net.minecraft.world.level.material.LavaFluid` / the fluid registry in
       the field doc-comment.
    2. Add `lavaLevelOf(id block.StateID) (level int, isLava bool)` mirroring `waterLevelOf` (l.78-86)
       but matching `block.Lava` instead of `block.Water`. Cite the generated `block.Lava{Level}` type.
    3. Branch `decodeFluid` (l.117-130): try `waterLevelOf` first (unchanged), then `lavaLevelOf`; return
       `{isLava:true, ...}` with lava's verified amount/falling inversion. If neither matches, return the
       zero `fluidState{}` as today. Use lava's javap-verified drop-off in the level→amount math (do NOT
       assume it equals water's — overworld lava drop-off is 2, NOT 1; verify and cite).
    4. AUDIT every existing `.isWater` reader so the lava extension does not perturb water behavior. Grep
       `grep -rn "\.isWater" server/` and confirm each one (fluid.go flow sim, breath.go eyeInWater +
       fluidSurfaceHeight, fluid_physics.go playerInWater, encodeFluid) still treats lava as NON-water —
       i.e. `encodeFluid` must NOT write a water state for an isLava fluidState (return airStateID or
       leave the lava untouched per its decode-only scope). The water FLOW sim must keep `if fs.isWater`
       guards so lava is never flowed by the water scheduler (lava flow sim is DEFERRED this phase — cite
       it as deferred; FloatGoal only READS lava, never flows it).
    DO NOT add a lava flow simulation — decode-only satisfies Phase 30 (cite FlowingFluid.tick for lava
    as the deferred follow-up).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestDecodeFluid|TestFluid' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isLava" server/fluid.go` shows the field on fluidState + the decodeFluid lava branch + lavaLevelOf
    - `grep -rn "\.isWater" server/` audited: every consumer documented as unperturbed (water sim still `if fs.isWater`-guarded)
    - `CGO_ENABLED=0 go build ./...` clean; existing water TestFluid* goldens green (water unchanged)
    - the lava drop-off / level mapping doc-comment cites `javap LavaFluid` (verified value, not assumed = water's)
  </acceptance_criteria>
  <done>fluidState has isLava; decodeFluid returns isLava for lava block ids; every .isWater consumer audited + water sim unperturbed; lava decode jar-cited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port mobInWater / mobFluidHeight / mobInLava / getFluidJumpThreshold</name>
  <read_first>
    - server/fluid_physics.go l.40-73 (playerInWater — the AABB scan to mirror with entity dims)
    - server/breath.go l.128-152 (eyeInWater's `surface = float64(by) + height` max pattern + fluidSurfaceHeight)
    - entity.go l.75 (the *Entity width/height fields; confirm exact field names via grep before use)
    - 30-PATTERNS.md "server/fluid_physics.go — mobInWater / mobFluidHeight / mobInLava" (the substitution recipe + the const-with-citation convention)
    - jar: `javap -c -p temp/cache/26.2-inner.jar net.minecraft.world.entity.Entity` — verify `getFluidHeight(TagKey)` (the MAX of per-cell surface-minus-cellY over the AABB) and `updateFluidHeightAndDoFluidPushing`; `javap net.minecraft.world.entity.Mob` getFluidJumpThreshold (default 0.0); cite `EntityFluidInteraction.isInFluid` for the AABB membership scan
  </read_first>
  <files>server/fluid_physics.go, server/fluid_mob_test.go</files>
  <behavior>
    - Test 1 (RED first): a mob placed in a water source column reads mobInWater(e)==true; mobFluidHeight(e, fluidWater)>0.
    - Test 2: the same mob in a DRY column reads mobInWater(e)==false; mobFluidHeight==0.
    - Test 3: a mob in a lava column reads mobInLava(e)==true; mobInWater==false; mobFluidHeight(e, fluidLava)>0.
    - Test 4: getFluidJumpThreshold(e)==0.0 (the pig default).
  </behavior>
  <action>
    Mirror `playerInWater` (fluid_physics.go l.40-61) 1:1 but take `*Entity` and use `e.width/2` for the
    half-width and `e.y` .. `e.y + e.height` for the vertical span (cite `EntityFluidInteraction.isInFluid`):
    1. `func (t *TickLoop) mobInWater(e *Entity) bool` — the AABB scan, gate `fluidAt(...).isWater`.
    2. `func (t *TickLoop) mobInLava(e *Entity) bool` — same scan, gate `fluidAt(...).isLava`.
    3. Add a fluid-kind selector (a small typed enum or two const tags `fluidWater`/`fluidLava`) so
       `mobFluidHeight(e *Entity, kind fluidKind) float64` can scan for either fluid. Port
       `Entity.getFluidHeight(tag)` = the MAX over the mob's AABB cells of `fluidSurfaceHeight(cell, fs)`
       for cells whose decoded fluid matches `kind` (mirror breath.go l.128-136's `surface = float64(by) +
       height` max). Reuse `fluidSurfaceHeight` (breath.go) — but pass the kind so the same-fluid-above
       check matches lava-above-lava (the audit point from Task 1). Cite `Entity.getFluidHeight` /
       `Entity.updateFluidHeightAndDoFluidPushing`. Return 0.0 when no matching fluid cell is found.
    4. `func (t *TickLoop) getFluidJumpThreshold(e *Entity) float64 { return mobFluidJumpThreshold }`
       with `const mobFluidJumpThreshold = 0.0 // Mob.getFluidJumpThreshold default (javap-verified) —
       a cited constant that becomes a per-type read later`. Follow the breath.go/xp_orb.go
       const-with-citation convention.
    Do NOT port isAffectedByFluids / isPushedByFluid here — Plan 02's jump branch ports only the minimum
    it reads (cite the rest). Keep these reads SYNCHRONOUS + inline (Pitfall: fluid checks SYNCHRONOUS
    first, profile before any async).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestMobInWater|TestMobInLava|TestMobFluidHeight|TestFluidJumpThreshold' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func (t \*TickLoop) mobInWater(" server/fluid_physics.go` matches
    - `grep -n "func (t \*TickLoop) mobInLava(" server/fluid_physics.go` matches
    - `grep -n "func (t \*TickLoop) mobFluidHeight(" server/fluid_physics.go` matches
    - `grep -vn '^[[:space:]]*//' server/fluid_physics.go | grep -c "mobFluidJumpThreshold = 0.0"` >= 1 (the cited const, comment-stripped)
    - `go test ./server/ -run 'TestMobIn|TestMobFluid|TestFluidJump' -count=1` exits 0
    - each new predicate's doc-comment cites its jar method (EntityFluidInteraction.isInFluid / Entity.getFluidHeight / Mob.getFluidJumpThreshold)
  </acceptance_criteria>
  <done>mobInWater/mobInLava/mobFluidHeight(kind)/getFluidJumpThreshold ported + jar-cited; headless tests prove water+lava reads + the 0.0 threshold; reads are synchronous inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fluid read → unloaded chunk | `fluidAt` may be asked for a position in an unloaded chunk during the AABB scan |
| region goroutine → world read | the predicate runs on the owning region's tick goroutine reading tick-owned ChunkManager (TICK-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | mobInWater/mobFluidHeight AABB scan over fluidAt | mitigate | reuse `fluidAt`'s existing unloaded-chunk guard (returns zero fluidState for an absent chunk — same guard playerInWater relies on); the scan is bounded by the mob AABB (a handful of cells), never unbounded |
| T-30-02 | Tampering | lava decode perturbing the water flow sim | mitigate | Task 1's explicit `.isWater`-consumer audit — the water flow scheduler keeps its `if fs.isWater` guards so lava is never flowed; water goldens stay green |
| T-30-03 | Information Disclosure | the fluid predicate reading another region's store | accept | the predicate reads only the world ChunkManager on the owner's tick goroutine (TICK-05); no cross-region entity access — no surface |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` + `go vet ./...` clean
- `CGO_ENABLED=0 go test ./server/ -count=1` green (new mob-fluid tests + unchanged water TestFluid* goldens)
- Docker `-race` over ./server/ clean (the predicates are tick-owned reads — race-clean by construction)
- `go list -deps ./... | grep -i gopy` empty (no new deps; CGO_ENABLED=0 preserved)
- every new const/predicate cites its jar method; lava decode value javap-verified (not assumed = water's)
</verification>

<success_criteria>
- A mob in water reads isInWater=true + fluidHeight>0; a mob in lava reads isInLava=true + fluidHeight(LAVA)>0; a dry mob reads false/0.
- The lava extension does NOT change any water behavior (every .isWater consumer audited; water goldens green).
- getFluidJumpThreshold returns the cited 0.0 default.
- MOB-SUB-05 closed: the mob fluid predicates exist on *Entity and return jar-correct values.
</success_criteria>

<output>
After completion, create `.planning/phases/30-jumpcontrol-fluid-s1/30-01-SUMMARY.md`
</output>
