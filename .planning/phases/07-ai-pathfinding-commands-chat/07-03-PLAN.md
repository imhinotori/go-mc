---
phase: 07-ai-pathfinding-commands-chat
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - server/spawner.go
  - server/mob_category.go
  - server/tick_phases.go
  - server/debug.go
  - server/spawner_test.go
autonomous: true
requirements: [AI-03]

must_haves:
  truths:
    - "server/spawner.go ports a faithful-but-minimal net.minecraft.world.level.NaturalSpawner (resolved Open Question 3): each spawn cycle it COUNTS live mobs by MobCategory from the tick-owned entityStore (the cap-accounting that bounds spawning — Pitfall 3), and for eligible loaded chunks near players, if CREATURE is UNDER its cap (MobCategory.getMaxInstancesPerChunk × spawnableChunkCount), it attempts ONE placement of the v1 test mob (Pig) at a valid standable air block (SpawnPlacements ON_GROUND: solid below + the mob's height of air above) via entityStore.add — which the existing tracker then spawns on clients (the unchanged AddEntity); v1 covers the per-chunk CREATURE attempt + cap, deferring the full multi-category density model + per-player distance weighting + structure spawns + light checks (documented)"
    - "server/mob_category.go ports net.minecraft.world.entity.MobCategory (the enum incl. CREATURE + getMaxInstancesPerChunk); v1 only needs CREATURE; the per-category live count reads the tick-owned store each cycle so a stale/uncounted mob cannot flood or starve the world (Pitfall 3)"
    - "tickAI() (server/tick_phases.go — the EMPTY STUB this phase fills, tick_phases.go:122) is filled to drive, per mob in a stable snapshot (like tickPhysics): the ported serverAiStep (07-01 goal arbitration + 07-02 navigation.tick) for every AI mob, THEN the throttled NaturalSpawner attempt — in the FIXED pipeline slot (tickEntities -> tickAI -> tickPhysics -> applyAsyncResults -> tracker.Tick -> flushOutbound); the pipeline order is NOT changed (TestTickPhaseOrder preserved)"
    - "the throwaway sinusoidal debug pig (server/debug.go — its cosmetic SULFUR_DEBUG pacing) is RETIRED/SUPERSEDED: a naturally-spawned (or debug-spawned) Pig is now driven by the REAL ported GoalSelector + A* navigation so the interactive check (07-06) shows VANILLA behavior (wander + obstacle navigation), NOT sinusoidal pacing; the SULFUR_DEBUG spawn trigger may remain to GUARANTEE a visible mob, but its motion comes from the real AI, and the sinApprox/cosApprox sinusoidal mover is removed/no longer drives the pig"
    - "everything runs SYNCHRONOUSLY on the tick goroutine over the tick-owned store (TICK-05); the spawner is throttled (vanilla attempts every tick but most no-op under cap; v1 may run every N ticks); ZERO new dependencies (NO xsync/ants/conc — the off-tick candidate scan is Phase 8 / OPT-03)"
  artifacts:
    - path: "server/spawner.go"
      provides: "the ported NaturalSpawner-minimal: countByCategory (from entityStore) + the per-chunk CREATURE attempt (pick a column, find a standable air block, SpawnPlacements ON_GROUND, entityStore.add the Pig under cap)"
      contains: "func (t *TickLoop) naturalSpawn"
    - path: "server/mob_category.go"
      provides: "the ported MobCategory enum (CREATURE + getMaxInstancesPerChunk) + the entity-type -> category mapping for v1"
      contains: "CREATURE"
    - path: "server/tick_phases.go"
      provides: "tickAI() filled: per-mob serverAiStep (07-01/07-02) + the throttled naturalSpawn, in the fixed pipeline slot"
      contains: "func (t *TickLoop) tickAI"
    - path: "server/debug.go"
      provides: "the sinusoidal pig mover RETIRED — the debug pig (if spawned) is driven by the real GoalSelector/navigation, not sinApprox/cosApprox"
      contains: "tickDebug"
  key_links:
    - from: "server/tick_phases.go tickAI"
      to: "server/ai_mob.go serverAiStep (07-01 goals + 07-02 navigation)"
      via: "tickAI drives the ported AI for every mob each tick (the call site 07-01/07-02 prepared)"
      pattern: "serverAiStep"
    - from: "server/tick_phases.go tickAI"
      to: "server/spawner.go naturalSpawn"
      via: "tickAI runs the throttled NaturalSpawner attempt after the per-mob AI"
      pattern: "naturalSpawn"
    - from: "server/spawner.go naturalSpawn"
      to: "server/entity_store.go add + near() (cap count + placement)"
      via: "count live mobs by category from the store; add the Pig at a valid block under cap (the tracker then spawns it)"
      pattern: "add"
    - from: "server/debug.go tickDebug"
      to: "server/ai_mob.go (the real AI)"
      via: "the debug pig's motion now comes from the ported GoalSelector/navigation, not the sinusoidal mover"
      pattern: "mobAI"
---

<objective>
Port a faithful-but-minimal natural mob spawner (AI-03), FILL the `tickAI()` call site that drives the ported AI for every mob, and RETIRE the throwaway sinusoidal debug pig — the last AI slot of Phase 7. Per the resolved Open Question 3, the spawner is a faithful-but-minimal `NaturalSpawner` (port `net.minecraft.world.level.NaturalSpawner` + `MobCategory` cap accounting + `SpawnPlacements` checks): each cycle it COUNTS live mobs by `MobCategory` from the tick-owned `entityStore` (the cap-accounting that bounds spawning — Pitfall 3) and, for eligible loaded chunks near players, if CREATURE is UNDER its cap, attempts ONE placement of the v1 test mob (Pig) at a valid standable air block (`SpawnPlacements` ON_GROUND) via `entityStore.add` — which the existing tracker then spawns on clients via the unchanged `AddEntity`. v1 covers the per-chunk CREATURE attempt + cap, deferring the full multi-category density model, per-player distance weighting, structure spawns, and light checks (documented).

This plan also FILLS `tickAI()` (server/tick_phases.go:122 — the EMPTY STUB Phase 7 fills): per mob in a stable snapshot (like `tickPhysics`), it drives the ported `serverAiStep` (07-01 goal arbitration + 07-02 `navigation.tick`) for every AI mob, THEN the throttled `naturalSpawn` — in the FIXED pipeline slot (`tickEntities -> tickAI -> tickPhysics -> applyAsyncResults -> tracker.Tick -> flushOutbound`), NOT reordering the pipeline (`TestTickPhaseOrder` preserved). And it RETIRES the throwaway sinusoidal debug pig (server/debug.go): a Pig is now driven by the REAL ported GoalSelector + A* navigation so the 07-06 interactive check shows VANILLA behavior (wander + obstacle navigation), not sinusoidal pacing. The `SULFUR_DEBUG` spawn trigger may remain to GUARANTEE a visible mob, but its motion comes from the real AI, and the `sinApprox`/`cosApprox` mover no longer drives the pig.

Everything is SYNCHRONOUS on the tick goroutine (TICK-05); the spawner is throttled (vanilla attempts every tick but most no-op under cap; v1 may run every N ticks). ZERO new dependencies (the off-tick candidate scan is Phase 8 / OPT-03).

Purpose: AI-03 "mob spawning populates the world by vanilla-style rules" + the tickAI() fill is what makes the whole phase observable — a mob that spawns and then wanders/navigates via the real AI is the 07-06 interactive proof. The cap-accounting is the load-bearing anti-flood discipline (Pitfall 3).
Output: server/spawner.go (the NaturalSpawner-minimal), server/mob_category.go (MobCategory), the filled tickAI() in server/tick_phases.go, the retired sinusoidal pig in server/debug.go, server/spawner_test.go. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-01-PLAN.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-02-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-03-PLAN.md

<interfaces>
<!-- VERIFIED on disk this session. AI-03 adds mobs to the store; tickAI drives the 07-01/07-02 AI + the spawner. -->

From server/entity_store.go + server/entity.go (the store — the cap count source + the placement add, REUSE):
```go
func (s *entityStore) add(e *Entity)                       // adds a mob (the tracker then spawns it via AddEntity)
func (s *entityStore) near(x, z float64, rangeChunks int) []*Entity // chunks/mobs near a player (cap region + eligibility)
func (s *entityStore) get(id int32) (*Entity, bool)
func (s *entityStore) len() int
func NewEntity(id int32, t entity.Entity, x, y, z float64) *Entity
func (a *EntityIDAllocator) AllocID() int32               // a fresh mob id
type Entity struct { typ entity.Entity; ai *mobAI; ... }  // typ -> MobCategory; ai is the 07-01/07-02 AI handle
```

From server/tick_phases.go (the tickAI STUB to FILL + the fixed pipeline — VERIFIED):
```go
func (t *TickLoop) tickAI() { t.trace("tickAI") } // tick_phases.go:122 — THE EMPTY STUB to fill
// tickOnce order (tick_phases.go:14): tickWorld -> tickChunks -> tickEntities -> tickAI -> tickPhysics ->
//   applyAsyncResults -> tracker.Tick -> flushOutbound. DO NOT reorder (TestTickPhaseOrder).
// tickPhysics (tick_phases.go:141) is the SNAPSHOT-then-loop PATTERN to mirror (copy the byID values to a
// local slice, then range — stable even if a move re-buckets mid-loop).
```

From server/ai_mob.go (Plan 07-01 + 07-02 — the per-mob driver tickAI calls):
```go
func (m *mobAI) serverAiStep(t *TickLoop, e *Entity) // 07-01 goals.tick + tickRunningGoals; 07-02 navigation.requestPath + tick
// tickAI ranges the store's AI mobs and calls e.ai.serverAiStep(t, e) for each (a moved mob re-buckets via moveEntity).
```

From server/physics.go (the standable-block test for placement — REUSE):
```go
func (t *TickLoop) blockSolidAt(x, y, z int) bool // physics.go:76 — solid below + clear air above = standable
const dimMinY = -64                               // physics.go:55
```

From world/manager.go (the placement world read — REUSE):
```go
func (m *ChunkManager) GetBlock(pos pk.Position, minY int) (block.StateID, bool) // (state, ok); ok=false = unloaded
```

From server/debug.go (the throwaway sinusoidal pig to RETIRE — VERIFIED):
```go
func (t *TickLoop) tickDebug()   // debug.go:87 — spawns a pig (NewEntity(id, entity.Pig, ...)) + MOVES it via sinApprox
func sinApprox(x float64) float64 // debug.go:167 — the cosmetic sinusoidal mover (RETIRE as the pig's motion)
// SetDebug(surfaceY) arms it when SULFUR_DEBUG=1. KEEP the spawn trigger (guarantees a visible mob) but
// drive the pig's MOTION via the real ai *mobAI (07-01/07-02), NOT sinApprox/cosApprox.
```

From data/entity (the v1 mob — VERIFIED):
```go
entity.Pig // ID 100 — the CREATURE-category v1 test mob
```
</interfaces>

<key_facts>
<!-- VERIFIED against 07-RESEARCH (Java Sources To Port / AI-03 + Pattern 4 + Pitfall 3) + the jar this session. -->
- PORT NaturalSpawner-MINIMAL (07-RESEARCH AI-03 row 1 + Pattern 4 + Open Question 3): javap net.minecraft.world.level.NaturalSpawner — createState (per-category live counts vs caps), getFilteredSpawningCategories (categories under cap), spawnForChunk/spawnCategoryForChunk (the per-chunk attempt: pick a random column, find a standable Y, roll a mob, place if isValidEmptySpawnBlock + SpawnPlacements.checkSpawnRules), isValidEmptySpawnBlock. v1: a faithful-but-minimal loop — for eligible loaded chunks near players, if CREATURE is under its per-chunk cap, attempt ONE placement of the Pig at a valid standable air block. DEFER the full multi-category density math, the LocalMobCapCalculator per-player distance weighting, structure spawns, and light checks (document each deferral).
- MobCategory (row 2): javap net.minecraft.world.entity.MobCategory — the enum (MONSTER, CREATURE, AMBIENT, AXOLOTLS, WATER_CREATURE, ...) + getMaxInstancesPerChunk() (the cap). v1 only needs CREATURE (the Pig's category). Port the enum + the CREATURE cap + an entity-type -> category mapping (Pig -> CREATURE).
- SpawnPlacements (row 3): javap net.minecraft.world.entity.SpawnPlacements — checkSpawnRules + the placement TYPE (ON_GROUND/IN_WATER) + the heightmap predicate. v1: port the ON_GROUND check (solid below + 2 air above + a heightmap/light predicate); v1 may RELAX the light check (document it).
- THE CAP-ACCOUNTING TRAP (07-RESEARCH Pitfall 3 — load-bearing): vanilla counts live mobs PER CATEGORY against a cap derived from spawnableChunkCount × maxInstancesPerChunk; getting the count wrong floods or starves the world. Each spawn cycle, COUNT live mobs by MobCategory from the tick-owned entityStore (range the store, map typ -> category, tally); only attempt CREATURE spawns when under cap. Add via entityStore.add on the tick (TICK-05). ASSERT this with TestSpawnCapAccounting (cap reached -> no spawn).
- FILL tickAI() (the EMPTY STUB, tick_phases.go:122 — 07-RESEARCH Pattern 1 + the System Architecture Diagram): for each AI mob in a STABLE SNAPSHOT (mirror tickPhysics's copy-then-range, tick_phases.go:141), call e.ai.serverAiStep(t, e) (07-01 goals + 07-02 navigation); THEN run the throttled naturalSpawn. Stay in the FIXED pipeline slot (tickEntities -> tickAI -> tickPhysics -> ...) — do NOT reorder (TestTickPhaseOrder). A moved mob re-buckets via moveEntity (07-02) so the tracker's near() stays correct; the tracker (after tickPhysics) auto-broadcasts.
- RETIRE THE SINUSOIDAL PIG (07-RESEARCH Anti-Patterns + the STATE.md MANDATE + the prompt): server/debug.go's sinApprox/cosApprox cosmetic pacing is a THROWAWAY SULFUR_DEBUG trigger — the mandate REQUIRES the real ported AI to replace its motion. KEEP the spawn trigger (it guarantees a visible mob for the 07-06 check) but ATTACH a real mobAI (newPigAI from 07-01 + the 07-02 navigation) to the debug pig and let tickAI drive it; REMOVE the sinApprox/cosApprox motion as the pig's mover (the helpers may be deleted if unused elsewhere). The debug pig must show VANILLA wander/navigation, not sinusoidal pacing.
- THROTTLE (07-RESEARCH Pattern 4 'When to use'): vanilla attempts spawning every tick but most attempts no-op under cap; v1 may run naturalSpawn every N ticks (a simple gametime % N) to keep the per-tick cost low. Document the cadence.
- TICK-OWNED (TICK-05): the count, the placement add, the per-mob serverAiStep all run on the tick goroutine over the tick-owned store. No off-tick mutation. The off-tick candidate scan is Phase 8 / OPT-03 — NOT this plan.
- PERSISTENCE (07-RESEARCH Runtime State Inventory): v1 does NOT need to persist spawned mobs (entity persistence exists from ENT-06; spawning persistence is a deferrable nuance). Do not add a new store; if persistence is desired, reuse the ENT-06 entities-region path — but it is OUT of v1 scope (document).
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/spawner.go (countByCategory + naturalSpawn) + server/mob_category.go (the enum + cap + Pig->CREATURE). FILL tickAI() in server/tick_phases.go (per-mob serverAiStep snapshot-loop + the throttled naturalSpawn). EDIT server/debug.go (attach a real mobAI to the debug pig; remove the sinusoidal mover as its motion). Create server/spawner_test.go. This is the wave that wires 07-01 + 07-02 into the live tick — run the Docker -race gate here.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new serverbound) — spawning is server-driven | A client cannot trigger a spawn or place a mob; the spawner runs entirely on the tick over server-owned state. The resource concern is spawn flood — bounded by the cap accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-11 | Denial of Service | spawn flood (uncounted/mis-counted mobs filling the world) | mitigate | Spawning is SERVER-driven (the client cannot trigger it); each cycle counts live mobs by category from the tick-owned store and only spawns under the CREATURE cap (Pitfall 3). Asserted by TestSpawnCapAccounting. |
| T-7-12 | Denial of Service | the per-tick AI + spawn loop blowing the MSPT budget | mitigate | tickAI ranges a bounded mob set (the cap bounds the count); naturalSpawn is throttled (every N ticks) and attempts ONE placement; the A* itself is budget-bounded (07-02 maxVisitedNodes). The per-tick cost is bounded by the capped mob count. |
| T-7-08 | Tampering/race | the spawner/AI mutating the store off the tick goroutine | mitigate | naturalSpawn (entityStore.add) + the per-mob serverAiStep run on the tick goroutine (TICK-05); a moved mob re-buckets on the owner. Proven by the Docker -race gate over ./server/... ./world/... — THIS wave wires the live tickAI call site, so the gate runs here. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port MobCategory + the faithful-minimal NaturalSpawner (cap accounting + ON_GROUND placement) (AI-03)</name>
  <files>server/spawner.go, server/mob_category.go, server/spawner_test.go</files>
  <read_first>
    - .planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md — Java Sources To Port / AI-03 (NaturalSpawner/MobCategory/SpawnPlacements rows), Pattern 4, Pitfall 3, Open Question 3.
    - The jar (javap): `javap net.minecraft.world.level.NaturalSpawner` (createState/getFilteredSpawningCategories/spawnForChunk/isValidEmptySpawnBlock), `javap net.minecraft.world.entity.MobCategory` (the enum + getMaxInstancesPerChunk — read the CREATURE cap), `javap net.minecraft.world.entity.SpawnPlacements` (the ON_GROUND check). Record the CREATURE cap value + any divergence in the SUMMARY.
    - server/entity_store.go — add/near/get + ranging the store (the per-category count source).
    - server/physics.go — blockSolidAt + dimMinY (the standable-block test); world/manager.go GetBlock.
  </read_first>
  <behavior>
    AI-03 (cap accounting + placement):
    - Test 1 (TestMobCategoryCap): MobCategory.CREATURE has getMaxInstancesPerChunk == the javap-read value; the Pig maps to CREATURE.
    - Test 2 (TestSpawnCapAccounting): with the store already holding CREATURE mobs AT the cap (for the spawnable region), naturalSpawn attempts NO new spawn (the count-by-category gate works); below cap it MAY spawn (Pitfall 3 — no flood, no starvation).
    - Test 3 (TestSpawnPlacementOnGround): naturalSpawn places the Pig only at a VALID standable block (solid below + clear air above); it does NOT place inside a wall or in the air (the ON_GROUND check).
    - Test 4 (TestSpawnAddsToStore): a successful spawn calls entityStore.add with a fresh AllocID'd Pig at the placement position (so the tracker will spawn it on clients).
  </behavior>
  <action>
    1. server/mob_category.go — port `type mobCategory int` with the const set (MONSTER/CREATURE/AMBIENT/... per javap) + `func (c mobCategory) maxInstancesPerChunk() int` (the javap-read caps; CREATURE is the v1-relevant one) + `func categoryOf(t entity.Entity) mobCategory` (Pig -> CREATURE; v1 maps the test mob, others a sensible default). Cite the javap source.
    2. server/spawner.go — `func (t *TickLoop) countByCategory() map[mobCategory]int` (range the entityStore, tally by categoryOf(e.typ)) and `func (t *TickLoop) naturalSpawn()`: compute the spawnable region (loaded chunks near players via near()/the player set), the CREATURE cap (maxInstancesPerChunk × spawnableChunkCount), the live CREATURE count; if under cap, pick a random eligible column, find a standable Y (solid below + clear air above via blockSolidAt — the SpawnPlacements ON_GROUND check, light relaxed for v1 + documented), and entityStore.add(NewEntity(AllocID(), entity.Pig, x, y, z)) with a fresh mobAI attached (newPigAI from 07-01 + the 07-02 navigation, so tickAI drives it). Document the deferred density/distance/structure/light nuances.
    3. server/spawner_test.go (package server) — the 4 tests in <behavior>, building a small loaded world (Superflat or hand-placed floor) + a player + a pre-populated store, asserting the cap gate + the ON_GROUND placement + the store add.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestMobCategoryCap|TestSpawnCapAccounting|TestSpawnPlacementOnGround|TestSpawnAddsToStore' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - MobCategory + the CREATURE cap are ported from javap; the Pig maps to CREATURE; the per-category live count reads the tick-owned store each cycle.
    - naturalSpawn respects the CREATURE cap (no flood/starvation — Pitfall 3) and places the Pig only at a valid standable air block (SpawnPlacements ON_GROUND), adding it via entityStore.add with a real mobAI attached.
    - The deferred nuances (full density model, distance weighting, structure spawns, light) are documented; idiomatic Go, javap-cited, not a GPL paste.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>A faithful-but-minimal NaturalSpawner spawns the v1 Pig under the CREATURE cap at valid blocks, with a real mobAI attached so it will wander/navigate — the spawn half of AI-03 is implemented (tickAI drives it in Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill tickAI() (drive serverAiStep + naturalSpawn) + retire the sinusoidal debug pig (AI-03)</name>
  <files>server/tick_phases.go, server/debug.go, server/spawner_test.go</files>
  <read_first>
    - .planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md — Pattern 1 (the tickAI driver), the System Architecture Diagram (the tickAI internals + the fixed pipeline), the Anti-Patterns (no new tick phase; retire the sinusoidal pig).
    - server/tick_phases.go — the tickAI STUB (tick_phases.go:122) + the tickPhysics snapshot-then-range PATTERN (tick_phases.go:141) to mirror + the fixed tickOnce order (do NOT reorder; TestTickPhaseOrder).
    - server/ai_mob.go (07-01 + 07-02) — serverAiStep (the per-mob driver tickAI calls).
    - server/debug.go — tickDebug (the spawn trigger to KEEP) + sinApprox/cosApprox (the motion to RETIRE) + SetDebug.
    - server/spawner.go (Task 1) — naturalSpawn.
  </read_first>
  <behavior>
    AI-03 (the tickAI fill + the debug-pig retirement):
    - Test 1 (TestTickAIDrivesMobs): with an AI mob in the store, calling tickAI runs its serverAiStep (the mob's goal/navigation advances — e.g. a mob with a wantTarget moves toward it over ticks). The per-mob AI is actually driven.
    - Test 2 (TestTickAISpawns): over enough throttled cycles with players present + under cap, tickAI's naturalSpawn adds a Pig to the store (the spawner is wired into the live tick).
    - Test 3 (TestTickPhaseOrderUnchanged): the fixed tickOnce phase order still holds (tickAI in its slot, after tickEntities, before tickPhysics — no reorder).
    - Test 4 (TestDebugPigUsesRealAI): the debug pig (when SULFUR_DEBUG armed) is driven by a real mobAI (serverAiStep), NOT sinApprox — assert the pig has an ai handle and its motion comes from the AI path, not the sinusoidal mover (e.g. the sinApprox mover no longer mutates the pig's position).
  </behavior>
  <action>
    1. server/tick_phases.go — FILL tickAI(): keep t.trace("tickAI"); snapshot the store's AI mobs into a local slice (mirror tickPhysics's copy-then-range, tick_phases.go:141); for each, call e.ai.serverAiStep(t, e) (07-01 goals + 07-02 navigation) — guarding e.ai != nil. THEN run the throttled naturalSpawn (e.g. `if t.gametime % spawnInterval == 0 { t.naturalSpawn() }`). Stay in the FIXED slot — do NOT change tickOnce's order or the call site. Document that physics (tickPhysics) runs AFTER so gravity settles the post-AI-move position, and the tracker (after tickPhysics) auto-broadcasts.
    2. server/debug.go — RETIRE the sinusoidal mover: in tickDebug, KEEP the spawn trigger (NewEntity(id, entity.Pig, ...)) but ATTACH a real mobAI (newPigAI + the 07-02 navigation) to the debug pig when it is spawned, and REMOVE the sinApprox/cosApprox position update (the pig's motion now comes from tickAI's serverAiStep, not the sinusoidal pacing). If sinApprox/cosApprox become unused, delete them (or leave a comment if kept for another purpose). The debug pig must show VANILLA wander/navigation.
    3. server/spawner_test.go — add the 4 tests in <behavior> (tickAI drives mobs + spawns + preserves the phase order + the debug pig uses real AI), reusing the test world/store/player harness.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestTickAIDrivesMobs|TestTickAISpawns|TestTickPhaseOrderUnchanged|TestDebugPigUsesRealAI|TestSpawnCapAccounting|TestServerAiStepWalksToGoalTarget' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - tickAI() is filled: it drives serverAiStep (07-01 goals + 07-02 navigation) for every AI mob in a stable snapshot, then runs the throttled naturalSpawn — in the FIXED pipeline slot (no reorder; TestTickPhaseOrder preserved).
    - The throwaway sinusoidal debug pig is RETIRED: the debug pig (if spawned) is driven by the real ported GoalSelector/A* navigation, NOT sinApprox/cosApprox — it shows vanilla wander/navigation.
    - Everything is tick-owned (TICK-05); the spawner is throttled.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>tickAI() drives the ported AI for every mob + the throttled NaturalSpawner, the sinusoidal debug pig is superseded by real vanilla-style AI, and AI-01/02/03 are now wired into the live tick — the world populates and mobs wander/navigate. Run the Docker -race gate over ./server/... ./world/... before marking the wave done (tickAI mutates only tick-owned state, drives moveEntity's re-bucket — TICK-05 / T-7-08).</done>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestMobCategoryCap|TestSpawnCapAccounting|TestSpawnPlacementOnGround|TestSpawnAddsToStore|TestTickAIDrivesMobs|TestTickAISpawns|TestTickPhaseOrderUnchanged|TestDebugPigUsesRealAI' -count=1` passes.
- The existing server/world tests still pass — tickAI is filled + debug.go is edited, but the pipeline order (TestTickPhaseOrder) and the prior phases are NOT regressed.
- Docker `-race` gate over `./server/... ./world/...` is clean — THIS wave wires the live tickAI call site that drives serverAiStep (07-01/07-02) + the spawner, so the race gate over the full AI tick path runs here (tickAI mutates only tick-owned state; moveEntity re-buckets on the owner — TICK-05 / T-7-08).
- `go vet ./... && go build ./...` clean; ZERO new dependencies (NO xsync/ants/conc — the off-tick candidate scan is Phase 8 / OPT-03).
</verification>

<success_criteria>
- AI-03: mob spawning populates the world by vanilla-style rules — the ported faithful-but-minimal NaturalSpawner counts live mobs by MobCategory from the tick-owned store and spawns the v1 Pig under the CREATURE cap at valid standable blocks (SpawnPlacements ON_GROUND), with the full density/distance/structure/light nuances documented as deferred (resolved Open Question 3 / Pitfall 3 — no flood/starvation).
- tickAI() (the empty stub) is FILLED to drive the ported serverAiStep (07-01 goals + 07-02 A* navigation) for every mob + the throttled spawner, in the FIXED pipeline slot (no reorder); the throwaway sinusoidal debug pig is RETIRED in favor of the real vanilla-style AI.
- All mob logic is PORTED from the javap-read Java sources (the STANDING MANDATE), idiomatic Go, never a GPL paste; tick-owned, single-owner (TICK-05); -race clean; zero new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-pathfinding-commands-chat/07-03-SUMMARY.md`.
</output>
</content>
