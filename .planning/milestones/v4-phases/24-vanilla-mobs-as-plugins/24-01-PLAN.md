---
phase: 24-vanilla-mobs-as-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/ai_random.go
  - server/ai_random_test.go
  - server/entity.go
  - server/plugin_entity.go
  - server/plugin_entity_test.go
  - server/plugin_capability.go
autonomous: true
requirements: [PLUGIN-04]

must_haves:
  truths:
    - "Each AI mob owns a per-entity seeded RandomSource; drawing nextInt/nextFloat is deterministic for a fixed seed"
    - "The 3 passive goals draw from the per-entity source in the EXACT bytecode draw order (RandomStroll: nextInt(interval) gate THEN the offset draws)"
    - "TestTickAIDrivesMobs is deterministic for a fixed seed (the global-rand flake retired)"
    - "entity.set_look(yaw, pitch=0) writes e.headYaw AND e.yaw (and e.pitch) exactly as the Go look goals do, gated on entities.write"
    - "world.nearest_player(x,y,z,range) returns the nearest player position in range (or None), gated on world.read"
    - "entity.rand_int(n)/entity.rand_float() draw from the mob's per-entity seeded source, no capability gate (the mob's own RNG)"
  artifacts:
    - path: "server/ai_random.go"
      provides: "per-entity seeded RandomSource (newEntityRandom, nextInt/nextFloat) reproducing the jar draw order"
      contains: "func newEntityRandom"
    - path: "server/plugin_entity.go"
      provides: "entityHandle.set_look + rand_int/rand_float, worldHandle.nearest_player"
      contains: "set_look"
    - path: "server/plugin_entity_test.go"
      provides: "TestEntityRandFaithfulDrawOrder, TestSetLookSeam, TestNearestPlayerSeam"
      contains: "TestSetLookSeam"
  key_links:
    - from: "server/ai_goals_passive.go"
      to: "server/ai_random.go"
      via: "the goals draw from e.ai's seeded source instead of math/rand/v2 package RNG"
      pattern: "rng\\.(nextInt|nextFloat)"
    - from: "server/plugin_entity.go set_look"
      to: "Entity.headYaw / Entity.yaw"
      via: "tick-owned field write after re-resolving the entity by id"
      pattern: "e\\.headYaw"
    - from: "server/plugin_entity.go nearest_player"
      to: "nearestPlayerWithin (the existing t.players scan)"
      via: "a pos-based variant of the existing seam"
      pattern: "nearestPlayer"
---

<objective>
Build the THREE faithful handle extensions the Phase-23 API is missing (the dogfood signal: "if vanilla AI doesn't fit the API, the API is wrong"), plus the per-entity seeded RandomSource that makes the AI 1:1-correct AND deterministic. This is Wave 1 — it ships the seams the Wave-2 vanilla_pig.star goals consume, with NO swap yet.

Three gaps, closed faithfully (each tick-owned, capability-gated, -race-safe per the Phase-23 discipline):
1. `entity.set_look(yaw, pitch=0)` — writes e.headYaw + e.yaw (+ e.pitch) instantly, EXACTLY what lookAtPlayerGoal.tick / randomLookAroundGoal.tick do today. Gate: entities.write.
2. `world.nearest_player(x,y,z,range)` — the nearest-PLAYER read (players are NOT in entityStore; scan t.players via the existing nearestPlayerWithin seam). Gate: world.read.
3. `entity.rand_int(n)` / `entity.rand_float()` — the per-entity seeded RandomSource (the mob.getRandom() analogue), replacing the package math/rand/v2 in the AI. No gate (the mob's own RNG).

The per-entity seeded source is the linchpin: it satisfies the 1:1 mandate (faithful draw ORDER) AND retires the documented TestTickAIDrivesMobs flake (STATE.md: "needs a seeded source per the 1:1 mandate") AND removes a shared-global-rand data-race source.

Purpose: deliver the API surface Wave 2's swap depends on, proven in isolation before the live pig swap.
Output: server/ai_random.go (the seeded source), the 3 handle extensions in plugin_entity.go, the per-entity RNG plumbed into the AI + the 3 existing Go goals, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# The phase artifacts (read both in full before writing)
@.planning/phases/24-vanilla-mobs-as-plugins/24-CONTEXT.md
@.planning/phases/24-vanilla-mobs-as-plugins/24-RESEARCH.md
@.planning/phases/24-vanilla-mobs-as-plugins/24-VALIDATION.md

# The code being modified / extended
@server/ai_goals_passive.go
@server/ai_mob.go
@server/plugin_entity.go
@server/plugin_capability.go

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase this session. -->

From server/plugin_entity.go — the handle surface to EXTEND (additive, do NOT change existing methods):
```go
type entityHandle struct { t *TickLoop; id int32; caps capSet }
func (h *entityHandle) bound(name string, fn func(...) (starlark.Value, error)) starlark.Value
func (h *entityHandle) Attr(name string) (starlark.Value, error)   // mutate methods returned WITHOUT a read-cap check; reads require capEntitiesRead
func (h *entityHandle) AttrNames() []string
func (h *entityHandle) resolve() (*Entity, error)                  // re-resolves on the tick goroutine; clean error if gone
// existing caps used: capEntitiesRead, capEntitiesWrite, capWorldRead, capWorldWrite, capNav
// capError(name string) error  (plugin_capability.go)

type worldHandle struct { t *TickLoop; caps capSet }
func (h *worldHandle) Attr(name string) (starlark.Value, error)
func (h *worldHandle) AttrNames() []string
```

From server/ai_goals_passive.go — the EXISTING player scan + facing math to REUSE (do NOT rewrite):
```go
func nearestPlayerWithin(t *TickLoop, e *Entity, maxDist float64) (x, y, z float64, ok bool)  // scans t.players
func yawTowardDeg(dx, dz float64) float32                                                       // MC-convention atan2(-dx, dz) in degrees
// the 3 goals currently call: rand.IntN(n), rand.Float32(), rand.Float64()  (package math/rand/v2 — REPLACE with the per-entity source)
```

From server/ai_mob.go — where the per-entity source attaches:
```go
type mobAI struct { goals goalSelector; navigation groundNavigation; wantX,wantY,wantZ float64; hasTarget bool }
func newPigAI() *mobAI            // attaches the 3 goals; this is where the seeded source gets created
```

From server/ai_goal.go — the goal methods receive (t *TickLoop, e *Entity); the per-entity source must be reachable from e (e.ai or a new e field).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-entity seeded RandomSource (bytecode draw order) + retire the global-rand AI flake</name>
  <files>server/ai_random.go, server/ai_random_test.go, server/entity.go, server/ai_mob.go, server/ai_goals_passive.go</files>
  <behavior>
    - newEntityRandom(seed) yields a deterministic sequence: two sources with the same seed produce IDENTICAL nextInt(n)/nextFloat()/nextDouble() streams.
    - nextInt(n) returns [0,n); nextFloat() returns [0,1) float32; nextDouble() returns [0,1) float64 — the three draws the 3 goals use.
    - The 3 ported goals draw in the EXACT vanilla order: RandomStroll = nextInt(interval) GATE first, then (only if it passed) 3× nextInt for the offset; LookAtPlayer = nextFloat() probability roll then (on start) 40 + nextInt(40); RandomLookAround = nextFloat() roll then (on start) nextDouble() heading then 20 + nextInt(20).
    - With a fixed seed and a forced/known input, a test pins the exact first N draws (a regression for draw-order permutation).
  </behavior>
  <action>
    Per CONTEXT decision 2 + RESEARCH Pattern 4 / Pitfall 1. FIRST: `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.entity.ai.goal.RandomStrollGoal` and confirm canUse draws `getRandom().nextInt(reducedTickDelay(interval))` THEN getPosition() (which draws again). Also javap LookAtPlayerGoal.canUse/start and RandomLookAroundGoal.canUse/start for their draw counts. javap-verify against the jar FIRST — do NOT trust this plan's intermediate numbers.

    Implement a per-entity seeded RandomSource in server/ai_random.go. Per RESEARCH Open-Q §4 (A4: no current test asserts bit-exact vanilla sequences), a seeded `math/rand/v2.Rand` (NewPCG or ChaCha8, per-entity, fixed/derivable seed) is sufficient and far simpler than a bit-exact LegacyRandomSource port — use it. Expose: `type entityRandom struct { r *rand.Rand }`, `func newEntityRandom(seed uint64) *entityRandom`, and methods `nextInt(n int) int`, `nextFloat() float32`, `nextDouble() float64`. Document each method cites its RandomSource analogue.

    Attach ONE entityRandom per mob: add a field on mobAI (e.g. `rng *entityRandom`) created in newPigAI (and later buildAIFromDecl). Seed it deterministically (e.g. from the entity id, or a tick-loop-owned seed sequence so tests can pin it). It is tick-owned (mutated only on the tick goroutine — no shared global, removing the data-race source).

    REPLACE every `rand.IntN`/`rand.Float32`/`rand.Float64` in server/ai_goals_passive.go with draws from the mob's per-entity source (reach it via e.ai.rng). Keep the EXACT draw order each goal already has (the order matches the bytecode — re-confirm against the javap output, do not reorder). Remove the `math/rand/v2` import from ai_goals_passive.go once unused. The existing test seams (forceTrigger / alwaysLook / always) stay — they bypass the roll, not the source.

    Make the seeded source the determinism fix for TestTickAIDrivesMobs: the test must become deterministic for a fixed seed (own the flake per STATE.md — do NOT leave it flaky and do NOT just skip it).
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go test ./server/ -run 'TestSeededAIRandom|TestEntityRandFaithfulDrawOrder|TestTickAIDrivesMobs' -count=5</automated>
  </verify>
  <done>newEntityRandom is deterministic for a seed; the 3 goals draw from the per-entity source in the bytecode order; ai_goals_passive.go no longer imports math/rand/v2; TestTickAIDrivesMobs passes 5/5; a draw-order test pins the exact sequence. Each ported draw cites its jar class/method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entity.set_look + entity.rand_int/rand_float + world.nearest_player handle extensions (capability-gated)</name>
  <files>server/plugin_entity.go, server/plugin_entity_test.go</files>
  <behavior>
    - entity.set_look(yaw) writes e.headYaw == e.yaw == float32(yaw); set_look(yaw, pitch=p) also writes e.pitch == float32(p). A call without entities.write returns a capability error. A call on a removed entity returns a clean "no longer exists" error.
    - entity.rand_int(n) returns an int in [0,n) from the mob's per-entity source; entity.rand_float() returns a float in [0,1). Deterministic for a fixed mob seed. No capability gate. A mob with no AI/rng returns a clean error (or a documented default — pick and test it).
    - world.nearest_player(x,y,z,range) returns a (px,py,pz) tuple for the nearest player within range, or None when none. A call without world.read returns a capability error.
  </behavior>
  <action>
    Per CONTEXT decision 1 + RESEARCH Pattern 1 / Code Examples + 24-VALIDATION 24-01-02. Additive ONLY — do not alter the existing entityHandle/worldHandle reads or mutates.

    entity.set_look: add a `setLook` bound *Builtin (the RESEARCH Code-Examples signature). UnpackArgs "yaw", "pitch?" (default 0.0). Gate on capEntitiesWrite (return capError("entities.write") — mutate methods are returned without a read check; the method enforces its own write cap, matching moveTo/setVelocity). resolve() the entity, then write `e.headYaw = float32(yaw); e.yaw = float32(yaw); e.pitch = float32(pitch)` — EXACTLY what lookAtPlayerGoal.tick / randomLookAroundGoal.tick write (verified against ai_goals_passive.go this session: they set headYaw=yaw and yaw=yaw). This instant non-gradual set IS the current behavior the existing tests assert (Pitfall 2). Wire "set_look" into Attr's mutate switch + AttrNames. Optionally add "head_yaw"/"eye_y" reads only if Wave 2 confirms it needs them (no-built-but-unwired: do not add reads no goal consumes — the lookAt goal reads e.x/e.z which already exist).

    entity.rand_int / entity.rand_float: add bound *Builtins drawing from the mob's per-entity source (e.ai.rng from Task 1). resolve() the entity; if e.ai or e.ai.rng is nil return a clean error. No capability gate (CONTEXT decision 1: "the per-entity seeded source"; RESEARCH: "Gate: none — a read of the mob's own RNG"). UnpackPositionalArgs for rand_int's n (validate n > 0). Wire into Attr + AttrNames.

    world.nearest_player: add a `nearestPlayer` bound *Builtin (the RESEARCH Code-Examples signature). Gate on capWorldRead. UnpackPositionalArgs 4 (x,y,z,maxDist). REUSE the existing scan — factor a pos-based `nearestPlayerAt(t, x,y,z, maxDist) (px,py,pz float64, ok bool)` out of nearestPlayerWithin (or call it with a synthetic position) so the SCAN logic is unchanged (Don't-Hand-Roll). Return a starlark.Tuple{Float,Float,Float} or starlark.None. Wire into Attr + AttrNames.

    Write TestSetLookSeam, TestEntityRandFaithfulDrawOrder, TestNearestPlayerSeam in plugin_entity_test.go: cover the happy path, the capability-denied path (a handle with a narrowed capSet errors), the removed-entity path, and None-when-no-player. For set_look assert headYaw AND yaw both update (the behavior-identical contract).
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./... && go vet ./server/ && CGO_ENABLED=0 go test ./server/ -run 'TestSetLookSeam|TestEntityRand|TestNearestPlayerSeam'</automated>
  </verify>
  <done>set_look writes headYaw+yaw(+pitch) gated on entities.write; rand_int/rand_float draw from the per-entity source ungated; nearest_player scans t.players gated on world.read; all three error cleanly on a removed entity / denied capability; the existing entityHandle/worldHandle tests still pass (no regression to the existing surface). set_look's instant headYaw+yaw write cites lookAtPlayerGoal.tick / randomLookAroundGoal.tick.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin .star callback → handle seam | untrusted plugin code crosses into tick-owned entity/world state via the new set_look/nearest_player/rand seams |
| tick goroutine → entity store | the seam re-resolves an entity by id; a stale id must not deref a freed pointer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering / data-race | set_look / rand / nearest_player seams leaking a live *Entity off-tick | mitigate | The seams are methods on the id-handle, re-resolving t.entities.get(id) on the tick goroutine (Phase-23 discipline); never store a *Entity. The per-entity rng is tick-owned (created in newPigAI/buildAIFromDecl, drawn only inside a running goal on the tick goroutine — no shared global). Wave 2's Docker -race covers the live tick. |
| T-24-03 | Tampering | set_look writes a NaN/garbage yaw → bad facing / encoder panic | mitigate | yawTowardDeg never produces NaN for a finite vector; the wire byte-angle pack wraps via the existing degree packer. Optionally clamp/validate non-finite yaw to 0 at the seam. |
| T-24-04 | Elevation of privilege | a plugin without entities.write mutates look, or without world.read reads players | mitigate | set_look gates on capEntitiesWrite, nearest_player on capWorldRead, both returning capError before any resolve/scan (tested in the capability-denied path). |
| T-24-05 | Information disclosure | nearest_player leaks player positions to an unprivileged plugin | accept | world.read is the existing read grant; player position is already broadcast on the wire to nearby clients — no new disclosure beyond the existing world.read scope. |
</threat_model>

<verification>
- `cd /d/ender && CGO_ENABLED=0 go build ./...` clean (ship binary stays CGO=0).
- `go vet ./server/` clean.
- `CGO_ENABLED=0 go test ./server/ -run 'TestSeededAIRandom|TestEntityRandFaithfulDrawOrder|TestSetLookSeam|TestNearestPlayerSeam|TestTickAIDrivesMobs' -count=5` passes.
- `! grep -nE 'math/rand/v2' server/ai_goals_passive.go` (the package RNG is gone from the AI goals; the per-entity source replaced it).
- javap evidence for the RandomStroll / LookAtPlayer / RandomLookAround draw order captured in the SUMMARY (the 1:1 anchor).
- IGNORE stale gopls "undefined" diagnostics (RESEARCH: FALSE; gate on go build/vet/test only).
</verification>

<success_criteria>
- A per-entity seeded RandomSource exists, is tick-owned, deterministic for a seed, and the 3 goals draw from it in the bytecode order.
- TestTickAIDrivesMobs is deterministic (the flake retired).
- entity.set_look, entity.rand_int/rand_float, world.nearest_player are live, capability-gated as specified, and tested (happy + denied + removed-entity paths).
- No change to the existing handle reads/mutates; existing Phase-23 handle tests stay green.
- CGO=0 build clean; go vet clean.
</success_criteria>

<output>
After completion, create `.planning/phases/24-vanilla-mobs-as-plugins/24-01-SUMMARY.md` recording: the seeded-source design + seed derivation, the exact draw order per goal (with the javap evidence), the three handle-extension signatures, and which existing tests now run against the per-entity source. Note explicitly whether RandomLookAround's requiresUpdateEveryTick needs handling in Wave 2 (the starlarkGoal does NOT honor it today — flag it for Plan 02).
</output>
