---
phase: 14-structure-pipeline-temples
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - world/structure/jungle_temple.go
  - world/structure/jungle_temple_test.go
  - world/structure/igloo.go
  - world/structure/igloo_test.go
  - world/structure/swamp_hut.go
  - world/structure/swamp_hut_test.go
  - world/structure/acceptance_test.go
  - world/noisegen.go
requirements: [STRUCT-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The jungle temple is ported (javap -c net.minecraft.world.level.levelgen.structure.structures.JungleTempleStructure findGenerationPoint + JungleTemplePiece against temp/cache/26.2-inner.jar — single-piece, hardcoded geometry, 0 .nbt, shares the desert set's scattered machinery): the StartGenerator gates ownership via 14-01's getPotentialStructureChunk (jungle_pyramid shares the desert structure_set's spacing 32/separation 8 but cross-check the salt from the embedded structure_set JSON — do NOT assume), a REAL jungle biome check (biomeAt(origin) ∈ the embedded has_structure/jungle_temple allow-set {minecraft:bamboo_jungle, minecraft:jungle} — VERIFIED; a non-jungle origin → NO start, NO accept-by-default), the SampleSurfaceY project height, and JungleTemplePiece.PostProcess writes the JAR-EXACT cobblestone/mossy-cobblestone temple (the stepped tower + the two hidden chambers + the tripwire/redstone-dispenser trap + the lever/piston puzzle + the 2 chests as blocks + tag, loot deferred) via the 14-02 base-piece helpers (generateBox/placeBlock/createChest), block-by-block + draw order pinned."
    - "The igloo is ported (javap -c IglooStructure findGenerationPoint + IglooPieces — a TINY FIXED SET of pieces, NOT a single piece: the snow-block igloo dome ALWAYS, plus a PROBABILISTIC ladder+basement (the brewing-stand/cauldron lab with the villager+zombie-villager markers + the chest)): the StartGenerator gates via the igloo structure_set (salt 14357618 — cross-check the JSON), a REAL snowy biome check (biomeAt(origin) ∈ the embedded has_structure/igloo allow-set {minecraft:snowy_taiga, minecraft:snowy_plains, minecraft:snowy_slopes} — VERIFIED; otherwise NO start), the surface Y; IglooPieces builds the dome piece + (per a nextInt probability draw) the ladder + basement pieces via addChildren (the FIRST exercise of the 14-02 addChildren multi-piece hook, though still a tiny fixed set not recursion); each piece's PostProcess writes its hardcoded geometry (snow blocks / the basement lab + chest as block+tag, loot deferred + the entity markers deferred), draw order pinned including the basement-present probability."
    - "The swamp hut (witch hut) is ported (javap -c SwampHutStructure findGenerationPoint + SwampHutPiece — single-piece, 0 .nbt): the StartGenerator gates via the swamp_hut structure_set (salt 14357620 — cross-check the JSON), a REAL swamp biome check (biomeAt(origin) ∈ the embedded has_structure/swamp_hut allow-set {minecraft:swamp} — VERIFIED; otherwise NO start), the surface Y; SwampHutPiece.PostProcess writes the JAR-EXACT spruce-plank hut on oak-log stilts (the floor on legs, the walls, the roof, the cauldron + crafting-table + the flower-pot-with-red-mushroom — the witch/black-cat entity markers DEFERRED) via the base-piece helpers, block-by-block pinned. All three temples are REGISTERED into the NoiseGenerator's StartGenerator set so ComputeStarts + the PLACE pass place them."
    - "The STRUCTURES-PIPELINE ACCEPTANCE gate is green (automated, NO visual gate — that is Phase 16): a deterministic placement test for ALL FOUR scattered temples (desert pyramid from 14-02 + jungle temple + igloo + swamp hut) — each lands at its expected chunk (derived from getPotentialStructureChunk with its salt) in its correct biome with its expected block fingerprint; the cross-chunk references+clip still places a chunk-spanning temple idempotently; STARTS is pure over (seed,pos) for all four; and the Phase-11 5x5 reorder-determinism (TestDecorationReorderIdentical) + emit-once gates STAY byte-identical with all four temples placing."
    - "The full Docker -race gate (CGO golang:1.26, CGO_ENABLED=0 host build) over ./world/... ./world/structure/... is clean with all four temples live; go.mod/go.sum are UNCHANGED (zero new deps — the pipeline reuses xsync/singleflight from 14-01); no encoder/packet/chunk-wire file is touched (worldgen adds no new wire surface — the chunk format was sealed in v1 Phase 4). The structures-pipeline half of Phase 14 is closed, ready for Phase 15 (mineshaft/stronghold reusing the 14-02 piece + addChildren machinery)."
  artifacts:
    - path: "world/structure/jungle_temple.go"
      provides: "the jungle_temple StartGenerator (getPotentialStructureChunk + jungle biome/surface) + JungleTemplePiece + its hardcoded PostProcess (cobble tower + chambers + traps + 2 chests as blocks, loot deferred), registered"
      contains: "JungleTemplePiece"
      min_lines: 180  # floor only — the REAL completeness gate is TestJungleTemplePlaces' full block fingerprint (catches truncated geometry); the cobble tower + 2 chambers + traps + 2 chests is plausibly 180-300+ lines
    - path: "world/structure/igloo.go"
      provides: "the igloo StartGenerator + IglooPieces (the dome + the probabilistic ladder+basement via addChildren — the first multi-piece use of the 14-02 hook) + each piece's hardcoded PostProcess (snow dome / basement lab + chest, loot+entities deferred), registered"
      contains: "IglooPieces"
      min_lines: 140  # floor only — the dome + ladder + basement lab across the fixed piece set; the real gate is TestIglooDomeAlways/BasementProbabilistic' fingerprints
    - path: "world/structure/swamp_hut.go"
      provides: "the swamp_hut StartGenerator + SwampHutPiece + its hardcoded PostProcess (spruce hut on oak-log stilts + cauldron/crafting-table/flowerpot, witch/cat entities deferred), registered"
      contains: "SwampHutPiece"
      min_lines: 100  # floor only — the stilts + hut + cauldron/crafting-table/flowerpot; the real gate is TestSwampHutPlaces' fingerprint
    - path: "world/structure/acceptance_test.go"
      provides: "the structures-pipeline acceptance: all 4 temples land at their expected chunks (derived from getPotentialStructureChunk per salt) IN their correct biomes (the REAL biome check, not accept-by-default) with FULL expected block fingerprints (the completeness gate, not min_lines) + cross-chunk idempotence + STARTS purity + the 5x5 reorder/emit-once byte-identity with all temples placing"
      contains: "TestStructuresPipelineAcceptance"
  key_links:
    - from: "world/structure/jungle_temple.go + igloo.go + swamp_hut.go PostProcess"
      to: "world/structure/piece.go (14-02) generateBox/placeBlock/createChest/addChildren"
      via: "all three temples reuse the 14-02 base-piece machinery; igloo is the first addChildren multi-piece user"
      pattern: "placeBlock|generateBox|addChildren"
    - from: "world/noisegen.go StartGenerator set"
      to: "world/structure/{jungle_temple,igloo,swamp_hut}.go StartGenerators"
      via: "the three temples register alongside the desert pyramid so ComputeStarts + placeStructures place them"
      pattern: "StartGenerator|register"
    - from: "world/structure/acceptance_test.go"
      to: "world/structure/placement.go getPotentialStructureChunk (per-salt)"
      via: "each temple's expected chunk is derived from its salt'd placement math, not eyeballed"
      pattern: "getPotentialStructureChunk"
---

<objective>
Land the cheap single-piece temple follow-ons (STRUCT-02) on 14-02's proven machinery, then close the structures-pipeline half of Phase 14 with the automated acceptance gate.

1. **Jungle temple** (`JungleTemplePiece`, single-piece, 0 .nbt): the cobblestone/mossy-cobble stepped temple + the hidden chambers + the tripwire/dispenser + lever/piston puzzle + 2 chests (blocks + tag, loot deferred) — shares the desert scattered machinery, gated by the `jungle_pyramid` structure_set.

2. **Igloo** (`IglooPieces`, a tiny FIXED set — the snow dome ALWAYS + a probabilistic ladder+basement lab): the **first exercise of 14-02's `addChildren` multi-piece hook** (still a fixed set, not recursion). The basement chest + the villager/zombie-villager markers are deferred; the dome + lab geometry is delivered.

3. **Swamp hut** (`SwampHutPiece`, single-piece, 0 .nbt): the spruce-plank witch hut on oak-log stilts + cauldron/crafting-table/flowerpot (witch/cat entities deferred).

4. **The structures-pipeline acceptance gate** (automated — NO visual gate, that is Phase 16): all FOUR scattered temples land at their expected chunks (derived from `getPotentialStructureChunk` per salt) in their correct biomes with their expected block fingerprints; the cross-chunk references+clip stays idempotent; STARTS stays pure over (seed,pos); and the 5×5 reorder-determinism + emit-once + Docker `-race` gates stay green with all four placing.

All three temples are cheap because 14-02 already built `placeBlock`/`generateBox`/`createChest`/`addChildren` + the placeInChunk clip — these are geometry transcriptions on a proven pipeline.

Purpose: complete the Tier-1 scattered-temple set + close STRUCT-02, leaving Phase 15 (mineshaft/stronghold) a fully-shaken-down piece + addChildren machinery to reuse.
Output: `jungle_temple.go` + `igloo.go` + `swamp_hut.go` + the acceptance gate. ZERO new deps, NO visual gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-structures.md
@.planning/phases/14-structure-pipeline-temples/14-01-PLAN.md
@.planning/phases/14-structure-pipeline-temples/14-02-PLAN.md

<interfaces>
<!-- 14-02's StructurePiece machinery + the desert pyramid are the template. Re-read the AS-BUILT world/structure/{piece,place,desert_pyramid}.go BEFORE coding — copy the desert pyramid's StartGenerator + Piece shape. Trust `go build ./...` over gopls/Serena. -->

THE 14-02 PIECE MACHINERY (world/structure/piece.go, the template — VERIFY field/method names AS-BUILT):
```go
type Piece interface { BoundingBox() BoundingBox; PostProcess(view WorldGenView, box BoundingBox, chunkPos level.ChunkPos, rng levelgen.RandomSource) }
type WorldGenView interface { SetBlock(wx,wy,wz int, st block.StateID); GetBlock(wx,wy,wz int) block.StateID }
// base StructurePiece + helpers (cite the AS-BUILT names):
func (p *StructurePiece) placeBlock(view WorldGenView, st block.StateID, x,y,z int, box BoundingBox)
func (p *StructurePiece) generateBox(view WorldGenView, box BoundingBox, x1,y1,z1, x2,y2,z2 int, st block.StateID)
func (p *StructurePiece) fillColumnDown(view WorldGenView, st block.StateID, x,z, y int, box BoundingBox)
func (p *StructurePiece) createChest(view WorldGenView, box BoundingBox, x,y,z int, lootTag string) bool  // block+tag, loot DEFERRED
func findCollisionPiece(pieces []Piece, box BoundingBox) Piece
func (p *StructurePiece) addChildren(...)  // the recursion/multi-piece hook (igloo is the first user)
```
THE DESERT PYRAMID TEMPLATE (world/structure/desert_pyramid.go, 14-02 — COPY its shape): desertPyramidStartGen.GenerateStarts (isStructureChunk gate + SampleSurfaceY + biome check + build piece + StructureStart) + DesertPyramidPiece (struct embedding base StructurePiece + constructor bbox + PostProcess geometry). The three new temples mirror this exactly — only the salt, the biome, and the geometry differ.

THE PLACEMENT MATH (world/structure/placement.go, 14-01 — VERIFIED): getPotentialStructureChunk(worldSeed, cx, cz, spacing, separation, salt, spreadType) + isStructureChunk. LOAD the per-temple spacing/separation/salt from the embedded structure_set JSON (world/levelgen/data/structure_set/) — desert/jungle share the desert set machinery, igloo salt 14357618, swamp_hut salt 14357620 (research) — but CROSS-CHECK every value against the embedded JSON, do NOT hardcode from memory.

THE SEED HELPER (world/levelgen/random.go, VERIFIED Phase 10): the PIECE rng = WorldgenRandom.SetLargeFeatureSeed(worldSeed, startChunkX, startChunkZ) — the igloo basement-present probability + any geometry draws come from THIS.

THE TEMPLE BLOCKS (level/block/blocks.go, VERIFIED present): Cobblestone{}, MossyCobblestone{}, SnowBlock{} (verify the exact type name — likely Snow / SnowBlock), SprucePlanks{}, OakLog{Axis:...}, Cauldron{}, CraftingTable{}, FlowerPot{}/PottedRedMushroom{}, BrewingStand{}, Ladder{Facing:...}, Chest{Facing:...,Type:...}, plus the dispenser/tripwire/lever/piston for the jungle trap — resolve via block.ToStateID (match the exact props; grep level/block/blocks.go for the precise type names BEFORE coding).

THE REGISTRATION + HOOK (world/noisegen.go, VERIFIED 14-02): the NoiseGenerator holds the StartGenerator set + the filled placeStructures hook. REGISTER the three new StartGenerators alongside the desert pyramid (the same registration site 14-02 used). No pipeline change — the existing ComputeStarts + PLACE pass place them.

THE DETERMINISM GATES (world/worker_seam_test.go, VERIFIED): TestDecorationReorderIdentical (5x5) + TestEmitOnce MUST stay green with all four temples placing. Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... ./world/structure/...`.
</interfaces>

<key_facts>
<!-- PORT each temple's findGenerationPoint + *Piece postProcess from jar bytecode (cite each class). Hardcoded geometry, 0 .nbt. Reuse the 14-02 base-piece helpers. No GPL paste. Loot + entities DEFERRED v3. -->
- PORT TARGETS (javap -c, cite each): JungleTempleStructure.findGenerationPoint + JungleTemplePiece.postProcess; IglooStructure.findGenerationPoint + IglooPieces (the dome piece + the addChildren ladder/basement); SwampHutStructure.findGenerationPoint + SwampHutPiece.postProcess. All extend ScatteredFeaturePiece (the shared scattered-temple base 14-02 ported).
- BIOME CHECK IS LOAD-BEARING (BLOCKER fix — "vanilla positions" requires it): each temple gates on a REAL biomeAt(origin) ∈ its embedded has_structure/<temple> allow-set (desert→{desert}, igloo→{snowy_taiga,snowy_plains,snowy_slopes}, jungle_temple→{bamboo_jungle,jungle}, swamp_hut→{swamp} — all VERIFIED in the jar + embedded by 14-01). biomeAt is the NoiseGenerator's MultiNoiseBiomeSource.GetBiome(x,y,z) (public, world/levelgen/biome/source.go:119), threaded into the StartGenerator per the 14-01 interface; load each allow-set from the embedded tag, parse to a levelbiome.Type set (via .String()/the name lookup). There is NO accept-by-default escape — a temple whose origin biome is outside its set produces NO start (a desert pyramid must NOT appear in an ocean/forest). This is the difference between "places" and "places in vanilla positions".
- SALT CROSS-CHECK (Pitfall #3 — do NOT trust memory): the jungle_pyramid/desert_pyramid SHARE the desert structure_set's spacing 32/separation 8, but VERIFY the salt + the spread_type from the embedded structure_set JSON (jungle may share or differ); igloo salt 14357618, swamp_hut salt 14357620 per research — but confirm against world/levelgen/data/structure_set/*.json. A wrong salt → the temple lands in the wrong chunk vs a vanilla seed.
- IGLOO = THE FIRST MULTI-PIECE (addChildren): IglooPieces is NOT a single piece — the snow dome is ALWAYS placed, and a ladder + a basement (the brewing-stand/cauldron lab) is added PROBABILISTICALLY (a nextInt draw against the jar's basement-present chance). This is the first user of 14-02's addChildren hook (still a fixed 1-3 piece set, NOT mineshaft recursion — that is Phase 15). The basement chest (block + loot tag, loot deferred) + the villager/zombie-villager entity markers (DEFERRED v3) are the deferrals; the snow dome + the lab GEOMETRY is delivered. The basement-present probability draw is part of the determinism contract.
- ENTITY/LOOT DEFERRALS (documented, matches REQUIREMENTS.md v3 + the Phase-13 dungeon precedent): chests place as the chest BLOCK + a loot-table tag; the igloo villager/zombie-villager + the swamp-hut witch/black-cat + any trap mob are DEFERRED (entities/loot/block-entities are v3 subsystems). The VISIBLE temples (full geometry + chest/cauldron/spawner-trigger blocks) are delivered. Document each deferral inline.
- TRAP BLOCKS, NOT TRAP LOGIC: the jungle temple tripwire/dispenser/lever/piston puzzle + the desert TNT trap place as BLOCKS in their hardcoded positions; the redstone/dispenser-fire RUNTIME logic is a separate concern (not this milestone). The block layout is the deliverable + the determinism contract.
- THE TEMPLES ARE CHEAP (the 14-02 payoff): each is a geometry transcription using the already-ported placeBlock/generateBox/createChest/addChildren + the desert-pyramid StartGenerator template. NO new pipeline, cache, placement-math, or seam work — only per-temple findGenerationPoint (salt+biome+surface) + postProcess (the hardcoded blocks). If a temple's geometry is large, the cost is transcription volume, not architecture.
- THE ACCEPTANCE IS AUTOMATED (NO visual gate — Phase 16 owns the structures visual gate): each temple lands at its EXPECTED chunk (computed FROM getPotentialStructureChunk with its salt IN the test, not eyeballed) in its correct biome with its expected block fingerprint; the cross-chunk idempotence (from 14-02) holds for a chunk-spanning temple; STARTS is pure over (seed,pos); the 5x5 reorder-determinism + emit-once stay byte-identical with all four placing; Docker -race clean. Do NOT add an autonomous:false human-verify task.
- NO NEW WIRE SURFACE: worldgen adds no new packets (the chunk format was capture-diff-sealed in v1 Phase 4) — grep-confirm no encoder/packet file is touched; the Phase-4/5 wire goldens stay the authority + stay green. ZERO new deps (the pipeline reuses 14-01's xsync/singleflight).
- STALE-LSP caveat: trust `go build ./...` over gopls/Serena phantom errors.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: jungle temple + swamp hut (single-piece scattered temples, reusing the 14-02 machinery)</name>
  <files>world/structure/jungle_temple.go, world/structure/jungle_temple_test.go, world/structure/swamp_hut.go, world/structure/swamp_hut_test.go, world/noisegen.go</files>
  <behavior>
    - The jungle_temple StartGenerator gates ownership via getPotentialStructureChunk (the jungle_pyramid structure_set salt/spacing — cross-checked from the embedded JSON) + a REAL jungle biome check (biomeAt(origin) ∈ has_structure/jungle_temple = {bamboo_jungle, jungle}, no accept-by-default) + SampleSurfaceY; JungleTemplePiece.PostProcess writes the JAR-EXACT cobble/mossy-cobble stepped temple + chambers + the tripwire/dispenser + lever/piston puzzle (as blocks) + 2 chests (block + tag, loot deferred) — block-by-block + draw order pinned.
    - The swamp_hut StartGenerator gates via the swamp_hut structure_set (salt 14357620, cross-checked) + a REAL swamp biome check (biomeAt(origin) ∈ has_structure/swamp_hut = {swamp}, no accept-by-default) + SampleSurfaceY; SwampHutPiece.PostProcess writes the spruce-plank hut on oak-log stilts + cauldron + crafting-table + flowerpot-with-red-mushroom (witch/cat entities deferred) — pinned.
    - Both are registered; a known worldSeed → each at its expected chunk (derived from getPotentialStructureChunk in-test) with its expected fingerprint; both starts pure over (seed,pos).
  </behavior>
  <action>
First re-read the AS-BUILT world/structure/desert_pyramid.go (the StartGenerator + Piece template) + piece.go (the helpers) + the jungle_pyramid + swamp_hut structure_set JSON (cross-check salt/spacing/spread_type). grep level/block/blocks.go for the exact block type names (Cobblestone/MossyCobblestone/SprucePlanks/OakLog/Cauldron/CraftingTable/FlowerPot/dispenser/tripwire/lever/piston).
world/structure/jungle_temple.go: port (javap -c, cite JungleTempleStructure.findGenerationPoint + JungleTemplePiece) — jungleTempleStartGen (isStructureChunk via the jungle_pyramid set + the REAL biome check biomeAt(origin) ∈ the embedded has_structure/jungle_temple {bamboo_jungle,jungle} — threaded biomeAt from the NoiseGenerator per the 14-01 StartGenerator interface, NO accept-by-default + SampleSurfaceY) + JungleTemplePiece (embed base StructurePiece, constructor bbox, PostProcess: TRANSCRIBE the cobble/mossy tower + chambers + the trap blocks + createChest x2 [loot deferred]). Resolve StateIDs via block.ToStateID. No GPL paste; document the loot/trap-logic deferrals.
world/structure/swamp_hut.go: port (javap -c, cite SwampHutStructure + SwampHutPiece) — swampHutStartGen (salt 14357620, the REAL biome check biomeAt(origin) ∈ has_structure/swamp_hut {swamp} — no accept-by-default, surface) + SwampHutPiece (PostProcess: TRANSCRIBE the stilts + floor + walls + roof + cauldron/crafting-table/flowerpot). Document the witch/cat entity deferral.
world/noisegen.go: REGISTER jungleTempleStartGen + swampHutStartGen into the StartGenerator set (alongside the desert pyramid, same site).
world/structure/jungle_temple_test.go: TestJungleTempleExpectedChunk (worldSeed → owner == getPotentialStructureChunk(seed,...,jungle salt,...)) + TestJungleTemplePlaces (the cobble shell + chambers + 2 chest blocks at expected coords, fingerprint-pinned) + TestJungleTempleStartPure.
world/structure/swamp_hut_test.go: TestSwampHutExpectedChunk + TestSwampHutPlaces (stilts + hut + cauldron/crafting-table/flowerpot) + TestSwampHutStartPure.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestJungleTemple|TestSwampHut' -count=1 -race && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The jungle temple + swamp hut are ported (single-piece, 0 .nbt, hardcoded geometry via the 14-02 helpers), gated by their cross-checked structure_set salts + REAL has_structure biome checks (biomeAt ∈ allow-set, no accept-by-default), registered, and each lands at its expected chunk (derived from the placement algorithm) with its expected block fingerprint; loot/trap-logic/entities deferred (documented); both starts pure over (seed,pos), -race clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: igloo (the first addChildren multi-piece) + the structures-pipeline acceptance gate</name>
  <files>world/structure/igloo.go, world/structure/igloo_test.go, world/structure/acceptance_test.go, world/noisegen.go</files>
  <behavior>
    - The igloo StartGenerator gates via the igloo structure_set (salt 14357618, cross-checked) + a REAL snowy biome check (biomeAt(origin) ∈ has_structure/igloo {snowy_taiga,snowy_plains,snowy_slopes}, no accept-by-default) + SampleSurfaceY; IglooPieces builds the snow dome piece ALWAYS + (per a nextInt basement-present probability draw) the ladder + basement pieces via addChildren — the first multi-piece use of the 14-02 hook; each piece's PostProcess writes its hardcoded geometry (snow dome / basement lab + brewing-stand/cauldron + chest as block+tag, loot+villager-markers deferred). The basement-present probability is part of the determinism contract.
    - The structures-pipeline acceptance: all FOUR temples (desert pyramid from 14-02 + jungle + igloo + swamp hut) land at their expected chunks (derived per-salt from getPotentialStructureChunk) in their biomes with expected fingerprints; a chunk-spanning temple places idempotently (cross-chunk references+clip); STARTS pure over (seed,pos) for all four; the 5x5 reorder-determinism + emit-once byte-identical with all four placing; Docker -race clean.
  </behavior>
  <action>
First re-read the AS-BUILT piece.go addChildren hook + the igloo structure_set JSON (cross-check salt 14357618). grep level/block for the snow/ladder/brewing-stand/cauldron block type names.
world/structure/igloo.go: port (javap -c, cite IglooStructure.findGenerationPoint + IglooPieces) — iglooStartGen (isStructureChunk via the igloo set + the REAL biome check biomeAt(origin) ∈ the embedded has_structure/igloo {snowy_taiga,snowy_plains,snowy_slopes} — threaded biomeAt, NO accept-by-default + SampleSurfaceY) + IglooPieces: the dome Piece (always) + the ladder + basement Pieces added via addChildren GATED on the jar's basement-present nextInt draw (TRANSCRIBE the probability + the piece offsets). Each piece's PostProcess: the snow-block dome geometry / the basement lab (brewing stand + cauldron + the chest via createChest [loot deferred] + the villager/zombie-villager entity markers DEFERRED). Resolve StateIDs via block.ToStateID. Document the loot/entity deferrals + that this is the first addChildren multi-piece (fixed set, not recursion).
world/noisegen.go: REGISTER iglooStartGen (now all four temples are in the StartGenerator set).
world/structure/igloo_test.go: TestIglooExpectedChunk (owner == getPotentialStructureChunk(seed,...,14357618,...)) + TestIglooDomeAlways (the dome places) + TestIglooBasementProbabilistic (a seed that draws basement-present → the ladder+basement pieces appear via addChildren; a seed that does not → dome only) + TestIglooStartPure.
world/structure/acceptance_test.go: TestStructuresPipelineAcceptance — for each of the 4 temples, assert it lands at its expected chunk (per-salt getPotentialStructureChunk) ONLY WHEN the origin biome is in its has_structure allow-set (a forced out-of-biome origin → NO start, proving the biome check is load-bearing, not accept-by-default) and with its FULL expected block fingerprint; TestStructuresCrossChunkIdempotent (a chunk-spanning temple places once-per-chunk, union = complete, no double-write); TestStructuresStartsPure (all four pure over (seed,pos)). Then assert TestDecorationReorderIdentical (5x5) + TestEmitOnce STILL byte-identical with all four temples placing (this test may live in world/ — reference + re-run it, or add a world-package acceptance that drives the full Decorate with structures live). Run the full Docker -race over ./world/... ./world/structure/....
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestIgloo|TestStructuresPipelineAcceptance|TestStructuresCrossChunkIdempotent|TestStructuresStartsPure' -count=1 -race && go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce' -count=1 && go build ./... && CGO_ENABLED=0 go build ./... && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... ./world/structure/...</automated>
  </verify>
  <done>The igloo is ported (the dome always + the probabilistic ladder+basement via the first use of 14-02's addChildren multi-piece hook; loot/entities deferred), registered; and the structures-pipeline acceptance is green — all four scattered temples land at their expected chunks (derived per-salt) in their biomes with expected fingerprints, the cross-chunk references+clip stays idempotent, STARTS is pure over (seed,pos), the 5x5 reorder-determinism + emit-once stay byte-identical with all four placing, and Docker -race is clean. STRUCT-02 is closed; the structures-pipeline half of Phase 14 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → temple geometry | Each temple's geometry is hardcoded in the ported postProcess (0 .nbt); the structure/structure_set is the pinned 26.2 jar (salts cross-checked against the embedded JSON). No client input reaches the StartGenerators/PostProcess — they run off-tick on the single scheduler goroutine over (worldSeed, chunkPos). |
| temple PostProcess → cross-chunk WorldGenView | All writes go through the 14-02 placeBlock (clipped to the piece bbox AND the chunk writableBox); out-of-slice writes dropped. No new boundary — reuses the 14-02 mechanism. |
| igloo addChildren → the piece set | The basement is added probabilistically (a fixed 1-3 piece set, NOT recursion); the genDepth bound is inherited from 14-02. No unbounded growth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | a wrong per-temple salt (from memory, not the JSON) landing the temple in the wrong chunk vs a vanilla seed | mitigate | Every salt/spacing/spread_type is cross-checked against the embedded structure_set JSON (TestStructuresPipelineAcceptance derives each expected chunk from getPotentialStructureChunk with the loaded salt); a wrong salt fails the expected-chunk assertion. |
| T-14-14 | Tampering | a temple postProcess block/draw-order divergence (wrong geometry) | mitigate | Each geometry is transcribed javap -c + pinned by Test{JungleTemple,SwampHut,Igloo}Places (the expected block fingerprint); the igloo basement-present probability is pinned by TestIglooBasementProbabilistic. |
| T-14-15 | Integrity (non-reproducible world) | a temple producing a different position/shape for the same seed (incl. the igloo basement draw) | mitigate | Each start is pure over (seed,pos) (Test*StartPure + TestStructuresStartsPure); the piece RNG is re-derivable via SetLargeFeatureSeed; the 5x5 reorder-determinism + emit-once gates re-run byte-identical with all four temples placing. |
| T-14-16 | Tampering | the cross-chunk clip failing for a chunk-spanning temple (double-write/truncation) | mitigate | Reuses the 14-02 placeBlock/placeInChunk clip (proven there); TestStructuresCrossChunkIdempotent re-pins once-per-chunk slice writes for the temple set. |
| T-14-17 | Information disclosure | the temple chests / igloo basement / swamp-hut entities exposing loot or entity data | accept | Loot + entities are v3 deferrals — chests are blocks + tags (no loot rolled), the igloo villager/zombie-villager + swamp-hut witch/cat + trap mobs are not spawned. No data surface. Documented inline. |
| T-14-18 | DoS | the igloo addChildren growing unbounded | accept | IglooPieces is a FIXED 1-3 piece set (dome + optional ladder+basement), not recursion; the genDepth bound is inherited from 14-02. No unbounded growth — mineshaft recursion is Phase 15. |
</threat_model>

<verification>
- `go test ./world/structure/ -run 'TestJungleTemple|TestSwampHut|TestIgloo' -count=1 -race` green: the three temples place jar-exact geometry at their expected (per-salt) chunks in their biomes; the igloo dome always + the basement probabilistically via addChildren; starts pure over (seed,pos).
- `go test ./world/structure/ -run 'TestStructuresPipelineAcceptance|TestStructuresCrossChunkIdempotent|TestStructuresStartsPure' -count=1 -race` green: all four temples land at expected chunks with expected fingerprints, cross-chunk idempotent, STARTS pure.
- `go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce' -count=1` green: the 5x5 reorder-determinism + emit-once stay byte-identical with all four temples placing.
- Docker `-race` over `./world/... ./world/structure/...` clean (CGO golang:1.26 — the intended race-gate toolchain; go.mod targets the go 1.25.0 floor and golang:1.26 is a strict superset, no go.mod bump needed).
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; NO new deps (go.mod/go.sum unchanged); NO encoder/packet/chunk-wire/capture-diff-golden file touched (the v1 wire goldens stay green — grep-confirm).
</verification>

<success_criteria>
- STRUCT-02 (closed): the jungle temple, igloo (the first addChildren multi-piece — dome + probabilistic basement), and swamp hut follow the desert pyramid as cheap single-piece scattered-temple add-ons, all reusing the 14-02 StructurePiece machinery, each generating in vanilla positions (the expected chunk derived from getPotentialStructureChunk per salt) in its correct biome.
- The structures-pipeline acceptance is green (automated, no visual gate — Phase 16): all four scattered temples land at their expected chunks with their expected block fingerprints, the cross-chunk references+clip stays idempotent, STARTS is pure over (seed,pos), the 5x5 reorder-determinism + emit-once stay byte-identical with all four placing, Docker -race clean.
- The structures-pipeline half of Phase 14 is complete (STRUCT-01 + STRUCT-02), ready for Phase 15 (mineshaft/stronghold) to reuse the proven piece + addChildren machinery; loot/entities/trap-logic deferred to v3 (documented, not dropped); zero new deps, no new wire surface.
</success_criteria>

<output>
After completion, create `.planning/phases/14-structure-pipeline-temples/14-03-SUMMARY.md`
</output>
