---
phase: 07-ai-pathfinding-commands-chat
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - server/chat.go
  - server/tick.go
  - server/gameplay_tick.go
  - server/commands.go
  - server/chat_test.go
autonomous: true
requirements: [CMD-02]

must_haves:
  truths:
    - "server/chat.go decodes ServerboundChat DEFENSIVELY (jar order: readUtf(256) message -> readInstant timeStamp -> readLong salt -> readNullable(MessageSignature) -> LastSeenMessages$Update) keeping ONLY the message string and IGNORING the signature/lastSeen (offline mode, server-authoritative); a Scan error is a no-op, never a panic (T-3-02); the 256-char readUtf cap bounds the message"
    - "the message is broadcast to EVERY player via ClientboundSystemChat (jar: Component content [chat.Message NBT] + Boolean overlay) — pk.Marshal(ClientboundSystemChat, chat.Message{Text: \"<name> msg\"}, pk.Boolean(false)) sent to every player's bounded outbound queue (client.Send; the writeLoop stays the sole socket writer); v1 uses SystemChat to SKIP the signed-message chain entirely (resolved Open Question 2)"
    - "the ClientboundPlayerChat globalIndex layout is DOCUMENTED (jar-derived, in a chat.go comment + the SUMMARY) to satisfy CMD-02's literal 'PlayerChat globalIndex prefix handled' ask — write order: globalIndex VarInt (FIELD 1) -> sender UUID -> index VarInt -> nullable MessageSignature -> SignedMessageBody$Packed -> nullable unsignedContent -> FilterMask -> ChatType$Bound — WITHOUT shipping the fragile signed path (deferred to v2/ONLINE)"
    - "a tickPlayer.name field is threaded from AcceptPlayer's login profile (gameplay_tick.go — the name is accepted but NOT stored today) so the broadcast renders the vanilla '<name> message' format; the chat case is added to server/tick.go dispatch (resolved INLINE like ServerboundClientCommand, NOT the subtick buffer — chat is not a timestamped movement input)"
    - "the SystemChat broadcast helper is REUSED by Plan 07-04's command reply path (server/commands.go runCommand replies a command result via this helper) — consolidating the command reply that 07-04 left as a documented stub; everything runs SYNCHRONOUSLY on the tick goroutine (TICK-05); ZERO new dependencies (chat.Message is the fork's NBT Component encoder)"
  artifacts:
    - path: "server/chat.go"
      provides: "handleChat (defensive ServerboundChat decode -> SystemChat fan-out) + broadcastSystemChat(text) + a documented ClientboundPlayerChat globalIndex layout comment"
      contains: "func (t *TickLoop) handleChat"
    - path: "server/tick.go"
      provides: "dispatch EXTENDED with a ServerboundChat case (inline resolve -> handleChat) + tickPlayer.name field"
      contains: "ServerboundChat"
    - path: "server/gameplay_tick.go"
      provides: "AcceptPlayer threads the login profile name into tickPlayer.name (it is accepted but unstored today)"
      contains: "name"
    - path: "server/commands.go"
      provides: "runCommand's reply path now uses broadcastSystemChat/a SystemChat reply (consolidating 07-04's stubbed reply)"
      contains: "broadcastSystemChat"
  key_links:
    - from: "server/tick.go dispatch ServerboundChat"
      to: "server/chat.go handleChat"
      via: "the chat packet is decoded + fanned out on the tick goroutine (inline resolve)"
      pattern: "handleChat"
    - from: "server/chat.go broadcastSystemChat"
      to: "every tickPlayer's client.Send (ClientboundSystemChat)"
      via: "the SystemChat packet is sent to every player's bounded outbound queue"
      pattern: "ClientboundSystemChat"
    - from: "server/gameplay_tick.go AcceptPlayer"
      to: "server/tick.go tickPlayer.name"
      via: "the login profile name is stored so the broadcast renders <name> message"
      pattern: "name"
    - from: "server/commands.go runCommand"
      to: "server/chat.go broadcastSystemChat"
      via: "a command reply is delivered via the SystemChat helper (consolidating 07-04's stub)"
      pattern: "broadcastSystemChat"
---

<objective>
Receive and broadcast chat (CMD-02). Per the resolved Open Question 2, v1 broadcasts via `ClientboundSystemChat` (jar: `Component content` [NBT] + `Boolean overlay`) to SKIP the signed-message chain entirely, decodes `ServerboundChat` defensively (ignoring the signature/lastSeen fields), and DOCUMENTS the `ClientboundPlayerChat` globalIndex layout (jar-derived) to satisfy CMD-02's literal "PlayerChat globalIndex prefix handled" ask — without shipping the fragile signed path (deferred to v2/ONLINE).

This plan: (1) `server/chat.go` — `handleChat` decodes `ServerboundChat` (jar order: `readUtf(256)` message -> `readInstant` timeStamp -> `readLong` salt -> `readNullable(MessageSignature)` -> `LastSeenMessages$Update`), keeps ONLY the message string, and broadcasts a `ClientboundSystemChat` (`chat.Message{Text: "<name> msg"}` + `overlay=false`) to EVERY player's bounded outbound queue; (2) extends `server/tick.go` dispatch with a `ServerboundChat` case (resolved INLINE like `ServerboundClientCommand`, NOT the subtick buffer — chat is not a timestamped movement input) and adds a `tickPlayer.name` field; (3) threads the login profile name from `AcceptPlayer` into `tickPlayer.name` (the name is accepted but NOT stored today) so the broadcast renders the vanilla `<name> message` format; (4) the SystemChat broadcast helper is REUSED by Plan 07-04's command reply path (`runCommand`), consolidating the reply 07-04 left as a documented stub.

Decode is DEFENSIVE: a Scan error is a no-op, never a panic (T-3-02); the 256-char `readUtf` cap bounds the message. Everything is SYNCHRONOUS on the tick goroutine (TICK-05). ZERO new dependencies (`chat.Message` is the fork's NBT Component encoder, already a `pk.FieldEncoder`).

This plan is Wave 2 and depends on 07-04 (Wave 1) ONLY because BOTH edit the `server/tick.go` dispatch switch — sequential to avoid a same-wave file conflict (07-04 adds the command case in Wave 1; 07-05 adds the chat case + the name field in Wave 2). It also consolidates 07-04's reply stub via the shared SystemChat helper.

Purpose: CMD-02 "chat messages are received and broadcast (with the PlayerChat globalIndex prefix handled)" — a real client typing a message and seeing it broadcast to all players is the interactive proof. SystemChat is the "don't ship the signed chain" shortcut; documenting the globalIndex satisfies the literal ask.
Output: server/chat.go (decode + SystemChat broadcast + the documented globalIndex), the dispatch ServerboundChat case + tickPlayer.name, the AcceptPlayer name threading, the runCommand reply consolidation, server/chat_test.go. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-04-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-04-PLAN.md

<interfaces>
<!-- VERIFIED on disk this session. CMD-02 REUSES chat.Message + the tick player set; it adds one serverbound decode + one broadcast. -->

From chat (the NBT Component body — REUSE; already a pk.FieldEncoder, the Phase-2 double-encode bug is fixed):
```go
type Message struct { Text string; /* color/append builders */ } // chat/nbtmessage.go:19 — WriteTo/MarshalNBT = the NBT Component
// chat.Text(...) builder for the broadcast body. This is the ClientboundSystemChat `content` field.
```

From data/packetid (chat ids — ALL VERIFIED present):
```go
packetid.ServerboundChat        // = 9
packetid.ClientboundSystemChat  // = 121 ; Component content + Boolean overlay
packetid.ClientboundPlayerChat  // = 65 ; the signed path (DOCUMENTED, deferred)
packetid.ServerboundChatAck     // ack (decode-and-ignore for v1; confirm necessity in 07-06 capture-diff)
```

From net/packet (the wire codecs — REUSE):
```go
type String string; type Long int64; type Boolean bool; type UUID uuid.UUID
func Marshal(id int32, fields ...FieldEncoder) Packet
func (p Packet) Scan(fields ...FieldScanner) error
// NOTE: readInstant (timeStamp) is a Long(ms) on the wire; readNullable(MessageSignature) = a Boolean-present
// prefix + (if present) a fixed-size signature byte array; LastSeenMessages$Update = a VarInt + a bitset.
// Decode them into throwaway fields (or skip to EOF after the message) — v1 IGNORES them. javap the packet
// for the exact trailing layout; if the Scan mis-frames, decode only the message and tolerate the rest.
```

From server/tick.go (the dispatch switch + the player set to EXTEND — VERIFIED):
```go
func (t *TickLoop) dispatch(c *Client, p pk.Packet) // tick.go:664 — ADD `case packetid.ServerboundChat:` (inline resolve)
type tickPlayer struct { client *Client; entityID int32; uuid uuid.UUID; /* ADD: name string */ ... } // tick.go:222
// loop.players []*tickPlayer (tick-owned) — the broadcast fan-out target. The ServerboundClientCommand case
// (tick.go:739) is the inline-resolve PATTERN to mirror (decode + act in the case body, NOT the subtick buffer).
func (c *Client) Send(p pk.Packet) // client.go:126 — the bounded outbound queue
```

From server/gameplay_tick.go (AcceptPlayer — the login name is accepted but NOT stored; VERIFIED gameplay_tick.go:140-144):
```go
// "The login profile (name/id/pub key/properties/protocol) is accepted but not yet [stored]" (gameplay_tick.go:140)
func ... AcceptPlayer(name string, ...) // the `name` param exists — thread it into tickPlayer.name at registration.
```

From server/commands.go (Plan 07-04 — the reply stub to consolidate):
```go
func (t *TickLoop) runCommand(p *tickPlayer, cmd string) // 07-04: Execute + a DOCUMENTED stubbed reply path
// THIS plan: replace the stub with a SystemChat reply via broadcastSystemChat / a per-player SystemChat send.
```
</interfaces>

<key_facts>
<!-- VERIFIED against 07-RESEARCH (CMD-02 / Pattern 6 / Pitfall 2) + the jar this session. -->
- v1 = SystemChat (resolved Open Question 2, 07-RESEARCH Pattern 6 + Pitfall 2): broadcast via ClientboundSystemChat (Component content NBT + Boolean overlay — TWO fields, no signing) which renders identically as a server message. Do NOT ship signed PlayerChat (it drags MessageSignature + SignedMessageBody$Packed + FilterMask + ChatType$Bound + a chat session the offline client never establishes — Pitfall 2: one wrong field disconnects the client). out := pk.Marshal(int32(packetid.ClientboundSystemChat), chat.Message{Text: "<"+name+"> "+text}, pk.Boolean(false)); for _, pl := range t.players { if pl.client != nil { pl.client.Send(out) } } (07-RESEARCH Code Example).
- ServerboundChat DECODE (07-RESEARCH CMD-02 row 1, jar bytecode): readUtf(256) message -> readInstant() timeStamp -> readLong() salt -> readNullable(MessageSignature) -> LastSeenMessages$Update. Decode all fields DEFENSIVELY but keep ONLY the message string; IGNORE the signature/lastSeen (server is authoritative, offline mode). javap -c net.minecraft.network.protocol.game.ServerboundChatPacket to confirm the trailing layout; if the full Scan mis-frames, decode the leading message string and tolerate the rest. A Scan error -> no-op, never panic (T-3-02). The 256 readUtf cap bounds the message length (ASVS V5).
- DOCUMENT THE PlayerChat globalIndex (07-RESEARCH CMD-02 row 3, the literal CMD-02 ask): ClientboundPlayerChat write order (jar bytecode) = globalIndex VarInt (FIELD 1) -> sender UUID -> index VarInt -> nullable MessageSignature -> SignedMessageBody$Packed (content/timeStamp/salt/lastSeen) -> nullable unsignedContent Component -> FilterMask -> ChatType$Bound (Holder<ChatType> + name Component + Optional<targetName>). Record this in a chat.go comment + the SUMMARY so "globalIndex prefix handled" is satisfied by UNDERSTANDING + DOCUMENTING the layout (globalIndex is field 1) while v1 ships SystemChat. Defer the signed path to v2/ONLINE.
- THREAD tickPlayer.name (the format requirement): the login name is accepted at AcceptPlayer but NOT stored (gameplay_tick.go:140). Add a `name string` to tickPlayer and set it at registration from the AcceptPlayer name param so the broadcast renders the vanilla "<name> message" format. (If threading through the register MESSAGE boundary is needed, mirror how entityID/uuid are threaded — they are claimed at AcceptPlayer and recorded on the tickPlayer.)
- INLINE RESOLVE, NOT SUBTICK (07-RESEARCH Pattern 6 'When to use'): the ServerboundChat case resolves on the tick so the player set is consistent — resolve it INLINE in the dispatch case body (like ServerboundClientCommand at tick.go:739), NOT via the subtick buffer (chat is not a timestamped movement input).
- BROADCAST VIA THE BOUNDED QUEUE (07-RESEARCH Pattern 6 'Critical'): fan out via each player's client.Send (the bounded queue; the writeLoop stays the sole socket writer). All on the tick goroutine.
- ServerboundChatAck (A6): the client may send ServerboundChat(Session)Ack — for v1 decode-and-ignore (route as a no-op). Whether the client needs an ack before accepting SystemChat back is CONFIRMED in the 07-06 capture-diff (A6); v1 starts with no ack and the capture-diff verifies the round-trip.
- CONSOLIDATE 07-04's REPLY STUB: Plan 07-04 left runCommand's reply path as a documented stub (SystemChat lands here). THIS plan replaces it with a SystemChat reply (a per-player SystemChat send via the broadcast helper or a single-target variant) so a command result is delivered to the player. This is the only edit to server/commands.go.
- TICK-OWNED (TICK-05): handleChat + the fan-out run on the tick goroutine; the sends go through the bounded queues. No off-tick game-state access. chat.Message is the fork's NBT Component (already a pk.FieldEncoder; the Phase-2 double-encode bug is fixed).
- DISPATCH FILE CONFLICT (wave ordering): 07-04 (Wave 1) added the ServerboundChatCommand case; THIS plan (Wave 2) adds the ServerboundChat case + the name field — sequential so the two never edit the dispatch switch in the same wave.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/chat.go (handleChat + broadcastSystemChat + the globalIndex doc comment). In server/tick.go ADD the ServerboundChat dispatch case (inline) + the tickPlayer.name field. In server/gameplay_tick.go thread the name into tickPlayer.name. In server/commands.go replace runCommand's reply stub with a SystemChat reply. Create server/chat_test.go. javap ServerboundChatPacket + ClientboundSystemChatPacket + ClientboundPlayerChatPacket first.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted 26.2 client -> tick (chat messages) | The client sends a chat message (+ a signature/lastSeen it claims). A malicious client sends an oversized/malformed payload or a forged signature. The server decodes defensively on the tick, bounds the message at readUtf(256), IGNORES the signature (offline/authoritative), and broadcasts a server-mediated SystemChat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-06 | Denial of Service | oversized / malformed ServerboundChat (huge string, bad signature/lastSeen) | mitigate | readUtf(256) bounds the message; the signature/lastSeen are decoded-and-ignored; a Scan error -> no-op (never panics, T-3-02). Asserted by TestChatMalformed + TestChatLengthBounded. |
| T-7-07 | Spoofing | a forged signature claiming a message is from another player | accept | v1 is offline mode + SystemChat (server-attributed "<name> msg"); the signature is IGNORED (the server is the authority on the sender name from the connection's login profile). Signed PlayerChat (with verification) is v2/ONLINE. Documented (Pitfall 2 / A2). |
| T-7-10 | Denial of Service | chat spam flood | accept | v1 relies on the bounded outbound queue; a per-player chat cooldown (vanilla's chat-spam counter) is a documented post-v1 refinement. The broadcast itself is O(players) per message — bounded. |
| T-7-08 | Tampering/race | the broadcast fan-out touching the player set off the tick goroutine | mitigate | handleChat + the fan-out run on the tick goroutine over the tick-owned players slice (TICK-05); the sends go through the bounded queues (writeLoop sole writer). Proven by the Docker -race gate. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handleChat — defensive ServerboundChat decode + SystemChat broadcast + documented globalIndex (CMD-02)</name>
  <files>server/chat.go, server/chat_test.go</files>
  <read_first>
    - .planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md — CMD-02 (the ServerboundChat decode + SystemChat + the PlayerChat globalIndex rows), Pattern 6, Pitfall 2, the Code Examples, A2/A6.
    - The jar (javap): `javap -c net.minecraft.network.protocol.game.ServerboundChatPacket` (readUtf(256)/readInstant/readLong/readNullable(sig)/LastSeenMessages$Update), `javap net.minecraft.network.protocol.game.ClientboundSystemChatPacket` (content + overlay composite), `javap -c net.minecraft.network.protocol.game.ClientboundPlayerChatPacket` write() (the globalIndex-first 8-field order). Record the trailing ServerboundChat layout precisely.
    - chat/nbtmessage.go — chat.Message (the NBT Component body for SystemChat content).
    - server/client.go — Client.Send (the broadcast target).
  </read_first>
  <behavior>
    CMD-02 (decode + broadcast):
    - Test 1 (TestChatDecodeKeepsMessage): a crafted ServerboundChat (message + timestamp + salt + null signature + empty lastSeen) decodes to the message string (the signature/lastSeen are ignored).
    - Test 2 (TestSystemChatBroadcast): broadcastSystemChat("<bob> hi") sends a ClientboundSystemChat packet (content = the NBT Component, overlay=false) to EVERY player in a test player set (assert each captured client received the packet with the SystemChat id).
    - Test 3 (TestChatMalformed): a malformed ServerboundChat payload Scan-errors to a no-op (no panic, no broadcast).
    - Test 4 (TestChatLengthBounded): an over-long message is bounded by the readUtf(256) cap (no unbounded work).
  </behavior>
  <action>
    1. server/chat.go — `func (t *TickLoop) handleChat(p *tickPlayer, raw pk.Packet)`: decode `var msg pk.String; var ts, salt pk.Long` (+ the trailing signature/lastSeen into throwaway fields per the javap layout, OR decode only the message and tolerate the rest); `if err := raw.Scan(&msg, &ts, &salt /*, sig, lastSeen */); err != nil { return }`; build the line `"<"+p.name+"> "+string(msg)` and call `t.broadcastSystemChat(line)`. `func (t *TickLoop) broadcastSystemChat(text string)`: `out := pk.Marshal(int32(packetid.ClientboundSystemChat), chat.Message{Text: text}, pk.Boolean(false)); for _, pl := range t.players { if pl.client != nil { pl.client.Send(out) } }`. Add a `func (t *TickLoop) sendSystemChat(p *tickPlayer, text string)` single-target variant (for the command reply). Add a HEADER COMMENT documenting the ClientboundPlayerChat globalIndex layout (jar-derived, field-1-is-globalIndex, the full 8-field order) + why v1 uses SystemChat (Pitfall 2). Cite the javap source.
    2. server/chat_test.go (package server) — the 4 tests in <behavior>, using captured-send fake Clients (reuse the prior-phase capture helpers) over a small tick-owned players slice with names set, asserting the SystemChat fan-out + the decode + the malformed/length paths.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestChatDecodeKeepsMessage|TestSystemChatBroadcast|TestChatMalformed|TestChatLengthBounded' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - handleChat decodes ServerboundChat defensively (keeps the message, ignores signature/lastSeen, Scan-error -> no-op) and bounds the message at readUtf(256).
    - broadcastSystemChat fans a ClientboundSystemChat (content + overlay=false) to every player via the bounded queue; sendSystemChat targets one player.
    - The ClientboundPlayerChat globalIndex layout (field 1 = globalIndex, the full 8-field order) is documented in a chat.go comment (CMD-02's literal "prefix handled" ask) with the v1-uses-SystemChat rationale.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps (chat.Message is the fork NBT Component).
  </acceptance_criteria>
  <done>Chat is decoded defensively and broadcast to all players via SystemChat (the signed chain skipped), and the PlayerChat globalIndex layout is documented to satisfy CMD-02's literal ask — the chat core is in place to wire into dispatch (Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route ServerboundChat in dispatch + thread tickPlayer.name + consolidate the 07-04 command reply (CMD-02)</name>
  <files>server/tick.go, server/gameplay_tick.go, server/commands.go, server/chat_test.go</files>
  <read_first>
    - .planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md — CMD-02 (Pattern 6 'When to use': inline resolve on the tick), Pitfall 2.
    - server/tick.go — the dispatch switch (tick.go:664-755): the ServerboundClientCommand inline-resolve case (tick.go:739) to mirror + the tickPlayer struct (tick.go:222, where the name field is added) + the default no-op (tick.go:755).
    - server/gameplay_tick.go — AcceptPlayer (gameplay_tick.go:143): the name param + how entityID/uuid are threaded onto the tickPlayer at registration (mirror for name).
    - server/chat.go (Task 1) — handleChat + sendSystemChat.
    - server/commands.go (Plan 07-04) — runCommand's reply stub to replace.
  </read_first>
  <behavior>
    CMD-02 (the dispatch route + the name + the command reply):
    - Test 1 (TestChatRouted): a ServerboundChat passed to dispatch decodes + broadcasts (the handleChat path runs; every player receives the SystemChat). Resolved INLINE (not buffered).
    - Test 2 (TestChatRendersName): a player whose tickPlayer.name is "bob" produces a "<bob> ..." broadcast (the name is threaded from AcceptPlayer).
    - Test 3 (TestCommandReplyViaSystemChat): runCommand (07-04) now delivers a command result/error to the player via sendSystemChat (the 07-04 stub is consolidated) — assert the player receives a SystemChat reply.
    - Test 4 (TestChatAckIgnored): a ServerboundChatAck/SessionUpdate is a no-op (decode-and-ignore), no panic.
  </behavior>
  <action>
    1. server/tick.go — add `name string` to the tickPlayer struct (tick.go:222). ADD `case packetid.ServerboundChat:` to the dispatch switch (resolve player := t.clientIndex[c]; nil -> no-op; call t.handleChat(player, p)). Resolve INLINE (like ServerboundClientCommand), NOT the subtick buffer. Optionally add a `case packetid.ServerboundChatAck, packetid.ServerboundChatSessionUpdate:` no-op (decode-and-ignore) so they do not fall to the silent default (document).
    2. server/gameplay_tick.go — in AcceptPlayer / the registration path, set the tickPlayer.name from the `name` param (mirror how entityID/uuid are recorded onto the tickPlayer across the register MESSAGE boundary — the name is a value, crosses no tick-owned state).
    3. server/commands.go — replace runCommand's documented reply stub (07-04) with a real reply: on an Execute error (or a result message) call t.sendSystemChat(p, "...") so the player sees the command outcome. This consolidates 07-04's stub via the shared helper.
    4. server/chat_test.go — add the 4 tests in <behavior>, driving dispatch with crafted ServerboundChat packets over a registered tickPlayer with a name, asserting the route + the name rendering + the command-reply consolidation + the ack no-op.
  </action>
  <verify>
    <automated>go test ./server/ -run 'TestChatRouted|TestChatRendersName|TestCommandReplyViaSystemChat|TestChatAckIgnored|TestSystemChatBroadcast|TestChatCommandRouted' -count=1 && go vet ./server/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dispatch routes ServerboundChat (it was NOT routed before — verified default no-op at tick.go:755) and resolves it INLINE on the tick to handleChat (not the subtick buffer); ServerboundChatAck/SessionUpdate are decode-and-ignore no-ops.
    - tickPlayer.name is threaded from AcceptPlayer so the broadcast renders "<name> message".
    - runCommand's reply (07-04 stub) is consolidated to a SystemChat reply via the shared helper.
    - `go build ./...`, `go vet ./server/...` clean; zero new deps.
  </acceptance_criteria>
  <done>A client's chat message is decoded + broadcast to all players via SystemChat with the vanilla "<name> message" format, the command reply path is consolidated, and the PlayerChat globalIndex is documented — CMD-02 is implemented. Run the Docker -race gate over ./server/... before marking the wave done (the broadcast fan-out is tick-owned — TICK-05). The chat/command wire is sealed by the 07-06 capture-diff.</done>
</task>

</tasks>

<verification>
- `go test ./server/ -run 'TestChatDecodeKeepsMessage|TestSystemChatBroadcast|TestChatMalformed|TestChatLengthBounded|TestChatRouted|TestChatRendersName|TestCommandReplyViaSystemChat|TestChatAckIgnored' -count=1` passes.
- The existing server tests + the 07-04 command tests still pass — dispatch + the bootstrap + commands.go are extended, not regressed.
- Docker `-race` gate over `./server/...` is clean (handleChat + the fan-out are tick-owned — TICK-05).
- `go vet ./... && go build ./...` clean; ZERO new dependencies (chat.Message is the fork NBT Component encoder).
</verification>

<success_criteria>
- CMD-02: chat messages are received + broadcast — ServerboundChat is decoded defensively (message kept, signature/lastSeen ignored, readUtf(256) bound) and broadcast to every player via ClientboundSystemChat ("<name> message"); the ClientboundPlayerChat globalIndex prefix layout is DOCUMENTED (jar-derived, field 1) to satisfy the literal ask while v1 skips the signed chain (resolved Open Question 2 / Pitfall 2).
- tickPlayer.name is threaded for the vanilla format; the 07-04 command reply is consolidated via the shared SystemChat helper.
- Tick-owned, single-owner (TICK-05); -race clean; zero new dependencies. The chat/command WIRE is sealed by the 07-06 capture-diff (self-round-trip is insufficient — the standing seal method).
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-pathfinding-commands-chat/07-05-SUMMARY.md`.
</output>
</content>
