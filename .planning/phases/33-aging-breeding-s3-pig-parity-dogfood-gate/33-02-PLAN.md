---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/entity.go
  - server/combat_mob.go
  - server/attack_dispatch.go
  - server/entity_encode.go
  - server/inlove_feed_test.go
autonomous: true
requirements: [MOB-SUB-09]
tags: [breeding, in-love, feed, particles, pig-parity]

must_haves:
  truths:
    - "Feeding pig_food to an ADULT pig (breedAge==0 && canFallInLove) sets inLove=600, consumes 1 item, plays the eat sound"
    - "Feeding pig_food to a BABY pig (breedAge<0, canAgeUp) ages it up toward 0, consumes 1 item, plays the eat sound"
    - "inLove decrements each tick (adult-only) and emits HEART particles every 10 ticks while inLove>0"
    - "A lone un-fed adult pig (inLove==0) is a pure-int no-op: inLove stays 0, no hearts, zero RNG draws"
    - "ClientboundLevelParticles encodes a HEART particle broadcast (the previously-missing encoder)"
  artifacts:
    - path: "server/entity.go"
      provides: "inLove int field + setInLove/canFallInLove/isInLove helpers"
      contains: "inLove int"
    - path: "server/combat_mob.go"
      provides: "the adult-only inLove decrement + heart emit in tickMobAging"
      contains: "inLove"
    - path: "server/attack_dispatch.go"
      provides: "the FEED path in handleInteract (adult→setInLove, baby→ageUp, consume, eat sound)"
      contains: "itemInTag(id, \"pig_food\")"
    - path: "server/entity_encode.go"
      provides: "encodeLevelParticles — the HEART particle wire-out (the GAP this plan closes)"
      contains: "func encodeLevelParticles"
    - path: "server/inlove_feed_test.go"
      provides: "feed-sets-love (adult), feed-ages-up (baby), inLove-decrement tests"
      contains: "func TestFeed"
  key_links:
    - from: "server/attack_dispatch.go handleInteract"
      to: "Entity.inLove / Entity.breedAge"
      via: "isFood(pig_food) → adult setInLove / baby ageUp"
      pattern: "setInLove|breedAge"
    - from: "server/combat_mob.go tickMobAging"
      to: "encodeLevelParticles (hearts)"
      via: "inLove>0 && inLove%10==0 → broadcast HEART to trackers"
      pattern: "inLove%10"
---

<objective>
Build the Animal in-love state and the feed interaction: the `inLove` int (setInLove→600, canFallInLove=inLove<=0, isInLove=inLove>0), the adult-only per-tick inLove decrement + heart-particle emit (folded into Plan A's tickMobAging tail), the FEED path in handleInteract (pig_food + adult → setInLove; pig_food + baby → ageUp; consume 1 + eat sound), and the missing `encodeLevelParticles` encoder for HEART particles.

Purpose: This makes pigs fall in love when fed — the trigger BreedGoal (Plan C) gates on. It MUST keep the pig oracle byte-identical: the inLove decrement is pure int, adult-only, and dormant on the un-fed lone-adult oracle pig (inLove==0). The feed path only fires on a real ServerboundInteract — the oracle never feeds.
Output: inLove field + helpers + the feed path + encodeLevelParticles + feed/inLove tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md

<oracle_gate_strategy>
THE MAKE-OR-BREAK CONSTRAINT:
The oracle pig is an un-fed lone adult: breedAge==0, inLove==0. For THIS plan:
- The inLove decrement is PURE INT, adult-only, and ONLY runs when inLove>0 → dormant on the oracle pig.
- Heart particles emit only when inLove>0 (dormant) AND are a BROADCAST, not an RNG draw — gate-safe even if they fired.
- setInLove fires ONLY from the FEED path (a real ServerboundInteract). The oracle never feeds → inLove stays 0.
- Adding the inLove decrement to tickMobAging keeps it OUTSIDE serverAiStep (same loop as tickMobIFrames).
GATE: run `TestPluginPigEqualsGoNativePig` at the end — it MUST stay green (dormant inLove = no-op).
</oracle_gate_strategy>

<interfaces>
<!-- Verified from the codebase + Plan A. Use directly. -->

From Plan A (server/entity.go + combat_mob.go):
```go
breedAge int                      // <0 baby, >0 cooldown, ==0 adult (Plan A)
func (e *Entity) isBaby() bool    // breedAge < 0 (Plan A)
func (t *TickLoop) tickMobAging(e *Entity)  // the pure-int aging tick — ADD the inLove tail here (combat_mob.go)
```

From server/attack_dispatch.go:717-731 (the v1 no-op to REPLACE):
```go
func (t *TickLoop) handleInteract(p *tickPlayer, pkt pk.Packet) {
	_ = p
	_ = pkt   // ServerboundInteract = right-click: entityId + InteractionHand + Vec3 + Boolean usingSecondaryAction
}
```

From the held-item + consume + sound seams (reuse):
```go
func nearestPlayerHolding(t *TickLoop, e *Entity, maxDist float64, pred func(itemID int32) bool) (x, y, z float64, ok bool)  // ai_goals_passive.go:381 — held-read precedent
// playerHoldsTempt held-read: inv.get(heldWindowSlot(inv.heldSlot)) — ai_goals_passive.go:405-416; block_interact.go:326 heldWindowSlot
// shrinkHeldItem(p, inv) — block_interact.go:339 (consume 1)
func itemInTag(id int32, tag string) bool   // Phase 32 — used at ai_mob.go:264 for "pig_food"
func encodeSoundEntity(soundID int32, source int, entityID int32, volume, pitch float32, seed int64) pk.Packet  // entity_encode.go:749 — playEatingSound seam
```

From handleAttack (same file) — the cross-region owner-resolve precedent for resolving the target *Entity by id:
```go
// handleAttack resolves the victim via the store + t.owningRegion(id); a cross-region target is queued, not mutated inline (region_transfer.go:186 queueDamageIntent).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inLove field + helpers + the adult-only decrement & heart emit</name>
  <read_first>
    - server/entity.go (the breedAge field from Plan A — add inLove beside it)
    - server/combat_mob.go (tickMobAging from Plan A — add the inLove tail)
    - 33-JARNOTES.md:20-32 (Animal: inLove=0; isInLove=inLove>0; canFallInLove=inLove<=0; setInLove→600; aiStep tail `if age!=0 inLove=0; if inLove>0 {--inLove; if %10==0 hearts}`)
    - 33-PATTERNS.md §1 (the field) + §2 (the inLove decrement in the tick tail)
  </read_first>
  <files>server/entity.go, server/combat_mob.go</files>
  <action>
    1. In server/entity.go, add `inLove int` to the per-entity int block right after breedAge (Plan A).
       Doc-comment: Animal.inLove — love-mode countdown (setInLove→600); >0 = in love. Tick-owned plain int,
       zero for non-animals, snapshot-friendly. Cite Animal.
    2. Add three helper methods on *Entity:
       - `func (e *Entity) isInLove() bool { return e.inLove > 0 }`           // Animal.isInLove
       - `func (e *Entity) canFallInLove() bool { return e.inLove <= 0 }`     // Animal.canFallInLove
       - `func (e *Entity) setInLove() { e.inLove = 600 }`                    // Animal.setInLove → 600 (30s)
         (the loveCause/player record is OUT OF SCOPE for v5 — the XP-orb breeder credit is a Plan-C concern;
          cite that setInLove(player) is reduced to the inLove countdown here, NOT baked away.)
    3. In server/combat_mob.go, in tickMobAging (Plan A), append the Animal.aiStep tail AFTER the aging branch
       (jarnotes:26): `if e.breedAge != 0 { e.inLove = 0 }` then `if e.inLove > 0 { e.inLove--; if e.inLove%10 == 0 { t.broadcastHearts(e) } }`.
       This is PURE INT except the broadcastHearts broadcast (added in Task 2) — NO RNG draw. Doc-cite Animal.aiStep tail
       and reaffirm the OUTSIDE-serverAiStep position preserves the oracle stream.
       (broadcastHearts is defined in Task 2 — write the call now; it compiles once Task 2 lands. If you prefer,
        do Task 2 first; the executor may reorder Tasks 1↔2 freely since they share combat_mob.go.)
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inLove int" server/entity.go` returns 1.
    - `grep -c "func (e \*Entity) isInLove\|func (e \*Entity) canFallInLove\|func (e \*Entity) setInLove" server/entity.go` returns 3.
    - `grep -c "inLove%10" server/combat_mob.go` returns 1 (the heart cadence).
    - `grep -c "e.breedAge != 0" server/combat_mob.go` returns 1 (the adult-only inLove=0 guard).
  </acceptance_criteria>
  <done>inLove field + 3 helpers + the adult-only decrement/heart tail exist in tickMobAging; build green (after Task 2's broadcastHearts).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: encodeLevelParticles (the HEART encoder gap) + broadcastHearts</name>
  <read_first>
    - server/entity_encode.go:740-790 (encodeSoundEntity :749 — the framing twin: pk.Marshal(packetid.X, fields...) + broadcast-to-trackers)
    - data/packetid/packetid.go:130 (ClientboundLevelParticles id is defined)
    - server/combat_mob.go (how hurt/death sounds fan out to trackers — copy that broadcast loop)
    - 33-JARNOTES.md:132-136 (the heart-particle gap: ClientboundLevelParticles = particle id + long-dist bool + xyz + offset xyz + maxSpeed + count; HEART id from the generated particle registry)
    - 33-PATTERNS.md §9 (the GAP — write a new encodeLevelParticles, framing twin encodeSoundEntity)
  </read_first>
  <files>server/entity_encode.go, server/combat_mob.go</files>
  <action>
    1. JAVAP-CONFIRM the wire layout (do NOT guess):
       `javap -c -p net.minecraft.network.protocol.game.ClientboundLevelParticlesPacket` (write/STREAM_CODEC)
       — pin the field order: typically `overrideLimiter` (Boolean) + x,y,z (Double) + xDist,yDist,zDist (Float) +
       maxSpeed (Float) + count (Int/VarInt) + the ParticleOptions (the particle-type id VarInt, then any
       per-particle data — HEART has NONE, it is a simple SimpleParticleType). Confirm whether the particle-type
       id is leading or trailing in 26.2 and whether it is VarInt. CITE the bytecode.
    2. Find the HEART particle registry id: grep the generated particle registry (data/ or the codegen output)
       for "heart" / ParticleTypes.HEART. If a particle-id lookup helper exists, use it; otherwise pin the
       VarInt id the same way other registry ids were pinned (cite the source).
    3. Add `func encodeLevelParticles(particleID int32, x, y, z float64, xDist, yDist, zDist, maxSpeed float32, count int32) pk.Packet`
       to server/entity_encode.go, mirroring encodeSoundEntity's pk.Marshal shape with the javap-confirmed field order.
    4. Add `func (t *TickLoop) broadcastHearts(e *Entity)` in server/combat_mob.go (near the sound broadcasts):
       build encodeLevelParticles for minecraft:heart at e.x/e.y/e.z with the vanilla offset/speed/count
       (Animal heart emit: count 1 per emit with small random offset — CHECK the jar's `addParticlesAroundSelf`
        / heart-emit call; if it draws RNG for the offset, that draw must be added to BOTH the Go pig AND both
        .star pigs in lockstep per the oracle rule — but hearts only fire when inLove>0, dormant on the oracle).
       If the vanilla offset uses RNG, port the exact nextDouble draws; if it is a fixed offset, use the constants.
       Broadcast to the mob's trackers using the SAME fan-out combat_mob uses for hurt/death sounds.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func encodeLevelParticles" server/entity_encode.go` returns 1.
    - `grep -c "func (t \*TickLoop) broadcastHearts" server/combat_mob.go` returns 1.
    - encodeLevelParticles doc-comment cites the javap ClientboundLevelParticlesPacket field order and the HEART registry id source.
    - `CGO_ENABLED=0 go build ./server/` exits 0 (Task 1 + Task 2 together compile).
  </acceptance_criteria>
  <done>encodeLevelParticles + broadcastHearts exist (javap-confirmed wire), wired into the inLove%10 cadence; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the FEED path in handleInteract + feed/inLove tests + the oracle gate</name>
  <read_first>
    - server/attack_dispatch.go:710-731 (handleInteract no-op :725; handleAttack above it for the target-resolve + cross-region precedent)
    - server/ai_goals_passive.go:405-416 (playerHoldsTempt held-read) + block_interact.go:326 (heldWindowSlot) + :339 (shrinkHeldItem)
    - server/ai_mob.go:264 (itemInTag(id, "pig_food") usage)
    - 33-JARNOTES.md:92-115 (Animal.mobInteract VERBATIM: isFood → age=getAge; if ServerPlayer && age==0 && canFallInLove → usePlayerItem + setInLove + playEatingSound + SUCCESS_SERVER; if canAgeUp → usePlayerItem + ageUp(getSpeedUpSecondsWhenFeeding(-age)) + playEatingSound; getSpeedUpSecondsWhenFeeding(d)=(int)(d/20*0.1f); usePlayerItem=survival shrink(1))
    - 33-PATTERNS.md §4 (assemble the feed path) + the canAgeUp/getSpeedUpSecondsWhenFeeding note in §"Gaps"
  </read_first>
  <files>server/attack_dispatch.go, server/inlove_feed_test.go</files>
  <action>
    1. Replace the handleInteract no-op (attack_dispatch.go:725-731) with the Animal.mobInteract feed path (jarnotes:92-115):
       a. DECODE the ServerboundInteract: entityId + InteractionHand + Vec3 + Boolean usingSecondaryAction
          (defensive — malformed payload → return silently, keep the existing discipline).
       b. RESOLVE the target *Entity by id via the store (mirror handleAttack's resolve). If the mob is
          cross-region, mirror handleAttack's owner-resolve/queue discipline — do NOT mutate a foreign region's
          mob inline (cite queueDamageIntent / region_transfer.go:186). For the same-region common case (and the
          live-bot test), mutate in-region.
       c. READ the player's held item (heldWindowSlot(inv.heldSlot); guard slotIsEmpty; int32(main.ItemID)) —
          reuse playerHoldsTempt's read.
       d. `if !itemInTag(id, "pig_food") { return }` (Pig.isFood).
       e. ADULT branch: `if e.breedAge == 0 && e.canFallInLove()` → shrinkHeldItem(p, inv) (survival consume 1)
          + e.setInLove() + playEatingSound (encodeSoundEntity for the eat sound — find the id: vanilla
          playEatingSound = SoundEvents.GENERIC_EAT; pin the registry id, cite it). Broadcast to trackers.
       f. BABY branch: `if e.breedAge < 0` (canAgeUp = breedAge<0 && forcedAgeTimer<=0; forcedAgeTimer is a
          const-0 v1 stub — cite it) → shrinkHeldItem + ageUp toward 0 by getSpeedUpSecondsWhenFeeding(-breedAge)
          = `int(float64(-e.breedAge) / 20.0 * 0.1)` (port the (int)(d/20*0.1f) float cast EXACTLY — apply +amount,
          clamped at 0) + playEatingSound. Add the ageUp helper on *Entity if cleaner: `func (e *Entity) ageUp(amount int)`.
       Order MUST match the jar: adult-love branch FIRST, then the baby age-up branch.
    2. Create server/inlove_feed_test.go:
       - `TestFeedAdultSetsLove`: spawn an adult pig (breedAge 0), simulate a feed with a pig_food item held,
         assert e.inLove == 600 and the item shrank by 1.
       - `TestFeedBabyAgesUp`: spawn a baby (breedAge -100), feed, assert breedAge increased toward 0 by the
         getSpeedUpSecondsWhenFeeding amount and the item shrank by 1.
       - `TestFeedNonFoodNoOp`: feed a non-pig_food item, assert inLove stays 0 and no item consumed.
       - `TestInLoveDecrement`: set inLove=20 on an adult, call tickMobAging, assert inLove==19; at %10==0 boundaries
         the heart broadcast does not panic. Set breedAge!=0 → assert inLove forced to 0 (adult-only guard).
       Use the existing test harness (spawnVanillaPig + the held-item setup helpers used in the tempt tests).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestFeed|TestInLove|TestPluginPigEqualsGoNativePig|TestTempt' -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go vet ./server/` exits 0.
    - `grep -v '^[[:space:]]*//' server/attack_dispatch.go | grep -c 'itemInTag(id, "pig_food")'` returns 1 (the feed path is live).
    - `CGO_ENABLED=0 go test ./server/ -run 'TestFeed|TestInLove' -v` shows TestFeedAdultSetsLove, TestFeedBabyAgesUp, TestFeedNonFoodNoOp, TestInLoveDecrement PASS.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -timeout 120s` PASSES (THE GATE — the un-fed oracle pig never sets inLove → dormant → byte-identical).
  </acceptance_criteria>
  <done>The feed path sets love (adult) / ages up (baby) + consumes + eat sound; feed/inLove tests pass; the pig oracle stays byte-identical green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (ServerboundInteract) | UNTRUSTED: the player-sent right-click packet (entityId, hand, vec3) crosses into handleInteract. |
| tick → client (particles/sound broadcast) | server-authored HEART particles + eat sound; outbound only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Spoofing/Tampering | handleInteract target entityId (a forged/foreign id) | mitigate | Resolve via the store; cross-region target uses handleAttack's owner-resolve/queue discipline (no inline foreign-region mutation). Malformed payload → silent ignore. |
| T-33-04 | Tampering | inLove decrement perturbing the oracle RNG stream | mitigate | Adult-only PURE INT, dormant on the un-fed oracle (inLove==0); hearts are a broadcast not an RNG draw. Gated by TestPluginPigEqualsGoNativePig (Task 3). |
| T-33-05 | Denial of service | feed-spam consuming items / spawning particles | accept | Each feed consumes 1 held item (survival); inLove caps at 600; rate is the player's click rate — vanilla-faithful, no amplification. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0.
- `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestFeed|TestInLove|TestAging|TestPluginPigEqualsGoNativePig|TestPluginPigBootLoads|TestTempt|TestVanillaPig' -timeout 240s -v` all PASS.
- `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` empty (no .star change in this plan — confirm still identical).
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/` clean.
- gopls is STALE — trust the compiler.
</verification>

<success_criteria>
- inLove field + setInLove/canFallInLove/isInLove + the adult-only decrement & heart emit exist.
- The FEED path: pig_food + adult → setInLove + consume + eat sound; pig_food + baby → ageUp + consume + eat sound.
- encodeLevelParticles (the HEART encoder gap) exists, javap-confirmed.
- Feed/inLove tests pass; the pig oracle stays byte-identical (the gate); -race clean; no new Go deps.
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-02-SUMMARY.md`
</output>
