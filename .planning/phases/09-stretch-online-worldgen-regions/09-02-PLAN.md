---
phase: 09-stretch-online-worldgen-regions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - world/levelgen/random.go
  - world/levelgen/random_test.go
  - world/levelgen/synth/improved.go
  - world/levelgen/synth/perlin.go
  - world/levelgen/synth/normal.go
  - world/levelgen/synth/blended.go
  - world/levelgen/synth/synth_test.go
autonomous: true
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "world/levelgen/random.go PORTS the Tier-A seeding chain EXACTLY from the unobfuscated jar (temp/cache/26.2-inner.jar, javap): XoroshiroRandomSource (the xoroshiro128++ state, setSeed(long), nextLong, nextBits, the (long) and (long,long) ctors, forkPositional), RandomSupport (the seed mixing — mixStafford13, the 128-bit seed upgrade, seedSlimUuid: how a world seed + a noise registry name → that noise's seed), and the XoroshiroPositionalRandomFactory (at(x,y,z)/fromHashOf(name)/fromSeed) — this is the determinism + parity HINGE; a wrong seed map yields a different (but plausible) world (09-RESEARCH Tier A / Pitfall 1), so it is ported bit-for-bit and TESTED FIRST against known Xoroshiro output vectors. This unchanged primitive set serves the FULL-parity graph (caves/aquifers/ore-veins seed through the SAME factory)"
    - "world/levelgen/synth/ PORTS the Tier-B noise primitives EXACTLY from synth/*.class: ImprovedNoise (single-octave improved-Perlin: the gradient table, noise(x,y,z), gradDot + the fade curve), PerlinNoise (the octave stack over ImprovedNoise: firstOctave, amplitudes[], lowestFreqValueFactor/lowestFreqInputFactor, getValue, wrap/maxValue — seeded via the RandomSource/PositionalRandomFactory), NormalNoise (the 2-PerlinNoise normalized noise the density-function Noise nodes call: create(RandomSource, firstOctave, amplitudes...), getValue(x,y,z), the INPUT_FACTOR/TARGET_DEVIATION/expectedDeviation/valueFactor constants), and BlendedNoise (the legacy old_blended_noise the overworld base_3d_noise uses: xz_scale 0.25, y_scale 0.125, xz_factor 80, y_factor 160, smear_scale_multiplier 8) — each ported constant-for-constant from the bytecode, idiomatic Go, no GPL paste, cite the .class (09-RESEARCH Tier B)"
    - "NormalNoise$NoiseParameters (firstOctave + amplitudes[]) is consumed as DATA, not hand-typed: NormalNoise.create takes the params parsed from the embedded noise/*.json (Wave 1, Plan 09-01) — but THIS plan's tests may construct params inline for golden-sample coverage; the runtime wiring of params-from-JSON lands in Wave 2 (Plan 09-03's RandomState) (09-RESEARCH Tier B NoiseParameters row, Pitfall 2: parse firstOctave+amplitudes, don't hardcode)"
    - "Everything is PURE + deterministic over the seed (PARITY-01 / WORLD-04): all randomness flows from the world seed through the ported Xoroshiro positional factory — NO math/rand, NO time-based seed, NO global mutable state; the same seed → identical noise samples (Pitfall 7), and the Xoroshiro golden-vector test (the cheapest highest-value test) catches the #1 pitfall before any terrain renders"
  artifacts:
    - path: "world/levelgen/random.go"
      provides: "the ported XoroshiroRandomSource + RandomSupport seed mixing + XoroshiroPositionalRandomFactory (the RandomSource interface + at/fromHashOf/fromSeed) — the seed→noise determinism hinge"
      contains: "XoroshiroRandomSource"
    - path: "world/levelgen/synth/normal.go"
      provides: "the ported NormalNoise (create(RandomSource, firstOctave, amplitudes...) + getValue + the deviation/valueFactor constants) — the noise the density Noise nodes call"
      contains: "NormalNoise"
    - path: "world/levelgen/synth/perlin.go"
      provides: "the ported PerlinNoise (octave stack over ImprovedNoise: firstOctave/amplitudes/lowestFreq factors/getValue) seeded via the RandomSource"
      contains: "PerlinNoise"
  key_links:
    - from: "world/levelgen/synth/normal.go NormalNoise.create"
      to: "world/levelgen/random.go RandomSource"
      via: "each NormalNoise is seeded from the positional factory's RandomSource (the seed→noise chain)"
      pattern: "RandomSource"
    - from: "world/levelgen/random.go XoroshiroPositionalRandomFactory.fromHashOf"
      to: "world/levelgen/random.go RandomSupport seed mixing"
      via: "a noise registry name is hashed through the mixing into that noise's seed"
      pattern: "fromHashOf"
    - from: "world/levelgen/synth/perlin.go PerlinNoise"
      to: "world/levelgen/synth/improved.go ImprovedNoise"
      via: "PerlinNoise stacks octaves of ImprovedNoise"
      pattern: "ImprovedNoise"
---

<objective>
Port the Tier-A seeding chain and the Tier-B noise primitives — the FOUNDATION of PARITY-01, exactly testable, ported directly from the unobfuscated jar (the STANDING MANDATE, extended to worldgen). 09-RESEARCH names these as the port targets in strict dependency order: Tier A (Xoroshiro seeding) FIRST because it is the determinism + parity HINGE — a wrong seed map yields a different-but-plausible world you won't notice until a byte-diff fails (Pitfall 1) — then Tier B (the 4 noise primitives) the density graph evaluates.

**Scope note (FULL parity):** these primitives are IDENTICAL whether the target is T0-surface or full parity — the SAME Xoroshiro factory + the SAME 4 noise primitives seed and feed the WHOLE overworld graph, including the cave functions (entrances/noodle/pillars/spaghetti), the Aquifer's barrier/fluid-level noises, and the OreVeinifier's vein noises. So this plan is unchanged from the original; the expansion to full parity happens DOWNSTREAM (the full node-type set in 09-03, Aquifer/OreVein in 09-05, carvers in 09-06, surface/biome in 09-07). This plan's job is the bit-exact foundation everything sits on.

This plan delivers TWO layers: (1) `world/levelgen/random.go` — `XoroshiroRandomSource` (xoroshiro128++) + `RandomSupport` (the seed mixing that maps a world seed + a noise name → that noise's seed) + the `XoroshiroPositionalRandomFactory` (`at`/`fromHashOf`/`fromSeed`), ported bit-for-bit from `javap XoroshiroRandomSource`/`RandomSupport`; (2) `world/levelgen/synth/` — `ImprovedNoise`, `PerlinNoise`, `NormalNoise`, `BlendedNoise`, each ported constant-for-constant from `synth/*.class`. `NormalNoise` is the one the density-function `Noise` nodes actually call (Wave 2); `BlendedNoise` is the legacy `old_blended_noise` the overworld `base_3d_noise` uses.

These are EXACTLY testable — port a primitive, test its output against known vanilla values (the Xoroshiro golden-vector test is the cheapest, highest-value test in the whole phase: it catches the #1 pitfall before any terrain renders). Everything is PURE + deterministic over the seed (no `math/rand`, no global state — Pitfall 7). ZERO new dependencies.

Purpose: the seed→noise chain is the determinism + parity hinge of PARITY-01 (09-RESEARCH "Pattern 3: Seed → noise determinism chain"); the density graph (Wave 2) and everything above it (the chunk fill, aquifer, ore veins, carvers, surface, biomes) sit ON TOP of these primitives — a wrong primitive at the top corrupts everything below it.
Output: world/levelgen/random.go (+ test), world/levelgen/synth/{improved,perlin,normal,blended}.go (+ test). ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md

<interfaces>
<!-- VERIFIED via 09-RESEARCH (Java Sources To Port Tier A/B) + javap this session. This plan is greenfield: a new world/levelgen package + a synth sub-package. It imports NOTHING from the rest of the runtime (pure math). -->

The Java sources to port (javap temp/cache/26.2-inner.jar — the AUTHORITATIVE source, read them):
```
util.RandomSource (interface)                — nextInt/nextLong/nextDouble/nextGaussian/consumeCount/fork/forkPositional
levelgen.XoroshiroRandomSource               — xoroshiro128++ state; setSeed(long); nextLong; nextBits(int); ctor(long) + ctor(long,long); forkPositional
levelgen.RandomSupport                       — mixStafford13; the 128-bit seed upgrade; seedSlimUuid (the seed→per-noise mapping)
levelgen.PositionalRandomFactory (interface) — at(int x,int y,int z); fromHashOf(String name); fromSeed(long)  [+ the Xoroshiro impl]
synth.ImprovedNoise                          — the gradient table; noise(double x,y,z); gradDot; the fade curve (5892 bytes)
synth.PerlinNoise                            — firstOctave; amplitudes[]; lowestFreqValueFactor/InputFactor; getValue; wrap; maxValue (11281 bytes)
synth.NormalNoise                            — create(RandomSource,int firstOctave,double... amplitudes); getValue(x,y,z); INPUT_FACTOR/TARGET_DEVIATION/expectedDeviation; valueFactor
synth.BlendedNoise                           — the legacy main-noise; reads xz_scale/y_scale/xz_factor/y_factor/smear_scale_multiplier (9584 bytes)
synth.NoiseUtils                             — small helpers (biasTowardsExtreme); port if a node needs it (1602 bytes)
```
NOTE: synth.SimplexNoise / PerlinSimplexNoise are DEFERRED (legacy/feature noise, not the overworld density router). levelgen.LegacyRandomSource is ported ONLY IF BlendedNoise's internals require it (overworld is legacy_random_source:false; confirm via javap whether BlendedNoise constructs a LegacyRandomSource internally and port only that subset).

The Wave-1 DATA seam (Plan 09-01 — the noise params as DATA):
```go
// world/levelgen/data (Plan 09-01) exposes the noise/*.json octave params:
func Noise(id string) ([]byte, error)  // minecraft:temperature → {firstOctave:-10, amplitudes:[1.5,0,1,0,0,0]}
```
This plan's NormalNoise.create takes (firstOctave int, amplitudes []float64) — the RUNTIME wiring of params-from-data.Noise(...) lands in Wave 2 (09-03 RandomState); this plan's golden tests MAY construct params inline.
</interfaces>

<key_facts>
<!-- VERIFIED against 09-RESEARCH (Tier A/B, Pattern 3, Pitfalls 1/2/7) + javap this session. -->
- PORT EXACTLY, DON'T APPROXIMATE (the STANDING MANDATE, STATE.md, extended to worldgen): javap the named classes from temp/cache/26.2-inner.jar and translate their ALGORITHM constant-for-constant into idiomatic Go. NOT a GPL paste. The constants (gradient tables, INPUT_FACTOR, the BlendedNoise scales, the Stafford mix constants) MUST be read from the bytecode, not memory.
- TIER A IS THE PARITY HINGE — PORT + TEST FIRST (09-RESEARCH Tier A 'Why FIRST' + Pitfall 1): 26.2 overworld uses Xoroshiro (legacy_random_source:false), NOT the legacy LCG. Write the Xoroshiro golden-vector test (reproduce a few known nextLong/nextInt outputs for a fixed seed) BEFORE touching noise. If Tier A is wrong, every higher tier produces a different world and you won't notice until a seed-heightmap diff fails. The positional fromHashOf(name) mixing (RandomSupport) is subtle — port it exactly; carry the noise's registry name into its seed exactly as RandomState does (Wave 2 wires that).
- xoroshiro128++ (Tier A): the state is two int64 (seedLo/seedHi); setSeed upgrades a single long via RandomSupport's mixStafford13 + the golden-ratio constant. Go has no int64 overflow UB — use uint64 for the rotations/adds (bits.RotateLeft64) and convert at the boundaries; match Java's long arithmetic (Java long == Go int64/uint64 two's-complement). nextBits/nextInt/nextLong/nextDouble must match Java's exact bit extraction.
- TIER B PRIMITIVES (09-RESEARCH Tier B + Pitfall 2): ImprovedNoise = single-octave improved-Perlin (gradient table + fade curve + gradDot). PerlinNoise = octave stack over ImprovedNoise (firstOctave often NEGATIVE — e.g. temperature -10; the lowestFreqValueFactor/lowestFreqInputFactor are exact constants). NormalNoise = 2 PerlinNoise (first+second) normalized — the one density Noise nodes call; its expectedDeviation/INPUT_FACTOR/TARGET_DEVIATION/valueFactor are exact. BlendedNoise = the legacy old_blended_noise (base_3d_noise: xz_scale 0.25, y_scale 0.125, xz_factor 80, y_factor 160, smear_scale_multiplier 8). Off-by-one on firstOctave / a missing amplitude / wrong lowestFreq factor → right terrain CHARACTER but wrong SCALE (Pitfall 2).
- PARAMS ARE DATA, NOT HARDCODED (Pitfall 2): NormalNoise.create takes (firstOctave, amplitudes) — these come from the Wave-1 embedded noise/*.json at runtime (Wave 2 wires it). Do NOT bake amplitude arrays into the primitive. This plan's tests may pass params inline for golden coverage.
- PURE + DETERMINISTIC (PARITY-01 / WORLD-04 / Pitfall 7): NO math/rand, NO time seed, NO global mutable state, NO map-iteration-order dependence. Same seed → identical samples. The primitives are stateless after construction (the gradient table is seeded once from the RandomSource).
- TESTABILITY (09-RESEARCH Validation — Tier A/B golden tests): the Xoroshiro golden-vector test (TestXoroshiro: known seed → known nextLong/nextInt sequence) and a NormalNoise golden-sample test (TestNormalNoise: known seed+params+coords → a known value, DERIVABLE by running the same math, or asserting determinism + expected range/deviation if exact vanilla goldens aren't capturable). Where an exact vanilla golden isn't derivable, assert (a) determinism (same input → same output across two constructions) and (b) the documented statistical properties (mean≈0, within expectedDeviation) — and note in the SUMMARY which are exact-golden vs property-based.
- ZERO NEW DEPS: stdlib math + math/bits only. No xsync/ants/conc (Phase 8 substrate; irrelevant — worldgen is already off-tick via the existing worker).
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create world/levelgen/random.go (RandomSource interface + XoroshiroRandomSource + RandomSupport + the positional factory) + random_test.go FIRST (the golden-vector test). Then world/levelgen/synth/{improved,perlin,normal,blended}.go + synth_test.go. Cite the javap source class in each file header. Do NOT touch any other runtime file.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) — pure deterministic math over the world seed | The noise primitives take a server-owned world seed + a server-clamped chunk position and produce deterministic samples. No client input reaches them; no goroutine boundary is crossed (the existing off-tick worker calls Generate). The only integrity concern is determinism (same seed → same world). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-03 | Integrity (world corruption) | non-deterministic generation (wrong/leaky RNG) breaking same-seed→same-world | mitigate | ALL randomness flows from the world seed through the ported Xoroshiro positional factory — NO math/rand, NO time seed, NO global mutable state (Pitfall 7). Asserted by the Xoroshiro golden-vector test + a determinism test (two constructions → identical samples). |
| T-9-04 | Integrity (silent parity divergence) | a wrong Xoroshiro/RandomSupport mix yielding a different-but-plausible world | mitigate | The seeding is ported bit-for-bit from javap and tested FIRST against known output vectors (Tier A 'Why FIRST' / Pitfall 1) before any higher tier is built on it. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the Tier-A seeding chain (Xoroshiro + RandomSupport + PositionalRandomFactory) — test FIRST (PARITY-01)</name>
  <files>world/levelgen/random.go, world/levelgen/random_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Java Sources To Port Tier A (the row-by-row method surface), Tier A 'Why FIRST', Pattern 3 (the seed→noise chain), Pitfall 1 (wrong seeding) + Pitfall 7 (determinism).
    - The jar (javap, temp/cache/26.2-inner.jar): `javap -p net.minecraft.world.level.levelgen.XoroshiroRandomSource` (the state, setSeed, nextLong, nextBits, the two ctors, forkPositional), `javap -p net.minecraft.world.level.levelgen.RandomSupport` (mixStafford13, the 128-bit upgrade, seedSlimUuid), `javap net.minecraft.util.RandomSource` (the interface surface), `javap -p net.minecraft.world.level.levelgen.PositionalRandomFactory` + the Xoroshiro impl (at/fromHashOf/fromSeed). Read the CONSTANTS (the golden-ratio + Stafford constants) from the bytecode. Record any divergence from the research in the SUMMARY.
  </read_first>
  <behavior>
    PARITY-01 (Tier A — the determinism + parity hinge):
    - Test 1 (TestXoroshiro): a XoroshiroRandomSource seeded with a fixed long reproduces a known sequence of nextLong/nextInt/nextDouble outputs (the golden vectors — derive by tracing the bytecode math or, where capturable, from a reference). This is the #1-value test: it catches wrong seeding before any terrain.
    - Test 2 (TestRandomSupportSeedMix): RandomSupport's seed mixing maps a fixed (seed, name) deterministically — fromHashOf("minecraft:temperature") with a fixed world seed yields a stable, reproducible RandomSource state (assert determinism + the documented mix behavior).
    - Test 3 (TestPositionalFactoryDeterministic): the positional factory's at(x,y,z) and fromHashOf(name) are PURE — the same inputs yield identical downstream RandomSource state across two constructions (Pitfall 7).
    - Test 4 (TestNoGlobalRNG): constructing two factories from the same seed and drawing the same sequence yields identical results — proving no global/time state leaks in.
  </behavior>
  <action>
    1. world/levelgen/random.go — package levelgen. Define `type RandomSource interface { ... }` (the subset the noise stack needs: NextInt/NextLong/NextDouble/Fork/ForkPositional + consumeCount if a primitive needs it). Port `XoroshiroRandomSource` (the two-uint64 state; SetSeed via the RandomSupport upgrade; NextBits/NextInt/NextLong/NextDouble matching Java's bit extraction; the (long) and (long,long) ctors; ForkPositional). Port `RandomSupport` (mixStafford13, the 128-bit seed upgrade, the seedSlimUuid/seed→per-noise mapping) — use uint64 + math/bits.RotateLeft64 for the rotations and match Java long two's-complement arithmetic. Port the `PositionalRandomFactory` interface + the Xoroshiro impl (At(x,y,z), FromHashOf(name), FromSeed(seed)). Cite the javap class in the file header.
    2. world/levelgen/random_test.go (package levelgen) — the 4 tests in <behavior>, with the golden-vector sequences (document how each was derived: bytecode-traced vs reference-captured). The Xoroshiro golden test is written FIRST.
  </action>
  <verify>
    <automated>go test ./world/levelgen/ -run 'TestXoroshiro|TestRandomSupportSeedMix|TestPositionalFactoryDeterministic|TestNoGlobalRNG' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - XoroshiroRandomSource reproduces the golden nextLong/nextInt/nextDouble vectors for a fixed seed (Tier A correct — Pitfall 1 closed).
    - RandomSupport maps (seed, noise name) → a stable RandomSource state; the positional factory is PURE (same input → same state, no global/time leak — Pitfall 7).
    - The port cites the javap source; constants are bytecode-derived; idiomatic Go (uint64 + math/bits), no GPL paste.
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The Tier-A seeding chain is ported bit-for-bit and TESTED FIRST against known vectors — the determinism + parity hinge is in place for the noise primitives (Task 2) to seed from.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the Tier-B noise primitives (Improved/Perlin/Normal/Blended) seeded from Tier A (PARITY-01)</name>
  <files>world/levelgen/synth/improved.go, world/levelgen/synth/perlin.go, world/levelgen/synth/normal.go, world/levelgen/synth/blended.go, world/levelgen/synth/synth_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Java Sources To Port Tier B (the row-by-row primitive surface + constants), Pitfall 2 (octave/amplitude/firstOctave mismatch → wrong scale).
    - The jar (javap): `javap -p net.minecraft.world.level.levelgen.synth.ImprovedNoise` (gradient table, noise(x,y,z), gradDot, fade), `javap -p ...synth.PerlinNoise` (firstOctave, amplitudes, lowestFreqValueFactor/InputFactor, getValue, wrap, maxValue), `javap -p ...synth.NormalNoise` (create(RandomSource,int,double...), getValue, INPUT_FACTOR/TARGET_DEVIATION/expectedDeviation/valueFactor), `javap -p ...synth.BlendedNoise` (the xz_scale/y_scale/xz_factor/y_factor/smear_scale_multiplier reads). Confirm whether BlendedNoise constructs a LegacyRandomSource internally (port only that subset if so). Read the CONSTANTS from the bytecode.
    - world/levelgen/random.go (Task 1) — the RandomSource the primitives seed from.
  </read_first>
  <behavior>
    PARITY-01 (Tier B — the noise primitives the graph evaluates):
    - Test 1 (TestImprovedNoise): an ImprovedNoise seeded from a fixed RandomSource returns a deterministic value at fixed coords; two constructions from the same seed agree (determinism); the output is within the documented bound.
    - Test 2 (TestPerlinNoiseOctaves): a PerlinNoise built with a known firstOctave + amplitudes (incl. a NEGATIVE firstOctave like -10) is deterministic at fixed coords and its maxValue matches the documented octave formula (catching the Pitfall-2 scale errors).
    - Test 3 (TestNormalNoise): NormalNoise.create(rs, firstOctave, amplitudes...) is deterministic at fixed coords; over many samples the mean ≈ 0 and the spread is consistent with expectedDeviation/valueFactor (the density Noise nodes call this). Where an exact vanilla golden is derivable, assert it; otherwise assert determinism + the statistical property and note it.
    - Test 4 (TestBlendedNoise): BlendedNoise with the base_3d_noise scales (xz_scale 0.25, y_scale 0.125, xz_factor 80, y_factor 160, smear 8) is deterministic at fixed coords and within bound.
  </behavior>
  <action>
    1. world/levelgen/synth/improved.go — port ImprovedNoise (the gradient table from the bytecode, noise(x,y,z), gradDot, the fade curve), seeded from a RandomSource. Cite the .class.
    2. world/levelgen/synth/perlin.go — port PerlinNoise (the octave stack over ImprovedNoise: firstOctave + amplitudes[] + lowestFreqValueFactor/lowestFreqInputFactor + getValue + wrap + maxValue), seeded via the RandomSource. firstOctave may be negative. Cite the .class.
    3. world/levelgen/synth/normal.go — port NormalNoise: `func NewNormalNoise(rs levelgen.RandomSource, firstOctave int, amplitudes []float64) *NormalNoise` (the create signature) + getValue(x,y,z) using the two inner PerlinNoise (first+second) + the INPUT_FACTOR/TARGET_DEVIATION/expectedDeviation/valueFactor constants. Do NOT hardcode amplitudes (they are DATA — Wave 1's noise/*.json, wired in Wave 2). Cite the .class.
    4. world/levelgen/synth/blended.go — port BlendedNoise (the legacy main noise) reading the base_3d_noise scales. Port the minimal LegacyRandomSource subset ONLY if BlendedNoise's ctor needs it (document). Cite the .class.
    5. world/levelgen/synth/synth_test.go (package synth) — the 4 tests in <behavior>; document each golden as exact-vanilla-derived vs property-based.
  </action>
  <verify>
    <automated>go test ./world/levelgen/synth/ -run 'TestImprovedNoise|TestPerlinNoiseOctaves|TestNormalNoise|TestBlendedNoise' -count=1 && go vet ./world/levelgen/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ImprovedNoise/PerlinNoise/NormalNoise/BlendedNoise are ported constant-for-constant from synth/*.class (gradient table, octave factors, the deviation constants, the base_3d_noise scales), seeded from the Tier-A RandomSource — deterministic, idiomatic Go, no GPL paste.
    - NormalNoise.create takes (firstOctave, amplitudes) as parameters (DATA, not hardcoded — Pitfall 2); PerlinNoise handles a negative firstOctave + the correct maxValue.
    - The golden/determinism tests pass; each golden is documented as exact-vanilla vs property-based.
    - `go build ./...`, `go vet ./world/levelgen/...` clean; zero new deps.
  </acceptance_criteria>
  <done>The 4 noise primitives are ported (seeded from Tier A) and tested — the Tier-B foundation the density-function Noise/old_blended_noise nodes (Wave 2), the Aquifer noises, the OreVeinifier noises, and the Climate biome noises (later waves) all evaluate is in place.</done>
</task>

</tasks>

<verification>
- `go test ./world/levelgen/ -run 'TestXoroshiro|TestRandomSupportSeedMix|TestPositionalFactoryDeterministic|TestNoGlobalRNG' -count=1` passes (Tier A — the golden-vector + determinism tests).
- `go test ./world/levelgen/synth/ -run 'TestImprovedNoise|TestPerlinNoiseOctaves|TestNormalNoise|TestBlendedNoise' -count=1` passes (Tier B).
- `go vet ./... && go build ./...` clean; ZERO new dependencies (stdlib math + math/bits only).
- (No -race needed yet — the primitives are pure/stateless after construction; the determinism/-race sanity over the assembled generator lands in the final wave.)
</verification>

<success_criteria>
- PARITY-01 (Tier A + B foundation): the Xoroshiro seeding chain (XoroshiroRandomSource + RandomSupport + PositionalRandomFactory) and the 4 noise primitives (Improved/Perlin/Normal/Blended) are ported bit-for-bit from the javap-read jar — the determinism + parity HINGE, tested FIRST against known vectors (Pitfall 1).
- Everything is PURE + deterministic over the world seed (no math/rand, no global state — Pitfall 7); NormalNoise params are DATA, not hardcoded (Pitfall 2); idiomatic Go, no GPL paste, javap-cited.
- Zero new dependencies; this is the foundation the FULL graph (Wave 2 node types + the cave functions), the Aquifer/OreVeinifier (Wave 5), the carvers (Wave 6), and the Climate biome source (Wave 7) all sit on.
</success_criteria>

<output>
After completion, create `.planning/phases/09-stretch-online-worldgen-regions/09-02-SUMMARY.md`.
</output>
