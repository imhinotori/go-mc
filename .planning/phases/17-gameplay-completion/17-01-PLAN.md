---
phase: 17-gameplay-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tick.go
  - server/tick_phases.go
  - server/play_join.go
  - server/gameplay_tick.go
  - server/player_visibility.go
  - server/inventory_join.go
  - server/fluid.go
  - server/fall_damage.go
  - server/player_visibility_test.go
  - server/position_load_test.go
  - server/inventory_join_test.go
autonomous: true
requirements: [GAMEPLAY-01, GAMEPLAY-02, GAMEPLAY-03]
must_haves:
  truths:
    - "A second player joining is added to the entity store as an entity.Player (ID 156) with id == p.entityID"
    - "Each tick the player Entity's position is synced from the authoritative tickPlayer before tracker.Tick"
    - "The joining player's PlayerInfoUpdate(ADD_PLAYER) is broadcast to every OTHER player, and existing players' entries are sent to the joiner"
    - "On leave the player Entity is removed from the store and PlayerInfoUpdate(REMOVE_PLAYER) is broadcast"
    - "A reconnecting player's persisted x/y/z is applied to the single bootstrap teleport (no second teleport)"
    - "ContainerSetContent is sent exactly once on the first tick after register"
  artifacts:
    - path: "server/player_visibility.go"
      provides: "player Entity construction, per-tick pos-sync, tab-list broadcast at join/leave, entity-id->tickPlayer reverse lookup"
      min_lines: 60
    - path: "server/inventory_join.go"
      provides: "first-tick ContainerSetContent join-sync guarded by a per-player flag"
      min_lines: 15
  key_links:
    - from: "server/tick.go drainRegistrations"
      to: "t.entities.add(playerEntity) + broadcastPlayerInfoAdd"
      via: "join seam"
      pattern: "entities\\.add"
    - from: "server/tick_phases.go tickEntities"
      to: "t.entities.move(pe, p.x, p.y, p.z)"
      via: "per-tick pos sync before tracker"
      pattern: "syncPlayerEntities|entities\\.move"
    - from: "server/play_join.go sendPlayBootstrap"
      to: "persisted Pos teleport"
      via: "bootstrapParams.spawn{X,Y,Z}"
      pattern: "params\\.spawn"
---

<objective>
GAMEPLAY-01 (the keystone) + GAMEPLAY-02 + GAMEPLAY-03: wire the three player-join seams whose core logic already exists but was never connected. This plan ALSO owns every shared tick-pipeline file (tick.go, tick_phases.go) and creates the Wave-2 SEAM FILES (fluid.go, fall_damage.go) as compiling stubs, so that Wave-2 plans 17-02 (fluid) and 17-03 (damage) OVERWRITE their own dedicated stub file with zero edit to any shared tick-pipeline file — eliminating all Wave-2 file conflicts.

Purpose: Players are invisible to each other (GAMEPLAY-01), positions reset on reconnect (GAMEPLAY-02), and inventory windows are empty on join (GAMEPLAY-03). All three are single missing wiring seams. GAMEPLAY-01 is the keystone — GAMEPLAY-04 (PvP target resolution) and GAMEPLAY-06 (item-drop broadcast) depend on player/entity Entities existing in the store.
Output: A second real client sees the first move; reconnecting players spawn at their saved position; the inventory window populates on join. Plus the struct fields, hook call sites, and the two stub seam files Wave 2 overwrites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gameplay-completion/17-RESEARCH.md

<interfaces>
<!-- Verified against the live code this planning session. Use these directly — no exploration needed. -->

entity store (server/entity_store.go) — tick-owned, plain maps, mutate ONLY on the tick goroutine:
  func (s *entityStore) add(e *Entity)                 // overwrites on duplicate id (do not double-add)
  func (s *entityStore) remove(id int32)               // missing id is a no-op
  func (s *entityStore) move(e *Entity, x, y, z float64) // re-buckets on column cross (the pos-sync path)
  func (s *entityStore) get(id int32) (*Entity, bool)
  func (s *entityStore) near(x, z float64, rangeChunks int) []*Entity

Entity (server/entity.go) — fields are unexported; construct in-package:
  type Entity struct { id int32; typ entity.ID; uuid uuid.UUID; x,y,z float64; vx,vy,vz float64;
                       yaw,pitch,headYaw float32; onGround bool; width,height float64; metadata []byte; ai *mobAI }
  func NewEntity(id int32, t entity.Entity, x, y, z float64) *Entity  // assigns a FRESH uuid — NOT usable for a player (needs p.uuid). Construct the player Entity directly with id==p.entityID, uuid==p.uuid.

data/entity records (data/entity/entity.go): entity.Player (ID 156, W 0.6, H 1.8, line 1424); entity.Item (ID 71, W 0.25, line 659)

tickPlayer (server/tick.go:264) — tick-owned fields you READ here:
  entityID int32; uuid uuid.UUID; name string; x,y,z float64; yaw,pitch float32; onGround bool;
  client *Client; center level.ChunkPos; sentChunks map[...]bool; centerSent bool; inventory *Inventory;
  awaitingTeleport int; confirmedTeleport bool
  (ADD new fields in this plan — see Task 0: playerEntity *Entity; headYaw float32; bootstrapped bool; fallDistance float64; wasOnGround bool; lastY float64)

tracker (server/tracker.go:45) entityTracker.Tick(): iterates t.players, near()s the store, SKIPS e.id==p.entityID,
  emits encodeAddEntity/encodeSetEntityData/encodeTeleportEntity/encodeRotateHead/encodeRemoveEntities. ENTITY-TYPE-AGNOSTIC — it already works for a player Entity; the fix is DATA (put players in the store), not tracker logic.

drainRegistrations (server/tick.go:699): the join/leave seam. Join appends to t.players + t.clientIndex. removePlayer (tick.go:721) swap-removes.
tickEntities (server/tick_phases.go:152): runs AFTER tickChunks, BEFORE tracker.Tick. The pos-sync seam. Calls t.tickDebug() at its end.
tickWorld (server/tick_phases.go:113): currently `func (t *TickLoop) tickWorld() { t.trace("tickWorld") }` — the fluid call site (this plan adds the dispatcher call; 17-02 fills the seam file).
sendPlayBootstrap (server/play_join.go:480): hardcodes spawn 8.5/surfaceY+2/8.5 via bootstrapParams. The GAMEPLAY-02 seam.
AcceptPlayer (server/gameplay_tick.go:160-259): loadPlayer applies health/food/sat at :251-257 but EXPLICITLY skips Pos (:246 "NOT applied for v1"). The GAMEPLAY-02 load seam.
sendContent (server/inventory.go:80): builds+sends authoritative ContainerSetContent. Exists, correct, never called on join (GAMEPLAY-03).
writePlayerInfoUpdateAdd (server/play_join.go:337): builds a SINGLE self-entry PlayerInfoUpdate(ADD). playerInfoEntriesEncoder (play_join.go:356) writes one entry. Generalize for broadcast (a list of entries) + a REMOVE_PLAYER builder.
loadPlayer (server/persistence.go:131) returns (save.PlayerData, ok). save.PlayerData.Pos is [3]float64; .Rotation is [2]float32.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave-0 test scaffolds + shared TickLoop struct fields + tick_phases.go hooks + the two Wave-2 stub seam files</name>
  <files>server/player_visibility_test.go, server/position_load_test.go, server/inventory_join_test.go, server/tick.go, server/tick_phases.go, server/fluid.go, server/fall_damage.go</files>
  <read_first>
    - server/tick.go:264-423 (the tickPlayer struct — add fields here) AND the TickLoop struct definition (grep `type TickLoop struct` in tick.go — add the fluidSchedule field here as a nil-safe zero value)
    - server/tick_phases.go:113 (tickWorld stub) and :152-164 (tickEntities, which ends with t.tickDebug())
    - server/combat_test.go (existing test style: table tests, how a TickLoop+tickPlayer is built in tests — REUSE its helpers, do NOT redeclare them)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md "Validation Architecture" → "Phase Requirements → Test Map" and "Wave 0 Gaps"
  </read_first>
  <behavior>
    - TestPlayerVisibility: two tickPlayers registered → after drainRegistrations both have a playerEntity in t.entities (get(p.entityID) ok); after a tick the entity pos equals the player pos; after removePlayer the entity is gone from the store.
    - TestPositionLoadApplied: an AcceptPlayer with a persisted .dat (Pos = {100.5, 70, -40.5}) → the resulting tickPlayer.x/y/z == the persisted pos AND p.center == the chunk column of that pos (NOT {0,0}).
    - TestInventoryJoinSync: a registered player's bootstrapped flag is false initially; after the first-tick hook runs once, ContainerSetContent was sent exactly once and bootstrapped==true; a second tick sends nothing.
  </behavior>
  <action>
    Write the three `_test.go` scaffolds as RED tests (they will not compile/pass until Tasks 1-3 land — that is the Nyquist gate). Use the existing combat_test.go / subtick_test.go test-construction helpers; do NOT redeclare shared helpers (STALE gopls "redeclared" diagnostics are FALSE — trust `go build`).

    THEN set up the shared tick-pipeline state and the Wave-2 seam files:
    1. On `tickPlayer` (tick.go:264 struct) ADD these six fields, declared HERE so NO Wave-2 plan ever edits tick.go's tickPlayer:
       - `playerEntity *Entity` — the player's store Entity, set at register (this plan, Task 1)
       - `headYaw float32` — synced with yaw for the player Entity (Task 1)
       - `bootstrapped bool` — GAMEPLAY-03 first-tick guard (Task 3)
       - `fallDistance float64` — 17-03 fall-damage accumulator (USED by 17-03)
       - `wasOnGround bool` — 17-03 landing-edge detector (USED by 17-03)
       - `lastY float64` — 17-03 previous-tick y for the per-tick descent delta (USED by 17-03)
    2. On the `TickLoop` struct (grep `type TickLoop struct` in tick.go) ADD: `fluidSchedule *fluidScheduleQueue` (the type is declared in the fluid.go stub below; a nil queue is a no-op drain). Doc comment: "// 17-02 GAMEPLAY-05 wires this at SetWorld; nil drains to nothing."
    3. In `tick_phases.go` tickWorld(): after `t.trace("tickWorld")`, ADD the call `t.tickFluids()`. tickWorld stays Wave-1-owned and is NOT edited by any Wave-2 plan.
    4. In `tick_phases.go` tickEntities(): after `t.tickDebug()`, ADD in order: `t.syncPlayerEntities()` (this plan, Task 1), `t.syncJoinInventories()` (this plan, Task 3), `t.tickFallDamage()` (the dispatcher; the body lives in fall_damage.go). tickEntities stays Wave-1-owned.
    5. CREATE server/fluid.go as a STUB SEAM FILE this plan owns and 17-02 OVERWRITES: declare `type fluidScheduleQueue struct{}` (empty stub) and `func (t *TickLoop) tickFluids() {}` (no-op). Add a header comment: "// STUB created by 17-01 so the tick pipeline compiles. 17-02 (GAMEPLAY-05) OWNS and OVERWRITES this file with the real FlowingFluid port + scheduled-tick queue. tick_phases.go (the call site) is NOT touched by 17-02."
    6. CREATE server/fall_damage.go as a STUB SEAM FILE this plan owns and 17-03 OVERWRITES: declare `func (t *TickLoop) tickFallDamage() {}` (no-op). Header comment: "// STUB created by 17-01. 17-03 (GAMEPLAY-04) OWNS and OVERWRITES this file with the real fall-damage body. tick_phases.go (the call site) is NOT touched by 17-03."
    WAVE-2 CONFLICT ELIMINATION: because tickFluids/tickFallDamage are DEFINED in their own dedicated files (fluid.go / fall_damage.go) — not inline in tick_phases.go — 17-02 overwrites fluid.go and 17-03 overwrites fall_damage.go (DISJOINT files), and NEITHER edits tick.go or tick_phases.go. 17-02's only other edit is wiring the fluidSchedule init at SetWorld — confirm in Task 0 whether SetWorld lives in a file 17-02 can own exclusively (grep `func.*SetWorld`); if SetWorld is in a shared file, 17-02 instead lazy-inits the queue inside tickFluids (nil-check), so 17-02 touches ONLY fluid.go. Document the chosen init point in this SUMMARY.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestPlayerVisibility|TestPositionLoadApplied|TestInventoryJoinSync' 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./server/...` exits 0 (the two stub seam files compile)
    - server/tick.go contains `playerEntity *Entity`, `bootstrapped bool`, `fallDistance float64`, `wasOnGround bool`, `lastY float64` on tickPlayer and `fluidSchedule` on TickLoop
    - server/tick_phases.go tickEntities contains `syncPlayerEntities()`, `syncJoinInventories()`, `tickFallDamage()` and tickWorld contains `tickFluids()`
    - server/fluid.go and server/fall_damage.go exist as compiling stubs with the "17-02/17-03 OVERWRITES this file" header comments
    - the three test files exist (RED acceptable here; Tasks 1-3 turn them GREEN)
  </acceptance_criteria>
  <done>Struct fields + hooks in tick_phases.go (Wave-1-owned) + two dedicated stub seam files (fluid.go, fall_damage.go) that Wave-2 plans overwrite disjointly. Zero Wave-2 shared-file overlap by construction. Test scaffolds exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: GAMEPLAY-01 — player Entity in the store + per-tick pos-sync + bidirectional tab-list broadcast + reverse lookup</name>
  <files>server/player_visibility.go, server/tick.go, server/play_join.go</files>
  <read_first>
    - server/tracker.go:45-108 (the generic entityTracker.Tick — confirm it skips e.id==p.entityID and reads e.x/e.z for near + e.x/y/z for encoders)
    - server/entity_store.go:80-169 (add/remove/move/get/near contracts — add OVERWRITES on dup id; move re-buckets)
    - server/entity.go:40-110 (Entity fields + NewEntity assigns a FRESH uuid — so construct the player Entity DIRECTLY, not via NewEntity, so uuid==p.uuid and id==p.entityID)
    - server/tick.go:699-752 (drainRegistrations join append + removePlayer swap-remove — the seam to add `t.entities.add`/`t.entities.remove` into)
    - server/play_join.go:323-391 (writePlayerInfoUpdateAdd + playerInfoEntriesEncoder — generalize to a multi-entry collection + add a REMOVE_PLAYER builder)
    - data/entity/entity.go:1424 (entity.Player ID 156, W 0.6, H 1.8)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 1 + Pitfall 1 + Pitfall 4 (the tab-list-precedes-AddEntity rule and the self-skip/pos-sync rules)
    - javap (port-from-jar mandate, cite the class): `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.network.protocol.game.ClientboundPlayerInfoRemovePacket` — confirm REMOVE_PLAYER wire = a VarInt-prefixed list of UUIDs (the packet is ClientboundPlayerInfoRemovePacket, a SEPARATE packet id from ClientboundPlayerInfoUpdate). Cite it in a code comment.
  </read_first>
  <action>
    Create server/player_visibility.go. Implement:
    1. `func newPlayerEntity(p *tickPlayer) *Entity` — construct DIRECTLY (NOT NewEntity): `&Entity{id: p.entityID, typ: entity.Player.ID, uuid: p.uuid, x: p.x, y: p.y, z: p.z, width: entity.Player.Width, height: entity.Player.Height}`. The id MUST equal p.entityID so the tracker's self-skip works (Pitfall 4). Cite data/entity/entity.go:1424.
    2. In tick.go `drainRegistrations` (the join `case p := <-t.register:` branch, AFTER the `t.players = append`): set `p.playerEntity = newPlayerEntity(p)` and `t.entities.add(p.playerEntity)`. Add EXACTLY ONCE here — never in AcceptPlayer (crosses the tick boundary, TICK-05 violation). Then broadcast: `t.broadcastPlayerInfoAdd(p)` (send the joiner's ADD entry to every OTHER player) and `t.sendExistingPlayersTo(p)` (send all existing players' ADD entries to the joiner). Order: tab-list ADD BEFORE the tracker's AddEntity next tick (Pitfall 1).
    3. In tick.go `removePlayer` (after the swap-remove): `t.entities.remove(p.entityID)` and `t.broadcastPlayerInfoRemove(p.uuid)` to every remaining player.
    4. `func (t *TickLoop) syncPlayerEntities()` (called from tickEntities per Task 0): for each `p` in t.players with a non-nil playerEntity, `t.entities.move(p.playerEntity, p.x, p.y, p.z)` then copy `p.playerEntity.yaw = p.yaw; .pitch = p.pitch; .headYaw = p.headYaw; .onGround = p.onGround`. Runs BEFORE tracker.Tick so the tracker broadcasts current positions (Pitfall 4).
    5. `func (t *TickLoop) lookupPlayerByEntityID(id int32) *tickPlayer` — scan t.players for `pl.entityID == id`, return it or nil. The GAMEPLAY-04 (17-03) reverse-lookup dependency; provide it here since 17-03 must NOT edit tick.go. Comment: "// used by 17-03 GAMEPLAY-04 attack target resolution".
    6. In play_join.go: generalize the tab-list encoder. Add `func (t *TickLoop) broadcastPlayerInfoAdd(joiner *tickPlayer)` sending `writePlayerInfoUpdateAdd(joiner.uuid, joiner.name, gameModeSurvival)` to every `other` in t.players where `other != joiner`. Add `func (t *TickLoop) sendExistingPlayersTo(joiner *tickPlayer)` sending each existing player's ADD entry to joiner.client. Add `writePlayerInfoUpdateRemove(ids ...uuid.UUID) pk.Packet` building `ClientboundPlayerInfoRemove` (jar-cited: VarInt count + raw UUIDs) and `func (t *TickLoop) broadcastPlayerInfoRemove(id uuid.UUID)` sending it to all players. Offline-mode sends 0 properties (reuse the existing 0-property path) — default skins for Phase 17 (Open Question 2).
    Turn TestPlayerVisibility GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestPlayerVisibility' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/player_visibility.go contains `func newPlayerEntity` and `func (t *TickLoop) syncPlayerEntities` and `func (t *TickLoop) lookupPlayerByEntityID`
    - server/tick.go drainRegistrations contains `t.entities.add(p.playerEntity)` and removePlayer contains `t.entities.remove(p.entityID)`
    - server/play_join.go contains `broadcastPlayerInfoAdd`, `broadcastPlayerInfoRemove`, and a `ClientboundPlayerInfoRemove` builder with a javap citation naming `ClientboundPlayerInfoRemovePacket`
    - `go test ./server/ -run TestPlayerVisibility` passes
  </acceptance_criteria>
  <done>A second player is in the store with id==entityID, position-synced each tick before the tracker, and tab-list-broadcast bidirectionally at join + removed at leave. The tracker now broadcasts AddEntity/Teleport/Remove for players.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAMEPLAY-02 — apply the persisted position to the single bootstrap teleport</name>
  <files>server/gameplay_tick.go, server/play_join.go</files>
  <read_first>
    - server/gameplay_tick.go:160-259 (AcceptPlayer: teleportID at :177, entityID at :186, sendPlayBootstrap at :199, the player := &tickPlayer{} at :218, the loadPlayer block at :251-257 that applies health/food/sat but SKIPS Pos)
    - server/play_join.go:460-502 (sendPlayBootstrap + bootstrapParams — it hardcodes spawnX/Y/Z from `center`; generalize to accept an explicit spawn pos)
    - server/persistence.go:131-160 (loadPlayer returns save.PlayerData with .Pos [3]float64, .Rotation [2]float32, ok bool)
    - server/combat.go:130-174 (performRespawn — the reference for a re-teleport pattern, but DO NOT use it here; feed the pos into the SINGLE bootstrap teleport instead — Pitfall 3)
    - server/entity_store.go:72-78 (columnOf / the chunkCenterOf helper it wraps — reuse for the center calc)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 2 + Pitfall 3 (no double teleport — feed the persisted pos into the EXISTING bootstrap teleport pre-register; set tickPlayer.x/y/z + center from it)
  </read_first>
  <action>
    In AcceptPlayer (gameplay_tick.go), apply the persisted Pos to the SINGLE bootstrap teleport (Pitfall 3 — never a second teleport):
    1. Read loadPlayer ONCE before sendPlayBootstrap (after entityID is claimed at :186). Compute `spawnX, spawnY, spawnZ` defaulted to the hardcoded spawn (chunk (0,0) center: 8.5 / spawnSurfaceY+2 / 8.5).
    2. If loadPlayer returns ok, set `spawnX, spawnY, spawnZ = data.Pos[0], data.Pos[1], data.Pos[2]`. Keep data to apply health/food/sat (existing :251-257) AND, when constructing `player` at :218, set `player.x, player.y, player.z = spawnX, spawnY, spawnZ`, `player.yaw, player.pitch = data.Rotation[0], data.Rotation[1]`, and `player.center = chunkCenterOf(int32(math.Floor(spawnX)), int32(math.Floor(spawnZ)))` (reuse the helper columnOf wraps — confirm its exact name in entity_store.go).
    3. Thread `spawnX/Y/Z` into `bootstrapParams` so sendPlayBootstrap teleports to the persisted position with the SAME single teleport id (existing awaitingTeleport/confirm gate reused — no second teleport, no gate re-arm).
    4. In play_join.go: add `spawnX, spawnY, spawnZ float64` + `hasSpawn bool` to `bootstrapParams`; in sendPlayBootstrap, when `hasSpawn` use those coords for the PlayerPosition teleport AND SetDefaultSpawnPosition; else fall back to the center-derived spawn.
    Turn TestPositionLoadApplied GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestPositionLoadApplied' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/gameplay_tick.go applies `data.Pos` to `player.x/y/z` and `player.center` (the ":246 NOT applied for v1" skip is removed/replaced)
    - server/play_join.go bootstrapParams has spawn coordinate fields + hasSpawn, and sendPlayBootstrap uses them for the single bootstrap teleport (no second post-register teleport)
    - `go test ./server/ -run TestPositionLoadApplied` passes
    - `grep -n "NOT applied for v1" server/gameplay_tick.go` returns nothing
  </acceptance_criteria>
  <done>A reconnecting player spawns at its saved x/y/z (and view ring centers there) via the single existing bootstrap teleport — no rubber-band, no second teleport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAMEPLAY-03 — inventory join-sync on the first tick</name>
  <files>server/inventory_join.go</files>
  <read_first>
    - server/inventory.go:70-85 (ensureInventory + sendContent — sendContent is correct and safe for a nil inventory)
    - server/tick.go:264-423 (the bootstrapped flag added in Task 0)
    - server/tick_phases.go:152-164 (tickEntities — where the syncJoinInventories() hook call was added in Task 0; this task does NOT edit tick_phases.go, only confirms the call is present)
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 3 + the "GAMEPLAY-03: inventory join-sync" code example (fire exactly once, guarded by a per-player flag)
  </read_first>
  <action>
    Create server/inventory_join.go with `func (t *TickLoop) syncJoinInventories()`: for each `p` in t.players where `!p.bootstrapped`, call `t.sendContent(p)` then set `p.bootstrapped = true`. Fires exactly once per player on the first tick after drainRegistrations registers them (the flag guards re-send). The call site `t.syncJoinInventories()` was wired in tickEntities by Task 0 — do not re-edit tick_phases.go. sendContent calls ensureInventory so a nil inventory sends an empty-but-valid window.
    Turn TestInventoryJoinSync GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestInventoryJoinSync' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/inventory_join.go contains `func (t *TickLoop) syncJoinInventories` calling `t.sendContent(p)` guarded by `p.bootstrapped`
    - `go test ./server/ -run TestInventoryJoinSync` passes (fires once, second tick sends nothing)
  </acceptance_criteria>
  <done>The client's inventory window 0 is populated by an authoritative ContainerSetContent on the first tick after join, so the existing click/creative/carried handlers work end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tick (join) | The login profile (name/UUID) is server-accepted; the joiner cannot forge another player's entry |
| persisted .dat → tick (load) | A server-written .dat is low-risk, but a corrupt/forged Pos must not crash the join or teleport-exploit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Spoofing | tab-list broadcast | mitigate | The broadcast entry is built from the SERVER-authoritative `joiner.uuid`/`joiner.name` (login profile), never a client-supplied field |
| T-17-02 | Tampering | persisted position load | mitigate | The persisted Pos feeds the SINGLE bootstrap teleport behind the existing confirm gate (T-5-03); a reconnecting client cannot claim an arbitrary position mid-session — load is server-read from disk, not client-sent |
| T-17-03 | Denial of Service | player Entity in store | accept | One Entity per connected player; the connection cap already bounds player count; no unbounded growth |
| T-17-04 | Tampering | inventory join-sync | mitigate | sendContent sends the SERVER-authoritative inventory; the client's window is overwritten, never trusted (reuses the established T-6-02 discard discipline) |
</threat_model>

<verification>
- `cd /d/ender && go build ./server/...` exits 0
- `go test ./server/...` passes (the three new tests + all existing, including TestTickPhaseOrder which MUST stay green — the new hooks run INSIDE existing phases, no phase reorder)
- `grep -n "syncPlayerEntities\|syncJoinInventories\|tickFallDamage\|tickFluids" server/tick_phases.go` shows all four hooks wired
- Docker -race (per HANDOFF constraint #4): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race -timeout 1800s ./server/...` clean
</verification>

<success_criteria>
- Two registered players each get a store Entity (id==entityID), position-synced each tick; the tracker broadcasts AddEntity to the other; tab-list ADD/REMOVE is bidirectional.
- A reconnecting player's persisted Pos drives the single bootstrap teleport + view-ring center.
- ContainerSetContent fires once on the first tick after join.
- TestTickPhaseOrder stays green. The two stub seam files (fluid.go, fall_damage.go) compile and are no-ops until 17-02/17-03 overwrite them.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gameplay-completion/17-01-SUMMARY.md`. CRITICAL: document the Wave-2 handoff — fluid.go and fall_damage.go are STUB SEAM FILES this plan created for 17-02 and 17-03 to OVERWRITE (disjoint files); tick.go and tick_phases.go are Wave-1-owned and NOT edited by any Wave-2 plan. List the provided-for-Wave-2 surface: tickPlayer fields (fallDistance, wasOnGround, lastY), the fluidSchedule TickLoop field, the `lookupPlayerByEntityID` reverse lookup, and the chosen fluidSchedule init point (SetWorld vs lazy-in-tickFluids).
</output>
