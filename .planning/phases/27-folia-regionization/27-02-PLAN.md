---
phase: 27-folia-regionization
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - go.mod
  - go.sum
  - server/region.go
  - server/region_coordinator.go
  - server/tick.go
  - server/tick_phases.go
  - server/region_coordinator_test.go
autonomous: true
requirements: [REGION-01]

must_haves:
  truths:
    - "github.com/sourcegraph/conc is added (pure-Go, CGO=0 confirmed) and used for the per-tick fan-out/join barrier"
    - "tickOnce becomes: drain registrations (global) → fan out the region tick(s) via conc → BARRIER (join) → cross-region post-phase (tracker/broadcasts/flushOutbound) on the coordinator → advance the shared gametime ONCE"
    - "The shared 50ms gametime is advanced EXACTLY once per logical tick by the coordinator; each region reads the same tick number (no per-region accumulator)"
    - "A SEPARATE global-region phase ticks the server-wide state (registrations, on_tick, player flush) distinct from the per-region world tick"
    - "A panic inside a region's tick is propagated structured by conc (recovered per region) and does NOT hang the tick — the coordinator survives and advances gametime"
    - "Still behavior-neutral at N=1: the FULL existing suite passes UNCHANGED + -race clean"
  artifacts:
    - path: "server/region_coordinator.go"
      provides: "the conc fan-out/barrier coordinator: region.tick(gt) per-region pipeline + the cross-region post-phase + the single gametime advance"
      contains: "conc.WaitGroup"
      min_lines: 60
    - path: "server/region_coordinator_test.go"
      provides: "TestCoordinatorBarrier (fan-out joins before the post-phase) + TestRegionPanicIsolated + TestSharedGameTimeAdvancedOnce"
      contains: "func TestRegionPanicIsolated"
  key_links:
    - from: "server/tick_phases.go tickOnce"
      to: "the coordinator fan-out/barrier (region.tick) then the cross-region post-phase"
      via: "conc.WaitGroup.Go per region + Wait()"
      pattern: "conc\\.WaitGroup|\\.Go\\(|\\.Wait\\(\\)"
    - from: "server/region_coordinator.go region.tick"
      to: "the shared gametime gt passed in (read-only) by the coordinator"
      via: "gametime advanced once AFTER the barrier"
      pattern: "gametime\\+\\+|gt "
---

<objective>
STEP 2 of the operator's locked incremental path: add the conc fan-out/barrier coordinator at N=1.

Add `github.com/sourcegraph/conc` (verified pure-Go, CGO=0 safe — v0.3.0 confirmed at plan time) and
restructure `tickOnce` into the Folia coordinator shape WHILE STAYING N=1 (one region in the fan-out):

1. drain registrations (GLOBAL — already runs pre-step in advanceDraining; unchanged)
2. read the ONE shared gametime `gt` for this tick
3. FAN OUT: `conc.WaitGroup.Go(func(){ r.tick(gt) })` for each region — the per-region pipeline
   (tickWorld → tickEntities → tickAI → tickPhysics → applyAsyncResults(region) → per-region Emit)
4. BARRIER: `wg.Wait()` — no region mutates its store past this point this tick
5. CROSS-REGION / GLOBAL POST-PHASE on the coordinator (all regions quiescent): tickEntityMovement,
   tickEquipment, tracker.Tick(), flushOutbound, the global on_tick Emit
6. advance the shared gametime EXACTLY ONCE + recordMSPT

At N=1 this is behavior-neutral: one region in the fan-out runs the exact same pipeline serially, the
coordinator advances gametime once (the shared 50ms anchor — Pitfall 3), and the post-phase runs after
the single region finishes. conc gives structured panic propagation: a region panic is recovered per
region and re-raised on Wait(), so one region's panic does NOT hang the tick (the coordinator's
recover backstop, hoisted from tickOnce, handles it and still advances gametime).

Purpose: prove the barrier discipline + the global/region split + the shared-anchor invariant with
ZERO behavior change, before Plan 03 flips to N=2. The proof: the full existing suite passes unchanged
+ -race clean.

Output: `go.mod`/`go.sum` (conc added), `server/region_coordinator.go` (the coordinator + region.tick),
tickOnce rewritten to fan-out→barrier→post-phase→advance-once. The split between the per-region tick
(steps 3-4) and the global post-phase (step 5) is the explicit seam Plan 03 fills with cross-region
transfer + the cross-region tracker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-folia-regionization/27-RESEARCH.md
@.planning/phases/27-folia-regionization/27-CONTEXT.md
@.planning/phases/27-folia-regionization/27-VALIDATION.md
@CLAUDE.md

@server/region.go
@server/tick.go
@server/tick_phases.go

<interfaces>
<!-- The current tickOnce (tick_phases.go:24-64) — the pipeline to SPLIT into per-region + post-phase.
     Extracted from server/tick_phases.go. -->

Today's tickOnce order (all on the tick goroutine):
```
resolveSubtickInputs   // GLOBAL (per-player input) — stays on coordinator (pre/at fan-out boundary)
tickWorld              // PER-REGION (scheduled blocks/fluids/chunk-save over the region's world)
tickChunks             // PER-REGION (per-player ring → region.world requests)
tickEntities           // PER-REGION (the per-entity seams over region.entities)
tickAI                 // PER-REGION (serverAiStep over region.entities + naturalSpawn)
tickPhysics            // PER-REGION (moveEntity over region.entities)
applyAsyncResults      // PER-REGION drain (region.asyncIn) + GLOBAL drain (asyncIn2 — see note)
tickEntityMovement     // CROSS-REGION post-phase (sendChanges to trackers across regions)
tickEquipment          // CROSS-REGION post-phase
tracker.Tick()         // CROSS-REGION post-phase (near() spans region seams — Plan 03)
flushOutbound          // GLOBAL (per-player chunk stream over the global player list)
gametime++             // GLOBAL — advanced ONCE by the coordinator (the shared 50ms anchor)
plugins.Emit(on_tick)  // GLOBAL — ONE per-tick emit on the coordinator
recordMSPT             // GLOBAL telemetry
```

The conc API (verified pure-Go v0.3.0):
```go
import "github.com/sourcegraph/conc"
var wg conc.WaitGroup
for _, r := range t.regions { r := r; wg.Go(func() { r.tick(gt) }) }
wg.Wait() // re-panics with the first region panic's value + stack (structured propagation)
```

The recover backstop currently in tickOnce (tick_phases.go:32-37) — HOIST it to wrap the whole
coordinator so a region panic surfaced by wg.Wait() is logged and gametime still advances.

resolveSubtickInputs reads per-player subtick buffers (GLOBAL player list) — at N=1 it stays on the
coordinator BEFORE the fan-out (it touches no region store). Note in the code where it will route to
the owning region in a future N>1 step if a player becomes region-affine (out of scope here).

applyAsyncResults note: the per-region asyncIn (chunkReady) drains inside r.tick; the global asyncIn2
(path/tracker/spawn results) — at N=1 — drains on the coordinator AFTER the barrier (its results
target the only region). Plan 03 routes asyncIn2 results to the OWNING region (Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conc (pure-Go, CGO=0 confirmed) and the region.tick per-region pipeline</name>
  <files>go.mod, go.sum, server/region.go, server/region_coordinator.go</files>
  <action>
Run `go get github.com/sourcegraph/conc@v0.3.0` then `go mod tidy`. Confirm pure-Go/CGO=0:
`CGO_ENABLED=0 go build ./...` must stay clean and `go list -deps github.com/sourcegraph/conc` must
show no cgo. (Plan-time verification already confirmed v0.3.0 is the latest and pure-Go.)

Create `server/region_coordinator.go` with `func (r *region) tick(gt int64)` — the PER-REGION pipeline,
lifted from today's tickOnce middle (the PER-REGION rows in the `<interfaces>` block), in the SAME order:
tickWorld → tickChunks → tickEntities → tickAI → tickPhysics → applyAsyncResults-for-this-region. These
phase methods currently hang off `*TickLoop` and read `t.only().X`; for N=1 keep them as `*TickLoop`
methods but have `r.tick` call them via `r.coord` (the back-ref) — i.e. `r.tick(gt)` calls the existing
`r.coord.tickWorld()/tickChunks()/...` unchanged because at N=1 `only()==r`. Keep the phase bodies
UNCHANGED in this plan (the field-move in Plan 01 already pointed them at the region); document that
Plan 03 will thread `r` explicitly so each phase ranges THIS region's store.

Add a per-region chunkReady drain: `applyAsyncResults` today drains both asyncIn (per-region) and
asyncIn2 (global). Split it: `r.drainRegionAsync()` drains the region's asyncIn (chunkReady → region.world);
the coordinator drains asyncIn2 after the barrier (Task 2). Keep the drain order/semantics identical.

The gametime `gt` passed to r.tick is READ-ONLY inside the region (scheduled block ticks compare against
it). The region NEVER advances gametime — only the coordinator does (Pitfall 3). Document this.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && go list -deps github.com/sourcegraph/conc | grep -c . > /dev/null && CGO_ENABLED=0 go test ./server/ -run TestRegionStruct</automated>
  </verify>
  <done>conc@v0.3.0 in go.mod, `go mod tidy` clean, CGO_ENABLED=0 build clean; region_coordinator.go defines `func (r *region) tick(gt int64)` running the per-region pipeline in the existing order; gt is read-only in the region; the region never advances gametime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite tickOnce as fan-out → barrier → cross-region post-phase → advance-once; prove behavior-neutral + panic-isolated</name>
  <files>server/tick_phases.go, server/region_coordinator.go, server/region_coordinator_test.go</files>
  <behavior>
    - Existing test: the FULL server + world suite passes UNCHANGED (N=1 fan-out == today's serial pipeline).
    - Existing test TestTickPhaseOrder: the observable phase order is preserved (the per-region phases run, then the cross-region post-phase, then on_tick) — update the trace expectations ONLY for the new structural markers, not the gameplay order.
    - New TestCoordinatorBarrier: a probe asserts the cross-region post-phase runs AFTER every region's tick returns (the barrier holds — no post-phase read while a region still ticks).
    - New TestSharedGameTimeAdvancedOnce: after one tickOnce, gametime increased by exactly 1; the region observed `gt` == the pre-advance value during its tick.
    - New TestRegionPanicIsolated: a region whose tick panics is recovered (conc propagates it to Wait()); the coordinator logs it and STILL advances gametime (the tick does not hang, the loop survives) — mirror the existing tickOnce recover backstop semantics.
  </behavior>
  <action>
Rewrite `tickOnce` (tick_phases.go) into the coordinator shape:

```
func (t *TickLoop) tickOnce() {
    start := t.clock.Now()
    defer t.recoverTick(start) // HOISTED backstop: logs a panic (incl. one surfaced by wg.Wait),
                               // advances gametime so the anchor never stalls, records MSPT
    t.resolveSubtickInputs()   // GLOBAL pre-phase (per-player input; touches no region store at N=1)
    gt := t.gametime           // the ONE shared tick number every region reads this tick (Pitfall 3)

    var wg conc.WaitGroup
    for _, r := range t.regions { r := r; wg.Go(func() { r.tick(gt) }) }
    wg.Wait()                  // BARRIER: every region quiescent past here

    // CROSS-REGION / GLOBAL POST-PHASE (coordinator, all regions quiescent):
    t.drainGlobalAsync()       // asyncIn2 (path/tracker/spawn) — at N=1 applies to the only region
    t.tickEntityMovement()
    t.tickEquipment()
    t.trace("tracker.Tick"); t.tracker.Tick()
    t.flushOutbound()

    t.gametime++               // EXACTLY once — the shared 50ms anchor (TICK-02)
    if t.plugins != nil { t.plugins.Emit(host.EventTick, host.TickEvent{Tick: int(t.gametime)}) }
    t.recordMSPT(t.clock.Now().Sub(start))
}
```

Implement `recoverTick(start)` as a method wrapping the existing recover logic (log stack, `t.gametime++`
on panic so the anchor does not stall, recordMSPT). It must handle a panic value surfaced by `wg.Wait()`
identically to a direct phase panic (conc re-panics on the calling goroutine, so the existing
`defer recover()` catches it). NOTE: keep the on-panic gametime++ EXACTLY-once (do not double-advance
when a region panics before the normal advance) — structure recoverTick so the normal path advances
once and the panic path advances once, never both.

Write the tests FIRST (RED). For TestRegionPanicIsolated, inject a region whose tick panics (a test-only
region with a tick hook, or a panicking phase via a test seam) and assert: the loop survives, gametime
advanced by 1, the panic was logged. For TestCoordinatorBarrier, use a phaseTrace-style probe or an
atomic flag set at the end of r.tick and asserted before the post-phase to prove the join happened.

THE LOAD-BEARING CHECK: the FULL existing server + world suite passes UNCHANGED. TestTickPhaseOrder may
need its expected trace updated for the structural split (region fan-out vs post-phase markers) but the
GAMEPLAY phase ordering (world→entities→ai→physics→tracker→flush, on_tick last) must be byte-identical
in observable effect. If a gameplay test changes outcome, the rewrite changed behavior — STOP and fix.
  </behavior>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && go vet ./server/ && CGO_ENABLED=0 go test ./server/ ./world/...</automated>
  </verify>
  <done>tickOnce is the fan-out→barrier→post-phase→advance-once coordinator using conc.WaitGroup; gametime advances exactly once per tick by the coordinator (regions read gt read-only); a region panic is recovered+logged and the tick survives + still advances; TestCoordinatorBarrier/TestSharedGameTimeAdvancedOnce/TestRegionPanicIsolated pass; the FULL existing suite passes UNCHANGED.</done>
</task>

<task type="auto">
  <name>Task 3: -race gate — the coordinator/barrier at N=1 is race-clean and still behavior-neutral</name>
  <files>server/region_coordinator_test.go</files>
  <action>
Run the Docker -race gate (the load-bearing gate) over the new coordinator:

```
MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ ./plugin/...
```

It MUST PASS with no race reports. At N=1 the fan-out runs one region serially, so this MUST be as clean
as Plan 01. If a race surfaces, it is a real bug in the coordinator (e.g. the post-phase reading a region
store before wg.Wait, or recoverTick racing gametime) — fix it, do NOT suppress, and pin it with a
regression test in region_coordinator_test.go.

Confirm the default build stays pure-Go static (conc added but pure-Go, no cgo in the graph):

```
CGO_ENABLED=0 go build ./...
```

This proves the one new dependency (conc) did not break the CGO=0 mandate.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src -v sulfur-gomod://go/pkg/mod golang:1.26 go test -race -timeout 900s ./server/ ./plugin/...</automated>
  </verify>
  <done>The Docker -race gate over ./server/ ./plugin/... is green (no race reports); CGO_ENABLED=0 go build ./... is clean with conc in the graph but no cgo; any race the gate caught is fixed in the coordinator and pinned by a regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator ↔ region goroutine | conc.WaitGroup.Go spawns the region tick; the coordinator only reads region state AFTER wg.Wait() (the barrier) — no concurrent access at N=1 (one region) and the barrier enforces it for N>1. |
| region tick ↔ shared gametime | the region reads `gt` (a value passed in), never mutates the coordinator's gametime — the anchor stays single-writer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Denial of Service | a region panic hangs the whole tick (every player disconnects) | mitigate | conc.WaitGroup recovers each region's panic and re-raises on Wait(); the hoisted recoverTick backstop logs it and advances gametime so the loop survives. TestRegionPanicIsolated is the proof. |
| T-27-02-GT | Tampering | gametime drift — a region advances the anchor, or it advances twice on a panic | mitigate | The region receives gt read-only and NEVER advances it; only the coordinator does, exactly once (normal path) or once (panic path), never both. TestSharedGameTimeAdvancedOnce asserts +1 per tick. |
| T-27-02-BAR | Tampering | the cross-region post-phase reads a region store while it still ticks | mitigate | wg.Wait() is the barrier; the post-phase runs only after every region tick returns. TestCoordinatorBarrier asserts the join precedes the post-phase. |
| T-27-02-CGO | Tampering | conc pulls cgo and breaks the CGO=0 mandate | mitigate | conc v0.3.0 verified pure-Go at plan time; Task 1 re-confirms `CGO_ENABLED=0 go build ./...` clean + no cgo in `go list -deps`. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` clean (conc added, pure-Go, no cgo).
- `go vet ./server/` clean.
- `CGO_ENABLED=0 go test ./server/ ./world/...` — the FULL existing suite passes UNCHANGED.
- `CGO_ENABLED=0 go test ./server/ -run "TestCoordinatorBarrier|TestSharedGameTimeAdvancedOnce|TestRegionPanicIsolated"` pass.
- Docker `-race ./server/ ./plugin/...` green (Task 3).
</verification>

<success_criteria>
- conc added (pure-Go, CGO=0 preserved) and driving the per-tick fan-out/join barrier.
- tickOnce = drain → fan-out → barrier → cross-region post-phase → advance-gametime-once.
- The shared 50ms gametime is advanced exactly once per tick by the coordinator; regions read it read-only.
- A region panic is structured-propagated and isolated (the tick survives, gametime still advances).
- Still behavior-neutral at N=1: the full existing suite passes UNCHANGED + Docker -race clean.
</success_criteria>

<output>
After completion, create `.planning/phases/27-folia-regionization/27-02-SUMMARY.md`.
</output>
