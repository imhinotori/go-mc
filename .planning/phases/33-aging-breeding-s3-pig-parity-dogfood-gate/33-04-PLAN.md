---
phase: 33-aging-breeding-s3-pig-parity-dogfood-gate
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - server/plugin_entity.go
  - plugins/vanilla_pig/main.star
  - server/assets/vanilla_pig/main.star
  - server/plugin_pig_test.go
  - server/breed_follow_test.go
autonomous: true
requirements: [MOB-SUB-09, MOB-GATE-01]
tags: [breeding, lockstep, starlark, host-handles, goals, pig-parity]

must_haves:
  truths:
    - "BOTH vanilla_pig/main.star copies are byte-identical and declare the two new goals (@3 breed {MOVE,LOOK}, @5 follow EMPTY flags) + their callbacks"
    - "The host owns the breed/follow scans + the breed RNG (XP nextInt(7) + variant nextBoolean()) — the .star only navigates + signals; the draws + order are identical to the Go-native breedGoal (lockstep)"
    - "5 host handles exist: is_in_love, is_baby, breed_age, nearest_breeding_partner, nearest_adult_parent"
    - "The 3 plugin-side goal-count tests assert 9 goals (boot-load-9, declares-9, goals-ported @3/@5)"
    - "breed/follow scenario tests pass: two in-love pigs → a baby spawns + cooldown + inLove reset + XP orb; a baby paths to a nearby adult"
    - "The plugin pig now has 9 goals matching the Go-native pig (the 9v9 oracle gate is set up to close in 33-05)"
  artifacts:
    - path: "server/plugin_entity.go"
      provides: "5 host handles (3 read accessors + 2 host scans)"
      contains: "nearest_breeding_partner"
    - path: "plugins/vanilla_pig/main.star"
      provides: "the @3 breed + @5 follow goal declarations + callbacks (byte-identical to the assets copy)"
      contains: "breed_can_use"
    - path: "server/assets/vanilla_pig/main.star"
      provides: "the byte-identical twin"
      contains: "breed_can_use"
    - path: "server/breed_follow_test.go"
      provides: "breed + follow scenario tests"
      contains: "func TestBreed"
  key_links:
    - from: "plugins/vanilla_pig/main.star"
      to: "server/assets/vanilla_pig/main.star"
      via: "byte-identical lockstep copies"
      pattern: "breed_can_use|follow_can_use"
    - from: "plugins/vanilla_pig/main.star"
      to: "server/plugin_entity.go host handles"
      via: "entity.nearest_breeding_partner / nearest_adult_parent / is_in_love / is_baby"
      pattern: "nearest_breeding_partner|is_in_love"
---

<objective>
Mirror the Go-native goals (33-03) onto the PLUGIN pig in lockstep: add the 5 host handles (is_in_love, is_baby, breed_age, nearest_breeding_partner, nearest_adult_parent), declare BreedGoal@3 + FollowParentGoal@5 + their callbacks in BOTH byte-identical vanilla_pig/main.star copies (the host owns the scans + the breed RNG so both halves draw identically), update the 3 plugin-side goal-count tests (7→9), and add the breed/follow scenario tests. This brings the plugin pig to 9 goals matching the Go-native pig.

Purpose: This is C2 — the plugin/lockstep half of the goal work (split from 33-03's Go-native goals to protect quality on the make-or-break surface). After this plan BOTH pigs have 9 goals; the full 9v9 byte-identical oracle gate is formally run/closed in 33-05. The lockstep is the CORE risk: every goal + every breed RNG draw (XP nextInt(7) + variant nextBoolean()) must be identical on the Go pig (33-03) AND both .star pigs (here), in the SAME order.
Output: 5 host handles + both .star copies (9 goals) + the 3 plugin-side tests + breed/follow scenario tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-CONTEXT.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-JARNOTES.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-PATTERNS.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-01-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-02-SUMMARY.md
@.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-03-SUMMARY.md

<oracle_gate_strategy>
THE MAKE-OR-BREAK CONSTRAINT (the lockstep half — the divergence risk lives HERE):
After 33-03 the Go-native pig has 9 goals + the breed RNG draws (XP nextInt(7) + variant nextBoolean()).
The plugin pig still has 7 — so TestPluginPigEqualsGoNativePig is RED until this plan lands. This plan
mirrors the 2 goals + BOTH breed draws onto the plugin pig so the 9v9 oracle can close in 33-05.
- The host OWNS the breed/follow scans (same-class near() + canMate + isPanicking) AND the breed RNG —
  the .star callbacks only navigate + call a host breed op. So the XP nextInt(7) + variant nextBoolean()
  draws are made HOST-SIDE (the same per-mob RNG source, the same ORDER) for BOTH the Go pig and the
  plugin pig → identical draws → byte-identical.
- breed_can_use gates on is_in_love (false on the un-fed oracle → dormant → zero draws); follow_can_use
  gates on is_baby (false on the adult oracle → dormant). The oracle pig stays a lone un-fed adult.
- LOCKSTEP DIFF: after editing, `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star`
  MUST be EMPTY (both copies byte-identical).
GATE for THIS plan: the 3 plugin-side goal-count tests pass (plugin pig = 9 goals); the breed/follow scenario
tests pass; the .star copies are byte-identical. The full 9v9 byte-identical run is 33-05 — but verify
TestPluginPigEqualsGoNativePig is now GREEN here (both at 9 goals, breed/follow dormant) and record it.
</oracle_gate_strategy>

<interfaces>
<!-- From Plans A/B + 33-03. Use directly. -->
```go
// 33-03 Go-native: breedGoal@3, followParentGoal@5, getFreePartner(t,e,range), nearestAdultSameClass(t,e,range), breed() (XP nextInt(7) + variant nextBoolean()), isPanicking, adjustedTickDelay
// Plan A/B fields: breedAge, inLove, isBaby(), isInLove(), refreshDimensions(), broadcastBabyFlag(), broadcastHearts()
```

From server/plugin_entity.go (the host-handle analogs):
```go
// READ accessors (plugin_entity.go:193-229): case "was_hurt": return starlark.Bool(e.hurtTime > 0), nil ; AttrNames :236-245
// HOST scan (plugin_entity.go:326): nearestPlayerHoldingPigFood → resolve() → nearestPlayerHolding(...) → starlark.Tuple{x,y,z} or None ; capEntitiesRead-gated ; registered in Attr :133-162
```

From plugins/vanilla_pig/main.star (the Tempt block — the copy template):
```python
# main.star:124-203 — tempt_pigfood_can_use/tick/stop/continue + the goal(...) decl list :341-420 (currently 7 entries)
def tempt_pigfood_can_use(entity, world, nav):
    p = entity.nearest_player_holding_pig_food(TEMPT_RANGE)   # HOST scan; predicate stays Go-side
    if p == None: return False
    entity.set_state("tempt_p_px", p[0]); ...; return True
```

From the 3 plugin-side goal-count tests (update 7→9 — exact lines):
```go
plugin_pig_test.go:55      if got := len(pig.ai.goals.goals); got != 7   // → != 9 ; loop :66 range []int{0,1,4,6,7,8} → add 3,5
plugin_pig_test.go:103     if len(decl.goals) != 7                       // → != 9 ; byPriority :120-125 add [3]{MOVE,LOOK}, [5] EMPTY
plugin_pig_test.go:151     TestVanillaPigGoalsPorted byPriority walk      // add @3/@5 (neither requires-update-every-tick → assert false, like @6/@7)
// (TestPigGoalSetRegistered @137 was already flipped in 33-03 — do NOT re-edit it.)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 5 host handles (3 read accessors + 2 host scans)</name>
  <read_first>
    - server/plugin_entity.go:193-245 (READ accessors + AttrNames) + :326-345 (nearestPlayerHoldingPigFood host-scan) + :133-162 (Attr method block)
    - server/ai_goals_breed.go (33-03 — getFreePartner + the nearestAdultSameClass scan to expose)
    - 33-PATTERNS.md §11 (the 5 host handles)
  </read_first>
  <files>server/plugin_entity.go</files>
  <action>
    Add the 5 host handles mirroring nearestPlayerHoldingPigFood:
    1. 3 READ accessors (after `in_lava`, ~:229) + register in AttrNames (:236-245):
       `case "is_in_love": return starlark.Bool(e.inLove > 0), nil`        // Animal.isInLove
       `case "is_baby":    return starlark.Bool(e.breedAge < 0), nil`      // AgeableMob.isBaby
       `case "breed_age":  return starlark.MakeInt(e.breedAge), nil`       // AgeableMob.getAge
    2. 2 host-scan methods following nearestPlayerHoldingPigFood (:326), capEntitiesRead-gated:
       - `nearest_breeding_partner(range)` → resolve() → calls the Go-side getFreePartner(t, e, range) (33-03) →
         returns starlark.Tuple{x,y,z} or starlark.None.
       - `nearest_adult_parent(range)` → resolve() → calls nearestAdultSameClass(t, e, range) (the follow scan from
         33-03) → tuple or None.
       Register both in Attr (:133-162) + AttrNames. The scans + canMate/adult-filter stay HOST-side; the .star sees
       only a position tuple or None (mirroring how tempt's item-id predicate stays Go-side).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./server/ 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_in_love\|is_baby\|breed_age\|nearest_breeding_partner\|nearest_adult_parent" server/plugin_entity.go` returns >= 5.
    - The 2 host scans are capEntitiesRead-gated (mirror nearestPlayerHoldingPigFood) and return a tuple or starlark.None.
    - `CGO_ENABLED=0 go build ./server/` exits 0.
  </acceptance_criteria>
  <done>The 5 host handles (3 read accessors + 2 host scans) exist, cap-gated, returning scalars/tuples to the .star.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare BreedGoal@3 + FollowParentGoal@5 + callbacks in BOTH .star copies (byte-identical lockstep)</name>
  <read_first>
    - plugins/vanilla_pig/main.star:124-203 (the Tempt callback block — the copy template) + :341-420 (the goal(...) decl list — 7 entries)
    - server/assets/vanilla_pig/main.star (the byte-identical twin — EVERY edit lands in BOTH)
    - server/ai_goals_breed.go (33-03 — how breed() owns the XP + variant draws host-side, so the .star calls a host breed op)
    - 33-PATTERNS.md §10 (the .star lockstep block + the breed/follow callback pattern + the empty-flags decl) + the "host-computes-the-scan" shared pattern
    - 33-JARNOTES.md "breed() RNG draws" (the XP + variant draws + ORDER must be host-side + identical)
  </read_first>
  <files>plugins/vanilla_pig/main.star, server/assets/vanilla_pig/main.star</files>
  <action>
    Edit BOTH vanilla_pig/main.star copies IDENTICALLY (the lockstep rule — both must stay byte-identical):
    1. Add two goal(...) entries to the decl list (:341-420), matching the jar:
       ```python
       # @3 BreedGoal(mob, 1.0) [MOVE, LOOK]
       goal(priority = 3, flags = ["MOVE", "LOOK"],
            can_use = breed_can_use, tick = breed_tick, stop = breed_stop, can_continue = breed_continue),
       # @5 FollowParentGoal(mob, 1.1) EMPTY flags
       goal(priority = 5, flags = [],
            can_use = follow_can_use, tick = follow_tick, stop = follow_stop, can_continue = follow_continue),
       ```
       (verify the decl parser accepts `flags = []` — the selector handles an empty-flag goal per PATTERNS §6.)
    2. Add the callbacks mirroring the Tempt block (:124-203), using the host handles from Task 1:
       - breed_can_use(entity, world, nav): `if not entity.is_in_love(): return False`; `p = entity.nearest_breeding_partner(8.0)`;
         `if p == None: return False`; store p + reset a love-timer via set_state; return True.
       - breed_tick: read the stored partner pos; entity.set_look_at(px,py,pz); entity.move_to(px,py,pz) (speed from the
         decl 1.0); increment the love-timer; AT the breed threshold (love-timer >= 60 && near) call a HOST breed op
         (e.g. `entity.try_breed(partner_pos)`) so the actual child-spawn + XP nextInt(7) + variant nextBoolean() draws
         happen HOST-SIDE in the SAME per-mob RNG / SAME ORDER as the Go-native breedGoal.breed() (33-03). The .star
         NEVER draws RNG itself — it only navigates + signals readiness. (If a `try_breed`/host breed op is needed, add
         it as a host method alongside the Task-1 handles AND route the Go-native breedGoal through the same host breed()
         so both halves call ONE breed implementation → identical draws. Cite the lockstep.)
       - breed_stop / breed_continue: mirror BreedGoal.stop / canContinueToUse (partner in love, love-timer<60, !panicking).
       - follow_can_use(entity, world, nav): `if not entity.is_baby(): return False`; `p = entity.nearest_adult_parent(8.0)`;
         `if p == None: return False`; store + return True. follow_tick: re-path timer (every 10) + move_to(parent).
         follow_stop / follow_continue (still a baby, parent alive, 3..16 blocks).
    3. After editing, run `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` — MUST be EMPTY.
  </action>
  <verify>
    <automated>cd /d/ender && diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star && echo "IDENTICAL" && CGO_ENABLED=0 go build ./... 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` produces EMPTY output (byte-identical lockstep).
    - `grep -c "breed_can_use\|follow_can_use" plugins/vanilla_pig/main.star` returns >= 2 (the new declarations).
    - The .star breed path does NOT draw RNG itself (the XP/variant draws are host-side via the Go breed() — grep the .star for any random/rand op should find none in breed_tick).
    - `CGO_ENABLED=0 go build ./...` exits 0.
  </acceptance_criteria>
  <done>Both .star copies declare the 2 new goals + callbacks byte-identically; the host owns the scans + breed RNG; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The 3 plugin-side goal-count tests (7→9) + breed/follow scenario tests + verify the oracle</name>
  <read_first>
    - server/plugin_pig_test.go:55-66 (TestPluginPigBootLoads), :103-146 (TestVanillaPigDeclaresGoalSet), :151-164 (TestVanillaPigGoalsPorted), :251+ (TestPluginPigEqualsGoNativePig — the build closure :258+ spawns ONE lone pig)
    - server/vanilla_pig.go:36 (spawnVanillaPig) + the tick-drive harness in plugin_pig_test.go
    - 33-PATTERNS.md §12 (the 3 plugin-side tests — exact lines; TestPigGoalSetRegistered was done in 33-03)
  </read_first>
  <files>server/plugin_pig_test.go, server/breed_follow_test.go</files>
  <action>
    1. Update the 3 plugin-side goal-count tests (TestPigGoalSetRegistered @137 was already flipped in 33-03 — do NOT touch it):
       - plugin_pig_test.go:55 `!= 7` → `!= 9` (update the want-7 message to want-9 + the new goal list); the loop :66 `range []int{0,1,4,6,7,8}` → add 3 and 5.
       - plugin_pig_test.go:103 `!= 7` → `!= 9` (update message); after :146 add byPriority[3] {MOVE,LOOK} + byPriority[5] EMPTY (flags==0/empty) checks.
       - plugin_pig_test.go:151 TestVanillaPigGoalsPorted: add @3/@5 to the byPriority walk (:158-164); neither requires-update-every-tick (assert false, like @6/@7).
    2. Create server/breed_follow_test.go (the scenario tests — drive the live behavior, NOT the oracle):
       - `TestBreedSpawnsBaby`: spawn TWO adult pigs in range, setInLove both, drive the tick loop until the breed
         threshold, assert a THIRD (baby, breedAge==-24000, half-scale AABB) pig appears, both parents breedAge==6000,
         both inLove reset, and an XP orb was awarded.
       - `TestFollowParentPathsToAdult`: spawn a baby (breedAge<0) + an adult same-class within 3..16 blocks, drive
         the tick, assert the baby's nav want target points at the adult (re-path every 10 ticks, NO RNG).
       Use spawnVanillaPig + the tick-drive harness from plugin_pig_test.go.
    3. VERIFY the oracle: with BOTH pigs now at 9 goals (33-03 Go-native + this plan's plugin), confirm
       TestPluginPigEqualsGoNativePig is GREEN (the oracle pig is a lone un-fed adult → breed/follow dormant →
       byte-identical). If RED, the divergence is an un-mirrored breed RNG draw (XP/variant) — fix the lockstep
       (the .star must route through the SAME host breed()), do NOT weaken the gate. Record the result for 33-05.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./server/ -run 'TestBreed|TestFollowParent|TestPluginPigBootLoads|TestVanillaPig|TestPigGoalSet|TestPluginPigEqualsGoNativePig' -timeout 240s 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "!= 9" server/plugin_pig_test.go` returns >= 2 (the boot-load + declares counts flipped).
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigBootLoads|TestVanillaPigDeclaresGoalSet|TestVanillaPigGoalsPorted'` PASS (plugin pig = 9 goals).
    - `CGO_ENABLED=0 go test ./server/ -run 'TestBreed|TestFollowParent' -v` shows TestBreedSpawnsBaby + TestFollowParentPathsToAdult PASS.
    - `CGO_ENABLED=0 go test ./server/ -run 'TestPluginPigEqualsGoNativePig' -timeout 240s` PASSES (9v9 byte-identical — breed/follow dormant on the un-fed lone-adult oracle). If RED, fix the breed-RNG lockstep before completing.
  </acceptance_criteria>
  <done>The 3 plugin-side goal-count tests assert 9; breed/follow scenario tests pass; the 9v9 oracle is byte-identical green (or the lockstep divergence is fixed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go host ↔ Starlark plugin (host handles + host breed op) | the .star goals call host handles (is_in_love, nearest_breeding_partner, try_breed); the scan + breed RNG stay host-side. Cap-gated (capEntitiesRead). |
| tick RNG stream → oracle | the breed XP nextInt(7) + variant nextBoolean() draws must be identical on the Go pig AND both .star pigs (one host breed() → lockstep). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | an un-mirrored breed RNG draw diverging the 9v9 oracle | mitigate | The host owns ONE breed() (XP + variant draws); both the Go pig and the .star route through it → identical draws/order. breed/follow canUse-gated dormant on the oracle. Verified by TestPluginPigEqualsGoNativePig (Task 3). |
| T-33-07 | Elevation of privilege | a .star plugin reading arbitrary entity state via the host scans | mitigate | The host scans are cap-gated (capEntitiesRead, mirroring nearestPlayerHoldingPigFood); they return only a position tuple, never an entity id or class set. |
| T-33-08 | Tampering | cross-region breeding mutating a foreign region's mob | accept | v5 ships the SAME-REGION cut (the in-region near() scan — the documented ROADMAP deviation; rolled into 33-deviations.md in 33-05). |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0; `CGO_ENABLED=0 go vet ./server/` exits 0.
- `CGO_ENABLED=0 go test ./server/ -run 'TestBreed|TestFollowParent|TestPluginPigBootLoads|TestVanillaPig|TestPigGoalSet|TestPluginPigEqualsGoNativePig|TestAging|TestBabyHitbox|TestInLove|TestFeed|TestTempt|TestPanic|TestFloat|TestPigStrolls' -timeout 240s -v` all PASS.
- `diff plugins/vanilla_pig/main.star server/assets/vanilla_pig/main.star` EMPTY.
- gopls is STALE — trust the compiler.
</verification>

<success_criteria>
- The 5 host handles + both byte-identical .star copies declare BreedGoal@3 + FollowParentGoal@5 in lockstep.
- The host owns the breed/follow scans + the breed RNG (XP + variant) so both halves draw identically.
- The 3 plugin-side goal-count tests assert 9; breed/follow scenario tests pass; the 9v9 oracle is byte-identical green.
- CGO=0 clean; no new Go deps. (Docker -race + the live dogfood verify are the 33-05 gate.)
</success_criteria>

<output>
After completion, create `.planning/phases/33-aging-breeding-s3-pig-parity-dogfood-gate/33-04-SUMMARY.md`
</output>
