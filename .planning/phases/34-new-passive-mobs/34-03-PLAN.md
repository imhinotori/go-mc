---
phase: 34-new-passive-mobs
plan: 03
type: execute
wave: 2
depends_on: [34-00]
files_modified:
  - plugins/vanilla_chicken/main.star
  - plugins/vanilla_chicken/plugin.toml
  - server/assets/vanilla_chicken/main.star
  - server/assets/vanilla_chicken/plugin.toml
  - server/chicken_test.go
autonomous: true
requirements: [MOB-PASS-03]
must_haves:
  truths:
    - "A vanilla_chicken plugin declares the 8-goal Chicken.registerGoals set reusing the proven Go goal runtime + the 34-00 parameterized food handle"
    - "A chicken renders as entity.Chicken.ID with max_health 4.0 and movement_speed 0.25 and the 0.4x0.7 hitbox"
    - "The chicken slow-fall + egg-lay (built in 34-00) are observable on a spawned chicken: a falling chicken descends gently and lays an egg on the eggTime timer"
  artifacts:
    - path: "plugins/vanilla_chicken/main.star"
      provides: "Chicken declare_mob (8 goals, no carrot)"
      contains: "declare_mob"
    - path: "server/assets/vanilla_chicken/main.star"
      provides: "byte-identical embed copy"
      contains: "declare_mob"
    - path: "server/chicken_test.go"
      provides: "chicken boot-load + hitbox + behavior tests (slow-fall/egg-lay covered in 34-00)"
      contains: "func TestChickenBootLoads"
  key_links:
    - from: "plugins/vanilla_chicken/main.star tempt"
      to: "nearest_player_holding_food handle"
      via: "entity.nearest_player_holding_food(\"chicken_food\", range) (from 34-00)"
      pattern: "nearest_player_holding_food"
    - from: "plugins/vanilla_chicken/main.star"
      to: "server/assets/vanilla_chicken/main.star"
      via: "byte-identical copy"
      pattern: "declare_mob"
---

<objective>
Chicken as a jar-faithful Starlark plugin (MOB-PASS-03): the 8-goal Chicken.registerGoals set. The
chicken-only aiStep extras (slow-fall + egg-lay) are built + wired + RNG-tested in 34-00 (they are a
host aiStep hook, not goals); this plan declares the plugin and proves the chicken spawns + behaves +
its hitbox is correct.

Depends on 34-00 (the parameterized nearest_player_holding_food handle + the chickenAiStep hook +
eggTime field + spawn init + tick-loop wiring all live there).

Purpose: Third new passive mob — the plugin declaration + behavior gate.
Output: vanilla_chicken plugin pair (byte-identical), chicken_test.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-new-passive-mobs/34-CONTEXT.md
@.planning/phases/34-new-passive-mobs/34-JARNOTES.md
@.planning/phases/34-new-passive-mobs/34-PATTERNS.md
@.planning/phases/34-new-passive-mobs/34-00-SUMMARY.md

<interfaces>
<!-- The chicken .star copies the pig template (8 goals, no carrot). The slow-fall + egg-lay are HOST -->
<!-- aiStep extras (built in 34-00), NOT plugin-expressed — they are NOT in the declare_mob goals list. -->

baseTypeByName (plugin_mob_decl.go:108-121) maps "chicken" -> entity.Chicken (Width 0.4, Height 0.7 —
the smaller hitbox auto-applies via NewEntity(decl.baseType), 34-PATTERNS.md:319-322).

THE FOOD HANDLE (from 34-00): the chicken TemptGoal can_use calls
  entity.nearest_player_holding_food("chicken_food", TEMPT_RANGE)
(chicken_food tag CONFIRMED in data/tag/tags.go, 34-JARNOTES.md:54).

THE CHICKEN aiStep (built in 34-00, see 34-00-SUMMARY.md): chickenAiStep applies slow-fall (vy*=0.6
falling) + egg-lay (egg drop + 2 nextFloat sound + nextInt(6000) reset); eggTime is a field init'd at
spawn to nextInt(6000)+6000; the tick loop calls chickenAiStep for entity.Chicken. This plan does NOT
re-implement those — it declares the plugin so a spawned chicken HAS the aiStep applied (chickenAiStep
runs for any entity.Chicken regardless of how it spawned).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vanilla_chicken plugin pair (8 goals, byte-identical) + chicken behavior test</name>
  <read_first>
    - plugins/vanilla_pig/main.star (the template — copy, trim carrot)
    - plugins/vanilla_pig/plugin.toml
    - server/vanilla_pig_embed.go:25-34 (byte-identical embed-pair discipline)
    - .planning/phases/34-new-passive-mobs/34-00-SUMMARY.md (the nearest_player_holding_food signature + the chickenAiStep behavior to assert)
    - server/plugin_pig_test.go:35 (TestPluginPigBootLoads — boot-load + goal-count test shape)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:38-48 (Chicken.registerGoals @0..7) + :72,226 (Chicken attrs 4.0/0.25)
  </read_first>
  <action>
Create plugins/vanilla_chicken/main.star by copying plugins/vanilla_pig/main.star verbatim, then:
  1. DELETE the carrot Tempt block + its @4 goal entry (chicken has only the chicken_food Tempt).
  2. In the remaining tempt callbacks, replace the handle with
     entity.nearest_player_holding_food("chicken_food", TEMPT_RANGE) (the 34-00 parameterized handle).
  3. Retune declare_mob per Chicken.registerGoals (34-JARNOTES.md:38-48), USING THE JAR INDICES:
       name = "vanilla_chicken", base_type = "chicken",
       attributes = { "max_health": 4.0, "movement_speed": 0.25 },
       goals: @0 FloatGoal[JUMP], @1 PanicGoal[MOVE] (PanicGoal(1.4)), @2 BreedGoal[MOVE,LOOK],
         @3 TemptGoal[MOVE,LOOK] (chicken_food), @4 FollowParentGoal[], @5 WaterAvoidingRandomStroll[MOVE],
         @6 LookAtPlayer[LOOK], @7 RandomLookAround[MOVE,LOOK]. Cite
         net.minecraft.world.entity.animal.chicken.Chicken.registerGoals. The slow-fall + egg-lay are
         HOST aiStep extras (34-00), NOT goals — they are NOT in the goals list.
  4. Copy plugins/vanilla_chicken/main.star verbatim to server/assets/vanilla_chicken/main.star.
Create plugins/vanilla_chicken/plugin.toml (copy the pig's, name "vanilla_chicken"). Caps stay the pig
set ["entities.read", "entities.write", "world.read", "nav"] — the egg-lay/slow-fall are host-side, no
new plugin cap. Copy verbatim to server/assets/vanilla_chicken/plugin.toml.

Create server/chicken_test.go:
  - TestChickenBootLoads: spawned chicken renders entity.Chicken.ID, AI has 8 goals at jar priorities
    (mirror plugin_pig_test.go:35), and the hitbox is 0.4x0.7 (assert the AABB dims from entity.Chicken
    on the spawned entity).
  - TestChickenBehavior: chicken spawns, walks, panics, tempts on chicken_food, breeds, is CREATURE.
  - TestChickenSlowFallsLive: spawn a chicken via the loaded plugin, set onGround=false + vy<0, run the
    tick that calls chickenAiStep, assert vy was multiplied by 0.6 (the 34-00 hook is wired for a
    plugin-spawned chicken). (This proves the 34-00 hook applies to a real spawned chicken, not just a
    synthetic entity in the 34-00 unit test.)
  </action>
  <verify>
    <automated>diff plugins/vanilla_chicken/main.star server/assets/vanilla_chicken/main.star</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./server/ -run 'TestChicken' -v</automated>
  </verify>
  <acceptance_criteria>
    - `diff plugins/vanilla_chicken/main.star server/assets/vanilla_chicken/main.star` produces NO output
    - `diff plugins/vanilla_chicken/plugin.toml server/assets/vanilla_chicken/plugin.toml` produces NO output
    - `grep -c 'def tempt_carrot' plugins/vanilla_chicken/main.star` returns 0
    - `grep 'nearest_player_holding_food("chicken_food"' plugins/vanilla_chicken/main.star` matches
    - `grep 'base_type = "chicken"' plugins/vanilla_chicken/main.star` matches
    - `grep '"max_health": 4.0' plugins/vanilla_chicken/main.star` matches AND `grep '"movement_speed": 0.25' plugins/vanilla_chicken/main.star` matches
    - `grep -v '^#' plugins/vanilla_chicken/main.star | grep -c 'priority = '` returns 8
    - `CGO_ENABLED=0 go test ./server/ -run 'TestChickenBootLoads|TestChickenBehavior|TestChickenSlowFallsLive' -v` exits 0
  </acceptance_criteria>
  <done>vanilla_chicken declares 8 goals, byte-identical embed pair, attrs 4.0/0.25, 0.4x0.7 hitbox, chicken_food tempt; the 34-00 slow-fall hook applies to a plugin-spawned chicken; chicken tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin .star -> host food handle | sandboxed Starlark; the nearest_player_holding_food scan runs host-side under entities.read |
| chicken aiStep (34-00) | host hook; this plan only declares the plugin so a spawned chicken HAS the hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Elevation | chicken plugin caps | accept | chicken plugin.toml carries the pig least-privilege caps; the egg-lay/slow-fall are host-side, no widened cap |
| T-34-09 | Repudiation | egg-lay RNG desync | mitigate | the egg-lay RNG (built in 34-00) draws via mobRandom in jar order; 34-00's TestEggLayRNG pins it; the chicken is a separate mob (pig oracle untouched) |
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... clean; go vet ./server/ clean
- Both .star and both .toml copies byte-identical
- Chicken tests green: boot-load (8 goals, 0.4x0.7 hitbox), behavior, slow-fall applies to a spawned chicken
- The pig oracle UNTOUCHED
</verification>

<success_criteria>
- Chicken declares the 8-goal Chicken.registerGoals, renders entity.Chicken.ID, attrs 4.0/0.25, correct hitbox (MOB-PASS-03)
- The 34-00 slow-fall + egg-lay apply to a plugin-spawned chicken
- Both copies byte-identical; pig oracle green
</success_criteria>

<output>
After completion, create `.planning/phases/34-new-passive-mobs/34-03-SUMMARY.md`
</output>
