---
phase: 12-core-feature-types
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - world/feature_selector.go
  - world/feature_selector_test.go
  - world/feature_misc.go
  - world/feature_misc_test.go
  - world/decoration_test.go
autonomous: true
requirements: [FEAT-03]
user_setup: []

must_haves:
  truths:
    - "The composite selectors are ported with the sub-feature RECURSION (Pitfall #9): random_selector (an ordered list of WeightedPlacedFeature{chance, feature} + a default — walk the list, for each draw nextFloat() < chance to pick that sub-feature, else fall through to the default), simple_random_selector (pick ONE of N sub-features uniformly via nextInt(N)), random_boolean_selector (NextBoolean → feature_true|feature_false). Each picked sub-feature is a PlacedFeature whose OWN placement modifiers RE-APPLY with the SAME threaded rng — implemented via a shared placeSubFeature(bctx, sub *feature.PlacedFeature, ctx, rng, pos) that Binds the sub-feature's modifier chain + dispatches its configured body (the registry), so the rng flows through. Registered under \"random_selector\"/\"simple_random_selector\"/\"random_boolean_selector\"."
    - "The selector's nested sub-features are NOT pre-parsed by 11-01: a selector's config.features[].feature / config.default.feature / feature_true / feature_false sit in cf.Config.Raw as OPAQUE JSON (walkBlockStates resolved only {Name,Properties} block-state leaves, never nested {feature,placement} sub-PlacedFeatures). So each selector body RE-DECODES its config from cf.Config.Raw, then resolves each sub-entry through the registry plumbed by 12-01 (bctx.reg): a ref STRING via reg.ResolvePlaced(id), an inline {feature,placement} OBJECT via reg.ParsePlacedFeature(\"\", raw). This is why 12-01 had to plumb *feature.Registry into the bodyContext — without it the selector recursion has nothing to resolve refs on."
    - "placeSubFeature is the recursion seam: given a resolved sub-*PlacedFeature, it Binds its placement modifiers via placement.Bind (the SAME fold 11-03/applyBiomeDecoration uses) with a configured placer built from the registry (bodyPlacerFor(bctx, sub.Feature)), and runs bound.Place(ctx, rng, pos) — so a selector → weighted sub-feature → simple_block resolves end-to-end with one threaded rng. A nested selector (selector inside a selector) recurses correctly + terminates (the parsed sub-features form an acyclic graph — 11-01's cycle guard rejects any cycle on resolve)."
    - "BlockPileFeature (\"block_pile\") places a small pile of its state_provider blocks in the jar pattern (the bell-curve radius over the anchor), FallenTreeFeature (\"fallen_tree\") places a horizontal log line (trunk_provider over log_length) + its log/stump decorators as blocks, VegetationPatchFeature (\"vegetation_patch\") + waterlogged variant carve a ground_state disk (xz_radius, depth, the replaceable gate) and places the vegetation_feature on top — each ported jar-exact (the draw order is the determinism contract), registered in the featureBody registry."
    - "The selector recursion draws RNG correctly: a deterministic test builds a random_selector over two weighted simple_block sub-features and asserts (for a seed) WHICH sub-feature was picked, that its placement modifiers re-applied (the sub-feature's own count/offset draws happened), and the exact final block — so a biome's weighted feature choice (forest = mostly oak, some birch) works. A nested-selector test proves the recursion terminates + threads one rng."
    - "The FINAL Phase-12 acceptance gate is green: the Phase-11 5x5 determinism (TestDecorationReorderIdentical) + emit-once gates STILL pass with the REAL bodies from 12-02 + 12-03 now placing blocks (byte-identical chunks regardless of generation order), AND the full suite is Docker -race clean (CGO golang:1.26). go.mod/go.sum unchanged."
  artifacts:
    - path: "world/feature_selector.go"
      provides: "random_selector/simple_random_selector/random_boolean_selector bodies + placeSubFeature recursion seam (re-decodes nested sub-features from Raw, resolves via bctx.reg), registered"
      contains: "placeSubFeature"
    - path: "world/feature_misc.go"
      provides: "block_pile + fallen_tree + vegetation_patch (+ waterlogged) bodies, registered"
      contains: "block_pile"
    - path: "world/feature_selector_test.go"
      provides: "selector recursion tests: weighted pick (seed → which sub-feature), sub-modifier re-apply, nested-selector termination"
      contains: "TestRandomSelector"
    - path: "world/feature_misc_test.go"
      provides: "deterministic block_pile/fallen_tree/vegetation_patch placement tests"
      contains: "TestBlockPile"
    - path: "world/decoration_test.go"
      provides: "the final acceptance gate: 5x5 determinism + emit-once with REAL bodies placing blocks"
      contains: "TestDecorationReorderIdentical"
  key_links:
    - from: "world/feature_selector.go random_selector body"
      to: "world/feature_selector.go placeSubFeature"
      via: "the picked weighted sub-feature's placement re-applies via the shared recursion seam"
      pattern: "placeSubFeature"
    - from: "world/feature_selector.go placeSubFeature"
      to: "world/feature_body.go bodyContext.reg (ResolvePlaced/ParsePlacedFeature)"
      via: "nested sub-features re-decoded from Raw + resolved through the registry plumbed by 12-01"
      pattern: "reg"
    - from: "world/feature_selector.go placeSubFeature"
      to: "world/levelgen/placement/place.go Bind"
      via: "the sub-feature's own modifier chain binds + folds with the SAME threaded rng"
      pattern: "Bind"
---

<objective>
Land the TRICKIEST FEAT-03 piece (criterion 3) — the composite selectors and their sub-feature RECURSION (research Pitfall #9: get this wrong and every forest collapses to a single tree type) — plus the remaining feature bodies (criterion 4): `BlockPile`/`FallenTree`/`VegetationPatch`. Then close Phase 12 with the FINAL acceptance gate: the Phase-11 5x5 determinism + emit-once suite must stay green with the REAL bodies (12-02 + 12-03) now placing blocks, Docker `-race` clean.

The selectors (`random_selector`/`simple_random_selector`/`random_boolean_selector`) are COMPOSITE features: they pick a nested sub-`PlacedFeature` whose OWN placement modifiers re-apply with the SAME threaded rng. The crux is the `placeSubFeature` recursion seam — it Binds the sub-feature's modifier chain (the same `placement.Bind` fold 11-03 uses) and dispatches its configured body through the `featureBody` registry, so `selector → weighted simple_block` resolves end-to-end with one deterministic rng. This is isolated into THIS plan precisely because it is the risk.

PREMISE CORRECTION (checker WARNING 3, verified): a selector's nested sub-features are NOT pre-parsed by 11-01. `walkBlockStates` (parse.go:259) resolves only `{Name,Properties}` block-state leaves; the selector's `config.features[].feature` / `config.default.feature` / `feature_true` / `feature_false` sit in `cf.Config.Raw` as OPAQUE JSON. The real work: each selector body RE-DECODES its config from `cf.Config.Raw`, then for each sub-entry resolves a ref string via `bctx.reg.ResolvePlaced(id)` or an inline `{feature,placement}` object via `bctx.reg.ParsePlacedFeature("", raw)` — using the `*feature.Registry` that 12-01 plumbed into the `bodyContext`. (This is the exact dependency that made 12-01 BLOCKER-1's registry plumbing mandatory.)

This plan depends on 12-01 (the registry + provider + the `bodyContext.reg` plumbing + `NextBoolean`) but is FILE-DISJOINT from 12-02 (different body files; the registry is append-only via `registerFeatureBody` from `init()`), so 12-02 and 12-03 run in PARALLEL in wave 2. The one shared file is `world/decoration_test.go` (the final gate) — owned by this plan, the LAST to run, so it exercises ALL real bodies.

Purpose: a biome's weighted feature choice + the remaining ground cover, deterministic per seed — the full non-tree decoration set live.
Output: feature_selector.go (recursion) + feature_misc.go (pile/fallen/patch), with selector-recursion tests + the final 5x5/-race acceptance gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/phases/11-feature-pipeline-decoration/11-03-SUMMARY.md
@.planning/phases/12-core-feature-types/12-01-PLAN.md

<interfaces>
<!-- Re-read 12-01-SUMMARY for the AS-BUILT bodyContext + registry signatures before coding. -->

THE REGISTRY + bodyContext (world/feature_body.go, BUILT by 12-01) — register from init() in THIS plan's files (disjoint from 12-02); the bodyContext carries the *feature.Registry the recursion resolves through:
```go
type featureBody func(bctx *bodyContext, cf *feature.ConfiguredFeature, ctx placement.PlacementContext, rng levelgen.RandomSource, pos placement.BlockPos) bool
type bodyContext struct { view *Neighborhood; reg *feature.Registry }   // reg is the plumbed registry (BLOCKER 1)
func registerFeatureBody(t string, b featureBody)
// 12-01 also provides a shared helper to build a configured placer from the registry (reuse it in placeSubFeature):
//   func bodyPlacerFor(bctx *bodyContext, cf *feature.ConfiguredFeature) placement.PlacerFunc
```
CONFIRM the AS-BUILT param order/names + the bodyPlacerFor helper from 12-01-SUMMARY (12-01 may have adjusted them per a Rule-3 adaptation).

NESTED SUB-FEATURES ARE OPAQUE IN Raw (verified — parse.go:259 walkBlockStates resolves only {Name,Properties}). The selector config (verified):
```json
// random_selector (trees_plains.json):
{ "default": {"feature":"minecraft:oak_bees_005","placement":[]},
  "features":[ {"chance":0.333,"feature":{"feature":"minecraft:fancy_oak_bees_005","placement":[]}},
               {"chance":0.0125,"feature":"minecraft:fallen_oak_tree"} ] }
// simple_random_selector (dripleaf.json): { "features":[ {feature, placement}, ... ] }  (uniform pick over N)
// random_boolean_selector (lush_caves_clay.json): { "feature_true":{...}, "feature_false":{...} }
```
The "feature" field is EITHER a ref string ("minecraft:fallen_oak_tree") OR an inline {feature, placement} object. RESOLVE each:
```go
// ref string:    pf, err := bctx.reg.ResolvePlaced(id)                      // registry.go:70 (DAG-deduped + cycle-guarded)
// inline object:  pf, err := bctx.reg.ParsePlacedFeature("", rawObject)     // parse.go:188
```
ResolvePlaced's cycle guard (11-01) rejects a placed->configured->placed loop, so the recursion terminates.

THE SUB-FEATURE PLACEMENT (world/levelgen/placement/place.go, verified) — placeSubFeature reuses the EXACT fold 11-03 uses per feature:
```go
// placement.Bind(pf *feature.PlacedFeature, configured ConfiguredFeaturePlacer, deps ModifierDeps) (*BoundPlacedFeature, error)
// bound.Place(ctx, rng, origin) bool   // the ordered flatMap fold — threads ONE rng through the sub-feature's modifiers THEN its body
```
So placeSubFeature(bctx, sub *feature.PlacedFeature, ctx, rng, pos): placer := bodyPlacerFor(bctx, sub.Feature); bound, _ := placement.Bind(sub, placer, permissiveDeps); return bound.Place(ctx, rng, pos). The rng IS the threaded selector rng — the sub-feature's modifier draws + body draws continue the sequence (Pitfall #9 satisfied). deps.BiomeAllowed can be permissive for a sub-feature (the outer biome check already gated the selector) — CONFIRM against the jar (sub-features inside a selector do NOT re-run the biome filter unless their own placement lists it; the empty-placement sub-features just run the body).

THE SELECTOR DRAW ORDER (javap -c) — port EXACT:
```
random_selector.place: for each WeightedPlacedFeature wp in features: if rng.NextFloat() < wp.chance { return placeSubFeature(wp.feature) }   // one NextFloat per entry until one hits
                        return placeSubFeature(default)
simple_random_selector.place: i = rng.NextIntN(features.size()); return placeSubFeature(features[i])   // ONE nextInt
random_boolean_selector.place: return placeSubFeature(rng.NextBoolean() ? feature_true : feature_false)   // NextBoolean = next(1), ONE bit
```
NextBoolean is PORTED BY 12-01 on LegacyRandomSource (next(1), NOT NextIntN(2) — bit-exact). 12-03 CALLS the exported NextBoolean; it does NOT edit random.go (keeps wave-2 file-disjoint). Confirm WorldgenRandom exposes NextBoolean (it embeds LegacyRandomSource) or type-assert the rng to the method per 12-01's interface-membership choice.

THE MISC BODIES (javap -c, cite each) — block_pile (BlockPileFeature: a low pile of state_provider blocks over a radius — the bell pattern + per-block placement draws), fallen_tree (FallenTreeFeature: a horizontal trunk_provider log line of log_length blocks in a random horizontal direction + the log/stump decorators applied as blocks — the decorator draws), vegetation_patch (VegetationPatchFeature: place ground_state in a disk of xz_radius down `depth`, gated by the replaceable predicate, then place the vegetation_feature sub-PlacedFeature on the disk top per vegetation_chance — uses placeSubFeature). waterlogged_vegetation_patch is vegetation_patch with the placed blocks waterlogged. Use 12-01's BlockStateProvider (feature.ParseProvider) for the providers + placeSubFeature for the inner vegetation_feature.

THE WRITE PROXY (world/neighborhood.go, verified) — view.SetBlock/GetBlock (via bctx.view), cross-chunk + heightmap-live. All bodies write ONLY through it.

THE FINAL GATE (world/decoration_test.go, verified 11-03) — TestDecorationReorderIdentical (5x5, two request orders → byte-identical), TestEmitOnce/TestEmitOnceUnderHold. With real bodies now writing blocks, these MUST stay green (determinism under reorder). The Docker -race command (from 11-03-SUMMARY): `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...`. NOTE: 12-01 changed newConfiguredPlacer's signature (added the *feature.Registry param) + updated the test makePlacer sites; this plan's decoration_test.go edits build ON that updated file — re-read it for the AS-BUILT makePlacer call shape.

CONFIRMED ON DISK: the selector configs verified (trees_plains/dripleaf/lush_caves_clay); the nested-feature-is-opaque-in-Raw premise verified (parse.go:259); placement.Bind + bound.Place + ResolvePlaced (registry.go:70) + ParsePlacedFeature (parse.go:188) verified; the registry + bodyContext + NextBoolean are 12-01's deliverables. These body files are NEW + disjoint from 12-02's; decoration_test.go is shared (this plan, last, owns the final gate edit).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the composite selectors + placeSubFeature recursion</name>
  <files>world/feature_selector.go, world/feature_selector_test.go</files>
  <behavior>
    - placeSubFeature resolves a sub-PlacedFeature (ref via bctx.reg.ResolvePlaced, inline via bctx.reg.ParsePlacedFeature), Binds its modifier chain, and runs its configured body via the registry with the SAME threaded rng — a selector→weighted simple_block places the leaf state end-to-end.
    - random_selector draws one NextFloat per weighted entry until one passes (< chance), placing that sub-feature; if none pass, places the default; the draw count = the number of entries tried (jar order).
    - simple_random_selector draws ONE NextIntN(N) and places features[i].
    - random_boolean_selector draws NextBoolean (next(1), one bit, the 12-01 primitive) and places feature_true/feature_false.
    - A nested selector (selector whose sub-feature is another selector) recurses and TERMINATES (ResolvePlaced's cycle guard rejects loops), threading one rng.
  </behavior>
  <action>
feature_selector.go: `placeSubFeature(bctx *bodyContext, sub *feature.PlacedFeature, ctx, rng, pos) bool` — placer := bodyPlacerFor(bctx, sub.Feature) (12-01's shared helper), placement.Bind(sub, placer, permissiveDeps), bound.Place(ctx, rng, pos). Then the three selector bodies: each RE-DECODES its config from cf.Config.Raw (random_selector: {default, features:[{chance, feature}]}; simple_random_selector: {features:[{feature, placement}]}; random_boolean_selector: {feature_true, feature_false}), resolves each sub-entry's "feature" via bctx.reg.ResolvePlaced (ref string) or bctx.reg.ParsePlacedFeature("", raw) (inline object), draws per the jar order, and calls placeSubFeature. init() registers all three. Call the exported NextBoolean (12-01) for random_boolean_selector — do NOT touch random.go.
feature_selector_test.go: TestRandomSelectorWeightedPick (a 2-entry weighted selector over two distinct simple_block sub-features → for a fixed seed, the EXACT sub-feature picked + its leaf state placed + the per-entry NextFloat draw count). TestRandomSelectorFallsToDefault (a seed where no entry passes → default placed). TestSimpleRandomSelectorUniform (NextIntN(N) pick). TestRandomBooleanSelector (NextBoolean one-bit draw → true/false branch). TestNestedSelectorTerminates (selector→selector→simple_block resolves with one rng, terminates). TestSubFeatureModifiersReapply (a sub-feature with a count modifier → the sub-feature's own draws happen, proving the recursion threads the rng). TestInlineVsRefSubFeature (both an inline {feature,placement} entry and a ref-string entry resolve + place).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./world/ && go test ./world/ -run 'TestRandomSelector|TestSimpleRandomSelector|TestRandomBoolean|TestNestedSelector|TestSubFeatureModifiers|TestInlineVsRef' -count=1</automated>
  </verify>
  <done>The 3 selectors re-decode their config, resolve nested sub-features (ref + inline) via the plumbed registry, and place the jar-picked sub-feature via placeSubFeature with the rng threaded through the sub-feature's own modifiers; the weighted pick + nested recursion + sub-modifier re-apply are draw-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BlockPile + FallenTree + VegetationPatch bodies</name>
  <files>world/feature_misc.go, world/feature_misc_test.go</files>
  <behavior>
    - block_pile places its state_provider blocks in the jar pile pattern (the radius/height draws + per-position placement) for a seed → exact positions + states.
    - fallen_tree places a horizontal trunk_provider log line of log_length blocks in a random horizontal direction, then applies its log/stump decorators as blocks — draw-exact.
    - vegetation_patch lays ground_state in an xz_radius disk down `depth` (gated by the replaceable predicate), then places the vegetation_feature sub-PlacedFeature on top per vegetation_chance (via placeSubFeature); waterlogged variant waterlogs the placed blocks.
    - All write only through bctx.view.SetBlock; a patch near a chunk edge spills into the neighbor.
  </behavior>
  <action>
feature_misc.go: blockPileBody / fallenTreeBody / vegetationPatchBody (+ the waterlogged variant) — each transcribed javap -c (BlockPileFeature / FallenTreeFeature / VegetationPatchFeature / WaterloggedVegetationPatchFeature.place), decoding their config from cf.Config.Raw (state_provider / trunk_provider+log_length+decorators / ground_state+xz_radius+depth+vegetation_feature+replaceable+vegetation_chance) via 12-01's feature.ParseProvider + the IntProvider Sample (log_length/xz_radius) + placeSubFeature (the vegetation_feature inner — uses bctx.reg). The replaceable gate reuses 12-01's BlockPredicate (resolve the `replaceable` tag → StateID set). init() registers all four types. Honor the exact draw order.
feature_misc_test.go: TestBlockPilePattern (seed → exact pile positions + states). TestFallenTreeLine (a horizontal log line of log_length in the drawn direction + decorators). TestVegetationPatchDisk (the ground_state disk + the vegetation_feature on top for a seed). TestMiscCrossChunkEdge.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/ -run 'TestBlockPile|TestFallenTree|TestVegetationPatch|TestMiscCrossChunk' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>block_pile/fallen_tree/vegetation_patch (+waterlogged) place jar-exact patterns for known seeds, cross-chunk spill confirmed, registered.</done>
</task>

<task type="auto">
  <name>Task 3: the final Phase-12 acceptance gate (5x5 determinism + emit-once + Docker -race with REAL bodies)</name>
  <files>world/decoration_test.go</files>
  <action>
With all FEAT-03 bodies now LIVE in the registry (ore/simple_block/random_patch from 12-02; selectors + pile/fallen/vegetation_patch from this plan), re-assert the Phase-11 determinism contract under REAL block writes. First re-read decoration_test.go for the AS-BUILT makePlacer call shape (12-01 added the *feature.Registry param to newConfiguredPlacer + updated these sites). Add/extend: TestFeatureBodiesProduceBlocks (a chunk whose biome references an ore + a vegetation placed_feature now has the expected ore/vegetation blocks after Decorate — proving the bodies actually write through the live pipeline, not just in unit tests), and confirm TestDecorationReorderIdentical (the 5x5, two request orders) STILL produces byte-identical chunks with the real bodies (the rng-draw determinism holds end-to-end), and TestEmitOnce/TestEmitOnceUnderHold hold. Run the full Docker -race gate over ./world/... to prove the live decoration with real bodies is race-clean (the bodies are pure over (rng, Neighborhood) on the single scheduler goroutine).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/ -run 'TestFeatureBodiesProduceBlocks|TestDecorationReorderIdentical|TestEmitOnce' -count=1 && MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...</automated>
  </verify>
  <done>The 5x5 reorder determinism + emit-once gates stay green with the REAL bodies placing blocks; Docker -race over ./world/... is clean; a live-decoration test confirms ore + vegetation blocks actually appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → selector/misc config decode + sub-feature resolution | Selector + feature config JSON (incl. nested sub-features re-decoded from Raw) is build-time-trusted (pinned 26.2 jar). The bodies run off-tick on the single worker scheduler goroutine over server-derived anchors; no client input reaches them. |
| selector → sub-feature recursion | The recursion resolves sub-features through reg.ResolvePlaced, whose 11-01 cycle guard rejects any placed->configured->placed loop — the recursion terminates by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | feature_selector.go (sub-feature RNG not threaded — Pitfall #9) | mitigate | placeSubFeature Binds the sub-feature's modifier chain and runs its body with the SAME passed rng (no fork/new source); TestSubFeatureModifiersReapply pins that the sub-feature's own draws advanced the shared rng — a broken thread fails the test (forest would collapse to one type). |
| T-12-10 | Tampering | selector draw order (NextBoolean / NextFloat-per-entry divergence) | mitigate | NextBoolean is the 12-01 next(1) primitive (NOT NextIntN(2)); random_selector draws one NextFloat per entry in order. The selector tests pin the picked branch + the draw count for a seed. |
| T-12-11 | Denial of Service | nested selector recursion (unbounded depth) | accept | ResolvePlaced's cycle guard (11-01) rejects loops; recursion depth is bounded by the build-trusted feature graph (vanilla nests ≤ a few levels). A pathological depth would be a build-data bug caught on resolve, not a runtime input. |
| T-12-12 | Tampering | live decoration determinism regression with real bodies | mitigate | The final gate re-runs the 5x5 reorder + emit-once + Docker -race with all real bodies writing — a draw-order regression in ANY body breaks byte-identity and fails the gate. |
</threat_model>

<verification>
- `go test ./world/ -run 'TestRandomSelector|TestSimpleRandomSelector|TestRandomBoolean|TestNestedSelector|TestSubFeatureModifiers|TestInlineVsRef|TestBlockPile|TestFallenTree|TestVegetationPatch' -count=1` green: selectors re-decode + resolve nested sub-features via the plumbed registry and thread the rng through them (draw-pinned), the misc bodies place jar-exact patterns.
- `go test ./world/ -run 'TestFeatureBodiesProduceBlocks|TestDecorationReorderIdentical|TestEmitOnce' -count=1` green: real bodies write blocks; the 5x5 reorder determinism + emit-once hold.
- Docker -race over `./world/...` clean (CGO golang:1.26).
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps (go.mod/go.sum unchanged).
</verification>

<success_criteria>
- FEAT-03 criterion 3: random_selector/simple_random_selector/random_boolean_selector resolve nested sub-features (re-decoded from Raw, resolved via the plumbed registry) with the rng threaded through (the weighted feature choice works — Pitfall #9 satisfied).
- FEAT-03 criterion 4: BlockPile/FallenTree/VegetationPatch place deterministically per seed — the full non-tree ground cover + ore set generates.
- The Phase-11 5x5 determinism + emit-once gates stay green with the REAL bodies live, Docker -race clean — the determinism contract holds end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-feature-types/12-03-SUMMARY.md`
</output>
