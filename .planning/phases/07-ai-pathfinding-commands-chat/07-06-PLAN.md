---
phase: 07-ai-pathfinding-commands-chat
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - server/chat_command_capture_test.go
  - .planning/phases/07-ai-pathfinding-commands-chat/07-CAPTURE-DIFF.md
  - .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-commands.bin
  - .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-system-chat.bin
  - .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-serverbound-chat.bin
autonomous: false
requirements: [AI-01, AI-02, AI-03, CMD-01, CMD-02]

must_haves:
  truths:
    - "a capture-diff test (server/chat_command_capture_test.go) byte-compares Sulfur's ClientboundCommands (the command graph), ClientboundSystemChat (a broadcast), and the ServerboundChat DECODE (Sulfur consumes a real vanilla client's chat bytes without mis-framing) against real vanilla 26.2 server/client packets — the WIRE surfaces are the ONLY 776 truth via capture-diff (a self-round-trip is symmetric and insufficient — the standing Phase-2/4/5/6 lesson); the AI/A*/spawn LOGIC has no wire and is gated by the real-client visual check, not a byte-diff"
    - "the vanilla golden packets are captured + committed as fixtures so CI byte-diffs without booting Java every run; the test t.Skips gracefully with a clear message if a fixture is absent (native CI stays green; the capture is the gate)"
    - "07-CAPTURE-DIFF.md records the capture method + the per-field diff (ClientboundCommands node framing, ClientboundSystemChat content+overlay, ServerboundChat readUtf256/instant/salt/nullable-sig/lastSeen trailing layout) and RESOLVES the chat/command Open Questions (A2 SystemChat-renders-as-broadcast; A3 command graph completeness; A6 whether the client needs a ServerboundChatAck before accepting SystemChat back); if a capture reveals a divergence, the fix is applied to the OWNING plan's encoder (server/chat.go, server/commands.go) and the affected tests re-run before sign-off"
    - "an unmodified vanilla 26.2 client connects to cmd/sulfur and the FULL interactive loop works (BLOCKING human sign-off, mirroring 06-07): a naturally-spawned (or debug) mob WANDERS with vanilla-style AI and NAVIGATES around an obstacle (the ported GoalSelector + A* visibly working), a /command runs and REPLIES, and a chat message broadcasts to all players — no kick/hang; this is the decisive AI-01/02/03 + CMD-01/02 milestone that cannot be self-approved"
    - "the executor FIRST does the automatable capture/diff work (boot the cached vanilla 26.2 server, capture ClientboundCommands + a SystemChat + a real ServerboundChat, commit golden fixtures, byte-diff Sulfur's encoders/decoder, finalize 07-CAPTURE-DIFF.md, fix + re-diff any divergence), THEN PAUSES for the blocking human-verify checkpoint"
  artifacts:
    - path: "server/chat_command_capture_test.go"
      provides: "TestChatCommandBytesVsVanillaCapture: the authoritative CMD-01/02 byte-diff against vanilla golden fixtures (ClientboundCommands framing, SystemChat content+overlay, ServerboundChat decode)"
      contains: "func TestChatCommandBytesVsVanillaCapture"
    - path: ".planning/phases/07-ai-pathfinding-commands-chat/07-CAPTURE-DIFF.md"
      provides: "the vanilla 26.2 capture-diff record + the resolution of the chat/command Open Questions + the AI-01/02/03 + CMD-01/02 interactive human sign-off"
      contains: "ClientboundCommands"
    - path: ".planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-commands.bin"
      provides: "the committed golden vanilla ClientboundCommands packet"
      contains: ""
  key_links:
    - from: "server/chat_command_capture_test.go"
      to: ".planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-commands.bin"
      via: "load the golden vanilla packet + byte-diff Sulfur's encoder output"
      pattern: "vanilla-commands"
    - from: "07-CAPTURE-DIFF.md"
      to: "the real vanilla 26.2 interactive session"
      via: "the human confirms a mob wanders + navigates an obstacle (AI), a command replies (CMD-01), a chat broadcasts (CMD-02)"
      pattern: "interactive"
---

<objective>
Seal the TWO Phase-7 wire surfaces against the ONLY authoritative source for proto 776 — a byte-for-byte capture-diff against a real vanilla 26.2 server — and then confirm the AI-01/02/03 + CMD-01/02 INTERACTIVE milestone: an unmodified vanilla 26.2 client sees a mob WANDER with vanilla-style AI and NAVIGATE around an obstacle (the ported GoalSelector + A*), runs a /command that REPLIES, and broadcasts a chat message to all players. This mirrors Phases 4/5/6: a self-round-trip cannot prove vanilla-correctness for a protocol no spec covers (the wiki documents ≤773; the `ClientboundCommands` node framing, the `ClientboundSystemChat` content+overlay, and the `ServerboundChat` trailing signature/lastSeen layout are jar-confirmed in SHAPE but their exact bytes drift). The capture-diff is the producer-side proof; the real-client interactive run is the consumer-side proof. The AI/A*/spawn LOGIC has NO wire (a moved mob rides the Phase-6-sealed entity encoders) — it is gated by the real-client VISUAL check, not a byte-diff.

The executor FIRST does the automatable capture/diff work (boot `temp/cache/26.2-server.jar` offline+flat, capture a `ClientboundCommands` at join, a `ClientboundSystemChat` broadcast, and a real `ServerboundChat` the vanilla client sends; commit golden fixtures; byte-diff Sulfur's encoders + the `ServerboundChat` decoder; finalize `07-CAPTURE-DIFF.md` resolving the chat/command Open Questions; apply any fix to the owning plan and re-run), THEN PAUSES for a BLOCKING human-verify checkpoint: the user runs an unmodified vanilla 26.2 client against `cmd/sulfur` and confirms a mob wanders + navigates an obstacle, a command replies, and a chat broadcasts.

Purpose: CMD-01/02 wire-correctness (the uncertain command-graph + chat surfaces) and AI-01/02/03 + CMD-01/02 (the interactive milestone) are the phase's highest-risk requirements — "mobs behave and navigate, the server dispatches commands and broadcasts chat" is only TRUE when a real client SEES a mob wander/navigate, a command reply, and a chat broadcast. This plan is autonomous:false because the decisive check — a real client + a live world — cannot be self-approved.
Output: server/chat_command_capture_test.go (the golden byte-diff), the committed vanilla golden .bin fixtures, and a signed-off 07-CAPTURE-DIFF.md resolving the Open Questions. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-01-PLAN.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-02-PLAN.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-03-PLAN.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-04-PLAN.md
@.planning/phases/07-ai-pathfinding-commands-chat/07-05-PLAN.md
@.planning/phases/06-entities-physics-interaction/06-07-PLAN.md
@.planning/phases/05-player-session-in-world-first-playable/05-03-PLAN.md

<interfaces>
<!-- VERIFIED this session. The capture-diff consumes the encoders/decoder from Plans 07-04/07-05. -->

From server/commands.go (Plan 07-04 — the command-graph send under test):
```go
func buildCommandGraph() *command.Graph // the v1 command tree (ClientboundCommands via Graph.WriteTo/ClientJoin)
func (t *TickLoop) runCommand(p *tickPlayer, cmd string) // Graph.Execute + the SystemChat reply (07-05)
```

From server/chat.go (Plan 07-05 — the chat encoder/decoder under test):
```go
func (t *TickLoop) handleChat(p *tickPlayer, raw pk.Packet) // ServerboundChat decode (the DECODER under test)
func (t *TickLoop) broadcastSystemChat(text string)         // ClientboundSystemChat (content + overlay) — the ENCODER under test
```

From server/command/serialize.go (the ClientboundCommands serialization — under test):
```go
func (g *Graph) WriteTo(w io.Writer) (int64, error) // serialize.go:16 — the node framing byte-diffed vs vanilla
func (g *Graph) ClientJoin(client Client)           // component.go — sends packetid.ClientboundCommands(g)
```

From the bot harness + the Phase-4/5/6 capture method (reuse — VERIFIED present):
```go
// Point the fork bot/client at the VANILLA server (offline login -> config -> play), capture the raw bytes
// of ClientboundCommands (sent at join) + ClientboundSystemChat (trigger a /say or a server message), and
// capture a ServerboundChat (what the vanilla CLIENT sends when you type a chat message).
// Alternatively a logging TCP proxy between a vanilla client and the vanilla server.
// The exact method is documented in 05-CAPTURE-DIFF.md / 06-CAPTURE-DIFF.md.
```

Environment (VERIFIED available per 07-RESEARCH Environment Availability):
```
temp/cache/26.2-server.jar  (the real vanilla 26.2 server)
temp/cache/26.2-inner.jar   (javap source for any remaining jar-derive)
Java (Zulu/Temurin) 25      (run the vanilla server; javap)
golang:1.26 Docker          (-race, host CGO_ENABLED=0)
PrismLauncher 26.2 client   (the interactive gate, operator-run)
```
</interfaces>

<key_facts>
<!-- VERIFIED against 07-RESEARCH this session. -->
- THE AUTHORITATIVE GATE (the Phase-2/4/5/6 lesson): a Go self-round-trip CANNOT prove the uncertain encoders are vanilla-correct (it is symmetric — a shared bug round-trips green). The ClientboundCommands node framing, the ClientboundSystemChat content+overlay, and the ServerboundChat trailing signature/lastSeen layout are jar-confirmed in SHAPE but MEDIUM-confidence in exact bytes (07-RESEARCH A2/A3/A6, Confidence: chat/command wire MEDIUM until the capture-diff). Only a byte-diff vs the real vanilla 26.2 server proves them.
- TWO WIRE SURFACES (07-RESEARCH Summary + Sampling Rate): (1) ClientboundCommands (the command graph) — capture what the vanilla server sends a joining client; (2) ServerboundChat / ClientboundSystemChat — capture a real chat round-trip (the ServerboundChat the vanilla CLIENT sends + a SystemChat the vanilla server sends). The AI/A*/goal LOGIC is gameplay behavior (NO wire) — gated by the real-client visual check, not a byte-diff. The moved mob rides the Phase-6-sealed entity encoders (no new wire).
- CAPTURE METHOD (mirror 05/06-CAPTURE-DIFF.md): boot temp/cache/26.2-server.jar offline + flat with a fixed seed/spawn (online-mode=false, eula accepted). Capture (1) the ClientboundCommands sent at join (the command tree); (2) a ClientboundSystemChat (trigger a server message / a /say); (3) a ServerboundChat (type a chat message in a vanilla client pointed at the vanilla server — capture what the CLIENT sends, to seal Sulfur's DECODER). Commit each as a golden .bin under fixtures/. Document whether the packet-id byte is included.
- Sulfur side: produce ClientboundCommands (buildCommandGraph.WriteTo) + ClientboundSystemChat (broadcastSystemChat) with the same inputs and byte-diff the FRAMING; and assert Sulfur's handleChat DECODER consumes the captured vanilla ServerboundChat bytes WITHOUT mis-framing (the readUtf256/instant/salt/nullable-sig/lastSeen trailing layout — the A6 risk).
- Test TestChatCommandBytesVsVanillaCapture: load each fixture, parse it, assert Sulfur's encoder produces a structurally-matching packet (the ClientboundCommands node framing; the SystemChat content+overlay) and that Sulfur's ServerboundChat decoder consumes the captured vanilla client bytes cleanly. t.Skip with a message pointing at 07-CAPTURE-DIFF.md if a fixture is absent (native CI stays green; the capture is the gate). Mirror 06-07's TestEntityBytesVsVanillaCapture + 05-03's TestPlayBytesVsVanillaCapture.
- RESOLVE THE OPEN QUESTIONS IN 07-CAPTURE-DIFF.md from the capture/real-client run:
    - A2: SystemChat renders as an acceptable broadcast (the real client shows "<name> msg") — confirmed by the interactive run.
    - A3: the fork command.Graph serializes a ClientboundCommands the real client accepts (tab-completes) — confirmed by the byte-diff + the interactive /command.
    - A6: whether the client needs a ServerboundChatAck / session handshake before accepting SystemChat back — confirmed by the chat round-trip in the interactive run (if chat never appears, add the ack; the v1 plan started with decode-and-ignore).
- If ANY divergence is found, FIX it in the OWNING plan's encoder (server/commands.go / server/command/serialize.go for ClientboundCommands; server/chat.go for SystemChat / the ServerboundChat decode), re-run that plan's wire tests + this capture-diff, and record the fix in 07-CAPTURE-DIFF.md before sign-off.
- THE AI-01/02/03 + CMD-01/02 REAL-CLIENT INTERACTIVE CHECK (the human-verify): connect an unmodified vanilla 26.2 client (PrismLauncher) to cmd/sulfur and confirm: (a) a naturally-spawned (or debug) mob is VISIBLE and WANDERS with vanilla-style AI (the ported GoalSelector — stroll/look, NOT sinusoidal pacing); (b) the mob NAVIGATES AROUND an obstacle (place a wall between the mob and its wander target — the A* routes around it, the 07-02 seam visibly working); (c) a /command runs and REPLIES (e.g. /say broadcasts, or a command sends a SystemChat reply — CMD-01); (d) a chat message typed by the player broadcasts to all players as "<name> message" (CMD-02); (e) NO kick / no "decoder error" / no "Loading terrain…" hang. This is the decisive milestone; it cannot be self-approved.
- A SPAWN + OBSTACLE TRIGGER for the interactive run: ensure cmd/sulfur SPAWNS a visible mob driven by the real AI (the debug pig retired to real AI in 07-03, or the natural spawner) and that the operator can place a wall (block place from Phase 6) to observe the A* navigate around it. Provide a clear way to make the mob path (e.g. its stroll goal naturally targets across the obstacle, or a debug "go here" trigger) so the navigation is OBSERVABLE.
- ZERO new dependencies. Pure stdlib testing + the existing bot/ harness + the cached jar.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create server/chat_command_capture_test.go (package server) + the fixtures/ dir + .bin files. Finalize 07-CAPTURE-DIFF.md. If a fix is needed, make a precise symbol-level edit in the owning encoder and re-run. Read 06-07-PLAN.md's Task 1/2 + 05-03-PLAN.md for the exact capture-diff + human-verify checkpoint shape to mirror.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sulfur chat/command encoders -> vanilla 26.2 client | The client silently rejects/mis-renders a packet whose byte framing diverges from vanilla (chat never appears, command tree rejected, "decoder error" kick). The only authoritative validation is a byte-diff vs the real server + a real-client interactive run. |
| Vanilla golden fixture -> Sulfur decoder (the captured ServerboundChat) | Sulfur's handleChat decoder must consume the real vanilla client's chat bytes (incl. the trailing signature/lastSeen) without mis-framing; the captured chat is a trusted committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-13 | Tampering (data integrity) | a wire-framing divergence (wrong ClientboundCommands node framing, wrong SystemChat content+overlay, mis-framed ServerboundChat trailing layout) that self-round-trips green but the client rejects/mis-renders | mitigate | Byte-diff against the real vanilla 26.2 server output (the golden fixtures) + the real-client interactive run — the framing assertions are exactly the divergences that reject the command tree / vanish chat / kick on a decoder error. Any divergence is fixed in the owning plan and re-diffed before sign-off. |
| T-7-14 | Spoofing/Info | the ServerboundChat decoder over-reading the trailing signature/lastSeen and mis-framing the next packet | mitigate | The capture-diff proves Sulfur's decoder consumes a REAL vanilla client's ServerboundChat (incl. the trailing fields) cleanly without mis-framing the stream (A6); decode is defensive (Scan-error -> no-op). Asserted by the ServerboundChat decode portion of the test. |
| T-7-17 | Denial of Service | decode of a captured golden packet over-allocating from a length field | accept | The golden fixtures are trusted committed artifacts captured from the real vanilla server/client (not attacker input); parsing uses bounded pk.Scan with no unbounded make-before-check. Low risk for trusted test fixtures. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture vanilla 26.2 command/chat packets + the byte-diff test (automatable prep)</name>
  <files>server/chat_command_capture_test.go, .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-commands.bin, .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-system-chat.bin, .planning/phases/07-ai-pathfinding-commands-chat/fixtures/vanilla-serverbound-chat.bin, .planning/phases/07-ai-pathfinding-commands-chat/07-CAPTURE-DIFF.md</files>
  <read_first>
    - .planning/phases/07-ai-pathfinding-commands-chat/07-RESEARCH.md — the Validation Architecture (the capture-diff surfaces), CMD-01/02 wire facts, Pitfall 2, the Open Questions A2/A3/A6, Environment Availability.
    - .planning/phases/05-player-session-in-world-first-playable/05-03-PLAN.md + 05-CAPTURE-DIFF.md — the proven capture method + the diff-test shape.
    - .planning/phases/06-entities-physics-interaction/06-07-PLAN.md — the Task-1/Task-2 capture-diff + human-verify shape to mirror exactly.
    - server/commands.go + server/command/serialize.go + server/chat.go — the encoders/decoder under test (Plans 07-04/07-05).
  </read_first>
  <behavior>
    CMD-01/02 (authoritative byte-diff — automatable portion):
    - Capture: boot temp/cache/26.2-server.jar offline + flat + fixed seed; capture the ClientboundCommands sent at join, a ClientboundSystemChat (server message / /say), and a real ServerboundChat (typed in a vanilla client); commit each as a golden .bin under fixtures/.
    - Test TestChatCommandBytesVsVanillaCapture: load the ClientboundCommands fixture; assert Sulfur's buildCommandGraph.WriteTo produces structurally-matching node framing. Load the SystemChat fixture; assert Sulfur's broadcastSystemChat framing (content NBT Component + Boolean overlay) matches. Load the ServerboundChat fixture; assert Sulfur's handleChat decoder consumes it without mis-framing (the readUtf256/instant/salt/nullable-sig/lastSeen trailing layout). Skip with a clear message if a fixture is absent.
  </behavior>
  <action>
    1. Capture (document every step in 07-CAPTURE-DIFF.md so it is reproducible — mirror 05/06-CAPTURE-DIFF.md):
       - Start the vanilla server: `java -jar temp/cache/26.2-server.jar` in a temp dir with server.properties = flat level-type, fixed level-seed, online-mode=false, eula accepted. Record the exact server.properties.
       - Capture: point the fork bot/client at the vanilla server (offline login -> config -> play) OR a logging proxy. Save the ClientboundCommands sent at join; trigger + save a ClientboundSystemChat (a server message / a /say broadcast); type a chat message in a real vanilla client and save the ServerboundChat the CLIENT sends. Save each to the three fixtures/*.bin (document whether the id byte is included).
    2. Write server/chat_command_capture_test.go (package server): TestChatCommandBytesVsVanillaCapture as in <behavior>. Load each fixture with os.ReadFile (t.Skip if missing). Parse + assert the vanilla framing, then assert Sulfur's encoder matches (or, for the ServerboundChat, that Sulfur's decoder consumes it cleanly). Keep the assertions on the load-bearing framing (the ClientboundCommands node tree framing, the SystemChat content+overlay, the ServerboundChat trailing layout); document any content (vs framing) difference.
    3. Finalize 07-CAPTURE-DIFF.md: the capture method, the exact server.properties/seed/inputs, the per-field diff table, and the resolution of the Open Questions (A2 SystemChat-renders; A3 command-graph completeness; A6 ServerboundChatAck necessity — deferred to the Task-2 real-client run if the byte-diff alone cannot confirm it). If a divergence is found, FIX it in the owning plan's encoder (server/commands.go / serialize.go / server/chat.go) and re-run that plan's wire tests + this test; record the fix.
  </action>
  <verify>
    <automated>go test ./server/ ./server/command/ -run 'TestChatCommandBytesVsVanillaCapture|TestCommandGraphBuilds|TestSystemChatBroadcast|TestChatDecodeKeepsMessage' -count=1 && go vet ./... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The three golden fixtures are captured from the real vanilla 26.2 server/client and committed.
    - TestChatCommandBytesVsVanillaCapture loads the fixtures and asserts Sulfur's ClientboundCommands node framing + the SystemChat content+overlay match vanilla, and that the handleChat decoder consumes a real ServerboundChat cleanly; it skips cleanly if a fixture is absent.
    - 07-CAPTURE-DIFF.md records the reproducible capture method, the per-field diff, and resolves the chat/command Open Questions (A2/A3/A6); any divergence found is fixed in the owning plan and re-diffed.
    - `go build ./...`, `go vet ./...` clean; zero new deps.
  </acceptance_criteria>
  <done>Sulfur's ClientboundCommands / ClientboundSystemChat / ServerboundChat wire is byte-diffed against a real vanilla 26.2 server: the golden fixtures are committed, the diff test asserts the vanilla framing + the clean decode, the Open Questions are resolved (ack necessity deferred to the real-client run if needed), and any divergence is fixed — the automatable half of the authoritative CMD-01/02 gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real vanilla 26.2 client INTERACTS (mob wanders/navigates + command + chat) + human sign-off (Phase-7 milestone gate)</name>
  <files>.planning/phases/07-ai-pathfinding-commands-chat/07-CAPTURE-DIFF.md</files>
  <action>
    This is a human-verification checkpoint, not an autonomous code task. The capture-diff (Task 1) proves the chat/command bytes match vanilla; this proves a real client SEES a mob wander with vanilla-style AI, navigate around an obstacle, run a command, and broadcast a chat — the decisive AI-01/02/03 + CMD-01/02 milestone that cannot be self-approved (mirrors 06-07 + 05-03). The executor must FIRST ensure Task 1 is complete (fixtures committed, diff green, Open Questions resolved, any divergence fixed and re-diffed), finalize 07-CAPTURE-DIFF.md, then PAUSE and present it. Do not self-approve.

    Automatable preparation (do before pausing):
    1. Confirm Task 1's TestChatCommandBytesVsVanillaCapture is green and 07-CAPTURE-DIFF.md resolves the Open Questions with any divergence fixed + re-diffed.
    2. Run the full suite + the Docker -race gate over ./server/... ./world/... to confirm the AI/navigation/spawn/command/chat tick seams are race-clean under load (Plans 07-01..05) — this is where the live tickAI path (07-03 wired 07-01/07-02) + the chat/command dispatch are proven race-clean.
    3. Run cmd/sulfur (which now: drives the ported GoalSelector AI + A* navigation for mobs, spawns mobs via the NaturalSpawner / the real-AI debug pig, dispatches commands via the fork graph, and broadcasts chat via SystemChat). Confirm it builds, listens on proto 776, and has the spawn + obstacle triggers wired so the operator can SEE a mob wander, place a wall, and watch it navigate around.
  </action>
  <what-built>
    The full Phase 7 interactive world: the ported GoalSelector goal model (Plan 07-01) + the request->snapshot->result A* navigation (Plan 07-02, the Phase-8 hinge) drive mobs to wander + navigate around obstacles via the existing moveEntity (the moved mob auto-broadcasts via the Phase-6-sealed tracker — no new wire); the faithful-minimal NaturalSpawner populates the world under the CREATURE cap and tickAI() is filled to drive it all, with the throwaway sinusoidal debug pig retired to real AI (Plan 07-03); the fork command.Graph dispatches server-side commands sent at join as ClientboundCommands (Plan 07-04); and chat is received + broadcast via SystemChat with the documented PlayerChat globalIndex (Plan 07-05). 07-CAPTURE-DIFF.md + the golden fixtures are the audit artifacts proving Sulfur's uncertain command/chat encoders match a real vanilla 26.2 server's.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/07-ai-pathfinding-commands-chat/07-CAPTURE-DIFF.md.
    2. Confirm the per-field diff shows Sulfur's encoders match vanilla framing: the ClientboundCommands node tree framing; the ClientboundSystemChat content (NBT Component) + Boolean overlay; the ServerboundChat decoder consuming a real vanilla client's chat (readUtf256/instant/salt/nullable-sig/lastSeen) without mis-framing.
    3. Confirm the chat/command Open Questions are resolved (A2 SystemChat renders as a broadcast; A3 the command graph is accepted/tab-completes; A6 whether a ServerboundChatAck is needed before SystemChat is accepted back).
    4. The decisive check — connect an UNMODIFIED vanilla 26.2 client (PrismLauncher) to cmd/sulfur and confirm:
       a. MOB WANDERS (AI-01): a spawned mob is VISIBLE and wanders with vanilla-style AI (stroll + look at the player), NOT the old sinusoidal pacing (the ported GoalSelector working).
       b. MOB NAVIGATES AN OBSTACLE (AI-02): place a wall between the mob and where it wants to go — the mob ROUTES AROUND the wall (the A* request->snapshot->result seam visibly working), it does not walk into the wall or clip through.
       c. MOBS POPULATE (AI-03): mobs spawn in the world over time (the NaturalSpawner under the cap) — the world is not empty.
       d. COMMAND RUNS + REPLIES (CMD-01): type a /command (e.g. /say hi) — it runs and the result is visible (a broadcast / a SystemChat reply).
       e. CHAT BROADCASTS (CMD-02): type a chat message — it broadcasts to all players as "<name> message".
       f. NO KICK / NO HANG: the client is not kicked (decoder error) and does not hang.
  </how-to-verify>
  <verify>Human sign-off — the user types the resume signal after confirming the capture-diff and that a real client sees a mob wander + navigate an obstacle, a command run/reply, and a chat broadcast.</verify>
  <done>The user has reviewed 07-CAPTURE-DIFF.md, confirmed Sulfur's command/chat encoders match vanilla 26.2 (ClientboundCommands node framing, SystemChat content+overlay, ServerboundChat decode), and confirmed an unmodified vanilla 26.2 client INTERACTS — a mob wanders with vanilla-style AI and navigates around an obstacle (AI-01/02), mobs populate the world (AI-03), a command runs and replies (CMD-01), and a chat message broadcasts to all players (CMD-02) — then typed "approved". The Phase-7 AI-01/02/03 + CMD-01/02 interactive milestone is met.</done>
  <resume-signal>Type "approved" if the capture-diff matches vanilla and a real client sees a mob wander + navigate an obstacle, a command run/reply, and a chat broadcast, or describe the divergence (mob stuck/sinusoidal/walks-into-wall, command not running, chat not appearing, kick on a decoder error) to fix.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./server/ ./server/command/ -run 'TestChatCommandBytesVsVanillaCapture' -count=1` passes (the byte-diff asserts the vanilla command/chat framing; the ServerboundChat decode consumes a real client's chat).
- The three golden fixtures are committed; 07-CAPTURE-DIFF.md records the diff + resolves the Open Questions + is signed off.
- An unmodified vanilla 26.2 client connected to cmd/sulfur SEES a mob wander + navigate an obstacle (AI-01/02), mobs populate (AI-03), a command run/reply (CMD-01), and a chat broadcast (CMD-02) — human-confirmed.
- Docker `-race` over `./server/... ./world/...` clean (the live tickAI AI path + the chat/command dispatch are tick-owned — TICK-05); `go build ./...` clean; any divergence found in the diff was fixed in the owning plan and re-diffed; zero new dependencies.
</verification>

<success_criteria>
- CMD-01/02 (authoritative): Sulfur's ClientboundCommands (node framing) / ClientboundSystemChat (content+overlay) / ServerboundChat (decode) bytes match a real vanilla 26.2 server — the framing that rejects the command tree / vanishes chat / kicks on a decoder error is correct.
- AI-01/02/03 + CMD-01/02 (real-client milestone): an unmodified vanilla 26.2 client connects and INTERACTS — a mob wanders with vanilla-style AI and navigates around an obstacle (the ported GoalSelector + A* seam visibly working), mobs populate the world, a command runs and replies, and a chat broadcasts — no kick/hang.
- The chat/command Open Questions (A2/A3/A6) are resolved from the capture + real-client run and recorded; any divergence is fixed in the owning plan and re-diffed before sign-off.
- The capture-diff + real-client interactive run are the authoritative gate (a self-round-trip is insufficient by design — the Phase-2/4/5/6 symmetric-bug lesson); the AI/A*/spawn LOGIC has no wire and is gated by the visual check.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-pathfinding-commands-chat/07-06-SUMMARY.md`.
</output>
</content>
