---
phase: 29-damage-keystone-s2
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/damage_source.go
  - server/combat_mob.go
  - server/entity.go
  - server/combat_mob_test.go
autonomous: true
requirements: [MOB-SUB-01, MOB-SUB-02]

must_haves:
  truths:
    - "A Go-native mob (*Entity) loses health when hit, with i-frame/invulnerable-time gating identical to the jar (the >10.0F excess gate, the amount<=lastHurt no-op, the fresh-hit invulnerableTime=20 arm)."
    - "actuallyHurtEntity reads the mob's REAL *attribute.Map via e.getAttributeValue (ARMOR/ARMOR_TOUGHNESS/MAX_ABSORPTION) — not a faked curve."
    - "src.is bypasses_armor is a GENUINE read against the Plan-01 tag table — armor is skipped only when the source bypasses it; never const false."
    - "The mob records its real lastDamageSource = {typeTag, attacker} in the flag2 block (bytecode 449-451) — a real ported source, NOT a faked hurt flag."
    - "The on_damage Emit fires at the post-mitigation site with the final landed amount."
    - "The i-frame DECREMENT (hurtTime--, invulnerableTime--) happens in a mob baseTick step (integer-only, RNG-free); pig oracle stays green."
  artifacts:
    - path: "server/damage_source.go"
      provides: "damageSource value type {typeTag, attacker}, damageTypeID consts, is(tag) over the Plan-01 table"
      contains: "type damageSource struct"
    - path: "server/combat_mob.go"
      provides: "applyDamageEntity, actuallyHurtEntity, getDamageAfterArmorAbsorbEntity, tickMobIFrames"
      contains: "func (t *TickLoop) applyDamageEntity"
    - path: "server/entity.go"
      provides: "new tick-owned *Entity fields: health, lastHurt, invulnerableTime, hurtTime, hurtDuration, lastDamageSource"
      contains: "lastDamageSource damageSource"
  key_links:
    - from: "server/combat_mob.go actuallyHurtEntity"
      to: "server/combat.go free helpers"
      via: "combatRulesGetDamageAfterAbsorb / maxF / mthClampF (reused, NOT duplicated)"
      pattern: "combatRulesGetDamageAfterAbsorb"
    - from: "server/combat_mob.go getDamageAfterArmorAbsorbEntity"
      to: "data/tag DamageTypeTags"
      via: "src.is bypasses_armor"
      pattern: "is\\(\"bypasses_armor\"\\)"
    - from: "server/combat_mob.go actuallyHurtEntity"
      to: "Entity.getAttributeValue(*attribute.Map)"
      via: "e.getAttributeValue(attribute.Armor)"
      pattern: "getAttributeValue\\(attribute\\."
---

<objective>
Build the keystone hurt pipeline: the *Entity siblings of the v3-sealed *tickPlayer combat path. applyDamageEntity (port of LivingEntity.hurtServer) + actuallyHurtEntity (port of LivingEntity.actuallyHurt, NOT Player) + the real damageSource value type + the mob baseTick i-frame decrement. The mob loses i-frame-gated health read off its REAL *attribute.Map, records its genuine lastDamageSource, and the armor-bypass branch is a real tag read against the Plan-01 table. ~80% a copy of combat.go with the three jar-verified mob-vs-player tail diffs and the lastDamageSource set added (MOB-SUB-01, MOB-SUB-02).

Purpose: Every PanicGoal (P31), target-selector (P35), and wolf-anger (P36) behavior reads this. Get the math + the source storage 1:1 before any consumer exists.
Output: server/damage_source.go, server/combat_mob.go, the new *Entity fields in server/entity.go, server/combat_mob_test.go. Attack ROUTING + cross-region is Plan 03; death/loot/XP is Plan 04 — this plan stops at "health hits 0" and leaves dieEntity as a documented seam Plan 04 fills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-damage-keystone-s2/29-CONTEXT.md
@.planning/phases/29-damage-keystone-s2/29-RESEARCH.md
@.planning/phases/29-damage-keystone-s2/29-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
REUSE-VERBATIM free helpers (server/combat.go — call them, do NOT duplicate):
  mthClampF(value, min, max float32) float32                                    // l.128
  combatRulesGetDamageAfterAbsorb(damage, armor, armorToughness float32) float32 // l.543
  combatRulesGetDamageAfterMagicAbsorb(...) float32                             // l.562
  maxF(a, b float32) float32                                                    // l.603
  isNaN32(f float32) bool / isInf32(f float32) bool                             // l.613-614
  const hurtCooldownConst float32 = 10.0 (l.141); hurtInvulnerableTicks int32 = 20 (l.146); hurtDurationTicks int32 = 10 (l.150)

The PLAYER path to MIRROR (server/combat.go):
  func (t *TickLoop) applyDamage(p *tickPlayer, amount float32)   // l.188 hurtServer i-frame gate; l.213 the >10.0F gate + amount<=lastHurt no-op + else invulnerableTime=20
  func (t *TickLoop) actuallyHurt(p *tickPlayer, amount float32)  // l.262; l.311 t.plugins.Emit(host.EventDamage, host.DamageEvent{...}) post-mitigation site
  func (t *TickLoop) getDamageAfterArmorAbsorb(p *tickPlayer,...) // l.337
  func (t *TickLoop) getDamageAfterMagicAbsorb(p *tickPlayer,...) // l.365

Mob attribute read (server/entity.go:200 — reads the REAL *attribute.Map with DefaultValue fallback):
  func (e *Entity) getAttributeValue(attr *attribute.Attribute) float64
  singletons (level/attribute/): attribute.Armor, attribute.ArmorToughness, attribute.MaxAbsorption, attribute.MaxHealth

Damage event payload (plugin/host/event.go:23,155): host.EventDamage; host.DamageEvent{ EntityID int; Amount float64 }
Region-scoped Emit (server/region_transfer.go:86): func (r *region) emitEntityEvent(event host.EventType, payload host.Event)  — use this, NOT raw t.plugins.Emit (PITFALLS Pitfall 7)
Plan-01 tag table (data/tag/tags.go): var tag.DamageTypeTags map[string]map[int32]bool
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): mob hurt-pipeline test scaffold</name>
  <read_first>
    - server/combat_test.go:131-132 (the player hurt-pipeline test pattern — invulnerableTime/lastHurt assertions to mirror)
    - server/combat.go:188-321 (applyDamage/actuallyHurt — the exact values to assert)
    - data/tag/tags.go (Plan 01 output — pick a real bypasses_armor member id and the player_attack id)
    - JAR to javap BEFORE asserting: LivingEntity.hurtServer + LivingEntity.actuallyHurt in temp/cache/26.2-inner.jar (the >10.0F gate, the amount<=lastHurt no-op, the armor curve constants, the absorption fold order)
  </read_first>
  <behavior>
    - TestApplyDamageEntity_FreshHit: a 20-health, 0-armor mob hit for 6.0 -> health 14.0, invulnerableTime=20, lastHurt=6.0, hurtDuration=hurtTime=10.
    - TestApplyDamageEntity_IFrameExcessGate: within i-frames (invulnerableTime>10) a smaller-or-equal follow-up is a no-op; a larger one applies (amount-lastHurt) only.
    - TestActuallyHurtEntity_Armor: a mob with armor on its *attribute.Map takes reduced damage matching combatRulesGetDamageAfterAbsorb exactly (float32).
    - TestDamageSourceIs_BypassesArmor: the bypass branch skips the armor fold for a bypasses_armor source (full damage), folds for player_attack.
    - TestLastDamageSource: after a hit, e.lastDamageSource == {typeTag, attacker}.
    - TestMobIFrameDecrement: tickMobIFrames decrements hurtTime (unconditional) and invulnerableTime (>0), integer-only.
    - TestMobOnDamageEmit: actuallyHurtEntity fires host.EventDamage with the post-mitigation amount.
  </behavior>
  <action>
    Create server/combat_mob_test.go (package server). Build a minimal *Entity fixture with a seeded *attribute.Map (grep server unit tests for how an *Entity with attributes is constructed; reuse that helper). Encode the exact float32 values javap-verified in read_first — not approximate. For the bypass test, use a real bypasses_armor-member damage-type id from data/tag/tags.go. For the Emit test, capture via the existing harness (grep server/plugin_events_test.go:95 for the EventDamage capture pattern). Tests reference applyDamageEntity / actuallyHurtEntity / tickMobIFrames / the new *Entity fields / damageSource — red until Tasks 1-3 land.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go vet ./server/ 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - server/combat_mob_test.go (package server) contains the 7 named tests above.
    - Each deterministic damage assertion uses an exact javap-derived float32 value (no epsilon-only checks for the exact cases).
    - The bypass test references a real damage-type id from data/tag/tags.go.
    - Red until the pipeline lands (expected at this task).
  </acceptance_criteria>
  <done>The 7-behavior scaffold exists with javap-exact values; red until the pipeline lands.</done>
</task>

<task type="auto">
  <name>Task 1: damage_source.go — the real DamageSource value type + is(tag)</name>
  <read_first>
    - .planning/phases/29-damage-keystone-s2/29-PATTERNS.md (the damage_source.go value-type pattern, l.99-110)
    - level/loot/context.go (a small typed value struct with accessor methods — the role analog)
    - data/tag/tags.go (Plan 01 — the DamageTypeTags map this reads)
    - server/entity.go:30-36 (the snapshot-friendly contract — damageSource must be a plain value: int enum + int32, NO pointers)
    - JAR to javap BEFORE writing: net.minecraft.world.damagesource.DamageSource — confirm is(TagKey)==type.is(tag) and getEntity()==causingEntity; confirm the damage-type id values for player_attack / mob_attack / fall from the damage_type registry
  </read_first>
  <action>
    Create server/damage_source.go (package server). Define type damageTypeID int32 with named consts for the types Phase 29 uses — at minimum damageTypePlayerAttack, damageTypeMobAttack, damageTypeFall — using the EXACT numeric ids from the damage_type registry (cross-reference data/tag/tags.go member ids / the registry-id data; do NOT invent ids). Define type damageSource struct { typeTag damageTypeID; attacker int32 } (attacker = entity id of the causing entity, 0=none per DamageSource.getEntity==causingEntity — NEVER a live pointer, Folia rule). Implement func (s damageSource) is(tag string) bool returning tag.DamageTypeTags[tag][int32(s.typeTag)] — the genuine membership read (DamageSource.is(tag)==type.is(tag)==id in tag set). Add func damageSourcePlayerAttack(attackerID int32) damageSource. Cite the jar class/method in comments. Confirm pointer-free (snapshot-friendly).
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./server/ 2>&1 | head && CGO_ENABLED=0 go test ./server/ -run TestDamageSourceIs -count=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - server/damage_source.go contains type damageSource struct with typeTag damageTypeID and attacker int32, no pointer fields.
    - Contains func (s damageSource) is(tag string) bool reading tag.DamageTypeTags.
    - damageTypeID consts use real registry ids (damageTypePlayerAttack maps to a player_attack id that is a member of is_player_attack/panic_causes in data/tag/tags.go).
    - Header cites the jar class/method.
    - go test ./server/ -run TestDamageSourceIs green.
  </acceptance_criteria>
  <done>damage_source.go ships the real {typeTag, attacker} value type with a genuine is(tag); TestDamageSourceIs green.</done>
</task>

<task type="auto">
  <name>Task 2: entity.go — tick-owned hurt fields + the mob baseTick i-frame decrement wiring</name>
  <read_first>
    - server/entity.go:42-167 (the Entity struct — where to add fields), :30-36 (snapshot-friendly contract), :119-135 (the ai/attributes exemption these value fields follow), :200 (getAttributeValue)
    - server/combat.go:95-114 (tickPlayerCombat — the PLAYER decrement at the ServerPlayer site; the mob site is DIFFERENT: baseTick, the !(instanceof ServerPlayer) guard is auto-true for a mob)
    - server/ai_mob.go (the mob per-tick path / serverAiStep driver — to find the RNG-free site to call tickMobIFrames; the spawn site newPigAI / vanilla_pig.go:66 to set initial health)
    - JAR to javap BEFORE writing: LivingEntity.baseTick (bytecode ~451-488) — confirm if(hurtTime>0)hurtTime-- (unconditional) and if(invulnerableTime>0 && !(this instanceof ServerPlayer))invulnerableTime--
  </read_first>
  <action>
    Modify server/entity.go. Add to the Entity struct, as PLAIN VALUE types (same exemption ai/attributes carry at l.119-135): health float32 (LivingEntity.health), lastHurt float32, invulnerableTime int32, hurtTime int32, hurtDuration int32, lastDamageSource damageSource (MOB-SUB-02). Comment each citing its LivingEntity field. Initialize health to the mob's MaxHealth at the goal-bearing-mob spawn site(s) (read e.getAttributeValue(attribute.MaxHealth) where the *Entity is constructed — grep spawnDeclaredMob / vanilla_pig.go / newPigAI spawn sites; do NOT leave health=0 or a mob is born dead). Wire the per-tick i-frame decrement call into the mob tick path OUTSIDE serverAiStep's goal callbacks and navigation.tick (the baseTick-equivalent slot) — call t.tickMobIFrames(e) (defined in combat_mob.go Task 3). The decrement is integer-only / RNG-free so it does not perturb the pig RNG stream (PITFALLS Pitfall 5), but it must not sit inside the AI RNG flow. Cite LivingEntity.baseTick.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./server/ 2>&1 | head && CGO_ENABLED=0 go test ./server/ -run 'TestMobIFrameDecrement|TestPluginPigEqualsGoNativePig' -count=1 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/entity.go Entity struct contains health float32, lastHurt float32, invulnerableTime int32, hurtTime int32, hurtDuration int32, lastDamageSource damageSource (grep each).
    - No pointer fields added (snapshot-friendly contract intact).
    - health initialized to MaxHealth at the mob spawn site (grep for getAttributeValue(attribute.MaxHealth) or an explicit health set at spawn).
    - t.tickMobIFrames(e) is called from the mob per-tick path OUTSIDE serverAiStep goal callbacks / navigation.tick.
    - go test ./server/ -run TestMobIFrameDecrement green AND TestPluginPigEqualsGoNativePig STILL GREEN.
  </acceptance_criteria>
  <done>The six value hurt fields exist on *Entity, health initialized at spawn, the RNG-free i-frame decrement wired; pig oracle green.</done>
</task>

<task type="auto">
  <name>Task 3: combat_mob.go — applyDamageEntity + actuallyHurtEntity (3 mob diffs + lastDamageSource + region-scoped Emit + tickMobIFrames)</name>
  <read_first>
    - server/combat.go:188-238 (applyDamage — the i-frame gate control flow to MIRROR), :262-321 (actuallyHurt — the armor/absorption fold), :337-392 (getDamageAfterArmorAbsorb/getDamageAfterMagicAbsorb), :543/603/128/613-614 (the free helpers to REUSE), :311 (the Emit site), :570-area (the getArmorValue math.Floor d2f cast)
    - server/entity.go:200 (getAttributeValue for ARMOR/ARMOR_TOUGHNESS/MAX_ABSORPTION)
    - server/damage_source.go (Task 1 — damageSource.is)
    - .planning/phases/29-damage-keystone-s2/29-PATTERNS.md:30-90 (the keystone pattern: the flag2 lastDamageSource add, the 3 diffs, the region-scoped Emit)
    - server/region_transfer.go:86 (emitEntityEvent — use INSTEAD of raw t.plugins.Emit; PITFALLS Pitfall 7)
    - JAR to javap BEFORE writing (the authority): LivingEntity.hurtServer (the i-frame gate + lastDamageSource set at bytecode 449-451 in the flag2 block) + LivingEntity.actuallyHurt (the armor->magic->absorption fold order; the NO causeFoodExhaustion / NO SetHealth diffs vs Player)
  </read_first>
  <action>
    Create server/combat_mob.go (package server). Port func (t *TickLoop) applyDamageEntity(e *Entity, src damageSource, amount float32) from LivingEntity.hurtServer, mirroring combat.go applyDamage EXACTLY (every op float32): isDeadOrDying guard (health<=0 -> return); amount<0 -> 0; NaN/Inf via isNaN32/isInf32 -> 3.4028235e38; the i-frame gate if float32(e.invulnerableTime) > hurtCooldownConst && !src.is("bypasses_cooldown") { if amount <= e.lastHurt { return }; actuallyHurtEntity(e, src, amount-e.lastHurt); e.lastHurt = amount } else { e.lastHurt = amount; e.invulnerableTime = hurtInvulnerableTicks; actuallyHurtEntity(e, src, amount); e.hurtDuration = hurtDurationTicks; e.hurtTime = e.hurtDuration }. MOB DIFF: set e.lastDamageSource = src in the flag2 (fresh-hit) block per bytecode 449-451 (MOB-SUB-02). Then if e.health <= 0 { t.dieEntity(e, src) } — dieEntity is a SEAM Plan 04 implements; in THIS plan declare a minimal func (t *TickLoop) dieEntity(e *Entity, src damageSource) that marks the entity dead/removed (a cited TODO Plan 04 replaces) so the package builds. Port func (t *TickLoop) actuallyHurtEntity(e *Entity, src damageSource, amount float32) from LivingEntity.actuallyHurt with the THREE diffs: (1) the armor fold calls a new getDamageAfterArmorAbsorbEntity(e, src, amount) that gates on src.is("bypasses_armor") — skip the fold when true, else call the SHARED combatRulesGetDamageAfterAbsorb(amount, armor, toughness) with armor=float32(e.getAttributeValue(attribute.Armor)) (apply the math.Floor d2f cast as combat.go does) and toughness from attribute.ArmorToughness (real *attribute.Map — NOT const false); (2) NO causeFoodExhaustion (Player-only, omit); (3) NO client SetHealth send — just e.health = e.health - amount (clamp 0); recordDamage + gameEvent(ENTITY_DAMAGE) stay cited no-op stubs. Absorption fold: REUSE maxF (amount = maxF(amount-absorption, 0)); if amount == 0 { return }. Fire on_damage at the post-mitigation site via the owning region's r.emitEntityEvent(host.EventDamage, host.DamageEvent{EntityID:int(e.id), Amount:float64(amount)}) — region-scoped (in this plan the synchronous same-region caller; Plan 03 supplies the cross-region routing). Add func (t *TickLoop) tickMobIFrames(e *Entity) { if e.hurtTime>0 {e.hurtTime--}; if e.invulnerableTime>0 {e.invulnerableTime--} }. Cite the jar class/method per function. DO NOT duplicate any combat.go free helper.
  </action>
  <verify>
    <automated>cd /d/ender && CGO_ENABLED=0 go build ./server/ && CGO_ENABLED=0 go test ./server/ -run 'Damage|Hurt|DamageSource' -count=1 2>&1 | tail -25 && CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server/combat_mob.go contains func (t *TickLoop) applyDamageEntity and func (t *TickLoop) actuallyHurtEntity and func (t *TickLoop) tickMobIFrames.
    - getDamageAfterArmorAbsorbEntity contains is("bypasses_armor") and calls combatRulesGetDamageAfterAbsorb (grep both); no math.Max / float64 combat math; no const bypassesArmor=false.
    - applyDamageEntity sets e.lastDamageSource = src in the fresh-hit block (grep e.lastDamageSource = src).
    - The on_damage Emit uses emitEntityEvent (grep emitEntityEvent in combat_mob.go), NOT t.plugins.Emit.
    - A dieEntity seam exists (cited TODO for Plan 04).
    - go test ./server/ -run 'Damage|Hurt|DamageSource' green (all 7 Task-0 behaviors) AND TestPluginPigEqualsGoNativePig green.
    - go list -deps ./... | grep -i gopy EMPTY; each ported func cites its jar method.
  </acceptance_criteria>
  <done>applyDamageEntity/actuallyHurtEntity ported 1:1 with the 3 diffs + lastDamageSource set + region-scoped Emit; all damage tests green; pig oracle green; no duplicated helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> applyDamageEntity | The amount is server-computed (Plan 03 supplies it from the ATTACK_DAMAGE attr); applyDamageEntity never trusts a client value. |
| region goroutine -> plugin Emit | The on_damage Emit must be region-scoped (emitEntityEvent), not a raw cross-goroutine plugins.Emit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | applyDamageEntity (server-authoritative damage) | mitigate | Damage amount is server-computed; applyDamageEntity is server-only (mirrors the v3-sealed applyDamage). The client never names an amount. (Reach/region gating is Plan 03.) |
| T-29-05 | Tampering/DoS | actuallyHurtEntity NaN/Inf input | mitigate | NaN/Inf clamp to Float.MAX (isNaN32/isInf32) verbatim from hurtServer — a malformed amount cannot corrupt health to NaN. |
| T-29-02 | Tampering | on_damage Emit from a region goroutine | mitigate | Use emitEntityEvent (region-scoped, nil-guarded) not raw plugins.Emit — prevents a cross-goroutine plugin-registry race (full cross-region barrier is Plan 03). |

This plan applies damage SYNCHRONOUSLY on the mob's owning region (the same-region fast path); the cross-region barrier-queue (T-29-02 full mitigation) lands in Plan 03.
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... && go vet ./... clean.
- CGO_ENABLED=0 go test ./server/ -run 'Damage|Hurt|DamageSource' green (all 7 behaviors).
- CGO_ENABLED=0 go test ./server/ -run TestPluginPigEqualsGoNativePig GREEN (no AI RNG perturbed — the i-frame decrement is integer-only).
- go list -deps ./... | grep -i gopy EMPTY (no cgo / no new deps).
- Every ported function cites its jar method (LivingEntity.hurtServer / actuallyHurt / baseTick; DamageSource.is/getEntity).
</verification>

<success_criteria>
- MOB-SUB-01: a Go-native mob takes i-frame-gated, attribute-read, armor-folded damage with exact vanilla float32 values; on_damage fires post-mitigation.
- MOB-SUB-02: the mob stores its real lastDamageSource {typeTag, attacker} (not a faked flag).
- The armor-bypass branch is a genuine tag read (no const false). combat.go helpers reused, never duplicated. CGO=0; pig oracle green.
</success_criteria>

<output>
After completion, create .planning/phases/29-damage-keystone-s2/29-02-SUMMARY.md.
</output>
