---
phase: 12-core-feature-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - world/levelgen/random.go
  - world/levelgen/feature/provider.go
  - world/levelgen/feature/provider_test.go
  - world/levelgen/placement/predicate.go
  - world/levelgen/placement/predicate_test.go
  - world/levelgen/placement/modifiers.go
  - world/levelgen/placement/heightprovider.go
  - world/levelgen/placement/modifiers_test.go
  - world/levelgen/placement/heightprovider_test.go
  - world/placement_context.go
  - world/noisegen.go
  - world/decoration_test.go
  - world/feature_body.go
  - world/feature_body_test.go
autonomous: true
requirements: [FEAT-03]
user_setup: []

must_haves:
  truths:
    - "A BlockStateProvider hierarchy exists in world/levelgen/feature: BlockStateProvider interface (GetState(rng, pos) block.StateID), SimpleStateProvider (single state, 0 draws), WeightedStateProvider (weighted pick: ONE rng draw over the cumulative weight, JAR-exact entry order), RuleBasedStateProvider (a fallback provider + ordered rules picking by the position's existing block), NoiseProvider/DualNoiseProvider (sample a NormalNoise at the position, map to a state list — for flower-forest variety). The provider is PARSED from the already-resolved ParsedConfig (the {Name,Properties} leaves are block.StateIDs after 11-01's walkBlockStates) — no re-resolution."
    - "Two new LCG primitives are ported on LegacyRandomSource in world/levelgen/random.go, jar-exact: NextBoolean() (java.util.Random.nextBoolean = next(1) != 0 — ONE bit, NOT NextIntN(2), needed by 12-03's random_boolean_selector since next() is unexported so a world-package helper cannot reach it) and NextGaussian() (java.util.Random.nextGaussian — the two-draw polar/Marsaglia method with the cached-second-value state, needed by the clamped_normal IntProvider). Both are added to the RandomSource interface (or kept LegacyRandomSource-only + type-asserted at the call site — choose the lower-churn option) and draw-pinned against a hand-traced oracle."
    - "The IntProvider TrapezoidalInt (\"trapezoid\": min/max/plateau PLAIN INTS — DISTINCT from the existing HeightProvider trapezoid which uses min_inclusive/max_inclusive VerticalAnchors) is ported in placement/heightprovider.go parseIntProvider as the PRIMARY random_offset spread (62 of 75 random_offset occurrences, e.g. flower_default's xz_spread {trapezoid,max:7,min:-7,plateau:0}); clamped_normal (via NextGaussian) and very_biased_to_bottom IntProviders are also ported there. parseIntProvider no longer errors on trapezoid/clamped_normal/very_biased_to_bottom."
    - "The deferred random_offset placement modifier is now BOUND in placement.BindModifier (11-02 deferred it): it draws xz_spread.Sample(rng) for X, y_spread.Sample(rng) for Y, xz_spread.Sample(rng) for Z (JAR RandomOffsetPlacement order x,y,z) and returns one offset position. The trapezoid IntProvider above is what its spreads sample."
    - "block_predicate_filter is BOUND as a PlacementFilter whose shouldPlace evaluates a BlockPredicate over the candidate position via the PlacementContext: the predicate set the FEAT-03 vegetation placed_features use — matching_block_tag (the position's block is in a block tag), matching_blocks (block in an explicit set), would_survive (the to-place state could survive at the position — gated by a conservative ground check), all_of/any_of/not (composites), solid, replaceable, matching_fluids — ported jar-exact, erroring loudly on an unported predicate type."
    - "The configured-feature placer dispatch is refactored into a featureBody REGISTRY that PLUMBS the *feature.Registry: featureBody is (bctx *bodyContext, cf, ctx, rng, pos) bool where bodyContext carries view + *feature.Registry; newConfiguredPlacer additionally takes the *feature.Registry (sourced from g.deco.registry in noisegen.go's makePlacer closure), so a registered body (12-03's selectors) can resolve a nested sub-PlacedFeature ref via reg.ResolvePlaced and inline-parse via reg.ParsePlacedFeature. Plans 12-02 and 12-03 register their bodies from SEPARATE files via registerFeatureBody with NO edit to a shared switch. An unregistered real type stays a recordable no-op (the tree/dungeon types Phase 13 owns); the test_set_block path + the invocation recorder are preserved."
    - "go build ./... and CGO_ENABLED=0 go build ./... are clean; no new deps (go.mod/go.sum unchanged). The provider draws + the random_offset draw order + NextBoolean/NextGaussian + the trapezoid sample have determinism tests; block_predicate_filter has a keep/drop test; the registry has a register/lookup + registry-plumbing test."
  artifacts:
    - path: "world/levelgen/random.go"
      provides: "NextBoolean (next(1)) + NextGaussian (two-draw polar) on LegacyRandomSource, jar-exact"
      contains: "NextBoolean"
    - path: "world/levelgen/feature/provider.go"
      provides: "BlockStateProvider interface + Simple/Weighted/RuleBased/Noise/DualNoise providers, parsed from ParsedConfig (states already StateIDs)"
      contains: "type BlockStateProvider"
    - path: "world/levelgen/placement/heightprovider.go"
      provides: "IntProvider trapezoid (TrapezoidalInt) + clamped_normal (NextGaussian) + very_biased_to_bottom ported in parseIntProvider"
      contains: "trapezoid"
    - path: "world/levelgen/placement/predicate.go"
      provides: "BlockPredicate set (matching_block_tag/matching_blocks/would_survive/all_of/any_of/not/solid/replaceable/matching_fluids) + ParsePredicate, errs loudly on unported"
      contains: "BlockPredicate"
    - path: "world/levelgen/placement/modifiers.go"
      provides: "random_offset + block_predicate_filter bound in BindModifier (11-02 deferred random_offset)"
      contains: "random_offset"
    - path: "world/placement_context.go"
      provides: "featureBody registry dispatch with the *feature.Registry plumbed so 12-03's selectors resolve nested sub-features"
      contains: "featureBody"
    - path: "world/noisegen.go"
      provides: "makePlacer closure passes g.deco.registry into newConfiguredPlacer (the registry-plumbing source)"
      contains: "newConfiguredPlacer"
    - path: "world/feature_body.go"
      provides: "featureBody type + the registry + register helper + the bodyContext (carries *feature.Registry) the selector recursion (12-03) resolves sub-features through"
      contains: "featureBody"
  key_links:
    - from: "world/levelgen/feature/provider.go WeightedStateProvider.GetState"
      to: "world/levelgen random.RandomSource"
      via: "ONE rng draw over the cumulative weight, JAR entry order"
      pattern: "GetState"
    - from: "world/levelgen/placement/modifiers.go random_offset"
      to: "world/levelgen/placement/heightprovider.go IntProvider trapezoid Sample"
      via: "xz then y then xz draws (x,y,z order), spreads sampled via TrapezoidalInt"
      pattern: "trapezoid"
    - from: "world/noisegen.go makePlacer"
      to: "world/feature_body.go bodyContext (*feature.Registry)"
      via: "g.deco.registry threaded into newConfiguredPlacer so a body can resolve nested sub-features"
      pattern: "registry"
    - from: "world/placement_context.go newConfiguredPlacer"
      to: "world/feature_body.go featureBodies registry"
      via: "dispatch on cf.Type looks up a registered body (no-op if absent)"
      pattern: "featureBodies"
---

<objective>
Land the SHARED PREREQUISITES every FEAT-03 feature body needs before any body can place a correct block: (1) the `BlockStateProvider` hierarchy (`Simple`/`Weighted`/`RuleBased` + `Noise`/`DualNoise`) that ore/patch/selector configs reference via `to_place`/`state_provider`/`ground_state`, (2) the two load-bearing placement modifiers 11-02 deferred that the vegetation placed_features require — `random_offset` (the tries/spread scatter — literally criterion 2's "jar-exact tries/spread draw order") and `block_predicate_filter` (the `would_survive`/`matching_block_tag` ground gate), (3) the LCG primitives those + 12-03 need that live on the unexported `next()` (`NextBoolean`, `NextGaussian`) — ported on `LegacyRandomSource` HERE so wave-2 stays file-disjoint, and (4) the dispatch refactor that turns the Phase-11 inline no-op switch into a `featureBody` REGISTRY **with the `*feature.Registry` plumbed through** so 12-03's selectors can resolve nested sub-features, and so 12-02 and 12-03 register their bodies from disjoint files and run in parallel.

DISCOVERY FINDING (verified against the embedded 26.2 data this session): vanilla 26.2 has ZERO top-level `random_patch` configured_features — grass/flowers/dead-bush are `simple_block` configured_features whose placed_features do the scatter via `count` + `random_offset` + `block_predicate_filter`. So the vegetation criterion's "tries/spread" lives in `random_offset` (a PLACEMENT modifier 11-02 deferred), not only in a `RandomPatchFeature` body. The DOMINANT random_offset spread is the IntProvider **`trapezoid` (TrapezoidalInt: plain-int min/max/plateau)** — 62 of 75 occurrences — which is a DIFFERENT type from the existing HeightProvider `trapezoid` (VerticalAnchor min_inclusive/max_inclusive) and is NOT in the ported `parseIntProvider`; it must be added here or random_offset cannot bind. Ore placed_features (verified `ore_coal`) use ONLY already-bound modifiers, so `OreFeature` needs nothing from this plan beyond the registry.

REGISTRY PLUMBING (checker BLOCKER 1, verified): `decorationData.registry` (decoration.go:19) holds the `*feature.Registry`, and noisegen.go's `makePlacer` closure (noisegen.go:243-250) has `g.deco.registry` in scope but does NOT pass it into `newConfiguredPlacer`. The current `featureBody` signature `(view, cf, ctx, rng, pos)` and the `placementContext`/`ConfiguredFeature` structs carry NO registry, so 12-03's `placeSubFeature` resolving a nested ref like `"minecraft:fallen_oak_tree"` via `reg.ResolvePlaced` would have nothing to call it on. Because 12-02/12-03 are file-disjoint parallel wave-2 plans, neither can edit the body seam — THIS plan must establish the registry plumbing.

Purpose: 12-02 (ore + simple_block/random_patch bodies) and 12-03 (selectors + pile/fallen/vegetation_patch) both consume the providers + the registry; 12-03's selectors additionally resolve nested sub-features through the plumbed registry and use NextBoolean; the vegetation bodies consume the two new modifiers (whose spreads sample the trapezoid IntProvider) via their placed_feature chains.
Output: random.go (NextBoolean/NextGaussian), provider.go (the hierarchy), the trapezoid/clamped_normal/very_biased IntProviders, predicate.go + the two bound modifiers, and the registry+plumbing refactor — all determinism-tested, zero new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/phases/11-feature-pipeline-decoration/11-02-SUMMARY.md
@.planning/phases/11-feature-pipeline-decoration/11-03-SUMMARY.md

<interfaces>
<!-- ALL verified on disk this session. -->

THE PARSED CONFIG (world/levelgen/feature/feature.go, verified) — a provider/feature body reads its type-specific fields out of ParsedConfig.Raw; the {Name,Properties} block-state leaves are ALREADY resolved to block.StateID in ParsedConfig.States by 11-01's walkBlockStates:
```go
type ParsedConfig struct {
    Raw    json.RawMessage   // verbatim "config" object JSON — decode type-specific fields here
    States []block.StateID   // every {Name,Properties} leaf, resolved at parse time (DO NOT re-resolve)
}
type ConfiguredFeature struct { ID, Type string; Config *ParsedConfig }
```
NOTE: walkBlockStates resolves bare {Name,Properties} refs. For a provider, re-decode the provider envelope from Raw and resolve its inner states via the SAME resolveBlockState path 11-01 used (feature/blockstate.go resolveBlockState — block.FromID + nbt Properties strip + block.ToStateID). Prefer reusing that helper over indexing States by position (position-indexing is brittle across nested providers).

THE RNG (world/levelgen/random.go, verified lines 303-372) — providers + modifiers consume RandomSource; LegacyRandomSource holds the 48-bit LCG state and has an UNEXPORTED `next(bits int) int32` (line 324). The two primitives to ADD (java.util.Random, jar-exact):
```go
// NextBoolean = next(1) != 0   (ONE bit; NOT NextIntN(2) — different draw). 12-03's random_boolean_selector needs it.
func (r *LegacyRandomSource) NextBoolean() bool { return r.next(1) != 0 }
// NextGaussian = the two-draw polar (Marsaglia) method WITH the cached-second-value state:
//   if haveNextNextGaussian { clear flag; return nextNextGaussian }
//   loop: v1=2*nextDouble()-1; v2=2*nextDouble()-1; s=v1*v1+v2*v2; until s<1 && s!=0
//   multiplier = sqrt(-2*ln(s)/s); nextNextGaussian = v2*multiplier; set flag; return v1*multiplier
// (LegacyRandomSource needs the bool+float64 cache fields. Transcribe java.util.Random.nextGaussian.)
func (r *LegacyRandomSource) NextGaussian() float64 { ... }
```
Add both to the RandomSource INTERFACE (so WorldgenRandom exposes them via the embedded LegacyRandomSource) OR keep them LegacyRandomSource-only and type-assert at the call site — choose the lower-churn option. If you add to the interface, Xoroshiro must also implement them (it has the draws — port the same algorithms) so `var _ RandomSource = (*Xoroshiro)(nil)` still holds; otherwise keep them off the interface + type-assert. Document the choice. clamped_normal uses NextGaussian; random_boolean_selector uses NextBoolean.

THE INTPROVIDER GAP (world/levelgen/placement/heightprovider.go, verified parseIntProvider 353-396) — cases today: constant/uniform/clamped/biased_to_bottom/weighted_list; default ERRORS naming clamped_normal/very_biased. ADD:
```
trapezoid (TrapezoidalInt — net.minecraft.util.valueproviders.TrapezoidalInt.sample): fields min,max,plateau (PLAIN INTS).
  sample: range = max-min; if plateau==range return min + rng.nextInt(range+1);  // uniform fallback
          base = (range-plateau)/2; rangeMinusBase = range-base;
          return min + rng.nextInt(rangeMinusBase+1) + rng.nextInt(base+1);  // TWO nextInt draws — transcribe exact
  ** This is the PRIMARY random_offset spread (62/75). DISTINCT from the HeightProvider trapezoid (anchors). **
clamped_normal (ClampedNormalInt.sample): mean,deviation,min,max -> Mth.clamp(round(mean+deviation*nextGaussian()), min, max). Uses NextGaussian.
very_biased_to_bottom (VeryBiasedToBottomInt.sample): the jar's two/three-nextInt biased pick — transcribe exact.
```
Each ported draw-exact; remove them from the default error. (The intProvider struct likely needs new kind constants + fields min/max/plateau/mean/deviation — extend it.)

THE PLACEMENT MODIFIER SEAM (world/levelgen/placement, verified) — BindModifier(type, raw, deps) turns a PlacementModifierRaw into a bound modifier; 11-02 ERRORS loudly on random_offset today:
```go
// 11-02 modifiers.go: in_square draws NextIntN(16) x then z. PlacementFilter base = shouldPlace ? {p} : {}.
// RandomOffsetPlacement (JAR net.minecraft.world.level.levelgen.placement.RandomOffsetPlacement.getPositions):
//   x = p.X + xzSpread.Sample(rng); y = p.Y + ySpread.Sample(rng); z = p.Z + xzSpread.Sample(rng)   // 3 draws: x,y,z
// (xz_spread is sampled TWICE — once for x, once for z — y from y_spread; spreads are IntProviders, usually trapezoid.)
```
PlacementContext (verified context.go) exposes GetHeight/MinY/Height/GetBlock(x,y,z) block.StateID/BiomeAt — block_predicate_filter reads GetBlock.

THE BLOCK PREDICATES (the FEAT-03 vegetation set — verified across the embedded placed_features) — port from net.minecraft.world.level.levelgen.blockpredicates.BlockPredicate:
```
matching_block_tag  : the block at offset(p) is in a block tag        -> tag membership (see TAG PRECEDENT below)
matching_blocks     : the block at offset(p) is in an explicit set    -> StateID set membership
would_survive       : the to-place state would survive at p           -> conservative ground gate; transcribe BlockPredicate.wouldSurvive + document the conservative port
all_of / any_of / not : composite predicates (recurse)
solid               : the block at offset(p) is solid -> NOT air (conservative)
replaceable         : the block at offset(p) is replaceable -> air-or-plant set
matching_fluids     : the fluid at offset(p) is in a set -> water/lava StateID set
```
Most predicates carry an "offset":[dx,dy,dz] (default 0,0,0). matching_block_tag with tag "minecraft:air" (the common flower/grass case) = "the block at p is air".

THE TAG PRECEDENT (world/levelgen/carver/config.go, verified lines 348-465) — Replaceables resolves a #block tag to a concrete StateID set, with nested tags expanded constant-for-constant (nestedReplaceableTags map, cited inline from the jar's data/minecraft/tags/block/*.json). matching_block_tag reuses THIS pattern: resolve the tag id to a StateID set (air is just block.IsAir; for the handful of other tags the vegetation predicates use, expand constant-for-constant like Replaceables does and cite the jar tag). Keep the resolved sets small + cited; do NOT embed new tag JSON unless a tag is large.

THE DISPATCH TO REFACTOR (world/placement_context.go, verified lines 90-140; makePlacer at noisegen.go:243-250) — TODAY newConfiguredPlacer(cf, view, testBlock, hasTest, invocations) discards ctx + rng (`_, _`) and no-ops every real type. makePlacer in noisegen.go has `g.deco.registry` in scope but does NOT pass it. REFACTOR (this plan):
```go
// world/feature_body.go:
type featureBody func(bctx *bodyContext, cf *feature.ConfiguredFeature, ctx placement.PlacementContext, rng levelgen.RandomSource, pos placement.BlockPos) bool
// bodyContext carries the shared deps a body (esp. 12-03's selector recursion) needs:
type bodyContext struct {
    view *Neighborhood        // the 3x3 write proxy
    reg  *feature.Registry    // for placeSubFeature: reg.ResolvePlaced(id) / reg.ParsePlacedFeature(id, raw)
}
var featureBodies = map[string]featureBody{}
func registerFeatureBody(t string, b featureBody) { featureBodies[t] = b }   // 12-02/12-03 call from init()
```
newConfiguredPlacer gains a `*feature.Registry` param (and builds the bodyContext): if a body is registered for ftype, call it with the bodyContext + the REAL ctx + rng (NOT the discarded ones); else the recordable no-op. KEEP test_set_block + the invocation recorder. noisegen.go makePlacer passes `g.deco.registry`; decoration_test.go's makePlacer sites pass the test registry (or nil where bodies are unused). CRITICAL: capture ctx + rng (currently `_`); the body consumes them so the determinism chain continues.

Registry accessors (verified): `(*Registry).ResolvePlaced(id string) (*PlacedFeature, error)` (registry.go:70), `(*Registry).ParsePlacedFeature(id string, raw json.RawMessage) (*PlacedFeature, error)` (parse.go:188) — these are what placeSubFeature (12-03) calls on bctx.reg.

CONFIRMED ON DISK: world/levelgen/feature/ (provider goes here, beside parse/blockstate). world/levelgen/placement/ (predicate + the 2 modifiers + the IntProviders). world/levelgen/random.go (NextBoolean/NextGaussian). world/ + world/noisegen.go (the registry refactor + plumbing). NO import cycle: feature does NOT import placement; placement imports feature; world imports both. The provider lives in feature (used by bodies in world via feature.ParseProvider).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NextBoolean + NextGaussian LCG primitives + BlockStateProvider hierarchy</name>
  <files>world/levelgen/random.go, world/levelgen/feature/provider.go, world/levelgen/feature/provider_test.go</files>
  <behavior>
    - NextBoolean returns next(1) != 0 (ONE bit draw); a seeded sequence matches a hand-traced java.util.Random.nextBoolean oracle (NOT NextIntN(2)).
    - NextGaussian returns the two-draw polar value with the cached second value (the second call returns the cached nextNextGaussian with ZERO new draws); a seeded sequence matches the java.util.Random.nextGaussian oracle.
    - SimpleStateProvider.GetState returns its single StateID with ZERO rng draws.
    - WeightedStateProvider.GetState draws exactly ONCE (jar WeightedList pick) and returns an entry by the cumulative-weight walk; a seeded rng returns the jar-exact entry (the test pins both the picked StateID and the post-draw rng state).
    - RuleBasedStateProvider.GetState picks by the position's existing block against ordered rules, falling back to the fallback provider when no rule matches.
    - NoiseProvider/DualNoiseProvider.GetState sample a NormalNoise at the position and index the state list (0 rng draws — noise is positional).
    - ParseProvider(raw) decodes the provider envelope by its "type", resolving inner states via the in-package resolveBlockState; an unported provider type errors loudly.
  </behavior>
  <action>
random.go: add `NextBoolean()` (= next(1) != 0) and `NextGaussian()` (the java.util.Random two-draw polar method with the haveNextNextGaussian + nextNextGaussian cache fields on LegacyRandomSource) — transcribe java.util.Random, cite it. Decide interface membership: add to the RandomSource interface IF Xoroshiro can implement equivalents cheaply (port the same algorithms there); else keep LegacyRandomSource-only + type-assert at the clamped_normal/selector call sites. Document the choice + ensure `var _ RandomSource = (*WorldgenRandom)(nil)` (and the Xoroshiro assertion) still holds.
provider.go: the `BlockStateProvider` interface (`GetState(rng levelgen.RandomSource, x, y, z int) block.StateID`). Port (cited javap -c, net.minecraft.world.level.levelgen.feature.stateproviders.*): SimpleStateProvider (0 draws), WeightedStateProvider (SimpleWeightedRandomList.getRandomValue — ONE draw over total weight, cumulative walk, entry order = determinism), RuleBasedStateProvider (fallback + ordered rules on the existing block), NoiseProvider + DualNoiseProvider (sample NormalNoise/dual-blend at the scaled pos → state-list index; reuse the world/levelgen NormalNoise port if present, else port minimal getState sampling). NoiseThresholdProvider/rotated_block_provider/randomized_int_state_provider: port as thin wrappers if cheap, else error loudly + defer to Phase 13 (document). `ParseProvider(raw) (BlockStateProvider, error)`: stripNS dispatch on the provider "type", resolving inner states via resolveBlockState, erroring loudly on an unported type.
provider_test.go: TestNextBooleanOneBit (vs oracle), TestNextGaussianTwoDrawCache (first call 2+ draws, second call 0 draws returns cache, vs oracle), TestSimpleProviderNoDraw, TestWeightedProviderOneDraw (seeded pick + post-draw state), TestRuleBasedFallback, TestNoiseProviderDeterministic, TestParseProviderUnknownErrors, + a parse over the REAL embedded weighted provider (flower_default's to_place).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./world/levelgen/... && go test ./world/levelgen/ -run 'TestNextBoolean|TestNextGaussian' -count=1 && go test ./world/levelgen/feature/ -run 'TestSimpleProvider|TestWeightedProvider|TestRuleBased|TestNoiseProvider|TestParseProvider' -count=1</automated>
  </verify>
  <done>NextBoolean/NextGaussian draw jar-exact; the provider hierarchy parses from ParsedConfig + draws jar-exact; the weighted pick + the unknown-type loud error are tested over a real embedded provider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntProvider trapezoid/clamped_normal/very_biased + random_offset + block_predicate_filter + the BlockPredicate set</name>
  <files>world/levelgen/placement/heightprovider.go, world/levelgen/placement/heightprovider_test.go, world/levelgen/placement/predicate.go, world/levelgen/placement/predicate_test.go, world/levelgen/placement/modifiers.go, world/levelgen/placement/modifiers_test.go</files>
  <behavior>
    - The IntProvider trapezoid (TrapezoidalInt: min/max/plateau plain ints) samples per the jar (two nextInt draws in the plateau<range case, one in the plateau==range fallback); a seeded rng + the flower_default {min:-7,max:7,plateau:0} spread yields the jar-exact offset, post-draw state pinned.
    - clamped_normal samples Mth.clamp(round(mean+deviation*NextGaussian()), min, max); very_biased_to_bottom samples the jar's biased pick. Both draw-exact; parseIntProvider no longer errors on them or on trapezoid.
    - random_offset draws xz_spread for X, y_spread for Y, xz_spread for Z (3 draws, order x,y,z); a seeded rng + fixed origin yields the jar-exact offset position + post-draw state.
    - block_predicate_filter keeps p iff the bound BlockPredicate(ctx, p) is true; matching_block_tag "minecraft:air" keeps iff GetBlock(p) is air; all_of keeps iff every sub holds; not inverts.
    - ParsePredicate errors loudly on an unported predicate type.
  </behavior>
  <action>
heightprovider.go: in parseIntProvider add the `trapezoid` case (TrapezoidalInt — transcribe sample: range=max-min; plateau==range → min+nextInt(range+1); else base=(range-plateau)/2, rangeMinusBase=range-base, min+nextInt(rangeMinusBase+1)+nextInt(base+1) — TWO draws), `clamped_normal` (ClampedNormalInt — mean/deviation/min/max via NextGaussian), `very_biased_to_bottom` (transcribe). Extend the intProvider struct with the new kinds + fields (min/max/plateau/mean/deviation). Remove them from the default error. NAME-CLASH GUARD: this `trapezoid` is the IntProvider type (plain-int fields) — keep it SEPARATE from the existing HeightProvider `trapezoid` (VerticalAnchor anchors); they are parsed by different functions (parseIntProvider vs the height-provider parser), so no collision, but comment the distinction.
predicate.go: the `BlockPredicate` interface (`Test(ctx PlacementContext, x, y, z int) bool`) + the FEAT-03 set (matching_block_tag via the carver Replaceables constant-for-constant tag pattern — air = block.IsAir, cite each tag; matching_blocks; would_survive conservative + documented; all_of/any_of/not; solid; replaceable; matching_fluids), each honoring "offset". `ParsePredicate(raw) (BlockPredicate, error)` stripNS dispatch, loud on unported.
modifiers.go: in BindModifier add `random_offset` (RandomOffsetPlacement.getPositions: x,y,z draw order, spreads via IntProvider.Sample) and `block_predicate_filter` (PlacementFilter whose shouldPlace = ParsePredicate(predicate).Test(ctx, p)). Remove random_offset from the deferred/error set.
heightprovider_test.go: TestTrapezoidIntSample (the flower_default spread, two-draw + uniform-fallback paths, post-state pinned), TestClampedNormalSample (uses NextGaussian), TestVeryBiasedToBottomSample.
predicate_test.go: TestMatchingBlockTagAir, TestAllOfAnyOfNot, TestWouldSurviveGate, TestParsePredicateUnknownErrors.
modifiers_test.go: TestRandomOffsetXYZOrder (3 draws x,y,z; post-state pinned), TestRandomOffsetTrapezoid (flower_default), TestBlockPredicateFilterKeepsDrops.
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/levelgen/placement/ -run 'TestTrapezoidInt|TestClampedNormal|TestVeryBiased|TestRandomOffset|TestBlockPredicate|TestMatching|TestAllOf|TestWouldSurvive|TestParsePredicate' -count=1 && go test ./world/levelgen/placement/ -count=1</automated>
  </verify>
  <done>The IntProvider trapezoid (primary spread) + clamped_normal + very_biased are draw-exact; random_offset (x,y,z) + block_predicate_filter bind without erroring; the predicate set is tested; the full placement suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: featureBody registry + the *feature.Registry plumbing (the body-dispatch + sub-feature-resolution seam)</name>
  <files>world/feature_body.go, world/feature_body_test.go, world/placement_context.go, world/noisegen.go, world/decoration_test.go</files>
  <behavior>
    - A featureBody registered for a type is invoked by newConfiguredPlacer with a bodyContext (carrying view + the *feature.Registry) + the REAL ctx + rng + cf, and its return is the placer's return.
    - The bodyContext's registry is the live g.deco.registry in production (passed through makePlacer); a body can call bctx.reg.ResolvePlaced / bctx.reg.ParsePlacedFeature on it.
    - A type with NO registered body stays a recordable no-op; test_set_block + the invocation recorder still work.
    - The existing decoration tests (TestTestSetBlockFlows, TestFeatureSeedTrace, the 5x5 reorder/emit-once gates) stay green — production decoration writes nothing new yet (no real body registered in THIS plan).
  </behavior>
  <action>
world/feature_body.go: define `featureBody` (the bodyContext-carrying signature from the interfaces block), `bodyContext{view *Neighborhood; reg *feature.Registry}`, `featureBodies map[string]featureBody`, `registerFeatureBody`, and any shared helpers genuinely used by BOTH 12-02 and 12-03 (e.g. a `bodyPlacerFor(bctx, cf) placement.PlacerFunc` 12-03's placeSubFeature reuses, and a `placeState` SetBlock wrapper). Keep body-specific logic OUT (so 12-02/12-03 stay file-disjoint).
world/placement_context.go: refactor newConfiguredPlacer — add a `reg *feature.Registry` param; capture the ctx + rng the closure currently discards; build a bodyContext{view, reg}; after the invocation record + the test_set_block check, look up featureBodies[ftype]; if present, return `body(bctx, cf, ctx, rng, pos)`; else false. Do NOT change applyBiomeDecoration's signature.
world/noisegen.go: in makePlacer, pass `g.deco.registry` into newConfiguredPlacer (the plumbing source). world/decoration_test.go: because newConfiguredPlacer's SIGNATURE changes (the added *feature.Registry param), THIS plan OWNS updating every makePlacer call site in decoration_test.go (verified sites: lines 96, 177, 272) to pass the test registry (or nil where no body runs) so the build + the Phase-11 gates stay green at end of wave 1 — the existing test_set_block site keeps working. (12-03, wave 2, later EXTENDS this same file for the final acceptance gate — sequential, no conflict.)
feature_body_test.go: TestRegisterAndDispatch (register a fake body for a synthetic type; assert it's called with the right pos + that the bodyContext.reg is the one passed + that its rng draws advance the threaded rng), TestBodyContextRegistryPlumbed (the body receives a non-nil reg it can ResolvePlaced on — using a real embedded id), TestUnregisteredIsNoOp, TestTestSetBlockStillWorks.
  </action>
  <verify>
    <automated>cd D:/ender && go build ./... && CGO_ENABLED=0 go build ./... && go test ./world/ -run 'TestRegisterAndDispatch|TestBodyContextRegistry|TestUnregistered|TestTestSetBlock|TestFeatureSeedTrace|TestDecorationReorderIdentical' -count=1</automated>
  </verify>
  <done>The body registry dispatches with a bodyContext carrying the live *feature.Registry (so 12-03 can resolve sub-features) + the real ctx+rng; unregistered types no-op; the Phase-11 decoration determinism + emit-once gates stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → ParseProvider/ParsePredicate/BindModifier/ParsePlacedFeature | Provider, predicate, modifier, and sub-feature JSON is build-time-trusted (extracted from the pinned 26.2 jar). No client input reaches these paths — they run off-tick on the worker scheduler over server-derived positions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | provider.go (WeightedStateProvider draw divergence) | mitigate | The weighted pick is transcribed javap -c; the test pins the picked state AND the post-draw rng state for a seed, so a draw-order regression fails loudly. |
| T-12-02 | Tampering | modifiers.go / heightprovider.go (random_offset / trapezoid wrong draw order) | mitigate | random_offset draws x,y,z; the trapezoid IntProvider draws per the transcribed TrapezoidalInt; TestRandomOffsetXYZOrder + TestTrapezoidIntSample pin the post-draw rng state — a reorder corrupts every downstream feature seed and fails the test. |
| T-12-03 | Tampering | predicate.go / provider.go / parseIntProvider (silent unported type) | mitigate | ParsePredicate / ParseProvider / BindModifier / parseIntProvider error LOUDLY on an unported type — never a silent keep/drop/no-op. |
| T-12-04 | Tampering | matching_block_tag (wrong tag membership) | mitigate | Tag→StateID sets are resolved constant-for-constant from the cited jar tag defs (carver Replaceables precedent); a membership test over a real embedded predicate pins the resolved set. |
| T-12-05 | Tampering | NextBoolean/NextGaussian draw divergence | mitigate | NextBoolean = next(1) (NOT NextIntN(2)); NextGaussian = the cached two-draw polar; both pinned against a hand-traced java.util.Random oracle so a wrong-primitive substitution fails. |
</threat_model>

<verification>
- `go test ./world/levelgen/ -run 'TestNextBoolean|TestNextGaussian' -count=1` + `go test ./world/levelgen/feature/ -count=1` + `go test ./world/levelgen/placement/ -count=1` green: the LCG primitives, providers, IntProvider trapezoid/clamped_normal/very_biased, random_offset + block_predicate_filter, the predicate set.
- `go test ./world/ -run 'TestRegister|TestBodyContextRegistry|TestUnregistered|TestFeatureSeedTrace|TestDecorationReorderIdentical|TestEmitOnce' -count=1` green: the registry dispatches with the plumbed *feature.Registry, the Phase-11 determinism + emit-once gates hold.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps (go.mod/go.sum unchanged).
- Acyclic: feature does NOT import placement; placement imports feature; world imports both.
- Docker -race (CGO) is run by the LAST Phase-12 plan (12-03) that touches the live decoration; this plan's code is single-threaded by construction (providers/predicates/primitives are pure over (rng, pos); the registry is built before the scheduler starts).
</verification>

<success_criteria>
- The BlockStateProvider hierarchy (Simple/Weighted/RuleBased + Noise/DualNoise) is ported and parses from the already-resolved ParsedConfig — the prerequisite ore/patch/selector bodies consume.
- random_offset + block_predicate_filter (the vegetation tries/spread + ground gate) are bound jar-exact, with the IntProvider trapezoid (the primary spread) + clamped_normal + very_biased ported — the deferred-modifier gap 11-02 left that FEAT-03 criterion 2 depends on is closed.
- NextBoolean + NextGaussian are ported on LegacyRandomSource (so wave-2 stays file-disjoint from random.go).
- The configured-feature dispatch is a featureBody registry WITH the *feature.Registry plumbed through (g.deco.registry → makePlacer → newConfiguredPlacer → bodyContext), so 12-03's selectors resolve nested sub-features and 12-02/12-03 register bodies from disjoint files and run in parallel.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-feature-types/12-01-SUMMARY.md`
</output>
