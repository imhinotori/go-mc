---
phase: 32-held-item-item-tags-s4
verified: 2026-06-30T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification: # No previous VERIFICATION.md existed
  previous_status: none
  previous_score: n/a
---

# Phase 32: Held-Item + Item Tags (S4) Verification Report

**Phase Goal:** A goal can read the nearest player's held item and test item-tag membership — the read that powers tempting and (next phase) love-on-feed.
**Verified:** 2026-06-30
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                          | Status     | Evidence                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A pig follows a player holding a `pig_food` item within TEMPT_RANGE (path toward + look at)                   | ✓ VERIFIED | `TestTemptGoalFollowsPlayerHoldingPigFood` PASS — asserts `pig.ai.wantX/Y/Z == player pos` AND head yaw aimed at player (id 1257 main hand)                                                              |
| 2   | A pig follows a player holding a `carrot_on_a_stick` (id 887) within TEMPT_RANGE, incl. OFF hand             | ✓ VERIFIED | `TestTemptGoalFollowsPlayerHoldingCarrot` PASS — off-hand read at `offhandWindowSlot 45`, want-target + head aim asserted                                                                                |
| 3   | A non-tempt item OR an out-of-range tempt item does NOT trigger (`canUse == false`)                          | ✓ VERIFIED | `TestTemptGoalIgnoresNonTemptItem` (dirt id 1, both preds) + `TestTemptGoalIgnoresOutOfRange` (pig_food 15 blocks) both assert `canUse == false` directly                                                |
| 4   | The pig oracle `TestPluginPigEqualsGoNativePig` stays byte-identical over 500 ticks with both TemptGoals live | ✓ VERIFIED | PASS; oracle player at (11.5,65,8.5) holds empty inventory → `playerHoldsTempt` false → both `canUse` false → ZERO new RNG draws → stream preserved (no oracle weakening)                                 |
| 5   | The pig declares 7 goals at priorities {0,1,4,4,6,7,8} — exactly two TemptGoals @4                            | ✓ VERIFIED | `TestPigGoalSetRegistered` / `TestPluginPigBootLoads` / `TestVanillaPigDeclaresGoalSet` / `TestVanillaPigGoalsPorted` all PASS; assert `len(goals)==7` + exactly two `*temptGoal` @4 with {MOVE,LOOK}    |
| 6   | Docker `-race` over ./server/ is clean                                                                        | ✓ VERIFIED | `golang:1.26 go test -race -timeout 900s ./server/` → `ok ... 13.570s`, exit 0, no DATA RACE, no FAIL                                                                                                    |
| 7   | SC1 intent: a goal reads the nearest player's held item (main + off hand) via a host-side scan               | ✓ VERIFIED | `nearestPlayerHolding` + `playerHoldsTempt` (main `heldWindowSlot` + off `offhandWindowSlot 45`) + two host handles `nearest_player_holding_*` returning a position tuple or None (sanctioned surface)   |
| 8   | SC2 intent: item-tag membership bool against the jar-extracted table, across the boundary                    | ✓ VERIFIED | `itemInTag(id,tag)` over `data/tag.ItemTags` (code-generated by `tools/gen_tags.go` from `GenTags.java`→`tags.json`); `pig_food`={1257,1258,1317}; surfaced via the host handles' Go-side predicate      |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                                          | Expected                                                      | Status     | Details                                                                                                                |
| ------------------------------------------------- | ------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------- |
| `server/item_tag.go`                              | `itemInTag(itemID, tagName)` item-tag membership read         | ✓ VERIFIED | `func itemInTag(itemID int32, tagName string) bool { return tag.ItemTags[tagName][itemID] }`; wired into goal preds    |
| `server/ai_goals_passive.go`                      | `temptGoal` port + `nearestPlayerHolding` sibling scan        | ✓ VERIFIED | `type temptGoal`, full canUse/canContinueToUse/start/tick/stop port; `nearestPlayerHolding`+`playerHoldsTempt` siblings |
| `server/inventory.go`                             | `offhandWindowSlot` const (45)                                | ✓ VERIFIED | `const offhandWindowSlot = 45`; used by `playerHoldsTempt` off-hand read                                               |
| `server/plugin_entity.go`                         | two host-computed nearest-tempt-player handles (tuple-or-None) | ✓ VERIFIED | `nearestPlayerHoldingCarrotOnAStick` / `nearestPlayerHoldingPigFood`, gated `capEntitiesRead`, registered in Attr+AttrNames |
| `server/ai_mob.go`                                | two `temptGoal@4` in newPigAI (carrot first)                  | ✓ VERIFIED | `addGoal(4, newTemptGoal(1.2, id==887, false))` then `addGoal(4, newTemptGoal(1.2, itemInTag(id,"pig_food"), false))` |
| `plugins/vanilla_pig/main.star` + assets copy     | two @4 TemptGoals via host handles, byte-identical            | ✓ VERIFIED | `diff` EMPTY; 2× `priority = 4`; `nearest_player_holding_*` callbacks; TEMPT_RANGE/SPEED/STOP_DISTANCE constants       |
| `server/ai_goal_tempt_test.go`                    | follow/ignore regression set                                 | ✓ VERIFIED | 4 substantive tests asserting want-target pos + head aim + direct canUse==false                                       |

### Key Link Verification

| From                              | To                                                  | Via                                              | Status  | Details                                                                                              |
| --------------------------------- | --------------------------------------------------- | ------------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------- |
| `newPigAI`                        | two `temptGoal@4` (carrot first, pig_food second)   | `m.goals.addGoal(4, newTemptGoal(...))`          | ✓ WIRED | ai_mob.go:263-264; insertion-sort keeps carrot before pig_food among equal @4                        |
| `nearestPlayerHolding`            | main-hand + off-hand item ids                       | `inv.get(heldWindowSlot(heldSlot))` + slot 45    | ✓ WIRED | `playerHoldsTempt` reads both hands, slotIsEmpty-guarded, `int32(stack.ItemID)`                       |
| `vanilla_pig/main.star`           | host `nearest_player_holding_*` handles             | `entity.nearest_player_holding_pig_food(...)` etc | ✓ WIRED | both .star callbacks call the handles; host owns the item-id set; .star sees only a pos tuple or None |
| `temptGoal.canUse`                | TemptRange attribute (10.0)                          | `e.getAttributeValue(attribute.TemptRange)`      | ✓ WIRED | TemptRange registered at 10.0 (attributes.go:87); test asserts pig carries it via createAnimalAttributes |

### Data-Flow Trace (Level 4)

| Artifact                  | Data Variable             | Source                                                       | Produces Real Data | Status     |
| ------------------------- | ------------------------- | ----------------------------------------------------------- | ------------------ | ---------- |
| `temptGoal.tick`          | `g.px,g.py,g.pz`          | `nearestPlayerHolding` ← real `t.players` + `inv.get(...)`   | Yes (real player pos + real inventory item ids) | ✓ FLOWING  |
| host `nearest_player_holding_*` | tuple `(px,py,pz)`  | `nearestPlayerHolding(h.t, e, maxDist, pred)`               | Yes (host scan over live players + real `ItemTags`) | ✓ FLOWING  |
| `itemInTag`               | bool                      | `tag.ItemTags["pig_food"] = {1257,1258,1317}` (jar-generated) | Yes (jar-extracted table, not hardcoded inline) | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                          | Command                                                   | Result                | Status |
| ------------------------------------------------- | --------------------------------------------------------- | --------------------- | ------ |
| Tempt follow/ignore suite                          | `CGO_ENABLED=0 go test ./server/ -run TestTempt -v`       | 4/4 PASS              | ✓ PASS |
| Oracle byte-identical (500 ticks, both TemptGoals) | `... -run TestPluginPigEqualsGoNativePig -v`              | PASS                  | ✓ PASS |
| Boot-load 7 goals / goal-count (4 tests)           | `... -run 'TestPluginPigBootLoads\|...' -v`               | 4/4 PASS              | ✓ PASS |
| No regressions (server + tag + loot)               | `go test ./server/ ./data/tag/ ./level/loot/`            | all ok                | ✓ PASS |
| Build (CGO=0)                                       | `CGO_ENABLED=0 go build ./...`                            | exit 0                | ✓ PASS |
| Vet (CGO=0)                                         | `CGO_ENABLED=0 go vet ./server/`                          | exit 0                | ✓ PASS |
| .star byte-identical                               | `diff plugins/... server/assets/...`                     | EMPTY                 | ✓ PASS |
| Docker -race                                        | `golang:1.26 go test -race -timeout 900s ./server/`      | ok 13.570s, exit 0    | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                 | Status      | Evidence                                                                                                         |
| ----------- | ----------- | --------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| MOB-SUB-06  | 32-01       | Nearest-player held-item read + item-tag bool across the Starlark boundary  | ✓ SATISFIED | `nearestPlayerHolding`+`playerHoldsTempt`(main+off) + two host handles; REQUIREMENTS.md marks `[x]` Complete     |
| MOB-SUB-07  | 32-01       | Item-food tag membership is data-driven (jar-extracted table via codegen)   | ✓ SATISFIED | `data/tag.ItemTags` generated by `tools/gen_tags.go`←`GenTags.java`←`tags.json`; consumed by `itemInTag`         |
| MOB-GATE-01 | 32-01       | Pig deferred goals wired 1:1 in lockstep (TemptGoal@4 ×2 contributed here)  | ✓ SATISFIED | Two @4 TemptGoals wired Go + .star lockstep; formally CLOSED at Phase 33 (incremental wiring per REQUIREMENTS.md) |

### Anti-Patterns Found

| File                       | Line | Pattern                        | Severity | Impact                                                                                              |
| -------------------------- | ---- | ------------------------------ | -------- | --------------------------------------------------------------------------------------------------- |
| `canContinueToUse`         | 498  | empty `if g.canScare {}` block | ℹ️ Info  | Intentional CITED DEAD SKIP — canScare=false flee branch ported faithfully (CLAUDE.md 1:1 mandate)  |

No stubs, no TODO/FIXME, no hollow data. All flagged code paths trace to real inventory reads + the jar-generated tag table.

### Notes on ROADMAP SC API-naming deviation (non-blocking)

ROADMAP SC1/SC2 tentatively named generic boundary reads `world.player_main_hand` and
`world.item_in_tag` / host-side `itemTagContains`. The implementation instead exposes two
**host-encapsulated** handles (`entity.nearest_player_holding_carrot_on_a_stick` / `_pig_food`) plus the
Go-side `itemInTag`, so the Starlark side never sees an item id — mirroring Phase-31 `damageInTag`.

This deviation is **sanctioned in the phase's own 32-CONTEXT.md `<decisions>`** ("PREFER the
RUNTIME-COMPUTES pattern ... This keeps the item-tag id set Go-side"), is functionally equivalent, and is
a stronger security posture (information-disclosure mitigation T-32-01). The binding **Goal** ("a goal can
read the nearest player's held item and test item-tag membership") and the underlying SC1/SC2 *intent* are
fully met, and REQUIREMENTS.md marks MOB-SUB-06/07 Complete. Recorded as VERIFIED with this note rather
than a gap — the observable truths all pass and no generic-read capability was promised to a consumer that
now lacks it.

### Human Verification Required

None. The follow mechanism is fully verified headlessly (want-target set to the player position, head-yaw
aim, navigate-vs-stop threshold at 6.25). A live `botlive` test independently confirmed end-to-end that a
pig is tempted to within 0.00 blocks by a held carrot on a clean server — corroborating, not required.

### Gaps Summary

None. All 4 ROADMAP Success Criteria are met (SC1/SC2 via the sanctioned host-handle surface, SC3/SC4
literally), all 8 derived observable truths VERIFIED, all artifacts substantive + wired + data-flowing,
all 8 gates GREEN (build, vet, TestTempt ×4, oracle byte-identical, goal-count ×4, regressions, diff-empty,
Docker -race). 1:1 jar fidelity confirmed against 32-CONTEXT.md bytecode: speed 1.2, stopDistance 2.5,
TEMPT_RANGE 10.0, calmDown reducedTickDelay(100)=50, carrot id 887, pig_food {1257,1258,1317}, offhand 45,
canScare=false dead skip, zero RNG. CGO_ENABLED=0 preserved; no new Go deps.

---

_Verified: 2026-06-30_
_Verifier: Claude (gsd-verifier)_
