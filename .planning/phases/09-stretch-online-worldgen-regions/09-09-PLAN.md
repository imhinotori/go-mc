---
phase: 09-stretch-online-worldgen-regions
plan: 09
type: execute
wave: 7
depends_on: [08]
files_modified:
  - cmd/sulfur/main.go
  - world/noisegen_test.go
autonomous: false
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "cmd/sulfur/main.go swaps the generator: NewSuperflat → NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY) (the ONE-LINE swap per 09-RESEARCH Code Examples), with a worldSeed constant/flag added (a fixed deterministic seed — determinism is the hard requirement, WORLD-04); the worker wiring (world.NewWorker(gen, \"\", workerBuf)) is UNCHANGED (generator-agnostic) and Superflat is KEPT as a fallback/test generator (not deleted), so a SULFUR_SUPERFLAT escape hatch can fall back if needed"
    - "the off-tick worker + the tick + the Generator interface + the level.Chunk wire are ALL UNTOUCHED — PARITY-01 is purely 'a better Generate' behind the Phase-4 seam; NO concurrency work, NO new wire surface (the chunk format was capture-diff-sealed in Phase 4), so the validation GATE is the real-client VISUAL check + the determinism/sanity tests, NOT a capture-diff"
    - "the AUTOMATABLE determinism + sanity gate passes: the determinism test (same seed → identical chunk bytes — the worker purity contract) + the FULL-PARITY sanity test (terrain height VARIES, water at sea level, varied biomes across chunks, underground noise-cave air pockets, carved tunnels/ravines, aquifer fluids away from sea level, ore-vein blocks, valid heightmaps, no all-air/all-stone columns) are green, AND a Docker -race sanity over ./world/... is clean (the noise stack is pure/deterministic; worldgen already runs off-tick proven Phase 8)"
    - "the BLOCKING real-client visual gate (autonomous:false, like Phases 4/5/6/7) is the phase GATE: an unmodified vanilla 26.2 client connects to cmd/sulfur on localhost:25565, the world renders as RECOGNIZABLE Minecraft terrain (rolling hills/valleys/plains/mountains, BIOME-VARIED — desert sand, ocean water, badlands), with water at sea level + EXPLORABLE CAVES (the player can find + walk into caves) + RAVINES + underground WATER/LAVA aquifers + ORE VEINS visible when mining — the player stands on SOLID walkable ground with NO void/holes/stripes, spawned on solid ground; directly proving the full-parity success criterion ('a real vanilla 26.2 client explores hilly biome-varied terrain WITH working cave systems, ravines, underground aquifers, and ore veins')"
    - "the OPTIONAL seed-heightmap capture-diff (a non-blocking diff vs a real vanilla 26.2 server for a fixed seed) is included ONLY IF cheap — it is the aspirational byte-identical measurement, NOT the gate; if a running vanilla server isn't readily available it is SKIPPED/documented as a follow-on, never blocking the phase (09-RESEARCH Validation Strategy: byte-identical is a measurement tool, not the gate)"
  artifacts:
    - path: "cmd/sulfur/main.go"
      provides: "the generator swap (NewSuperflat → NewNoiseGenerator(worldSeed, ...)) + the worldSeed constant/flag + the spawn-on-noise-surface fix; Superflat kept as a fallback"
      contains: "NewNoiseGenerator"
    - path: "world/noisegen_test.go"
      provides: "the determinism + the FULL-PARITY sanity gate tests (height varies, varied biomes, noise caves, carved tunnels/ravines, aquifer fluids, ore veins, valid heightmaps); optionally a non-blocking seed-heightmap diff scaffold"
      contains: "TestTerrainSanity"
  key_links:
    - from: "cmd/sulfur/main.go"
      to: "world.NewNoiseGenerator (Plan 09-08)"
      via: "the one-line swap NewSuperflat → NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY)"
      pattern: "NewNoiseGenerator"
    - from: "cmd/sulfur/main.go gen"
      to: "world.NewWorker (UNCHANGED, generator-agnostic)"
      via: "the swapped generator is handed to the unchanged off-tick worker — no concurrency work"
      pattern: "NewWorker"
---

<objective>
Wire the full-parity `NoiseGenerator` into the server, prove the automatable gate (determinism + full-parity sanity + -race), and run the BLOCKING real-client visual check that seals PARITY-01. This is the one-line swap (09-RESEARCH Code Examples): `cmd/sulfur/main.go`'s `NewSuperflat(...)` → `NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY)`, with the off-tick `world.NewWorker(gen, "", workerBuf)` wiring UNCHANGED (the worker is generator-agnostic) and Superflat KEPT as a fallback/test generator. A `worldSeed` is added (a fixed deterministic seed — WORLD-04).

The off-tick worker + the tick + the `Generator` interface + the `level.Chunk` wire are ALL UNTOUCHED — PARITY-01 is purely "a better Generate" behind the Phase-4 seam. There is NO new wire surface (the chunk format was capture-diff-sealed in Phase 4), so the validation GATE is the real-client VISUAL check + the determinism/sanity tests, NOT a capture-diff.

The phase GATE is the BLOCKING real-client visual check (autonomous:false, mirroring Phases 4/5/6/7): an unmodified vanilla 26.2 client connects, the world renders as RECOGNIZABLE, BIOME-VARIED Minecraft terrain with EXPLORABLE CAVES + RAVINES + underground WATER/LAVA AQUIFERS + ORE VEINS — directly proving the full-parity success criterion. The automatable gate (determinism: same seed → identical bytes; full-parity sanity: height varies, varied biomes, noise caves, carved tunnels/ravines, aquifer fluids, ore veins, valid heightmaps; plus a Docker -race sanity) backs it. The OPTIONAL seed-heightmap capture-diff vs a real vanilla server is included ONLY IF cheap — never the gate.

Purpose: this closes PARITY-01 — the swap + the gate turn "a full-parity NoiseGenerator exists and tests pass" into "a real client explores a recognizable vanilla-character world with working caves, ravines, aquifers, and ore veins."
Output: the cmd/sulfur/main.go swap (+ worldSeed + spawn fix), the determinism/full-parity-sanity gate tests, the BLOCKING real-client visual checkpoint, and (optional, non-blocking) a seed-heightmap diff scaffold. ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md
@.planning/phases/09-stretch-online-worldgen-regions/09-08-PLAN.md

<interfaces>
<!-- VERIFIED on disk this session. This plan touches ONLY main.go (the swap + spawn) + the gate tests. -->

From world/noisegen.go (Plan 09-08 — the drop-in full-parity generator):
```go
func NewNoiseGenerator(seed int64, secs, minY int) *NoiseGenerator
func (g *NoiseGenerator) Generate(pos level.ChunkPos) *level.Chunk  // implements world.Generator, PURE, full-parity
var _ Generator = (*NoiseGenerator)(nil)
```

The swap point — cmd/sulfur/main.go (VERIFIED this session, lines ~46-58 + ~118-130):
```go
// constants block (~46-58):
overworldSecs     = 24   // height/16
overworldMinY     = -64
overworldSurfaceY = -48   // Superflat's flat top (NoiseGenerator does NOT use this — terrain comes from final_density)
workerBuf         = 256

// the wiring (~118-130):
gen := world.NewSuperflat(overworldSecs, overworldMinY, overworldSurfaceY)  // ← SWAP THIS LINE
worker := world.NewWorker(gen, "", workerBuf)                                // ← UNCHANGED (generator-agnostic)
mgr := world.NewChunkManager()
tick.SetWorld(mgr, worker)
tick.SetSpawn(overworldSurfaceY)   // NOTE: spawn surface — the player must spawn ON the noise terrain, not in void/inside it
```
NOTE: tick.SetSpawn(overworldSurfaceY) places the player at a FIXED surface Y (-48). The noise terrain's spawn-column surface VARIES — handle the spawn-on-noise-terrain so the player lands on solid ground (read the spawn chunk's WorldSurface heightmap or a generator heightAt(x,z)); a buried/void spawn FAILS the gate.

The SULFUR_DEBUG escape hatches (main.go ~132-139) — the precedent for an env-gated fallback (SULFUR_SUPERFLAT).
</interfaces>

<key_facts>
<!-- VERIFIED against 09-RESEARCH (Code Examples / Validation Strategy) + main.go this session. -->
- THE ONE-LINE SWAP: replace `gen := world.NewSuperflat(...)` with `gen := world.NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY)`. The worker line is UNCHANGED. Keep Superflat (a fallback) — env-gate it (SULFUR_SUPERFLAT=1 → NewSuperflat) mirroring SULFUR_DEBUG. Do NOT delete Superflat (the determinism/fallback reference + its purity test still runs).
- worldSeed (WORLD-04): add a `worldSeed` constant (a fixed documented int64) + optionally a -seed flag. A fixed default makes the v2 world reproducible. Document it.
- SPAWN ON THE NOISE TERRAIN (a buried/void spawn fails the gate): derive the spawn Y from the generator's surface for the spawn column (query the generated spawn chunk's WorldSurface heightmap, or a generator heightAt(spawnX, spawnZ)) so the player lands on solid ground. Coordinate with server/play_join.go's spawn-Y derivation. This is the one wiring subtlety the swap introduces.
- NO CAPTURE-DIFF GATE (Validation Strategy + the constraint): the chunk WIRE was sealed in Phase 4/5. Worldgen adds NO encoder/packet change (grep-confirm no encoder file is touched). So the gate is VISUAL + determinism/sanity, NOT a new capture-diff. The Phase-4/5 goldens stay the wire authority + stay green (do not touch them).
- THE AUTOMATABLE GATE: (a) TestNoiseGenDeterministic (same seed+pos → identical encoded bytes — reuse the 09-08 test), (b) TestTerrainSanity (FULL-PARITY: surface height VARIES; water at sea level; >1 biome across chunks; underground noise-cave air pockets; carved tunnels/ravines present; aquifer fluid away from sea level; ore-vein blocks; valid 3 CLIENT heightmaps; no all-air/all-stone column), (c) a Docker -race sanity over ./world/... (the STATE.md command: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/...`).
- THE BLOCKING VISUAL GATE (autonomous:false — like 04-04/05-03/06-07/07): a checkpoint:human-verify. The operator runs the server (SULFUR_DEBUG off, the noise gen on) + connects a vanilla 26.2 client (PrismLauncher) to localhost:25565, then confirms: recognizable BIOME-VARIED terrain (hills/valleys/plains/mountains + desert/ocean/etc.), water at sea level, EXPLORABLE CAVES (walk into them) + RAVINES, underground water/lava aquifers, ore veins when mining, solid walkable ground, NO void/holes/stripes, spawned on solid ground. This directly proves the full-parity success criterion. This is the phase gate.
- OPTIONAL SEED-HEIGHTMAP DIFF (non-blocking): if a real vanilla 26.2 server is readily available, capture a few fixed-seed column heightmaps + byte-diff vs Sulfur's for the same seed — the aspirational byte-identical measurement. Include ONLY IF cheap; NOT the gate. If no vanilla server is handy, document + SKIP.
- ZERO NEW DEPS, ZERO CONCURRENCY: the swap is config; the worker/tick/interface/wire are untouched.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: edit cmd/sulfur/main.go (the swap + worldSeed + the spawn-on-noise-surface fix + the optional SULFUR_SUPERFLAT fallback). Extend world/noisegen_test.go with the full-parity sanity/determinism gate (reuse the 09-08 determinism test). Run the Docker -race sanity. Then the BLOCKING checkpoint. Do NOT touch any encoder/packet file, the Phase-4/5 capture-diff goldens, world/worker.go, world/generator.go, or the tick pipeline.
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted 26.2 client → server (the visual gate) | The client connects over the EXISTING (Phase 2-5 sealed) handshake→login→config→play→movement path; this plan changes only WHICH chunks the off-tick worker generates (the generator swap), not the wire or the network boundary. No new trust boundary — the chunk position remains the only server-clamped Generate input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-25 | Integrity (non-reproducible world) | a non-fixed/leaky seed making the world differ across runs (WORLD-04) | mitigate | worldSeed is a FIXED documented default (a flag may override); Generate is pure over (seed, pos). Asserted by TestNoiseGenDeterministic. |
| T-9-26 | Availability (player can't play) | the player spawning buried in rock or in void over a valley → the gate fails | mitigate | The spawn Y is derived from the NOISE surface for the spawn column (the WorldSurface heightmap), not the fixed -48. Confirmed by the BLOCKING visual gate (player stands + walks). |
| T-9-27 | Denial of Service | the full pipeline Generate backing up the off-tick worker under a real client's view-ring burst | accept | Each pipeline stage is bounded (Plan 09-08 / Pattern 2 + the aquifer/carver/biome caches); the worker's bounded queue drops on overload (proven Phases 4/8). No new control needed. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Swap the generator in main.go (worldSeed + spawn-on-noise-surface + Superflat fallback) + the determinism/full-parity-sanity/-race gate (PARITY-01)</name>
  <files>cmd/sulfur/main.go, world/noisegen_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Code Examples (the swap), Validation Strategy (visual+determinism gate, NOT capture-diff; the optional diff).
    - cmd/sulfur/main.go — the constants block + the wiring (NewSuperflat → NewWorker → SetWorld → SetSpawn) + the SULFUR_DEBUG env pattern (~46-58, ~118-139).
    - server/play_join.go + the tick.SetSpawn call site — how the join bootstrap derives the spawn Y (to feed it the NOISE surface height for the spawn column instead of the fixed -48).
    - world/noisegen.go (Plan 09-08) — NewNoiseGenerator + Generate (+ any heightAt/SpawnY accessor; if none, query the spawn chunk's WorldSurface heightmap).
  </read_first>
  <action>
    1. cmd/sulfur/main.go — add a `worldSeed` constant (a fixed documented int64) + optionally a `-seed` flag defaulting to it (WORLD-04). Swap `gen := world.NewSuperflat(...)` → `gen := world.NewNoiseGenerator(worldSeed, overworldSecs, overworldMinY)`. Keep the worker line UNCHANGED. Keep Superflat behind an optional env gate (SULFUR_SUPERFLAT=1 → NewSuperflat, mirroring SULFUR_DEBUG); do NOT delete Superflat.
    2. cmd/sulfur/main.go — FIX the spawn: derive the spawn Y from the NOISE surface for the spawn column (generate the spawn chunk + read its WorldSurface heightmap, or call a generator heightAt(spawnX, spawnZ)) so the player spawns ON solid ground. Coordinate with server/play_join.go. Document the change.
    3. world/noisegen_test.go — ensure TestNoiseGenDeterministic (same seed+pos → identical encoded bytes) is present (reuse the 09-08 test). Add TestTerrainSanity (FULL-PARITY): across several positions/columns — surface height VARIES; water at sea level in low columns; >1 distinct biome across chunks; underground noise-cave air pockets exist; carved tunnels/ravines present (the carve pass replaced stone with air/water somewhere); an aquifer fluid appears away from sea level; ore-vein blocks appear; the 3 CLIENT heightmaps are valid; no all-air/all-stone column. (Optional, non-blocking: a TestSeedHeightmapVsVanilla scaffold gated behind a build tag / skipped if no vanilla fixture.)
    4. Run the Docker -race sanity over ./world/...: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... -count=1`.
  </action>
  <verify>
    <automated>go test ./world/ -run 'TestNoiseGenDeterministic|TestTerrainSanity' -count=1 && go vet ./... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - main.go uses NewNoiseGenerator(worldSeed, ...) behind the UNCHANGED worker; worldSeed is a fixed documented default (flag-overridable); Superflat is kept as a fallback (env-gated), not deleted.
    - The spawn Y is derived from the noise surface for the spawn column (player lands on solid ground).
    - TestNoiseGenDeterministic + TestTerrainSanity (full-parity: height varies, varied biomes, noise caves, carved tunnels/ravines, aquifer fluids, ore veins, valid heightmaps) pass; the Docker -race sanity over ./world/... is clean.
    - NO encoder/packet/capture-diff-golden file touched (grep-confirm); the Phase-4/5 goldens stay green. `go build ./...`, `go vet ./...` clean; zero new deps.
  </acceptance_criteria>
  <done>The server generates full-parity noise terrain (deterministic, full-parity-sanity-checked, -race clean) behind the unchanged seam, with the player spawning on solid ground — ready for the BLOCKING real-client visual gate (Task 2).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING real-client visual gate — recognizable vanilla terrain WITH explorable caves/ravines/aquifers/ores (PARITY-01 milestone)</name>
  <what-built>
    The generator was swapped from Superflat to the ported Mojang FULL-PARITY pipeline: Tier A Xoroshiro seeding → Tier B noise primitives → the FULL density-function graph incl. the cave functions (caves are negative final_density — they came "free" with the data-driven graph) → NoiseChunk cell-sample+trilerp → the real Fill with the Aquifer (water/lava/air in carved regions) + the OreVeinifier (copper/iron veins) → ApplyCarvers (CanyonWorldCarver ravines + CaveWorldCarver tunnels) → SurfaceSystem/SurfaceRules + the multi-noise Climate biome source (biome-varied surfaces), behind the UNCHANGED off-tick worker + the capture-diff-sealed level.Chunk wire. The automatable gate is green: determinism (same seed → identical chunk bytes), full-parity sanity (height varies, varied biomes, noise caves, carved tunnels/ravines, aquifer fluids, ore veins, valid heightmaps), and a Docker -race sanity. This checkpoint is the full-parity success criterion: a real vanilla 26.2 client must explore hilly, biome-varied terrain WITH working cave systems, ravines, underground aquifers, and ore veins — the same human-verify discipline that sealed Phases 4/5/6/7 (worldgen adds NO wire surface, so the gate is VISUAL + determinism, not a capture-diff). DEFERRED (documented, not silently dropped): mineshafts-as-STRUCTURES + trees/vegetation features (the StructureFeature + feature/decoration passes are separate large subsystems).
  </what-built>
  <how-to-verify>
    1. Ensure SULFUR_DEBUG is OFF (the noise gen is on; SULFUR_SUPERFLAT unset). Run the server: `go run ./cmd/sulfur` (listens on :25565, proto 776). Optionally pass `-seed <n>` (default is the fixed worldSeed).
    2. Connect an unmodified vanilla 26.2 client (PrismLauncher) to `localhost:25565`. Log in (offline mode) and let the world load. (Tip: optionally use a creative/spectator mental model — fly down to inspect caves — but the gate is survival-walkable terrain.)
    3. Confirm ALL of:
       - The terrain is RECOGNIZABLE Minecraft: rolling hills, valleys, plains, and/or mountains — clearly NON-FLAT.
       - The terrain is BIOME-VARIED: you can find more than one biome (e.g. grassy plains/forest AND sand desert or beach AND ocean) — the surface blocks + grass colors vary by biome, not a uniform grass slab.
       - Oceans/lakes/low areas are filled with WATER at sea level (~y=63) — not dry basins, not floating water.
       - CAVES are EXPLORABLE: dig down or find a cave entrance — there are open underground cave systems (noise caves) AND winding tunnels + dramatic RAVINES (the carvers) you can walk into.
       - Underground AQUIFERS: caves/ravines below the water table contain WATER, and deep down there is LAVA (not blanket-air holes).
       - ORE VEINS: mining underground reveals ore-vein deposits (copper/iron regions).
       - The ground is SOLID and WALKABLE — no falling through void, no holes, no stripes/missing chunks, no all-air/all-stone columns.
       - The player SPAWNED on solid ground (not buried, not in mid-air).
       - Walk/explore several chunks: terrain + caves + biomes stream in continuously (no seams, no chunk-edge-truncated ravines).
    4. (OPTIONAL, non-blocking) If a real vanilla 26.2 server is handy, compare the general terrain CHARACTER for the same seed — it should look like the same FAMILY of terrain (full parity is recognizable-and-cave-complete; byte-identical-for-a-seed remains the aspirational measurement).
    5. If the terrain is flat / has void / has stripes / spawns you buried / has no water / has NO caves or ravines / caves are dry blanket-air / no biome variety / no ore veins — describe the failure (which symptom, where) so the gap can be closed.
  </how-to-verify>
  <resume-signal>Type "approved" if a real vanilla 26.2 client explores recognizable, biome-varied, non-flat, walkable terrain with water at sea level AND explorable caves + ravines + underground aquifers + ore veins, no void/holes (PARITY-01 full parity met); otherwise describe the visual failure (flat / void / stripes / buried spawn / no water / no caves / dry caves / no biome variety / no ores / etc.).</resume-signal>
</task>

</tasks>

<verification>
- `go test ./world/ -run 'TestNoiseGenDeterministic|TestTerrainSanity' -count=1` passes (the automatable determinism + full-parity sanity gate).
- Docker `-race` over `./world/...` is clean (`MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race ./world/... -count=1`).
- `go vet ./... && go build ./...` clean; ZERO new dependencies; NO encoder/packet/capture-diff-golden file touched (the Phase-4/5 wire goldens stay green — grep-confirm).
- The BLOCKING real-client visual gate is APPROVED: a vanilla 26.2 client explores recognizable, biome-varied, walkable terrain with water at sea level, explorable caves + ravines + underground aquifers + ore veins, no void/holes, player spawned on solid ground.
</verification>

<success_criteria>
- PARITY-01 (full parity — the phase gate): a real vanilla 26.2 client explores RECOGNIZABLE, BIOME-VARIED Minecraft terrain (hills/valleys/plains/mountains + desert/ocean/etc., water at sea level) WITH working CAVE SYSTEMS (noise caves + carved tunnels) + RAVINES + underground WATER/LAVA AQUIFERS + ORE VEINS — walkable, no void — generated by the real ported density-function graph + aquifer + ore-veinifier + carvers + multi-noise biomes behind the UNCHANGED Generator seam + off-tick worker; the visual gate (autonomous:false) plus determinism (same seed → identical bytes — WORLD-04) + full-parity sanity + -race.
- The swap is config-only (NewSuperflat → NewNoiseGenerator); the worker/tick/Generator-interface/level.Chunk-wire are UNTOUCHED (no concurrency work, no new wire surface — so the gate is visual+determinism, NOT a capture-diff). Superflat kept as a fallback. The optional seed-heightmap diff is non-blocking.
- Full parity honored end-to-end: hills + caves + ravines + aquifers + ore veins + biome-varied surfaces ship; mineshafts-as-STRUCTURES + trees/vegetation features are the ONLY documented deferrals (separate large subsystems, not silently dropped); zero new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/09-stretch-online-worldgen-regions/09-09-SUMMARY.md`.
</output>
