---
phase: 17-gameplay-completion
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - server/fluid.go
  - server/fluid_schedule.go
  - server/fluid_physics.go
  - server/fluid_test.go
  - server/fluid_schedule_test.go
autonomous: true
requirements: [GAMEPLAY-05]
must_haves:
  truths:
    - "A flowing (non-source) water block recomputes its level via getNewLiquid and spreads to passable neighbors on its scheduled tick"
    - "Water level decrements by getDropOff (1) per horizontal spread step and terminates (does not spread forever)"
    - "Scheduled fluid ticks drain in a deterministic order (sorted by packed pos) each gametime"
    - "A fluid block is scheduled with getTickDelay (water=5) spacing, not re-evaluated every tick"
    - "A player whose AABB intersects water is slowed (0.8 horizontal) and buoyant (reduced gravity / swim-up)"
    - "getLegacyLevel maps source->0, flowing amount->(8-amount), falling->+8 exactly per the jar"
  artifacts:
    - path: "server/fluid.go"
      provides: "FlowingFluid port: tickFluids dispatcher, fluidTick, getNewLiquid, spread, spreadToSides, getSlopeDistance, getLegacyLevel, water level read/write (OVERWRITES the 17-01 stub)"
      min_lines: 120
    - path: "server/fluid_schedule.go"
      provides: "tick-owned scheduled-block-tick queue (per-gametime bucket), schedule, drainDue, scheduleFluidTick"
      min_lines: 50
    - path: "server/fluid_physics.go"
      provides: "player in-water detection + 0.8 slowdown + buoyancy/swim (EntityFluidInteraction port)"
      min_lines: 40
  key_links:
    - from: "server/fluid.go tickFluids"
      to: "drainDue -> fluidTick per pos"
      via: "the fluid pass (called from tickWorld, wired by 17-01)"
      pattern: "drainDue|fluidTick"
    - from: "server/fluid.go fluidTick"
      to: "world.SetBlock + scheduleFluidTick"
      via: "spread application"
      pattern: "SetBlock|scheduleFluidTick"
---

<objective>
GAMEPLAY-05: implement fluid simulation — the one large net-new subsystem of Phase 17. Port vanilla `FlowingFluid` flow propagation + a net-new scheduled-block-tick queue + player fluid physics, directly from the unobfuscated jar per the standing mandate.

This plan OVERWRITES the two stub seam pieces 17-01 created (`server/fluid.go` carrying the empty `tickFluids()`/`type fluidScheduleQueue struct{}`) with the real implementation, and adds two new files (fluid_schedule.go, fluid_physics.go). It does NOT edit tick.go or tick_phases.go — 17-01 already wired `tickWorld → t.tickFluids()` and the `fluidSchedule` TickLoop field. This keeps 17-02 fully parallel with 17-03 (fall_damage.go) and 17-04 (block_drop.go) in Wave 2 — zero shared-file overlap.

Purpose: Water renders (worldgen places it) but never flows, never affects movement, and never settles after a break/place. The only seam of the six that is build-new, not reconnect-the-wire.
Output: Water flows with vanilla-faithful level decrement + slope-find + source rules on a getTickDelay-spaced scheduled queue; players are slowed and buoyant in water.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gameplay-completion/17-RESEARCH.md
@.planning/phases/17-gameplay-completion/17-01-SUMMARY.md

<interfaces>
<!-- Verified live this planning session. -->

world block IO (world/manager.go) — tick-owned, the SOLE block mutator:
  func (m *ChunkManager) GetBlock(pos pk.Position, minY int) (block.StateID, bool)  // ok=false for unloaded/out-of-range
  func (m *ChunkManager) SetBlock(pos pk.Position, state block.StateID, minY int) (changed bool)
  (the server uses `dimMinY` (overworld -64) as minY — grep `dimMinY` in server/ for the const)

block states (level/block) — Water/Lava carry a `Level Integer` property (block.Integer, NOT int — cast required):
  block.Water{Level: block.Integer(n)}   // level/block/blocks.go:68-70
  block.Lava{Level: block.Integer(n)}    // level/block/blocks.go:71-73
  block.StateList []block.Block          // StateID -> Block (type-assert to block.Water/Lava)
  block.ToStateID map[block.Block]block.StateID  // Block -> StateID
  block.Air{}                            // the empty target
  (many blocks carry `Waterlogged Boolean` — blocks.go:65,118,210+)

FROM 17-01 (already in place — see 17-01-SUMMARY):
  - TickLoop has field `fluidSchedule *fluidScheduleQueue` (nil until wired; this plan defines the REAL type in fluid_schedule.go and inits it)
  - server/fluid.go EXISTS as a 17-01 stub: `type fluidScheduleQueue struct{}` + `func (t *TickLoop) tickFluids() {}`. THIS plan OVERWRITES the whole file with the real implementation. (The `fluidScheduleQueue` type definition MOVES to fluid_schedule.go; fluid.go keeps the algorithm + tickFluids dispatcher.)
  - tick_phases.go tickWorld already calls `t.tickFluids()` — this plan does NOT touch tick_phases.go.

gametime: the tick's monotonic counter — grep `gametime` in server/tick.go for the field name (it increments in the TickLoop). The schedule key is (gametime + delay).
player movement seam: grep `collidePlayer` / `tickPhysics` in server/subtick.go for where the server applies the accepted movement (the fluid-physics integration point).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: javap deep-dive + the scheduled-block-tick queue (net-new infra) + getLegacyLevel/level read-write</name>
  <files>server/fluid_schedule.go, server/fluid.go, server/fluid_schedule_test.go</files>
  <read_first>
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 5 (A)+(B)+(D) and Pitfall 2 (determinism) and the "scheduled-tick drain in tickWorld" + "fluid level read/write" code examples
    - .planning/phases/17-gameplay-completion/17-01-SUMMARY.md (the fluid.go stub shape + the chosen fluidSchedule init point + the dimMinY/gametime field names 17-01 confirmed)
    - world/manager.go:158-189 (GetBlock/SetBlock signatures + minY)
    - level/block/blocks.go:67-73 (Water/Lava Level Integer) and block.go:1-40 (StateList/ToStateID/StateID)
    - server/fluid.go (the 17-01 stub this task OVERWRITES — note the `tickFluids` + `fluidScheduleQueue` stub it replaces)
    - javap (port-from-jar mandate — CITE each class in code comments):
      `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.material.FlowingFluid` → read getLegacyLevel, getTickDelay-callers
      `... net.minecraft.world.level.material.WaterFluid` → confirm getSlopeFindDistance=4 (iconst_4), getDropOff=1 (iconst_1), getTickDelay=5 (iconst_5)
      `... net.minecraft.world.level.material.LavaFluid` → lava constants (dropOff, tickDelay)
  </read_first>
  <behavior>
    - TestGetLegacyLevel: source -> 0; amount=8 flowing -> 0; amount=7 -> 1; ... amount=1 -> 7; falling(amount=8) -> 8; falling(amount=1) -> 15 (per `source?0 : (8-min(amount,8)) + (falling?8:0)`).
    - TestWaterLevelRoundTrip: waterStateID(n) then waterLevelOf -> (n, true); a non-water StateID -> (_, false).
    - TestScheduleDrainDeterministic: schedule three ticks at the same gametime in shuffled order -> drainDue returns them sorted by packed pos (stable, run-to-run identical). A tick scheduled for gametime+5 does NOT drain at gametime.
  </behavior>
  <action>
    1. server/fluid_schedule.go (NEW): define the REAL `type fluidScheduleQueue` — a per-gametime bucket `map[int64][]scheduledFluidTick` with `type scheduledFluidTick struct { pos pk.Position }`. Methods: `func newFluidScheduleQueue() *fluidScheduleQueue`; `func (q *fluidScheduleQueue) schedule(pos pk.Position, dueGametime int64)`; `func (q *fluidScheduleQueue) drainDue(gametime int64) []scheduledFluidTick` — collect the bucket for `gametime`, SORT by packed pos (deterministic — Pitfall 2), delete the bucket, return the sorted slice. (The `fluidScheduleQueue` type 17-01 stubbed in fluid.go MOVES here — the OVERWRITE of fluid.go in step 2 removes the stub type so there is exactly one definition.)
    2. server/fluid.go (OVERWRITE the 17-01 stub): keep `func (t *TickLoop) tickFluids()` as the real dispatcher (Task 2 fills its per-pos body) and add the level helpers: `func waterLevelOf(id block.StateID) (level int, isWater bool)` (type-assert block.StateList[id] to block.Water, `int(w.Level)`); `func waterStateID(level int) block.StateID` (`block.ToStateID[block.Water{Level: block.Integer(level)}]`); `func getLegacyLevel(amount int, falling, source bool) int` (cite FlowingFluid.getLegacyLevel). Consts: `waterDropOff=1`, `waterTickDelay=5`, `waterSlopeFindDistance=4` (cite WaterFluid javap). Init the queue lazily: in `tickFluids`, `if t.fluidSchedule == nil { t.fluidSchedule = newFluidScheduleQueue() }` (lazy init avoids editing SetWorld/tick.go — keeps fluid.go the only file). `func (t *TickLoop) scheduleFluidTick(pos pk.Position)`: `t.fluidSchedule.schedule(pos, t.gametime + waterTickDelay)`.
    Turn TestGetLegacyLevel + TestWaterLevelRoundTrip + TestScheduleDrainDeterministic GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestGetLegacyLevel|TestWaterLevelRoundTrip|TestScheduleDrainDeterministic' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/fluid_schedule.go contains the real `type fluidScheduleQueue` with `schedule` and a `drainDue` that SORTS by packed pos
    - server/fluid.go contains `func getLegacyLevel`, `func waterLevelOf`, `func waterStateID`, and consts `waterDropOff = 1`, `waterTickDelay = 5`, `waterSlopeFindDistance = 4` with a javap citation naming `WaterFluid`, and the 17-01 stub `type fluidScheduleQueue struct{}` is gone (defined once, in fluid_schedule.go)
    - `go build ./server/...` exits 0 (no duplicate-definition error)
    - the three tests pass; drain order is deterministic (run twice, identical)
  </acceptance_criteria>
  <done>The scheduled-block-tick queue exists (net-new infra) with a deterministic drain; the jar-exact level encoding + water constants are ported and tested; the 17-01 fluid.go stub is replaced with exactly one definition of each symbol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FlowingFluid.getNewLiquid + spread + spreadToSides + getSlopeDistance — the algorithm port</name>
  <files>server/fluid.go, server/fluid_test.go</files>
  <read_first>
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 5 (B) (the tick/spread/getNewLiquid/getSlopeDistance method descriptions) + the anti-pattern (NO brute-force scan) + Pitfall 2
    - server/fluid.go (Task 1 — the level read/write + consts + tickFluids dispatcher + schedule helpers)
    - world/manager.go GetBlock/SetBlock
    - javap (CITE FlowingFluid in code comments — the plan-checker decompiles to verify fidelity):
      `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.material.FlowingFluid` →
        port `tick(...)` (non-source: getNewLiquid; if differs/empty set AIR; then spread),
             `spread(...)` (try DOWN first via canMaybePassThrough; if sourceNeighborCount>=3 also spreadToSides; the water-hole rule),
             `getNewLiquid(...)` (the 4 horizontal neighbors + above; highest reaching level minus getDropOff; source-conversion at >=2 source neighbors; falling rule),
             `getSlopeDistance`/`getSpread` (8-direction slope-find within getSlopeFindDistance=4)
  </read_first>
  <behavior>
    - TestGetNewLiquid (a PORT-EXACT table lifted from the jar algorithm, like the worldgen determinism gates): table of (neighbor levels around a cell, block-above state) -> expected new level. Cases: a single source neighbor -> level decremented by dropOff(1); two source neighbors with canConvertToSource -> becomes a source (level 0); fluid directly above -> falling (full level); no fluid neighbors -> empty (AIR).
    - TestWaterSettles: a source placed in an open 3x3 flat basin, drained through scheduled ticks until the queue empties -> the surrounding cells reach the expected decreasing levels (1 ring out = level 1, etc.) and the queue TERMINATES (no infinite spread — the dropOff decrement bottoms out at level 7).
    - TestFlowDownColumn: a source over an air column -> water falls straight down (falling level) before spreading sideways.
  </behavior>
  <action>
    Port the FlowingFluid algorithm into server/fluid.go (cite `net.minecraft.world.level.material.FlowingFluid.<method>` on each ported function — idiomatic Go, NO GPL paste, translate the bytecode behavior):
    1. Fill `func (t *TickLoop) tickFluids()` (the dispatcher from Task 1): `if t.world == nil { return }`; for each `st := range t.fluidSchedule.drainDue(t.gametime)` call `t.fluidTick(st.pos)`.
    2. `func (t *TickLoop) fluidTick(pos pk.Position)`: read the block; if it's water and NOT a source, compute `getNewLiquid`; if it differs (or is empty) apply via `t.world.SetBlock` + `t.scheduleFluidTick` for the changed cell + schedule the down/side neighbors that became eligible; THEN call `spread`.
    3. `func (t *TickLoop) getNewLiquid(pos pk.Position) (level int, falling bool, isAir bool)`: examine the 4 horizontal neighbors + the block above; find the highest fluid level that can reach this cell; decrement by `waterDropOff`; apply the source-conversion rule (>=2 source neighbors + canConvertToSource → source/level 0); the falling rule (fluid above → falling, full). Empty → isAir.
    4. `func (t *TickLoop) spread(pos pk.Position, level int)`: try DOWN (pos.below) first — if the cell below can be replaced + hold fluid, spreadTo DOWN as a falling column; if sourceNeighborCount(pos) >= 3 also spreadToSides; if down can't take it and (source OR down-not-a-water-hole) spreadToSides.
    5. `func (t *TickLoop) spreadToSides(...)` using `getSlopeDistance` (the 8-direction slope-find within waterSlopeFindDistance=4) to bias flow toward the nearest drop-off.
    Use `pk.Position` math for below/neighbors (grep how Position x/y/z are accessed). Schedule every cell this touches with `getTickDelay` spacing — NEVER a per-tick full scan (the anti-pattern).
    Turn TestGetNewLiquid + TestWaterSettles + TestFlowDownColumn GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestGetNewLiquid|TestWaterSettles|TestFlowDownColumn' -x 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - server/fluid.go contains `func (t *TickLoop) getNewLiquid`, `func (t *TickLoop) spread`, `func (t *TickLoop) spreadToSides`, and a slope-find helper, each with a `FlowingFluid.<method>` javap citation comment
    - server/fluid.go fluidTick applies the new liquid via `t.world.SetBlock` and re-schedules via `t.scheduleFluidTick` (no per-tick full-water scan anywhere)
    - the three algorithm tests pass; TestWaterSettles proves termination (no infinite spread)
    - re-running the settle test twice produces identical water shape (determinism — Pitfall 2)
  </acceptance_criteria>
  <done>Water spreads with the jar-exact getNewLiquid level decrement + source-conversion + slope-find, terminates deterministically, and propagates on the getTickDelay-spaced scheduled queue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Player fluid physics — in-water detection + 0.8 slowdown + buoyancy (EntityFluidInteraction port)</name>
  <files>server/fluid_physics.go, server/fluid_test.go</files>
  <read_first>
    - .planning/phases/17-gameplay-completion/17-RESEARCH.md Pattern 5 (F) (the 26.2 RENAME — updateFluidHeightAndDoFluidPushing → updateFluidInteraction + EntityFluidInteraction; push 0.014; getWaterSlowDown 0.8) AND the "State of the Art" table (the renamed class) AND Assumption A3 (waterlogged as read-only source)
    - server/subtick.go (grep `collidePlayer` and `tickPhysics` — the player movement integration point where horizontal slowdown + vertical buoyancy apply; the player velocity fields)
    - server/fluid.go (waterLevelOf to detect a water block under the player AABB)
    - javap (CITE in comments — the 26.2-RENAMED methods, NOT the old name):
      `"/c/Program Files/Zulu/zulu-25/bin/javap" -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.entity.Entity` → find `updateFluidInteraction` (push scale 0.014 = ldc2_w double 0.014d)
      `... net.minecraft.world.entity.EntityFluidInteraction` → update/applyCurrentTo
      `... net.minecraft.world.entity.LivingEntity` → getWaterSlowDown (dreturn 0.8), travel/travelInFluid, BASE_SWIM_SPEED/WATER_DRAG static finals
  </read_first>
  <behavior>
    - TestInWaterDetection: a player whose feet/eye AABB overlaps a water block -> inWater true; a player on dry land -> false.
    - TestWaterSlowdown: a horizontal velocity, applied through the in-water path, is multiplied by 0.8 (getWaterSlowDown); a dry player's velocity is unchanged.
    - TestBuoyancy: a submerged player gets reduced downward velocity (buoyancy / push 0.014 upward component) vs a dry player's full gravity.
  </behavior>
  <action>
    Create server/fluid_physics.go (cite the 26.2-renamed `Entity.updateFluidInteraction` / `EntityFluidInteraction` / `LivingEntity.getWaterSlowDown` — NOT the pre-26.2 name):
    1. `func (t *TickLoop) playerInWater(p *tickPlayer) bool`: sample the water blocks intersecting the player AABB (feet at p.y, head at p.y+playerHeight ~1.8) via waterLevelOf over the world. Treat a waterlogged block as a read-only source contributor (A3 — do NOT overwrite the block).
    2. `func (t *TickLoop) applyFluidPhysics(p *tickPlayer)`: if playerInWater, apply the horizontal `getWaterSlowDown=0.8` multiplier to the player's horizontal movement and the buoyancy/reduced-gravity vertical adjustment (push scale 0.014 upward; reduced downward accel when submerged). Integrate at the player movement seam — call it from the collidePlayer/tickPhysics path. This is the one MEDIUM-confidence integration — port the constants exactly, tune the integration point to Sulfur's existing movement.
    3. Wire the call into the existing player physics path (NOT a new tick phase — TestTickPhaseOrder must stay green). The integration site is in subtick.go's collidePlayer (the accepted-movement path); if the server tracks position only (no server-side velocity yet), apply the slowdown to the accepted movement delta and document the limitation in the SUMMARY (the visual gate confirms feel).
    Turn TestInWaterDetection + TestWaterSlowdown + TestBuoyancy GREEN.
  </action>
  <verify>
    <automated>cd /d/ender && go build ./server/... && go test ./server/ -run 'TestInWaterDetection|TestWaterSlowdown|TestBuoyancy' -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - server/fluid_physics.go contains `playerInWater` and `applyFluidPhysics` with the constant 0.8 (getWaterSlowDown) and 0.014 (push) and a javap citation naming `EntityFluidInteraction` / `updateFluidInteraction` (the 26.2 name, NOT updateFluidHeightAndDoFluidPushing)
    - the three physics tests pass
    - TestTickPhaseOrder still passes (no new tick phase added)
  </acceptance_criteria>
  <done>A player in water is slowed (0.8) and buoyant (push 0.014), ported from the 26.2-renamed EntityFluidInteraction/LivingEntity constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| placed/broken fluid → scheduled queue | A client placing water near another fluid can trigger spread work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service | scheduled fluid tick queue | mitigate | The queue is bounded by loaded chunks + getTickDelay(5) spacing; only schedule on real block changes (place/break/neighbor), NOT every loaded water block on chunk load (Open Question 3) — pre-existing oceans stay static until disturbed, matching vanilla |
| T-17-06 | Tampering | fluid level write | mitigate | All fluid writes go through the tick-owned `world.SetBlock` (the sole mutator, TICK-05); no off-tick or client-driven fluid mutation |
</threat_model>

<verification>
- `cd /d/ender && go build ./server/...` exits 0 (no duplicate fluidScheduleQueue definition after the 17-01 stub overwrite)
- `go test ./server/...` passes including all fluid tests + TestTickPhaseOrder (no phase reorder)
- `grep -rn "updateFluidHeightAndDoFluidPushing" server/` returns NOTHING (the old pre-26.2 name must not appear)
- `grep -c "getNewLiquid\|getLegacyLevel\|FlowingFluid" server/fluid.go` shows the jar citations present
- this plan does NOT appear in `git diff --name-only` for server/tick.go or server/tick_phases.go (those are 17-01-owned; this plan only touches fluid*.go)
- Docker -race: `MSYS_NO_PATHCONV=1 docker run --rm -v //d/ender://src -w //src golang:1.26 go test -race -timeout 1800s ./server/...` clean
</verification>

<success_criteria>
- A non-source water block recomputes + spreads on its scheduled tick with jar-exact level decrement; spread terminates deterministically.
- The scheduled-tick queue drains in deterministic packed-pos order, getTickDelay-spaced.
- A player in water is slowed 0.8 + buoyant.
- No brute-force per-tick water scan; no pre-26.2 method name; no edit to tick.go/tick_phases.go.
</success_criteria>

<output>
After completion, create `.planning/phases/17-gameplay-completion/17-02-SUMMARY.md`. Document: the scheduled-queue trigger policy (schedule-on-change, oceans static until disturbed — Open Question 3); the lazy fluidSchedule init (in tickFluids, no tick.go edit); the waterlogged read-only-source simplification (A3); lava status (algorithm shared, constants swapped — A4) if implemented or deferred; and the player-physics integration point + any feel limitation for the GAMEPLAY-07 gate.
</output>
