---
phase: 34-new-passive-mobs
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plugin_entity.go
  - server/entity.go
  - server/entity_encode.go
  - server/attack_dispatch.go
  - server/plugin_mob_decl.go
  - server/ai_goals_eat.go
  - server/sheep_eat.go
  - server/chicken_aistep.go
  - server/tick_phases.go
  - server/mob_extras_test.go
autonomous: true
requirements: [MOB-PASS-01, MOB-PASS-02, MOB-PASS-03]
must_haves:
  truths:
    - "A parameterized host handle nearest_player_holding_food(tag, range) lets any mob .star scan for a player holding a tagged food (cow_food / sheep_food / chicken_food)"
    - "Sheep host eat seam: grass_block-below -> dirt, wool regrow via DATA_WOOL setSheared(false), baby ageUp(60); exposed to the .star via entity.eat_grass_block + entity.eat_broadcast_byte10"
    - "Sheep SHEAR host interact: a player holding SHEARS on an adult, un-sheared sheep -> SHEEP_SHEAR sound + the shearing/sheep/white loot drop (per-stack 5 nextFloat mob-stream scatter) + shears durability -1 + setSheared(true) DATA_WOOL broadcast; wired into handleInteract BEFORE tryFeedAnimal, sheep-gated"
    - "DATA_WOOL is the Sheep wool-color byte at accessor index 18 (BYTE serializer id 0): isSheared=(DATA_WOOL&0x10)!=0; setSheared(true)=cur|0x10, setSheared(false)=cur&0xEF; default 0x00 (WHITE, not sheared); both shear and eat-regrow broadcast ClientboundSetEntityData with the woolDataEntry"
    - "Chicken host aiStep: slow-fall (vy *= 0.6 falling) + egg-lay (egg drop + 2 nextFloat sound + nextInt(6000) reset) with eggTime field + spawn init nextInt(6000)+6000, wired into the tick loop"
    - "adjustedTickDelay stays identity (no ceilDiv); all new RNG draws via mobRandom(e) in jar order"
  artifacts:
    - path: "server/plugin_entity.go"
      provides: "nearest_player_holding_food(tag,range) + entity.eat_grass_block + entity.eat_broadcast_byte10 handles"
      contains: "nearest_player_holding_food"
    - path: "server/sheep_eat.go"
      provides: "eatGrassBlock + sheepAte (grass->dirt, wool regrow, ageUp) + setSheared/DATA_WOOL + trySheepShear (shears->wool+setSheared(true))"
      contains: "func (t *TickLoop) eatGrassBlock"
    - path: "server/entity_encode.go"
      provides: "dataWoolIndex uint8 = 18 + woolDataEntry(woolByte) (Byte(18) VarInt(0) Byte(woolByte))"
      contains: "dataWoolIndex"
    - path: "server/attack_dispatch.go"
      provides: "trySheepShear wired into handleInteract before tryFeedAnimal (sheep-gated)"
      contains: "trySheepShear"
    - path: "server/chicken_aistep.go"
      provides: "chickenAiStep (slow-fall + egg-lay) + dropChickenEgg"
      contains: "func (t *TickLoop) chickenAiStep"
    - path: "server/entity.go"
      provides: "eggTime int + sheared bool fields"
      contains: "eggTime"
  key_links:
    - from: "server/attack_dispatch.go handleInteract"
      to: "trySheepShear"
      via: "sheep + held-shears gate BEFORE tryFeedAnimal"
      pattern: "trySheepShear"
    - from: "server/sheep_eat.go setSheared"
      to: "entity_encode.go woolDataEntry"
      via: "ClientboundSetEntityData DATA_WOOL broadcast on shear / regrow"
      pattern: "woolDataEntry"
    - from: "server/tick_phases.go per-mob loop"
      to: "chickenAiStep"
      via: "per-type branch gated on entity.Chicken"
      pattern: "chickenAiStep"
    - from: "server/plugin_mob_decl.go spawnDeclaredMob"
      to: "chicken eggTime init"
      via: "nextInt(6000)+6000 after reseedMobAI"
      pattern: "eggTime ="
---

<objective>
Phase-34 SHARED INFRASTRUCTURE (wave 1): every edit to a Go file that more than one mob plan would
otherwise touch. Adding these once, here, lets the cow/sheep/chicken plans (wave 2) stay disjoint and
run in parallel — each touching only its own .star pair + test + (cow only) attack_dispatch.go.

This plan owns:
  1. The parameterized food handle nearest_player_holding_food(tag, range) on the entity handle (used by
     all three new mobs' TemptGoal callbacks — cow_food / sheep_food / chicken_food).
  2. The sheep eat host seam (server/sheep_eat.go) + its two entity handles (eat_grass_block,
     eat_broadcast_byte10) + the DATA_WOOL/setSheared support (the sheep .star calls these).
  3. The sheep SHEAR host interact (trySheepShear) + its handleInteract wiring. The shear dispatch edit
     lives HERE (not in 34-02) so it does NOT collide with the cow's tryMilkCow edit to attack_dispatch.go
     (34-01, wave 2): 34-00 is wave 1, so the cow's wave-2 attack_dispatch.go edit is sequential, never a
     parallel collision. 34-02 (sheep) then owns ONLY its .star pair + sheep_test.go — disjoint from the
     other wave-2 plans.
  4. The DATA_WOOL accessor/encode support (entity_encode.go: dataWoolIndex=18 + woolDataEntry) shared by
     BOTH the shear (setSheared(true)) and the eat-regrow (setSheared(false)) broadcasts.
  5. The chicken aiStep host hook (server/chicken_aistep.go: slow-fall + egg-lay) + the eggTime field +
     the chicken spawn init + the tick-loop wiring.
  6. The shared EatBlockGoal constants home (server/ai_goals_eat.go).

Purpose: serialize the unavoidable shared-Go-file edits into one wave-1 plan so the three mob plans are
collision-free and parallel.
Output: the parameterized food handle, the sheep eat seam, the sheep shear interact + DATA_WOOL encode,
the chicken aiStep hook, the Entity fields, all wired + tested headless. The pig oracle stays UNTOUCHED.

W1-DEFER NOTE (revision): the OPTIONAL move of the chicken aiStep hook (Task 2) into 34-03 was declined —
splitting entity.go (eggTime vs sheared) + plugin_mob_decl.go (chicken spawn-init) + tick_phases.go across
two plans is fragile, and 34-00 must remain the SINGLE owner of all shared Go files. Per the revision
guidance ("if it risks a collision, LEAVE IT in 34-00"), the chicken aiStep stays here.

SCOPE DECISION (locked by 34-JARNOTES.md:88-91, 34-CONTEXT.md:67,133-134): EDIBLE_FOR_SHEEP is a BLOCK
tag NOT extracted. CITE-DEFER the tall-grass/fern branch; port ONLY the grass_block-below eat (the
common case). Do NOT extend the block-tag extractor.

SCOPE DECISION (shear, locked by 34-JARNOTES.md:241-286, 34-CONTEXT.md:133): v1 ships WHITE sheep ONLY —
getColor() is always WHITE, the shear loot table is shearing/sheep/white; dye-color mechanics are
deferred. The shear ACTION (shears->wool+setSheared(true)) + wool REGROW (eat->setSheared(false))
together satisfy MOB-PASS-02 "shear/wool with regrow".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-new-passive-mobs/34-CONTEXT.md
@.planning/phases/34-new-passive-mobs/34-JARNOTES.md
@.planning/phases/34-new-passive-mobs/34-PATTERNS.md

<interfaces>
<!-- The food handle is currently HARDCODED per-tag (plugin_entity.go:146 nearest_player_holding_pig_food -->
<!-- -> nearestPlayerHolding(... itemInTag(id,"pig_food"))). We ADD a parameterized sibling, leaving the -->
<!-- pig's two handles UNTOUCHED (the oracle is byte-identical). -->

server/plugin_entity.go:
  - Attr() switch (:140-159): add cases "nearest_player_holding_food", "eat_grass_block", "eat_broadcast_byte10".
  - AttrNames() (:262-273): append the three names.
  - nearestPlayerHoldingPigFood (:354-373) is the template for the parameterized handle:
      gate capEntitiesRead; UnpackPositionalArgs(b.Name(), args, kwargs, 2, &tag, &maxDist)  // tag string + range
      e := h.resolve(); x,y,z,ok := nearestPlayerHolding(h.t, e, maxDist, func(id) { return itemInTag(id, tag) })
      return tuple or None. Draws no RNG.
  - The eat handles resolve the entity (h.resolve(), region-bound — Pitfall 7) and call into sheep_eat.go.
    eat_grass_block needs the cap that lets it write the block (capWorldWrite) — gate on it; the sheep
    plugin.toml grants world.write for this. eat_broadcast_byte10 gates on capEntitiesWrite (a metadata
    broadcast).

Chicken.aiStep (34-JARNOTES.md:177-196 VERBATIM):
  if (!onGround && deltaMovement.y < 0) m.multiply(1.0, 0.6, 1.0);   // slow-fall, NO RNG
  if (isAlive && !isBaby && !chickenJockey && --eggTime <= 0) {
      if (dropFromGiftLootTable(CHICKEN_LAY, spawnAtLocation))         // 1 egg (direct NewItemEntity)
          playSound(CHICKEN_EGG, 1.0, (nextFloat()-nextFloat())*0.2 + 1.0);  // 2 nextFloat — ONLY if dropped
      eggTime = nextInt(6000) + 6000;                                  // nextInt(6000) — ALWAYS when <=0
  }
  RNG order when laying: [loot] -> 2 nextFloat (sound) -> nextInt(6000). When eggTime>0: ZERO draws.
  eggTime field-init = nextInt(6000)+6000 at construction.

EatBlockGoal eat branch + Sheep.ate (34-JARNOTES.md:163-171, 85-91 VERBATIM):
  below = pos.below(); if (getBlockState(below).is(GRASS_BLOCK)) {
      level.levelEvent(2001, below, ...); level.setBlock(below, DIRT, 2); mob.ate(); }
  Sheep.ate: super.ate(); setSheared(false) [wool regrow]; if (canAgeUp) ageUp(60).

Sheep.mobInteract SHEAR path + Sheep.shear (34-JARNOTES.md:241-286 VERBATIM):
  // mobInteract, runs BEFORE super=feed:
  if (itemStack.is(Items.SHEARS)) {                          // item.Shears.ID = 1134
      if (ServerLevel && readyForShearing()) {               // readyForShearing = !isSheared() && !isBaby()
          shear(sl, SoundSource.PLAYERS, itemStack);
          gameEvent(SHEAR, player);                          // gameEvent: v1 cite-defer if no seam (no net effect)
          itemStack.hurtAndBreak(1, player, hand.slot);      // shears durability -1
          return SUCCESS_SERVER;
      }
      return CONSUME;                                        // not ready -> consume (no fall-through to feed)
  }
  return super.mobInteract(...);                             // not shears -> feed/breed path
  // Sheep.shear(level, src, tool):
  //   level.playSound(null, this, SHEEP_SHEAR, src, 1.0, 1.0);   // soundid 1441 = entity.sheep.shear
  //   dropFromShearingLootTable(level, SHEAR_SHEEP, tool, (l, drop) -> {
  //       for (i = 0; i < drop.getCount(); i++) {
  //           ItemEntity e = spawnAtLocation(l, drop.copyWithCount(1), 1.0f);
  //           e.setDeltaMovement(e.getDeltaMovement().add(
  //               (nextFloat()-nextFloat())*0.1, nextFloat()*0.05, (nextFloat()-nextFloat())*0.1)); // 5 nextFloat / stack, MOB stream
  //       }
  //   });
  //   setSheared(true);
  // SHEAR_SHEEP for a WHITE sheep -> LoadTable("minecraft:shearing/sheep/white") (white.json EXISTS, drops wool).
  // The loot SEED itself is event-time rand.Int64() (OFF the mob stream, like death loot); the per-stack
  // scatter (5 nextFloat) is drawn from the MOB stream (mobRandom(e)).

DATA_WOOL (34-JARNOTES.md:274-286 VERBATIM):
  DATA_WOOL_ID accessor index = 18, BYTE serializer (id 0). Chain: Entity 0..7, LivingEntity 8..14,
  Mob 15, AgeableMob 16=DATA_BABY_ID + 17=AGE_LOCKED, Animal adds NONE, Sheep adds DATA_WOOL_ID = 18.
  isSheared():   (DATA_WOOL & 0x10) != 0.
  setSheared(b): DATA_WOOL = b ? (cur | 0x10) : (cur & 0xEF).
  getColor():    DyeColor.byId(DATA_WOOL & 0xF); DEFAULT WHITE (0) -> default DATA_WOOL byte = 0x00.
  readyForShearing(): !isSheared() && !isBaby().
  The DataValue frames Byte(18) + VarInt(0=byteSerializerID) + Byte(woolByte) — MIRROR the existing
  babyDataEntry(index 16, boolSerializerID 8) pattern in entity_encode.go:404-417. byteSerializerID=0
  (the FIRST serializer in the EntityDataSerializers registration sequence boolSerializerID=8 cites).

Host primitives:
  t.world().GetBlock(pk.Position, dimMinY) (stateID, ok) ; t.world().SetBlock(pk.Position, stateID, dimMinY)
  block.DefaultStateID["minecraft:grass_block"] / ["minecraft:dirt"]   (commands_dbg.go:41,46)
  encodeEntityEvent(id, byte) + broadcastToTrackers (death_mob.go:94)
  NewItemEntity(id, x, y, z, stack component.SlotData) (block_drop.go:168) ; item.Egg.ID = 1060 ; item.Shears.ID = 1134
  dropMobLoot's loot path (death_mob.go:199-235): loot.LoadTable(name) -> seed := rand.Int64() ->
    loot.NewEntityLootContext(seed, 0, ...) -> loot.Roll(tbl, seed, ctx) -> per-stack NewItemEntity.
    REUSE this for the shear drop (table "minecraft:shearing/sheep/white").
  encodeSoundEntity(soundID, source, id, vol, pitch, seed) (entity_encode.go:793) ; soundid 352 entity.chicken.egg ; soundid 1441 entity.sheep.shear
  mobRandom(e) (ai_goals_passive.go:48) -> nextInt(n)/nextFloat() (ai_random.go:71,80)
  e.onGround (entity.go:70), e.vy, e.isBaby()/canAgeUp()/ageUp() (entity.go:256-423), e.height
  ensureInventory/heldWindowSlot/slotIsEmpty/shrinkHeldItem — the held-item read + consume path
    (attack_dispatch.go tryFeedAnimal:801-847 + block_interact.go:339) for the shears read + durability.
  reseedMobAI (ai_mob.go:302) called in spawnDeclaredMob (plugin_mob_decl.go:403)
  tick_phases.go: the per-mob loop at :324 (aging, pure-int) + the serverAiStep snapshot at :361-370
  handleInteract (attack_dispatch.go:725-764): the dispatcher; tryFeedAnimal called unconditionally at :763.
    The metadata-update splice the baby DATA_BABY_ID path uses (plugin_mob_decl.go:415-417) is the model
    for splicing the woolDataEntry into a ClientboundSetEntityData broadcast.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterized food handle + sheep eat seam + DATA_WOOL encode + sheep shear interact</name>
  <read_first>
    - server/plugin_entity.go:140-159 (Attr switch), :262-273 (AttrNames), :354-373 (nearestPlayerHoldingPigFood template)
    - server/commands_dbg.go:36-52 (GetBlock/SetBlock + block.DefaultStateID pattern)
    - server/death_mob.go:90-140 (encodeEntityEvent + broadcastToTrackers) + :199-235 (dropMobLoot: LoadTable -> seed -> Roll -> NewItemEntity — the shear-drop template)
    - server/entity_encode.go:380-417 (dataBabyIndex=16 + boolSerializerID + babyDataEntry — the woolDataEntry MIRROR)
    - server/plugin_mob_decl.go:413-418 (the metadata data-entry splice the DATA_WOOL broadcast mirrors)
    - server/entity.go:256-423 (breedAge / isBaby / canAgeUp / ageUp)
    - server/attack_dispatch.go:725-764 (handleInteract dispatcher) + :801-847 (tryFeedAnimal: held read + shrinkHeldItem)
    - server/ai_goals_breed.go:43 (adjustedTickDelay — identity, REUSE; no ceilDiv)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:149-176 (EatBlockGoal) + :85-91 (Sheep.ate) + :198-221 (adjustedTickDelay RESOLVED) + :241-286 (SHEAR + DATA_WOOL — RESOLVED, use verbatim)
  </read_first>
  <behavior>
    - Test TestNearestPlayerHoldingFood: a player holding a cow_food item within range -> the handle
      returns the player position tuple; out of range / wrong tag -> None. Draws no RNG.
    - Test TestEatGrassBelowToDirt: a sheep on a grass_block -> eatGrassBlock sets the block below to
      dirt, broadcasts entity event byte 10 is available, wool regrows (sheared flag cleared).
    - Test TestEatGrassBabyAgesUp: a BABY sheep eating grass calls ageUp(60).
    - Test TestEatNoGrassNoOp: a sheep NOT above grass_block -> no block change.
    - Test TestSetShearedBroadcastsWool: setSheared(e, true) sets DATA_WOOL bit 0x10 and queues the
      woolDataEntry (index 18) broadcast; setSheared(e, false) clears it (cur&0xEF) and broadcasts.
    - Test TestWoolDataEntryLayout: woolDataEntry(woolByte) frames index=18, serializerID=0 (BYTE),
      value=Byte(woolByte); the default un-sheared white byte is 0x00.
    - Test TestShearReadyAdultDropsWool: an adult, un-sheared sheep + a player holding shears ->
      trySheepShear returns true, sets sheared (DATA_WOOL bit 0x10), broadcasts SHEEP_SHEAR sound (1441),
      drops >=1 wool item (from shearing/sheep/white), and draws exactly 5 nextFloat per dropped stack
      off the MOB stream for the scatter (the loot seed is OFF the mob stream).
    - Test TestShearBabyOrShearedNoOp: a BABY sheep (or an already-sheared adult) + shears ->
      readyForShearing()==false -> trySheepShear returns false (CONSUME); no drop, no setSheared change.
    - Test TestShearNonShearsFallsThrough: a sheep + a player NOT holding shears -> trySheepShear returns
      false so handleInteract falls through to tryFeedAnimal.
  </behavior>
  <action>
1. server/plugin_entity.go — add the parameterized food handle (leave the pig's two handles UNTOUCHED):
   - Attr(): case "nearest_player_holding_food" -> h.bound(..., h.nearestPlayerHoldingFood).
   - AttrNames(): append "nearest_player_holding_food".
   - Add nearestPlayerHoldingFood (model on nearestPlayerHoldingPigFood:354): unpack TWO positional args
     (tag string, maxDist float64), gate capEntitiesRead, h.resolve(), then
       nearestPlayerHolding(h.t, e, maxDist, func(id int32) bool { return itemInTag(id, tag) })
     return the tuple or None. Cite Animal.isFood = stack.is(ItemTags.<X>_FOOD), parameterized by tag.
   - Also add the two sheep eat handles:
       case "eat_grass_block" -> h.bound(..., h.eatGrassBlockHandle)   // gate capWorldWrite
       case "eat_broadcast_byte10" -> h.bound(..., h.eatBroadcastByte10Handle)  // gate capEntitiesWrite
     plus their names in AttrNames(). Each resolves the entity (h.resolve()) and calls into sheep_eat.go:
       eatGrassBlockHandle -> h.t.eatGrassBlock(e)
       eatBroadcastByte10Handle -> h.t.broadcastToTrackers(e.id, encodeEntityEvent(e.id, 10))

2. server/entity.go — add `sheared bool` (cite Sheep DATA_WOOL sheared bit) and `eggTime int` (cite
   Chicken.eggTime; used by Task 2). Self-gated fields (only sheep/chicken touch them).

3. server/entity_encode.go — add the DATA_WOOL encode support, mirroring babyDataEntry (:404-417):
   - const dataWoolIndex uint8 = 18  (cite 34-JARNOTES.md:274-286: Entity 0..7, LivingEntity 8..14,
     Mob 15, AgeableMob 16/17, Animal +0, Sheep DATA_WOOL_ID = 18; VERIFIED javap).
   - const byteSerializerID int32 = 0  (the FIRST EntityDataSerializers registration; the boolSerializerID=8
     comment at :393-402 documents the sequence 0=BYTE..8=BOOLEAN — cite that for id 0).
   - func woolDataEntry(woolByte byte) entityDataEntry { return entityDataEntry{ index: dataWoolIndex,
     serializerID: byteSerializerID, value: pk.Byte(woolByte) } }  // frames Byte(18)+VarInt(0)+Byte(woolByte)
   Cite net.minecraft.world.entity.animal.sheep.Sheep DATA_WOOL_ID = EntityDataAccessor<Byte> (BYTE codec).

4. server/sheep_eat.go — the host eat seam + setSheared + the SHEAR interact:
   func (t *TickLoop) eatGrassBlock(e *Entity):                     // EatBlockGoal.tick eat branch VERBATIM
       below := pk.Position{X: int(math.Floor(e.x)), Y: int(math.Floor(e.y)) - 1, Z: int(math.Floor(e.z))}
       st, ok := t.world().GetBlock(below, dimMinY)
       if !ok || st != block.DefaultStateID["minecraft:grass_block"] { return }   // is(GRASS_BLOCK)
       // level.levelEvent(2001,...) client particle: cite-defer if no levelEvent seam exists.
       t.world().SetBlock(below, block.DefaultStateID["minecraft:dirt"], dimMinY)  // setBlock(below, DIRT, 2)
       t.sheepAte(e)                                                                // mob.ate()
   func (t *TickLoop) sheepAte(e *Entity):                          // Sheep.ate VERBATIM
       t.setSheared(e, false)                                       // setSheared(false) -> wool regrows
       if e.canAgeUp() { e.ageUp(60) }                              // canAgeUp branch
   func (t *TickLoop) setSheared(e *Entity, v bool):                // DATA_WOOL bit 0x10
       e.sheared = v
       woolByte := byte(0x00)                                       // low nibble = color; v1 WHITE (0)
       if v { woolByte |= 0x10 }                                    // cur|0x10 ; !v -> cur&0xEF (here cur=0)
       t.broadcastToTrackers(e.id, encodeEntityData(e.id, woolDataEntry(woolByte)))  // ClientboundSetEntityData
       // (use the SAME data-entry splice the baby DATA_BABY_ID broadcast uses, plugin_mob_decl.go:415-417)
   func (t *TickLoop) trySheepShear(p *tickPlayer, mob *Entity) bool:   // Sheep.mobInteract SHEAR path VERBATIM
       inv := ensureInventory(p)
       held := inv.get(heldWindowSlot(inv.heldSlot))
       if slotIsEmpty(held) || int32(held.ItemID) != int32(item.Shears.ID) { return false }  // not shears -> super=feed
       if mob.sheared || mob.isBaby() { return true }               // !readyForShearing -> CONSUME (no feed fall-through)
       t.shearSheep(mob)                                            // shear(): sound + loot + setSheared(true)
       t.shrinkHeldItem(p, 1)                                       // hurtAndBreak(1): v1 = consume 1 durability;
       //   if no durability subsystem, cite-defer to a held-item decrement (document the deferral)
       return true
   func (t *TickLoop) shearSheep(e *Entity):                        // Sheep.shear VERBATIM
       t.broadcastToTrackers(e.id, encodeSoundEntity(1441, soundSourcePlayers, e.id, 1.0, 1.0, 0))  // SHEEP_SHEAR
       tbl, err := loot.LoadTable("minecraft:shearing/sheep/white")  // SHEAR_SHEEP, WHITE (v1)
       if err == nil {
           seed := rand.Int64()                                     // event-time, OFF the mob stream (like death loot)
           ctx := loot.NewEntityLootContext(seed, 0, loot.EntityLootParams{})  // shearing has no kill/fire gates
           for _, stack := range loot.Roll(tbl, seed, ctx) {        // white.json -> wool
               for i := int32(0); i < int32(stack.Count); i++ {     // for i in drop.getCount(): copyWithCount(1)
                   one := stack; one.Count = 1
                   ie := NewItemEntity(t.idAlloc.AllocID(), e.x, e.y+e.height/2.0, e.z, one)
                   // the per-item scatter add: 5 nextFloat per stack, MOB stream (this.random) — ON TOP of
                   // NewItemEntity's own ItemEntity.<init> toss. Cite Sheep.shear setDeltaMovement.add:
                   ie.vx += (mobRandom(e).nextFloat() - mobRandom(e).nextFloat()) * 0.1   // 2 nextFloat (x)
                   ie.vy += mobRandom(e).nextFloat() * 0.05                                // 1 nextFloat (y)
                   ie.vz += (mobRandom(e).nextFloat() - mobRandom(e).nextFloat()) * 0.1   // 2 nextFloat (z)
                   t.regionForEntity(e).entities.add(ie)
               }
           }
       }
       t.setSheared(e, true)                                        // setSheared(true) AFTER the drop (jar order)
   Document the tall-grass/EDIBLE_FOR_SHEEP cite-deferral in the file header (the common-case grass_block
   shortcut). Document the WHITE-only shear (getColor always WHITE; dye deferred per CONTEXT). Verify the
   exact names (encodeEntityData/encodeSoundEntity, soundSourcePlayers, shrinkHeldItem, loot.Roll/
   NewEntityLootContext) against the read_first files; use whatever the codebase actually exports.

5. server/attack_dispatch.go — wire trySheepShear into handleInteract BEFORE tryFeedAnimal (the :763
   call). Replace the unconditional t.tryFeedAnimal(p, mob) with:
       if mob.baseType == entity.Sheep && t.trySheepShear(p, mob) { return }   // shear consumes the interact
       t.tryFeedAnimal(p, mob)
   (read the Entity baseType field name from entity.go; shear is sheep-gated and tried FIRST; a non-shears
   held item returns false and falls through to feed. The cow's tryMilkCow gate, added in 34-01, slots in
   the SAME spot — both are wave-ordered after this wave-1 plan, so no parallel edit to this file.)

6. server/ai_goals_eat.go — the shared EatBlockGoal constants home + a documented header citing
   net.minecraft.world.entity.ai.goal.EatBlockGoal: EAT_ANIMATION_TICKS=40, the gate bounds (adult 1000
   / baby 50, adjustedTickDelay IDENTITY — cite 34-JARNOTES.md:198-221, NO ceilDiv), the eat-act tick==4.
   Document that the goal itself is PLUGIN-EXPRESSED (the sheep .star draws the RNG gate via
   entity.rand_int; the host owns only the eat seam above) — so this file is constants + documentation,
   not a goalSelector type (no built-but-unwired Go goal type).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./...</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestNearestPlayerHoldingFood|TestEatGrass|TestEatNoGrass|TestSetSheared|TestWoolDataEntry|TestShear' -v</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` exits 0
    - `grep -c 'func (h \*entityHandle) nearestPlayerHoldingFood' server/plugin_entity.go` returns 1
    - `grep '"nearest_player_holding_food"' server/plugin_entity.go` matches in BOTH Attr() and AttrNames()
    - `grep 'nearest_player_holding_pig_food' server/plugin_entity.go` STILL present (pig handle untouched)
    - `grep -c 'func (t \*TickLoop) eatGrassBlock' server/sheep_eat.go` returns 1
    - `grep 'minecraft:dirt' server/sheep_eat.go` matches AND `grep 'ageUp(60)' server/sheep_eat.go` matches
    - `grep -c 'func (t \*TickLoop) trySheepShear' server/sheep_eat.go` returns 1 AND `grep 'minecraft:shearing/sheep/white' server/sheep_eat.go` matches
    - `grep '1441' server/sheep_eat.go` matches (entity.sheep.shear, cited SHEEP_SHEAR) AND `grep 'item.Shears.ID' server/sheep_eat.go` matches
    - `grep -c 'mobRandom(e).nextFloat()' server/sheep_eat.go` returns 5 (the per-stack shear scatter)
    - `grep 'dataWoolIndex uint8 = 18' server/entity_encode.go` matches AND `grep -c 'func woolDataEntry' server/entity_encode.go` returns 1
    - `grep 'serializerID: byteSerializerID' server/entity_encode.go` matches (woolDataEntry frames the BYTE serializer id 0)
    - `grep '0x10' server/sheep_eat.go` matches (setSheared(true) sets bit 0x10)
    - `grep 'trySheepShear' server/attack_dispatch.go` shows the handleInteract gate on entity.Sheep BEFORE tryFeedAnimal
    - `grep -v '^#' server/sheep_eat.go server/ai_goals_eat.go | grep -c 'ceilDiv'` returns 0
    - `grep -c 'eggTime' server/entity.go` >= 1 AND `grep -c 'sheared' server/entity.go` >= 1
    - `CGO_ENABLED=0 go test ./server/ -run 'TestNearestPlayerHoldingFood|TestEatGrassBelowToDirt|TestEatGrassBabyAgesUp|TestEatNoGrassNoOp|TestSetShearedBroadcastsWool|TestWoolDataEntryLayout|TestShearReadyAdultDropsWool|TestShearBabyOrShearedNoOp|TestShearNonShearsFallsThrough' -v` exits 0
  </acceptance_criteria>
  <done>Parameterized food handle added (pig handles untouched); sheep eat seam ports EatBlockGoal.tick + Sheep.ate 1:1 with DATA_WOOL setSheared; DATA_WOOL encode (index 18, BYTE serializer 0, woolDataEntry) added; trySheepShear ports Sheep.mobInteract+Sheep.shear 1:1 (shears->white wool drop + 5 nextFloat/stack scatter + setSheared(true) + durability) wired into handleInteract before feed; eggTime/sheared fields added; adjustedTickDelay identity preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chicken aiStep hook (slow-fall + egg-lay) + spawn init + tick wiring</name>
  <read_first>
    - server/chicken_aistep.go (created in this task) + the interfaces block above
    - server/plugin_mob_decl.go:386-430 (spawnDeclaredMob — eggTime init after reseedMobAI:403)
    - server/tick_phases.go:324-377 (the per-mob loops + serverAiStep snapshot)
    - server/block_drop.go:160-188 (NewItemEntity + egg SlotData construction)
    - server/combat_mob.go (playSound -> encodeSoundEntity -> broadcastToTrackers + the (nextFloat-nextFloat)*0.2+1.0 pitch)
    - server/ai_random.go:71,80 + server/ai_goals_passive.go:48 (mobRandom/nextInt/nextFloat)
    - .planning/phases/34-new-passive-mobs/34-JARNOTES.md:177-196 (Chicken.aiStep verbatim) + :223-239 (CHICKEN EGG LOOT GAP — drop plain minecraft:egg directly, v1 cite-defer)
  </read_first>
  <behavior>
    - Test TestSlowFall: falling chicken (onGround=false, vy<0) -> vy *= 0.6; grounded or rising -> unchanged. No RNG.
    - Test TestEggLayRNG: adult chicken eggTime reaches 0 -> drops egg (id 1060), draws 2 nextFloat
      (sound pitch) THEN nextInt(6000) (reset) in that order; eggTime becomes nextInt(6000)+6000.
      eggTime>0 -> only decrement, ZERO draws.
    - Test TestEggLayBabySkips: baby chicken never lays (the egg block is !isBaby-gated).
    - Test TestChickenEggTimeInit: a spawned chicken has eggTime in [6000, 12000).
  </behavior>
  <action>
1. server/chicken_aistep.go — port Chicken.aiStep's server extras VERBATIM (34-JARNOTES.md:177-196):
   func (t *TickLoop) chickenAiStep(e *Entity):
       if !e.onGround && e.vy < 0 { e.vy *= 0.6 }                 // SLOW FALL, NO RNG
       if e.isBaby() { return }                                   // !isBaby gate (isChickenJockey: see const below)
       e.eggTime--
       if e.eggTime > 0 { return }                                // eggTime>0: ZERO draws
       dropped := t.dropChickenEgg(e)                             // CHICKEN_LAY -> 1 plain egg (v1 cite-defer, JARNOTES:223-239)
       if dropped {
           pitch := (mobRandom(e).nextFloat() - mobRandom(e).nextFloat())*0.2 + 1.0   // 2 nextFloat, ONLY if dropped
           t.broadcastToTrackers(e.id, encodeSoundEntity(352, soundSourceNeutral, e.id, 1.0, pitch, 0))
       }
       e.eggTime = mobRandom(e).nextInt(6000) + 6000              // reset, drawn AFTER the sound (jar order)
   // chickenIsChickenJockey is the cited Chicken.isChickenJockey default (false in v1 — no jockey
   // subsystem; the field defaults false on a naturally-spawned/dbg chicken). Make it a NAMED const so
   // the !isChickenJockey gate is explicit, not a parenthetical (W2):
   //   const chickenIsChickenJockey = false  // cite Chicken.isChickenJockey default; jockey deferred per CONTEXT
   // and gate: if e.isBaby() || chickenIsChickenJockey { return }
   func (t *TickLoop) dropChickenEgg(e *Entity) bool: build egg SlotData (item.Egg.ID 1060, count 1) the
       way block_drop.go builds a drop, ie := NewItemEntity(t.idAlloc.AllocID(), e.x, e.y+e.height/2, e.z,
       stack); t.regionForEntity(e).entities.add(ie); return true. Cite Chicken.aiStep dropFromGiftLootTable
       (CHICKEN_LAY yields exactly 1 egg; the variant-gated alternatives are cite-deferred to plain
       minecraft:egg per JARNOTES:223-239 — the direct drop is the faithful v1 default-biome path).
   Verify the soundSourceNeutral constant name against combat_mob.go's playSound (use whatever it uses).

2. server/plugin_mob_decl.go — chicken eggTime spawn init (AFTER reseedMobAI at :403): if
   e.baseType == entity.Chicken { e.eggTime = mobRandom(e).nextInt(6000) + 6000 }. Cite Chicken.<init>.
   This is the chicken's FIRST mob-stream draw (document it before any goal draw).

3. server/tick_phases.go — wire chickenAiStep into the serverAiStep snapshot area (:361-370). For each
   snapshot entity with e.baseType == entity.Chicken, call t.chickenAiStep(e). Apply it in tickAI
   (BEFORE tickPhysics integrates gravity, so slow-fall lands on this tick's velocity — mirror where the
   pig jump impulse lands). It runs unconditionally (aiStep is independent of running goals in vanilla).
   Keep it a per-type branch (minimal faithful wiring, no new generic customServerAiStep seam).
  </action>
  <verify>
    <automated>cd D:/ender && CGO_ENABLED=0 go build ./...</automated>
    <automated>cd D:/ender && CGO_ENABLED=0 go test ./server/ -run 'TestSlowFall|TestEggLay|TestChickenEggTimeInit' -v</automated>
  </verify>
  <acceptance_criteria>
    - `CGO_ENABLED=0 go build ./...` exits 0
    - `grep -c 'func (t \*TickLoop) chickenAiStep' server/chicken_aistep.go` returns 1
    - `grep '\* 0.6' server/chicken_aistep.go` matches (slow-fall) AND `grep 'nextInt(6000) + 6000' server/chicken_aistep.go` matches
    - `grep -c 'nextFloat()' server/chicken_aistep.go` returns 2 (sound pitch draws, before the reset)
    - `grep '352' server/chicken_aistep.go` matches (entity.chicken.egg, cited CHICKEN_EGG)
    - `grep 'chickenIsChickenJockey' server/chicken_aistep.go` matches (the NAMED cited const, not a parenthetical)
    - `grep 'eggTime = mobRandom(e).nextInt(6000) + 6000' server/plugin_mob_decl.go` matches (spawn init)
    - `grep 'chickenAiStep' server/tick_phases.go` shows the wiring gated on entity.Chicken
    - `CGO_ENABLED=0 go test ./server/ -run 'TestSlowFall|TestEggLayRNG|TestEggLayBabySkips|TestChickenEggTimeInit' -v` exits 0
  </acceptance_criteria>
  <done>chickenAiStep ports Chicken.aiStep 1:1 (slow-fall y*=0.6; egg-lay 2 nextFloat then nextInt(6000) in jar order, baby + named-const jockey gated); eggTime spawn init; wired into the tick loop; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin .star -> entity eat handles | sandboxed Starlark; eat_grass_block does a host-side block write under capWorldWrite, restricted to the mob's own below-position |
| client ServerboundInteract -> trySheepShear | untrusted target id + hand; the held shears is read server-side (never from packet); the mob is re-resolved through its owning region |
| chickenAiStep host hook | tick-goroutine over tick-owned state; touches velocity + spawns an item + RNG |
| new RNG draws | the eat gate (plugin) + shear scatter (host, mob stream) + egg-lay (host) must stay byte-faithful (1:1 mandate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Tampering | eat_grass_block world write | mitigate | the seam re-reads GetBlock + guards on is(GRASS_BLOCK) and writes ONLY the mob's below-position; the .star passes no position |
| T-34-05 | Elevation | sheep world.write cap | mitigate | eat_grass_block gates on capWorldWrite; the handle exposes no arbitrary set_block to the .star |
| T-34-06 | Repudiation | new RNG desync | mitigate | the eat gate (nextInt via mobRandom in eat_can_use) + shear scatter (5 nextFloat/stack, mob stream) + egg-lay (2 nextFloat then nextInt(6000)) are drawn in jar order; focused tests pin the count+order; the pig is a separate untouched mob |
| T-34-07 | DoS | egg item flood | accept | eggTime gates a drop to once per 5-10 min (nextInt(6000)+6000); normal item lifecycle |
| T-34-14 | Tampering | trySheepShear held item from packet | mitigate | the held shears is read server-side from the player's selected hand (inv.get), never trusted from the Interact payload; the mob is re-resolved through owningRegion; a forged/non-sheep id falls through harmlessly |
| T-34-15 | DoS | wool drop flood via shear | accept | readyForShearing() gates the drop to once per regrow cycle (setSheared(true) until an eat regrows it); normal item lifecycle |
</threat_model>

<verification>
- CGO_ENABLED=0 go build ./... clean; go vet ./server/ clean
- Parameterized food handle works; the pig's two food handles are untouched
- Sheep eat seam: grass->dirt + setSheared(false) wool regrow + canAgeUp ageUp(60); tall-grass cite-deferred
- DATA_WOOL encode: index 18, BYTE serializer 0, woolDataEntry frames Byte(18)+VarInt(0)+Byte(woolByte); default 0x00
- Sheep shear: shears + adult/un-sheared -> SHEEP_SHEAR (1441) + white wool drop + 5 nextFloat/stack scatter + setSheared(true) + durability; baby/sheared -> no-op; non-shears -> feed fall-through
- Chicken aiStep: slow-fall y*=0.6 (no RNG); egg-lay 2 nextFloat then nextInt(6000), baby + named-const jockey gated; eggTime init [6000,12000)
- adjustedTickDelay stays identity (no ceilDiv); the existing adjustedTickDelay tests untouched
- The pig oracle UNTOUCHED (no edit to vanilla_pig.go / newPigAI / vanilla_pig/main.star / the pig food handles)
</verification>

<success_criteria>
- The shared infrastructure all three mobs need is in place: parameterized food handle, sheep eat seam (DATA_WOOL), sheep shear interact, chicken aiStep hook
- The shear ACTION + the eat-regrow together satisfy MOB-PASS-02 "shear/wool with regrow"
- All new RNG draws are jar-faithful (identity delay, mobRandom order); pig oracle untouched
- Headless tests green for the food handle, the sheep eat, the DATA_WOOL/shear, and the chicken slow-fall/egg-lay
</success_criteria>

<output>
After completion, create `.planning/phases/34-new-passive-mobs/34-00-SUMMARY.md`
</output>
