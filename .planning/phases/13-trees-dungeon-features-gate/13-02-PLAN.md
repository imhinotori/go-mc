---
phase: 13-trees-dungeon-features-gate
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - world/levelgen/feature/tree_placers.go
  - world/levelgen/feature/tree_placers_test.go
  - world/levelgen/feature/tree_decorator.go
  - world/levelgen/feature/tree_decorator_test.go
  - world/feature_tree.go
  - world/feature_tree_test.go
autonomous: true
requirements: [FEAT-04]
user_setup: []

must_haves:
  truths:
    - "The COMMON overworld trunk placers are ported jar-exact (net.minecraft.world.level.levelgen.feature.trunkplacers.*, javap -c against temp/cache/26.2-inner.jar): the subset the EMBEDDED common-overworld configured_feature tree configs reference — derived from the data like Phase 12 derived providers. At minimum ForkingTrunkPlacer (swamp/fancy-oak base), FancyTrunkPlacer (large oak, the branch-arc math), DarkOakTrunkPlacer (the 2x2 dark oak trunk, REUSED by pale_oak in 13-03), MegaJungleTrunkPlacer + GiantTrunkPlacer (mega jungle/spruce 2x2). (StraightTrunkPlacer is 13-01; the special-biome cherry/bending/upwards_branching trunks are 13-03.) Each plugs into the 13-01 TrunkPlacer base (getTreeHeight + the setBlock/read callback seam) and returns its FoliageAttachment(s) with the JAR-EXACT draw order."
    - "The COMMON overworld foliage placers are ported jar-exact (net.minecraft.world.level.levelgen.feature.foliageplacers.*): the common subset — SpruceFoliagePlacer (the tapered spruce cone), PineFoliagePlacer (the pine tuft), AcaciaFoliagePlacer (the flat acacia canopy), BushFoliagePlacer (oak/jungle bush), FancyFoliagePlacer (large-oak blobs), DarkOakFoliagePlacer (the dark-oak canopy, REUSED by pale_oak in 13-03), JungleFoliagePlacer (`jungle_foliage_placer` — used by mega_jungle_tree; jungle_tree itself uses blob_foliage_placer from 13-01), MegaPineFoliagePlacer (the mega-pine cap). (BlobFoliagePlacer is 13-01; the special-biome cherry/random_spread foliage are 13-03.) Each plugs into the 13-01 FoliagePlacer base (placeLeavesRow + the per-placer shouldSkipLocation) with the JAR-EXACT row/offset/corner draw order. NOTE: there is NO `MegaJungleFoliagePlacer` in 26.2 — mega_jungle_tree.json uses `jungle_foliage_placer` (verified on disk)."
    - "The COMMON tree decorators the overworld uses are ported (net.minecraft.world.level.levelgen.feature.treedecorators.*, the inline `decorators` array): the subset the common-overworld tree configs reference — AlterGroundDecorator (spruce/taiga podzol+coarse-dirt under the trunk), BeehiveDecorator (the oak/birch/cherry bee-nest with a probability draw), CocoaDecorator (jungle cocoa pods on the trunk), LeaveVineDecorator + TrunkVineDecorator (jungle vines). Each TreeDecorator.place runs AFTER the trunk+foliage with the placed log/leaf position lists as input (TreeDecorator.Context: logs, leaves, roots), drawing in JAR-EXACT order. (The special-biome decorators — attached_to_leaves+randomized_int_state_provider, pale_moss, creaking_heart — are 13-03.) An unported decorator a COMMON overworld config references errors loudly (never silently skipped)."
    - "The 13-01 ParseTrunkPlacer/ParseFoliagePlacer dispatch is extended to recognize every newly-ported COMMON placer (the 13-01 unported-error path now resolves them; the cherry/bending/upwards_branching/random_spread/cherry_foliage arms still error loudly -> 13-03), and a new ParseTreeDecorator parses the common inline `decorators[]` entries; the 13-01 tree body iterates the real decorator list AFTER placing trunk+foliage, feeding each decorator the accumulated placed-log/placed-leaf positions (the TreeDecorator context) through the same setBlock/read callback seam — so a spruce gets podzol, an oak gets a bee nest, a jungle tree gets cocoa+vines."
    - "Deterministic placement tests prove each new COMMON placer + decorator draws jar-exact for a known seed (the spruce cone shape, the acacia canopy, the dark-oak 2x2 trunk, the fancy-oak branches; the AlterGround podzol disk, the Beehive probability + nest placement, the Cocoa pod attachment), pinned vs an in-test oracle; and a per-biome integration smoke (taiga->spruce with podzol, savanna->acacia, jungle->jungle tree with cocoa/vines, dark_forest->dark oak) resolves through the selector recursion to real trees. (The all-overworld-tree-configs-decode COMPLETENESS guard lands in 13-03, after the special-biome trees + their placers are ported — it cannot pass until then.)"
  artifacts:
    - path: "world/levelgen/feature/tree_placers.go"
      provides: "the common overworld trunk placers (Forking/Fancy/DarkOak/MegaJungle/Giant) + foliage placers (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/Jungle/MegaPine), each on the 13-01 TrunkPlacer/FoliagePlacer base, registered in ParseTrunkPlacer/ParseFoliagePlacer"
      contains: "FancyTrunkPlacer"
      min_lines: 250
    - path: "world/levelgen/feature/tree_decorator.go"
      provides: "TreeDecorator interface + the common overworld decorators (AlterGround/Beehive/Cocoa/LeaveVine/TrunkVine) + ParseTreeDecorator + the TreeDecorator.Context (logs/leaves/roots positions)"
      contains: "AlterGroundDecorator"
    - path: "world/feature_tree.go"
      provides: "the tree body extended to iterate the real decorator list after trunk+foliage, feeding each the accumulated placed positions"
      contains: "decorator"
    - path: "world/levelgen/feature/tree_placers_test.go"
      provides: "per-placer draw-order/geometry tests (spruce cone, acacia canopy, dark-oak 2x2, fancy-oak branches, jungle_foliage)"
      contains: "TestSpruceFoliagePlacer"
    - path: "world/levelgen/feature/tree_decorator_test.go"
      provides: "AlterGround/Beehive/Cocoa decorator draw-order + placement tests pinned to an oracle"
      contains: "TestAlterGroundDecorator"
  key_links:
    - from: "world/levelgen/feature/tree.go ParseTrunkPlacer/ParseFoliagePlacer (13-01)"
      to: "world/levelgen/feature/tree_placers.go the new common placers"
      via: "the 13-01 dispatch's unported-error arms now resolve the common overworld placer roster"
      pattern: "ParseTrunkPlacer|ParseFoliagePlacer"
    - from: "world/feature_tree.go tree body"
      to: "world/levelgen/feature/tree_decorator.go ParseTreeDecorator + TreeDecorator.place"
      via: "after placing trunk+foliage the body runs each decorator with the accumulated placed-log/placed-leaf positions"
      pattern: "TreeDecorator|ParseTreeDecorator"
    - from: "world/levelgen/feature/tree_decorator.go decorators"
      to: "world/levelgen/feature/provider.go BlockStateProvider"
      via: "the bee-nest/cocoa/podzol states resolve through the existing provider/resolveBlockState path"
      pattern: "resolveBlockState|ParseProvider"
---

<objective>
Complete the COMMON overworld tree coverage (FEAT-04 criterion 1, the common breadth): port the common trunk placers (Forking/Fancy/DarkOak/MegaJungle/Giant) + foliage placers (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/Jungle/MegaPine) the embedded common-overworld tree configs reference, plus the common TreeDecorator subsystem (AlterGround podzol, Beehive bee-nests, Cocoa pods, jungle vines). After 13-01 made oak/birch render, this makes the COMMON overworld biomes grow their correct vanilla tree: taiga spruce (with podzol), savanna acacia, jungle jungle-trees (with cocoa+vines), dark_forest dark-oak, the mega variants.

The SPECIAL-biome trees (cherry_grove, mangrove_swamp [+ the mangrove_root_placer subsystem], lush_caves azalea, pale_garden pale_oak [+ pale_moss/creaking_heart]) are explicitly OUT of this plan — they land in 13-03, which also owns the all-overworld-tree-configs-decode completeness guard (it cannot pass until the special trees are ported). This split keeps each plan within budget: this plan is two dense placer/decorator-roster tasks; 13-03 is the special-biome roster + the root_placer subsystem + the guard.

Like Phase 12 derived the provider roster from the data, this plan derives the placer/decorator roster from the EMBEDDED common-overworld tree configs — port what the COMMON `tree` configured_feature entries actually use. An unported placer/decorator a COMMON config references errors loudly and gets ported here; the special-biome arms (cherry/bending/upwards_branching/random_spread/cherry_foliage/attached_to_leaves/pale_moss/creaking_heart) stay loud-errors routed to 13-03.

The placers extend 13-01's pure `TrunkPlacer`/`FoliagePlacer` bases (the setBlock/read callback seam — no `world` import), and the decorators run AFTER trunk+foliage via 13-01's tree body, fed the accumulated placed-log/placed-leaf positions (the `TreeDecorator.Context`). This plan depends on 13-01 (the bases + the tree body + ParseTreeConfiguration + the root_placer field) and extends `tree.go`'s dispatch + the body's decorator loop — so it is SEQUENCED after 13-01 (wave 2), sharing `tree.go`/`feature_tree.go`.

Purpose: full COMMON per-biome tree coverage — every common overworld biome's tree set renders with the right trunk/canopy shape + decorations, deterministic per seed.
Output: tree_placers.go (the common placer roster) + tree_decorator.go (the common overworld decorators) + the extended dispatch/body, with per-placer/decorator draw-order tests + per-biome smokes. ZERO new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v2-features-decoration.md
@.planning/phases/13-trees-dungeon-features-gate/13-01-PLAN.md

<interfaces>
<!-- VERIFIED on disk. Re-read 13-01-SUMMARY for the AS-BUILT TrunkPlacer/FoliagePlacer base + ParseTreeConfiguration + the tree body's placement loop + the RootPlacer field BEFORE coding (13-01 may have adjusted the callback signatures). -->

THE 13-01 PLACER BASES (world/levelgen/feature/tree.go, BUILT by 13-01 — the new placers plug into these):
```go
// the pure callback seam (no world import): the body supplies these
type setBlockFn func(x, y, z int, st block.StateID)
type readFn     func(x, y, z int) block.StateID
// TrunkPlacer: getTreeHeight(rng) + placeTrunk(setBlock, read, rng, freeHeight, pos, cfg) []FoliageAttachment
// FoliagePlacer: createFoliage(setBlock, read, rng, attachment, cfg) + placeLeavesRow + shouldSkipLocation
// FoliageAttachment{ Pos BlockPos; RadiusOffset int; DoubleTrunk bool }
// ParseTrunkPlacer(raw)  -> TrunkPlacer  (13-01: straight ported; others error "ported in 13-02/13-03")
// ParseFoliagePlacer(raw)-> FoliagePlacer(13-01: blob ported; others error "ported in 13-02/13-03")
// ParseRootPlacer(raw)   -> RootPlacer   (13-01: stub; mangrove_root_placer errors "ported in 13-03")
// ParseTreeConfiguration(resolvedConfigRaw) -> TreeConfiguration{trunkProvider,foliageProvider,belowTrunkProvider,trunkPlacer,foliagePlacer,rootPlacer(optional),minimumSize,ignoreVines,decorators}
```
CONFIRM the AS-BUILT names/signatures from 13-01-SUMMARY — they are this plan's hard dependency. This plan ADDS arms to ParseTrunkPlacer/ParseFoliagePlacer (the common 13-01 error arms become real cases; the special-biome arms stay loud-errors -> 13-03) and adds ParseTreeDecorator + the decorator list into ParseTreeConfiguration.

THE 13-01 TREE BODY (world/feature_tree.go, BUILT by 13-01 — extended here for decorators):
```go
// func init(){ registerFeatureBody("tree", treeBody) }
// treeBody: decode TreeConfiguration -> (rootPlacer hook, nil here) -> validity scan -> getTreeHeight -> placeTrunk -> createFoliage -> iterate decorators (EMPTY in 13-01)
```
13-01's body already iterates the (empty) decorators list. This plan: (a) ACCUMULATE the placed log positions + leaf positions during placeTrunk/createFoliage (the TreeDecorator.Context inputs — vanilla TreeFeature collects logsCollector/leavesCollector Sets), (b) after foliage, run each decorator.place(ctx, rng, logs, leaves, roots, setBlock). CONFIRM from 13-01-SUMMARY whether the position collectors already exist or must be added here.

THE PROVIDERS + RESOLVER (world/levelgen/feature/provider.go + blockstate.go, Phase 12 — REUSE):
```go
func ParseProvider(raw json.RawMessage) (BlockStateProvider, error)   // simple/weighted/rule_based/noise/dual_noise (NOT randomized_int_state_provider — that is 13-03)
func ResolveBlockStateJSON(raw json.RawMessage) (block.StateID, error)   // {Name,Properties} -> StateID (the bee-nest/cocoa/podzol states)
```
The common decorators resolve their block states (bee_nest{honey_level,facing}, cocoa{age,facing}, podzol, coarse_dirt) through these. Cocoa/Beehive have a facing/age property set — resolve via ResolveBlockStateJSON like the ore targets did. (Mangrove's attached_to_leaves uses `randomized_int_state_provider` which is STILL deferred in ParseProvider — that port is 13-03, not here.)

THE TREEDECORATOR CONTEXT (port from TreeDecorator.Context, javap -c) — the inputs each decorator.place needs:
```
TreeDecorator.place(ctx): ctx exposes the LOG positions, the LEAF positions, the ROOT positions (lists collected during placeTrunk/createFoliage), the rng, and a placer (setBlock). Each decorator iterates the relevant list + draws + places its decoration blocks.
```
Port the Context as a Go struct {logs, leaves, roots []BlockPos; rng; setBlock; read}. Cite TreeDecorator + the concrete decorators.

THE RNG (world/levelgen/random.go — VERIFIED): NextIntN / NextFloat / NextBoolean / NextInt on the threaded LegacyRandomSource. The decorators draw on the SAME threaded rng (continue the sequence AFTER the foliage draws — the determinism contract). DO NOT fork a source.
</interfaces>

<key_facts>
<!-- PORT from jar bytecode; cite each class. DERIVE the COMMON roster from the EMBEDDED common-overworld `tree` configs (the data is authoritative, like Phase 12's providers). No GPL paste. -->
- DERIVE THE COMMON ROSTER FIRST: enumerate the embedded `tree` configured_features for the COMMON overworld biomes (oak/oak_bees, birch/birch_bees/tall_birch/super_birch_bees, spruce/pine, jungle_tree/mega_jungle_tree/jungle_bush, acacia, dark_oak, mega_spruce/mega_pine, fancy_oak/fancy_oak_bees, swamp_oak, ...). Read each config's trunk_placer.type / foliage_placer.type / decorators[].type. Port EXACTLY that COMMON set. EXCLUDE the special-biome configs (cherry, mangrove/tall_mangrove, azalea_tree, pale_oak/pale_oak_creaking) — those are 13-03.
- TrunkPlacers (javap -c, cite each): ForkingTrunkPlacer, FancyTrunkPlacer (the large-oak branch-arc math — the heaviest; the trunk + 4 diagonal branches), DarkOakTrunkPlacer (a 2x2 trunk + branches — REUSED by pale_oak in 13-03), MegaJungleTrunkPlacer / GiantTrunkPlacer (2x2 mega trunks). spruce/acacia use straight or their own — confirm from the configs. Each on the 13-01 base.
- FoliagePlacers (javap -c, cite each): SpruceFoliagePlacer (the tapered cone), PineFoliagePlacer (the pine tuft), AcaciaFoliagePlacer (the flat 2-layer canopy + the offset-arm leaves), BushFoliagePlacer (the small bush blob), FancyFoliagePlacer (blob-per-branch), DarkOakFoliagePlacer (the wide flat canopy — REUSED by pale_oak in 13-03), JungleFoliagePlacer (`jungle_foliage_placer`, used by mega_jungle_tree — NOTE jungle_tree.json uses blob_foliage_placer [13-01]), MegaPineFoliagePlacer (the mega-pine cap). Each on the 13-01 FoliagePlacer base + its own shouldSkipLocation. THERE IS NO `MegaJungleFoliagePlacer` in 26.2 (verified) — do not name it.
- TreeDecorators (javap -c, cite each — the COMMON `decorators[]` set): AlterGroundDecorator (replace the dirt under the trunk with podzol/coarse_dirt in a radius — spruce/taiga; the per-position provider draw), BeehiveDecorator (probability nextFloat < p, then attach a bee_nest with bees to a trunk-side at the canopy base — the facing draw), CocoaDecorator (per upper-trunk log, nextFloat < p, attach cocoa{age,facing} on a random horizontal face), LeaveVineDecorator + TrunkVineDecorator (jungle vines on leaves/trunk sides). TreeDecorator.place takes the collected log/leaf positions. (attached_to_leaves/pale_moss/creaking_heart are special-biome -> 13-03.)
- minimum_size variants: 13-01 ported the bare TwoLayersFeatureSize; add ThreeLayersFeatureSize (mega/dark trees) + any parameterized two_layers (e.g. dark_oak/mega may parameterize upper_size) the COMMON configs use (getSizeAtLayer feeds the foliage radius bound).
- An unported placer/decorator a COMMON overworld config references = a BLOCKER to port HERE (loud error -> port it). The special-biome arms (cherry_trunk/cherry_foliage/bending_trunk/upwards_branching_trunk/random_spread_foliage/mangrove_root_placer/attached_to_leaves/pale_moss/creaking_heart) stay LOUD-ERRORS routed to 13-03 — NOT silent skips. The all-tree-configs-decode guard is 13-03's (it covers the special set too).
- ZERO new deps; single-threaded; the Docker -race gate is 13-04's. STALE-LSP: trust `go build`.
- This plan SHARES tree.go + feature_tree.go with 13-01 + 13-03 (the dispatch + body) — wave 2 depends_on [13-01], NOT parallel. The new placer/decorator BODIES live in their own files (tree_placers.go / tree_decorator.go) to minimize the shared-file edit surface; only the ParseTrunkPlacer/ParseFoliagePlacer arms + the body's decorator loop touch the 13-01 files.
</key_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the common overworld trunk + foliage placers (derived from the embedded common-overworld tree configs)</name>
  <files>world/levelgen/feature/tree_placers.go, world/levelgen/feature/tree_placers_test.go, world/levelgen/feature/tree.go</files>
  <behavior>
    - Each ported common trunk placer (Forking/Fancy/DarkOak/MegaJungle/Giant) places its jar-exact trunk geometry for a seed and returns the correct FoliageAttachment(s), draw-pinned.
    - Each ported common foliage placer (Spruce/Pine/Acacia/Bush/Fancy/DarkOak/Jungle/MegaPine) places its jar-exact leaf shape (cone/tuft/flat-canopy/blob) with its own shouldSkipLocation, draw-pinned. `jungle_foliage_placer` (NOT a non-existent MegaJungleFoliagePlacer) is the mega_jungle_tree foliage.
    - ParseTrunkPlacer/ParseFoliagePlacer now resolve every newly-ported COMMON type (13-01's common error arms become real cases); the special-biome arms (cherry/bending/upwards_branching/random_spread/cherry_foliage) STILL error loudly -> 13-03. ThreeLayersFeatureSize (+ any new minimum_size the common configs use) parses.
    - Every COMMON `tree` configured_feature decodes end-to-end (placers + size all resolve). (The FULL all-overworld guard incl. special biomes is 13-03.)
  </behavior>
  <action>
FIRST derive the COMMON roster: enumerate the embedded `tree` configs for the common overworld biomes (the LoadAll set MINUS cherry/mangrove/tall_mangrove/azalea_tree/pale_oak/pale_oak_creaking) + read their trunk_placer/foliage_placer/minimum_size types. Then in tree_placers.go port (javap -c, cite each class) the exact common trunk set + foliage set those configs reference, each on the 13-01 TrunkPlacer/FoliagePlacer base (the setBlock/read callback seam). FancyTrunkPlacer (the branch-arc math) is the heaviest — give it real care; transcribe the branch-count/length draws exactly. In tree.go: extend ParseTrunkPlacer + ParseFoliagePlacer with the COMMON cases (replace the common errors; keep the special-biome arms erroring -> 13-03) + add ThreeLayersFeatureSize.
tree_placers_test.go: per-placer geometry+draw tests (TestSpruceFoliagePlacer cone, TestAcaciaFoliagePlacer canopy, TestDarkOakTrunkPlacer 2x2, TestFancyTrunkPlacer branches, TestMegaJungleTrunkPlacer, TestJungleFoliagePlacer) pinned to oracles, + TestCommonOverworldTreeConfigsDecode (the COMMON subset decodes; the special-biome configs are asserted to still error -> 13-03).
  </action>
  <verify>
    <automated>cd D:/ender && go build ./world/levelgen/feature/ && go test ./world/levelgen/feature/ -run 'TestSpruceFoliage|TestPineFoliage|TestAcacia|TestDarkOak|TestFancy|TestMegaJungle|TestMegaPine|TestBushFoliage|TestForkingTrunk|TestGiantTrunk|TestJungleFoliage|TestCommonOverworldTreeConfigsDecode' -count=1</automated>
  </verify>
  <done>The common overworld trunk + foliage placer roster (derived from the embedded common tree configs) is ported jar-exact + draw-pinned; ParseTrunkPlacer/ParseFoliagePlacer resolve them all (the special-biome arms still error -> 13-03); every COMMON `tree` config decodes. `jungle_foliage_placer` (not a non-existent MegaJungle foliage) is the mega_jungle foliage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the common overworld TreeDecorator subsystem + wire it into the tree body</name>
  <files>world/levelgen/feature/tree_decorator.go, world/levelgen/feature/tree_decorator_test.go, world/feature_tree.go, world/feature_tree_test.go</files>
  <behavior>
    - AlterGroundDecorator replaces the dirt under the trunk with podzol/coarse_dirt over its radius (the per-position provider draw), for spruce/taiga.
    - BeehiveDecorator draws nextFloat < probability, and on success attaches a bee_nest (with bees) to a trunk-side block at the canopy base with the jar facing — draw-exact.
    - CocoaDecorator iterates the upper trunk logs, per-log nextFloat < probability, attaching cocoa{age,facing} on a random horizontal face — draw-exact.
    - LeaveVine/TrunkVine attach vines to the jungle tree's leaves/trunk sides (the referenced subset).
    - The tree body, after placing trunk+foliage, runs each parsed decorator with the ACCUMULATED placed-log/placed-leaf positions (the TreeDecorator.Context) on the SAME threaded rng — so a spruce gets podzol, an oak_bees gets a nest, a jungle tree gets cocoa+vines.
    - An unported decorator referenced by a COMMON overworld config errors loudly (decode); the special-biome decorators (attached_to_leaves/pale_moss/creaking_heart) stay loud-errors -> 13-03.
  </behavior>
  <action>
tree_decorator.go: TreeDecorator interface { place(ctx *DecoratorContext) } + DecoratorContext{logs, leaves, roots []BlockPos, rng, setBlock, read}. Port (javap -c, cite each) AlterGroundDecorator / BeehiveDecorator / CocoaDecorator / LeaveVineDecorator / TrunkVineDecorator. ParseTreeDecorator(raw) dispatches by "type"; the bee_nest/cocoa/podzol states resolve via ResolveBlockStateJSON / ParseProvider. An unported type errors loudly (attached_to_leaves/pale_moss/creaking_heart -> 13-03).
feature_tree.go: extend ParseTreeConfiguration's decorators decode to call ParseTreeDecorator per entry; in the tree body, COLLECT placed log positions + leaf positions during placeTrunk/createFoliage (add collectors if 13-01 didn't), and after foliage run each decorator.place(ctx) with those collectors + the threaded rng.
tree_decorator_test.go: TestAlterGroundDecorator (podzol disk under the trunk, draw-pinned). TestBeehiveDecorator (probability draw + nest attach + facing). TestCocoaDecorator (per-log probability + cocoa face).
feature_tree_test.go (extend): TestSpruceWithPodzol (a taiga spruce config -> spruce cone + podzol under). TestOakBeesNest (oak_bees -> bee_nest placed at p=1). TestJungleCocoaVines (a jungle tree -> cocoa + vines). TestPerBiomeTreeSmoke (taiga->spruce, savanna->acacia, dark_forest->dark_oak, jungle->jungle tree resolve through the selector to real decorated trees).
  </action>
  <verify>
    <automated>cd D:/ender && go test ./world/levelgen/feature/ -run 'TestAlterGround|TestBeehive|TestCocoa|TestLeaveVine|TestTrunkVine' -count=1 && go test ./world/ -run 'TestSpruceWithPodzol|TestOakBeesNest|TestJungleCocoaVines|TestPerBiomeTreeSmoke' -count=1 && go build ./... && CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <done>The common overworld tree decorators (AlterGround/Beehive/Cocoa/vines) place jar-exact for known seeds, the tree body runs the real decorator list with the accumulated placed positions on the threaded rng, and every common overworld biome's decorated tree (spruce+podzol, oak+nest, jungle+cocoa/vines, acacia, dark_oak) resolves through the selector to a real tree. The special-biome decorators stay routed to 13-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-data → placer/decorator roster decode | The placer + decorator config (from the pinned 26.2 jar embedded common tree configs) is build-time-trusted. The bodies run off-tick on the single worker scheduler goroutine; no client input reaches them. |
| decorator → cross-chunk Neighborhood | Decorators write bee-nests/cocoa/podzol through the same bctx.view (blockStateWriteRadius=1) as the trunk/foliage; out-of-3x3 writes are dropped. The placed-position collectors feed the decorators, all on the single goroutine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | placer draw-order divergence across the common roster (fancy-oak branches, spruce cone, mega trunks) | mitigate | Each placer is transcribed javap -c + pinned by a geometry+draw-order test vs an oracle; FancyTrunkPlacer (the heaviest math) gets its own branch-count/length assertion. A reorder fails the test. |
| T-13-06 | Tampering | decorator draws happening at the wrong point in the sequence (before foliage, or wrong rng) | mitigate | Decorators run AFTER trunk+foliage on the SAME threaded rng (vanilla order); the decorator tests pin the post-foliage draw count + the placed decoration. The Beehive/Cocoa probability draws are pinned for a seed. |
| T-13-07 | Spoofing/Integrity | a silently-skipped common overworld placer/decorator → a biome's tree missing or wrong | mitigate | A loud error on any unported type a COMMON overworld config references; the per-biome smoke + the COMMON decode test prove the common set is covered. (The FULL all-overworld guard incl. special biomes is 13-03's — the special arms stay loud-errors here, never silent skips.) |
| T-13-08 | Denial of Service | a pathological placer (huge fancy-oak) writing thousands of cross-chunk blocks | accept | Tree footprints are bounded by minimum_size + the world height; writes outside the 3x3 are dropped by the proxy; the worker queue is bounded (Phases 4/8). No new control needed. |
</threat_model>

<verification>
- `go test ./world/levelgen/feature/ -run 'TestSpruceFoliage|TestPineFoliage|TestAcacia|TestDarkOak|TestFancy|TestMegaJungle|TestMegaPine|TestBushFoliage|TestForkingTrunk|TestGiantTrunk|TestJungleFoliage|TestCommonOverworldTreeConfigsDecode|TestAlterGround|TestBeehive|TestCocoa' -count=1` green: the common placer/decorator roster is jar-exact + draw-pinned, every common `tree` config decodes, the special arms still error -> 13-03.
- `go test ./world/ -run 'TestSpruceWithPodzol|TestOakBeesNest|TestJungleCocoaVines|TestPerBiomeTreeSmoke' -count=1` green: every common overworld biome's decorated tree resolves through the selector to a real tree.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` clean; no new deps; feature does NOT import placement or world (acyclic).
</verification>

<success_criteria>
- FEAT-04 (common breadth of criterion 1): every common overworld biome grows its correct vanilla tree set — oak/birch (13-01) + spruce/pine/jungle/acacia/dark_oak/mega variants (here), each with the right trunk/canopy shape AND the common decorations (podzol/bee-nests/cocoa/vines), deterministic per seed. (The special-biome trees + the full completeness guard are 13-03.)
- The common roster is jar-exact: no non-existent placer named (`jungle_foliage_placer`, not MegaJungleFoliagePlacer); any unported common overworld placer/decorator errors loudly; the special-biome arms stay routed to 13-03 (loud-errors, never silent skips).
- The determinism contract holds across the common roster: each placer + decorator draw order is pinned to an oracle; trees + their decorations write through the cross-chunk Neighborhood on the threaded rng.
</success_criteria>

<output>
After completion, create `.planning/phases/13-trees-dungeon-features-gate/13-02-SUMMARY.md`
</output>
