---
phase: 15-mineshaft-stronghold
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - world/structure/stronghold_pieces.go
  - world/structure/stronghold_pieces_test.go
  - world/structure/concentric_rings.go
  - world/structure/phase15_acceptance_test.go
  - world/noisegen.go
requirements: [STRUCT-04]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The recursive StrongholdPieces are ported (javap -c net.minecraft.world.level.levelgen.structure.structures.StrongholdPieces against temp/cache/26.2-inner.jar) using the SAME multi-piece machinery the mineshaft (15-01) proved: the base StructurePiece + addChildren + FindCollisionPiece + PieceAccessor + the StructurePiecesBuilder accessor + placeBlock/generateBox/fillColumnDown/createChest (ALL ported in Phase 14, exercised recursively by 15-01). Per the research (line 84-86) the stronghold pieces are 'more of the same' once the mineshaft recursion works — the hard part (the global ring placement) is already done in 15-02. Port the StrongholdPieces piece set: the StartPiece (the start staircase/down-stairs), StraightStairsDown, Corridor/StraightCorridor, the 5-way Crossing, the spiral StairsDown, the RoomCrossing, the ChestCorridor, the Library (1-2 floor bookshelf room), the PortalRoom (the end_portal_frame ring + the silverfish spawner + the lava), and the FillerCorridor/Turn pieces — each via createRandomPiece/generatePieceFromSmallDoor (the jar's weighted piece-selection factory with the per-piece placedPieceWeights/maxPlaceCount caps)."
    - "The recursion is bounded + collision-checked: addChildren on each stronghold piece proposes the jar-exact children at its doors/exits (the SmallDoor/openings), each constructed at the RNG-drawn offset, collision-checked via FindCollisionPiece against the in-progress list (a collision OR exceeding the genDepth cap rejects the candidate → the corridor turns/ends), and AddPiece'd. The genDepth cap (port the EXACT StrongholdPieces constant — vanilla caps stronghold genDepth at 50) bounds the twisting-corridor recursion so it TERMINATES; the per-piece maxPlaceCount (e.g. exactly ONE PortalRoom, ONE Library run, the weighted distribution) is ported from the StrongholdPieces.PieceWeight table (a draw-count or weight divergence changes the layout). This reuses the 15-01-proven addChildren/FindCollisionPiece pattern verbatim — same accessor, same bound discipline."
    - "The PortalRoom + Library are delivered as VISIBLE geometry (the signature stronghold rooms): the PortalRoom places the end_portal_frame ring (the 12-frame portal, frames hardcoded WITHOUT eyes-of-ender — eye placement is a v3/entity detail, document inline), the silverfish spawner BLOCK (the spawner block, no spawn logic — entity/v3 deferral), the lava, the iron_bars + the stone_brick_stairs; the Library places the bookshelf grid + the smooth_stone_slab walkways + the (optional second floor) ladder + cobweb/torch + the 1-2 chests via createChest (loot tag minecraft:chests/stronghold_library, loot DEFERRED v3). The block palette resolves via block.ToStateID (the established Phase-14 path): stone_bricks + mossy_stone_bricks + cracked_stone_bricks + infested variants (port the StrongholdPieces.SmoothStoneSelector / the per-cell stone-brick-variant RNG selector — the load-bearing per-cell draw, like the mineshaft/jungle MossStoneSelector), iron_bars, iron_door, stone_brick_stairs, wall_torch, chest, end_portal_frame, spawner, bookshelf, cobblestone, smooth_stone_slab."
    - "The stronghold pieces hang on 15-02's anchor: the strongholdStartGen (15-02's placement-half, currently piece-stubbed) is FILLED — on a ring chunk it now seeds the builder (SetLargeFeatureSeed(seed,pos.X,pos.Z)), adds the StartPiece at the ring position + surface-sampled Y, runs the recursive addChildren assembly to the genDepth bound, Encapsulates the piece bboxes into the start BBox, and returns the populated StructureStart. The byte-inert stub is replaced by REAL pieces — the stronghold now places BLOCKS (mirroring how 14-02 replaced 14-01's empty PLACE hook)."
    - "Cross-chunk correctness holds for the stronghold (genuinely many chunks — like the mineshaft): placeBlock clips each piece to the chunk writable box; the re-derivable piece RNG (SetLargeFeatureSeed per owner chunk) makes placeInChunk from any overlapping chunk redraw the same graph + clip → idempotent once-per-overlapping-chunk; a stronghold owned ≥2 (up to 8) chunks out is found by the center's ±8 REFERENCES + placed into the center slice. NO double-write, NO truncation."
    - "The FULL Phase-15 acceptance is automated (NO visual gate — Phase 16 owns that): a single suite asserts (1) STRUCT-03 — the mineshaft places at the legacy-frequency rate (a known seed → a mineshaft at the reducer-derived chunk — the reference computed from the 15-01-CORRECTED reducer (legacy_type_3 → legacyProbabilityReducerWithDouble, jar-seeded SetLargeFeatureSeed(seed,chunkX,chunkZ)), NOT the pre-15-01 mis-ported code) + the recursive corridor/crossing/room fingerprints; (2) STRUCT-04 placement — the stronghold ring positions match vanilla (the ~128 positions for a known seed, the reference derived from generateRingPositions) + isPlacementChunk; (3) STRUCT-04 pieces — the stronghold recursive piece graph places (StartPiece/corridor/stairs/PortalRoom-end_portal_frame/Library-bookshelf signature blocks + fingerprint); (4) both structures deterministic per seed (the start pure over (seed,pos), the graph pure over the SetLargeFeatureSeed stream); (5) cross-chunk idempotence for BOTH multi-chunk structures; (6) the 5x5 reorder-determinism + emit-once stay byte-identical WITH both structures live; all green under Docker -race. Every Phase-15 success criterion → an assertion."
  artifacts:
    - path: "world/structure/stronghold_pieces.go"
      provides: "the recursive StrongholdPieces: the StartPiece/StraightStairsDown/Corridor/Crossing/StairsDown/RoomCrossing/ChestCorridor/Library/PortalRoom/FillerCorridor/Turn structs + addChildren (the door-driven bounded recursion + FindCollisionPiece gate + the per-piece weight/maxPlaceCount table) + the per-piece hardcoded postProcess geometry + the SmoothStoneSelector + createChest"
      contains: "PortalRoom"
      min_lines: 350
    - path: "world/structure/stronghold_pieces_test.go"
      provides: "the stronghold-pieces acceptance: the recursive graph terminates (genDepth 50 bound) + exactly one PortalRoom (maxPlaceCount) + the end_portal_frame/bookshelf signature blocks + the graph fingerprint + the chest block+tag + cross-chunk-span idempotence"
      contains: "TestStronghold"
      min_lines: 130
    - path: "world/structure/concentric_rings.go"
      provides: "the strongholdStartGen piece-stub FILLED: the StartPiece + the recursive addChildren assembly driver hung on 15-02's ring anchor (the byte-inert stub replaced by real pieces)"
      contains: "addChildren"
    - path: "world/structure/phase15_acceptance_test.go"
      provides: "the full Phase-15 close gate: mineshaft (frequency+graph) + stronghold (rings+pieces) + both deterministic + both cross-chunk idempotent + the 5x5/emit-once byte-identity — every STRUCT-03/04 success criterion as an assertion"
      contains: "TestPhase15Acceptance"
      min_lines: 120
    - path: "world/noisegen.go"
      provides: "the stronghold StartGenerator now placing real pieces in the CompositeStartGenerator (the stub generator swapped for the full one — the second BLOCKS-placing Phase-15 structure)"
      contains: "Stronghold"
  key_links:
    - from: "world/structure/stronghold_pieces.go addChildren"
      to: "world/structure/piece.go FindCollisionPiece / PieceAccessor (the 15-01-proven recursion)"
      via: "the stronghold reuses the SAME bounded-recursion machinery the mineshaft proved — door-driven child proposal, collision-checked, genDepth-bounded"
      pattern: "FindCollisionPiece"
    - from: "world/structure/concentric_rings.go strongholdStartGen"
      to: "world/structure/concentric_rings.go StrongholdRingState.isPlacementChunk (15-02)"
      via: "the pieces hang on 15-02's ring anchor — the StartPiece is added only on a ring chunk, then addChildren grows the graph"
      pattern: "isPlacementChunk"
    - from: "world/structure/stronghold_pieces.go PortalRoom/Library postProcess"
      to: "world/structure/piece.go placeBlock / generateBoxSelector / createChest"
      via: "the end_portal_frame ring + the bookshelf grid + the stone-brick-variant selector + the library chest (block + loot tag, loot deferred) write via the Phase-14 helpers"
      pattern: "placeBlock|createChest|generateBoxSelector"
    - from: "world/structure/phase15_acceptance_test.go"
      to: "world/structure/concentric_rings.go generateRingPositions + world/structure/mineshaft.go ApplyFrequencyReducer"
      via: "the close gate asserts the ~128 ring positions + the mineshaft frequency rate against the algorithm-derived references"
      pattern: "generateRingPositions|ApplyFrequencyReducer"
---

<objective>
Port the recursive STRONGHOLD PIECES (STRUCT-04, the geometry half) onto 15-02's proven ring anchor + the 15-01-proven recursion machinery, then CLOSE Phase 15 with the full automated acceptance. Per the research, once the mineshaft recursion (15-01) + the global ring placement (15-02) work, the stronghold pieces are "more of the same":

1. **The recursive piece set**: the StartPiece, twisting Corridors, StairsDown, the 5-way Crossing, RoomCrossing, ChestCorridor, the Library (bookshelf room), and the signature PortalRoom (the end_portal_frame ring + silverfish spawner block + lava) — assembled via the SAME `addChildren`/`FindCollisionPiece` bounded recursion the mineshaft proved, bounded by genDepth 50 + the per-piece `maxPlaceCount` weight table (exactly one PortalRoom).

2. **Hang on 15-02's anchor**: fill the piece-stubbed `strongholdStartGen` — on a ring chunk, add the StartPiece + run the recursive assembly. The byte-inert stub becomes REAL blocks (mirroring 14-02 filling 14-01's empty PLACE hook).

3. **Close Phase 15**: a single automated acceptance maps EVERY STRUCT-03/04 success criterion to an assertion — the mineshaft frequency rate + graph, the stronghold ~128 ring positions + pieces, both deterministic, both cross-chunk idempotent, the 5x5/emit-once byte-identity, all under Docker -race. NO visual gate (Phase 16 owns it).

Purpose: deliver the full stronghold (placement + recursive pieces) in vanilla positions + shape, deterministic per seed, and close Phase 15 with the goal-shaped automated gate.
Output: world/structure/stronghold_pieces.go + the pieces test + the filled strongholdStartGen + the full Phase-15 acceptance + the noisegen swap to the real generator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v2-structures.md

# The Phase-14 piece machinery + the 15-01/15-02 foundations this hangs on:
@.planning/phases/14-structure-pipeline-temples/14-02-SUMMARY.md
@.planning/phases/14-structure-pipeline-temples/14-03-SUMMARY.md
@.planning/phases/15-mineshaft-stronghold/15-01-PLAN.md
@.planning/phases/15-mineshaft-stronghold/15-02-PLAN.md

<interfaces>
<!-- The machinery + foundations this plan builds against. -->

From world/structure/piece.go (the recursion + block helpers — proven recursively by 15-01):
```go
type Piece interface {
    BoundingBox() BoundingBox
    PostProcess(view WorldGenView, box BoundingBox, chunkPos level.ChunkPos, rng levelgen.RandomSource)
}
type PieceAccessor interface { AddPiece(p Piece); FindCollisionPiece(box BoundingBox) Piece }
func FindCollisionPiece(pieces []Piece, box BoundingBox) Piece
// base StructurePiece: bbox/orientation→rotation/mirror/genDepth + getWorldX/Y/Z
// placeBlock / generateBox / generateAirBox / generateBoxSelector(view,box,...,sel BlockSelector) / fillColumnDown
// maybeGenerateBlock / createChest(view,box,x,y,z,lootTable,*[]LootChest) bool
// addChildren(parent, acc PieceAccessor, rng) — the recursion hook (mineshaft 15-01 overrode it; stronghold overrides it)
// transformState (FACING + stair SHAPE + multi-face) — for the iron_door / stone_brick_stairs orientation
type BlockSelector interface { next(rng levelgen.RandomSource, x,y,z int, alwaysReplace bool) block.StateID }  // per-cell variant selector
```

From world/structure/concentric_rings.go (15-02 — the ring anchor this fills):
```go
type StrongholdRingState struct { /* sync.Once-cached []ChunkPos */ }
func (s *StrongholdRingState) isPlacementChunk(pos level.ChunkPos) bool
// strongholdStartGen.GenerateStarts — 15-02 returns a piece-STUBBED anchor on ring chunks; THIS plan fills the pieces
```

From world/structure/mineshaft_pieces.go (15-01 — the recursion pattern to mirror):
```go
// the StructurePiecesBuilder accessor (a []Piece + AddPiece/FindCollisionPiece) + the genDepth-bounded addChildren driver
// — the stronghold reuses the EXACT same builder/accessor/bound discipline
```

From world/structure/cache.go + start.go:
```go
type StartGenerator interface { GenerateStarts(seed int64, pos level.ChunkPos, sampler SurfaceSampler, biomeAt BiomeAt) []*StructureStart }
type StructureStart struct { Structure string; ChunkPos level.ChunkPos; Pieces []Piece; BBox BoundingBox; References int }
func (s *StructureStart) RecomputeBBox()
```

From world/levelgen/random.go:
```go
func (w *WorldgenRandom) SetLargeFeatureSeed(worldSeed int64, chunkX, chunkZ int)   // the re-derivable piece RNG
```

From level/block (the palette resolver — mirror world/structure/jungle_temple.go stateOf/dir helpers):
```go
// stone_bricks/mossy_stone_bricks/cracked_stone_bricks/infested_* , iron_bars, iron_door, stone_brick_stairs,
// wall_torch, chest, end_portal_frame, spawner, bookshelf, cobblestone, smooth_stone_slab → block.ToStateID
```
</interfaces>

# The stronghold loot tables (loot DEFERRED v3 — referenced by tag only):
# minecraft:chests/stronghold_library, minecraft:chests/stronghold_corridor, minecraft:chests/stronghold_crossing
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the recursive StrongholdPieces (addChildren + genDepth/maxPlaceCount bound + the PortalRoom/Library geometry + the stone-brick selector) hung on 15-02's anchor</name>
  <files>world/structure/stronghold_pieces.go, world/structure/concentric_rings.go, world/structure/stronghold_pieces_test.go, world/noisegen.go</files>
  <behavior>
    - Test: the recursive assembly TERMINATES — the piece graph for a known ring-seed has a bounded count (genDepth 50 + maxPlaceCount caps), never infinite; FindCollisionPiece rejects overlapping candidates (no two pieces' bboxes overlap beyond the shared-door tolerance).
    - Test: exactly ONE PortalRoom in the graph (the maxPlaceCount=1 cap); the Library appears within its weighted cap; the weighted piece distribution matches the jar PieceWeight table for the seed.
    - Test: the PortalRoom places the end_portal_frame ring (12 frames, no eyes — assert the frame blocks) + the silverfish spawner block + lava + iron_bars; the Library places the bookshelf grid + smooth_stone_slab + a chest (block + loot tag minecraft:chests/stronghold_library, loot deferred — assert the LootChest tag).
    - Test: the graph fingerprint (FNV over sorted (pos,state)) matches a pinned value for a known seed; the SmoothStoneSelector per-cell stone-brick-variant draw is the determinism contract (a draw-count divergence flips the hash).
    - Test: the strongholdStartGen (15-02) now returns a POPULATED start on a ring chunk (≥1 piece, BBox non-empty), nil off-ring; pure over (seed,pos).
  </behavior>
  <action>Port StrongholdPieces (javap -c net.minecraft.world.level.levelgen.structure.structures.StrongholdPieces): the piece structs (StartPiece, StraightStairsDown, StraightCorridor, the 5-way Crossing, StairsDown spiral, RoomCrossing, ChestCorridor, Library, PortalRoom, FillerCorridor, Turn) each embedding the base StructurePiece + a genDepth field. Implement addChildren on each: propose the jar-exact children at the piece's SmallDoor openings/exits via the createRandomPiece/generatePieceFromSmallDoor weighted factory (port the PieceWeight table — weight + maxPlaceCount + the placeCount tracking; exactly one PortalRoom), construct each candidate bbox, call acc.FindCollisionPiece(candidateBox) — reject collisions AND candidates exceeding the genDepth 50 cap (port the EXACT constant) → the corridor turns/ends, acc.AddPiece survivors, recurse. Reuse the 15-01 StructurePiecesBuilder accessor + the bounded-recursion driver VERBATIM (same pattern). PostProcess per piece: the hardcoded geometry via placeBlock/generateBox/generateBoxSelector/fillColumnDown — the stone-brick shell via the SmoothStoneSelector (port the per-cell stone_bricks/mossy/cracked/infested nextFloat draws, the load-bearing determinism contract, like the mineshaft/jungle selectors); the PortalRoom (end_portal_frame ring without eyes + the silverfish spawner block + lava + iron_bars + stone_brick_stairs); the Library (bookshelf grid + smooth_stone_slab walkways + optional 2nd-floor ladder + cobweb/wall_torch + 1-2 chests via createChest with minecraft:chests/stronghold_library); the corridors/stairs/crossings. Resolve every state via block.ToStateID (mirror the jungle_temple stateOf/dir helpers). FILL the 15-02 strongholdStartGen stub: on a ring chunk, seed the builder via SetLargeFeatureSeed(seed,pos.X,pos.Z), add the StartPiece at the ring pos + surface Y, drive addChildren to the genDepth bound, RecomputeBBox, return the populated start. Swap the byte-inert stub generator in world/noisegen.go for the full one (the CompositeStartGenerator now places real stronghold blocks). Document the eye-of-ender + spawner-spawn-logic + loot deferrals (v3) inline.</action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestStronghold' -count=1</automated>
  </verify>
  <done>The recursive stronghold graph grows bounded (genDepth 50 + one PortalRoom), collision-checked, fingerprint-pinned; the PortalRoom (end_portal_frame ring) + Library (bookshelves + chest block+tag) place; the strongholdStartGen returns a populated start hung on 15-02's ring anchor; the stronghold places real blocks.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: the full Phase-15 acceptance — every STRUCT-03/04 success criterion as an automated assertion + cross-chunk idempotence + the 5x5/emit-once close</name>
  <files>world/structure/phase15_acceptance_test.go, world/noisegen.go</files>
  <behavior>
    - Assert STRUCT-03: the mineshaft places at the legacy-frequency rate — a known seed → a mineshaft at the reducer-derived chunk (the chunk computed FROM the 15-01-CORRECTED ApplyFrequencyReducer — legacy_type_3 → legacyProbabilityReducerWithDouble, jar-seeded; NOT the pre-fix mis-ported reducer; not eyeballed); the recursive corridor/crossing/room graph fingerprint matches.
    - Assert STRUCT-04 placement: the ~128 stronghold ring positions for a known seed match the generateRingPositions-derived reference; isPlacementChunk true at a ring pos, false off-ring.
    - Assert STRUCT-04 pieces: the stronghold recursive graph places — StartPiece + corridor/stairs + the PortalRoom end_portal_frame ring + the Library bookshelves (signature blocks + the graph fingerprint).
    - Assert determinism: both structures pure over (seed,pos); the same seed → the same mineshaft + stronghold (graph fingerprints stable across two computations).
    - Assert cross-chunk idempotence: BOTH the multi-chunk mineshaft AND the multi-chunk stronghold — the union of per-chunk slices == the whole graph, no double-write, no truncation; a structure owned ≥2 (up to 8) chunks out found by the center's ±8 REFERENCES.
    - Assert the 5x5 reorder-determinism (world TestDecorationReorderIdentical) + emit-once (TestEmitOnce) stay byte-identical WITH both structures live.
  </behavior>
  <action>Write world/structure/phase15_acceptance_test.go (TestPhase15Acceptance + the cross-chunk + purity sub-tests) mirroring the Phase-14 acceptance_test.go pattern (per-structure expected chunk derived from the algorithm + the load-bearing biome/ring gate + the full fingerprint + cross-chunk idempotence + starts purity). Cover BOTH STRUCT-03 (mineshaft: the 15-01-CORRECTED ApplyFrequencyReducer-derived chunk [legacy_type_3 → legacyProbabilityReducerWithDouble, jar-seeded] + the graph fingerprint — flag in-test that this reference depends on the 15-01 reducer fix landing first) and STRUCT-04 (stronghold: the generateRingPositions-derived ~128 positions + isPlacementChunk + the piece-graph fingerprint incl. PortalRoom/Library signature blocks). Use the same fixed-surface/biome-stub technique 14-02/14-03 used for router-independent stable fingerprints where the real router is too slow, but ALSO include at least one REAL-pipeline placement test (via the NoiseGenerator) per structure proving the production path (router + GetBiome + STARTS + REFERENCES + PLACE) places the structure — mirroring TestDesertPyramidPlacesInPipeline. Add the cross-chunk-span idempotence for both (the union==whole-graph, no overlap). Then run the ./world/ 5x5 reorder + emit-once gates to confirm byte-identity WITH both structures live. This is the Phase-15 close gate — NO autonomous:false human-verify task (Phase 16 owns the visual gate).</action>
  <verify>
    <automated>cd D:/ender && go test ./world/structure/ -run 'TestPhase15Acceptance' -count=1 && go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce|TestStructuresPipelineAcceptance|TestMineshaft|TestStronghold' -count=1</automated>
  </verify>
  <done>The full Phase-15 acceptance is green: the mineshaft frequency+graph, the stronghold rings+pieces, both deterministic, both cross-chunk idempotent (incl. a real-pipeline placement per structure), and the 5x5/emit-once byte-identity — every STRUCT-03/04 success criterion asserted, no visual gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worldgen RNG → stronghold piece graph | the SetLargeFeatureSeed stream + the PieceWeight table drive the twisting layout; a divergence desyncs the whole stronghold |
| piece postProcess → chunk blocks | the many-chunk stronghold must clip per chunk (no cross-chunk double-write) |
| the acceptance suite → STRUCT-03/04 closure | the gate must assert every success criterion against algorithm-derived references (not eyeballed values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | DoS | the recursive stronghold addChildren running away (twisting corridors, unbounded) | mitigate | The genDepth 50 cap (the exact jar constant) + the per-piece maxPlaceCount bound recursion; FindCollisionPiece prunes overlaps; TestStrongholdTerminates pins a bounded count + exactly one PortalRoom. |
| T-15-13 | Tampering | the piece graph diverging from vanilla (wrong addChildren draw order, wrong PieceWeight table, wrong SmoothStoneSelector draws, wrong PortalRoom/Library geometry) | mitigate | The assembly + the weight table + the selector are javap -c transcribed; TestStrongholdFingerprint pins the graph + the end_portal_frame/bookshelf signature blocks; a reorder/wrong-weight flips the hash. |
| T-15-14 | Tampering | the cross-chunk clip failing for the many-chunk stronghold (double-write/truncation, Pitfall #2) | mitigate | placeBlock clips to box.IsInside; the re-derivable piece RNG (SetLargeFeatureSeed per owner chunk) makes placeInChunk redraw+clip per chunk; TestPhase15Acceptance pins union==whole-graph, idempotent; the ±8 REFERENCES finds a ≥2-out owner. |
| T-15-15 | Integrity (non-reproducible world) | the same seed yielding a different stronghold (placement-order or RNG divergence) | mitigate | The start is pure over (seed,pos) (hung on the pure ring anchor); the piece RNG is re-derivable; TestPhase15Acceptance asserts both structures stable across two computations + the 5x5/emit-once gates re-run byte-identical. |
| T-15-16 | Information disclosure | the stronghold chests / the portal frame / the spawner exposing loot or spawn behavior | accept | Loot is a v3 deferral (chest block + loot tag minecraft:chests/stronghold_library, no loot rolled); the end_portal_frame places WITHOUT eyes-of-ender (a v3/entity detail); the silverfish spawner is the BLOCK only (no spawn logic). Documented inline; matches the Phase-14 temple precedent. |
| T-15-17 | Spoofing | a Phase-15 success criterion left unasserted (a silent gap closing the phase) | mitigate | TestPhase15Acceptance maps EVERY STRUCT-03/04 success criterion to an explicit assertion (mineshaft frequency+graph, stronghold rings+pieces, both deterministic, both cross-chunk, 5x5/emit-once) + at least one real-pipeline placement per structure; the references are algorithm-derived, not eyeballed. |
</threat_model>

<verification>
- `go test ./world/structure/ -run 'TestStronghold|TestPhase15Acceptance'` green: the recursive stronghold graph (genDepth 50, one PortalRoom) + the PortalRoom/Library geometry + the chest block+tag + the full Phase-15 acceptance (mineshaft + stronghold, deterministic, cross-chunk idempotent).
- `go test ./world/ -run 'TestDecorationReorderIdentical|TestEmitOnce|TestStructuresPipelineAcceptance|TestMineshaft|TestStronghold|TestDesertPyramid'` green: the 5x5 reorder-determinism + emit-once stay byte-identical WITH both Phase-15 structures live; the temples + mineshaft stay green.
- Docker `-race` clean across `./world/... ./world/structure/...` on golang:1.26 (CGO_ENABLED=0): no race in the recursive stronghold assembly / cross-chunk placement / the sync.Once ring read.
- `go build ./...` + `CGO_ENABLED=0 go build ./...` + `go vet ./world/...` clean; NO new deps; NO import cycle (world→world/structure only); NO encoder/packet/chunk-wire/golden file touched.
</verification>

<success_criteria>
- STRUCT-04 success criterion 3: the stronghold's recursive piece set (corridors/stairs/portal room/library) is ported reusing the Phase-14 StructurePiece/addChildren machinery.
- STRUCT-03 + STRUCT-04 success criterion 4: both structures generate in vanilla positions + shapes, deterministic per seed (verified against known-seed references).
- The full Phase-15 acceptance maps every STRUCT-03/04 success criterion to an automated assertion (mineshaft frequency+graph, stronghold rings+pieces, both deterministic, both cross-chunk idempotent, 5x5/emit-once byte-identity) + a real-pipeline placement per structure.
- The recursion is genDepth-50-bounded + maxPlaceCount-capped (terminates, one PortalRoom).
- Docker -race clean. NO human-verify gate (Phase 16 owns the visual gate).
</success_criteria>

<output>
After completion, create `.planning/phases/15-mineshaft-stronghold/15-03-SUMMARY.md` documenting: the StrongholdPieces set + the genDepth/maxPlaceCount bounds, the PortalRoom/Library geometry + the SmoothStoneSelector, the filled strongholdStartGen, the full Phase-15 acceptance results (the mineshaft frequency chunk + the ~128 ring positions + the piece fingerprints), and the v3 deferrals (eyes-of-ender, spawner spawn logic, loot). Mark STRUCT-03 + STRUCT-04 complete.
</output>
