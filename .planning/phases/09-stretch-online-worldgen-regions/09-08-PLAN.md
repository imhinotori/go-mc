---
phase: 09-stretch-online-worldgen-regions
plan: 08
type: execute
wave: 6
depends_on: [06, 07]
files_modified:
  - world/noisegen.go
  - world/noisegen_test.go
autonomous: true
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "world/noisegen.go's NoiseGenerator implements the UNCHANGED world.Generator interface (Generate(pos level.ChunkPos) *level.Chunk) and DRIVES the full pipeline in vanilla's chunk-status order: (1) NoiseChunk cell-sample final_density [09-04], (2) the real Fill — solid→OreVein/stone, non-solid→Aquifer water/lava/air [09-05], (3) ApplyCarvers — ravines + extra tunnel caves on top, aquifer-aware [09-06], (4) buildSurface + getBiome — biome-correct surface + the biome containers + the 3 CLIENT heightmaps [09-07]; then per-section FluidCount + full sky light (mirroring Superflat's finishing), filling the REUSED capture-diff-sealed level.Chunk — this is the full-parity terrain (hills + caves + ravines + aquifers + ore veins + biome-varied surfaces) behind the UNCHANGED off-tick worker"
    - "Generate is PURE (same seed+pos → identical *level.Chunk bytes — the worker's purity contract, WORLD-04/Pitfall 7): NewNoiseGenerator(seed, secs, minY) parses+seeds the router + builds the biome source + the surface system ONCE; Generate(pos) reads only the seed + the bound graph + the clamped pos — NO math/rand, NO global state, NO map-iteration-order in the pipeline. The existing Superflat purity-test shape (world/generator_test.go) applies to the noise gen"
    - "ZERO concurrency/seam work: the off-tick world.Worker already runs Generate off the tick (the worker is generator-agnostic) — PARITY-01 is purely 'a better Generate'. The Generator interface, world/worker.go, the tick, and the level.Chunk wire are ALL UNTOUCHED (the chunk format was capture-diff-sealed in Phase 4 — no new wire surface). Superflat stays as a fallback/test generator (not deleted)"
    - "a generated chunk is a complete, valid, recognizable vanilla overworld chunk: solid biome-varied ground (not all-air), water at sea level + perched aquifers + lava deep down, EXPLORABLE caves (noise caves + carved tunnels) + ravines, ore veins in the rock, valid varied biome containers, valid 3 CLIENT heightmaps — and it round-trips the Phase-4 encoder (the wire is sealed; the generator only fills it). Determinism holds (same seed+pos → identical encoded bytes)"
  artifacts:
    - path: "world/noisegen.go"
      provides: "NoiseGenerator implementing world.Generator: NewNoiseGenerator(seed, secs, minY) builds the router+biome+surface once; Generate(pos) drives noise→fill(aquifer/ore)→carve→surface(biome+heightmaps) into the reused level.Chunk, pure"
      contains: "func (g *NoiseGenerator) Generate"
  key_links:
    - from: "world/noisegen.go NoiseGenerator.Generate"
      to: "world/levelgen NoiseChunk[09-04] + Fill/Aquifer/OreVeinifier[09-05] + carver.ApplyCarvers[09-06] + surface.buildSurface + biome.getBiome[09-07]"
      via: "Generate drives the full pipeline in chunk-status order: sample → fill → carve → surface"
      pattern: "ApplyCarvers"
    - from: "world/noisegen.go NoiseGenerator"
      to: "level.Chunk SetBlock + NewBiomesPaletteContainer (REUSE, Phase 4)"
      via: "the generator fills the capture-diff-sealed paletted container + the varied biome + the 3 CLIENT heightmaps"
      pattern: "SetBlock"
    - from: "world/noisegen.go"
      to: "world.Generator interface (UNCHANGED) + world/generator.go Superflat finishing"
      via: "compile-time var _ Generator = (*NoiseGenerator)(nil); mirror Superflat's FluidCount/skylight finishing"
      pattern: "Generator"
---

<objective>
Assemble the full-parity `NoiseGenerator` — the `world.Generator` that drives the whole ported pipeline (Waves 4-7) into a complete, valid, recognizable vanilla overworld chunk. This is where the ported primitives (Wave 2), the bound graph incl. caves (Wave 3), the cell sampler (Wave 4), the Aquifer + OreVeinifier (Wave 5), the carvers (Wave 6), and the surface + biome source (Wave 7) become a real chunk a client renders — with hills, caves, ravines, aquifers, ore veins, and biome-varied surfaces.

The Generator drives the pipeline in vanilla's chunk-status order: (1) `NoiseChunk` cell-sample `final_density` (Wave 4) → (2) the real `Fill` (solid→OreVein/stone, non-solid→Aquifer water/lava/air — Wave 5) → (3) `ApplyCarvers` (ravines + extra tunnel caves, aquifer-aware — Wave 6) → (4) `buildSurface` + `getBiome` (biome-correct surface + the biome containers + the 3 CLIENT heightmaps — Wave 7); then the per-section finishing (FluidCount, full sky light) mirroring Superflat. It fills the REUSED, capture-diff-sealed `level.Chunk` (Phase 4).

`Generate` is PURE (same seed+pos → identical `*level.Chunk` bytes — the worker's purity contract, WORLD-04/Pitfall 7): `NewNoiseGenerator` builds the router + biome source + surface system ONCE; `Generate` reads only the seed + the bound graph + the clamped pos. ZERO concurrency/seam work — the off-tick `world.Worker` already runs `Generate` off the tick (the worker is generator-agnostic); the `Generator` interface, `world/worker.go`, the tick, and the `level.Chunk` wire are ALL UNTOUCHED. Superflat stays as a fallback/test generator. ZERO new dependencies.

Purpose: this is the integration point that turns "the pieces exist and their tests pass" into "a pure, drop-in Generator producing a full-parity chunk." The wiring into main.go + the BLOCKING real-client gate is Wave 7 (Plan 09-09).
Output: world/noisegen.go (+ test). ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md
@.planning/phases/09-stretch-online-worldgen-regions/09-05-PLAN.md
@.planning/phases/09-stretch-online-worldgen-regions/09-06-PLAN.md
@.planning/phases/09-stretch-online-worldgen-regions/09-07-PLAN.md

<interfaces>
<!-- VERIFIED on disk this session. This plan consumes Waves 4-7 + REUSES the Phase-4 level.Chunk + the Superflat finishing discipline. -->

REUSE — world/generator.go (the UNCHANGED interface + the Superflat reference to MIRROR):
```go
type Generator interface { Generate(pos level.ChunkPos) *level.Chunk } // implement this; Superflat is the reference
// Superflat demonstrates: EmptyChunk(secs); sectionLocal(x,y,z)→(sec,local) = ((y-MinY)>>4, (y&15)<<8|(z&15)<<4|(x&15));
//   per-section FluidCount + NewBiomesPaletteContainer + fullSkyLight();
//   the 3 CLIENT heightmaps via NewBitStorage(bitsForHeight, 256, nil).Set(col, (top-MinY)+1)
// NewSuperflat takes (secs, minY, surfaceY); NoiseGenerator's constructor takes the SEED + (secs, minY) and derives the rest from the router.
```
The pipeline pieces (Waves 4-7):
```go
// Wave 4 (world/levelgen):
func NewNoiseChunk(r *Router, pos level.ChunkPos) *NoiseChunk
func (nc *NoiseChunk) finalDensity(localX, y, localZ int) float64
// Wave 5 (world/levelgen):
func NewAquifer(r *Router, pos level.ChunkPos) *Aquifer
func NewOreVeinifier(/* the vein functions + RandomState */) *OreVeinifier
func Fill(nc *NoiseChunk, aq *Aquifer, ov *OreVeinifier, set func(localX,y,localZ int, state block.StateID))
// Wave 6 (world/levelgen/carver):
func ApplyCarvers(seed int64, pos level.ChunkPos, aq *Aquifer, carvers []*ConfiguredCarver, set func(localX,y,localZ int, state block.StateID))
// Wave 7 (world/levelgen/biome + surface):
func NewMultiNoiseBiomeSource(r *Router) *MultiNoiseBiomeSource
func (s *MultiNoiseBiomeSource) getBiome(x, y, z int) biome.Type
func (ss *SurfaceSystem) buildSurface(column, pos, getBiome, set, writeHeightmaps ...)
```
NOTE: confirm the exact constructor/method signatures from the Wave-4..7 SUMMARYs/source before wiring (they may have adjusted names during the port — read the actual exported API). The fill/carve/surface set-block callbacks write into a per-chunk block buffer the generator then commits to level.Chunk sections (or write directly to the chunk — pick the simplest correct approach; a column/section buffer is cleaner for the top-down surface walk).
</interfaces>

<key_facts>
<!-- VERIFIED against 09-RESEARCH (Code Examples / Reuse table / Pitfall 7) + world/generator.go this session. -->
- DRIVE THE PIPELINE IN CHUNK-STATUS ORDER (the vanilla sequence): noise (cell sample) → fill (aquifer/ore) → carve (ravines/caves) → surface (biome + heightmaps). This mirrors vanilla's NOISE → SURFACE → CARVERS status order (note: vanilla runs CARVERS after SURFACE in the status pipeline, but the carve operates on the pre-surface stone — confirm the exact order from ChunkStatusTasks: the carvers run BEFORE the surface in the block sense (they carve stone, then the surface is applied to the carved top). Read ChunkStatusTasks to get fill→carve→surface right). Get the order right so a carved ravine gets a correct surface on its new top.
- REUSE THE SEAM (09-RESEARCH Reuse table + STATE.md): NoiseGenerator implements the UNCHANGED world.Generator; the off-tick world.Worker already runs Generate (zero concurrency work). Mirror Superflat's finishing (EmptyChunk, sectionLocal, FluidCount, the biome container, fullSkyLight, the 3 CLIENT heightmaps via NewBitStorage). The chunk WIRE was sealed in Phase 4 — NO new wire surface (the gate is visual+determinism, not a capture-diff — Wave 7/Plan 09-09).
- BIOME CONTAINERS CARRY THE VARIED BIOME (full-parity): unlike Superflat's single plains, fill NewBiomesPaletteContainer from the Wave-7 getBiome (per the noise-biome 4×4×4 resolution). The container may now be multi-valued (multiple biomes per section) — confirm the Phase-4 paletted biome container handles a multi-entry palette (it does — it is the same paletted container the blocks use; Superflat just happened to be single-valued). A multi-biome section is the normal case now.
- PURE GENERATE (Pitfall 7 / WORLD-04 / the worker contract): NewNoiseGenerator(seed, secs, minY) builds the router (NewRouter) + the biome source + the surface system + the carver configs ONCE; Generate(pos) is PURE — same seed+pos → identical *level.Chunk bytes. NO math/rand, NO global state, NO map-iteration-order in the pipeline (the biome nearest-match + the carver seeding are deterministic).
- CONSTRUCTOR SHAPE: NewNoiseGenerator(seed int64, secs, minY int) *NoiseGenerator — builds the Router (carrying SeaLevel/dims/blocks/aquifers/ore-veins) + the Aquifer/OreVein function refs + the carver configs (data.ConfiguredCarver) + the MultiNoiseBiomeSource + the SurfaceSystem, resolving block ids once. secs/minY are passed by main.go (overworldSecs=24, overworldMinY=-64) exactly as Superflat; SeaLevel comes from the router (63).
- ZERO NEW DEPS, ZERO CONCURRENCY: stdlib only; the worker/tick/Generator interface are UNTOUCHED. NO xsync/ants/conc.
- STALE-LSP CAVEAT: trust `go build ./...` over gopls/Serena phantom errors.
- Serena guidance: create world/noisegen.go (NoiseGenerator + NewNoiseGenerator + Generate driving the 4-stage pipeline + the Superflat-style finishing) + world/noisegen_test.go. Mirror world/generator.go (Superflat) for the finishing; mirror world/generator_test.go for the determinism test. Do NOT touch world/worker.go, world/generator.go (the interface/Superflat), the tick, the levelgen sub-packages (consume them), or cmd/sulfur/main.go (Wave 7/Plan 09-09 wires it).
</key_facts>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-clamped chunk position → Generate (off-tick) | Generate's only input is the chunk position (server-clamped by the view-distance ring, Phase 4). It runs on the existing off-tick worker over the build-trusted pipeline. No client data reaches the generator beyond the clamped position. Integrity = determinism (purity); availability = per-chunk pipeline cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-22 | Denial of Service | the full pipeline (sample+fill+carve+surface+biome) making Generate too slow off-tick → chunks stream late | mitigate | Each stage is bounded (cell interpolation ~768 samples; aquifer per-cell cache; carver bounded range; biome per-4×4×4 cell); the worker's bounded queue drops on overload (proven Phases 4/8). Asserted by the chunk-gen test completing fast. |
| T-9-23 | Integrity (world corruption) | a non-pure Generate breaking same-seed+pos→same-bytes | mitigate | Generate reads only the seed + the bound pipeline + the clamped pos; no math/rand/global/time (Pitfall 7). Asserted by TestNoiseGenDeterministic (two Generate calls → identical encoded bytes). |
| T-9-24 | Integrity (mis-render) | wrong heightmaps / missing surface / degenerate columns → client glitches or void | mitigate | buildSurface (Wave 7) writes the 3 CLIENT heightmaps from the computed top; asserted by TestNoiseGenChunk (valid heightmaps, no all-air/all-stone column, water at sea level, varied biomes, explorable caves present). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NoiseGenerator implements world.Generator, driving the full noise→fill→carve→surface pipeline (PARITY-01)</name>
  <files>world/noisegen.go, world/noisegen_test.go</files>
  <read_first>
    - .planning/phases/09-stretch-online-worldgen-regions/09-RESEARCH.md — Code Examples (the NoiseGenerator/Generate skeleton + the main.go swap), Reuse table (the Generator seam + level.Chunk), Pitfall 6 (heightmaps), Pitfall 7 (determinism/purity).
    - The jar (javap): `javap -c net.minecraft.world.level.chunk.status.ChunkStatusTasks` — confirm the fill→carve→surface order (which status runs the carvers relative to the surface). Cite it.
    - world/generator.go — the Generator interface + the FULL Superflat.Generate (EmptyChunk, the fill loop, the per-section finishing, the 3 CLIENT heightmaps) to MIRROR for the finishing.
    - The Wave-4..7 SUMMARYs + the exported API of world/levelgen (NoiseChunk/Fill/Aquifer/OreVeinifier), world/levelgen/carver (ApplyCarvers/ConfiguredCarver), world/levelgen/biome (MultiNoiseBiomeSource/getBiome), world/levelgen/surface (SurfaceSystem/buildSurface) — read the ACTUAL signatures.
    - world/generator_test.go — the Superflat purity-test shape to mirror for TestNoiseGenDeterministic.
  </read_first>
  <behavior>
    PARITY-01 (the full-parity Generator assembly — pure, reusing the seam):
    - Test 1 (TestNoiseGenDeterministic): NewNoiseGenerator(seed, secs, minY).Generate(pos) called twice for the same pos produces *level.Chunk values that ENCODE to identical bytes (the worker purity contract / WORLD-04); two generators from the same seed also agree.
    - Test 2 (TestNoiseGenImplementsGenerator): a *NoiseGenerator satisfies world.Generator (compile-time var _ Generator = (*NoiseGenerator)(nil)) — a drop-in for the worker.
    - Test 3 (TestNoiseGenChunkComplete): a generated chunk has solid ground (not all-air), water at sea level (low columns), varied biome containers (more than one biome across several chunks), valid 3 CLIENT heightmaps, and round-trips the Phase-4 encoder without error.
    - Test 4 (TestNoiseGenFullParityFeatures): across a sampling of positions/columns, the chunk shows the full-parity features — surface height VARIES (hills/valleys), there exist underground air pockets (noise caves), carved tunnels/ravines (the carve pass replaced stone with air/water), ore-vein blocks appear in the rock, and a perched/deep fluid (aquifer water/lava away from sea level) appears — proving the whole pipeline ran (not just a surface).
  </behavior>
  <action>
    1. world/noisegen.go — package world. `type NoiseGenerator struct { router *levelgen.Router; biomes *biome.MultiNoiseBiomeSource; surface *surface.SurfaceSystem; carvers []*carver.ConfiguredCarver; seed int64; secs, minY, seaLevel int; /* resolved block ids */ }`. `func NewNoiseGenerator(seed int64, secs, minY int) *NoiseGenerator`: build the Router (panic on a build-data error like NewSuperflat), the MultiNoiseBiomeSource, the SurfaceSystem, the carver configs (data.ConfiguredCarver), resolve block ids + read SeaLevel/dims from the router ONCE. `func (g *NoiseGenerator) Generate(pos level.ChunkPos) *level.Chunk`: PURE — ch := level.EmptyChunk(g.secs); build a NoiseChunk + an Aquifer + an OreVeinifier for pos; (1) Fill (cell sample → solid OreVein/stone, non-solid Aquifer water/lava/air) into a per-chunk block buffer; (2) ApplyCarvers (ravines + caves, aquifer-aware) over the buffer in the carving range; (3) buildSurface (top-down, biome-correct surface + the 3 CLIENT heightmaps) over the carved buffer using getBiome; commit the buffer to level.Chunk sections via SetBlock; fill NewBiomesPaletteContainer per section from getBiome (the varied biomes); FluidCount + fullSkyLight (mirror Superflat); ch.Status = level.StatusFull; return ch. Add `var _ Generator = (*NoiseGenerator)(nil)`. Cite ChunkStatusTasks for the stage order.
    2. world/noisegen_test.go (package world) — the 4 tests in <behavior>; mirror world/generator_test.go for the determinism/encode round-trip.
  </action>
  <verify>
    <automated>go test ./world/ -run 'TestNoiseGenDeterministic|TestNoiseGenImplementsGenerator|TestNoiseGenChunkComplete|TestNoiseGenFullParityFeatures' -count=1 && go vet ./world/... && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - NoiseGenerator implements the UNCHANGED world.Generator (compile-time asserted), drives the full noise→fill(aquifer/ore)→carve→surface(biome+heightmaps) pipeline in chunk-status order, and is a drop-in for the off-tick worker (zero concurrency/seam work — the worker/tick/interface untouched).
    - Generate is PURE: same seed+pos → identical encoded bytes (WORLD-04/Pitfall 7); two generators from the same seed agree.
    - A generated chunk is full-parity: solid varied-biome ground, sea-level water + aquifer fluids, explorable noise caves + carved tunnels/ravines, ore veins, valid 3 CLIENT heightmaps, varying surface height — and round-trips the Phase-4 encoder; no all-air/all-stone columns.
    - `go build ./...`, `go vet ./world/...` clean; zero new deps; NO change to world/worker.go / world/generator.go / the tick.
  </acceptance_criteria>
  <done>NoiseGenerator is a pure, drop-in world.Generator producing full-parity terrain (hills + caves + ravines + aquifers + ore veins + biome-varied surfaces) by driving the whole ported pipeline into the reused capture-diff-sealed level.Chunk — Wave 7 (Plan 09-09) wires it into main.go + runs the BLOCKING real-client visual gate.</done>
</task>

</tasks>

<verification>
- `go test ./world/ -run 'TestNoiseGenDeterministic|TestNoiseGenImplementsGenerator|TestNoiseGenChunkComplete|TestNoiseGenFullParityFeatures' -count=1` passes.
- The Wave-1..7 tests + the existing Superflat purity test still pass — the generator REUSES the seam, does not regress it.
- `go vet ./... && go build ./...` clean; ZERO new dependencies; world/worker.go, world/generator.go (interface/Superflat), the tick, and cmd/sulfur/main.go are UNTOUCHED.
</verification>

<success_criteria>
- PARITY-01 (the full-parity Generator): a PURE NoiseGenerator implementing the UNCHANGED world.Generator drives the whole ported pipeline — NoiseChunk cell-sample (Wave 4) → Fill with Aquifer + OreVeinifier (Wave 5) → ApplyCarvers ravines+caves (Wave 6) → buildSurface + multi-noise biomes + heightmaps (Wave 7) — into the reused capture-diff-sealed level.Chunk, producing recognizable full-parity overworld terrain (hills, caves, ravines, aquifers, ore veins, biome-varied surfaces).
- The Generator seam is REUSED (zero concurrency/seam work — the worker/tick/interface untouched); Generate is deterministic (same seed+pos → identical bytes — WORLD-04/Pitfall 7); no new wire surface (the chunk fills the Phase-4-sealed container).
- Idiomatic Go, no GPL paste, javap-cited; zero new dependencies; the wiring into main.go + the BLOCKING real-client gate is Plan 09-09.
</success_criteria>

<output>
After completion, create `.planning/phases/09-stretch-online-worldgen-regions/09-08-SUMMARY.md`.
</output>
