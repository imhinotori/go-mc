---
phase: 20-structure-polish-loot-inhabitants-beard-persistence
plan: 05
type: execute
wave: 2
depends_on: ["20-03"]
files_modified:
  - world/structure/beard.go
  - world/structure/beard_test.go
  - world/levelgen/noisechunk/fill.go
  - world/noisegen.go
autonomous: true
requirements: [STRUCT-POLISH-03]

must_haves:
  truths:
    - "Village (beard_thin) raises terrain to meet a floating structure; stronghold (bury) digs terrain to bury it"
    - "Temples/igloo/mineshaft/swamp-hut (terrain_adaptation NONE) are UNCHANGED — no beard, byte-identical to Phase 16"
    - "Structure STARTS are computed BEFORE fillFromNoise so the beard density term influences final_density at FILL time (not after)"
    - "The beard is an additive per-block term in the fill loop (NOT a router-graph node, NOT the per-marker interpolator)"
    - "The beard does not double-apply (gated on Beardifier.affectedBox.isInside)"
  artifacts:
    - path: "world/structure/beard.go"
      provides: "Beardifier.forStructuresInChunk + compute + getBuryContribution + getBeardContribution (ported)"
      contains: "getBuryContribution|getBeardContribution"
    - path: "world/levelgen/noisechunk/fill.go"
      provides: "the additive beard term in the final_density fill summation"
      contains: "beard|Beard"
    - path: "world/noisegen.go"
      provides: "STARTS computed pre-fill + the beard term threaded into the fill"
      contains: "ComputeStarts|Beard"
  key_links:
    - from: "world/noisegen.go (pre-fill)"
      to: "world/structure/cache.ComputeStarts"
      via: "compute starts for C + its ±12 window before NewNoiseChunk/fillFromNoise"
      pattern: "ComputeStarts"
    - from: "world/levelgen/noisechunk/fill.go"
      to: "structure.Beardifier.compute"
      via: "additive final_density term per block (BeardifierMarker substitution)"
      pattern: "compute|Beardifier"
---

<objective>
Make structures adapt to terrain (STRUCT-POLISH-03) via the Beardifier density contribution — the ONE ordering-sensitive port. Only village (`beard_thin`) and stronghold (`bury`) declare `terrain_adaptation`; temples/igloo/mineshaft/swamp-hut are `NONE` and stay byte-identical. Port `Beardifier.forStructuresInChunk` + `compute` + `getBuryContribution`/`getBeardContribution`, then resolve THE HAZARD: the beard contributes to `final_density` at FILL time, but Sulfur places structures at Decorate (post-fill). So the structure STARTS for the chunk (+ its ±12 beard window) must be computed BEFORE `fillFromNoise`, and the additive beard term threaded into the noisechunk fill loop (the `BeardifierMarker` substitution vanilla does in the NoiseChunk ctor).

Purpose: villages sit ON terrain, strongholds bury (no floating/clipping). This is the last wave because it touches the generator fill ordering. It depends on 20-03 (starts may be persisted; computing them pre-fill must read the same cache). It does NOT touch worker.go/tick.go (disjoint from 20-04) — only the generator (noisegen.go) + the fill (noisechunk/fill.go) + a new beard.go.

Output: the 2 adapting structures fit terrain; the 4 non-adapting ones are unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-RESEARCH.md
@.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-03-SUMMARY.md
@CLAUDE.md

<read_first>
- temp/cache/26.2-inner.jar (decompile before writing — this is the ordering-sensitive port):
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.Beardifier` (forStructuresInChunk / compute / getBuryContribution / getBeardContribution / computeBeardContribution / the Rigid record / the affectedBox.inflatedBy(24))
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.DensityFunctions$BeardifierMarker` (the marker the NoiseChunk replaces per-chunk)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.util.Mth` (clampedMap, length)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.TerrainAdjustment` (NONE/BURY/BEARD_THIN/BEARD_BOX/ENCAPSULATE — which apply)
  - `javap -c -p -classpath temp/cache/26.2-inner.jar net.minecraft.world.level.levelgen.structure.Structure` (afterPlace — the BURY block-fill fixup, distinct from the beard)
- world/noisegen.go:330-401 (Decorate's structure seam: placeStructures runs STARTS+REFERENCES+PLACE at the END of Decorate, AFTER fill); world/noisegen.go GenerateTerrain (where fillFromNoise runs — the beard must apply HERE, pre-Decorate)
- world/levelgen/noisechunk/fill.go + noisechunk.go + interpolator.go — the fill loop + final_density summation (09-09 mapAll: the beard is a NON-interpolated additive term, NOT a per-marker interpolator — A5; verify the placement)
- world/structure/cache.go — ComputeStarts (pure (seed,pos), singleflight-deduped — computing earlier is cheap + order-independent)
- temp/cache/26.2-datagen/.../worldgen/structure/{village_plains,stronghold}.json — confirm terrain_adaptation values (only these 2 apply)
</read_first>

<interfaces>
From world/structure/cache.go (compute starts earlier — pure, cheap, memoized):
  func (c *Cache) ComputeStarts(seed int64, pos level.ChunkPos, gen StartGenerator) []*StructureStart
  // Computing for C + its ±12 window before fill is order-independent (singleflight-deduped).

From world/levelgen/noisechunk (the fill seam — find the exact final_density summation site):
  // fill.go drives the cell loop + per-block final_density; 09-09 mapAll substitutes per
  // interpolated marker. The beard is an ADDITIVE per-block term applied at the summation,
  // NOT a router node and NOT inside the interpolator (A5 — verify during read_first).

From world/structure (new):
  type Beardifier struct { rigids []Rigid; affectedBox BoundingBox }
  func ForStructuresInChunk(starts []*StructureStart, pos level.ChunkPos) *Beardifier  // EMPTY if none
  func (b *Beardifier) Compute(wx, wy, wz int) float64  // 0 outside affectedBox
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Beardifier — forStructuresInChunk + compute + bury/beard contributions</name>
  <files>world/structure/beard.go, world/structure/beard_test.go</files>
  <read_first>
    - javap Beardifier (forStructuresInChunk, compute, getBuryContribution, getBeardContribution, computeBeardContribution, the Rigid record, affectedBox.inflatedBy(24)) + Mth.clampedMap/length + TerrainAdjustment
    - world/structure/start.go (StructureStart + BoundingBox) + cache.go (starts source)
  </read_first>
  <behavior>
    - Test (TestBeardContribution): getBuryContribution(dx,dy,dz) = Mth.clampedMap(Mth.length(dx,dy,dz),0,6,1,0) — assert exact values at sample offsets
    - Test: getBeardContribution (BEARD_THIN) applies the kernel-range gate + the -dy/sqrt falloff — assert exact values
    - Test (TestBeardScope): forStructuresInChunk gathers ONLY starts with terrain_adaptation (village/stronghold); a chunk with only NONE-adaptation structures (temple/igloo) yields EMPTY (compute returns 0 everywhere)
    - Test: compute returns 0 outside affectedBox (no double-apply gate)
  </behavior>
  <action>
    Port Beardifier LITERALLY (research Subsystem 3, all decompiled):
    - forStructuresInChunk(starts, chunkPos): gather startsForStructure with hasTerrainAdaptation==true; for each start's pieces within 12 of the chunk, record a Rigid{box, groundLevelDelta}; affectedBox = union.inflatedBy(24); EMPTY -> compute returns 0.
    - compute(ctx): if outside affectedBox -> 0; else sum over pieces of getBuryContribution (BURY/ENCAPSULATE = Mth.clampedMap(Mth.length(dx,dy,dz),0,6,1,0)) OR getBeardContribution (BEARD_THIN/BEARD_BOX kernel-range gate + magnitude = -dy'/sqrt(lengthSquared+...) falloff). Cite each javap method.
    - Map TerrainAdjustment: only BURY (stronghold) + BEARD_THIN (village) are reachable here; NONE -> not gathered. Read terrain_adaptation from the structure def (village_plains=beard_thin, stronghold=bury).
    Pure function over (starts, pos) — no chunk state, order-independent.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/structure/ && go vet ./world/structure/ && go test ./world/structure/ -run 'TestBeardContribution|TestBeardScope'</automated>
  </verify>
  <done>getBuryContribution/getBeardContribution match the jar at sample offsets; only village+stronghold gather; compute is 0 outside the box; tests green; CGO=0 build clean. Never gopls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute starts pre-fill + thread the additive beard term into the noisechunk fill</name>
  <files>world/noisegen.go, world/levelgen/noisechunk/fill.go</files>
  <read_first>
    - world/noisegen.go GenerateTerrain (the fillFromNoise call) + placeStructures (the current STARTS-at-Decorate seam) — STARTS must move EARLIER
    - world/levelgen/noisechunk/fill.go + noisechunk.go (the final_density summation site; 09-09 mapAll — the beard is additive, NOT interpolated, A5)
    - world/structure/cache.go ComputeStarts (pure, cheap to call earlier)
  </read_first>
  <behavior>
    - Test (TestVillageBeardRaises): a chunk under a village (beard_thin) start has RAISED terrain density vs the same chunk with no start (village no longer floats)
    - Test (TestStrongholdBuries): a chunk under a stronghold (bury) start has terrain filled to bury it
    - Test (TestNonAdaptingUnchanged): a chunk with a temple/igloo/mineshaft (NONE) start is BYTE-IDENTICAL to Phase 16 (no beard term applied) — the regression guard
  </behavior>
  <action>
    Resolve the ordering hazard (Pitfall 3):
    1. In GenerateTerrain (BEFORE NewNoiseChunk/fillFromNoise), compute the structure STARTS for C + its ±12 window via g.structCache.ComputeStarts (pure geometry, singleflight-deduped, order-independent — computing earlier is free; reads the same cache 20-03 may have seeded from region). Build a structure.Beardifier (forStructuresInChunk) for C.
    2. Thread the Beardifier into noisechunk; in fill.go, ADD beardifier.Compute(wx,wy,wz) to final_density PER BLOCK at the summation site (the BeardifierMarker substitution vanilla does in the NoiseChunk ctor) — NOT inside the per-marker interpolator (A5: it is a non-interpolated additive term; confirm placement against 09-09 mapAll). When the Beardifier is EMPTY (no adapting structure), Compute returns 0 -> the fill is byte-identical (the NONE regression guard).
    3. The afterPlace BURY block-fill fixup (Structure.afterPlace) runs in the PLACE pass alongside PlaceStructures (noisegen.go placeStructures / Decorate) — port it there (distinct from the beard density term). Cite javap.
    Keep GenerateTerrain PURE (same seed+pos -> identical bytes). Do NOT modify the density graph (09-09 forbids); the beard is additive at the fill summation only.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./world/... && go vet ./world/ ./world/levelgen/noisechunk/ && go test ./world/... -run 'TestVillageBeardRaises|TestStrongholdBuries|TestNonAdaptingUnchanged'</automated>
  </verify>
  <done>Starts compute pre-fill; the beard term raises village / buries stronghold terrain; NONE-adaptation chunks are byte-identical to Phase 16; afterPlace BURY fill runs in PLACE; GenerateTerrain stays pure; CGO=0 build clean. Never gopls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-fill start computation -> fill | Computing starts earlier must not change the pure (seed,pos) determinism or trigger neighbor gen |
| beard term -> final_density | The additive term must apply ONLY for adapting structures (NONE stays byte-identical) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | T (Tampering) | fill.go final_density | mitigate | The NONE regression test (TestNonAdaptingUnchanged) gates byte-identity for non-adapting structures — a stray beard term fails CI |
| T-20-14 | (correctness) | beard double-apply | mitigate | compute gated on affectedBox.isInside; forStructuresInChunk scopes per-chunk (Pitfall 3) |
| T-20-15 | D (DoS) | pre-fill ±12 start window | accept | The window is bounded (±12 chunks); ComputeStarts is pure + singleflight-deduped + memoized — no neighbor chunk generation triggered |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./world/...` clean (NEVER gopls).
- `go vet ./world/ ./world/structure/ ./world/levelgen/noisechunk/` clean.
- `go test ./world/structure/ -run 'TestBeardContribution|TestBeardScope'` + `go test ./world/... -run 'TestVillageBeardRaises|TestStrongholdBuries|TestNonAdaptingUnchanged'` green.
- The NONE regression guard (TestNonAdaptingUnchanged) is the critical gate — non-adapting structures must stay byte-identical to Phase 16.
- Docker `-race` over ./world/ green (GenerateTerrain stays pure; pre-fill start computation reads the memoized cache).
- The Phase-13/16 worldgen capture-diff/determinism goldens stay green for non-adapting terrain (beard changes ONLY village+stronghold terrain).
</verification>

<success_criteria>
- Village raises terrain (beard_thin); stronghold buries (bury); the 4 NONE structures are unchanged.
- Starts compute pre-fill; the beard is an additive per-block fill term (not a router node / interpolator).
- No double-apply (affectedBox gate). GenerateTerrain stays pure + byte-identical for non-adapting chunks.
</success_criteria>

<output>
After completion, create `.planning/phases/20-structure-polish-loot-inhabitants-beard-persistence/20-05-SUMMARY.md`.
Stage files BY NAME. Commit message: `feat(20): Beardifier terrain adaptation (village beard_thin + stronghold bury) (STRUCT-POLISH-03)` — NO Co-Authored-By / NO Claude attribution. Push: development.
Record in the SUMMARY: the exact fill.go summation site where the beard term was added, the pre-fill start-computation placement in GenerateTerrain, and confirmation the NONE-adaptation byte-identity held (the regression guard).

NOTE FOR PHASE CLOSE: this is the FINAL v3 phase. After 20-05, the phase (and v3) likely closes with an autonomous:false real-client visual gate (open a structure chest with vanilla loot, see villagers/witch/cat, structures fit terrain). Flag this to the orchestrator for a closing human-verify checkpoint.
</output>
